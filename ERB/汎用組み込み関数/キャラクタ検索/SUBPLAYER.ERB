;========================================
;@SUBPLAYER MASTERとASSIのうちPLAYERでないほうを返す
;ただし、性質上、ASSI == -1(ASSI不在)と一致を取れてしまうと色々面倒なことになるので
;ASSIが存在しえない状況だと-2を返させる
;========================================
@SUBPLAYER
#FUNCTION
SIF ASSI < 0
	RETURNF -2
SIF ASSIPLAY
	RETURNF MASTER
RETURNF ASSI