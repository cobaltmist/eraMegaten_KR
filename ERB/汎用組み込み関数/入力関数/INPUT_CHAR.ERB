;-------------------------------------------------
;文字임프ット関数@INPUT_CHAR
;引数ARGSに含まれる文字が입력されるまで文字の입력を待ちます
;有効な文字が입력されたらそれをRESULTSへ격납します
;全角文字には対応していません
;-------------------------------------------------
;ARG 値がゼロでない場合、리턴キー("")を有効な입력とみなします。
@INPUT_CHAR(ARGS,ARG = 0)
#LOCALSIZE 1
ONEINPUTS
SIF ARG != 0 && RESULTS == ""
	RETURN
FOR LOCAL,0,STRLENS(ARGS),1
	SIF RESULTS == SUBSTRING(ARGS,LOCAL,1)
		RETURN
NEXT
SIF RESULTS == ""
	RESTART
CLEARLINE 1
REUSELASTLINE 
RESTART