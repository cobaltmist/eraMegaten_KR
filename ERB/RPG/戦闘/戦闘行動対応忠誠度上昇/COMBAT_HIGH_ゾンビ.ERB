;===============================
;전투行動に応じて소질ごとの충성도上昇量を산출
;ARG      대상キャラクタ
;ARG:1    입력행동
;RESULT   충성도上昇量
;===============================
;[좀비]はＨＰ・ＭＰ・ＭＡＧの残量に応じて충성도が上昇
@COMBAT_HIGH_좀비(ARG,ARG:1)
LOCAL = BASE:ARG:ＨＰ == 0    ? 0     # BASE:ARG:ＨＰ * 100 / MAXBASE:ARG:ＨＰ
LOCAL = MAXBASE:ARG:ＭＰ == 0 ? LOCAL # MIN(LOCAL,BASE:ARG:ＭＰ * 100 / MAXBASE:ARG:ＭＰ)
LOCAL = MAXBASE:ARG:CP == 0   ? LOCAL # MIN(LOCAL,BASE:ARG:ＭＡＧ * 100 / MAXBASE:ARG:ＭＡＧ)
RETURN SQRT(LOCAL)-4