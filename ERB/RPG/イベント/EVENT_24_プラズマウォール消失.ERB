;フラグ
;이벤트플래그:24:0
;====================
;조건
;====================
@PLAYABLE_EVENT_24
;地下鉄をクリアしてない
A = 던전플래그:9:2 + 5
IF A < DAY && 던전플래그:9:1 == 0 && 이벤트플래그:24:0 == 0
	RESULTS = 플라즈마 월 소실
	RETURN 1
ENDIF

;====================
;内容
;====================
@SHOPCOM_EVENT_24
PRINTW 팬텀에서 연락이 들어왔다.
PRINTW 미카게 쵸를 덮고 있던 수수께끼의 빛의 벽이 소멸됐다고 한다.
PRINTW 누가 어떻게 했는 지는 모르겠지만, 사건은 해결된 모양이다.
PRINTW 팬텀의 서머너가 해결한 것은 아닌 모양으로, 사건의 추적 조사 등의 요청이 나온 듯하지만
PRINTW 수고와 시간만 낭비하고 실익은 적다.
PRINTW 관여할 필요는 없을 것이다‥‥
PRINTW -여신이문록 페르소나 루트가 소멸되었습니다-
이벤트플래그:24:0 = 1
