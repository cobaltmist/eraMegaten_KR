;통상공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_1608,ARG
IF SKILL_CHANGE(ARG, [[キャラ:니토리]])
	RESULTS = 수부｢워터 카펫｣
ELSE
	RESULTS = 피어트렌트
ENDIF

;　명중
@SKILL_HITRATE_1608, ARG
RETURN 95

;　위력
@SKILL_POWER_1608, ARG
RETURN 290

;　최저공격횟수
@SKILL_MINATTACKNUMBER_1608,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_1608,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_1608, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_1608,ARG
RETURN 24

;　상성
@SKILL_TYPE_1608,ARG
RETURN 13
;빙결

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_1608, ARG
RETURN 1
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_1608, ARG
IF (U > 0 && U < CHARANUM && NO:U == [[キャラ:니토리]]) || ((FLAG:행동순1) > 0 && NO:(FLAG:행동순1) == [[キャラ:니토리]])
	RETURN 1
ELSE
	RETURN 2
ENDIF


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_1608
RETURN 1

;　사거리
@SKILL_RANGE_1608,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_1608,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_1608
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_1608,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_1608
RETURN 16
;빙결

;속도보정
@SKILL_SPEED_1608, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_1608,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_1608
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_1608
RETURN 10

@SKILL_RANKUP_1608
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_1608,ARG
IF SKILL_CHANGE(ARG, [[キャラ:니토리]])
	RETURN 3
ELSE
	RETURN 5
ENDIF

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_1608,ARG
IF SKILL_CHANGE(ARG, [[キャラ:니토리]])
	RETURN 9
ELSE
	RETURN 13
ENDIF

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_1608,ARG
RETURN 15

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_1608,ARG
RETURN 30

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_1608,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,1608






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_1608
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:니토리]]
	PRINTL 거센 물살로 적 전체를 밀고 수격 상성의 물리 대 데미지＋낮은 확률로 혼란。
	PRINTL 요귀　카와시로 니토리의 스펠카드。
ELSE
	PRINTL [후열] 적 전체에 수격속성 대 마법데미지. (낮은 확률로 CLOSE)
ENDIF

;계승에 필요한 부위
@계승부위_1608,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_1608,ARG
RETURN 1
;とくにない

