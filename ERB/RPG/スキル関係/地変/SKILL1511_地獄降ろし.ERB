
;스킬名
@SKILL_NAME_1511,ARG
IF SKILL_CHANGE(ARG, [[キャラ:스이카]])
	RESULTS = 췌부「토가쿠시산 던지기」
ELSE
	RESULTS = 지옥떨구기
ENDIF

;　명중
@SKILL_HITRATE_1511, ARG
RETURN 100

;　위력
@SKILL_POWER_1511, ARG
SIF FLAG:확산플래그
	RETURN 130
RETURN 210

;　최저공격횟수
@SKILL_MINATTACKNUMBER_1511,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_1511,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_1511, ARG
SIF FLAG:확산플래그
	RETURN 1
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_1511,ARG
RETURN 10

;　相性
@SKILL_TYPE_1511,ARG
RETURN 12
;지변

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_1511, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_1511, ARG
IF (U > 0 && U < CHARANUM && NO:U == [[キャラ:스이카]]) || ((FLAG:행동순1) > 0 && NO:(FLAG:행동순1) == [[キャラ:스이카]])
	RETURN 1
ELSE
	RETURN 2
ENDIF


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_1511
RETURN 1

;　사거리
@SKILL_RANGE_1511,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_1511,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_1511
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_1511,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_1511
RETURN 15
;지변

;속도보정
@SKILL_SPEED_1511, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_1511,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_1511
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_1511
RETURN 3

@SKILL_RANKUP_1511
;극락떨구기
D:1 = 1512
RETURN 1

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_1511,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_1511,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_1511,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_1511,ARG
RETURN 0

;拡散공격用추가処理
@SKILL_SPREAD_1511,ARG
RETURN 1
;拡散距離:1

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_1511,ARG,ARG:1
CALL ATTACK_SPREAD,ARG,ARG:1,1511



;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_1511
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:스이카]]
	PRINTL 거대한 암괴를 던져서、대상과 그 주변에 지변속성 물리데미지。
	PRINTL 요귀　이부키 스이카의 스펠카드。
ELSE
	PRINTL [후열] 적 1체와 주변의 적에게 지변속성 중 마법데미지.
ENDIF

;継承に필요한部位
@継承部位_1511,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_1511,ARG
RETURN 1
;とくにない




