;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_4004,ARG
RESULTS = 더블어택

;　명중
@SKILL_HITRATE_4004, ARG = -1
RETURN 150

;　위력
@SKILL_POWER_4004, ARG = -1
RETURN 150

;　최저공격횟수
@SKILL_MINATTACKNUMBER_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_MINATTACKNUMBER_0,ARG
RETURN RESULT

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_MAXATTACKNUMBER_0,ARG
RETURN RESULT

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4004, ARG
RETURN 2
;소비なし＝1

;　コストの量
@SKILL_COST_4004,ARG
RETURN 5

;　相性
@SKILL_TYPE_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_TYPE_0,ARG
RETURN RESULT

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_4004, ARG
RETURN 0
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_4004, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_4004
RETURN 1

;　사거리
@SKILL_RANGE_4004,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_4004,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_4004
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4004,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_4004
RETURN 0
;継承不可

;속도보정
@SKILL_SPEED_4004, ARG = -1
RETURN 25

;　구상の표시とその타입
@SKILL_MESSAGE_4004,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_4004
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_4004
RETURN 0
;変化不可

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_ADDTIONAL_STATE_0,ARG
RETURN RESULT

;추가효과상성
@SKILL_ADDTIONAL_TYPE_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_ADDTIONAL_TYPE_0,ARG
RETURN RESULT

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_ADDTIONAL_BASE_RATE_0,ARG
RETURN RESULT

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_4004,ARG
SIF ARG < 0
	RETURN 0
CALL SKILL_ADDTIONAL_MAX_RATE_0,ARG
RETURN RESULT
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_4004,ARG,ARG:1
LOCAL = POS(ARG:1)
LOCAL:1 = CFLAG:ARG:링케이지참가자1
CALL ATTACK_SINGLE,ARG,LOCAL,4004,0
;対象がまだ居るand死んでいない
IF POS(ARG:1) >= 0 && GET_STATE(CFLAG:POS(ARG:1):ステート) != "DYING"
	CALL ATTACK_SINGLE,LOCAL:1,LOCAL,4004,0
;居ないならランダムターゲット
ELSE
	CALL RANDOM_TARGET, LOCAL:1, 4004
	SIF POS(CFLAG:(LOCAL:1):ターゲット) > 0
		CALL ATTACK_SINGLE,LOCAL:1,POS(CFLAG:(LOCAL:1):ターゲット),4004,0
ENDIF



;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_4004
PRINTL 둘이서 콤비네이션 공격을 한다

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4004,ARG
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4004
RETURN 1

@참가人数_4004
RETURN 1

@참가조건표시_4004,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM %CALLNAME:MASTER%
	CASE 2
		PRINTFORM 【맹우】
ENDSELECT

@発動者조건_4004 , ARG
;아기を使えること
SIF TALENT:ARG:맹우
	RETURN 1
RETURN 0


@참가者조건_4004, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4004, ARG:1
SIF RESULT == 0
	RETURN 0
SELECTCASE ARG:1
	CASE 1
		SIF ARG == MASTER
			RETURN 1
		RETURN 0
ENDSELECT

@링케이지コスト타입_4004, ARG
SELECTCASE ARG
	CASE 1
		RETURN 2
ENDSELECT
@링케이지コスト_4004, ARG
SELECTCASE ARG
	CASE 1
		RETURN 5
ENDSELECT
