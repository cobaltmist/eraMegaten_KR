

;스킬명
@SKILL_NAME_4857,ARG
RESULTS = 일제사격

;　명중
@SKILL_HITRATE_4857, ARG
RETURN 300

;　위력
@SKILL_POWER_4857, ARG
SIF !CHARANUM_CHECK(ARG) || !CHARANUM_CHECK(FLAG:행동순1)
	RETURN 390
RETURN FLAG:확산플래그

;　최저공격횟수
@SKILL_MINATTACKNUMBER_4857,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4857,ARG
RETURN 1


;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_4857, ARG, ARG:1
RETURN 2
;소비없음＝1

;　코스트량
@SKILL_COST_4857, ARG
RETURN 5

;　상성
@SKILL_TYPE_4857,ARG
RETURN GET_TYPE_NUM("비구")
;　1 EXTRA　2 MAGIC

@SKILL_DECIDE_TYPE_4857, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_4857, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_4857
RETURN 1

;　사거리
@SKILL_RANGE_4857,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_4857,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_4857
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_4857,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_4857
RETURN 23
;

;속도보정
@SKILL_SPEED_4857, ARG = -1
RETURN 20

;　구상의 표시와 그 타입
@SKILL_MESSAGE_4857,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_4857
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_4857
RETURN 0

@SKILL_RANKUP_4857
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_4857,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_4857,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_4857,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_4857,ARG
RETURN 0
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_4857,ARG,ARG:1
#DIM L_CHARA,3
#DIM L_DAMAGE,3
#DIM L_POWER,3
#DIM L_COUNT
#LOCALSIZE 3

L_CHARA = ARG, CFLAG:ARG:링케이지참가자1, CFLAG:ARG:링케이지참가자2
FOR L_COUNT,0,3
	LOCAL = L_CHARA:L_COUNT
	CALL BTL_CALC_BASE_DAMAGE(LOCAL, POS(ARG:1), 100, "총")
	L_DAMAGE:L_COUNT = RESULT
	L_POWER:L_COUNT = 100 * L_DAMAGE:L_COUNT / L_DAMAGE:0

	CALLFORM 최저공격횟수_{EQUIP:LOCAL:총},LOCAL
	LOCAL:1 = RESULT
	CALLFORM 최대공격횟수_{EQUIP:LOCAL:총},LOCAL
	LOCAL:2 = RESULT
	L_POWER:L_COUNT = L_POWER:L_COUNT * MAX((LOCAL:1 + LOCAL:2) / 2,1)
	
	RESULT = 0
;	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:LOCAL:총},LOCAL
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:LOCAL:총}
	IF RESULT == 0
		CALLFORM 공격범위_{EQUIP:LOCAL:총},LOCAL
		IF RESULT > 2
			TIMES L_POWER:L_COUNT , 1.50
		ELSEIF RESULT == 2
			TIMES L_POWER:L_COUNT , 1.25
		ENDIF
	ENDIF
NEXT

FLAG:확산플래그 = (L_POWER:0 + L_POWER:1 + L_POWER:2) * 130 / 100

CALL GUN_SINGLE,ARG,POS(ARG:1),4855

FLAG:확산플래그 = 0

;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_4857
PRINTL 3인의 총의 달인이 적 1체에 일제사격한다
PRINTL 위력은 총의 공격횟수와 범위에 따라 달라진다

;계승에 필요한 부위
@계승부위_4857,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4857,ARG
CALL CHECK_COST,ARG,4857
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4857
RETURN 1

@참가인수_4857
RETURN 2

@참가조건표시_4857,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM 건슬링거
	CASE 2,3
		PRINTFORM 건슬링거
ENDSELECT

@발동자조건_4857 , ARG
SIF EQUIP:ARG:총 == 0
	RETURN 0
SIF CFLAG:ARG:악마변신
	RETURN 0
SIF !TALENT:ARG:건슬링거
	RETURN 0
RETURN 1


@참가자조건_4857, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4857, ARG:1
SIF RESULT == 0
	RETURN 0
SIF EQUIP:ARG:총 == 0
	RETURN 0
SIF CFLAG:ARG:악마변신
	RETURN 0
SIF !TALENT:ARG:건슬링거
	RETURN 0
RETURN 1

@링케이지코스트타입_4857, ARG
SELECTCASE ARG
	CASE 1,2
		RETURN 2
ENDSELECT
@링케이지코스트_4857, ARG
SELECTCASE ARG
	CASE 1,2
		RETURN 5
ENDSELECT
