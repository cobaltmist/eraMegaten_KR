

;스킬名
@SKILL_NAME_4852,ARG
RESULTS = 주령마법탄

;　명중
@SKILL_HITRATE_4852, ARG
RETURN 100

;　위력
@SKILL_POWER_4852, ARG
SIF ARG == -1 || CFLAG:ARG:단독링케이지
	RETURN 90
RETURN (90 + MAXBASE:(CFLAG:ARG:링케이지참가자1):마력/2 + MAXBASE:(CFLAG:ARG:링케이지참가자1):지혜 / 4)

;　최저공격횟수
;머신건というか랜덤타겟系は횟수が1.5倍になる
@SKILL_MINATTACKNUMBER_4852,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
IF RESULT == 1
	CALLFORM 최저공격횟수_{EQUIP:ARG:총}
	RETURN RESULT * 3 / 2
ELSE
	CALLFORM 최저공격횟수_{EQUIP:ARG:총}
	RETURN RESULT
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4852,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
IF RESULT == 1
	CALLFORM 최대공격횟수_{EQUIP:ARG:총}
	RETURN RESULT * 3 / 2
ELSE
	CALLFORM 최대공격횟수_{EQUIP:ARG:총}
	RETURN RESULT
ENDIF
;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4852, ARG, ARG:1
RETURN 2
;소비なし＝1

;　コストの量
@SKILL_COST_4852, ARG
RETURN 5

;　相性
@SKILL_TYPE_4852,ARG
RETURN GET_TYPE_NUM("주살")
;　1EXTRAか2MAGICか

@SKILL_DECIDE_TYPE_4852, ARG
RETURN 1

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_4852, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_4852
RETURN 1

;　사거리
@SKILL_RANGE_4852,ARG
CALLFORM 사거리_{EQUIP:ARG:총}
RETURN RESULT

;　범위　1１체　2１열　3전체　4　적아군전체
;　랜덤타겟でない총は범위が一단계分강화される
@SKILL_SPHERE_4852,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
IF RESULT == 1
	CALLFORM 공격범위_{EQUIP:ARG:총}
	RETURN RESULT
ELSE
	CALLFORM 공격범위_{EQUIP:ARG:총}
	RETURN LIMIT(RESULT + 1, 1, 3)
ENDIF
;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_4852
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4852,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_4852
RETURN 23
;성

;속도보정
@SKILL_SPEED_4852, ARG = -1
RETURN 20

;　구상の표시とその타입
@SKILL_MESSAGE_4852,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_4852
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_4852
RETURN 0

@SKILL_RANKUP_4852
RETURN 0

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_4852,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_4852,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_4852,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_4852,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_4852,ARG,ARG:1
CALL SKILL_SPHERE_4852,ARG
IF RESULT == 1
	LOCAL = POS(ARG:1)
	CALL GUN_SINGLE,ARG,LOCAL,4852
ELSE
	RESULT = 0
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
	IF RESULT == 1
		CALL GUN_RAND,ARG,ARG:1,4852
	ELSE
		CALL GUN_FIELD,ARG,ARG:1,4852
	ENDIF
	
ENDIF
SIF CFLAG:ARG:PT플래그 > 0
	ITEM:5504 -= 1





;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_4852
PRINTL 사악한 힘을 담은 탄환으로、적을 꿰뚫는다

;継承に필요한部位
@継承部位_4852,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4852,ARG
CALL CHECK_COST,ARG,4852
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4852
RETURN 1

@참가人数_4852
RETURN 1

@참가조건표시_4852,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM 메기도
	CASE 2
		PRINTFORM 저주의탄환
ENDSELECT

@発動者조건_4852 , ARG
SIF EQUIP:ARG:총 == 0
	RETURN 0
SIF ITEM:5504 < 1
	RETURN 0
TRYCALLFORM 특수탄창_{EQUIP:ARG:총}
FOR LOCAL, 1, RESULT + 1
	LOCALS = 특수탄{LOCAL}
	SIF EQUIP:ARG:LOCALS == 5504
		RETURN 1
NEXT
RETURN 0


@참가者조건_4852, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4852, ARG:1
SIF RESULT == 0
	RETURN 0
SELECTCASE ARG:1
	CASE 1
		;메기도を使えること
		IF HAVE_SKILL(ARG, [[스킬:메기도]])
			CALL CHECK_ACTIONABLE , ARG , [[스킬:메기도]]
			SIF RESULT
				RETURN 1
		ENDIF
		RETURN 0
ENDSELECT

@링케이지コスト타입_4852, ARG
SELECTCASE ARG
	CASE 1
		RETURN 3
ENDSELECT
@링케이지コスト_4852, ARG
SELECTCASE ARG
	CASE 1
		RETURN 4
ENDSELECT
