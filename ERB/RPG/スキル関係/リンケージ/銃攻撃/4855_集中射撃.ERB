

;스킬名
@SKILL_NAME_4855,ARG
RESULTS = 집중사격

;　명중
@SKILL_HITRATE_4855, ARG
RETURN 300

;　위력
@SKILL_POWER_4855, ARG
SIF !CHARANUM_CHECK(ARG) || !CHARANUM_CHECK(FLAG:行動順1)
	RETURN 230
RETURN FLAG:확산플래그

;　최저공격횟수
@SKILL_MINATTACKNUMBER_4855,ARG
RETURN 1



;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4855,ARG
RETURN 1


;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4855, ARG, ARG:1
RETURN 2
;소비なし＝1

;　コストの量
@SKILL_COST_4855, ARG
RETURN 5

;　相性
@SKILL_TYPE_4855,ARG
RETURN GET_TYPE_NUM("비구")
;　1EXTRAか2MAGICか

@SKILL_DECIDE_TYPE_4855,ARG
RETURN 1

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_4855, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_4855
RETURN 1

;　사거리
@SKILL_RANGE_4855,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_4855,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_4855
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4855,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_4855
RETURN 23
;

;속도보정
@SKILL_SPEED_4855, ARG = -1
RETURN 20

;　구상の표시とその타입
@SKILL_MESSAGE_4855,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_4855
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_4855
RETURN 0

@SKILL_RANKUP_4855
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_4855,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_4855,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_4855,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_4855,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_4855,ARG,ARG:1
#DIM L_CHARA,2
#DIM L_DAMAGE,2
#DIM L_POWER,2
#DIM L_COUNT
#LOCALSIZE 3

L_CHARA = ARG, CFLAG:ARG:링케이지참가자1
FOR L_COUNT,0,2
	LOCAL = L_CHARA:L_COUNT
	CALL BTL_CALC_BASE_DAMAGE(LOCAL, POS(ARG:1), 100, "총")
	L_DAMAGE:L_COUNT = RESULT
	L_POWER:L_COUNT = 100 * L_DAMAGE:L_COUNT / L_DAMAGE:0
	
	CALLFORM 최저공격횟수_{EQUIP:LOCAL:총},LOCAL
	LOCAL:1 = RESULT
	CALLFORM 최대공격횟수_{EQUIP:LOCAL:총},LOCAL
	LOCAL:2 = RESULT
	L_POWER:L_COUNT = L_POWER:L_COUNT * MAX(LOCAL:1 + LOCAL:2,2) / 2

	RESULT = 0
;	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:LOCAL:총},LOCAL
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:LOCAL:총}
	IF RESULT == 0
		CALLFORM 공격범위_{EQUIP:LOCAL:총},LOCAL
		IF RESULT > 2
			TIMES L_POWER:L_COUNT , 1.50
		ELSEIF RESULT == 2
			TIMES L_POWER:L_COUNT , 1.25
		ENDIF
	ENDIF
NEXT

FLAG:확산플래그 = (L_POWER:0 + L_POWER:1) * 115 / 100

CALL GUN_SINGLE,ARG,POS(ARG:1),4855

FLAG:확산플래그 = 0

;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_4855
PRINTL 두 명의 총의 달인이 적 1체에 집중 사격한다
PRINTL 위력은 총의 공격 횟수와 범위에 따라 달라진다

;継承に필요な部位
@継承部位_4855,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4855,ARG
CALL CHECK_COST,ARG,4855
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4855
RETURN 1

@참가人数_4855
RETURN 1

@참가조건표시_4855,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM 건슬링거
	CASE 2
		PRINTFORM 건슬링거
ENDSELECT

@発動者조건_4855 , ARG
SIF EQUIP:ARG:총 == 0
	RETURN 0
SIF CFLAG:ARG:악마변신
	RETURN 0
SIF !TALENT:ARG:건슬링거
	RETURN 0
RETURN 1


@참가者조건_4855, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4855, ARG:1
SIF RESULT == 0
	RETURN 0
SIF EQUIP:ARG:총 == 0
	RETURN 0
SIF CFLAG:ARG:악마변신
	RETURN 0
SIF !TALENT:ARG:건슬링거
	RETURN 0
RETURN 1

@링케이지コスト타입_4855, ARG
SELECTCASE ARG
	CASE 1
		RETURN 2
ENDSELECT
@링케이지コスト_4855, ARG
SELECTCASE ARG
	CASE 1
		RETURN 5
ENDSELECT
