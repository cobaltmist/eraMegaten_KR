

;스킬명
@SKILL_NAME_4854,ARG
RESULTS = 주신마법탄

;　명중
@SKILL_HITRATE_4854, ARG
RETURN 100

;　위력
@SKILL_POWER_4854, ARG
SIF ARG == -1 || CFLAG:ARG:단독링케이지
	RETURN 180
RETURN (90 + MAXBASE:(CFLAG:ARG:링케이지참가자1):마력/2 + MAXBASE:(CFLAG:ARG:링케이지참가자1):지혜 / 4) * 2

;　최저공격횟수
;머신건というか랜덤타겟系は횟수が1.5倍になる
@SKILL_MINATTACKNUMBER_4854,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
IF RESULT == 1
	CALLFORM 최저공격횟수_{EQUIP:ARG:총}
	RETURN RESULT * 3 / 2
ELSE
	CALLFORM 최저공격횟수_{EQUIP:ARG:총}
	RETURN RESULT
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4854,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
IF RESULT == 1
	CALLFORM 최대공격횟수_{EQUIP:ARG:총}
	RETURN RESULT * 3 / 2
ELSE
	CALLFORM 최대공격횟수_{EQUIP:ARG:총}
	RETURN RESULT
ENDIF
;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_4854, ARG, ARG:1
RETURN 2
;소비없음＝1

;　코스트량
@SKILL_COST_4854, ARG
RETURN 10

;　상성
@SKILL_TYPE_4854,ARG
RETURN GET_TYPE_NUM("주살")
;　1 EXTRA　2 MAGIC

@SKILL_DECIDE_TYPE_4854, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_4854, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_4854
RETURN 1

;　사거리
@SKILL_RANGE_4854,ARG
CALLFORM 사거리_{EQUIP:ARG:총}
RETURN RESULT

;　범위　1 1체　2 1열　3 전체　4 적아군전체
;　랜덤타겟でない총は범위が一단계分강화される
@SKILL_SPHERE_4854,ARG
RESULT = 0
TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
IF RESULT == 1
	CALLFORM 공격범위_{EQUIP:ARG:총}
	RETURN RESULT
ELSE
	CALLFORM 공격범위_{EQUIP:ARG:총}
	RETURN LIMIT(RESULT + 1, 1, 3)
ENDIF
;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_4854
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_4854,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_4854
RETURN 23
;화염

;속도보정
@SKILL_SPEED_4854, ARG = -1
RETURN 20

;　구상의 표시와 그 타입
@SKILL_MESSAGE_4854,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_4854
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_4854
RETURN 0

@SKILL_RANKUP_4854
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_4854,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_4854,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_4854,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_4854,ARG
RETURN 0
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_4854,ARG,ARG:1
CALL SKILL_SPHERE_4854,ARG
IF RESULT == 1
	LOCAL = POS(ARG:1)
	CALL GUN_SINGLE,ARG,LOCAL,4854
ELSE
	RESULT = 0
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
	IF RESULT == 1
		CALL GUN_RAND,ARG,ARG:1,4854
	ELSE
		CALL GUN_FIELD,ARG,ARG:1,4854
	ENDIF
	
ENDIF
SIF CFLAG:ARG:PT플래그 > 0
	ITEM:5504 -= 4





;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_4854
PRINTL 사악한 힘을 담은 탄환으로、적을 꿰뚫는다。

;계승에 필요한 부위
@계승부위_4854,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4854,ARG
CALL CHECK_COST,ARG,4854
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4854
RETURN 1

@참가인수_4854
RETURN 1

@참가조건표시_4854,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM 메기도라온
	CASE 2
		PRINTFORM 저주의탄환 * 4
ENDSELECT

@발동자조건_4854 , ARG
SIF EQUIP:ARG:총 == 0
	RETURN 0
SIF ITEM:5504 < 4
	RETURN 0
TRYCALLFORM 특수탄창_{EQUIP:ARG:총}
FOR LOCAL, 1, RESULT + 1
	LOCALS = 특수탄{LOCAL}
	SIF EQUIP:ARG:LOCALS == 5504
		RETURN 1
NEXT
RETURN 0


@참가자조건_4854, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4854, ARG:1
SIF RESULT == 0
	RETURN 0
SELECTCASE ARG:1
	CASE 1
		;메기도라온を使えること
		IF HAVE_SKILL(ARG, [[스킬:메기도라온]])
			CALL CHECK_ACTIONABLE , ARG , [[스킬:메기도라온]]
			SIF RESULT
				RETURN 1
		ENDIF
		RETURN 0
ENDSELECT

@링케이지코스트타입_4854, ARG
SELECTCASE ARG
	CASE 1
		RETURN 3
ENDSELECT
@링케이지코스트_4854, ARG
SELECTCASE ARG
	CASE 1
		RETURN 12
ENDSELECT
