

;스킬名
@SKILL_NAME_4561,ARG
RESULTS = Ｌ마하무도온

;　명중
@SKILL_HITRATE_4561, ARG
RETURN 100

;　위력
@SKILL_POWER_4561, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_4561,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4561,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4561, ARG, ARG:1
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_4561,ARG , ARG:1
RETURN 16

;　相性
@SKILL_TYPE_4561,ARG
RETURN GET_TYPE_NUM("주살")
;　1EXTRAか2MAGICか

@SKILL_DECIDE_TYPE_4561, ARG
RETURN 1

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_4561, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_4561
RETURN 1

;　사거리
@SKILL_RANGE_4561,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군전체
@SKILL_SPHERE_4561,ARG
RETURN 2

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_4561
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4561,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_4561
RETURN 23
;화염

;속도보정
@SKILL_SPEED_4561, ARG = -1
RETURN 20

;　구상の표시とその타입
@SKILL_MESSAGE_4561,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_4561
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_4561
RETURN 0

@SKILL_RANKUP_4561
RETURN 0

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_4561,ARG
RETURN GET_STATE_NUM("DYING")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_4561,ARG
RETURN GET_TYPE_NUM("주살")

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_4561,ARG
RETURN 60

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_4561,ARG
RETURN 95
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_4561,ARG,ARG:1
CALL FIELD_BADSTATE,ARG,ARG:1,4561






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_4561
PRINTL 적１열을 높은 확률로 저주해 죽인다。

;継承に필요한部位
@継承部位_4561,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4561,ARG
CALL CHECK_COST,ARG,4561
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4561
RETURN 1

@참가人数_4561
RETURN 1

@참가조건표시_4561,ARG
SELECTCASE ARG
	CASE 1
		PRINTFORM 상태 이상 마법
	CASE 2
		PRINTFORM 무도온or마하무도온
ENDSELECT

@発動者조건_4561 , ARG
IF CHECK_SKILL(ARG, [[스킬:무도온]])
	CALL CHECK_ACTIONABLE , ARG , [[스킬:무도온]]
	SIF RESULT
		RETURN 1
ENDIF
IF CHECK_SKILL(ARG, [[스킬:마하무도온]])
	CALL CHECK_ACTIONABLE , ARG , [[스킬:마하무도온]]
	SIF RESULT
		RETURN 1
ENDIF
RETURN 0


@참가者조건_4561, ARG , ARG:1
#LOCALSIZE 2
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4561, ARG:1
SIF RESULT == 0
	RETURN 0
;상태변화마법스킬を使えること(DYINGは除く)
FOR LOCAL, 1, 42
	LOCAL = LOCAL == FLAG:스킬수 + 1 ? 21 # LOCAL
	LOCAL:1 = ABL:ARG:@"\@ LOCAL > 20 ? 장비 # \@스킬{LOCAL > 20 ? LOCAL - 20 # LOCAL}"
	SIF !LOCAL:1 && LOCAL > 20
		BREAK
	SIF !LOCAL:1
		CONTINUE
	CALLFORM SKILL_DECIDE_TYPE_{LOCAL:1}
	SIF RESULT != 2
		CONTINUE
	CALLFORM SKILL_EFECT_{LOCAL:1}
	SIF RESULT != 3
		CONTINUE
	CALLFORM SKILL_ADDTIONAL_STATE_{LOCAL:1}
	SIF RESULT == GET_STATE_NUM("DYING")
		CONTINUE
	CALL CHECK_ACTIONABLE , ARG , LOCAL:1
	SIF RESULT == 0
		CONTINUE
	RETURN 1
NEXT
RETURN 0
@링케이지コスト타입_4561, ARG
SELECTCASE ARG
	CASE 1
		RETURN 3
ENDSELECT
@링케이지コスト_4561, ARG
SELECTCASE ARG
	CASE 1
		RETURN 16
ENDSELECT
