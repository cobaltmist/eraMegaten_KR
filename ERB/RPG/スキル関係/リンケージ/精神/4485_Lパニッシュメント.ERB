;적 전체に정신相性のパニック상태を부여し、정신相性の即死を与える
;스킬名
@SKILL_NAME_4485,ARG
RESULTS = Ｌ퍼니시먼트

;　명중
@SKILL_HITRATE_4485, ARG
RETURN 100

;　위력
@SKILL_POWER_4485, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_4485,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_4485,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_4485, ARG, ARG:1
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_4485, ARG
RETURN 15

;　相性
@SKILL_TYPE_4485,ARG
RETURN GET_TYPE_NUM("정신")
;정신

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_4485, ARG
RETURN 1

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_4485, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_4485
RETURN 1

;　사거리
@SKILL_RANGE_4485,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_4485,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_4485
RETURN 3

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_4485,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_4485
RETURN 23
;継承

;속도보정
@SKILL_SPEED_4485, ARG = -1
RETURN 20

;　구상の표시とその타입
@SKILL_MESSAGE_4485,ARG

;전투時이외に사용できるか
@SKILL_ACTIONABLE_FIELD_4485
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_4485
RETURN 0

@SKILL_RANKUP_4485
RETURN 0

;テン플레이트用、추가효과시리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_4485,ARG
RETURN GET_STATE_NUM("PANIC")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_4485,ARG
RETURN GET_TYPE_NUM("정신")

;추가효과기본부여확률
;4482_L파이널누드同様に三人分の마법효과を발して판정する
@SKILL_ADDTIONAL_BASE_RATE_4485,ARG
RETURN 100 * (MAXBASE:ARG:마법효과 * 2 + MAXBASE:(CFLAG:ARG:링케이지참가자1):마법효과 + MAXBASE:(CFLAG:ARG:링케이지참가자2):마법효과) / (MAXBASE:ARG:마법효과 * 4)

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_4485,ARG
RETURN 95
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_4485,ARG,ARG:1
CALL FIELD_BADSTATE,ARG,ARG:1,4485
IF CFLAG:(ARG):PT플래그 == 0
	LOCAL:7 = 1
	LOCAL:8 = 7
ELSE
	LOCAL:7 = 7
	LOCAL:8 = 17
ENDIF

FOR LOCAL,LOCAL:7,LOCAL:8
	LOCALS = 포지션{LOCAL}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF CFLAG:(FLAG:LOCALS):ステート != GET_STATE_NUM("PANIC")
		CONTINUE
	PRINTFORM 　　TARGET:[{LOCAL,2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
	CALL ATTACK_BADSTATE,ARG,FLAG:LOCALS,GET_STATE_NUM("DYING"),GET_TYPE_NUM("정신"),200,95
NEXT

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_4485
PRINTL 적 전체를 혼란시키고、혼란(PANIC) 상태인 적을 처형한다。

;継承に필요한部位
@継承部位_4485,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_4485,ARG
CALL CHECK_COST,ARG,4485
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない


;=====================================================
;링케이지설정
;=====================================================
@LINKAGE_4485
RETURN 1

@참가人数_4485
RETURN 2

@참가조건표시_4485,ARG
SELECTCASE ARG
	CASE 1, 2
		PRINTFORM 텐타라후
	CASE 3
		PRINTFORM 퍼니시먼트
ENDSELECT

@発動者조건_4485 , ARG
;퍼니시먼트を使えること
SIF HAVE_SKILL(ARG, [[스킬:퍼니시먼트]]) == 0
	RETURN 0
CALL CHECK_ACTIONABLE , ARG , [[스킬:퍼니시먼트]]
	RETURN 1
RETURN 0


@참가者조건_4485, ARG , ARG:1
SIF CPOS(ARG) < 1 || CPOS(ARG) > 6
	RETURN 0
CALL CHECK_COST_LINCAGE, ARG, 4485, ARG:1
SIF RESULT == 0
	RETURN 0
SELECTCASE ARG:1
	CASE 1, 2
		IF HAVE_SKILL(ARG, [[스킬:텐타라후]])
			CALL CHECK_ACTIONABLE , ARG , [[스킬:텐타라후]]
			SIF RESULT
				RETURN 1
		ENDIF
		RETURN 0
ENDSELECT

@링케이지コスト타입_4485, ARG
SELECTCASE ARG
	CASE 1, 2
		RETURN 3
ENDSELECT
@링케이지コスト_4485, ARG
SELECTCASE ARG
	CASE 1, 2
		RETURN 15
ENDSELECT
