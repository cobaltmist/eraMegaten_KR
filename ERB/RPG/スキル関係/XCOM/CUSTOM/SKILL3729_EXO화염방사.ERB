;XCOM2 스파크비트 기본기 + EXO 아머 기술

;스킬명
@SKILL_NAME_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = EXO화염방사

;　명중
@SKILL_HITRATE_3729, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 200

;　위력
@SKILL_POWER_3729, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 70
IF BASE:ARG:LV >= 30
	RETURN 70
ELSEIF BASE:ARG:LV >= 25
	RETURN 60
ELSEIF BASE:ARG:LV >= 20
	RETURN 50
ELSEIF BASE:ARG:LV >= 15
	RETURN 40
ELSE
	RETURN 40
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;척탄병 훈련을 받았을 때.
IF CFLAG:(ARG:1):척탄병_훈련 == 1
	RETURN 2
;레벨 35에 EXO 아머와 척탄병 장비(전자기 캐넌)를 장비하고 있을때
ELSEIF EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708 && EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):총 == 2813 && BASE:(ARG:1):LV > 34
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3729,ARG
;척탄병 훈련을 받았을 때.
IF CFLAG:(ARG:1):척탄병_훈련 == 1
	RETURN 2
;레벨 35에 EXO 아머와 척탄병 장비(전자기 캐넌)를 장비하고 있을때
ELSEIF EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708 && EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):총 == 2813 && BASE:(ARG:1):LV > 34
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_3729, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;消費なし＝1

;　비용의 양
@SKILL_COST_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 18
IF BASE:ARG:LV >= 30
	RETURN 18
ELSEIF BASE:ARG:LV >= 25
	RETURN 15
ELSEIF BASE:ARG:LV >= 20
	RETURN 12
ELSEIF BASE:ARG:LV >= 15
	RETURN 9
ELSE
	RETURN 6
ENDIF

;　상성
@SKILL_TYPE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4
;화염

;스킬 치명타율
@SKILL_CRITICAL_RATE_3729, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_3729, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 폭발계 만능. 물리

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_3729, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_3729
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_3729
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3729
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 7
;화염

;계승불능
@継承不能_3729
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_3729, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3729
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_3729
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_STATE_NUM("FLAME")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("화염")

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 40

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 80

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3729,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,3729
;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_3729
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL E.X.O 아머 슈트나 SPARK 비트로 사용할 수 있는 화염 상성의 방사 공격.
PRINTL 적 1열에 화염을 방사해 화상을 입힌다.

;継承に必要な部位
@継承部位_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_3729,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

