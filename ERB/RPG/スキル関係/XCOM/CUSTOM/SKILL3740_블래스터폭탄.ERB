;XCOM2 WAR 슈트용 강화 중화기 공격
;블래스터 폭탄

;스킬명
@SKILL_NAME_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 블래스터폭탄

;　명중
@SKILL_HITRATE_3740, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9999

;　위력
@SKILL_POWER_3740, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 140
IF BASE:ARG:LV >= 40
	RETURN 140
ELSEIF BASE:ARG:LV >= 39
	RETURN 127
ELSEIF BASE:ARG:LV >= 38
	RETURN 104
ELSEIF BASE:ARG:LV >= 37
	RETURN 91
ELSEIF BASE:ARG:LV >= 36
	RETURN 88
ELSEIF BASE:ARG:LV >= 35
	RETURN 85
ELSEIF BASE:ARG:LV >= 34
	RETURN 82
ELSEIF BASE:ARG:LV >= 33
	RETURN 79
ELSEIF BASE:ARG:LV >= 32
	RETURN 76
ELSEIF BASE:ARG:LV >= 31
	RETURN 73
ELSEIF BASE:ARG:LV >= 30
	RETURN 70
ELSE
	RETURN 70
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;척탄병 훈련을 받았을 때.
IF CFLAG:(ARG:1):척탄병_훈련 == 1
	RETURN 2
;레벨 35에 WAR 슈트와 척탄병 장비(빔 캐넌)를 장비하고 있을때
ELSEIF EQUIP:(ARG:1):팔 == 4083 && EQUIP:(ARG:1):발 == 4711 && EQUIP:(ARG:1):몸통 == 3816 && EQUIP:(ARG:1):총 == 2825 && BASE:(ARG:1):LV > 34
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;척탄병 훈련을 받았을 때.
IF CFLAG:(ARG:1):척탄병_훈련 == 1
	RETURN 2
;레벨 35에 WAR 슈트와 척탄병 장비(빔 캐넌)를 장비하고 있을때
ELSEIF EQUIP:(ARG:1):팔 == 4083 && EQUIP:(ARG:1):발 == 4711 && EQUIP:(ARG:1):몸통 == 3816 && EQUIP:(ARG:1):총 == 2825 && BASE:(ARG:1):LV > 34
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_3740, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;消費なし＝1

;　비용의 양
@SKILL_COST_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG > -1
	RETURN MAX((MAXBASE:ARG:ＭＰ / 10),1)
RETURN 0

;　상성
@SKILL_TYPE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;스킬 치명타율
@SKILL_CRITICAL_RATE_3740, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_3740, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 폭발계 만능. 물리

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_3740, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_3740
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_3740
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3740
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 20
;만능

;계승불능
@継承不能_3740
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_3740, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 6

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3740
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_3740
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;확산 공격용 추가처리
@SKILL_SPREAD_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2
;확산거리:1

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3740,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
CALL ATTACK_SPREAD,ARG,ARG:1,3740
;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_3740
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL WAR 슈트나 강화형 비트로 사용 가능한 강력한 범위 추적탄 폭발 공격.
PRINTL 적 1체와 그 주변 넓은 범위에 만능 물리 상성의 강력한 폭발 피해를 입힌다.
PRINTL 전체 MP중 10분의 1을 소모한다. 빗나가지 않는다.

;継承に必要な部位
@継承部位_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_3740,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

