;XCOM2 WOTC 어쌔신 공격스킬

;스킬명
@SKILL_NAME_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 작별의비단

;　명중
@SKILL_HITRATE_3734, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 999999

;　위력
@SKILL_POWER_3734, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 140
IF BASE:ARG:LV >= 40
	RETURN 140
ELSEIF BASE:ARG:LV >= 35
	RETURN 110
ELSEIF BASE:ARG:LV >= 30
	RETURN 100
ELSEIF BASE:ARG:LV >= 20
	RETURN 80
ELSEIF BASE:ARG:LV >= 10
	RETURN 60
ELSE
	RETURN 60
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_3734, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;소모없음＝1

;　비용의 양
@SKILL_COST_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　상성
@SKILL_TYPE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_3734, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 근접계 특수기

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_3734, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_3734
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_3734
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3734
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;계승불능
@継承不能_3734
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도보정
@SKILL_SPEED_3734, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3734
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_3734
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_STATE_NUM("PANIC")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("정신")

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3734,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
A = 0
B = 0
C = 0
IF (MAXBASE:LOCAL:검격 < 100 || MAXBASE:LOCAL:검격 == 999) && CFLAG:LOCAL:검격가드킬 < 1 
		CFLAG:LOCAL:검격가드킬 = 1
		A = 1
	ENDIF

IF CFLAG:LOCAL:물리반사フラグ == 1
		B = CFLAG:LOCAL:물리반사フラグ
		CFLAG:LOCAL:물리반사フラグ = 0
	ENDIF

IF CFLAG:LOCAL:검격무효화횟수 > 0
		C = CFLAG:LOCAL:검격무효화횟수
		CFLAG:LOCAL:검격무효화횟수 = 0
	ENDIF

IF A + B + C > 0
    ;PRINTFORML 	TARGET:[{ARG:1,2}] %CALLNAME:LOCAL,20,LEFT%　>>>>>>　내성을 관통했다！
ENDIF

CFLAG:ARG:명중강화 = MIN(64, CFLAG:ARG:명중강화 + 32)
CFLAG:ARG:마법효과강화 = MIN(32, CFLAG:ARG:마법효과강화 + 32)
CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 32)
CALL ATTACK_SINGLE,ARG,LOCAL,3734
CFLAG:ARG:마법효과강화 = MAX(-32, CFLAG:ARG:마법효과강화 - 32)
CFLAG:ARG:마법위력강화 = MAX(-32, CFLAG:ARG:마법위력강화 - 32)
CFLAG:ARG:명중강화 = MAX(-64, CFLAG:ARG:명중강화 - 32)
SIF A == 1
     CFLAG:LOCAL:검격가드킬 = 0
SIF B > 0
     CFLAG:LOCAL:물리반사フラグ = B
SIF C > 0
     CFLAG:LOCAL:검격무효화횟수 = C
;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_3734
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 선택된 자의 행동.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_3734,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない