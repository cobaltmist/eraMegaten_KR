;XCOM2 사이오니스트 공격기

@SKILL_NAME_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 실성·분립

;　명중
@SKILL_HITRATE_3722, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9999

;　위력
@SKILL_POWER_3722, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 100
IF BASE:ARG:LV >= 40
	RETURN 100
ELSEIF BASE:ARG:LV >= 35
	RETURN 85
ELSEIF BASE:ARG:LV >= 30
	RETURN 70
ELSEIF BASE:ARG:LV >= 25
	RETURN 60
ELSEIF BASE:ARG:LV >= 20
	RETURN 50
ELSEIF BASE:ARG:LV >= 15
	RETURN 40
ELSEIF BASE:ARG:LV >= 10
	RETURN 30
ELSE
	RETURN 20
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_3722, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 10
IF BASE:ARG:LV >= 25
	RETURN 10
ELSEIF BASE:ARG:LV >= 20
	RETURN 8
ELSEIF BASE:ARG:LV >= 15
	RETURN 6
ELSEIF BASE:ARG:LV >= 10
	RETURN 4
ELSE
	RETURN 2
ENDIF

;　상성
@SKILL_TYPE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_3722, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사이오닉

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_3722, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2
;마법판정

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_3722
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_3722
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3722
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 20
;만능

;계승불능
@継承不能_3722
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_3722, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3722
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화용 랭크
@SKILL_RANK_3722
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
A = RAND:3
IF A == 0
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF A == 1
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 2
	RETURN GET_STATE_NUM("PANIC")
ELSE
	RETURN GET_STATE_NUM("SLEEP")
ENDIF

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("정신")

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 40

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 80

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3722,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL:1 = BASE:POS(ARG:1):ＨＰ

CALL ATTACK_SINGLE, ARG, POS(ARG:1),3722

IF POS(ARG:1) > 0 && LOCAL:1 > BASE:POS(ARG:1):ＨＰ && GET_STATE(CFLAG:POS(ARG:1):상태이상) != "DYING"
		CFLAG:POS(ARG:1):방어강화 = MAX(-24, CFLAG:POS(ARG:1):방어강화 - 8)
	PRINTFORML [{ARG:1}]%CALLNAME:POS(ARG:1),20% >>>>> 파열되었다！
	PRINTFORML 　　TARGET:[{ARG:1,2}] %CALLNAME:POS(ARG:1),20,LEFT%　>>>>>>　방어력이 내려갔다！
ENDIF
;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_3722
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 실성의 강화형. 적 1체에게 마법 피해와 확률로 정신계 상태이상을 일으키고 파열시켜 방어력을 내린다.
PRINTL 위력 자체는 매우 약하지만 빗나가지 않는다. 사용자 레벨에 따라 최대 위력 상승.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_3722,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;특별히 없음

