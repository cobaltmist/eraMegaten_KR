;XCOM2 척탄병 기본기

;스킬명
@SKILL_NAME_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 수류탄발사

;　명중
@SKILL_HITRATE_3704, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;　위력
@SKILL_POWER_3704, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 70
IF BASE:ARG:LV >= 30
	RETURN 70
ELSEIF BASE:ARG:LV >= 10
	RETURN 40
ELSE
	RETURN 30
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 2
IF BASE:ARG:LV >= 25
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 2
IF BASE:ARG:LV >= 25
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_3704, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 10
IF BASE:ARG:LV >= 25
	RETURN 10
ELSE
	RETURN 5
ENDIF

;　상성
@SKILL_TYPE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_3704, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;물리 특수

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_3704, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_3704
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 3
IF BASE:ARG:LV >= 30
	RETURN 3
ELSE
	RETURN 2
ENDIF

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_3704
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3704
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 20
;만능

;계승불능
@継承不能_3704
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도보정
@SKILL_SPEED_3704, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 5
IF BASE:ARG:LV >= 25
	RETURN 5
ELSE
	RETURN 0
ENDIF

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3704
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_3704
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3704,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,3704

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_3704
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 척탄병의 기본 스킬.
PRINTL 척탄병 기본 장비인 유탄발사기로 수류탄을 발사해 물리 만능 상성의 약한 피해를 입힌다.
PRINTL 척탄병 특성 획득에 따라 공격 횟수나 위력이 상승할 수 있다.

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3704,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

