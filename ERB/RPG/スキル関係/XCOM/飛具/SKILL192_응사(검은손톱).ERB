;======================================================
;자동 발동 카운터 스킬 응사(검은손톱 전용)
;======================================================

;스킬명
@SKILL_NAME_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 응사

;　명중
@SKILL_HITRATE_192, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 135
IF BASE:ARG:LV >= 40
	RETURN 135
ELSEIF BASE:ARG:LV >= 35
	RETURN 130
ELSEIF BASE:ARG:LV >= 30
	RETURN 125
ELSEIF BASE:ARG:LV >= 25
	RETURN 120
ELSEIF BASE:ARG:LV >= 20
	RETURN 115
ELSEIF BASE:ARG:LV >= 15
	RETURN 110
ELSEIF BASE:ARG:LV >= 10
	RETURN 105
ELSE
	RETURN 100
ENDIF

;　위력
@SKILL_POWER_192, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 180
IF BASE:ARG:LV >= 40
	RETURN 180
ELSEIF BASE:ARG:LV >= 35
	RETURN 170
ELSEIF BASE:ARG:LV >= 30
	RETURN 160
ELSEIF BASE:ARG:LV >= 25
	RETURN 150
ELSEIF BASE:ARG:LV >= 20
	RETURN 140
ELSEIF BASE:ARG:LV >= 15
	RETURN 130
ELSEIF BASE:ARG:LV >= 10
	RETURN 120
ELSE
	RETURN 110
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_192, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;소비없음＝1

;　비용의 양
@SKILL_COST_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　상성
@SKILL_TYPE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;스킬 치명타율
@SKILL_CRITICAL_RATE_192, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 10

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_192, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_192, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
CALLFORM 사거리_{EQUIP:ARG:총}
RETURN RESULT
;사거리도 총기 의존

;　범위　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 특수처리여부 및 처리내용
@SKILL_SPECIALEFECT_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1이었을 경우는 처리의 유무를 반환합니다
SIF ARG == 1
	RETURN 0
;이하에 특수한 처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구

;계승불능
@継承不能_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도보정
@SKILL_SPEED_192, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

;　구상의 표시와 그 타입
@SKILL_MESSAGE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화용 랭크
@SKILL_RANK_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;카운터 발동 조건
;====================================================
@SKILL_COUNTER_ACTIONABLE_192,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
;SIF !GETBIT(CFLAG:ARG:받음플래그,18) && !GETBIT(CFLAG:ARG:받음플래그,19)
;SIF !RECEIVE_CHECK(ARG,"물리") && !RECEIVE_CHECK(ARG,"마법")
;자신에게 공격이 안오면 NG
SIF !RECEIVE_CHECK(ARG,"공격")
	RETURN 0
;SIF CFLAG:ARG:회피실패
;	RETURN 0
SIF ARG:1 < 0 || GET_STATE(CFLAG:(ARG:1):상태이상) == "DYING"
	RETURN 0
;SIF !RAND:2
;	RETURN 0
RETURN 1

;====================================================
;스킬 및 행동 실행
;====================================================
@ACTION_192,ARG,ARG:1
#LOCALSIZE 5
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
A = 0
B = 0
C = 0
IF (MAXBASE:LOCAL:만능 < 100 || MAXBASE:LOCAL:만능 == 999) && CFLAG:LOCAL:만능가드킬 < 1 
		CFLAG:LOCAL:만능가드킬 = 1
		A = 1
	ENDIF

IF CFLAG:LOCAL:물리반사플래그 == 1
		B = CFLAG:LOCAL:물리반사플래그
		CFLAG:LOCAL:물리반사플래그 = 0
	ENDIF

IF CFLAG:LOCAL:만능무효화횟수 > 0
		C = CFLAG:LOCAL:만능무효화횟수
		CFLAG:LOCAL:만능무효화횟수 = 0
	ENDIF

IF A + B + C > 0
    ;PRINTFORML 	TARGET:[{ARG:1,2}] %CALLNAME:LOCAL,20,LEFT%　>>>>>>　내성을 관통했다！
ENDIF

CALL SKILL_POWER_192, ARG
LOCAL:1 = RESULT
CALLFORM SKILL_TYPE_192, ARG
LOCAL:2 = RESULT
TRYCCALLFORM SKILL_CRITICAL_RATE_192
	LOCAL:3 = RESULT
CATCH
	LOCAL:3 = 5
ENDCATCH
PRINTFORM 　　TARGET:[{CPOS(LOCAL),2}] %CALLNAME:LOCAL,20,LEFT%　>>>>>>　
CALL DAMAGE_1,ARG,LOCAL,LOCAL:1,LOCAL:2,LOCAL:3
PRINTL
SIF A == 1
     CFLAG:LOCAL:만능가드킬 = 0
SIF B > 0
     CFLAG:LOCAL:물리반사플래그 = B
SIF C > 0
     CFLAG:LOCAL:만능무효화횟수 = C

;====================================================
;실행 내용 여기까지
;====================================================

;　 설명문의 표시
@SKILL_EXPLAIN_192
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL [자동] XCOM2 저격병 총잡이의 스킬. 검은 손톱 전용.
PRINTL 자신이 적에게 공격당하면 회피 여부에 상관 없이 자신을 공격한 적 1체에게 즉시 반격한다.
PRINTL 공격 횟수는 1회이며 레벨에 따라 명중과 위력이 상승.
PRINTL 만능 상성이며 내성, 흡수, 반사에 상관 없이 적중. 테트라칸도 해제하지 않고 관통한다.

;계승에 필요한 부위
@継承部位_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;특별히 없음

@SKILL_ACTIONABLE_BATTLE_192
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_192,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;특별히 없음

