;XCOM2 WOTC 헌터 공격기

;스킬명
@SKILL_NAME_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 검은손톱사격

;　명중
@SKILL_HITRATE_175, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 140
IF BASE:ARG:LV >= 40
	RETURN 140
ELSEIF BASE:ARG:LV >= 35
	RETURN 130
ELSEIF BASE:ARG:LV >= 30
	RETURN 120
ELSEIF BASE:ARG:LV >= 20
	RETURN 110
ELSEIF BASE:ARG:LV >= 10
	RETURN 100
ELSE
	RETURN 100
ENDIF

;　위력
@SKILL_POWER_175, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 100
IF BASE:ARG:LV >= 40
	RETURN 100
ELSEIF BASE:ARG:LV >= 35
	RETURN 80
ELSEIF BASE:ARG:LV >= 30
	RETURN 70
ELSEIF BASE:ARG:LV >= 20
	RETURN 70
ELSEIF BASE:ARG:LV >= 10
	RETURN 65
ELSE
	RETURN 60
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_175, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;소모없음＝1

;　비용의 양
@SKILL_COST_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　상성
@SKILL_TYPE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;스킬 치명타율
@SKILL_CRITICAL_RATE_175, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_175, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_175, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_175
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_175
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_175
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구지만 만능

;계승불능
@継承不能_175
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_175, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

;　구상의 표시와 그 타입
@SKILL_MESSAGE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_175
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_175
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_175,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,175

;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_175
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 WOTC 헌터의 두번째 기본공격. 스킬로 분류한다.
PRINTL 만능 상성으로 공격이 적중하는 선택된 자를 위한 특전 스킬.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_175,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

