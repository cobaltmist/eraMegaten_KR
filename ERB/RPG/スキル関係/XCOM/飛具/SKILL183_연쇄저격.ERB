;XCOM2 저격병 최종단계 스킬

;스킬명
@SKILL_NAME_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 연쇄저격

;　명중
@SKILL_HITRATE_183, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 110

;　위력
@SKILL_POWER_183, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 170
IF BASE:ARG:LV >= 40
	RETURN 170
ELSEIF BASE:ARG:LV >= 39
	RETURN 160
ELSEIF BASE:ARG:LV >= 38
	RETURN 155
ELSEIF BASE:ARG:LV >= 37
	RETURN 150
ELSEIF BASE:ARG:LV >= 36
	RETURN 145
ELSEIF BASE:ARG:LV >= 35
	RETURN 140
ELSEIF BASE:ARG:LV >= 34
	RETURN 138
ELSEIF BASE:ARG:LV >= 33
	RETURN 136
ELSEIF BASE:ARG:LV >= 32
	RETURN 134
ELSEIF BASE:ARG:LV >= 31
	RETURN 132
ELSEIF BASE:ARG:LV >= 30
	RETURN 130
ELSEIF BASE:ARG:LV >= 28
	RETURN 127
ELSEIF BASE:ARG:LV >= 26
	RETURN 124
ELSEIF BASE:ARG:LV >= 24
	RETURN 121
ELSEIF BASE:ARG:LV >= 22
	RETURN 118
ELSEIF BASE:ARG:LV >= 20
	RETURN 115
ELSEIF BASE:ARG:LV >= 19
	RETURN 114
ELSEIF BASE:ARG:LV >= 18
	RETURN 113
ELSEIF BASE:ARG:LV >= 17
	RETURN 112
ELSEIF BASE:ARG:LV >= 16
	RETURN 111
ELSEIF BASE:ARG:LV >= 15
	RETURN 110
ELSE
	RETURN 110
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_183, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG > -1
	RETURN MAX((MAXBASE:ARG:ＭＰ / 8),1)
RETURN 1

;　상성
@SKILL_TYPE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG >= 0
	CALLFORM 공격상성_{EQUIP:ARG:총}
RETURN ARG >= 0 ? RESULT # 1
;총기 의존
;RETURN 1
;비구

;스킬 치명타율
@SKILL_CRITICAL_RATE_183, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 20

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_183, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_183, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_183
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_183
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_183
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구

;계승불능
@継承不能_183
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_183, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　구상의 표시와 그 타입
@SKILL_MESSAGE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_183
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_183
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_183,ARG,ARG:1
#LOCALSIZE 5
#LOCALSSIZE 2
LOCAL:2 = RAND(1, 11)

FOR LOCAL:3, 0, LOCAL:2
	LOCAL:1 = 0
     	FOR LOCAL, 7, 17
		SIF POS(LOCAL) == -1
             		CONTINUE
        	SIF GET_STATE(CFLAG:POS(LOCAL):ステート) == "DYING"
            	 	CONTINUE
        	LOCAL:1++
     	NEXT

     CALL ATTACK_RAND,ARG,ARG:1,183

     FOR LOCAL, 7, 17
         	SIF POS(LOCAL) == -1
             		CONTINUE
         	SIF GET_STATE(CFLAG:POS(LOCAL):ステート) == "DYING"
             		CONTINUE
         	LOCAL:1--
    	NEXT
     	;적이 쓰러지지 않았을 경우
    		SIF LOCAL:1 == 0
		BREAK
 	NEXT

;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_183
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 강력한 연쇄 저격 능력. 첫 공격으로 적이 쓰러지면 계속 공격을 가하며, 최대 10회 연속으로 사격한다.
PRINTL 총기 상성에 의존하는 랜덤 타겟팅 스킬이며 전체 MP의 8분의 1 소모.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_183,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

