;XCOM2 WOTC 헌터 특수 공격기

;스킬명
@SKILL_NAME_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 진정제탄

;　명중
@SKILL_HITRATE_176, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 130
IF BASE:ARG:LV >= 35
	RETURN 130
ELSEIF BASE:ARG:LV >= 30
	RETURN 120
ELSEIF BASE:ARG:LV >= 20
	RETURN 110
ELSEIF BASE:ARG:LV >= 10
	RETURN 100
ELSE
	RETURN 100
ENDIF

;　위력
@SKILL_POWER_176, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 70
IF BASE:ARG:LV >= 35
	RETURN 70
ELSEIF BASE:ARG:LV >= 30
	RETURN 60
ELSEIF BASE:ARG:LV >= 20
	RETURN 50
ELSEIF BASE:ARG:LV >= 10
	RETURN 45
ELSE
	RETURN 40
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_176, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;소모없음＝1

;　비용의 양
@SKILL_COST_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　상성
@SKILL_TYPE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_176, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_176, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_176
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_176
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_176
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구로 계승

;계승불능
@継承不能_176
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_176, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

;　구상의 표시와 그 타입
@SKILL_MESSAGE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_176
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_176
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
A = RAND:2
IF A == 0
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF A == 1
	RETURN GET_STATE_NUM("CHARM")
ELSE
	RETURN GET_STATE_NUM("PANIC")
ENDIF

;추가효과상성
@SKILL_ADDTIONAL_TYPE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("만능")

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_176,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
CFLAG:ARG:마법효과강화 = MIN(32, CFLAG:ARG:마법효과강화 + 32)
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,176
CFLAG:ARG:마법효과강화 = MAX(-32, CFLAG:ARG:마법효과강화 - 32)
;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_176
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 WOTC 헌터의 특수 스킬.
PRINTL 피격자 1체에게 100%의 확률로 정신계 상태이상을 부여해 현혹시킨다.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_176,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

