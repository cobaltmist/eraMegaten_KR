;XCOM2 저격병 저격수 상위 단계 스킬

;스킬명
@SKILL_NAME_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 살상지대

;　명중
@SKILL_HITRATE_172, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 115
IF BASE:ARG:LV >= 40
	RETURN 115
ELSEIF BASE:ARG:LV >= 30
	RETURN 105
ELSE
	RETURN 95
ENDIF

;　위력
@SKILL_POWER_172, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 100
IF BASE:ARG:LV >= 40
	RETURN 100
ELSEIF BASE:ARG:LV >= 39
	RETURN 98
ELSEIF BASE:ARG:LV >= 38
	RETURN 96
ELSEIF BASE:ARG:LV >= 37
	RETURN 94
ELSEIF BASE:ARG:LV >= 36
	RETURN 92
ELSEIF BASE:ARG:LV >= 35
	RETURN 90
ELSEIF BASE:ARG:LV >= 34
	RETURN 88
ELSEIF BASE:ARG:LV >= 33
	RETURN 86
ELSEIF BASE:ARG:LV >= 32
	RETURN 84
ELSEIF BASE:ARG:LV >= 31
	RETURN 82
ELSEIF BASE:ARG:LV >= 30
	RETURN 80
ELSEIF BASE:ARG:LV >= 28
	RETURN 77
ELSEIF BASE:ARG:LV >= 26
	RETURN 74
ELSEIF BASE:ARG:LV >= 24
	RETURN 71
ELSEIF BASE:ARG:LV >= 22
	RETURN 68
ELSEIF BASE:ARG:LV >= 20
	RETURN 65
ELSEIF BASE:ARG:LV >= 19
	RETURN 64
ELSEIF BASE:ARG:LV >= 18
	RETURN 63
ELSEIF BASE:ARG:LV >= 17
	RETURN 62
ELSEIF BASE:ARG:LV >= 16
	RETURN 61
ELSEIF BASE:ARG:LV >= 15
	RETURN 60
ELSE
	RETURN 60
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 7

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_172, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 16
IF BASE:ARG:LV >= 40
	RETURN 16
ELSEIF BASE:ARG:LV >= 30
	RETURN 12
ELSEIF BASE:ARG:LV >= 20
	RETURN 8
ELSE
	RETURN 4
ENDIF

;　상성
@SKILL_TYPE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG >= 0
	CALLFORM 공격상성_{EQUIP:ARG:총}
RETURN ARG >= 0 ? RESULT # 1
;총기 의존
;RETURN 1
;비구

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_172, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_172, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_172
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_172
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_172
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구

;계승불능
@継承不能_172
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_172, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 6

;　구상의 표시와 그 타입
@SKILL_MESSAGE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_172
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_172
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_172,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
CALL ATTACK_RAND,ARG,ARG:1,172


;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_172
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 저격병 저격수의 상위 단계 스킬.
PRINTL 적 전체를 대상으로 랜덤한 타겟에게 4~7회 사격한다.
PRINTL 총기 상성에 따라 스킬 상성도 바뀐다.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_172,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

