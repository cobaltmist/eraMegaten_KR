;XCOM2 척탄병 헤비거너 최종단계 스킬

;스킬명
@SKILL_NAME_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 파열

;　명중
@SKILL_HITRATE_180, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;　위력
@SKILL_POWER_180, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_180, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG > -1
	RETURN MAX((MAXBASE:ARG:ＭＰ / 10),1)
RETURN 1

;　상성
@SKILL_TYPE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG >= 0
	CALLFORM 공격상성_{EQUIP:ARG:총}
RETURN ARG >= 0 ? RESULT # 1
;총기 의존
;RETURN 1
;비구

;스킬 치명타율
@SKILL_CRITICAL_RATE_180, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_180, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_180, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_180
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_180
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_180
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구

;계승불능
@継承不能_180
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도보정
@SKILL_SPEED_180, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_180
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_180
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_180,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL:1 = BASE:POS(ARG:1):ＨＰ
CALL ATTACK_SINGLE, ARG, POS(ARG:1),180
IF POS(ARG:1) > 0 && LOCAL:1 > BASE:POS(ARG:1):ＨＰ && GET_STATE(CFLAG:POS(ARG:1):상태이상) != "DYING"
		CFLAG:POS(ARG:1):방어강화 = MAX(-24, CFLAG:POS(ARG:1):방어강화 - 8)
	PRINTFORMW 　　[{ARG:1}]%CALLNAME:POS(ARG:1),20% >>>>> 파열됨！방어력 저하！
ENDIF

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_180
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 척탄병 헤비거너의 최종 단계 스킬. 전체 MP의 10분의 1을 소모한다.
PRINTL 적 1체에게 파열 사격. 명중시 반드시 치명타를 입히며, 적의 방어력을 2단계 떨어뜨린다.
PRINTL 후열 사용이 가능하며 방어력 저하는 하한 6단계.

;継承に필요한部位
@継承部位_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_180,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

