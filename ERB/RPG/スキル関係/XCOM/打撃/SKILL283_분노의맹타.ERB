;XCOM2 RAGE아머 고유 스킬

;스킬명
@SKILL_NAME_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 분노의맹타

;　명중
@SKILL_HITRATE_283, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9999

;　위력
@SKILL_POWER_283, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 70

;　최저공격횟수
@SKILL_MINATTACKNUMBER_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_283, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG > -1
	RETURN MAX((MAXBASE:ARG:ＭＰ / 15),1)
RETURN 1

;　상성
@SKILL_TYPE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2
;타격

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_283, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 타격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_283, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_283
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_283
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_283
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2
;타격

;계승불능
@継承不能_283
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_283, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1000

;　구상의 표시와 그 타입
@SKILL_MESSAGE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_283
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_283
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
A = RAND:2
IF A == 0
	RETURN GET_STATE_NUM("PANIC")
ELSE
	RETURN GET_STATE_NUM("SHOCK")
ENDIF

;추가효과상성
@SKILL_ADDTIONAL_TYPE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 40

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 90

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_283,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
CFLAG:ARG:방어반감플래그 = 1
LOCAL = POS(ARG:1)
A = 0
B = 0
C = 0
IF (MAXBASE:LOCAL:타격 < 100 || MAXBASE:LOCAL:타격 == 999) && CFLAG:LOCAL:타격가드킬 < 1 
		CFLAG:LOCAL:타격가드킬 = 1
		A = 1
	ENDIF

IF CFLAG:LOCAL:물리반사플래그 == 1
		B = CFLAG:LOCAL:물리반사플래그
		CFLAG:LOCAL:물리반사플래그 = 0
	ENDIF

IF CFLAG:LOCAL:검격무효화횟수 > 0
		C = CFLAG:LOCAL:타격무효화횟수
		CFLAG:LOCAL:타격무효화횟수 = 0
	ENDIF

IF A + B + C > 0
    ;PRINTFORML 	TARGET:[{ARG:1,2}] %CALLNAME:LOCAL,20,LEFT%　>>>>>>　내성을 관통했다！
ENDIF
CFLAG:ARG:마법효과강화 = MIN(64, CFLAG:ARG:마법효과강화 + 32)
CALL ATTACK_SINGLE,ARG,LOCAL,283
CFLAG:ARG:마법효과강화 = MAX(-64, CFLAG:ARG:마법효과강화 - 32)
CFLAG:ARG:방어반감플래그 = 0
SIF A == 1
     CFLAG:LOCAL:타격가드킬 = 0
SIF B > 0
     CFLAG:LOCAL:물리반사플래그 = B
SIF C > 0
     CFLAG:LOCAL:타격무효화횟수 = C

;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_283
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 적 1명에게 돌진해 팔을 모아 지면을 깨부수는 타격을 내리꼿는다.
PRINTL 공격 자체는 반드시 명중하며 일정 확률로 상대를 혼란이나 SHOCK 상태에 빠뜨릴 수 있다。
PRINTL 전체 MP중 15분의 1을 소모하며 테트라칸을 관통하고 모든 타격 상성을 무효화한다.

;継承に必要な部位
@継承部位_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;使用条件
@SKILL_SPECIAL_ACTIONABLE_283,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない

