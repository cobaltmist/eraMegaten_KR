;XCOM2 WOTC 템플러 기본 단계 스킬 강화형

;스킬명
@SKILL_NAME_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 연쇄번개·여진

;　명중
@SKILL_HITRATE_960, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 200

;　위력
@SKILL_POWER_960, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 70
IF BASE:ARG:LV >= 40
	RETURN 70
ELSEIF BASE:ARG:LV >= 39
	RETURN 69
ELSEIF BASE:ARG:LV >= 38
	RETURN 68
ELSEIF BASE:ARG:LV >= 37
	RETURN 67
ELSEIF BASE:ARG:LV >= 36
	RETURN 66
ELSEIF BASE:ARG:LV >= 35
	RETURN 65
ELSEIF BASE:ARG:LV >= 34
	RETURN 64
ELSEIF BASE:ARG:LV >= 33
	RETURN 63
ELSEIF BASE:ARG:LV >= 32
	RETURN 62
ELSEIF BASE:ARG:LV >= 31
	RETURN 61
ELSEIF BASE:ARG:LV >= 30
	RETURN 60
ELSEIF BASE:ARG:LV >= 29
	RETURN 59
ELSEIF BASE:ARG:LV >= 28
	RETURN 58
ELSEIF BASE:ARG:LV >= 27
	RETURN 57
ELSEIF BASE:ARG:LV >= 26
	RETURN 56
ELSEIF BASE:ARG:LV >= 24
	RETURN 55
ELSEIF BASE:ARG:LV >= 22
	RETURN 54
ELSEIF BASE:ARG:LV >= 20
	RETURN 53
ELSEIF BASE:ARG:LV >= 18
	RETURN 52
ELSEIF BASE:ARG:LV >= 16
	RETURN 51
ELSEIF BASE:ARG:LV >= 15
	RETURN 50
ELSEIF BASE:ARG:LV >= 14
	RETURN 47
ELSEIF BASE:ARG:LV >= 13
	RETURN 44
ELSEIF BASE:ARG:LV >= 12
	RETURN 41
ELSEIF BASE:ARG:LV >= 11
	RETURN 38
ELSE
	RETURN 35
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_960, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4

;　상성
@SKILL_TYPE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 6
;전격

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_960, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 근접계 특수기

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_960, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_960
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_960
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_960
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9
;전격

;계승불능
@継承不能_960
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_960, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

;　구상의 표시와 그 타입
@SKILL_MESSAGE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_960
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_960
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_960,ARG,ARG:1
#LOCALSIZE 3
#LOCALSSIZE 2
#DIM 前HP
#DIM 被弾플래그
被弾플래그 = 0
IF CFLAG:(ARG):PTフラグ == 0
	LOCAL:1 = 1
	LOCAL:2 = 4
ELSE
	LOCAL:1 = 7
	LOCAL:2 = 12
ENDIF

FOR LOCAL,LOCAL:1,LOCAL:2
	SIF POS(LOCAL) < 0 || CFLAG:POS(LOCAL):ステート == GET_STATE_NUM("DYING")
		CONTINUE
	前HP = BASE:POS(LOCAL):ＨＰ
	CALL ATTACK_SINGLE,ARG,POS(LOCAL),960
	IF POS(LOCAL) > -1 && BASE:POS(LOCAL):ＨＰ < 前HP
		CFLAG:POS(LOCAL):회피강화 = MAX(-16, CFLAG:POS(LOCAL):회피강화 -8)
		被弾플래그++
	ENDIF
NEXT
SIF 被弾플래그
	PRINTFORMW 공격당한 \@ CFLAG:ARG:PTフラグ == 2 ? 적의 회피율이 내려갔다！ # 아군의 회피율이 내려갔다！ \@

;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_960
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 연쇄번개의 강화형. 적 1열에 전격(사이오닉) 상성의 약한 피해를 입힌다.
PRINTL 공격당한 적은 회피율이 2단계 저하된다. 하한 4단계. 물리 상성

;使用条件
@SKILL_SPECIAL_ACTIONABLE_960,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない