;XCOM2 스커미셔 상위단계 스킬

;스킬명
@SKILL_NAME_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 채찍질

;　명중
@SKILL_HITRATE_84, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 100

;　위력
@SKILL_POWER_84, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 60

;　최저공격횟수
@SKILL_MINATTACKNUMBER_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_84, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG > -1
	RETURN MAX((MAXBASE:ARG:ＭＰ / 15),1)
RETURN 1

;　상성
@SKILL_TYPE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;스킬 치명타율
@SKILL_CRITICAL_RATE_84, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 10

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_84, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 근접계 특수기

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_84, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_84
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_84
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_84
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;계승불능
@継承不能_84
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_84, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 10

;　구상의 표시와 그 타입
@SKILL_MESSAGE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_84
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_84
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_STATE_NUM("SHOCK")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 40

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 80

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_84,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
A = 0
B = 0
C = 0
IF (MAXBASE:LOCAL:검격 < 100 || MAXBASE:LOCAL:검격 == 999) && CFLAG:LOCAL:검격가드킬 < 1 
		CFLAG:LOCAL:검격가드킬 = 1
		A = 1
	ENDIF

IF CFLAG:LOCAL:물리반사フラグ == 1
		B = CFLAG:LOCAL:물리반사フラグ
		CFLAG:LOCAL:물리반사フラグ = 0
	ENDIF

IF CFLAG:LOCAL:검격무효화횟수 > 0
		C = CFLAG:LOCAL:전술무효화횟수
		CFLAG:LOCAL:검격무효화횟수 = 0
	ENDIF

IF A + B + C > 0
    ;PRINTFORML 	TARGET:[{ARG:1,2}] %CALLNAME:LOCAL,20,LEFT%　>>>>>>　내성을 관통했다！
ENDIF

CFLAG:ARG:공격강화 = MIN(32, CFLAG:ARG:공격강화 + 20)
CFLAG:ARG:마법효과강화 = MIN(32, CFLAG:ARG:마법효과강화 + 20)
CALL ATTACK_SINGLE,ARG,LOCAL,84
CFLAG:ARG:마법효과강화 = MAX(-32, CFLAG:ARG:마법효과강화 - 20)
CFLAG:ARG:공격강화 = MAX(-32, CFLAG:ARG:공격강화 - 20)
SIF A == 1
     CFLAG:LOCAL:검격가드킬 = 0
SIF B > 0
     CFLAG:LOCAL:물리반사フラグ = B
SIF C > 0
     CFLAG:LOCAL:검격무효화횟수 = C

;====================================================
;実行内容ここまで
;====================================================
;　 説明文の表示
@SKILL_EXPLAIN_84
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 스커미셔의 스킬. 검격 상성의 전기 채찍으로 적 1체를 공격해 SHOCK 시킨다.
PRINTL 후열 사용가능 및 전체 MP의 15분의 1 소모. 
PRINTL 모든 검격 상성을 관통 및 무시하며 테트라칸을 해제하지 않고 적중한다.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_84,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない