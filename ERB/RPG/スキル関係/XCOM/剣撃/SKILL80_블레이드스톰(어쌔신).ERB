;XCOM2 돌격병 상위 단계 스킬

;스킬명
@SKILL_NAME_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 블레이드 스톰

;　명중
@SKILL_HITRATE_80, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9999

;　위력
@SKILL_POWER_80, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 140
IF BASE:ARG:LV >= 40
	RETURN 140
ELSEIF BASE:ARG:LV >= 35
	RETURN 110
ELSEIF BASE:ARG:LV >= 30
	RETURN 100
ELSEIF BASE:ARG:LV >= 20
	RETURN 80
ELSEIF BASE:ARG:LV >= 10
	RETURN 60
ELSE
	RETURN 60
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_80, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;소비없음＝1

;　비용의 양
@SKILL_COST_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　상성
@SKILL_TYPE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_80, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;どちらでもない

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_80, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_80
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　범위　1１체　2１열　3전체　4　적아군전체
@SKILL_SPHERE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_80
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_80
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;계승불능
@継承不能_80
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도보정
@SKILL_SPEED_80, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 10

;　구상의 표시와 그 타입
@SKILL_MESSAGE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_80
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_80
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_80,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
A = 0
B = 0
C = 0
IF (MAXBASE:LOCAL:검격 < 100 || MAXBASE:LOCAL:검격 == 999) && CFLAG:LOCAL:검격가드킬 < 1 
		CFLAG:LOCAL:검격가드킬 = 1
		A = 1
	ENDIF

IF CFLAG:LOCAL:물리반사フラグ == 1
		B = CFLAG:LOCAL:물리반사フラグ
		CFLAG:LOCAL:물리반사フラグ = 0
	ENDIF

IF CFLAG:LOCAL:검격무효화횟수 > 0
		C = CFLAG:LOCAL:검격무효화횟수
		CFLAG:LOCAL:검격무효화횟수 = 0
	ENDIF

IF A + B + C > 0
    ;PRINTFORML 	TARGET:[{ARG:1,2}] %CALLNAME:LOCAL,20,LEFT%　>>>>>>　내성을 관통했다！
ENDIF

CFLAG:ARG:명중강화 = MIN(64, CFLAG:ARG:명중강화 + 32)
CALL ATTACK_SINGLE,ARG,LOCAL,80
CFLAG:ARG:명중강화 = MAX(-64, CFLAG:ARG:명중강화 - 32)
SIF A == 1
     CFLAG:LOCAL:검격가드킬 = 0
SIF B > 0
     CFLAG:LOCAL:물리반사フラグ = B
SIF C > 0
     CFLAG:LOCAL:검격무효화횟수 = C
;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_80
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 검의 달인 어쌔신에게 숨겨져있던 XCOM2 돌격병의 중간 단계 스킬.
PRINTL 어쌔신은 25%의 확률로 4번째 행동에서 전열의 적 1체에게 근접 공격을 가한다.
PRINTL 검격 내성, 흡수, 반사를 무효화하고 무조건 명중한다. 테트라칸을 관통한다.

;사용조건
@SKILL_SPECIAL_ACTIONABLE_80,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1