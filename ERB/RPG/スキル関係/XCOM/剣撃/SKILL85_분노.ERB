;XCOM2 스커미셔 중간단계 스킬

;스킬명
@SKILL_NAME_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 분노

;　명중
@SKILL_HITRATE_85, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 110
IF BASE:ARG:LV >= 31
	RETURN 110
ELSEIF BASE:ARG:LV >= 30
	RETURN 106
ELSEIF BASE:ARG:LV >= 29
	RETURN 102
ELSEIF BASE:ARG:LV >= 26
	RETURN 98
ELSEIF BASE:ARG:LV >= 23
	RETURN 94
ELSEIF BASE:ARG:LV >= 20
	RETURN 90
ELSEIF BASE:ARG:LV >= 17
	RETURN 86
ELSEIF BASE:ARG:LV >= 14
	RETURN 82
ELSEIF BASE:ARG:LV >= 11
	RETURN 78
ELSEIF BASE:ARG:LV >= 8
	RETURN 74
ELSEIF BASE:ARG:LV >= 5
	RETURN 70
ELSE
	RETURN 70
ENDIF

;　위력
@SKILL_POWER_85, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 80
IF BASE:ARG:LV >= 31
	RETURN 80
ELSEIF BASE:ARG:LV >= 16
	RETURN 60
ELSEIF BASE:ARG:LV >= 15
	RETURN 40
ELSE
	RETURN 40
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_85, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 12
IF BASE:ARG:LV >= 31
	RETURN 12
ELSEIF BASE:ARG:LV >= 16
	RETURN 8
ELSEIF BASE:ARG:LV >= 15
	RETURN 4
ELSE
	RETURN 4
ENDIF

;　상성
@SKILL_TYPE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;스킬 치명타율
@SKILL_CRITICAL_RATE_85, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 20

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_85, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 근접계 특수기

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_85, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_85
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_85
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_85
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;검격

;계승불능
@継承不能_85
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_85, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 6

;　구상의 표시와 그 타입
@SKILL_MESSAGE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_85
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_85
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
TRYCCALLFORM 추가효과_{EQUIP:ARG:검}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;추가효과상성
@SKILL_ADDTIONAL_TYPE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 4
IF BASE:ARG:LV >= 31
	RETURN 4
ELSEIF BASE:ARG:LV >= 16
	RETURN 6
ELSE
	RETURN 0
ENDIF

;추가효과基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 25

;추가효과付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 50

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_85,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
CFLAG:ARG:마법효과강화 = MIN(32, CFLAG:ARG:마법효과강화 + 12)
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,85
CFLAG:ARG:마법효과강화 = MAX(-32, CFLAG:ARG:마법효과강화 - 12)

;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_85
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 스커미셔들이 사용하는 립잭 공격기. 적 1체에게 직접 이동해 립잭으로 공격한다.
PRINTL 공격 사거리가 길기 때문에 후열에서 맨 후열의 적에게 사용할 수 있다.
PRINTL 레벨 15부터 SHOCK 상태이상을 일으키며 레벨 30부턴 FLAME 상태이상을 일으킨다.

;使用条件
@SKILL_SPECIAL_ACTIONABLE_85,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;とくにない