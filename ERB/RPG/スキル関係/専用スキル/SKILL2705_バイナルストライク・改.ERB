;通常공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_2705,ARG
RESULTS = 바이널스트라이크・개

;　명중
@SKILL_HITRATE_2705, ARG
RETURN 140

;　위력
@SKILL_POWER_2705, ARG
RETURN 590

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2705,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2705,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2705, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_2705,ARG
RETURN 20

;　상성
@SKILL_TYPE_2705,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("만능")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("만능")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0 || CFLAG:LOCAL:마법반사플래그 || CFLAG:LOCAL:무효플래그
	RETURN GET_TYPE_NUM("만능")
LOCAL:2 = -1000
LOCALS = 만능
LOCALS:1 = 타격
LOCALS:2 = 만능
FOR LOCAL:1, 1, 3
	;판정用に数値を仕舞う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	SIF LOCAL:1 == 1
		LOCAL:3 = LOCAL:1 == 1 && CFLAG:LOCAL:물리반사플래그 ? -200 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2705, ARG
RETURN 1
;魔法

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2705, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2705
RETURN 1

;　사거리
@SKILL_RANGE_2705,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2705,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2705
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2705,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2705
RETURN 20

;全て

;속도보정
@SKILL_SPEED_2705, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2705,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2705
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2705
RETURN 23

@継承不能_2705
RETURN 1
;계승불가

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2705,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2705,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2705,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2705,ARG
RETURN 0
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2705,ARG,ARG:1
IF ARG:1 == 17
	LOCAL:1 = 1
	LOCAL:2 = 4
ELSEIF ARG:1 == 18
	LOCAL:1 = 4
	LOCAL:2 = 7
ELSEIF ARG:1 == 19
	LOCAL:1 = 1
	LOCAL:2 = 7
ELSEIF ARG:1 == 20
	LOCAL:1 = 7
	LOCAL:2 = 12
ELSEIF ARG:1 == 21
	LOCAL:1 = 12
	LOCAL:2 = 17
ELSEIF ARG:1 == 22
	LOCAL:1 = 7
	LOCAL:2 = 17
ELSEIF ARG:1 == 23
	LOCAL:1 = 1
	LOCAL:2 = 17
ENDIF
FOR LOCAL, LOCAL:1, LOCAL:2
	LOCALS = 포지션{LOCAL}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
	CFLAG:ARG:타겟 = LOCAL
	CALL ATTACK_SINGLE, ARG, FLAG:LOCALS, 2705
NEXT

IF CFLAG:ARG:상태이상 != GET_STATE_NUM("DYING")
	CFLAG:ARG:HP흡수량 -= 999
ENDIF



;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2705
PRINTL 적 전체에 만능+타격 복합 데미지、사용후 999의 피해를 받는다
PRINTL 브라운의 필살기

;계승에 필요한 부위
@계승부위_2705,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2705,ARG
RETURN 1
;とくにない

