
;스킬명
@SKILL_NAME_224,ARG
IF SKILL_CHANGE(ARG, 5051)
	RESULTS = 화 났으니까……！
ELSE
	RESULTS = 분노의철권
ENDIF

;　명중
@SKILL_HITRATE_224, ARG
RETURN 90

;　위력
@SKILL_POWER_224, ARG = -1
SIF ARG == -1
	RETURN 300
SELECTCASE BASE:ARG:ＨＰ * 100 / MAXBASE:ARG:ＨＰ
	CASE IS <= 1
		RETURN 900
	CASE IS < 5
		RETURN 600
	CASE IS < 20
		RETURN 450
	CASE IS < 50
		RETURN 400
	CASE IS < 70
		RETURN 350
	CASEELSE
		RETURN 300
ENDSELECT


;　최저공격횟수
@SKILL_MINATTACKNUMBER_224,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_224,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_224, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_224,ARG
RETURN 13

;　상성
@SKILL_TYPE_224,ARG
RETURN 2
;타격

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_224, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_224, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_224
RETURN 1

;　사거리
@SKILL_RANGE_224,ARG
RETURN 4

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_224,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_224
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_224,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_224
RETURN 2
;타격

;속도보정
@SKILL_SPEED_224, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_224,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_224
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_224
RETURN 7

@SKILL_RANKUP_224
;신등거출팔백만격
D:1 = 225
;최후의일격
D:2 = 243
;랭크アップ先は１つ
RETURN 2

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_224,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_224,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_224,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_224,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_224,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,224






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_224
PRINTL [전열] 적 1체에 타격속성 대 물리데미지. 남은 HP의 비율이 적을수록 위력이 증가한다.

;계승에 필요한 부위
@계승부위_224,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_224,ARG
RETURN 1
;とくにない

