
;스킬名
@SKILL_NAME_206,ARG
;팬텀が사용する場合究極！ゲシュペンス토키ックに
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	RESULTS = 궁극！게슈펜스트킥
ELSEIF SKILL_CHANGE(ARG, 5054)
	RESULTS = 시엘사마
ELSEIF SKILL_CHANGE(ARG, [[キャラ:레이무]])
	RESULTS = 승천각
ELSE
	RESULTS = 섬머솔트
ENDIF
;　명중
@SKILL_HITRATE_206, ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	RETURN 110
ELSE
	RETURN 85
ENDIF

;　威힘
@SKILL_POWER_206, ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	RETURN 480
ELSE
	RETURN 210
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_206,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_206,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_206, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_206,ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	;比較的早く使える代わりに소비は他に比べて重め
	RETURN 48
ELSE
	RETURN 4
ENDIF
;　相性
@SKILL_TYPE_206,ARG
RETURN 2
;타격

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_206, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_206, ARG
	RETURN 1

;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_206
RETURN 1

;　사거리
@SKILL_RANGE_206,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_206,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_206
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_206,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_206
RETURN 2
;타격

;속도보정
@SKILL_SPEED_206, ARG = -1
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	RETURN 1
ELSE
	RETURN 0
ENDIF

;　구상の표시とその타입
@SKILL_MESSAGE_206,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_206
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_206
RETURN 3


;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_206,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_206,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_206,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_206,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_206,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,206






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_206
IF FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	PRINTL 뛰어오르며、적 1체에 타격속성의 궁극 발차기。후열사용가능、후열공격가능。
	PRINTL 또한 원래는 발동코드가 음성 입력이기 때문에、외치는 것이 약속인것 같지만 본작에서는 따로 외치지 않아도Ok。
ELSE
	PRINTL [후열] 적 1체에 타격속성 중 물리데미지. 명중률이 조금 낮다.
ENDIF

;継承に필요な部位
@継承部位_206,ARG
RETURN 1



;사용조건
@SKILL_SPECIAL_ACTIONABLE_206,ARG
RETURN 1
;とくにない

