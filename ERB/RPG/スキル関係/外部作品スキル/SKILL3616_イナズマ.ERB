;통상공격
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_3616,ARG
RESULTS = 이나즈마
SIF SKILLGAGE_D_GET(ARG, 3600)
	RESULTS = 이나즈마2

;　명중
@SKILL_HITRATE_3616, ARG
RETURN 100

;　위력
@SKILL_POWER_3616, ARG
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3616,ARG
IF ARG > -1
	IF SKILLGAGE_D_GET(ARG,3616) && SKILLGAGE_D_GET(ARG, 3600)
		RETURN 2
	ELSE
		RETURN 1
	ENDIF
ENDIF
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3616,ARG
IF ARG > -1
	IF SKILLGAGE_D_GET(ARG,3616) && SKILLGAGE_D_GET(ARG, 3600)
		RETURN 2
	ELSE
		RETURN 1
	ENDIF
ENDIF
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_3616, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_3616,ARG
RETURN 8

;　상성
@SKILL_TYPE_3616,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("전격")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("전격")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("전격")
	
LOCAL:2 = -300
LOCALS = 전격
LOCALS:1 = 비구
LOCALS:2 = 전격
FOR LOCAL:1, 1, 3
	;判定用に数値を仕舞う。反射は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	;LOCAL:3 = BTL_CHK_SKILL_관통(ARG) && INRANGE(MAXBASE:LOCAL:(LOCALS:(LOCAL:1)), 0, 100) ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	LOCAL:3 = GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17 && CFLAG:LOCAL:무효플래그 ? 0 # LOCAL:3
	SIF LOCAL:1 == 1
		LOCAL:3 = LOCAL:1 == 1 && CFLAG:LOCAL:물리반사플래그 ? -200 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;전격

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_3616, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_3616, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_3616
RETURN 1

;　사거리
@SKILL_RANGE_3616,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_3616,ARG
IF ARG > -1
	SIF SKILLGAGE_D_GET(ARG,3616)
		RETURN 3
ENDIF
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_3616
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_3616,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_3616
RETURN 6
;전격

;속도보정
@SKILL_SPEED_3616, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3616,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3616
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_3616
RETURN 0


;テンプレート用、追加効果シリーズ
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_3616,ARG
RETURN GET_STATE_NUM("SHOCK")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_3616,ARG
RETURN GET_TYPE_NUM("전격")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3616,ARG
RETURN 20

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_3616,ARG
RETURN 66

;====================================================
;トリガー発動条件
;====================================================
@SKILL_TRIGGER_ACTIONABLE_3616,ARG,ARG:1
;自分じゃなければNG
SIF ARG != ARG:1
	RETURN 0
;ゲージがなければ不発
SIF SKILLGAGE_D_GET(ARG,3616) < 1
	RETURN 0
CALLFORM SKILL_EFECT_{CFLAG:ARG:입력행동}
SIF RESULT != 1 || CFLAG:ARG:타겟 < 1
	RETURN 0
IF CFLAG:ARG:타겟 < 17
	SIF POS(CFLAG:ARG:타겟) < 0 || GET_STATE(CFLAG:POS(CFLAG:ARG:타겟):상태이상) == "DYING"
		RETURN 0
	RESULT:1 = CFLAG:ARG:타겟
ELSEIF CFLAG:ARG:타겟 < 20
	RESULT:1 = 19
ELSE
	RESULT:1 = 22
ENDIF
RETURN 1

;====================================================
;스킬 및 행동의 실행
;====================================================

@ACTION_3616,ARG,ARG:1
#LOCALSIZE 4
IF SKILLGAGE_D_GET(ARG,3616) == 0
	CALL ATTACK_SINGLE,ARG,POS(ARG:1),3616,1
	CALL SKILLGAGE_D_SET,ARG,3616,3
	PRINTFORML ＞　3턴 전격에 의한 추격이 발동하게 되었다
ELSE
	CALL ATTACK_FIELD,ARG,ARG:1,3616
ENDIF
WAIT

;ハイボルテージのゲージが蓄積していればコンボ判定、
SIF SKILLGAGE_H_GET(ARG, 3617) == 1
	CALL SKILLGAGE_H_SET,ARG,3617,2


;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_3616
PRINTL 공중에서 대각선 아래로 급강하 공격
PRINTL 궤도상에 전격의 띠가 발생해、3턴간 전체공격화
PRINTL 자신의 행동으로 공격스킬을 발동하면 전격+비구의 복합상성으로 추격이 발동한다

;계승에 필요한 부위
@계승부위_3616,ARG
RETURN 1
;とくにない

@SKILL_ACTIONABLE_BATTLE_3616
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3616,ARG
RETURN 1
;とくにない

