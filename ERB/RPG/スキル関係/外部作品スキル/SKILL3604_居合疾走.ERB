;통상공격
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_3604,ARG
RESULTS = 거합질주

;　명중
@SKILL_HITRATE_3604, ARG
RETURN 100

;　위력
@SKILL_POWER_3604, ARG
RETURN 120

;　クリティカル率
@SKILL_CRITICAL_RATE_3604, ARG
RETURN 30

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3604,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3604,ARG
RETURN 3

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_3604, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_3604,ARG
RETURN 30

;　상성
@SKILL_TYPE_3604,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("검격")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("검격")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("검격")
	
LOCAL:2 = -300
LOCALS = 검격
LOCALS:1 = 질풍
LOCALS:2 = 검격
FOR LOCAL:1, 1, 3
	;判定用に数値を仕舞う。反射は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = BTL_CHK_SKILL_관통(ARG) && INRANGE(MAXBASE:LOCAL:(LOCALS:(LOCAL:1)), 0, 100) ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	LOCAL:3 = GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17 && CFLAG:LOCAL:무효플래그 ? 0 # LOCAL:3
	IF GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 4
		LOCAL:3 = CFLAG:LOCAL:물리반사플래그 ? -200 # LOCAL:3
	ELSEIF GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17
		LOCAL:3 = CFLAG:LOCAL:마법반사플래그 ? -200 # LOCAL:3
	ENDIF
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;검격

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_3604, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_3604, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_3604
RETURN 1

;　사거리
@SKILL_RANGE_3604,ARG
RETURN 2

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_3604,ARG
RETURN 2

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_3604
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_3604,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_3604
RETURN 22
;검격

;속도보정
@SKILL_SPEED_3604, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3604,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3604
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_3604
RETURN 0


;テンプレート用、追加効果シリーズ
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_3604,ARG
RETURN 19

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_3604,ARG
RETURN 7

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3604,ARG
RETURN 30

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_3604,ARG
RETURN 95

;====================================================
;스킬 및 행동의 실행
;====================================================

@ACTION_3604,ARG,ARG:1
#LOCALSIZE 4
LOCAL:3 = CFLAG:ARG:타겟
IF ARG:1 == 17
	LOCAL:1 = 1
	LOCAL:2 = 4
ELSEIF ARG:1 == 18
	LOCAL:1 = 4
	LOCAL:2 = 7
ELSEIF ARG:1 == 19
	LOCAL:1 = 1
	LOCAL:2 = 7
ELSEIF ARG:1 == 20
	LOCAL:1 = 7
	LOCAL:2 = 12
ELSEIF ARG:1 == 21
	LOCAL:1 = 12
	LOCAL:2 = 17
ELSEIF ARG:1 == 22
	LOCAL:1 = 7
	LOCAL:2 = 17
ELSEIF ARG:1 == 23
	LOCAL:1 = 1
	LOCAL:2 = 17
ENDIF
FOR LOCAL, LOCAL:1, LOCAL:2
	SIF POS(LOCAL) < 0
		CONTINUE
	SIF GET_STATE(CFLAG:POS(LOCAL):상태이상) == "DYING"
		CONTINUE
	CFLAG:ARG:타겟 = LOCAL
	CALL ATTACK_SINGLE, ARG, POS(LOCAL),3604,1
NEXT
CFLAG:ARG:타겟 = LOCAL:3
WAIT


;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_3604
PRINTL 재빠르게 연속으로 거합을 날린다
PRINTL 적 전열에 검격+질풍의 복합 상성으로 3회 공격+SLIP。크리티컬률이 높다

;계승에 필요한 부위
@계승부위_3604,ARG
RETURN 1
;とくにない

@SKILL_ACTIONABLE_BATTLE_3604
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3604,ARG
RETURN 1
;とくにない

