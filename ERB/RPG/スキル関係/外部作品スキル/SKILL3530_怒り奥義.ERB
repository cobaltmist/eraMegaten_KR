
;스킬名
@SKILL_NAME_3530,ARG
IF TFLAG:전투中 == 0
	RESULTS = 분노오의
ELSEIF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RESULTS = 아무베・야트로
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RESULTS = 에레르시・카무이・림센
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RESULTS = 레라・오・카무이
	;나찰
	ELSE
		RESULTS = 이메르・시키테
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RESULTS = 느베키・레라・시키테
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RESULTS = 알.레타라.카무이훔.마키리
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	RESULTS = 6태도 난무
	SIF SKILLGAGE_H_GET(ARG,3530) == 1
		RESULTS = 아오이
	SIF SKILLGAGE_H_GET(ARG,3530) == 2
		RESULTS = 백화요란
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	RESULTS = 왓카・시쿠테
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RESULTS = 루프시・카무이・에무시
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RESULTS = 아토이・시쿠테
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RESULTS = 루얀페・콘루・노치우
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RESULTS = 콘루・스투・포로
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	RESULTS = 염멸
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RESULTS = 협격화달마대돌진
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RESULTS = 극대재염
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RESULTS = 염사각성
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RESULTS = 삼천대천세계전초토
	ENDIF
ELSE
	RESULTS = 분노오의
ENDIF

;　명중
@SKILL_HITRATE_3530, ARG
RETURN 100

;　위력
@SKILL_POWER_3530, ARG
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 250
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 450
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 480
	;나찰
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 350
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 600
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 650
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 100
	SIF SKILLGAGE_H_GET(ARG,3530) == 1
		RETURN 720
	SIF SKILLGAGE_H_GET(ARG,3530) == 2
		RETURN 140
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 275
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 550
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 580
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 150
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 600
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 250
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 680
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 700
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 680
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 600
	ENDIF
ENDIF
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 8
	SIF SKILLGAGE_H_GET(ARG,3530) == 1
		RETURN 1
	SIF SKILLGAGE_H_GET(ARG,3530) == 2
		RETURN 6
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	;修羅
	;IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 12
	ENDIF
ENDIF
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 8
	SIF SKILLGAGE_H_GET(ARG,3530) == 1
		RETURN 1
	SIF SKILLGAGE_H_GET(ARG,3530) == 2
		RETURN 6
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	;修羅
	;IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 12
	ENDIF
ENDIF
RETURN 1

;一体辺りの최저ＨＩＴ数
@SKILL_MINATK_PER_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 3
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	;修羅
	;IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 5
	ENDIF
ENDIF
RETURN 1

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 4
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	;修羅
	;IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 6
	ENDIF
ENDIF
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_3530, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 28
	;나찰
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 28
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 25
	SIF SKILLGAGE_H_GET(ARG,3530) == 1
		RETURN 25
	SIF SKILLGAGE_H_GET(ARG,3530) == 2
		RETURN 25
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 20
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 28
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 28
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 25
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 22
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 25
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 25
	ENDIF
ENDIF
RETURN 25

;　相性
@SKILL_TYPE_3530,ARG
;#LOCALSSIZE 3
#LOCALSIZE 5
VARSET LOCALS
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 4
		IF SKILLGAGE_H_GET(ARG,3530) == 1
			LOCALS = 검격
			LOCALS:1 = 파마
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
			LOCALS = 검격
			LOCALS:1 = 질풍
			LOCALS:2 = 파마
		ENDIF
	;나찰
	ELSE
		IF SKILLGAGE_H_GET(ARG,3530) == 0
			LOCALS = 전술
			LOCALS:1 = 전격
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 1
			LOCALS = 검격
			LOCALS:1 = 전격
			LOCALS:2 = 충격
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
			LOCALS = 검격
			LOCALS:1 = 전격
			LOCALS:2 = 충격
			LOCALS:3 = 파마
		ENDIF
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	RETURN 0
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 13
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 5
		IF SKILLGAGE_H_GET(ARG,3530) == 2
			LOCALS = 빙결
			LOCALS:1 = 수격
		ENDIF
	;羅刹
	ELSE
		RETURN 5
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	RETURN 4
ENDIF
SIF LOCALS == ""
	RETURN 0
SIF ARG < 0 || 스킬속성표시中 || CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM(LOCALS)
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM(LOCALS)
LOCAL:2 = -300
FOR LOCAL:4, 0, 20
	IF LOCALS:(LOCAL:4) == ""
		LOCALS:(LOCAL:4) = %LOCALS%
		BREAK
	ENDIF
NEXT
FOR LOCAL:1, 1, LOCAL:4
	;판정用に수치を仕마이(동방)う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = BTL_CHK_SKILL_관통(ARG) && INRANGE(MAXBASE:LOCAL:(LOCALS:(LOCAL:1)), 0, 100) ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	LOCAL:3 = GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17 && CFLAG:LOCAL:무효플래그 ? 0 # LOCAL:3
	IF GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 4
		LOCAL:3 = CFLAG:LOCAL:물리반사플래그 ? -200 # LOCAL:3
	ELSEIF GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17
		LOCAL:3 = CFLAG:LOCAL:마법반사플래그 ? -200 # LOCAL:3
	ENDIF
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_3530, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_3530, ARG
LOCAL = SKILL_USE_CHARA()
IF SKILL_CHANGE(LOCAL,[[キャラ:리무루루]])
	;羅刹時の秘奥義のみ物理
	SIF SKILLGAGE_H_GET(LOCAL,3530) == 2 && SKILLGAGE_F_GET(LOCAL,[[스킬:분노폭발]]) == 1
		RETURN 1
	RETURN 2
ELSEIF SKILL_CHANGE(LOCAL,[[キャラ:카즈키]])
	;修羅
	IF SKILLGAGE_F_GET(LOCAL,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(LOCAL,3530) == 2
			RETURN 0
	;羅刹
	ELSE
	ENDIF
ENDIF
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_3530
RETURN 1

;　사거리
@SKILL_RANGE_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 3
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 2
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 2
	;나찰
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 2
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 2
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 2
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	RETURN 1
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 3
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 3
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 3
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 3
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 2
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	RETURN 1
ENDIF
RETURN 1

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 2
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 3
	;나찰
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 1
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 1
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 3
	SIF SKILLGAGE_H_GET(ARG,3530) == 1
		RETURN 1
	SIF SKILLGAGE_H_GET(ARG,3530) == 2
		RETURN 1
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 2
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 3
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 3
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 3
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 1
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	RETURN 1
ENDIF
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_3530
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3530,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3530
RETURN 23
;빙결

;속도보정
@SKILL_SPEED_3530, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_3530,ARG

;전투時이외に사용できるか
@SKILL_ACTIONABLE_FIELD_3530
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_3530
RETURN 23

@SKILL_RANKUP_3530
RETURN 0


;テン플레이트用、추가효과시리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 0
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 11
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 11
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 5
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 5
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 15
	ENDIF
	RETURN 18
ENDIF
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 0
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 5
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 5
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 5
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 5
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	RETURN 4
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 18
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 650
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 700
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 650
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 600
	ENDIF
ENDIF
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 0
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 65
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 80
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 35
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 80
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 40
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 45
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 50
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 60
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			RETURN 60
	ENDIF
ENDIF
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 0
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 100
	;羅刹
	ELSE
		RETURN 100
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 95
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	ELSE
	ENDIF
	RETURN 100
ENDIF
RETURN 0

;拡散공격用추가処理
@SKILL_SPREAD_3530,ARG
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SIF SKILLGAGE_H_GET(ARG,3530) == 0
			RETURN 1
	;나찰
	ELSE
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	SIF SKILLGAGE_H_GET(ARG,3530) == 0
		RETURN 1
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	;羅刹
	ELSE
		SIF SKILLGAGE_H_GET(ARG,3530) == 1
			RETURN 1
	ENDIF
ENDIF

;====================================================
;ヒット時데미지前処理
;====================================================
@SKILL_HIT_EFFECT_3530,ARG,ARG:1
IF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	IF SKILLGAGE_H_GET(ARG,3530) == 2 && SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1 && FLAG:확산플래그 < 1
		IF CFLAG:(ARG:1):마법반사플래그 > 0
			CFLAG:(ARG:1):마법반사플래그 = 0
			PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　魔法反射結界を破壊！
		ENDIF
		IF CFLAG:(ARG:1):무효플래그 > 0
			CFLAG:(ARG:1):무효플래그 = 0
			PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　万能以外無効化を破壊！
		ENDIF
		IF CFLAG:(ARG:1):화염무효화횟수 > 0
			CFLAG:(ARG:1):화염무효화횟수 = 0
			PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　火炎無効化回数を全て破壊！
		ENDIF
	ENDIF
	IF SKILLGAGE_H_GET(ARG,3530) > 0
		;ガードキルできるのは中心のみ
		IF CFLAG:(ARG:1):무효플래그 < 1 && CFLAG:(ARG:1):마법반사플래그 < 1 && FLAG:확산플래그 < 1
			LOCAL = CFLAG:(ARG:1):화염무효화횟수
			LOCAL:1 = SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0 ? 3 # 5
			IF LOCAL >= LOCAL:1
				CFLAG:(ARG:1):화염무효화횟수 = LOCAL - LOCAL:1
				IF CFLAG:(ARG:1):화염무효화횟수 == 0
					PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　火炎無効化回数を全て破壊！
				ELSE
					PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　火炎無効化回数を軽減
				ENDIF
			ELSE
				SIF CFLAG:(ARG:1):화염무효화횟수 > 0
					PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　火炎無効化回数を全て破壊！
				CFLAG:(ARG:1):화염무효화횟수 = 0
				IF CFLAG:(ARG:1):화염가드킬 < LOCAL:1
					CFLAG:(ARG:1):화염가드킬 = LOCAL:1 - LOCAL
					PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　{CFLAG:(ARG:1):화염가드킬}ターン、火炎への耐性を無効化！
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ENDIF




;====================================================
;데미지発動時
;====================================================
@SKILL_DAMAGE_EFFECT_3530,ARG,ARG:1
IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		IF SKILLGAGE_H_GET(ARG,3530) == 0 && CFLAG:(ARG:1):공격강화 > -16
			PRINT  & 공격저하
			CFLAG:(ARG:1):공격강화 = MAX(-16, CFLAG:(ARG:1):공격강화 - 4)
		ENDIF
	;나찰
	ELSE
		IF SKILLGAGE_H_GET(ARG,3530) > 0
			PRINT  & 강화능력치해제
			FOR LOCAL,0,6
				CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화") = MIN(0, CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화"))
			NEXT
		ENDIF
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	;SIF SKILLGAGE_H_GET(ARG,3530) == 0
	;	RETURN 25
	IF SKILLGAGE_H_GET(ARG,3530) == 1
		PRINT  & 방어대폭저하
		CFLAG:(ARG:1):방어강화 = MAX(-16, CFLAG:(ARG:1):방어강화 - 8)
		FLAG:확산플래그 = 1
	ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
		FLAG:확산플래그 = 1
		FOR LOCAL,1,7
			IF SKILLGAGE_H_GETBIT(ARG,[[스킬:7의태도・유우초카]],LOCAL) == 0
				CALL YUCHOGE_SET,ARG,LOCAL
				BREAK
			ENDIF
		NEXT
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	IF SKILLGAGE_H_GET(ARG,3530) == 0
		CFLAG:(ARG:1):BS부착률강화 = MIN(4, CFLAG:(ARG:1):BS부착률강화 + 4)
		PRINT  & BS付着率UP
	;修羅
	ELSEIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		CFLAG:(ARG:1):공격강화 = MAX(-16, CFLAG:(ARG:1):공격강화 - 8)
		CFLAG:(ARG:1):마법위력강화 = MAX(-16, CFLAG:(ARG:1):마법위력강화 - 8)
		CFLAG:(ARG:1):명중강화 = MAX(-16, CFLAG:(ARG:1):명중강화 - 8)
		CFLAG:(ARG:1):회피강화 = MAX(-16, CFLAG:(ARG:1):회피강화 - 8)
		PRINT  & 공격、마법위력、명중・回避大幅ダウン
	;羅刹
	ELSE
		IF SKILLGAGE_H_GET(ARG,3530) == 1
			LOCAL:1 = 0
			FOR LOCAL,0,6
				IF CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화") > 0
					CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화") = MAX(0, CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화") - 4)
					LOCAL:1 = 1
				ENDIF
			NEXT
			SIF LOCAL:1 > 0
				PRINT  & 強化能力値低下
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
			PRINT  & 強化能力値해제
			FOR LOCAL,0,6
				CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화") = MIN(0, CFLAG:(ARG:1):(GET_BATTLESTATUS(LOCAL) + "강화"))
			NEXT
		ENDIF
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	IF SKILLGAGE_H_GET(ARG,3530) == 0
	;修羅
	ELSEIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		IF SKILLGAGE_H_GET(ARG,3530) == 1
			CFLAG:(ARG:1):방어강화 = MAX(-16, CFLAG:(ARG:1):방어강화 - 8)
			CFLAG:(ARG:1):마법효과강화 = MAX(-16, CFLAG:(ARG:1):마법효과강화 - 8)
			PRINT  & 방어・魔法効果大幅ダウン
		ENDIF
	;羅刹
	ELSE
		IF SKILLGAGE_H_GET(ARG,3530) == 1
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
		ENDIF
	ENDIF
ENDIF

;====================================================
;行動による스킬게이지操作
;====================================================
@SKILLGAGE_CHARGE_3530,ARG,ARG:1
SIF FLAG:行動順2 < 0
	CALL SKILLGAGE_H_SET,ARG,3530,0

;====================================================
;스킬及び行動の実行
;====================================================

@ACTION_3530,ARG,ARG:1
#LOCALSIZE 5
#DIM STOCK,6

IF SKILL_CHANGE(ARG,[[キャラ:나코루루]])
	STOCK:0 = MAXBASE:ARG:공격
	STOCK:1 = CFLAG:ARG:공격강화
	MAXBASE:ARG:공격 = MAXBASE:ARG:마법위력
	CFLAG:ARG:공격강화 = CFLAG:ARG:마법위력강화
	;修羅
	IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		IF SKILLGAGE_H_GET(ARG,3530) == 0
			CALL ATTACK_SPREAD,ARG,ARG:1,3530
		ELSEIF SKILLGAGE_H_GET(ARG,3530) > 0
			CALL ATTACK_FIELD,ARG,ARG:1,3530
			;PT判定
			IF CFLAG:(ARG):PT플래그 == 0
				LOCAL:1 = 1
				LOCAL:2 = 7
				LOCAL:3 = 7
				LOCAL:4 = 17
			ELSE
				LOCAL:1 = 7
				LOCAL:2 = 17
				LOCAL:3 = 1
				LOCAL:4 = 7
			ENDIF
			;敵
			FOR LOCAL,LOCAL:1,LOCAL:2
				SIF POS(LOCAL) < 0
					CONTINUE
				CFLAG:POS(LOCAL):공격강화 = MAX(-16, CFLAG:POS(LOCAL):공격강화 - 4)
				CFLAG:POS(LOCAL):마법위력강화 = MAX(-16, CFLAG:POS(LOCAL):마법위력강화 - 4)
			NEXT
			PRINTFORML \@ LOCAL:1 == 7 ? 敵 # 味方 \@の攻撃力、魔法威力を下げた！
			PRINTL 
			;味方
			FOR LOCAL,LOCAL:3,LOCAL:4
				SIF POS(LOCAL) < 0
					CONTINUE
				CFLAG:POS(LOCAL):방어강화 = MIN(32, CFLAG:POS(LOCAL):방어강화 + 4)
				CFLAG:POS(LOCAL):마법효과강화 = MIN(32, CFLAG:POS(LOCAL):마법효과강화 + 4)
				SIF SKILLGAGE_H_GET(ARG,3530) == 2
					CALL ALL_HEAL, ARG, POS(LOCAL), 0
			NEXT
			PRINTFORMW \@ LOCAL:3 == 1 ? 아군 # 적 \@의 방어력、마법효과이 올랐다！
			CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
		ENDIF
	;나찰
	ELSE
		CALL ATTACK_SINGLE,ARG,POS(ARG:1),3530
		IF SKILLGAGE_H_GET(ARG,3530) == 0
			CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 4)
			PRINTFORMW %CALLNAME:ARG%의 마법위력이 올랐다！
		ELSE
			CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
		ENDIF
	ENDIF
	MAXBASE:ARG:공격 = STOCK:0
	CFLAG:ARG:공격강화 = STOCK:1
ELSEIF SKILL_CHANGE(ARG,[[キャラ:요시토라]])
	FLAG:확산플래그 = 0
	IF SKILLGAGE_H_GET(ARG,3530) == 0
		CALL ATTACK_RAND,ARG,ARG:1,3530
		SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
			CALL SIX_TATI_CHANGE(ARG)
	ELSEIF SKILLGAGE_H_GET(ARG,3530) == 1
		LOCAL = POS(ARG:1)
		LOCAL:1 = 0
		LOCAL:2 = 0
		
		IF (MAXBASE:LOCAL:검격 < 100 || MAXBASE:LOCAL:검격 == 999) && CFLAG:LOCAL:검격가드킬 < 1
			CFLAG:LOCAL:검격가드킬 = 1
			LOCAL:1 = 0
		ENDIF
		IF CFLAG:LOCAL:검격무효화횟수 > 0
			LOCAL:2 = CFLAG:LOCAL:검격무효화횟수
			CFLAG:LOCAL:검격무효화횟수 = 0
		ENDIF
		CALL ATTACK_SINGLE,ARG,LOCAL,3530,1
		SIF LOCAL:1
			CFLAG:LOCAL:검격가드킬 = 0
		SIF LOCAL:2
			CFLAG:LOCAL:검격무효화횟수 = LOCAL:2
		IF FLAG:확산플래그
			CFLAG:LOCAL:검격무효화횟수 -= 6
			IF CFLAG:LOCAL:검격무효화횟수 < 0
				CFLAG:LOCAL:검격가드킬 = -CFLAG:LOCAL:검격무효화횟수
				PRINTFORMW [{ARG:1}]%CALLNAME:LOCAL,20% >>>>> {-CFLAG:LOCAL:검격무효화횟수}턴、검격의 내성을 무효화！
				CFLAG:LOCAL:검격무효화횟수 = 0
			ELSE
				PRINTFORMW [{ARG:1}]%CALLNAME:LOCAL,20% >>>>> 검격무효화횟수감소！
			ENDIF
		ENDIF
	ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
		LOCAL = POS(ARG:1)
		LOCAL:1 = 0
		IF (MAXBASE:LOCAL:검격 < 100 || MAXBASE:LOCAL:검격 == 999) && CFLAG:LOCAL:검격가드킬 < 1
			CFLAG:LOCAL:검격가드킬 = 1
			LOCAL:1 = 0
		ENDIF
		CALL ATTACK_SINGLE,ARG,LOCAL,3530
		SIF LOCAL:1
			CFLAG:LOCAL:검격가드킬 = 0
	ENDIF
	SIF FLAG:확산플래그
		CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
	FLAG:확산플래그 = 0
ELSEIF SKILL_CHANGE(ARG,[[キャラ:리무루루]])
	IF SKILLGAGE_H_GET(ARG,3530) == 0
		CALL ATTACK_FIELD,ARG,ARG:1,3530
	;修羅
	ELSEIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		SELECTCASE ARG:1
		CASE 1 TO 6,17,18,19
			LOCAL = 22
		CASE 7 TO 16,20,21,22
			LOCAL = 19
		CASEELSE
			IF INRANGE(CPOS(ARG), 1, 6)
				LOCAL = 19
			ELSE
				LOCAL = 22
			ENDIF
		ENDSELECT
		
		CALL ATTACK_FIELD,ARG,ARG:1,3530
		SIF SKILLGAGE_H_GET(ARG,3530) == 2
			CALL HEAL_FIELD,ARG,LOCAL,3530
		CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
	;羅刹
	ELSE
		IF SKILLGAGE_H_GET(ARG,3530) == 1
			CALL ATTACK_RAND,ARG,ARG:1,3530
			CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
			
			LOCAL = POS(ARG:1)
			CALL RATE_ATTACK_SINGLE,ARG,LOCAL,3530,-2,1800
			
			STOCK:0 = MAXBASE:ARG:공격
			STOCK:1 = CFLAG:ARG:공격강화
			MAXBASE:ARG:공격 = MAXBASE:ARG:마법위력
			CFLAG:ARG:공격강화 = CFLAG:ARG:마법위력강화
			
			CALL ATTACK_SINGLE,ARG,LOCAL,3530
			
			MAXBASE:ARG:공격 = STOCK:0
			CFLAG:ARG:공격강화 = STOCK:1
			SIF CFLAG:LOCAL:現데미지 > 0
				CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
		ENDIF
	ENDIF
ELSEIF SKILL_CHANGE(ARG,[[キャラ:카즈키]])
	IF SKILLGAGE_H_GET(ARG,3530) == 0
		CALL ATTACK_SPREAD,ARG,ARG:1,3530
		IF BASE:ARG:ＨＰ > 0 && CFLAG:ARG:ステート != 15
			CFLAG:ARG:명중강화 = MIN(32, CFLAG:ARG:명중강화 + 4)
			CFLAG:ARG:회피강화 = MIN(32, CFLAG:ARG:회피강화 + 4)
			PRINTFORMW %CALLNAME:ARG%の命中・回避が上がった！
		ENDIF
	;修羅
	ELSEIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		FLAG:확산플래그 = 0
		LOCAL = POS(ARG:1)
		IF SKILLGAGE_H_GET(ARG,3530) == 1
			CALL ATTACK_SINGLE,ARG,LOCAL,3530
			SIF CFLAG:LOCAL:現데미지 > 0
				CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
			CALL RATE_ATTACK_SINGLE,ARG,LOCAL,3530,1,2000
			FOR LOCAL:1,0,6
				CFLAG:ARG:(GET_BATTLESTATUS(LOCAL:1) + "강화") = MAX(0, CFLAG:ARG:(GET_BATTLESTATUS(LOCAL:1) + "강화"))
			NEXT
			PRINTFORML %CALLNAME:ARG%の低下した能力が戻った！
			PRINT 更に
			IF HAVE_SKILL(ARG,[[스킬:재염]]) && SKILLGAGE_H_GET(ARG,[[스킬:재염]]) < 3
				CALL SKILLGAGE_H_SET,ARG,[[스킬:재염]],3
				PRINT 재염が最大になり、
			ENDIF
			CFLAG:ARG:집중플래그 = 1
			CFLAG:ARG:기합플래그 = 1
			PRINTW 次の物理攻撃と魔法攻撃の威力がアップした！
			CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
		ENDIF
	;羅刹
	ELSE
		IF SKILLGAGE_H_GET(ARG,3530) == 1
			CALL ATTACK_SPREAD,ARG,ARG:1,3530
		ELSEIF SKILLGAGE_H_GET(ARG,3530) == 2
			FLAG:확산플래그 = 0
			LOCAL = POS(ARG:1)
			CALL RATE_ATTACK_SINGLE,ARG,LOCAL,3530,1,1800
			SIF CFLAG:LOCAL:ステート != GET_STATE_NUM("DYING")
				CALL ATTACK_SINGLE,ARG,LOCAL,3530
		ENDIF
		IF HAVE_SKILL(ARG,[[스킬:재염]])
			CALL SKILLGAGE_D_SET,ARG,[[스킬:재염]],5
			PRINTW ５ターンの間、炎熱獄の데미지が増え、発生しやすくなった
		ENDIF
		CALL SKILLGAGE_D_SET,ARG,[[스킬:분노폭발]],0
	ENDIF
ENDIF
;====================================================
;実行内容ここまで
;====================================================

;====================================================
;추가コマンド(EXTRAの下)
;====================================================
@SKILL_SPECIAL_COMMAND_EXTRA_3530,ARG,LOCAL
#LOCALSIZE 1
CALL TRUE_SKILL_COST,ARG,3530
IF SKILLGAGE_D_GET(ARG,[[스킬:분노폭발]])
	PRINTFORML [{LOCAL}] 오의
	C:LOCAL = 13530
	LOCAL += 1
ENDIF
RETURN LOCAL
;====================================================
;コマンド実行
;====================================================
@INPUT_COM_13530,ARG
#LOCALSSIZE 1
#LOCALSIZE 3
#DIM STOCK
#DIM LCOUNT
#DIM LINE
LINE = LINECOUNT
VARSET LOCAL
STOCK = SKILLGAGE_H_GET(ARG,3530)
FOR LCOUNT,0,3
	CALL SKILLGAGE_H_SET,ARG,3530,LCOUNT
	CALL CHECK_ACTIONABLE,ARG,3530
	IF RESULT && (LCOUNT < 2 || (LCOUNT == 2 && (SKILLGAGE_H_GET(ARG,[[스킬:분노폭발]]) == 200 || 現HP割合(ARG) < 26)))
		LOCAL:LCOUNT = 1
		CALL SKILL_NAME_3530,ARG
		LOCALS = %RESULTS%
		CALL TRUE_SKILL_COST,ARG,3530
		PRINTFORML [{LCOUNT+1}] %LOCALS, 38, LEFT%{RESULT, 3}ＭＰ
	ENDIF
NEXT
DRAWLINE
PRINTL [0] ＣＡＮＣＥＬ
CALL INPUTINT(0,1,2,3)
IF RESULT > 0 && LOCAL:(RESULT - 1) > 0
	CALL SKILLGAGE_H_SET,ARG,3530,RESULT - 1
	CALL SELECT_SKILL_INPUT,ARG,3530
	SIF RESULT > -1
		RETURN 1
ELSEIF RESULT == 0
	CALL SKILLGAGE_H_SET,ARG,3530,STOCK
	RETURN -1
ENDIF
CLEARLINE LINECOUNT - LINE
RESTART

;　 설명文の표시
@SKILL_EXPLAIN_3530
#LOCALSIZE 1
IF CFLAG:(CHARANUM-1):PT플래그 || U < 0 || U >= CHARANUM
	;전투中じゃない時の스킬설명
	PRINTL 분노 상태가 되면 오의를 얻게 된다。커맨드의 오의에서 사용할 수 있다。
	PRINTL 또한 분노 폭발 또는 HP 25% 이하면 비오의(秘奥義)도 사용할 수 있게 된다。오의、비오의를 사용하면 분노 상태가 사라진다
ENDIF
IF SKILL_CHANGE(U,[[キャラ:나코루루]])
	IF CFLAG:(CHARANUM-1):PT플래그
		PRINTL 수라　　통상　화염확산공격+공격력저하
		PRINTL 　　　　오의　검격・파마 앞 1열 공격+적 공격력、마법위력저하+아군방어、마법효과상승
		PRINTL 　　　비오의　검격・파마・질풍전체공격+적 공격력、마법위력저하+아군방어、마법효과상승、전회복
		PRINTL 나찰　　통상　전술・전격전체공격+자신의 마법위력상승
		PRINTL 　　　　오의　검격・전격・충격전체공격+능력상승해제
		PRINTL 　　　비오의　검격・전격・충격・파마전체공격+능력상승해제
	;아수라
	ELSEIF SKILLGAGE_F_GET(U,[[스킬:분노폭발]]) == 0
		IF SKILLGAGE_H_GET(U,3530) == 0
			PRINTL 불의 새。신호와 함께 매의 부모가 급강하 공격을 가한다。
			PRINTL 단체와 주위에 화염 중 데미지+공격력저하。이 스킬은 마법 위력으로 데미지를 계산한다
		ELSEIF SKILLGAGE_H_GET(U,3530) == 1
			PRINTL 빛나는 신의 윤무。적전 일렬에 검격+파마의 특대 데미지。이 스킬은 마법 위력으로 데미지를 계산한다
			PRINTL 사용후、적의 공격、마법위력저하、아군의 방어、마법효과가 상승한다
		ELSEIF SKILLGAGE_H_GET(U,3530) == 2
			PRINTL 바람을 타는 신。적 전체에 검격+파마+질풍의 특대 데미지。이 스킬은 마법 위력으로 데미지를 계산한다
			PRINTL 사용후、적의 공격、마법위력저하、아군의 방어、마법효과상승、HP 전부 회복
		ENDIF
	;나찰
	ELSE
		IF SKILLGAGE_H_GET(U,3530) == 0
			PRINTL 번개의 송곳니。늑대 식루에 올라탄 상태에서、돌진한다。
			PRINTL 단체에 전술+전격의 대 데미지+자신의 마법위력상승。이 스킬은 마법 위력으로 데미지를 계산한다
		ELSEIF SKILLGAGE_H_GET(U,3530) == 1
			PRINTL 빛나는 바람의 송곳니。식루를 타고 돌진후、상대를 베면서 상승한다。
			PRINTL 단체에 검격+전격+충격의 초특대 데미지+능력상승해제。이 스킬은 마법 위력으로 데미지를 계산한다
		ELSEIF SKILLGAGE_H_GET(U,3530) == 2
			PRINTL 새하얀 천둥소리의 주머니칼。식루가 빠른 속도로 달리며 상대에게 칼로 찌른다
			PRINTL 단체에 검격+전격+충격+파마의 초특대 데미지+능력상승해제。이 스킬은 마법 위력으로 데미지를 계산한다
		ENDIF
	ENDIF
ELSEIF SKILL_CHANGE(U,[[キャラ:요시토라]])
	IF CFLAG:(CHARANUM-1):PT플래그
		PRINTL 　통상　적 전체에 랜덤으로 검격 상성으로 3～8회 공격한다
		PRINTL 　오의　결계、상성을 무시한 검격공격+방어대폭저하+검격내성파괴
		PRINTL 비오의　상성을 무시한 검격 6회 공격+공격 명중시마다 유초화(遊蝶華)가 쌓인다
	ELSEIF SKILLGAGE_H_GET(U,3530) == 0
		PRINTL 적 전체 랜덤으로 검격 상성으로 3～8회 공격한다
		SIF SKILLGAGE_F_GET(U,[[스킬:분노폭발]]) == 1
			PRINTL 모든 6칼 기술을 바꾼다
	ELSEIF SKILLGAGE_H_GET(U,3530) == 1
		PRINTL 적 단체에 검격 초특대 데미지+방어대폭저하、6턴 검격에 대한 내성을 무효화한다
		PRINTL 이 공격은 검격 무효결계、내성을 무시한다
	ELSEIF SKILLGAGE_H_GET(U,3530) == 2
		PRINTL 적 단체에 검격 상성의 소 데미지 를 6회 공격、공격시마다 유초화(遊蝶華) 게이지가 쌓인다
		PRINTL 이 공격은 내성을 무시한다
	ENDIF
ELSEIF SKILL_CHANGE(U,[[キャラ:리무루루]])
	IF CFLAG:(CHARANUM-1):PT플래그
		PRINTL 　　　　通常　水撃一列+敵異常状態耐性低下
		PRINTL 修羅　　奥義　氷結全体攻撃+麻痺+敵攻撃力、마법위력、명중・回避低下
		PRINTL 　　　秘奥義　빙결・水撃全体攻撃+凍結+敵攻撃力、마법위력、명중・回避低下+味方大回復
		PRINTL 羅刹　　奥義　氷結全体ランダム攻撃+凍結+上昇能力低下
		PRINTL 　　　秘奥義　敵単体氷結割合데미지+凍結+能力上昇해제→氷結데미지
	ELSEIF SKILLGAGE_H_GET(U,3530) == 0
		PRINTL 水を満たす　敵一列に、水撃属性で中威力の魔法攻撃
		PRINTL 추가효과：BS付着率上昇
		;実際は回復技なんだけどね、水撃属性のサブウェポンとしてね
	;修羅
	ELSEIF SKILLGAGE_F_GET(U,[[스킬:분노폭발]]) == 0
		IF SKILLGAGE_H_GET(U,3530) == 1
			PRINTL 凍る神の刀　氷の礫を投げると氷の花となって地面を滑っていき相手を巨大な氷塊に閉じ込めてしまう技。
			PRINTL 敵全体に、氷結属性で特大威力の魔法攻撃
			PRINTL 추가효과：氷結属性PALYZE、공격・마법위력・命中回避大幅低下
		ELSEIF SKILLGAGE_H_GET(U,3530) == 2
			PRINTL 海を満たす　氷の塊で大気を満たし、敵への攻撃と味方全体の体力回復を行う
			PRINTL 敵全体に、빙결+水撃の複合属性で特大威力の魔法攻撃し、味方全体を大回復する
			PRINTL 추가효과：氷結属性PALYZE、공격・마법위력・命中回避大幅低下
		ENDIF
	;羅刹
	ELSE
		IF SKILLGAGE_H_GET(U,3530) == 1
			PRINTL 氷の星の嵐　頭の上で複数の氷塊を渦巻かせて、相手に向かって飛ばす
			PRINTL 敵全体ランダムに、12回攻撃(1体5～6HIT)の氷結属性の魔法攻撃
			PRINTL 추가효과：氷結属性FREEZE、上昇能力低下
		ELSEIF SKILLGAGE_H_GET(U,3530) == 2
			PRINTL 大きい氷の棍棒　相手を全身氷漬けにして、氷で出来た巨大한마ーで殴り倒す
			PRINTL 敵単体に氷結属性で対象の最大HPの60％데미지(上限1800)を与え、高確率FREEZE付与、能力上昇を해제する。
			PRINTL その後、氷結属性の超特大데미지の物理攻撃する。この攻撃は魔法威力で데미지計算する
		ENDIF
	ENDIF
ELSEIF SKILL_CHANGE(U,[[キャラ:카즈키]])
	IF CFLAG:(CHARANUM-1):PT플래그
		PRINTL 　　　　通常　火炎拡散+炎上+自身の命中・回避上昇
		PRINTL 修羅　　奥義　火炎単体+화염가드킬+炎上+방어、魔法効果低下
		PRINTL 　　　秘奥義　火炎単体割合데미지+화염가드킬+炎上+自身に기합、集中、能力低下해제
		PRINTL 羅刹　　奥義　火炎拡散+화염가드킬+炎上+炎熱獄
		PRINTL 　　　秘奥義　火炎単体割合데미지+火炎壁破壊、ガードキル+即死+火炎攻撃+炎熱獄
	ELSEIF SKILLGAGE_H_GET(U,3530) == 0
		PRINTL 自身の偽物を爆発させて攻撃する
		PRINTL 敵単体と周囲に火炎属性の中威力の物理攻撃を行う
		PRINTL 使用後自身の命中・回避を上昇させる
	;修羅
	ELSEIF SKILLGAGE_F_GET(U,[[스킬:분노폭발]]) == 0
		IF SKILLGAGE_H_GET(U,3530) == 1
			PRINTL 炎の分身を作り出し、分身と同時に突進する
			PRINTL 敵単体に火炎属性の超特大威力の物理攻撃を行う
			PRINTL 추가효과：FLAME、방어・魔法威力大幅低下、3ターン화염가드킬
		ELSEIF SKILLGAGE_H_GET(U,3530) == 2
			PRINTL 재염を一度に最大まで発生させる技
			PRINTL 敵単体に火炎属性の最大HPの70％の割合데미지(上限2000)を行い
			PRINTL 使用後、재염が最大まで上がり、能力低下が해제され、기합と컨센트레이트状態になる
			PRINTL 추가효과：FLAME、3ターン화염가드킬
		ENDIF
	;羅刹
	ELSE
		IF SKILLGAGE_H_GET(U,3530) == 1
			PRINTL 自分を中心に太い火柱を発生させる技
			PRINTL 敵単体と周囲に火炎属性の超特大威力の物理攻撃を行う
			PRINTL 使用後、5ターンの間炎熱獄の데미지が増え、FLAME状態の敵がいなくても発動するようになる
			PRINTL 추가효과：FLAME、中心のみ5ターン화염가드킬
		ELSEIF SKILLGAGE_H_GET(U,3530) == 2
			PRINTL 突進して相手を掴みあげ、自分もとろも巨大な炎の柱に巻き込み焼き尽くす
			PRINTL 敵単体に火炎属性で対象の最大HPの60％데미지(上限1800)を与える。この攻撃はあらゆる火炎耐性、防壁を破壊する
			PRINTL その後、火炎属性の超特大데미지の物理攻撃する
			PRINTL 使用後、5ターンの間炎熱獄の데미지が増え、FLAME状態の敵がいなくても発動するようになる
			PRINTL 추가효과：火炎属性DYING、5ターン화염가드킬
		ENDIF
	ENDIF
ENDIF

;継承に필요한部位
@継承部位_3530,ARG
RETURN 1
;とくにない

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3530,ARG
RETURN 1
;とくにない

