;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_3585,ARG
RESULTS = 로스트파워

;　명중
@SKILL_HITRATE_3585, ARG
RETURN 100

;　威힘
@SKILL_POWER_3585, ARG
RETURN 235

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3585,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3585,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_3585, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_3585,ARG
RETURN 20

;　相性
@SKILL_TYPE_3585,ARG
RETURN 17
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_3585, ARG
RETURN 1
;魔法

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_3585, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_3585
RETURN 4

;　사거리
@SKILL_RANGE_3585,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_3585,ARG
RETURN 4

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_3585
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3585,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3585
RETURN 20
;만능

;속도보정
@SKILL_SPEED_3585, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_3585,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_3585
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_3585
RETURN 16

@継承不能_3585
RETURN 1

@SKILL_RANKUP_3585
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3585,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3585,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3585,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_3585,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3585,ARG,ARG:1
#LOCALSSIZE 2
#LOCALSIZE 3
#DIM LCOUNT,2
#DIM HACKING_D
#DIM HACKING_H
HACKING_D = SKILLGAGE_D_GET(ARG,3581)
HACKING_H = SKILLGAGE_H_GET(ARG,3581)
IF HACKING_D > 0 && HACKING_H > -1 && GET_STATE(CFLAG:HACKING_H:상태이상) != "DYING" && CPOS(HACKING_H) > 0
	ARG:1 = CPOS(HACKING_H)
	LOCAL:1 = BASE:HACKING_H:ＨＰ
	CALL ATTACK_SINGLE, ARG, HACKING_H,3585,1
	IF POS(ARG:1) > 0 && LOCAL:1 > BASE:POS(ARG:1):ＨＰ && GET_STATE(CFLAG:POS(ARG:1):상태이상) != "DYING"
		LCOUNT:0 = -1
		LCOUNT:1 = -1
		FOR LOCAL,0,2
			$LP
			LCOUNT:LOCAL = RAND:5
			SIF LCOUNT:LOCAL > 2
				LCOUNT:LOCAL ++
			SIF LOCAL == 1 && LCOUNT:0 == LCOUNT:1
				GOTO LP
			IF LCOUNT:LOCAL == 1
				CFLAG:POS(ARG:1):(GET_BATTLESTATUS(LCOUNT:LOCAL  ) + "강화") = MAX(-16, CFLAG:POS(ARG:1):(GET_BATTLESTATUS(LCOUNT:LOCAL  ) + "강화") - 4)
				CFLAG:POS(ARG:1):(GET_BATTLESTATUS(LCOUNT:LOCAL+2) + "강화") = MAX(-16, CFLAG:POS(ARG:1):(GET_BATTLESTATUS(LCOUNT:LOCAL+2) + "강화") - 4)
				LOCALS:LOCAL= 명중・회피
			ELSE
				CFLAG:POS(ARG:1):(GET_BATTLESTATUS(LCOUNT:LOCAL) + "강화") = MAX(-16, CFLAG:POS(ARG:1):(GET_BATTLESTATUS(LCOUNT:LOCAL) + "강화") - 4)
				LOCALS:LOCAL = %GET_BATTLESTATUS(LCOUNT:LOCAL)%
			ENDIF
		NEXT
		PRINTFORMW 　　　　　　　　　　　　　　　　　　　　　　　　　　　　　 　%조사처리(LOCALS, "과")% %조사처리(LOCALS:1, "를")% 저하시켰다
	ENDIF
ELSE
	PRINTW 실패했다！
ENDIF



;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_3585
PRINTL [후열] 해킹중인 적에게 만능속성 중 마법데미지.
PRINTL 무작위로 두가지 능력을 저하시킨다.

;継承に필요な部位
@継承部位_3585,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3585,ARG
;적にハッキング効果があると사용可能
SIF SKILLGAGE_D_GET(ARG,3581) > 0
	RETURN 1
RETURN 0
;とくにない

