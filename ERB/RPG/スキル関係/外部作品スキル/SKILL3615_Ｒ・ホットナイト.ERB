
;스킬名
@SKILL_NAME_3615,ARG
RESULTS = R・핫나이트

;　명중
@SKILL_HITRATE_3615, ARG
RETURN 100

;　위력
@SKILL_POWER_3615, ARG
RETURN 450

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3615,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3615,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_3615, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_3615,ARG
RETURN 25

;　相性
@SKILL_TYPE_3615,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시中
	RETURN GET_TYPE_NUM("비구")
SIF CFLAG:ARG:ターゲット > 16 || CFLAG:ARG:ターゲット < 1
	RETURN GET_TYPE_NUM("비구")
LOCAL = POS(CFLAG:ARG:ターゲット)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("비구")
	
LOCAL:2 = -300
LOCALS = 비구
LOCALS:1 = 지변
LOCALS:2 = 비구
FOR LOCAL:1, 1, 3
	;판정用に수치を仕마이(동방)う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = BTL_CHK_SKILL_관통(ARG) && INRANGE(MAXBASE:LOCAL:(LOCALS:(LOCAL:1)), 0, 100) ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	LOCAL:3 = GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17 && CFLAG:LOCAL:무효フラグ ? 0 # LOCAL:3
	IF GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 4
		LOCAL:3 = CFLAG:LOCAL:물리반사フラグ ? -200 # LOCAL:3
	ELSEIF GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17
		LOCAL:3 = CFLAG:LOCAL:마법반사フラグ ? -200 # LOCAL:3
	ENDIF
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;비구

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_3615, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_3615, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_3615
RETURN 1

;　사거리
@SKILL_RANGE_3615,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_3615,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_3615
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3615,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3615
RETURN 23
;비구

;속도보정
@SKILL_SPEED_3615, ARG = -1
RETURN -20

;　구상の표시とその타입
@SKILL_MESSAGE_3615,ARG


;전투時이외に사용できるか
@SKILL_ACTIONABLE_FIELD_3615
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_3615
RETURN 23

@SKILL_RANKUP_3615
RETURN 0

;テン플레이트用、추가효과시리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3615,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3615,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3615,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_3615,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3615,ARG,ARG:1
SIF MONEY:99 < 90 && CFLAG:ARG:PTフラグ > 0
	RETURN 0
	
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,3615

SIF CFLAG:ARG:PTフラグ > 0
	MONEY:99 -= 90

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_3615
PRINTL 적 일체에 마법형의 비구+지변 상성의 특대 데미지를 준다。발동이 늦다
PRINTL 사용시 레드 오브를 90 소비한다

;継承に필요한部位
@継承部位_3615,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3615,ARG
SIF MONEY:99 < 90
	RETURN 0
RETURN 1
;とくにない

