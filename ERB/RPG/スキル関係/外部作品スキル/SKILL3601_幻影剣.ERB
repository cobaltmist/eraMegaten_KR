;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_3601,ARG
RESULTS = 환영검
IF ARG > -1
	SIF SKILLGAGE_H_GET(ARG,3601) == 1
		RESULTS = 엔진환영검
	SIF SKILLGAGE_H_GET(ARG,3601) == 2
		RESULTS = 강습환영검
	SIF SKILLGAGE_H_GET(ARG,3601) == 3
		RESULTS = 오월비환영검
ENDIF
;　명중
@SKILL_HITRATE_3601, ARG
RETURN 100

;　위력
@SKILL_POWER_3601, ARG
RETURN 80

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3601,ARG
IF ARG > -1
	SIF SKILLGAGE_H_GET(ARG,3601) == 1
		RETURN 2
	SIF SKILLGAGE_H_GET(ARG,3601) == 2
		RETURN 2
	SIF SKILLGAGE_H_GET(ARG,3601) == 3
		RETURN 1
ENDIF
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3601,ARG
IF ARG > -1
	SIF SKILLGAGE_H_GET(ARG,3601) == 1
		RETURN 3
	SIF SKILLGAGE_H_GET(ARG,3601) == 2
		RETURN 4
	SIF SKILLGAGE_H_GET(ARG,3601) == 3
		RETURN 3
ENDIF
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_3601, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_3601,ARG
RETURN 45

;　相性
@SKILL_TYPE_3601,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시中
	RETURN GET_TYPE_NUM("만능")
SIF CFLAG:ARG:ターゲット > 16 || CFLAG:ARG:ターゲット < 1
	RETURN GET_TYPE_NUM("만능")
LOCAL = POS(CFLAG:ARG:ターゲット)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("만능")
	
LOCAL:2 = -300
LOCALS = 만능
LOCALS:1 = 비구
LOCALS:2 = 만능
FOR LOCAL:1, 1, 3
	;판정用に수치を仕마이(동방)う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	;LOCAL:3 = BTL_CHK_SKILL_관통(ARG) && INRANGE(MAXBASE:LOCAL:(LOCALS:(LOCAL:1)), 0, 100) ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	LOCAL:3 = GET_TYPE_NUM(LOCALS:(LOCAL:1)) < 17 && CFLAG:LOCAL:무효フラグ ? 0 # LOCAL:3
	SIF LOCAL:1 == 1
		LOCAL:3 = LOCAL:1 == 1 && CFLAG:LOCAL:물리반사フラグ ? -200 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_3601, ARG
RETURN 0
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_3601, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_3601
RETURN 4

;　사거리
@SKILL_RANGE_3601,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_3601,ARG
IF ARG > -1
	SIF SKILLGAGE_H_GET(ARG,3601) == 1
		RETURN 1
	SIF SKILLGAGE_H_GET(ARG,3601) == 2
		RETURN 1
	SIF SKILLGAGE_H_GET(ARG,3601) == 3
		RETURN 3
ENDIF
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_3601
RETURN 6

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3601,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3601
RETURN 22
;검격

;속도보정
@SKILL_SPEED_3601, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_3601,ARG


;전투時이외に사용できるか
@SKILL_ACTIONABLE_FIELD_3601
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_3601
RETURN 0


;テン플레이트用、추가효과시리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3601,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3601,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3601,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_3601,ARG
RETURN 0
;====================================================
;カウンター発動조건
;====================================================
@SKILL_COUNTER_ACTIONABLE_3601,ARG,ARG:1
SIF SKILLGAGE_D_GET(ARG,3601) < 1
	RETURN 0
SIF !GETBIT(CFLAG:ARG:受けフラグ,18)
	RETURN 0
SIF ARG:1 < 0 || GET_STATE(CFLAG:(ARG:1):ステート) == "DYING"
	RETURN 0
CALL SKILLGAGE_H_SET,ARG,3601,1
RETURN 1

;====================================================
;トリガー発動조건
;====================================================
@SKILL_TRIGGER_ACTIONABLE_3601,ARG,ARG:1
;自分じゃなければNG
SIF ARG != ARG:1
	RETURN 0
SIF SKILLGAGE_D_GET(ARG,3601) < 1
	RETURN 0
CALLFORM SKILL_EFECT_{CFLAG:ARG:입력行動}
SIF RESULT != 1 || CFLAG:ARG:ターゲット < 1
	RETURN 0
IF CFLAG:ARG:ターゲット < 17
	SIF POS(CFLAG:ARG:ターゲット) < 0 || GET_STATE(CFLAG:POS(CFLAG:ARG:ターゲット):ステート) == "DYING"
		RETURN 0
	CALL SKILLGAGE_H_SET,ARG,3601,2
	RESULT:1 = CFLAG:ARG:ターゲット
ELSEIF CFLAG:ARG:ターゲット < 20
	CALL SKILLGAGE_H_SET,ARG,3601,3
	RESULT:1 = 19
ELSE
	CALL SKILLGAGE_H_SET,ARG,3601,3
	RESULT:1 = 22
ENDIF
RETURN 1

;====================================================
;스킬及び行動の実行
;====================================================

@ACTION_3601,ARG,ARG:1
#LOCALSIZE 4
IF SKILLGAGE_D_GET(ARG,3601) == 0
	CALL SKILLGAGE_D_SET,ARG,3601,3
	PRINTFORML 3턴 환영검에 의한 반격과 추격이 발동하게 되었다
ELSEIF SKILLGAGE_H_GET(ARG,3601) == 1
	CALL ATTACK_SINGLE,ARG,POS(ARG:1),3601,1
ELSEIF SKILLGAGE_H_GET(ARG,3601) == 2
	CALL ATTACK_SINGLE,ARG,POS(ARG:1),3601,1
ELSEIF SKILLGAGE_H_GET(ARG,3601) == 3
	LOCAL:3 = CFLAG:ARG:ターゲット
	IF ARG:1 == 19
		LOCAL:1 = 1
		LOCAL:2 = 7
	ELSEIF ARG:1 == 22
		LOCAL:1 = 7
		LOCAL:2 = 17
	ENDIF
	FOR LOCAL, LOCAL:1, LOCAL:2
		SIF POS(LOCAL) < 0
			CONTINUE
		SIF GET_STATE(CFLAG:POS(LOCAL):ステート) == "DYING"
			CONTINUE
		CFLAG:ARG:ターゲット = LOCAL
		CALL ATTACK_SINGLE, ARG, POS(LOCAL),3601,1
	NEXT
	CFLAG:ARG:ターゲット = LOCAL:3
ENDIF
WAIT


CALL SKILLGAGE_H_SET,ARG,3601,0



;====================================================
;実行内容ここまで
;====================================================

;====================================================
;추가コマンド(EXTRAの下)
;====================================================
@SKILL_SPECIAL_COMMAND_EXTRA_3601,ARG,LOCAL
#LOCALSIZE 1
CALL CHECK_COST,ARG,3601
IF SKILLGAGE_D_GET(ARG,3601) == 0 && RESULT
	CALL TRUE_SKILL_COST,ARG,3601
	PRINTFORML [{LOCAL}] 환영검　MP　{RESULT}
	C:LOCAL = 13601
	LOCAL += 1
ENDIF
RETURN LOCAL
;====================================================
;コマンド実行
;====================================================
@INPUT_COM_13601,ARG
#LOCALSIZE 1
CALL TRUE_SKILL_COST,ARG,3601
PRINTFORML 환영검을 사용합니까？(소비MP　{RESULT})
CALL INPUT_YN
IF RESULT == 0
	LOCAL = CFLAG:ARG:입력行動
	CFLAG:ARG:입력行動 = 3601
	CALL PAY_COST, ARG, CFLAG:ARG:입력行動
	SETCOLOR 0x33ffcc
	PRINTFORML ┏>CHARA━━━━━━━━━━┓　┏>ACT━━━━━━━━━━━━━━━━━━┓
	PRINTFORM ┃[{CPOS(ARG),2}] %CALLNAME:ARG,21,LEFT%┃　┃
	CALLFORM SKILL_NAME_3601,ARG
	PRINTFORML %RESULTS,40,LEFT%┃
	PRINTFORMW ┗━━━━━━━━━━━━━┛　┗━━━━━━━━━━━━━━━━━━━━┛
	RESETCOLOR
	SWAP TARGET,ARG
	CALL ACTION_3601,TARGET
	CALL 구상呼び出し , "BATTLE_MESSAGE" , TARGET , TARGET
	SWAP TARGET,ARG
	CFLAG:ARG:입력行動 = LOCAL
ENDIF
RETURN -1

;　 설명文の표시
@SKILL_EXPLAIN_3601
PRINTL 커맨드로 사용할 수 있으며、발동시 3턴 동안 만능+비구의 복합 상성으로 반격、추격이 발동된다
PRINTL 반격:상대의 물리공격에 대해 환영검으로 반격한다
PRINTL 추격:자신의 행동으로 공격스킬을 발동하면 환영검으로 추격한다、범위와 공격 횟수가 변한다

;継承に필요한部位
@継承部位_3601,ARG
RETURN 1
;とくにない

@SKILL_ACTIONABLE_BATTLE_3601
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3601,ARG
RETURN 0
;とくにない

