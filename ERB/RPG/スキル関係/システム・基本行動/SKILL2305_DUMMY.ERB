;DUMMY,スキル名をTSTRと吐き出すだけのDUMMYコマンド
;TSTRは思考パターン内で代入して、メッセージが必要なら敵口上を利用するといいでしょう
;使い道？　それは発想次第
;
;例としてはプリンストーラスはリロードで、特殊弾を入れ替えたってメッセージがでて、次のターンに撃ってくる属性弾の予告をする
;で、実際のところ、撃ってくる特殊弾の指定なんかの管理は思考パターンでやったほうがいいんで、スキル側でアクション書く必要がほとんどない
;なんで専用スキル作ってもほとんど空スキルになるだけだし、それならこういうのつくっといてもいいよねという気分に

;스킬명
@SKILL_NAME_2305,ARG
IF ARG > -1 && CSTR:ARG:더미스킬명 != ""
	RESULTS = %CSTR:ARG:더미스킬명%
ELSE
	RESULTS = %TSTR%
ENDIF

;　명중
@SKILL_HITRATE_2305, ARG
RETURN 0

;　위력
@SKILL_POWER_2305, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2305,ARG
RETURN 0

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2305,ARG
RETURN 0

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2305, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_2305,ARG
RETURN 0

;　상성
@SKILL_TYPE_2305,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2305, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2305, ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2305
RETURN 4

;　사거리
@SKILL_RANGE_2305,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2305,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2305
RETURN 4

;계승타입
@SKILL_SUCCESSION_TYPE_2305
RETURN 0
;계승불가

;속도보정
@SKILL_SPEED_2305, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2305,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2305
RETURN 1

;스킬변화？용 랭크
@SKILL_RANK_2305
RETURN 0
;변화불가

;====================================================
;スキル及び行動の実行
;敵が実行する場合は何もしない
;性質上、変化は思考パターン内で処理して、敵口上でメッセージを出すといいでしょう
;====================================================
@ACTION_2305,ARG,ARG:1

;====================================================
;실행내용 여기까지
;====================================================
;====================================================
;전투중 이외에서의 실행
;====================================================
@ACTION_FIELD_2305,ARG,ARG:1
;　 설명문 표시
@SKILL_EXPLAIN_2305
PRINTL 보스 등의 특수행동용 더미 커맨드。
;사용조건
@SKILL_SPECIAL_ACTIONABLE_2305,ARG
RETURN 1
;とくにない

