
;스킬명
@SKILL_NAME_2316,ARG
RESULTS = 혼의일격

;　명중
@SKILL_HITRATE_2316, ARG
SIF ARG < 0
	RETURN 100
SIF CFLAG:ARG:소환예정캐릭터 < 1
	RETURN 100
RETURN (200*BASE:(CFLAG:ARG:소환예정캐릭터):ＭＰ)/(MAXBASE:(CFLAG:ARG:소환예정캐릭터):ＭＰ)

;　위력
@SKILL_POWER_2316, ARG
SIF ARG < 0
	RETURN 100
SIF CFLAG:ARG:소환예정캐릭터 < 1
	RETURN 100
RETURN 100+BASE:(CFLAG:ARG:소환예정캐릭터):ＨＰ

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2316,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2316,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2316, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_2316,ARG
RETURN 0

;　상성
@SKILL_TYPE_2316,ARG
SIF ARG >= 0	;スキル属性表示で落ちるのでガード
	CALLFORM 공격상성_{EQUIP:ARG:총},ARG
RETURN ARG >= 0 ? RESULT # 1
;銃に依存
;RETURN 1
;비구

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2316, ARG
RETURN 0
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2316, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2316
RETURN 1

;　사거리
@SKILL_RANGE_2316,ARG
CALLFORM 사거리_{EQUIP:ARG:총}
RETURN RESULT

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2316,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2316
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2316,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2316
RETURN 0
;계승불가

;속도보정
@SKILL_SPEED_2316, ARG = -1
SIF ARG == -1
	RETURN 0
TRYCCALLFORM 속도보정_{EQUIP:ARG:총}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2316,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2316
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2316
RETURN 0
;변화불가

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2316,ARG
TRYCCALLFORM 추가효과_{EQUIP:ARG:총}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2316,ARG
TRYCCALLFORM 추가효과상성_{EQUIP:ARG:총}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2316,ARG
TRYCCALLFORM 추가효과명중률_{EQUIP:ARG:총}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2316,ARG
TRYCCALLFORM 추가효과최대명중률_{EQUIP:ARG:총}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2316,ARG,ARG:1
IF ARG < 0 || ARG:1 < 0
	PRINTW 공격에 실패했다
PRINTFORMW {CFLAG:(CFLAG:ARG:소환예정캐릭터):PT플래그}을 {(CFLAG:ARG:소환예정캐릭터)}으로。
ELSEIF CFLAG:(CFLAG:ARG:소환예정캐릭터):PT플래그 == 1
	
	IF CFLAG:(CFLAG:ARG:소환예정캐릭터):PT플래그 > 0 && ABL:(CFLAG:ARG:소환예정캐릭터):종족 != 0
		BASE:ARG:ＭＡＧ -= MAXBASE:(CFLAG:ARG:소환예정캐릭터):￥
	ENDIF
	CALL SKILL_SPHERE_2316,ARG
	LOCAL = POS(ARG:1)
	CALL GUN_SINGLE,ARG,LOCAL,2316
	IF CFLAG:ARG:상태이상 < GET_STATE_NUM("DYING")
		CALL VAR_HP,CFLAG:ARG:소환예정캐릭터,-BASE:(CFLAG:ARG:소환예정캐릭터):ＨＰ,0
		CALL VAR_MP,CFLAG:ARG:소환예정캐릭터,-BASE:(CFLAG:ARG:소환예정캐릭터):ＭＰ,0
	ENDIF
ELSE
	PRINTW 공격에 실패했다
ENDIF



;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2316
PRINTL 예비 악마를 희생해 강력한 GUN공격을 행한다。


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2316,ARG
SIF EQUIP:ARG:총 == 0 || CFLAG:ARG:악마변신
	RETURN 0
SIF TALENT:ARG:서머너 < 5
	RETURN 0
IF BASE:ARG:ＭＡＧ < MAXBASE:(CFLAG:ARG:소환예정캐릭터):￥
	RETURN 0
ENDIF
RETURN 1
;サマナーLv5であること。

