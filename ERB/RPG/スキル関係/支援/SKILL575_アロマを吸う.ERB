;通常攻撃
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_575,ARG
IF SKILL_CHANGE(ARG, [[キャラ:타츠마]])
	RESULTS = 각무
ELSEIF SKILL_CHANGE(ARG, [[キャラ:쿄이치]])
	RESULTS = 화살 떨어뜨리기
ELSEIF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RESULTS = 아이우치
ELSEIF SKILL_CHANGE(ARG, [[キャラ:키사라기]])
	RESULTS = 백인반환
ELSEIF SKILL_CHANGE(ARG, [[キャラ:마리(마인학원)]])
	RESULTS = 아에토네앙아그라
ELSE
	RESULTS = 아로마를피운다
ENDIF

;　명중
@SKILL_HITRATE_575, ARG
RETURN 100

;　위력
@SKILL_POWER_575, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_575,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_575,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_575, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_575,ARG
RETURN 8

;　상성
@SKILL_TYPE_575,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_575, ARG
RETURN 2
;魔法

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_575, ARG
RETURN 0

;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_575
RETURN 3

;　사거리
@SKILL_RANGE_575,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_575,ARG
RETURN 4

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_575
RETURN 4

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_575,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_575
RETURN 5
;지원

@계승불능_575
RETURN 1

;속도보정
@SKILL_SPEED_575, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_575,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_575
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_575
RETURN 6



;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_575,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_575,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_575,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_575,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_575,ARG,ARG:1
#LOCALSIZE 3
;コストを支払う
;CALL PAY_COST,ARG,575

CFLAG:ARG:회피강화 = MIN(32, CFLAG:ARG:회피강화 + 16)

PRINTFORML %CALLNAME:ARG%의 회피율이 크게 높아졌다！






;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================


;====================================================
;실행내용 여기까지
;====================================================


;　 설명문 표시
@SKILL_EXPLAIN_575
IF U > 0 && U < CHARANUM && NO:U == ([[キャラ:타츠마]])
	PRINTL 갈고닦은 고무도의 후각으로、상대의 공격을 판별한다。
	PRINTL 사용자의 회피율을 최대까지 상승시킨다。
ELSEIF U > 0 && U < CHARANUM && NO:U == ([[キャラ:쿄이치]])
	PRINTL 갈고닦은 검술의 후각으로、상대의 공격을 판별한다。
	PRINTL 사용자의 회피율을 최대까지 상승시킨다。
ELSEIF U > 0 && U < CHARANUM && NO:U == ([[キャラ:코마키]])
	PRINTL 갈고닦은 궁술의 후각으로、상대의 공격을 판별한다。
	PRINTL 사용자의 회피율을 최대까지 상승시킨다。
ELSEIF U > 0 && U < CHARANUM && NO:U == ([[キャラ:키사라기]])
	PRINTL 갈고닦은 비수류의 후각으로、상대의 공격을 판별한다。
	PRINTL 사용자의 회피율을 최대까지 상승시킨다。
ELSEIF U > 0 && U < CHARANUM && NO:U == ([[キャラ:마리(마인학원)]])
	PRINTL 마술로 힘의 흐름을 읽어、상대의 공격을 판별한다。
	PRINTL 사용자의 회피율을 최대까지 상승시킨다。
ELSE
	PRINTL 사용자의 회피율을 최대까지 상승시킨다。
ENDIF

;계승에 필요한 부위
@계승부위_575,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_575,ARG
RETURN 1
;とくにない

