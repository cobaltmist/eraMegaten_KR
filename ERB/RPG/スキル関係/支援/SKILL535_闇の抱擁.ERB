;通常攻撃
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_535,ARG
RESULTS = 어둠의포옹

;　명중
@SKILL_HITRATE_535, ARG
RETURN 100

;　위력
@SKILL_POWER_535, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_535,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_535,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_535, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_535,ARG
RETURN 0

;　상성
@SKILL_TYPE_535,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_535, ARG
RETURN 1
;特技

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_535, ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_535
RETURN 4

;　사거리
@SKILL_RANGE_535,ARG
RETURN 4

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_535,ARG
RETURN 4

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_535
RETURN 4

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_535,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_535
RETURN 5
;지원

;속도보정
@SKILL_SPEED_535, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_535,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_535
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_535
RETURN 2


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_535,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_535,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_535,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_535,ARG
RETURN 0

@적전용_535
RETURN 1

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_535,ARG,ARG:1
;コストを支払う
;CALL PAY_COST,ARG,535

FOR LOCAL:1,0,6
	SIF CFLAG:ARG:(GET_BATTLESTATUS(LOCAL:1) + "강화") < 0
		CFLAG:ARG:(GET_BATTLESTATUS(LOCAL:1) + "강화") = 0
NEXT
PRINTFORMW %CALLNAME:ARG%의 낮춰진 능력이 원래대로 되돌아왔다！

CFLAG:ARG:방어강화 += 4
SIF CFLAG:ARG:방어강화 > 32
	CFLAG:ARG:방어강화 = 32
PRINTFORMW %CALLNAME:ARG%의 방어력이 높아졌다！

CFLAG:ARG:기합플래그 = 1
PRINTFORMW %ARG는% 다음 공격을 위해 힘을 모았다！

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================


;====================================================
;실행내용 여기까지
;====================================================


;　 설명문 표시
@SKILL_EXPLAIN_535
PRINTL 큰 기술을 위해 공격력을 모은다。또한 자신의 방어력을 상승시킨다。

;계승에 필요한 부위
@계승부위_535,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_535,ARG
RETURN 1
;とくにない

