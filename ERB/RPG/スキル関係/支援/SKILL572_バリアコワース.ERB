;通常攻撃
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_572,ARG
RESULTS = 배리어코와스


;　명중
@SKILL_HITRATE_572, ARG
RETURN 100

;　위력
@SKILL_POWER_572, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_572,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_572,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_572, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_572,ARG
RETURN 7

;　상성
@SKILL_TYPE_572,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_572, ARG
RETURN 2
;魔法

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_572, ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_572
RETURN 1

;　사거리
@SKILL_RANGE_572,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_572,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_572
RETURN 4

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_572,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_572
RETURN 5
;지원

;속도보정
@SKILL_SPEED_572, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_572,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_572
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_572
RETURN 2


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_572,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_572,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_572,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_572,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_572,ARG,ARG:1
#LOCALSIZE 4
;コストを支払う
;CALL PAY_COST,ARG,572
IF ARG:1 == 22
	LOCAL:1 = 7
	LOCAL:2 = 17
ELSE
	LOCAL:1 = 1
	LOCAL:2 = 7
ENDIF

FOR LOCAL,LOCAL:1,LOCAL:2
	SIF POS(LOCAL) < 0
		CONTINUE
	CFLAG:POS(LOCAL):BS무효화횟수 = 0
	FOR LOCAL:3 , 0 , FLAG:상태이상수
		CFLAG:POS(LOCAL):(GET_STATE(LOCAL:3) + "무효화횟수") = 0
	NEXT
NEXT
PRINTFORMW TARGET:\@ ARG:1 == 22  ? ENEMY # PARTY \@ >>>>>> 모든 배드스테이터스 무효화 상태를 해제！




;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================


;====================================================
;실행내용 여기까지
;====================================================


;　 설명문 표시
@SKILL_EXPLAIN_572
PRINTL 적의 즉사를 포함한 모든 상태이상 무효화를 해제한다.

;계승에 필요한 부위
@계승부위_572,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_572,ARG
RETURN 1
;とくにない

