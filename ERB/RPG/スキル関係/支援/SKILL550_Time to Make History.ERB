;通常공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_550,ARG
RESULTS = Time to Make History


;　명중
@SKILL_HITRATE_550, ARG
RETURN 100

;　위력
@SKILL_POWER_550, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_550,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_550,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_550, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_550,ARG
SIF ARG == -1
    RETURN 105
IF ABL:ARG:가창기능 == 0
    RETURN 105
ELSEIF ABL:ARG:가창기능 == 1
    RETURN 100
ELSEIF ABL:ARG:가창기능 == 2
    RETURN 95
ELSEIF ABL:ARG:가창기능 == 3
    RETURN 90
ELSEIF ABL:ARG:가창기능 == 4
    RETURN 85
ELSEIF ABL:ARG:가창기능 == 5
    RETURN 80
ELSEIF ABL:ARG:가창기능 == 6
    RETURN 75
ELSEIF ABL:ARG:가창기능 == 7
    RETURN 70
ELSEIF ABL:ARG:가창기능 == 8
    RETURN 65
ELSEIF ABL:ARG:가창기능 == 9
    RETURN 60
ELSE
    RETURN 55
ENDIF
    RETURN 105

;　상성
@SKILL_TYPE_550,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_550, ARG
RETURN 2
;EXTRA

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_550, ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_550
RETURN 2

;　사거리
@SKILL_RANGE_550,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_550,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_550
RETURN 4

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_550,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_550
RETURN 5
;지원

@계승불능_550
RETURN 1

;속도보정
@SKILL_SPEED_550, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_550,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_550
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_550
RETURN 22



;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_550,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_550,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_550,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_550,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_550,ARG,ARG:1
#LOCALSIZE 3
;コストを支払う
;CALL PAY_COST,ARG,550
IF CFLAG:(ARG):PT플래그 == 0
	LOCAL:1 = 7
	LOCAL:2 = 17
ELSE
	LOCAL:1 = 1
	LOCAL:2 = 7
ENDIF

FOR LOCAL,LOCAL:1,LOCAL:2
	SIF POS(LOCAL) < 0
		CONTINUE
	IF ABL:ARG:가창기능 == 10
		;4段階上昇
		CFLAG:POS(LOCAL):공격강화 = MIN(32, CFLAG:POS(LOCAL):공격강화 + 16)
		CFLAG:POS(LOCAL):마법위력강화 = MIN(32, CFLAG:POS(LOCAL):마법위력강화 + 16)
	ELSEIF ABL:ARG:가창기능 > 6
		;3段階上昇
		CFLAG:POS(LOCAL):공격강화 = MIN(32, CFLAG:POS(LOCAL):공격강화 + 12)
		CFLAG:POS(LOCAL):마법위력강화 = MIN(32, CFLAG:POS(LOCAL):마법위력강화 + 12)
	ELSEIF ABL:ARG:가창기능 > 3
		;2段階上昇
		CFLAG:POS(LOCAL):공격강화 = MIN(32, CFLAG:POS(LOCAL):공격강화 + 8)
		CFLAG:POS(LOCAL):마법위력강화 = MIN(32, CFLAG:POS(LOCAL):마법위력강화 + 8)
	ELSE
		;1段階上昇
		CFLAG:POS(LOCAL):공격강화 = MIN(32, CFLAG:POS(LOCAL):공격강화 + 4)
		CFLAG:POS(LOCAL):마법위력강화 = MIN(32, CFLAG:POS(LOCAL):마법위력강화 + 4)
	ENDIF
NEXT
IF ABL:ARG:가창기능 > 3
    PRINTFORMW \@ CFLAG:ARG:PT플래그 == 0 ? 적 # 아군 \@ 전체의 공격력・마법위력이 매우 크게 올라갔다!
ELSE
    PRINTFORMW \@ CFLAG:ARG:PT플래그 == 0 ? 적 # 아군 \@ 전체의 공격력・마법위력이 올라갔다!
ENDIF

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================


;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_550
;ステータス画面からはこっち表示
IF CFLAG:(CHARANUM-1):PT플래그 || U < 0 || U >= CHARANUM
	PRINTL 勝負をキメにいくノリノリの歌。攻撃力と魔法威力が上昇する。
	PRINTL 歌唱技能に応じてコストが低下し、効果が強化される。
	PRINTL 　
	PRINTL 歌唱技能Lv10　 ：味方の攻撃力・마법위력　４段階上昇
	PRINTL 歌唱技能Lv7以上：味方の攻撃力・마법위력　３段階上昇
	PRINTL 歌唱技能Lv4以上：味方の攻撃力・마법위력　２段階上昇
	PRINTL それ以外　　　 ：味方の攻撃力・마법위력　１段階上昇
;戦闘中はこっち表示
ELSE
	PRINTL [특수] 勝負をキメにいくノリノリの歌。
    PRINTL 아군 전체의 공격력・마법위력을 매우 크게 상승시킨다. 가창기능이 높을수록 필요한 MP가 감소한다.
	IF ABL:U:가창기능 == 10
		PRINTL 味方の攻撃力・魔法威力を４段階上昇。
	ELSEIF ABL:U:가창기능 > 6
		PRINTL 味方の攻撃力・魔法威力を３段階上昇。
	ELSEIF ABL:U:가창기능 > 3
		PRINTL 味方の攻撃力・魔法威力を２段階上昇。
	ELSE
		PRINTL 味方の攻撃力・魔法威力を１段階上昇。
	ENDIF
ENDIF

;계승에 필요한 부위
@계승부위_550,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_550,ARG
RETURN 1
;とくにない

