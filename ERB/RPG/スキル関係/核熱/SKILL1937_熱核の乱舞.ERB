;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_1937,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RESULTS = 의부「오릴리즈산」
ELSE
	RESULTS = 열핵의난무
ENDIF

;　명중
@SKILL_HITRATE_1937, ARG
RETURN 90

;　위력
;仕様上,１체相손なら1~3回になるのでダイン相当の320
;上限HIT数に引っかからなければ１체あたりの期待値は240
;スペカの方は횟수増やした分ちょっと弱体化。
@SKILL_POWER_1937, ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 140
ELSE
	RETURN 160
ENDIF

;최저がここで渋いのは同랭크にあたるマハ系が최대5HITしかできないから
;적総数の판정を全후열합계にできるという強みもあるので、ここで少し絞っといても総HIT数は割合稼げる…はず
;최저공격횟수
@SKILL_MINATTACKNUMBER_1937,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 3
ELSE
	RETURN 2
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_1937,ARG
RETURN 8

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_1937,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 4
ELSE
	RETURN 3
ENDIF

;一体辺りの최저ＨＩＴ数
@SKILL_MINATK_PER_1937,ARG
RETURN 0

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_1937, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_1937,ARG
RETURN 15

;　相性
@SKILL_TYPE_1937,ARG
RETURN GET_TYPE_NUM("핵열")
;핵열

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_1937, ARG
RETURN 1
;EXTRA

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_1937, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_1937
RETURN 1

;　사거리
@SKILL_RANGE_1937,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_1937,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_1937
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_1937,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_1937
RETURN GET_SUCCESSION_NUM("핵열")
;핵열

;속도보정
@SKILL_SPEED_1937, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_1937,ARG

;전투時이외に사용できるか
@SKILL_ACTIONABLE_FIELD_1937
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_1937
RETURN 7

@SKILL_RANKUP_1937
;랭크アップなし
RETURN 0


;テン플레이트用、추가효과시리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_1937,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_1937,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_1937,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_1937,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_1937,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,1937






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_1937
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:마리사]]
	PRINTL 천체를 본뜬 탄막을 자신의 주위에 선회시켜 핵열 상성의 데미지를 여러번 준다。
	PRINTL 키리사메 마리사의 스펠카드。
ELSE
	PRINTL 원자도 무너뜨리는 핵반응으로 적 전체에 랜덤하게 여러회 공격한다。
	;PRINTL 幻想郷でまだ伝わっていた、古い異能の一つ。
ENDIF



;継承に필요한部位
@継承部位_1937,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_1937,ARG
RETURN 1
;とくにない

