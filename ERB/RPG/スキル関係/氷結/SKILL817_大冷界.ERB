;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_817,ARG
RESULTS = 대냉계

;　명중
@SKILL_HITRATE_817, ARG
RETURN 90

;　위력
;仕様上,１체相손なら1~5回になるので 특大相当の420
;複数いる場合は최대値にひっかからなければ350が期待値。複数回なので凍結を取りやすい…はず
;릴리즈제일・니플헤임よりはきっと弱い
@SKILL_POWER_817, ARG
;氷結版데들리번
IF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RETURN 420
ELSE
	RETURN 140
ENDIF
;최저がここで渋いのは同랭크にあたるマハ系が최대5HITしかできないから
;적総数の판정を全후열합계にできるという強みもあるので、ここで少し絞っといても総HIT数は割合稼げる…はず
;최저공격횟수
@SKILL_MINATTACKNUMBER_817,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_817,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RETURN 1
ELSE
	RETURN 16
ENDIF
;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_817,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RETURN 1
ELSE
	RETURN 5
ENDIF

;一体辺りの최저ＨＩＴ数
@SKILL_MINATK_PER_817,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RETURN 1
ELSE
	RETURN 0
ENDIF

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_817, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_817,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RETURN 35
ELSE
	RETURN 44
ENDIF

;　相性
@SKILL_TYPE_817,ARG
RETURN GET_TYPE_NUM("빙결")
;화염

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_817, ARG
RETURN 1
;마법

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_817, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_817
RETURN 1

;　사거리
@SKILL_RANGE_817,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군전체
@SKILL_SPHERE_817,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_817
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_817,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_817
RETURN GET_SUCCESSION_NUM("빙결")

;속도보정
@SKILL_SPEED_817, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_817,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_817
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_817
RETURN 14

@SKILL_RANKUP_817
;랭크アップなし
RETURN 0


;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_817,ARG
RETURN GET_STATE_NUM("FREEZE")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_817,ARG
RETURN GET_TYPE_NUM("빙결")

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_817,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RETURN 30
ELSE
	RETURN 20
ENDIF

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_817,ARG
RETURN 95
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_817,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,817






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_817
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:미츠루]]
	PRINTL 적 전체에 빙결 속성으로 대위력의 의 공격을 준다。동결의 추가효과。
ELSE
	PRINTL [후열] 적 전체에 1～16회(1인 최대 5회) 빙결속성 소 마법데미지. (낮은 확률로 FREEZE)
ENDIF
;継承に필요한部位
@継承部位_817,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_817,ARG
RETURN 1
;とくにない

