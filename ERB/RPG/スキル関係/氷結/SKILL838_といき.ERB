;といき
;氷の乱舞より攻撃回数が少なく、威力が高い。

;스킬명
@SKILL_NAME_838,ARG
RESULTS = 한숨

;　명중
@SKILL_HITRATE_838, ARG
RETURN 110

;　위력
@SKILL_POWER_838, ARG
RETURN 210


;최저공격횟수
@SKILL_MINATTACKNUMBER_838,ARG
RETURN 2

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_838,ARG
RETURN 5

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_838,ARG
RETURN 3

;一体辺りの최저ＨＩＴ数
@SKILL_MINATK_PER_838,ARG
RETURN 0

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_838, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_838,ARG
RETURN 20

;　상성
@SKILL_TYPE_838,ARG
RETURN GET_TYPE_NUM("빙결")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_838, ARG
RETURN 1
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_838, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_838
RETURN 1

;　사거리
@SKILL_RANGE_838,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_838,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_838
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_838,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_838
RETURN GET_SUCCESSION_NUM("빙결")

@계승불능_838
RETURN 1

;속도보정
@SKILL_SPEED_838, ARG = -1
RETURN 1

;　구상의 표시와 그 타입
@SKILL_MESSAGE_838,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_838
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_838
RETURN 7

@SKILL_RANKUP_838
;랭크アップなし
RETURN 0


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_838,ARG
RETURN GET_STATE_NUM("FREEZE")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_838,ARG
RETURN GET_TYPE_NUM("빙결")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_838,ARG
RETURN 45

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_838,ARG
RETURN 95
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_838,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,838






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_838
PRINTL 적 전체를 빙결 상성으로 랜덤하게 ２～５회 공격한다。

;계승에 필요한 부위
@계승부위_838,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_838,ARG
RETURN 1
;とくにない

