;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_3,ARG
;팬텀が사용する場合G・슬래시리퍼に
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	RESULTS = G・슬래시리퍼
ELSE
	RESULTS = 회전베기
ENDIF
;　명중
@SKILL_HITRATE_3, ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 110
ELSE
	RETURN 100
ENDIF

;　威힘
@SKILL_POWER_3, ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 195
ELSE
	RETURN 185
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_3, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_3,ARG
RETURN 16

;　相性
@SKILL_TYPE_3,ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 1
ELSE
;검격
	RETURN 0
ENDIF

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_3, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_3, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_3
RETURN 1

;　사거리
@SKILL_RANGE_3,ARG
RETURN 4

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_3,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_3
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_3,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_3
RETURN 0
;검격

;속도보정
@SKILL_SPEED_3, ARG = -1
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 0
ELSE
	RETURN -1
ENDIF

;　구상の표시とその타입
@SKILL_MESSAGE_3,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_3
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_3
RETURN 8

@SKILL_RANKUP_3
IF FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	D:1 = 116
	RETURN 1
ENDIF
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_3,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_3,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_3,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,3






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_3
IF FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	PRINTL 등부 웨폰 랙에 장착된 대형 회전 날을 던져、적 전체에 비구상성의 중 데미지를 입힌다.
	PRINTL 정식명칭은 「그랑･슬래시리퍼」
ELSE
	PRINTL [전열] 적 전체에 검격속성 중 물리데미지.
ENDIF

;継承に필요な部位
@継承部位_3,ARG
SIF TALENT:ARG:무기
	RETURN 1
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3,ARG
RETURN 1
;とくにない

