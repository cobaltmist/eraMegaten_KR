;通常공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_9, ARG
;팬텀が사용する場合ジェット・마그나ムに
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]]) || FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	RESULTS = 제트・매그넘
;桐条미츠루Lv50이상。アルテミシア限定
ELSEIF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	RESULTS = 꾸・드르와
ELSEIF SKILL_CHANGE(ARG, [[キャラ:블레이드(X)]]) && EQUIP:ARG:악세사리 == 5349
	RESULTS = 번개찌르기・벽뢰
ELSE
	RESULTS = 번개찌르기
ENDIF

;　명중
@SKILL_HITRATE_9, ARG
RETURN 100

;　威힘
@SKILL_POWER_9, ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 210
ELSEIF SKILL_CHANGE(ARG, [[キャラ:블레이드(X)]]) && EQUIP:ARG:악세사리 == 5349
	RETURN 280
ELSE
	RETURN 225
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_9, ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_9, ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_9, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_9, ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 6
ELSEIF SKILL_CHANGE(ARG, [[キャラ:블레이드(X)]]) && EQUIP:ARG:악세사리 == 5349
	RETURN 9
ELSE
	RETURN 7
ENDIF

;　상성
@SKILL_TYPE_9, ARG
#LOCALSSIZE 3
#LOCALSIZE 4
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	;타격
	RETURN 2
;桐条미츠루Lv50이상。アルテミシア限定
ELSEIF SKILL_CHANGE(ARG, [[キャラ:미츠루]]) && BASE:ARG:LV >= 50 && EQUIP:ARG:장비페르소나 == 0 && DITEMTYPE:(CFLAG:ARG:초기페르소나넘버):페르소나("NO") == 3952
	;전격
	RETURN 6
ELSEIF SKILL_CHANGE(ARG, [[キャラ:블레이드(X)]]) && EQUIP:ARG:악세사리 == 5349
	SIF ARG < 0 || 스킬속성표시중
		RETURN GET_TYPE_NUM("검격")
	SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
		RETURN GET_TYPE_NUM("검격")
	LOCAL = POS(CFLAG:ARG:타겟)
	SIF LOCAL < 0 || CFLAG:LOCAL:마법반사플래그 || CFLAG:LOCAL:무효플래그
		RETURN GET_TYPE_NUM("검격")
	SIF LOCAL < 0 || CFLAG:LOCAL:물리반사플래그
		RETURN GET_TYPE_NUM("전격")
	LOCAL:2 = -1000
	LOCALS = 검격
	LOCALS:1 = 검격
	LOCALS:2 = 전격
	FOR LOCAL:1, 1, 3
		;判定用に数値を仕舞う。反射は-200扱い
		LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
		LOCAL:3 = !(HAVE_SKILL(ARG, [[스킬:영세라이도우]]) || HAVE_SKILL(ARG,2947)) && CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? -5 # LOCAL:3
		LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
		SELECTCASE LOCAL:1
			CASE 1
				LOCAL:3 = CFLAG:LOCAL:물리반사플래그 ? -200 # LOCAL:3
			CASE 2
				LOCAL:3 = CFLAG:LOCAL:마법반사플래그 ? -200 # LOCAL:3
			CASEELSE
		ENDSELECT
		IF LOCAL:3 > LOCAL:2
			LOCAL:2 = LOCAL:3
			LOCALS = %LOCALS:(LOCAL:1)%
		ENDIF
	NEXT
	RETURN GET_TYPE_NUM(LOCALS)
ELSE
	;검격
	RETURN 0
ENDIF

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_9, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_9, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_9
RETURN 1

;　사거리
@SKILL_RANGE_9,ARG
IF SKILL_CHANGE(ARG, [[キャラ:팬텀]]) || SKILL_CHANGE(ARG, [[キャラ:팻]])
	RETURN 1
ELSE
	RETURN 2
ENDIF

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_9, ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_9
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_9, ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_9
RETURN 0
;검격

;속도보정
@SKILL_SPEED_9, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_9, ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_9
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_9
RETURN 4

@SKILL_RANKUP_9
IF FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	;썬더피스트
	D:1 = 257
	;랭크アップ先は１つ
	RETURN 1
ENDIF
RETURN 0

;テンプレート用、추가효과시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_9, ARG
RETURN GET_STATE_NUM("SHOCK")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_9, ARG
RETURN 6

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_9, ARG
RETURN 30

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_9, ARG
RETURN 70

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_9, ARG, ARG:1
LOCAL = POS(ARG:1)
IF SKILL_CHANGE(ARG, [[キャラ:블레이드(X)]]) && EQUIP:ARG:악세사리 == 5349
	CALL SKILL_TYPE_9, ARG
	PRINT 상성판정：
	SELECTCASE RESULT
		CASE 0
			PRINT 검격
		CASE 6
			PRINT 전격
	ENDSELECT
	PRINTL ！
ENDIF
CALL ATTACK_SINGLE, ARG, LOCAL, 9






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_9
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:미츠루]]
	PRINTL 키리조 미츠루의 스킬.
	PRINTL 커맨드는 ←모으기→+AorB。
	PRINTL 파고들어 적 한 체를 검으로 찌르는 돌진기.
	;PRINTL 성능
	;PRINTL A는 발동이 빠르고, B는 돌진거리가 길다. B 및 SB판만 발동시에 레버를 ←나 →로 둔 채로 써서 돌진거리를 조절할 수 있다.
	;PRINTL A판은 잔기술부터 이어질수록 발동이 빠르다. 연속기나 기습에 써먹을 수 있다.
	;PRINTL B판은 발동이 느리지만 슬라이드 다운을 발생시키고, 레버 ← 입력판은 가드되어도 끝부분이라면 반격당하기 힘들어진다.
	;PRINTL 버튼을 누른 상태로 쓰면 모으기가 가능하고, 최대한으로 모으면 데미지가 상승한다.
	;PRINTL 상대를 올려치기로 히트하면 특정 상황에서는 추격이 가능하다. 『P4U2』에서는 피 페이탈카운터 속성이 붙어있다.
	;PRINTL SB판은 발동이 꽤 빠르고, 경직도 B판보다 짧다.
	;PRINTL 또 B판과 같이 공중에서 히트를 시킬 경우 상대를 슬라이드다운시키기 때문에 경직이 짧은 것에 의해 추격을 쉽게 가할 수 있다. 단발 데미지는 B판보다 낮다.
	;PRINTL 슈퍼 캔슬 가능.
ELSEIF FINDCHARA_M(U, [[キャラ:팬텀]], [[キャラ:팻]])
	PRINTL 왼팔 플라즈마 스택 세 개를 대전시켜 공격한다. 또한 대전하고 있으므로, 마비 효과를 기대할 수 있다.
	PRINTL 또한 게슈펜스트 계열기 중에는 업어치기 후에 이것을 넣는 컴뱃 패턴을 가진 기종도 존재한다.
ELSE
	PRINTL [후열] 손에 든 무기로 전열의 적 1체에 검격속성 중 물리데미지. (낮은 확률로 SHOCK)
	IF U > 0 && U < CHARANUM && NO:U == [[キャラ:블레이드(X)]] && EQUIP:U:악세사리 == 5349
		SETCOLOR 0x14ffd7
		PRINTL 벽색의 뇌정 장비중　위력강화／속성변경：검격＋전격
		RESETCOLOR
	ENDIF
ENDIF

;계승에 필요한 부위
@계승부위_9, ARG
SIF TALENT:ARG:무기
	RETURN 1
RETURN 0
;무기


;사용조건
@SKILL_SPECIAL_ACTIONABLE_9, ARG
RETURN 1
;とくにない

