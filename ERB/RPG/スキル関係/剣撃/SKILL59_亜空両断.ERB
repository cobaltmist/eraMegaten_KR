

;스킬名
@SKILL_NAME_59,ARG
RESULTS = 아공양단

;　명중
@SKILL_HITRATE_59, ARG
RETURN 100

;高クリティカル
@SKILL_CRITICAL_RATE_59, ARG
RETURN 20

;　위력
@SKILL_POWER_59, ARG
RETURN 600

;　최저공격횟수
@SKILL_MINATTACKNUMBER_59,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_59,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_59, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_59,ARG
RETURN 40

;　相性
@SKILL_TYPE_59,ARG
RETURN GET_TYPE_NUM("검격")
;검격

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_59, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_59, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_59
RETURN 1

;　사거리
@SKILL_RANGE_59,ARG
RETURN 1

;　범위　11명　21열　3전체　4　적아군전체
@SKILL_SPHERE_59,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_59
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_59,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_59
RETURN GET_SUCCESSION_NUM("검격")
;검격

;속도보정
@SKILL_SPEED_59, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_59,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_59
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_59
RETURN 15

@継承不能_59
RETURN 1

;テン플레이트用、추가효과シ리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_59,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_59,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_59,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_59,ARG
RETURN 0

;====================================================
;ヒット時데미지前処理
;====================================================
@SKILL_HIT_EFFECT_59,ARG,ARG:1
SIF FLAG:확산플래그 == 1
	PRINTFORML 　　TARGET:[{CPOS(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　상성을 관통했다！

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_59,ARG,ARG:1
LOCAL = POS(ARG:1)
A = 0
B = 0
C = 0
D = 0
FLAG:확산플래그 = 0

CALL 相性소질체크, ARG, GET_TYPE_NUM("검격"), 1, 1
IF RESULT == 1 && CFLAG:ARG:집중플래그 > 0
	IF (MAXBASE:LOCAL:검격 < 100 || MAXBASE:LOCAL:검격 == 999) && CFLAG:LOCAL:검격가드킬 < 1
		CFLAG:LOCAL:검격가드킬 = 1
		A = 1
	ENDIF
	IF CFLAG:LOCAL:물리반사플래그 == 1
		B = CFLAG:LOCAL:물리반사플래그
		CFLAG:LOCAL:물리반사플래그 = 0
	ENDIF
	IF CFLAG:LOCAL:무효플래그 > 0
		C = CFLAG:LOCAL:무효플래그
		CFLAG:LOCAL:무효플래그 = 0
	ENDIF
	IF CFLAG:LOCAL:검격무효화횟수 > 0
		D = CFLAG:LOCAL:검격무효화횟수
		CFLAG:LOCAL:검격무효화횟수 = 0
	ENDIF
	IF A + B + C + D > 0
		FLAG:확산플래그 = 1
		CFLAG:ARG:집중플래그 = 0
	ENDIF
ENDIF
CALL ATTACK_SINGLE,ARG,LOCAL,59

SIF A == 1
	CFLAG:LOCAL:검격가드킬 = 0
SIF B > 0
	CFLAG:LOCAL:물리반사플래그 = B
SIF C > 0
	CFLAG:LOCAL:무효플래그 = C
SIF D > 0
	CFLAG:LOCAL:검격무효화횟수 = D
FLAG:확산플래그 = 0

;====================================================
;実行内容ここまで
;====================================================

;　 설명문の표시
@SKILL_EXPLAIN_59
PRINTL [전열] 전열의 적 1체에 검격속성 궁극 물리데미지. 
PRINTL 검격 소질을 가지고 컨센트레이트 상태라면 내성을 관통한다.
;継承に필요한部位
@継承部位_59,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_59,ARG
RETURN 1
;とくにない

