;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_618,ARG
RESULTS = 독각


;　명중
@SKILL_HITRATE_618, ARG
RETURN 100

;　위력
@SKILL_POWER_618, ARG
RETURN 120

;　최저공격횟수
@SKILL_MINATTACKNUMBER_618,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_618,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_618, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_618,ARG
RETURN 20

;　相性
@SKILL_TYPE_618,ARG
RETURN 0

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_618, ARG
RETURN 1
;特技

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_618, ARG
RETURN 0


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_618
RETURN 4
;　사거리
@SKILL_RANGE_618,ARG
RETURN 1

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_618,ARG
RETURN 4

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_618
RETURN 4

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_618,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_618
RETURN 6
;방어

;속도보정
@SKILL_SPEED_618, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_618,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_618
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_618
RETURN 11

@継承不能_618
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_618,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_618,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_618,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_618,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_618,ARG,ARG:1

IF CFLAG:ARG:독각플래그 == 0
	CFLAG:ARG:독각플래그 = 2
	PRINTFORMW %조사처리(CALLNAME:ARG,"는")%는 약점을 대비했다!
ELSE
	PRINTFORMW 실패했다!
ENDIF


;====================================================
;実行内容ここまで
;====================================================

;====================================================
;전투중以外での実行
;====================================================


;====================================================
;実行内容ここまで
;====================================================


;　 説明文の표시
@SKILL_EXPLAIN_618
PRINTL [특수] 사용한 턴에 약점을 공격받는다면, 기합과 컨센트레이트 상태가 된다. 
PRINTL 연속으로 사용하면 실패한다.

;継承に필요な部位
@継承部位_618,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_618,ARG
RETURN 1
;とくにない