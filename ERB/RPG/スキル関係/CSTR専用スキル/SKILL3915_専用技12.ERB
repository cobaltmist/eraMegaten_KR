;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_3915,ARG
RESULTS = 전용기12
SIF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_NAME_%CSTR:ARG:전용기12%,ARG


;　명중
@SKILL_HITRATE_3915, ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_HITRATE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　위력
@SKILL_POWER_3915, ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_POWER_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;高クリティカル
@SKILL_CRITICAL_RATE_3915, ARG
RESULT = 5
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_CRITICAL_RATE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　吸収率
@SKILL_HP_ABSORBER_3915, ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_HP_ABSORBER_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_MINATTACKNUMBER_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_MAXATTACKNUMBER_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;一体辺りの最大ＨＩＴ数
@SKILL_MAXATK_PER_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCCALLFORM SKILL_MAXATK_PER_%CSTR:ARG:전용기12%,ARG
	CATCH
		TRYCALLFORM SKILL_MAXATTACKNUMBER_%CSTR:ARG:전용기12%,ARG
	ENDCATCH
ENDIF
RETURN RESULT

;一体辺りの最低ＨＩＴ数
@SKILL_MINATK_PER_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCCALLFORM SKILL_MINATK_PER_%CSTR:ARG:전용기12%,ARG
	CATCH
		TRYCCALLFORM SKILL_MAXATK_PER_%CSTR:ARG:전용기12%,ARG
		CATCH
			TRYCALLFORM SKILL_MAXATTACKNUMBER_%CSTR:ARG:전용기12%,ARG
		ENDCATCH
	ENDCATCH
ENDIF
RETURN RESULT

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_3915, ARG
RESULT = 3
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_COSTTYPE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT
;消費なし＝1

;　코스트량
@SKILL_COST_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_COST_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　상성
@SKILL_TYPE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_TYPE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_3915, ARG
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_DECIDE_TYPE_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_3915, ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_DAMAGETYPE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_3915
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_TARGET_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT

;　사거리
@SKILL_RANGE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_RANGE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_SPHERE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_3915
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_EFECT_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_3915,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_3915
LOCAL = SKILL_USE_CHARA()
RESULT = 23
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_SUCCESSION_TYPE_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT

;속도보정
@SKILL_SPEED_3915, ARG = -1
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_SPEED_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;　口上の表示とそのタイプ
@SKILL_MESSAGE_3915,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3915
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_ACTIONABLE_FIELD_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT

;스킬변화？용 랭크
@SKILL_RANK_3915
RETURN 0

@SKILL_RANKUP_3915
RETURN 0

@계승불능_3915
RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_ADDTIONAL_STATE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_ADDTIONAL_TYPE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_ADDTIONAL_BASE_RATE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_ADDTIONAL_MAX_RATE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;拡散攻撃用追加処理
@SKILL_SPREAD_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_SPREAD_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;====================================================
;トリガー発動条件
;====================================================
@SKILL_TRIGGER_ACTIONABLE_3915,ARG,ARG:1
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_TRIGGER_ACTIONABLE_%CSTR:ARG:전용기12%,ARG,ARG:1
ENDIF
RETURN RESULT

;====================================================
;カウンター発動条件
;====================================================
@SKILL_COUNTER_ACTIONABLE_3915,ARG,ARG:1
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_COUNTER_ACTIONABLE_%CSTR:ARG:전용기12%,ARG,ARG:1
ENDIF
RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
@SKILL_HIT_EFFECT_3915,ARG,ARG:1
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_HIT_EFFECT_%CSTR:ARG:전용기12%,ARG,ARG:1
ENDIF
RETURN RESULT

;====================================================
;ダメージ発動時
;====================================================
@SKILL_DAMAGE_EFFECT_3915,ARG,ARG:1
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_DAMAGE_EFFECT_%CSTR:ARG:전용기12%,ARG,ARG:1
ENDIF
RETURN RESULT

;====================================================
;行動によるスキルゲージ操作
;====================================================
@SKILLGAGE_CHARGE_3915,ARG,ARG:1
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILLGAGE_CHARGE_%CSTR:ARG:전용기12%,ARG,ARG:1
ENDIF
RETURN RESULT



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_3915,ARG,ARG:1
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM ACTION_%CSTR:ARG:전용기12%,ARG,ARG:1,3915
ENDIF

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
@ACTION_FIELD_3915,ARG,ARG:1
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM ACTION_FIELD_%CSTR:ARG:전용기12%,ARG,ARG:1,3915
ENDIF

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
@SKILL_SPECIAL_COMMAND_EXTRA_3915,ARG,LOCAL
#LOCALSIZE 1
CALL SKILL_SPECIAL_COMMAND_3915,ARG,LOCAL,"EXTRA_"
RETURN RESULT

;====================================================
;追加コマンド(MAGICの下)
;====================================================
@SKILL_SPECIAL_COMMAND_MAGIC_3915,ARG,LOCAL
#LOCALSIZE 1
CALL SKILL_SPECIAL_COMMAND_3915,ARG,LOCAL,"MAGIC_"
RETURN RESULT

;====================================================
;追加コマンド(一番下)
;====================================================
@SKILL_SPECIAL_COMMAND_3915,ARG,LOCAL,ARGS = ""
#LOCALSIZE 1
IF CHARANUM_CHECK(ARG)
	RESULT = LOCAL
	TRYCALLFORM SKILL_SPECIAL_COMMAND_%ARGS%%CSTR:ARG:전용기12%,ARG,LOCAL,3915
	LOCAL = RESULT
ENDIF
RETURN LOCAL

;PRINTFORML [{LOCAL}] 剣質変化
;C:LOCAL = 13915
;LOCAL += 1
;RETURN LOCAL

;====================================================
;コマンド実行
;====================================================
@INPUT_COM_13915,ARG
RESULT = -1
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM INPUT_COM_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
@SKILLGAGE_DISPLAY_3915,ARG
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILLGAGE_DISPLAY_%CSTR:ARG:전용기12%,ARG
ENDIF


;　説明文の表示
@SKILL_EXPLAIN_3915
LOCAL = SKILL_USE_CHARA()
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_EXPLAIN_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF

;계승에 필요한 부위
@계승부위_3915,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3915,ARG
RESULT = 0
IF CHARANUM_CHECK(ARG)
	TRYCALLFORM SKILL_SPECIAL_ACTIONABLE_%CSTR:ARG:전용기12%,ARG
ENDIF
RETURN RESULT

;ターゲット条件
@SKILL_SPECIAL_TARGET_3915,ARG
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_SPECIAL_TARGET_%CSTR:ARG:전용기12%,LOCAL,ARG
ENDIF
RETURN RESULT

;控えに使えるか
@SKILL_T_RESERVE_3915
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_T_RESERVE_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT

;어떤 배드스테이터스를 고칠수 있는가
@SKILL_CURE_STATE_3915,ARG
LOCAL = SKILL_USE_CHARA()
RESULT = 0
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM SKILL_CURE_STATE_%CSTR:LOCAL:전용기12%,LOCAL,ARG
ENDIF
RETURN RESULT

;この技を複数持てるか
@중복습득수_3915
LOCAL = SKILL_USE_CHARA()
RESULT = 1
IF CHARANUM_CHECK(LOCAL)
	TRYCALLFORM 중복습득수_%CSTR:LOCAL:전용기12%,LOCAL
ENDIF
RETURN RESULT
