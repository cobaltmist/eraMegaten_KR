;스킬명
@SKILL_NAME_偽り写し記す万象,ARG
RESULTS = 偽り写し記す万象

;　명중
@SKILL_HITRATE_偽り写し記す万象, ARG
RETURN 100

;　위력
@SKILL_POWER_偽り写し記す万象, ARG
#DIM SKILL_NUM
SKILL_NUM = PU_SKILLNUM_GET(ARG,"偽り写し記す万象")

IF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 1000
	RETURN 400
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 800
	RETURN 350
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 700
	RETURN 300
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 600
	RETURN 270
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 500
	RETURN 230
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 400
	RETURN 200
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 300
	RETURN 150
ELSEIF SKILLGAGE_H_GET(ARG,SKILL_NUM) >= 200
	RETURN 130
ELSE
	RETURN 100
ENDIF

;;クリティカル率
@SKILL_CRITICAL_RATE_偽り写し記す万象, ARG
RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_偽り写し記す万象, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_偽り写し記す万象, ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_偽り写し記す万象, ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_偽り写し記す万象,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_偽り写し記す万象,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_偽り写し記す万象,ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_偽り写し記す万象,ARG
RETURN 40

;　상성
@SKILL_TYPE_偽り写し記す万象,ARG
RETURN 17
;万能

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_偽り写し記す万象,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_偽り写し記す万象,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_偽り写し記す万象,ARG
RETURN 1

;　사거리
@SKILL_RANGE_偽り写し記す万象,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_偽り写し記す万象,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_偽り写し記す万象,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_偽り写し記す万象,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_偽り写し記す万象,ARG
;RETURN 23
;剣撃

;속도보정
@SKILL_SPEED_偽り写し記す万象, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_偽り写し記す万象,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_偽り写し記す万象, ARG = -1
RETURN 0

;没
;@SKILL_RANK_偽り写し記す万象
;@SKILL_RANKUP_偽り写し記す万象
;@계승불능_偽り写し記す万象

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_偽り写し記す万象,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_偽り写し記す万象,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_偽り写し記す万象,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_偽り写し記す万象,ARG
RETURN 0

;拡散攻撃用追加処理
;ブレスなどの拡散攻撃に使う
@SKILL_SPREAD_偽り写し記す万象,ARG
RETURN 1

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_偽り写し記す万象,ARG,ARG:1
;RETURN 0

;====================================================
;화조ンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_偽り写し記す万象,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_偽り写し記す万象,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_偽り写し記す万象,ARG,ARG:1

;====================================================
;戦闘開始時に行う処理
;====================================================
@SKILL_BATTLE_START_偽り写し記す万象,ARG,ARG:1
;常に100の状態を維持する
SIF SKILLGAGE_F_GET(ARG,PU_SKILLNUM_GET(ARG,"ノウブル・ファンタズム")) < 100
	CALL SKILLGAGE_F_SET,ARG,PU_SKILLNUM_GET(ARG,"ノウブル・ファンタズム"),100

;====================================================
;行動によるスキルゲージ操作
;====================================================
@SKILLGAGE_CHARGE_偽り写し記す万象,ARG,ARG:1
#DIM SKILL_NUM
SKILL_NUM = PU_SKILLNUM_GET(ARG,"偽り写し記す万象")

;ダメージを受けた又はカウンター、トリガー以外の攻撃を受けたら
;ゲージが増える
IF CFLAG:ARG:현데미지 > 0 || GETBIT(CFLAG:ARG:받음플래그,18) || GETBIT(CFLAG:ARG:받음플래그,19)
	LOCAL = CFLAG:ARG:현데미지
	CALL SKILLGAGE_H_ADD,ARG,SKILL_NUM,LOCAL
ELSE
ENDIF

SIF GET_STATE(CFLAG:ARG:상태이상) == "DYING"
	CALL SKILLGAGE_H_SET,ARG,SKILL_NUM,0


;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_偽り写し記す万象,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
SKILL_NUM = PU_SKILLNUM_GET(ARG,"偽り写し記す万象")

;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM
;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、全体)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;この宝具を使用したら蓄積ダメージを0に
CALL SKILLGAGE_H_SET,ARG,SKILL_NUM,0

;スキルゲージが100以上だと100にする
SIF SKILLGAGE_F_GET(ARG,PU_SKILLNUM_GET(ARG,"ノウブル・ファンタズム")) > 100
	CALL SKILLGAGE_F_SET,ARG,PU_SKILLNUM_GET(ARG,"ノウブル・ファンタズム"),100

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_偽り写し記す万象,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_偽り写し記す万象,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_偽り写し記す万象,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_偽り写し記す万象,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_偽り写し記す万象,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_偽り写し記す万象,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"偽り写し記す万象")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_偽り写し記す万象,ARG
PRINTL 『偽り写し記す万象（ヴェルグ・アヴェスター）』
PRINTFORML 自分が受けた傷を相手に与え返す、%CALLNAME:ARG%が使用する『原初の呪い』。
PRINTL 
PRINTL 敵全体に万能属性の物理小ダメージを与える。
PRINTL 現戦闘中の自身の被ダメージ量に応じて威力が上昇する。
PRINTL 
PRINTFORML %CALLNAME:ARG%は常に魔力放出可能状態を維持し、任意のタイミングで使用出来る。
PRINTL 蓄積された被ダメージ量はこの宝具を使用する、もしくは自身が戦闘不能になるとリセットされる。
PRINTL

;没
;@계승부위_偽り写し記す万象,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_偽り写し記す万象,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_偽り写し記す万象,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_偽り写し記す万象,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_偽り写し記す万象,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_偽り写し記す万象,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"偽り写し記す万象"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"偽り写し記す万象"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"偽り写し記す万象"),0

