;통상공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_콘서트·하루카,ARG
RESULTS = 콘서트·하루카

;　명중
@SKILL_HITRATE_콘서트·하루카, ARG
RETURN 100

;　위력
@SKILL_POWER_콘서트·하루카, ARG
SIF ARG == -1
      RETURN 400
IF ABL:ARG:가창기능 >= 10 && BASE:ARG:LV >= 95
      RETURN 800
ELSEIF ABL:ARG:가창기능 == 9 && BASE:ARG:LV >= 80
      RETURN 700
ELSEIF ABL:ARG:가창기능 == 8 && BASE:ARG:LV >= 70
      RETURN 600
ELSEIF ABL:ARG:가창기능 == 7 && BASE:ARG:LV >= 60
      RETURN 550
ELSEIF ABL:ARG:가창기능 == 6 && BASE:ARG:LV >= 50
      RETURN 500
ELSEIF ABL:ARG:가창기능 == 5 && BASE:ARG:LV >= 40
      RETURN 450
ELSEIF ABL:ARG:가창기능 == 4 && BASE:ARG:LV >= 30
      RETURN 400
ELSEIF ABL:ARG:가창기능 == 3 && BASE:ARG:LV >= 20
      RETURN 350
ELSEIF ABL:ARG:가창기능 == 2 && BASE:ARG:LV >= 10
      RETURN 300
ELSEIF ABL:ARG:가창기능 == 1 && BASE:ARG:LV >= 5
      RETURN 200
ELSE
      RETURN 100
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_콘서트·하루카,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_콘서트·하루카,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_콘서트·하루카, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_콘서트·하루카,ARG
SIF ARG == -1
      RETURN 40
IF ABL:ARG:가창기능 >= 10 && BASE:ARG:LV >= 95
      RETURN 100
ELSEIF ABL:ARG:가창기능 == 9 && BASE:ARG:LV >= 80
      RETURN 85
ELSEIF ABL:ARG:가창기능 == 8 && BASE:ARG:LV >= 70
      RETURN 75
ELSEIF ABL:ARG:가창기능 == 7 && BASE:ARG:LV >= 60
      RETURN 65
ELSEIF ABL:ARG:가창기능 == 6 && BASE:ARG:LV >= 50
      RETURN 60
ELSEIF ABL:ARG:가창기능 == 5 && BASE:ARG:LV >= 40
      RETURN 55
ELSEIF ABL:ARG:가창기능 == 4 && BASE:ARG:LV >= 30
      RETURN 45
ELSEIF ABL:ARG:가창기능 == 3 && BASE:ARG:LV >= 20
      RETURN 30
ELSEIF ABL:ARG:가창기능 == 2 && BASE:ARG:LV >= 10
      RETURN 20
ELSEIF ABL:ARG:가창기능 == 1 && BASE:ARG:LV >= 5
      RETURN 10
ELSE
      RETURN 0
ENDIF

;　상성
@SKILL_TYPE_콘서트·하루카,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_콘서트·하루카, ARG
RETURN 2
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_콘서트·하루카, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_콘서트·하루카,ARG
RETURN 1

;　사거리
@SKILL_RANGE_콘서트·하루카,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_콘서트·하루카,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_콘서트·하루카,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_콘서트·하루카,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
; @SKILL_SUCCESSION_TYPE_콘서트·하루카
; RETURN 17
;만능

;속도보정
@SKILL_SPEED_콘서트·하루카, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_콘서트·하루카,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_콘서트·하루카, ARG = -1
RETURN 0

;스킬변화？용 랭크
; @SKILL_RANK_콘서트·하루카
; RETURN 22

; @継承不能_콘서트·하루카
; RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_콘서트·하루카,ARG
RETURN GET_STATE_NUM("HAPPY")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_콘서트·하루카,ARG
RETURN 9

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_콘서트·하루카,ARG
RETURN 60

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_콘서트·하루카,ARG
RETURN 90
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_콘서트·하루카,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

SKILL_NUM = PU_SKILLNUM_GET(ARG,"콘서트·하루카")
CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_콘서트·하루카,ARG
PRINTL 적 전체에 만능 상성의 데미지를 주고 HAPPY하게 한다.
PRINTL 가창기능과 레벨에 따라 위력과 소비 MP가 늘어난다.
PRINTL 하루카가 노래하는 장소는 모두 콘서트 스테이지가 된다.

;계승에 필요한 부위
@계승부위_콘서트·하루카,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_콘서트·하루카,ARG
RETURN 1
;とくにない

