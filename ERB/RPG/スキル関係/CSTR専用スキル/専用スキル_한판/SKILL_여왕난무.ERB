;이전번호 : 2557

;스킬명
@SKILL_NAME_여왕난무,ARG
RESULTS = 여왕난무

;　명중
@SKILL_HITRATE_여왕난무, ARG
RETURN 110

;　위력
@SKILL_POWER_여왕난무, ARG
RETURN MAX(FLAG:확산플래그 - 10,1)

;　최저공격횟수
@SKILL_MINATTACKNUMBER_여왕난무,ARG
RETURN 6

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_여왕난무,ARG
RETURN 8

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_여왕난무, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_여왕난무,ARG
RETURN 24

;　상성
@SKILL_TYPE_여왕난무,ARG
#LOCALSSIZE 3
#LOCALSIZE 4

SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("파마")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("파마")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0 || CFLAG:LOCAL:물리반사플래그 || CFLAG:LOCAL:무효플래그
	RETURN GET_TYPE_NUM("파마")
LOCAL:2 = -300
LOCALS = 파마
LOCALS:1 = 만능
LOCALS:2 = 파마
FOR LOCAL:1, 1, 3
	;판정用に数値を仕舞う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_여왕난무,ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_여왕난무,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_여왕난무,ARG
RETURN 1

;　사거리
@SKILL_RANGE_여왕난무,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_여왕난무,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_여왕난무,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_여왕난무,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
; @SKILL_SUCCESSION_TYPE_여왕난무
; RETURN 13
;파마

;속도보정
@SKILL_SPEED_여왕난무, ARG = -1
RETURN 2

;　구상의 표시와 그 타입
@SKILL_MESSAGE_여왕난무,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_여왕난무
RETURN 0

;스킬변화？용 랭크
; @SKILL_RANK_여왕난무
; RETURN 22

; @SKILL_RANKUP_여왕난무
; ;랭크アップ先は二つ
; RETURN 0

; @継承不能_여왕난무
; RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_여왕난무,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_여왕난무,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_여왕난무,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_여왕난무,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_여왕난무,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
SKILL_NUM = PU_SKILLNUM_GET(ARG,"여왕난무")

FLAG:확산플래그 = MAXBASE:ARG:마력 + 20
CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM

FLAG:확산플래그 = 0


;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_여왕난무
PRINTL 적 １명에게 파마+만능 상성으로 ６～８회 공격. 마력에 따라 위력이 상승한다.
PRINTL 이세리아 퀸 Ethereal Queen의 전용기술

;계승에 필요한 부위
@계승부위_여왕난무,ARG
	RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_여왕난무,ARG
RETURN 1
;とくにない

