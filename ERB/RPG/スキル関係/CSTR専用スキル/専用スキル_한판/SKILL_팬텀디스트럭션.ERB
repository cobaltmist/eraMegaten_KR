;이전번호 : 2558

;스킬명
@SKILL_NAME_팬텀디스트럭션,ARG
RESULTS = 팬텀디스트럭션

;　명중
@SKILL_HITRATE_팬텀디스트럭션, ARG
RETURN 100

;　위력
@SKILL_POWER_팬텀디스트럭션, ARG
SIF ARG == -1
	RETURN 75
SIF FLAG:확산플래그
	RETURN 60
SIF CFLAG:ARG:보스플래그
	RETURN 130
RETURN 75

;　최저공격횟수
@SKILL_MINATTACKNUMBER_팬텀디스트럭션,ARG
RETURN 8

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_팬텀디스트럭션,ARG
RETURN 8

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_팬텀디스트럭션, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_팬텀디스트럭션,ARG
RETURN 70

;　상성
@SKILL_TYPE_팬텀디스트럭션,ARG
#LOCALSSIZE 3
#LOCALSIZE 4

SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("파마")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("파마")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0 || CFLAG:LOCAL:마법반사플래그 || CFLAG:LOCAL:무효플래그
	RETURN GET_TYPE_NUM("파마")
LOCAL:2 = -300
LOCALS = 파마
LOCALS:1 = 만능
LOCALS:2 = 파마
FOR LOCAL:1, 1, 3
	;판정用に数値を仕舞う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_팬텀디스트럭션, ARG
RETURN 2
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_팬텀디스트럭션, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_팬텀디스트럭션,ARG
RETURN 1

;　사거리
@SKILL_RANGE_팬텀디스트럭션,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_팬텀디스트럭션,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_팬텀디스트럭션,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_팬텀디스트럭션,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
; @SKILL_SUCCESSION_TYPE_팬텀디스트럭션
; RETURN 13
;파마

;속도보정
@SKILL_SPEED_팬텀디스트럭션, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_팬텀디스트럭션,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_팬텀디스트럭션
RETURN 0

;스킬변화？용 랭크
; @SKILL_RANK_팬텀디스트럭션
; RETURN 25

; @SKILL_RANKUP_팬텀디스트럭션
; RETURN 0

; @계승불능_팬텀디스트럭션
; RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_팬텀디스트럭션,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_팬텀디스트럭션,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_팬텀디스트럭션,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_팬텀디스트럭션,ARG
RETURN 0

;拡散공격用추가処理
@SKILL_SPREAD_팬텀디스트럭션,ARG
RETURN 1
;拡散距離:1

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_팬텀디스트럭션,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
; SKILL_NUM = PU_SKILLNUM_GET(ARG,"팬텀디스트럭션")

CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM


;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_팬텀디스트럭션
PRINTL 파마+만능 복합 상성으로 대상과 그 주변에 8회 데미지
PRINTL 이세리아 퀸 Ethereal Queen의 필살기술

;계승에 필요한 부위
@계승부위_팬텀디스트럭션,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_팬텀디스트럭션,ARG
RETURN 1
;とくにない




