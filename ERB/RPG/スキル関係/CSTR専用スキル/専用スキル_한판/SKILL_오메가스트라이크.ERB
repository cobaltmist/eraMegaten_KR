;악마用に新規作成
;스킬명
;원번호 5633


@SKILL_NAME_오메가스트라이크,ARG
RESULTS = 오메가스트라이크

;　명중
@SKILL_HITRATE_오메가스트라이크, ARG
SIF CFLAG:ARG:보스플래그
	RETURN 300
RETURN 150

;　위력
@SKILL_POWER_오메가스트라이크, ARG
RETURN 550

;クリティカル率
@SKILL_CRITICAL_RATE_오메가스트라이크, ARG
RETURN 15

;　최저공격횟수
@SKILL_MINATTACKNUMBER_오메가스트라이크,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_오메가스트라이크,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_오메가스트라이크, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_오메가스트라이크,ARG
RETURN 115

;　상성
@SKILL_TYPE_오메가스트라이크,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_오메가스트라이크, ARG
RETURN 2
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_오메가스트라이크, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_오메가스트라이크,ARG
RETURN 1

;　사거리
@SKILL_RANGE_오메가스트라이크,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_오메가스트라이크,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_오메가스트라이크,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_오메가스트라이크,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
; @SKILL_SUCCESSION_TYPE_오메가스트라이크
; RETURN 20
;만능

; @계승불능_오메가스트라이크
; RETURN 1

;속도보정
@SKILL_SPEED_오메가스트라이크, ARG = -1
RETURN 2

;　구상의 표시와 그 타입
@SKILL_MESSAGE_오메가스트라이크,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_오메가스트라이크, ARG = -1
RETURN 0

;스킬변화？용 랭크
; @SKILL_RANK_오메가스트라이크
; RETURN 20

; @SKILL_RANKUP_오메가스트라이크
; ;랭크アップ先は二つ
; RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_오메가스트라이크,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_오메가스트라이크,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_오메가스트라이크,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_오메가스트라이크,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_오메가스트라이크,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

SKILL_NUM = PU_SKILLNUM_GET(ARG,"오메가스트라이크")

LOCAL = POS(ARG:1)
A = 0

IF CFLAG:LOCAL:화염무효화횟수 < 1 && CFLAG:LOCAL:마법반사플래그 < 1 && CFLAG:LOCAL:무효플래그 < 1 && CFLAG:ARG:집중플래그 > 0
	IF BASE:LOCAL:화염 < 100 && BASE:LOCAL:화염 >= 0 && CFLAG:LOCAL:화염가드킬 < 1
		CFLAG:LOCAL:화염가드킬 = 1
		A = 1
	ENDIF
	SIF A > 0
		PRINTFORML [{LOCAL}]%CALLNAME:LOCAL,20% >>>>> 상성을 관통했다！
ENDIF
CALL ATTACK_SINGLE,ARG,LOCAL,718


SIF A == 1
	CFLAG:LOCAL:화염가드킬 = 0
	A = 0

IF CFLAG:LOCAL:빙결무효화횟수 < 1 && CFLAG:LOCAL:마법반사플래그 < 1 && CFLAG:LOCAL:무효플래그 < 1 && CFLAG:ARG:집중플래그 > 0
	IF BASE:LOCAL:빙결 < 100 && BASE:LOCAL:빙결 >= 0 && CFLAG:LOCAL:빙결가드킬 < 1
		CFLAG:LOCAL:빙결가드킬 = 1
		A = 1
	ENDIF
	SIF A > 0
		PRINTFORML [{LOCAL}]%CALLNAME:LOCAL,20% >>>>> 상성을 관통했다！
ENDIF
CALL ATTACK_SINGLE,ARG,LOCAL,825


SIF A == 1
	CFLAG:LOCAL:빙결가드킬 = 0
	A = 0

IF CFLAG:LOCAL:충격무효화횟수 < 1 && CFLAG:LOCAL:마법반사플래그 < 1 && CFLAG:LOCAL:무효플래그 < 1 && CFLAG:ARG:집중플래그 > 0
	IF BASE:LOCAL:충격 < 100 && BASE:LOCAL:충격 >= 0 && CFLAG:LOCAL:충격가드킬 < 1
		CFLAG:LOCAL:충격가드킬 = 1
		A = 1
	ENDIF
	SIF A > 0
		PRINTFORML [{LOCAL}]%CALLNAME:LOCAL,20% >>>>> 상성을 관통했다！
ENDIF
CALL ATTACK_SINGLE,ARG,LOCAL,1016


SIF A == 1
	CFLAG:LOCAL:충격가드킬 = 0
	A = 0

IF CFLAG:LOCAL:전격무효화횟수 < 1 && CFLAG:LOCAL:마법반사플래그 < 1 && CFLAG:LOCAL:무효플래그 < 1 && CFLAG:ARG:집중플래그 > 0
	IF BASE:LOCAL:전격 < 100 && BASE:LOCAL:전격 >= 0 && CFLAG:LOCAL:전격가드킬 < 1
		CFLAG:LOCAL:전격가드킬 = 1
		A = 1
	ENDIF
	SIF A > 0
		PRINTFORML [{LOCAL}]%CALLNAME:LOCAL,20% >>>>> 상성을 관통했다！
ENDIF
CALL ATTACK_SINGLE,ARG,LOCAL,908


SIF A == 1
	CFLAG:LOCAL:전격가드킬 = 0

;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_오메가스트라이크
PRINTL 화염, 빙결, 충격, 전격의 최고 마법을 적에게 4연속 강타한다.
PRINTL 컨센트레이트 발동중일 경우 반사제외 내성무시

;계승에 필요한 부위
@계승부위_오메가스트라이크,ARG
RETURN 1



;사용조건
@SKILL_SPECIAL_ACTIONABLE_오메가스트라이크,ARG
RETURN 1
;とくにない

