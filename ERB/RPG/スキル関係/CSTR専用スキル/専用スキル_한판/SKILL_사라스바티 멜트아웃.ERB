;스킬名
@SKILL_NAME_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RESULTS = 쿠르 키갈 이르칼라
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RESULTS = 쿠르 키갈 이르칼라
ELSEIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RESULTS = 카마 삼모하나
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RESULTS = 카마 삼모하나
ELSE
	RESULTS = 사라스바티・멜트아웃
ENDIF

;　명중
@SKILL_HITRATE_사라스바티・멜트아웃, ARG
RETURN 120

;　위력
@SKILL_POWER_사라스바티・멜트아웃, ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN 190
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 190
ELSE
	RETURN 80
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 3
ELSE
	RETURN 6
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 3
ELSE
	RETURN 10
ENDIF

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_사라스바티・멜트아웃, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN 55
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 55
ELSE
	RETURN 40
ENDIF

;　相性
@SKILL_TYPE_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN GET_TYPE_NUM("지변")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN GET_TYPE_NUM("지변")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RETURN GET_TYPE_NUM("충격")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN GET_TYPE_NUM("충격")
ELSE
	RETURN GET_TYPE_NUM("수격")
ENDIF


;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_사라스바티・멜트아웃, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_사라스바티・멜트아웃, ARG
IF (U > 0 && U < CHARANUM && NO:U == [[キャラ:카마]]) || ((FLAG:行動順1) > 0 && NO:(FLAG:行動順1) == [[キャラ:카마]])
	RETURN 2;魔法
ELSEIF (U > 0 && U < CHARANUM && NO:U == [[キャラ:사쿠라]]) || ((FLAG:行動順1) > 0 && NO:(FLAG:行動順1) == [[キャラ:사쿠라]])
	RETURN 2;魔法
ELSE
	RETURN 1;物理
ENDIF

;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_사라스바티・멜트아웃, ARG
RETURN 1

;　사거리	1:S 2:M 3:L
@SKILL_RANGE_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 3
ELSE
	RETURN 2
ENDIF

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_사라스바티・멜트아웃,ARG
IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 3
ELSE
	RETURN 1
ENDIF

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_사라스바티・멜트아웃, ARG
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_사라스바티・멜트아웃,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
; @SKILL_SUCCESSION_TYPE_사라스바티・멜트아웃
; RETURN 16
; ;만능

; @継承不能_사라스바티・멜트아웃
; RETURN 1

;속도보정
@SKILL_SPEED_사라스바티・멜트아웃, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_사라스바티・멜트아웃,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_사라스바티・멜트아웃,ARG
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_사라스바티・멜트아웃,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RETURN GET_STATE_NUM("CHARM")
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN GET_STATE_NUM("CHARM")
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_사라스바티・멜트아웃,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RETURN GET_TYPE_NUM("충격")
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN GET_TYPE_NUM("충격")
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_사라스바티・멜트아웃,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RETURN 80
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 80
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_사라스바티・멜트아웃,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	RETURN 95
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 95
RETURN 0

;스킬변화？用랭크
; @SKILL_RANK_사라스바티・멜트아웃
; RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_사라스바티・멜트아웃,ARG,ARG:1,SKILL_NUM
#LOCALSIZE 5
#DIM SKILL_NUM

SKILL_NUM = PU_SKILLNUM_GET(ARG,"사라스바티・멜트아웃")

IF SKILL_CHANGE(ARG, [[キャラ:에레쉬・키・갈]])
	CFLAG:ARG:공격강화 = MIN(32, CFLAG:ARG:공격강화 + 8)
	PRINTFORMW 공격력이 크게 올라갔다！
	CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	CFLAG:ARG:공격강화 = MIN(32, CFLAG:ARG:공격강화 + 8)
	PRINTFORMW 공격력이 크게 올라갔다！
	CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM
ELSEIF SKILL_CHANGE(ARG, [[キャラ:카마]])
	CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 8)
	PRINTFORMW 마법위력이 크게 올라갔다！
	LOCAL = POS(ARG:1)
	CALL ATTACK_SINGLE,ARG,LOCAL,SKILL_NUM
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 8)
	PRINTFORMW 마법위력이 크게 올라갔다！
	LOCAL = POS(ARG:1)
	CALL ATTACK_SINGLE,ARG,LOCAL,SKILL_NUM
ELSE
	LOCAL = POS(ARG:1)
	CALL ATTACK_SINGLE,ARG,LOCAL,SKILL_NUM

	IF ARG:1 == 19
		LOCAL:2 = 1
		LOCAL:3 = 7
	ELSE
		LOCAL:2 = 7
		LOCAL:3 = 17
	ENDIF

	FOR LOCAL,LOCAL:2,LOCAL:3
		SIF POS(LOCAL) < 0
			CONTINUE
		FOR LOCAL:1,0,6
			CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") = MIN(0, CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화"))
		NEXT
	NEXT
	PRINTFORMW \@ CFLAG:ARG:PT플래그 == 2 ? 적 # 아군 \@의 강화된 능력이 돌아왔다！
ENDIF

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_사라스바티・멜트아웃
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:에레쉬・키・갈]]
	PRINTL 적 전체에 지변 상성 3회 물리 데미지
	PRINTL 추가효과：고명중. 공격 전에 자신의 공격력을 크게 상승
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:린Fate]]
	PRINTL 적 전체에 지변 상성 3회 물리 데미지
	PRINTL 추가효과：고명중. 공격 전에 자신의 공격력을 크게 상승
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:카마]]
	PRINTL 적 1체에 충격 상성 6-10회 마법 소 데미지
	PRINTL 추가효과：고명중. 공격 전에 자신의 마법위력을 크게 상승. 초고확률 매료
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:사쿠라]]
	PRINTL 적 1체에 충격 상성 6-10회 마법 소 데미지
	PRINTL 추가효과：고명중. 공격 전에 자신의 마법위력을 크게 상승. 초고확률 매료
ELSE
	PRINTL 얼터에고 멜트릴리스의 보구
	PRINTL 본래는 대인보구가 아닌 일정한 문명을 쌓아올린 문명권에 쓰는 대대중(対大衆)、대계(対界)보구。
	PRINTL 적 1체에 수속성의 특대 데미지를 주고、강화 효과를 소거한다。
ENDIF

;継承に필요한部位
@継承部位_사라스바티・멜트아웃,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_사라스바티・멜트아웃,ARG
RETURN 1
