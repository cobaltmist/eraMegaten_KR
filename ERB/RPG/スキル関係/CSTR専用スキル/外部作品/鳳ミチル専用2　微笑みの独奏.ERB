;専用スキル用のテンプレート
;「微笑みの独奏」を別の名前に置き換えてください

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_微笑みの独奏,ARG
IF BASE:ARG:LV > 59
	RESULTS = 미치루에게 맡겨！
ELSE
	RESULTS = 미소의 독주
ENDIF
;　명중
@SKILL_HITRATE_微笑みの独奏, ARG
RETURN 100

;　위력
@SKILL_POWER_微笑みの独奏, ARG
RETURN 0

;;クリティカル率
;@SKILL_CRITICAL_RATE_微笑みの独奏, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_微笑みの独奏, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_微笑みの独奏,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_微笑みの独奏,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_微笑みの独奏,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_微笑みの独奏,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_微笑みの独奏, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_微笑みの独奏,ARG
IF BASE:ARG:LV > 59
	RETURN 70
ELSE
	RETURN 40
ENDIF
;　상성
@SKILL_TYPE_微笑みの独奏,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_微笑みの独奏,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_微笑みの独奏,ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_微笑みの独奏,ARG
IF BASE:ARG:LV > 59
	RETURN 2
ELSE
	RETURN 4
ENDIF
;　사거리
@SKILL_RANGE_微笑みの独奏,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_微笑みの独奏,ARG
IF BASE:ARG:LV > 59
	RETURN 1
ELSE
	RETURN 4
ENDIF

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_微笑みの独奏,ARG
RETURN 4

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_微笑みの独奏,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_微笑みの独奏,ARG
;RETURN 23
;剣撃

;속도보정
@SKILL_SPEED_微笑みの独奏, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_微笑みの独奏,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_微笑みの独奏, ARG = -1
RETURN 0

;没
;@SKILL_RANK_微笑みの独奏
;@SKILL_RANKUP_微笑みの独奏
;@계승불능_微笑みの独奏

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_微笑みの独奏,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_微笑みの独奏,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_微笑みの独奏,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_微笑みの独奏,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_微笑みの独奏,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_微笑みの独奏,ARG,ARG:1
;RETURN 0

;====================================================
;화조ンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_微笑みの独奏,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_微笑みの独奏,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_微笑みの独奏,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_微笑みの独奏,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_微笑みの独奏,ARG,ARG:1,SKILL_NUM
#LOCALSIZE 1
#DIM SKILL_NUM

IF BASE:ARG:LV > 59
	CFLAG:POS(ARG:1):타인기합플래그 = 1
	CFLAG:POS(ARG:1):타인집중플래그 = 1
	PRINTFORMW 　　TARGET:[{ARG:1,2}] %CALLNAME:POS(ARG:1),20,LEFT%　>>>>>>　다음의 물리공격과 마법공격의 위력 상승！
ENDIF

CFLAG:ARG:기합플래그 = 1
SIF HAVE_SKILL(ARG,2480)
	CFLAG:ARG:기합플래그 = 2

IF CFLAG:(ARG):PT플래그 == 0
	FOR LOCAL,7,17
		SIF POS(LOCAL) < 0
			CONTINUE
		SIF HAVE_SKILL(POS(LOCAL),2480)
			CFLAG:POS(LOCAL):기합플래그 = 2
	NEXT
ENDIF

CFLAG:ARG:베어내기플래그 = 1

PRINTFORMW %ARG는% 목표를 정하고、미소지었다。

;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、全体)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_微笑みの独奏,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_微笑みの独奏,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_微笑みの独奏,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_微笑みの独奏,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_微笑みの独奏,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_微笑みの独奏,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"微笑みの独奏")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_微笑みの独奏,ARG
IF BASE:ARG:LV > 59
	PRINTL 다음에 행하는 물리공격이 반드시 명중하고、위력이 150% 증가、크리티컬하기 쉬워진다。
	PRINTL 동시에 아군 1명의、다음 물리와 마법공격의 위력이 50% 상승한다。기합、집중플래그와 합산된다。
	PRINTL 아군 1명을 고무시키면서、표적을 정해 자신의 기합을 넣는다。
	
ELSE
	PRINTL 다음에 행하는 물리공격이 반드시 명중하고、위력이 150% 증가、크리티컬하기 쉬워진다。
	PRINTL 미소지으며 무기를 높이 치켜들어、기합을 넣는다。
ENDIF

;没
;@계승부위_微笑みの独奏,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_微笑みの独奏,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_微笑みの独奏,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_微笑みの独奏,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_微笑みの独奏,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_微笑みの独奏,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"微笑みの独奏"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"微笑みの独奏"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"微笑みの独奏"),0

