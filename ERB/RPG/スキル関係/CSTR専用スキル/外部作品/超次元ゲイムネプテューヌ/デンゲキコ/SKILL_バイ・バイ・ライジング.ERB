;스킬명
@SKILL_NAME_バイ・バイ・ライジング,ARG
RESULTS = バイ・バイ・ライジング

;　명중
@SKILL_HITRATE_バイ・バイ・ライジング, ARG
#DIM ACC
ACC = 90
SIF ARG < 0
	RETURN ACC
SELECTCASE BASE:ARG:LV
	CASE IS >= 70
		ACC = 100
	CASE IS >= 35
		ACC = 95
ENDSELECT
RETURN ACC

;　위력
@SKILL_POWER_バイ・バイ・ライジング, ARG
#DIM L_POW
L_POW = 65
;195
SIF ARG < 0
	RETURN L_POW
SELECTCASE BASE:ARG:LV
	CASE IS >= 70
		L_POW = 130
		;390
	CASE IS >= 35
		L_POW = 90
		;270
ENDSELECT
RETURN L_POW

;クリティカル率
@SKILL_CRITICAL_RATE_バイ・バイ・ライジング, ARG
RETURN 10

;;　吸収率
;@SKILL_HP_ABSORBER_バイ・バイ・ライジング, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_バイ・バイ・ライジング,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_バイ・バイ・ライジング,ARG
RETURN 3

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_バイ・バイ・ライジング,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_バイ・バイ・ライジング,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_バイ・バイ・ライジング, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_バイ・バイ・ライジング,ARG
#DIM L_COST
L_COST = 17
SIF ARG == -1
	RETURN L_COST
SELECTCASE BASE:ARG:LV
	CASE IS >= 70
		L_COST = 49
	CASE IS >= 35
		L_COST = 28
ENDSELECT
RETURN L_COST

;　상성
@SKILL_TYPE_バイ・バイ・ライジング,ARG
RETURN 6

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_バイ・バイ・ライジング,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_バイ・バイ・ライジング,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_バイ・バイ・ライジング,ARG
RETURN 1

;　사거리
@SKILL_RANGE_バイ・バイ・ライジング,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_バイ・バイ・ライジング,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_バイ・バイ・ライジング,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_バイ・バイ・ライジング,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_バイ・バイ・ライジング,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_バイ・バイ・ライジング, ARG = -1
RETURN 5

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_バイ・バイ・ライジング,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_バイ・バイ・ライジング, ARG = -1
RETURN 0

;没
;@SKILL_RANK_バイ・バイ・ライジング
;@SKILL_RANKUP_バイ・バイ・ライジング
;@계승불능_バイ・バイ・ライジング

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_バイ・バイ・ライジング,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_バイ・バイ・ライジング,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_バイ・バイ・ライジング,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_バイ・バイ・ライジング,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_バイ・バイ・ライジング,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_バイ・バイ・ライジング,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_バイ・バイ・ライジング,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_バイ・バイ・ライジング,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_バイ・バイ・ライジング,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_バイ・バイ・ライジング,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_バイ・バイ・ライジング,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

CALL SKILLGAGE_D_SET,ARG,SKILL_NUM,1
PRINTFORMW %CALLNAME:ARG%の回避率が一時的に上昇！
CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM
;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	1체攻撃
;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、전체)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_バイ・バイ・ライジング,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;1체回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、전체)
;CALL ALL_HEAL, POS(ARG:1), 0				;1체前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_バイ・バイ・ライジング,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_バイ・バイ・ライジング,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_バイ・バイ・ライジング,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_バイ・バイ・ライジング,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_バイ・バイ・ライジング,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"バイ・バイ・ライジング")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_バイ・バイ・ライジング,ARG
PRINTL 적 전체에 小ダメージの電撃物理攻撃を３回行う。発生が速く、クリティカル率がやや高い。
PRINTL 使用後、そのターンの間だけ物理・魔法回避率がかなり上がる。
PRINTL 使用者のレベルが高くなると、性能が強化され、消費コストが増加する。
PRINTL
PRINTL もっと先へとスピードアップしていきたいという思いを解き放つ。
;PRINTL ネクストレベルへとアクセルしていくという細やかな信念をその身で体現する。
PRINTL

;没
;@계승부위_バイ・バイ・ライジング,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_バイ・バイ・ライジング,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_バイ・バイ・ライジング,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_バイ・バイ・ライジング,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_バイ・バイ・ライジング,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_バイ・バイ・ライジング,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"バイ・バイ・ライジング"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"バイ・バイ・ライジング"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"バイ・バイ・ライジング"),0

;====================================================
;独自処理
;====================================================
;（予定）と付けられている方は仮置きの物。実際に採用されるかどうかはわからない。
@AUTO_PU_SKILL_バイ・バイ・ライジング,ARG,ARGS,ARG:1,ARG:2
#DIM SKILL_NUM
#DIM L_USER
;ARG = 攻撃者
;ARGS （ダメージ↑、被ダメージ↑、命中↑、回避↑、魔威↑、魔効↑、etc…）といった感じの奴
;ARG:1 = 防御者
;ARG:2 = etc枠。 今回では使用しない
;文字列を参考にする都合上、どの文字列を使うか事前に定めておいた方がいいかも
;CSTR:ARG:100 = EXTRA勢出典作品、『超次元ゲイムネプテューヌ』とでも書き込む（予定）
;CSTR:ARG:101 = EXTRA勢出典作品でどのキャラなのか、とりあえず『デンゲキコ』とでも書き込む（予定）
L_USER = -1
SIF (ARGS == "物理回避判定↑" || ARGS == "魔法回避判定↑") && CSTR:(ARG:1):101 == "デンゲキコ"
	L_USER = ARG:1
SIF L_USER == -1
	RETURN 0
SKILL_NUM = PU_SKILLNUM_GET(L_USER,"バイ・バイ・ライジング")
IF SKILLGAGE_D_GET(L_USER,SKILL_NUM) > 0
	SELECTCASE ARGS
		CASE "物理回避判定↑","魔法回避判定↑"
			RETURN 8
	ENDSELECT
ENDIF
RETURN 0
