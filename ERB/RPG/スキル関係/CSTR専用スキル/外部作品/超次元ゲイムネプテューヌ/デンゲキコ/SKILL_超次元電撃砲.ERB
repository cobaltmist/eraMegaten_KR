;스킬명
@SKILL_NAME_超次元電撃砲,ARG
RESULTS = 超次元電撃砲

;　명중
@SKILL_HITRATE_超次元電撃砲, ARG
#DIM ACC
ACC = 100
SIF ARG < 0
	RETURN ACC
SELECTCASE BASE:ARG:LV
	CASE IS >= 70
		ACC = 120
	CASE IS >= 35
		ACC = 110
ENDSELECT
RETURN ACC

;　위력
@SKILL_POWER_超次元電撃砲, ARG
#DIM L_POW
L_POW = 63
;252
SIF ARG < 0
	RETURN L_POW
SELECTCASE BASE:ARG:LV
	CASE IS >= 70
		L_POW = 100
		;400
	CASE IS >= 35
		L_POW = 82
		;328
ENDSELECT
RETURN L_POW

;;クリティカル率
;@SKILL_CRITICAL_RATE_超次元電撃砲, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_超次元電撃砲, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_超次元電撃砲,ARG
RETURN 4

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_超次元電撃砲,ARG
RETURN 4

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_超次元電撃砲,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_超次元電撃砲,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_超次元電撃砲, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_超次元電撃砲,ARG
#DIM L_COST
L_COST = 14
SIF ARG == -1
	RETURN L_COST
SELECTCASE BASE:ARG:LV
	CASE IS >= 70
		L_COST = 32
	CASE IS >= 35
		L_COST = 23
ENDSELECT
RETURN L_COST

;　상성
@SKILL_TYPE_超次元電撃砲,ARG
RETURN 6

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_超次元電撃砲,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_超次元電撃砲,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_超次元電撃砲,ARG
RETURN 1

;　사거리
@SKILL_RANGE_超次元電撃砲,ARG
RETURN 2

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_超次元電撃砲,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_超次元電撃砲,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_超次元電撃砲,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_超次元電撃砲,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_超次元電撃砲, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_超次元電撃砲,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_超次元電撃砲, ARG = -1
RETURN 0

;没
;@SKILL_RANK_超次元電撃砲
;@SKILL_RANKUP_超次元電撃砲
;@계승불능_超次元電撃砲

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_超次元電撃砲,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_超次元電撃砲,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_超次元電撃砲,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_超次元電撃砲,ARG
RETURN 0

;拡散攻撃用追加処理
;ブレスなどの拡散攻撃に使う
@SKILL_SPREAD_超次元電撃砲,ARG
RETURN 1

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_超次元電撃砲,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_超次元電撃砲,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_超次元電撃砲,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_超次元電撃砲,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_超次元電撃砲,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_超次元電撃砲,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM
;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	1체攻撃
;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、전체)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_超次元電撃砲,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;1체回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、전체)
;CALL ALL_HEAL, POS(ARG:1), 0				;1체前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_超次元電撃砲,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_超次元電撃砲,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_超次元電撃砲,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_超次元電撃砲,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_超次元電撃砲,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"超次元電撃砲")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_超次元電撃砲,ARG
PRINTL 적 1체를 中心に小ダメージの電撃物理攻撃を４回行う。후열 사용 가능。
PRINTL 使用者のレベルが高くなると、性能が強化され、消費コストが増加する。
PRINTL
PRINTL 選ばれし者だけが扱える断罪の電撃を放つ。メダルが重要かもしれない。
;PRINTL 定められた者にのみ使えるという捌きの雷撃を放つ。 コインが重要らしい。
PRINTL

;没
;@계승부위_超次元電撃砲,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_超次元電撃砲,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_超次元電撃砲,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_超次元電撃砲,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_超次元電撃砲,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_超次元電撃砲,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"超次元電撃砲"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"超次元電撃砲"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"超次元電撃砲"),0

