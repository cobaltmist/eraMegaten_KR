;専用スキル用のテンプレート
;「サンダーボルト（LT）」を別の名前に置き換えてください

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_サンダーボルト（LT）,ARG
RESULTS = 썬더볼트（LT）

;　명중
@SKILL_HITRATE_サンダーボルト（LT）, ARG
RETURN 100

;　위력
@SKILL_POWER_サンダーボルト（LT）, ARG
#DIM 水浸しフラグ
水浸しフラグ = PU_SKILLNUM_GET(ARG,"ファヤダーン")
SIF SKILLGAGE_D_GET(ARG,水浸しフラグ) > 0
	RETURN 400
RETURN 300

;;クリティカル率
;@SKILL_CRITICAL_RATE_サンダーボルト（LT）, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_サンダーボルト（LT）, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_サンダーボルト（LT）,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_サンダーボルト（LT）,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_サンダーボルト（LT）,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_サンダーボルト（LT）,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_サンダーボルト（LT）, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_サンダーボルト（LT）,ARG
RETURN 36

;　상성
@SKILL_TYPE_サンダーボルト（LT）,ARG
RETURN GET_TYPE_NUM("전격")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_サンダーボルト（LT）, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_サンダーボルト（LT）,ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_サンダーボルト（LT）,ARG
RETURN 1

;　사거리
@SKILL_RANGE_サンダーボルト（LT）,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_サンダーボルト（LT）,ARG
#DIM 水浸しフラグ
水浸しフラグ = PU_SKILLNUM_GET(ARG,"ファヤダーン")
SIF SKILLGAGE_D_GET(ARG,水浸しフラグ) > 0
	RETURN 3
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_サンダーボルト（LT）,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_サンダーボルト（LT）,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_サンダーボルト（LT）,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_サンダーボルト（LT）, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_サンダーボルト（LT）,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_サンダーボルト（LT）, ARG = -1
RETURN 0

;没
;@SKILL_RANK_サンダーボルト（LT）
;@SKILL_RANKUP_サンダーボルト（LT）
;@계승불능_サンダーボルト（LT）

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_サンダーボルト（LT）,ARG
RETURN GET_STATE_NUM("PALYZE")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_サンダーボルト（LT）,ARG
RETURN GET_TYPE_NUM("전격")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_サンダーボルト（LT）,ARG
RETURN 25

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_サンダーボルト（LT）,ARG
RETURN 95

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_サンダーボルト（LT）,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_サンダーボルト（LT）,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_サンダーボルト（LT）,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_サンダーボルト（LT）,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_サンダーボルト（LT）,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_サンダーボルト（LT）,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_サンダーボルト（LT）,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
#DIM 水浸しフラグ
水浸しフラグ = PU_SKILLNUM_GET(ARG,"ファヤダーン")

LOCAL:0 = MAXBASE:ARG:마법위력
LOCAL:1 = CFLAG:ARG:마법위력강화

MAXBASE:ARG:마법위력 = MAXBASE:ARG:공격
CFLAG:ARG:마법위력강화 += CFLAG:ARG:공격강화

	IF SKILLGAGE_D_GET(ARG,水浸しフラグ) > 0
		CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、全体)
	ELSE
		CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
	ENDIF

MAXBASE:ARG:마법위력 = LOCAL:0
CFLAG:ARG:마법위력강화 = LOCAL:1

;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_サンダーボルト（LT）,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_サンダーボルト（LT）,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_サンダーボルト（LT）,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_サンダーボルト（LT）,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_サンダーボルト（LT）,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_サンダーボルト（LT）,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"サンダーボルト（LT）")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_サンダーボルト（LT）,ARG
#DIM 水浸しフラグ
水浸しフラグ = PU_SKILLNUM_GET(ARG,"ファヤダーン")
PRINTL 『썬더볼트』
IF SKILLGAGE_D_GET(U,水浸しフラグ) > 0
	SETCOLOR 0x33ffcc
		PRINT 적 전체
	RESETCOLOR
		PRINT 에、전격 속성으로 
	SETCOLOR 0x33ffcc
		PRINT 특대위력
	RESETCOLOR
		PRINTL 의 마법공격을 １회 행하고、전격 속성의 마비를 부여한다。
		PRINTL 파야단 추가효과 발휘 중。
ELSE
	PRINTL 적 1체에 、전격 속성으로 대위력의 마법공격을 １회 행하고、전격 속성의 마비를 부여한다。
ENDIF
PRINTL 데미지 계산은『공격』의존이며、마카카쟈와 동시에 타루카쟈의 영향을 합산한다。
PRINTL 
PRINTL 번개를 떨어뜨려、적에게 공격을 행한다。루나리에의 특기 마법이며
PRINTL 상황에 따라서는 자신에게 떨어뜨려、포획하려고 했던 적을 뿌리치는 데도 사용하고 있었다。
PRINTL 창기사 루나리에의 마법술（CSTR전용）

;没
;@계승부위_サンダーボルト（LT）,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_サンダーボルト（LT）,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_サンダーボルト（LT）,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_サンダーボルト（LT）,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_サンダーボルト（LT）,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_サンダーボルト（LT）,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"サンダーボルト（LT）"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"サンダーボルト（LT）"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"サンダーボルト（LT）"),0
