;専用スキル用のテンプレート
;「ヤーデアングリフ」を別の名前に置き換えてください

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_ヤーデアングリフ,ARG
RESULTS = 야데 앙그리프

;　명중
@SKILL_HITRATE_ヤーデアングリフ, ARG
RETURN 100

;　위력
@SKILL_POWER_ヤーデアングリフ, ARG
SIF ARG == -1
	RETURN 50
IF BASE:ARG:LV < 10
	RETURN 60
ELSEIF BASE:ARG:LV < 20
	RETURN 70
ELSEIF BASE:ARG:LV < 30
	RETURN 80
ELSEIF BASE:ARG:LV < 40
	;3回攻撃
	RETURN 100
ELSE
	;3回攻撃
	RETURN 100
ENDIF
;;クリティカル率
@SKILL_CRITICAL_RATE_ヤーデアングリフ, ARG
RETURN 5

;;　吸収率
@SKILL_HP_ABSORBER_ヤーデアングリフ, ARG
SIF ARG == -1
	RETURN 15
IF BASE:ARG:LV < 10
	RETURN 20
ELSEIF BASE:ARG:LV < 20
	RETURN 25
ELSEIF BASE:ARG:LV < 30
	RETURN 30
ELSEIF BASE:ARG:LV < 40
	RETURN 30
ELSE
	RETURN 30
ENDIF
;　최저공격횟수
@SKILL_MINATTACKNUMBER_ヤーデアングリフ,ARG
;3回攻撃
RETURN 3


;　최대공격횟수
@SKILL_MAXATTACKNUMBER_ヤーデアングリフ,ARG
;3回攻撃
RETURN 3


;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_ヤーデアングリフ,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_ヤーデアングリフ,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_ヤーデアングリフ, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_ヤーデアングリフ,ARG
SIF ARG == -1
	RETURN 10
IF BASE:ARG:LV < 10
	RETURN 13
ELSEIF BASE:ARG:LV < 20
	RETURN 15
ELSEIF BASE:ARG:LV < 30
	;2回攻撃
	RETURN 20
ELSEIF BASE:ARG:LV < 40
	;3回攻撃
	RETURN 30
ELSE
	;3回攻撃
	RETURN 30
ENDIF

;　상성
@SKILL_TYPE_ヤーデアングリフ,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_ヤーデアングリフ,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_ヤーデアングリフ,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_ヤーデアングリフ,ARG
RETURN 1

;　사거리
@SKILL_RANGE_ヤーデアングリフ,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_ヤーデアングリフ,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_ヤーデアングリフ,ARG
RETURN 1


;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_ヤーデアングリフ,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_ヤーデアングリフ,ARG
;RETURN 23
;剣撃

;속도보정
@SKILL_SPEED_ヤーデアングリフ, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_ヤーデアングリフ,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_ヤーデアングリフ, ARG = -1
RETURN 0

;没
;@SKILL_RANK_ヤーデアングリフ
;@SKILL_RANKUP_ヤーデアングリフ
;@계승불능_ヤーデアングリフ

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_ヤーデアングリフ,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_ヤーデアングリフ,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_ヤーデアングリフ,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_ヤーデアングリフ,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_ヤーデアングリフ,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_ヤーデアングリフ,ARG,ARG:1
;RETURN 0

;====================================================
;화조ンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_ヤーデアングリフ,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_ヤーデアングリフ,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_ヤーデアングリフ,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_ヤーデアングリフ,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_ヤーデアングリフ,ARG,ARG:1,SKILL_NUM
#LOCALSIZE 3
#DIM SKILL_NUM

CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃


;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃



		

	
;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_ヤーデアングリフ,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_ヤーデアングリフ,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_ヤーデアングリフ,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_ヤーデアングリフ,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_ヤーデアングリフ,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_ヤーデアングリフ,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"ヤーデアングリフ")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_ヤーデアングリフ,ARG

PRINT 적 1체에、
SIF ARG == -1
	PRINTL 충격 상성의 물리 소 데미지를 3회　데미지의 15％를 흡수
IF BASE:ARG:LV < 10
	PRINTL 충격 상성의 물리 소 데미지를 3회　데미지의 20％를 흡수
ELSEIF BASE:ARG:LV < 20
	PRINTL 충격 상성의 물리 소 데미지를 3회　데미지의 25％를 흡수
ELSEIF BASE:ARG:LV < 30
	PRINTL 충격 상성의 물리 소 데미지를 3회　데미지의 30％를 흡수
ELSEIF BASE:ARG:LV < 40
	PRINTL 충격 상성의 물리 소 데미지를 3회　데미지의 30％를 흡수
ELSE
	PRINTL 충격 상성의 물리 소 데미지를 3회　데미지의 30％를 흡수
ENDIF

IF BASE:ARG:LV < 40
	PRINTL Lv이 10 높아질 때마다 위력과 흡수가 강화된다。
ENDIF
PRINTL Jade Angriff　유메오오지 시오리의 반짝임으로 형성된 사브르
PRINTL 1체의 적에게 가련하게 덤벼들어 베어내、체력을 흡수한다。

;没
;@계승부위_ヤーデアングリフ,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_ヤーデアングリフ,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_ヤーデアングリフ,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_ヤーデアングリフ,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_ヤーデアングリフ,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_ヤーデアングリフ,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"ヤーデアングリフ"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"ヤーデアングリフ"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"ヤーデアングリフ"),0

