;스킬명
@SKILL_NAME_蒼き怒火,ARG
;KR판 임의수정, 蒼き怒火 명방 한판의 스킬명 푸른 분노로 번역
RESULTS = 푸른 분노

;　명중
@SKILL_HITRATE_蒼き怒火, ARG
RETURN 100

;　위력
@SKILL_POWER_蒼き怒火, ARG
RETURN 100

;;クリティカル率
;@SKILL_CRITICAL_RATE_蒼き怒火, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_蒼き怒火, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_蒼き怒火,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_蒼き怒火,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_蒼き怒火,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_蒼き怒火,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_蒼き怒火, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_蒼き怒火,ARG
RETURN 0

;　상성
@SKILL_TYPE_蒼き怒火,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_蒼き怒火,ARG
RETURN 0

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_蒼き怒火,ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_蒼き怒火,ARG
RETURN 4

;　사거리
@SKILL_RANGE_蒼き怒火,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_蒼き怒火,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_蒼き怒火,ARG
RETURN 4

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_蒼き怒火,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_蒼き怒火,ARG
;RETURN 23
;剣撃

;속도보정
@SKILL_SPEED_蒼き怒火, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_蒼き怒火,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_蒼き怒火, ARG = -1
RETURN 0

;没
;@SKILL_RANK_蒼き怒火
;@SKILL_RANKUP_蒼き怒火
;@계승불능_蒼き怒火

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_蒼き怒火,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_蒼き怒火,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_蒼き怒火,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_蒼き怒火,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_蒼き怒火,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_蒼き怒火,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_蒼き怒火,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_蒼き怒火,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_蒼き怒火,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_蒼き怒火,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_蒼き怒火,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、全体)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_蒼き怒火,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_蒼き怒火,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_蒼き怒火,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_蒼き怒火,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_蒼き怒火,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_蒼き怒火,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"蒼き怒火")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_蒼き怒火,ARG
PRINTFORML 전투에 참가하고 있는 동안、아군 전원의 데미지 상승량과 피데미지 경감률이 \@ CSTR:ARG:104 == "" ? 4 # 7\@％ 상승한다
PRINTL 사용자가 전용스킬을 발동 중 한정、효과량이 2배가 된다

;没
;@계승부위_蒼き怒火,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_蒼き怒火,ARG
RETURN 0

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_蒼き怒火,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_蒼き怒火,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_蒼き怒火,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_蒼き怒火,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"蒼き怒火"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"蒼き怒火"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"蒼き怒火"),0

;====================================================
;独自処理
;====================================================
;（予定）と付けられている方は仮置きの物。実際に採用されるかどうかはわからない。
@AUTO_PU_SKILL_蒼き怒火,ARG,ARGS,ARG:1,ARG:2
#DIM SKILL_RANK
#DIM SKILL_NUM
#DIM LAMMOUNT = 0
#DIM LCOUNT
#DIM L_USER
;ARG = 攻撃者
;ARGS （ダメージ↑、被ダメージ↑、命中↑、回避↑、魔威↑、魔効↑、etc…）といった感じの奴
;ARG:1 = 防御者
;文字列を参考にする都合上、どの文字列を使うか事前に定めておいた方がいいかも
;CSTR:ARG:100 = EXTRA勢出典作品、『アークナイツ』とでも書き込む（予定）
;CSTR:ARG:101 = EXTRA勢出典作品でどのキャラなのか、とりあえず『アーミヤ（前衛）』とでも書き込む（予定）
;CSTR:ARG:104 = 現在の霊格昇華の度合。 このスキルを習得している時点で空文字の可能性は低いが、念のためにの分岐を用意
;----------------------------------------------------------------------------------------------
;この効果は攻撃側がアーミヤでなくても効果が発揮されるので、一回検索する
L_USER = -1
FOR LCOUNT,0,CHARANUM
	;戦闘に参加してないorアーミヤでないなら飛ばす
	SIF CFLAG:LCOUNT:포지션 < 1 || CSTR:LCOUNT:101 != "アーミヤ（前衛）"
		CONTINUE
	SELECTCASE ARGS
		CASE "ダメージ↑"
			;ダメージ↑の場合、攻撃者と同PTでなければ飛ばす
			SIF CFLAG:ARG:PT플래그 != CFLAG:LCOUNT:PT플래그
				CONTINUE
		CASE "被ダメージ↓"
			;被ダメージ↓の場合、防御者と同PTでなければ飛ばす
			SIF CFLAG:(ARG:1):PT플래그 != CFLAG:LCOUNT:PT플래그
				CONTINUE
	ENDSELECT
	L_USER = LCOUNT
	BREAK
NEXT
;アーミヤが見つからない、専用技1が削除されている、蒼き怒火を習得していない場合は終了
SIF L_USER == -1 || !HAVE_SKILL(L_USER,[[스킬:전용기1]]) || !HAVE_PU_SKILL(L_USER,"蒼き怒火")
	RETURN 0
SKILL_NUM = [[스킬:전용기1]]
SELECTCASE ARGS
	CASE "ダメージ↑","被ダメージ↓"
		SELECTCASE CSTR:L_USER:104
			CASE ""
				LAMMOUNT = 4
			CASEELSE
				LAMMOUNT = 7
		ENDSELECT
		SIF SKILLGAGE_F_GET(L_USER,SKILL_NUM) < 0
			LAMMOUNT *= 2
		SIF ARGS == "被ダメージ↓"
			LAMMOUNT = 100 - LAMMOUNT
		RETURN LAMMOUNT
ENDSELECT
RETURN 0
