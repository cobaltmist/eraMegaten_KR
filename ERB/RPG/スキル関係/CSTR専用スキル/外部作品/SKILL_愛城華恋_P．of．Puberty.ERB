;更新履歴
;001	2019/09/XX		kamedakeisuke(ﾟдﾟ)			新規作成
;002	20XX/XX/XX		kamedakeisuke(ﾟдﾟ)			どっかで性能を引き上げた覚えがある

;意図的にどちゃくそ強くするため、許されそうな天井を狙っています
;これを参考に何かを作るときはご注意ください

;スキル名
@SKILL_NAME_P．of．Puberty,ARG
RESULTS = P of Puberty

;　명중
@SKILL_HITRATE_P．of．Puberty, ARG
RETURN 100

;　위력
@SKILL_POWER_P．of．Puberty, ARG
RETURN 240

;クリティカル率
@SKILL_CRITICAL_RATE_P．of．Puberty, ARG
RETURN 30

;　최저공격횟수
@SKILL_MINATTACKNUMBER_P．of．Puberty,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_P．of．Puberty,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_P．of．Puberty, ARG
RETURN 3
;消費なし＝1

;　코스트량
@SKILL_COST_P．of．Puberty,ARG
RETURN 7

;　상성
@SKILL_TYPE_P．of．Puberty,ARG
;검격
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_P．of．Puberty,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_P．of．Puberty,ARG
RETURN 1


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_P．of．Puberty,ARG
RETURN 1

;　사거리
@SKILL_RANGE_P．of．Puberty,ARG
RETURN 2

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_P．of．Puberty,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_P．of．Puberty,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_P．of．Puberty,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_P．of．Puberty,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_P．of．Puberty, ARG = -1
RETURN 50

;;　口上の表示とそのタイプ
;@SKILL_MESSAGE_P．of．Puberty,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_P．of．Puberty, ARG = -1
RETURN 0

;没
;@SKILL_RANK_P．of．Puberty
;@SKILL_RANKUP_P．of．Puberty
;@계승불능_P．of．Puberty

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_P．of．Puberty,ARG
RETURN GET_STATE_NUM("SHOCK")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_P．of．Puberty,ARG
RETURN 6

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_P．of．Puberty,ARG
RETURN 60

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_P．of．Puberty,ARG
RETURN 100

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_P．of．Puberty,ARG
;RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_P．of．Puberty,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM

;====================================================
;실행내용 여기까지
;====================================================

;　説明文の表示
@SKILL_EXPLAIN_P．of．Puberty,ARG
PRINTL Possibility of Puberty。소녀의 반짝임에 의해 주조된 재능의 사벨。
PRINTL 반짝임을 불태워 적 1체에 돌격、그 삶의 태도로 감전시킨다。후열 사용 가능、높은 크리티컬률、초고속 공격。

;没
;@계승부위_P．of．Puberty,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_P．of．Puberty,ARG
RETURN 1
