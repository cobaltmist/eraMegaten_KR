;更新履歴
;001	2019/09/XX		kamedakeisuke(ﾟдﾟ)			新規作成

;意図的にどちゃくそ強くするため、許されそうな天井を狙っています
;これを参考に何かを作るときはご注意ください

;スキル名
@SKILL_NAME_約束タワーブリッジ,ARG
RESULTS = 약속 타워브릿지

;　명중
@SKILL_HITRATE_約束タワーブリッジ, ARG
RETURN 120

;　위력
@SKILL_POWER_約束タワーブリッジ, ARG
RETURN 525

;クリティカル率
@SKILL_CRITICAL_RATE_約束タワーブリッジ, ARG
RETURN 30

;　최저공격횟수
@SKILL_MINATTACKNUMBER_約束タワーブリッジ,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_約束タワーブリッジ,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_約束タワーブリッジ, ARG
RETURN 3
;消費なし＝1

;　코스트량
@SKILL_COST_約束タワーブリッジ,ARG
RETURN 42

;　상성
@SKILL_TYPE_約束タワーブリッジ,ARG
RETURN GET_TYPE_NUM("만능")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_約束タワーブリッジ,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_約束タワーブリッジ,ARG
RETURN 1


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_約束タワーブリッジ,ARG
RETURN 1

;　사거리
@SKILL_RANGE_約束タワーブリッジ,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_約束タワーブリッジ,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_約束タワーブリッジ,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_約束タワーブリッジ,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_約束タワーブリッジ,ARG
;RETURN 23

;속도보정
@SKILL_SPEED_約束タワーブリッジ, ARG = -1
RETURN -20

;;　口上の表示とそのタイプ
;@SKILL_MESSAGE_約束タワーブリッジ,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_約束タワーブリッジ, ARG = -1
RETURN 0

;没
;@SKILL_RANK_約束タワーブリッジ
;@SKILL_RANKUP_約束タワーブリッジ
;@계승불능_約束タワーブリッジ

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_約束タワーブリッジ,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_約束タワーブリッジ,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_約束タワーブリッジ,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_約束タワーブリッジ,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_約束タワーブリッジ,ARG
;RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_約束タワーブリッジ,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM

;====================================================
;실행내용 여기까지
;====================================================

;　説明文の表示
@SKILL_EXPLAIN_約束タワーブリッジ,ARG
	PRINTL 소녀의 반짝임이、시작의 도쿄타워를 소환해、자신의 운명을 관통한다。
	PRINTL 본래는 단 한 명의 친구를 위해 내뿜은 눈부시게 빛나는 반짝임의 작렬이다。
	PRINTL 적 1체、만능 상성 특대 데미지。물리의존、후열 사용 가능、높은 크리티컬률、발동이 느리다。

;没
;@계승부위_約束タワーブリッジ,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_約束タワーブリッジ,ARG
RETURN 1
