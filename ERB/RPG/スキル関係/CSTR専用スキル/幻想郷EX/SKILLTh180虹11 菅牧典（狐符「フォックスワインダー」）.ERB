;専用スキル用のテンプレート
;「狐符「フォックスワインダー」」を別の名前に置き換えてください

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_狐符「フォックスワインダー」,ARG
RESULTS = 호부「폭스 와인더」

;　명중
@SKILL_HITRATE_狐符「フォックスワインダー」, ARG
RETURN 100

;　위력
@SKILL_POWER_狐符「フォックスワインダー」, ARG
RETURN 40

;;クリティカル率
;@SKILL_CRITICAL_RATE_狐符「フォックスワインダー」, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_狐符「フォックスワインダー」, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_狐符「フォックスワインダー」,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_狐符「フォックスワインダー」,ARG
RETURN 3

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_狐符「フォックスワインダー」,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_狐符「フォックスワインダー」,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_狐符「フォックスワインダー」, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_狐符「フォックスワインダー」,ARG
RETURN 40

;　상성
@SKILL_TYPE_狐符「フォックスワインダー」,ARG
RETURN GET_TYPE_NUM("만능")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_狐符「フォックスワインダー」, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_狐符「フォックスワインダー」,ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_狐符「フォックスワインダー」,ARG
RETURN 1

;　사거리
@SKILL_RANGE_狐符「フォックスワインダー」,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_狐符「フォックスワインダー」,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_狐符「フォックスワインダー」,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_狐符「フォックスワインダー」,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_狐符「フォックスワインダー」,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_狐符「フォックスワインダー」, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_狐符「フォックスワインダー」,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_狐符「フォックスワインダー」, ARG = -1
RETURN 0

;没
;@SKILL_RANK_狐符「フォックスワインダー」
;@SKILL_RANKUP_狐符「フォックスワインダー」
;@계승불능_狐符「フォックスワインダー」

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_狐符「フォックスワインダー」,ARG
IF RAND:3 == 0
	RETURN GET_STATE_NUM("CLOSE")
ELSEIF RAND:2 == 0
	RETURN GET_STATE_NUM("BIND")
ELSE
	RETURN GET_STATE_NUM("POISON")
ENDIF

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_狐符「フォックスワインダー」,ARG
RETURN GET_TYPE_NUM("만능")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_狐符「フォックスワインダー」,ARG
RETURN 10

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_狐符「フォックスワインダー」,ARG
RETURN 100

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_狐符「フォックスワインダー」,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_狐符「フォックスワインダー」,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_狐符「フォックスワインダー」,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_狐符「フォックスワインダー」,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_狐符「フォックスワインダー」,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_狐符「フォックスワインダー」,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_狐符「フォックスワインダー」,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、全体)

;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_狐符「フォックスワインダー」,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_狐符「フォックスワインダー」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_狐符「フォックスワインダー」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_狐符「フォックスワインダー」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_狐符「フォックスワインダー」,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_狐符「フォックスワインダー」,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"狐符「フォックスワインダー」")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_狐符「フォックスワインダー」,ARG
PRINTL 적 전체에、만능 속성으로 극소위력의 마법공격을 3회 행한다。
PRINTL 추가효과：랜덤하게 독・마봉・구속을 부여。（저확률）
PRINTL 
PRINTL 빈틈없이 움직임을 제한하는 듯한 탄막을 날려 위협사격한다。
PRINTL 『맞아』도 위력은 그다지 강하지 않지만、운나쁘게『적중』해버리면……。
PRINTL 쿠다키츠네 쿠다마키 츠카사의 스펠카드（CSTR전용）
;没
;@계승부위_狐符「フォックスワインダー」,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_狐符「フォックスワインダー」,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_狐符「フォックスワインダー」,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_狐符「フォックスワインダー」,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_狐符「フォックスワインダー」,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_狐符「フォックスワインダー」,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"狐符「フォックスワインダー」"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"狐符「フォックスワインダー」"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"狐符「フォックスワインダー」"),0
