;専用スキル用のテンプレート
;「「陰陽サフォケイション」」を別の名前に置き換えてください

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_「陰陽サフォケイション」,ARG
RESULTS = 「陰陽サフォケイション」

;　명중
@SKILL_HITRATE_「陰陽サフォケイション」, ARG
RETURN 130

;　위력
@SKILL_POWER_「陰陽サフォケイション」, ARG
RETURN 300

;;クリティカル率
;@SKILL_CRITICAL_RATE_「陰陽サフォケイション」, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_「陰陽サフォケイション」, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_「陰陽サフォケイション」,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_「陰陽サフォケイション」,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_「陰陽サフォケイション」,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_「陰陽サフォケイション」,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_「陰陽サフォケイション」, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_「陰陽サフォケイション」,ARG
RETURN 35

;　상성
@SKILL_TYPE_「陰陽サフォケイション」,ARG
RETURN GET_TYPE_NUM("파마")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_「陰陽サフォケイション」, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_「陰陽サフォケイション」,ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_「陰陽サフォケイション」,ARG
RETURN 1

;　사거리
@SKILL_RANGE_「陰陽サフォケイション」,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_「陰陽サフォケイション」,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_「陰陽サフォケイション」,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_「陰陽サフォケイション」,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_「陰陽サフォケイション」,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_「陰陽サフォケイション」, ARG = -1
RETURN -20

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_「陰陽サフォケイション」,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_「陰陽サフォケイション」, ARG = -1
RETURN 0

;没
;@SKILL_RANK_「陰陽サフォケイション」
;@SKILL_RANKUP_「陰陽サフォケイション」
;@계승불능_「陰陽サフォケイション」

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_「陰陽サフォケイション」,ARG
RETURN GET_STATE_NUM("DYING")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_「陰陽サフォケイション」,ARG
RETURN GET_TYPE_NUM("파마")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_「陰陽サフォケイション」,ARG
RETURN 70

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_「陰陽サフォケイション」,ARG
RETURN 150

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_「陰陽サフォケイション」,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_「陰陽サフォケイション」,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_「陰陽サフォケイション」,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
@SKILL_HIT_EFFECT_「陰陽サフォケイション」,ARG,ARG:1
SIF CFLAG:(ARG:1):파마가드킬 > 0
	FLAG:확산플래그 = 1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_「陰陽サフォケイション」,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_「陰陽サフォケイション」,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_「陰陽サフォケイション」,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
FLAG:확산플래그 = 0

CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	1체攻撃

IF FLAG:확산플래그 == 1
	PRINTL 破魔耐性を崩された敵に、陰陽玉が追撃する！
	CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	1체攻撃
	CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	1체攻撃
ENDIF

FLAG:확산플래그 = 0

;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、전체)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_「陰陽サフォケイション」,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;1체回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、전체)
;CALL ALL_HEAL, POS(ARG:1), 0				;1체前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_「陰陽サフォケイション」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_「陰陽サフォケイション」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_「陰陽サフォケイション」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_「陰陽サフォケイション」,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_「陰陽サフォケイション」,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"「陰陽サフォケイション」")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_「陰陽サフォケイション」,ARG
PRINTL 적 1체에 、破魔属性で大威力の魔法攻撃を1回行う。発生が遅く高命中。
PRINTL 追加効果：破魔属性即死（高確率）。ガードキル状態の敵に攻撃する場合、3回攻撃。
PRINTL 
PRINTL 窒息せんばかりの陰陽玉の嵐で敵を包み込み공격한다。
PRINTL 対象の破魔耐性が破壊されている場合、より苛烈に追い立てとどめを刺す。
PRINTL 玉祖命 玉造魅須丸のスペルカード（CSTR専用）

;没
;@계승부위_「陰陽サフォケイション」,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_「陰陽サフォケイション」,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_「陰陽サフォケイション」,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_「陰陽サフォケイション」,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_「陰陽サフォケイション」,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_「陰陽サフォケイション」,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"「陰陽サフォケイション」"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"「陰陽サフォケイション」"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"「陰陽サフォケイション」"),0
