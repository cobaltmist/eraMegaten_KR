;専用スキル用のテンプレート

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_招符「弾幕万来」,ARG
RESULTS = 招符「弾幕万来」

;　명중
@SKILL_HITRATE_招符「弾幕万来」, ARG
RETURN 150

;　위력
@SKILL_POWER_招符「弾幕万来」, ARG
SIF BASE:ARG:LV >= 50
	RETURN 420
RETURN 250

;;クリティカル率
@SKILL_CRITICAL_RATE_招符「弾幕万来」, ARG
RETURN 25

;;　吸収率
;@SKILL_HP_ABSORBER_招符「弾幕万来」, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_招符「弾幕万来」,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_招符「弾幕万来」,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_招符「弾幕万来」,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_招符「弾幕万来」,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_招符「弾幕万来」, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_招符「弾幕万来」,ARG
SIF BASE:ARG:LV >= 50
	RETURN 40
RETURN 20

;　상성
@SKILL_TYPE_招符「弾幕万来」,ARG
RETURN GET_TYPE_NUM("타격")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_招符「弾幕万来」, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_招符「弾幕万来」,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_招符「弾幕万来」,ARG
RETURN 1

;　사거리
@SKILL_RANGE_招符「弾幕万来」,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_招符「弾幕万来」,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_招符「弾幕万来」,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_招符「弾幕万来」,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_招符「弾幕万来」,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_招符「弾幕万来」, ARG = -1
RETURN -10

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_招符「弾幕万来」,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_招符「弾幕万来」, ARG = -1
RETURN 0

;没
;@SKILL_RANK_招符「弾幕万来」
;@SKILL_RANKUP_招符「弾幕万来」
;@계승불능_招符「弾幕万来」

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_招符「弾幕万来」,ARG
RETURN GET_STATE_NUM("HAPPY")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_招符「弾幕万来」,ARG
RETURN GET_TYPE_NUM("정신")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_招符「弾幕万来」,ARG
RETURN 30

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_招符「弾幕万来」,ARG
RETURN 95

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_招符「弾幕万来」,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_招符「弾幕万来」,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_招符「弾幕万来」,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_招符「弾幕万来」,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_招符「弾幕万来」,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_招符「弾幕万来」,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_招符「弾幕万来」,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;範囲攻撃(列、전체)


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_招符「弾幕万来」,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;1체回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、전체)
;CALL ALL_HEAL, POS(ARG:1), 0				;1체前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_招符「弾幕万来」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_招符「弾幕万来」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_招符「弾幕万来」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_招符「弾幕万来」,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_招符「弾幕万来」,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"招符「弾幕万来」")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_招符「弾幕万来」,ARG
PRINTL 적 전체에 打撃属性で中威力の物理攻撃を１回行い、精神属性で幸福状態を付与する。
PRINTL 追加効果：一定以上のレベルになると威力・コスト増加。発動が遅く高命中・高必殺率。
PRINTL 
PRINTL 虹色の弾をばら撒き、ばら撒かれた弾は徐々に加速して敵に降り注ぐ。
PRINTL 招き猫 豪徳寺ミケのスペルカード（CSTR専用）

;没
;@계승부위_招符「弾幕万来」,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_招符「弾幕万来」,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_招符「弾幕万来」,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_招符「弾幕万来」,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_招符「弾幕万来」,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_招符「弾幕万来」,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"招符「弾幕万来」"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"招符「弾幕万来」"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"招符「弾幕万来」"),0
