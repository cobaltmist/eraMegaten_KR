;専用スキル用のテンプレート
;「「弾幕のアジール」」を別の名前に置き換えてください

;アクティブスキルに必須なものはそのままにしており
;なくてもいいものはコメントアウトしております
;必要だと思ったらコメントアウトを外して設定してください
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_「弾幕のアジール」,ARG
RESULTS = 「弾幕のアジール」

;　명중
@SKILL_HITRATE_「弾幕のアジール」, ARG
RETURN 100

;　위력
@SKILL_POWER_「弾幕のアジール」, ARG
RETURN 100

;;クリティカル率
;@SKILL_CRITICAL_RATE_「弾幕のアジール」, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_「弾幕のアジール」, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_「弾幕のアジール」,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_「弾幕のアジール」,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_「弾幕のアジール」,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_「弾幕のアジール」,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_「弾幕のアジール」, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_「弾幕のアジール」,ARG
RETURN 80

;　상성
@SKILL_TYPE_「弾幕のアジール」,ARG
RETURN GET_TYPE_NUM("검격")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_「弾幕のアジール」, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_「弾幕のアジール」,ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_「弾幕のアジール」,ARG
RETURN 2

;　사거리
@SKILL_RANGE_「弾幕のアジール」,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_「弾幕のアジール」,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_「弾幕のアジール」,ARG
RETURN 5

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_「弾幕のアジール」,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_「弾幕のアジール」,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_「弾幕のアジール」, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_「弾幕のアジール」,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_「弾幕のアジール」, ARG = -1
RETURN 0

;没
;@SKILL_RANK_「弾幕のアジール」
;@SKILL_RANKUP_「弾幕のアジール」
;@계승불능_「弾幕のアジール」

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_「弾幕のアジール」,ARG
;RETURN GET_STATE_NUM("")
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_「弾幕のアジール」,ARG
;RETURN GET_TYPE_NUM("")
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_「弾幕のアジール」,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_「弾幕のアジール」,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_「弾幕のアジール」,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_「弾幕のアジール」,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_「弾幕のアジール」,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_「弾幕のアジール」,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_「弾幕のアジール」,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_「弾幕のアジール」,ARG,ARG:1



;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_「弾幕のアジール」,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM
#DIM アジールリキャスト
アジールリキャスト = PU_SKILLNUM_GET(ARG,"「弾幕のアジール」")

	;デ・クンダ部分
	IF CFLAG:(ARG):PT플래그 == 0
		LOCAL:2 = 7
		LOCAL:3 = 17
	ELSE
		LOCAL:2 = 1
		LOCAL:3 = 7
	ENDIF

	FOR LOCAL,LOCAL:2,LOCAL:3
		SIF POS(LOCAL) < 0
			CONTINUE
		FOR LOCAL:1,0,6
			CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") = MAX(0, CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화"))
		NEXT
	NEXT
	PRINTFORML \@ CFLAG:ARG:PT플래그 > 0 ? 아군 # 적 \@のンダ効果を打ち払った！
	
	;デ・カジャ部分
	IF CFLAG:(ARG):PT플래그 == 0
		LOCAL:2 = 1
		LOCAL:3 = 7
	ELSE
		LOCAL:2 = 7
		LOCAL:3 = 17
	ENDIF
	FOR LOCAL,LOCAL:2,LOCAL:3
		SIF POS(LOCAL) < 0
			CONTINUE
		FOR LOCAL:1,0,6
			CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") = MIN(0, CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화"))
		NEXT
	NEXT
	PRINTFORML \@ CFLAG:ARG:PT플래그 == 2 ? 적 # 아군 \@のカジャ効果を打ち消した！

IF CFLAG:(ARG):PT플래그
	;LOCAL:9に対象者、ARGに使用者、PTフラグ == 2はPTにいる仲間、PTフラグ == 1はPTにいない仲間、PTフラグ == 0は敵
	FOR LOCAL:9, 0, CHARANUM
		;使用者はスルー
		SIF (LOCAL:9) == ARG
			CONTINUE
		;PTに居ない人間は無理
		SIF CFLAG:(LOCAL:9):PT플래그 == 1 && (ABL:(LOCAL:9):종족 == 0 || ABL:(LOCAL:9):종족 > 45)
			CONTINUE
		;レンタル中は排除
		SIF CFLAG:(LOCAL:9):노역플래그 == 3
			CONTINUE
		SIF TALENT:(LOCAL:9):비전투원
			CONTINUE
		;ＣＯＭＰにもPTにもいない悪魔を排除
		SIF CFLAG:(LOCAL:9):소속ＣＯＭＰ == -1 && CFLAG:(LOCAL:9):PT플래그 < 2
			CONTINUE
		SIF CFLAG:(LOCAL:9):이자리에없는플래그 == 1
			CONTINUE
		SIF CFLAG:(LOCAL:9):PT플래그 == 0
			CONTINUE

		IF CFLAG:(LOCAL:9):상태이상 == GET_STATE_NUM("DYING")
			CFLAG:(LOCAL:9):상태이상 = 0
			CFLAG:(LOCAL:9):상태이상경과턴수 = 0
			CALL VAR_HP, (LOCAL:9), MAXBASE:(LOCAL:9):ＨＰ/3
			PRINTFORML %CALLNAME:(LOCAL:9)%は息を吹き返した
		ENDIF
	NEXT
;敵が使った場合
ELSE
	LOCAL:7 = 7
	LOCAL:8 = 17
	FOR LOCAL:9, LOCAL:7, LOCAL:8
		LOCALS = 포지션{LOCAL:9}
		SIF FLAG:LOCALS < 0
			CONTINUE
		LOCAL = FLAG:LOCALS
		CALL SKILL_CURE_STATE_「弾幕のアジール」, CFLAG:LOCAL:상태이상
		IF RESULT == 1
			PRINTFORML 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(LOCAL),20,LEFT%　>>>>>>　息を吹き返した
			CFLAG:LOCAL:상태이상 = 0
			CFLAG:LOCAL:상태이상경과턴수 = 0
			CALL VAR_HP, (LOCAL), MAXBASE:(LOCAL):ＨＰ/4
		ENDIF
	NEXT
ENDIF
WAIT

CALL SKILLGAGE_F_ADD,ARG,アジールリキャスト,4



;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	1체攻撃
;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、전체)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_「弾幕のアジール」,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;1체回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、전체)
;CALL ALL_HEAL, POS(ARG:1), 0				;1체前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_「弾幕のアジール」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_「弾幕のアジール」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_「弾幕のアジール」,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM
;RETURN LOCAL

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_「弾幕のアジール」,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
@SKILLGAGE_DISPLAY_「弾幕のアジール」,ARG
#DIM アジールリキャスト
アジールリキャスト = PU_SKILLNUM_GET(ARG,"「弾幕のアジール」")
SETCOLOR 0x999999
IF SKILLGAGE_D_GET(ARG,アジールリキャスト) == 0
	PRINTL 「弾幕のアジール」：ＲＥＡＤＹ
ELSEIF SKILLGAGE_D_GET(ARG,アジールリキャスト) == 1
	PRINTL 「弾幕のアジール」：あと1ターン
ELSEIF SKILLGAGE_D_GET(ARG,アジールリキャスト) == 2
	PRINTL 「弾幕のアジール」：あと2ターン
ELSEIF SKILLGAGE_D_GET(ARG,アジールリキャスト) == 3
	PRINTL 「弾幕のアジール」：あと3ターン
ENDIF
RESETCOLOR

;　説明文の表示
@SKILL_EXPLAIN_「弾幕のアジール」,ARG
PRINTL 回復：味方全員をDYINGから蘇生し、
PRINTL 味方全員の弱化、적 전체の強化を解除する。リキャスト：3ターン。
PRINTL 
PRINTL 全ての強化と弱化を無（カミ）に返し、ここで死ぬ定めではない者を黄泉帰らせる。
PRINTL 市神 天弓千亦のスペルカード（CSTR専用）


;没
;@계승부위_「弾幕のアジール」,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_「弾幕のアジール」,ARG
#DIM アジールリキャスト
アジールリキャスト = PU_SKILLNUM_GET(ARG,"「弾幕のアジール」")
SIF SKILLGAGE_D_GET(ARG,アジールリキャスト)
	RETURN 0
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_「弾幕のアジール」,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_「弾幕のアジール」,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
@SKILL_CURE_STATE_「弾幕のアジール」,USE_CHARA,ARG
#DIM USE_CHARA
SELECTCASE GET_STATE(ARG)
	CASE "DYING"
		RETURN 1
	CASEELSE
		RETURN 0
ENDSELECT
;この技を複数持てるか
;@중복습득수_「弾幕のアジール」,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"「弾幕のアジール」"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"「弾幕のアジール」"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"「弾幕のアジール」"),0
