;스킬名
@SKILL_NAME_1144,ARG
RESULTS = 바이스・고스펠

;　명중
@SKILL_HITRATE_1144, ARG
RETURN 100

;　위력
@SKILL_POWER_1144, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_1144,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_1144,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_1144, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_1144,ARG
RETURN 40

;　相性
@SKILL_TYPE_1144,ARG
RETURN GET_TYPE_NUM("신경")
;신경

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_1144, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_1144, ARG
RETURN 0


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_1144
RETURN 1

;　사거리
@SKILL_RANGE_1144,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_1144,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_1144
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_1144,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_1144
RETURN 11
;정신

;속도보정
@SKILL_SPEED_1144, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_1144,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_1144
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_1144
RETURN 15

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_1144,ARG
SIF !RAND:5
	RETURN GET_STATE_NUM("BIND")
SIF !RAND:4
	RETURN GET_STATE_NUM("CLOSE")
SIF !RAND:3
	RETURN GET_STATE_NUM("POISON")
SIF !RAND:2
	RETURN GET_STATE_NUM("BOMB")
	
	RETURN GET_STATE_NUM("PALYZE")
RETURN RAND:10 + 1

;추가효과상성
@SKILL_ADDTIONAL_TYPE_1144,ARG
RETURN GET_TYPE_NUM("신경")

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_1144,ARG
RETURN 30

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_1144,ARG
RETURN 50

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_1144,ARG,ARG:1
#LOCALSIZE 1
FOR LOCAL, CFLAG:ARG:PT플래그 ? 7 # 1, CFLAG:ARG:PT플래그 ? 17 # 7
	SIF POS(LOCAL) < 0
		CONTINUE
	CALL SINGLE_BADSTATE, ARG, POS(LOCAL), 1144
NEXT


;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_1144
PRINTL [후열] 적 전체에 신경속성 공격을 하여 무작위 상태이상을 건다. (낮은 확률로 무작위 상태이상)

;継承に필요な部位
@継承部位_1144,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_1144,ARG
RETURN 1
;とくにない

