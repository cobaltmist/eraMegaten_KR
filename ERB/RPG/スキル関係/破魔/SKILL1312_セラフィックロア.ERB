;스킬名
@SKILL_NAME_1312, ARG
RESULTS = 세라픽로어

;　명중
@SKILL_HITRATE_1312, ARG
RETURN 500

;　위력
@SKILL_POWER_1312, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_1312,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_1312,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_1312, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_1312,ARG
;原作準拠
RETURN 30

;　相性
@SKILL_TYPE_1312,ARG
RETURN GET_TYPE_NUM("파마")
;파마

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_1312, ARG
RETURN 2
;魔法

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_1312, ARG
RETURN 0


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_1312
RETURN 4

;　사거리
@SKILL_RANGE_1312,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_1312,ARG
RETURN 4

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_1312
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_1312,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_1312
RETURN 13

@継承不能_1312
RETURN 1
;不可

;속도보정
@SKILL_SPEED_1312, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_1312,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_1312
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_1312
RETURN 23
;変化不能

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_1312,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_1312,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_1312,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_1312,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_1312,ARG,ARG:1

CALL RATE_ATTACK_FIELD,ARG,ARG:1,1312,0,1500
; IF CFLAG:ARG:포지션 >= 1 && CFLAG:ARG:포지션 <= 6
; 	FOR LOCAL, 7, 17
; 		LOCALS = 포지션{LOCAL}
; 		SIF FLAG:LOCALS < 0 || FLAG:LOCALS == ARG || CFLAG:(FLAG:LOCALS):ボスフラグ
; 			CONTINUE
; 		SIF GET_STATE(CFLAG:(FLAG:LOCALS):ステート) == "DYING"
; 			CONTINUE
; 		PRINTFORM 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
; 		CALLFORM JUDG_HIT_2,ARG,FLAG:LOCALS,100
; 		IF RESULT == 1 && MAXBASE:(FLAG:LOCALS):파마 > 0 && MAXBASE:(FLAG:LOCALS):파마 < 999
; 			;명중した
; 			PRINTFORMW {(BASE:(FLAG:LOCALS):ＨＰ) * 80 / 100}데미지
; 			CALL VAR_HP, FLAG:LOCALS, -((BASE:(FLAG:LOCALS):ＨＰ) * 80 / 100),0
; 		ELSE
; 			;外した
; 			PRINTW MISS
; 		ENDIF
; 	NEXT
; ELSE
; 	FOR LOCAL, 1, 6
; 		LOCALS = 포지션{LOCAL}
; 		SIF FLAG:LOCALS < 0 || FLAG:LOCALS == ARG || CFLAG:(FLAG:LOCALS):ボスフラグ
; 			CONTINUE
; 		SIF GET_STATE(CFLAG:(FLAG:LOCALS):ステート) == "DYING"
; 			CONTINUE
; 		PRINTFORM 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
; 		CALLFORM JUDG_HIT_2,ARG,FLAG:LOCALS,100
; 		IF RESULT == 1 && MAXBASE:(FLAG:LOCALS):파마 > 0 && MAXBASE:(FLAG:LOCALS):파마 < 999
; 			;명중した
; 			PRINTFORMW {(BASE:(FLAG:LOCALS):ＨＰ) * 80 / 100}데미지
; 			CALL VAR_HP, FLAG:LOCALS, -((BASE:(FLAG:LOCALS):ＨＰ) * 80 / 100),0
; 		ELSE
; 			;外した
; 			PRINTW MISS
; 		ENDIF
; 	NEXT
; ENDIF




;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_1312
PRINTL [후열]  적 전체에 파마 궁합속성으로 현재 HP의 80% 피해를 입힌다 (상한 피해 1500)
;PRINTL [후열] 적과 아군 전체에 파마속성 공격으로 HP를 대폭 줄인다. 보스에게는 무효.

;継承に필요な部位
@継承部位_1312,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_1312,ARG
RETURN 1
;とくにない

