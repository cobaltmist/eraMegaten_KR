;

;스킬名
@SKILL_NAME_1214,ARG
RESULTS = 파이널누드

;　명중
@SKILL_HITRATE_1214, ARG
RETURN 100

;　위력
@SKILL_POWER_1214, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_1214,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_1214,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_1214, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_1214,ARG
RETURN 18

;　相性
@SKILL_TYPE_1214,ARG
RETURN 9
;정신

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_1214, ARG
RETURN 1
;魔法

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_1214, ARG
RETURN 0


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_1214
RETURN 1

;　사거리
@SKILL_RANGE_1214,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_1214,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_1214
RETURN 3

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_1214,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_1214
RETURN 12
;정신

;속도보정
@SKILL_SPEED_1214, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_1214,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_1214
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_1214
RETURN 12


;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_1214,ARG
RETURN GET_STATE_NUM("CHARM")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_1214,ARG
RETURN 9

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_1214,ARG
RETURN 80

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_1214,ARG
RETURN 95
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_1214,ARG,ARG:1
IF CFLAG:ARG:입력행동 == 1214 && FLAG:ムフフ전개 == 1
			IF TALENT :(ARG):162
SETCOLOR 0xFF33FF
		IF RAND:4 && EQUIP :(ARG):27
			PRINTFORML ＞%조사처리(CALLNAME:ARG,"는")% 속옷을 발목에 걸친 채, 다리를 벌려 비밀스러운 장소를 노출하였다.
		ELSEIF RAND:3 == 0
			PRINTFORML ＞%조사처리(CALLNAME:ARG,"가")% 모두에게 육체를 과시하며 달콤한 목소리로 유혹하자, 흥분한 악마들이 몰려들었다.
		ELSEIF RAND:2 == 0
			PRINTFORML ＞능욕에 대한 기대로 가슴이 부푼 %조사처리(CALLNAME:ARG,"는")% 실오라기 하나 걸치지 않은 모습으로 악마들을 유혹했다.
		ELSE
			PRINTFORML ＞정욕이 흘러 넘치는 음부가 드러나자, 악마들은 앞다투어 %CALLNAME:ARG%에게 달려들었다.
		ENDIF
				IF RAND:7 == 0
					PRINTFORM ＞%조사처리(CALLNAME:ARG,"는")% 
						IF RAND:7 == 0
							PRINT 커다랗게 발기한 페니스를 
						ELSEIF RAND:6 == 0
							PRINT 말도 안되게 큰 페니스를 
						ELSEIF RAND:5 == 0
							PRINT 크게 맥동하는 페니스를 
						ELSEIF RAND:4 == 0
							PRINT 혈관이 크게 부풀어 오른페니스를 
						ELSEIF RAND:3 == 0
							PRINT 뜨겁게 달아오른 페니스를 
						ELSEIF RAND:2 == 0
							PRINT 쿠퍼액으로 미끌거리는 페니스를 
						ELSE
							PRINT 남들보다 조금 못한 페니스를 
						ENDIF
							IF RAND:6 == 0
								PRINT 요염한 표정으로 
							ELSEIF RAND:5 == 0
								PRINT 부끄러운듯이 
							ELSEIF RAND:4 == 0
								PRINT 황홀한 표정으로 
							ELSEIF RAND:3 == 0
								PRINT 두손으로 사랑스럽게 
							ELSEIF RAND:2 == 0
								PRINT 손으로 강하게 
							ELSE
								PRINT 손가락으로 부드럽게 
							ENDIF
								IF RAND:4 == 0
									PRINT 문질렀다.
								ELSEIF RAND:3 == 0
									PRINT 감싸쥐었다.
								ELSEIF RAND:2 == 0
									PRINT 어루만졌다.
								ELSE
									PRINT 쓰다듬었다.
								ENDIF
						IF RAND:3 == 0
							IF TALENT :(ARG):109
								PRINT ＞그런 다음 발기한 유두를 필사적으로 문지르며, 애처로운 눈빛으로 
							ELSEIF TALENT :(ARG):110
								PRINT ＞그런 다음 귀여운 가슴에 열심히 페니스를 문지르며, 애처로운 눈빛으로 
							ELSEIF TALENT :(ARG):111
								PRINT ＞그런 다음 풍만한 가슴 사이에 페니스를 끼워넣고, 애처로운 눈빛으로 
							ELSEIF TALENT :(ARG):112
								PRINT ＞그런 다음 거대한 가슴 사이에 페니스를 파묻고, 애처로운 눈빛으로 
							ELSE
								PRINT ＞그런 다음 가슴 사이에 페니스를 끼워넣고, 애처로운 눈빛으로 
							ENDIF
						ELSEIF RAND:2 == 0
						PRINT ＞그러면서 자신에게 페니스가 침범하자 
							IF RAND:8 == 0
								PRINT 연결된 부분에서 추잡한 소리를 내며, 
							ELSEIF RAND:7 == 0
								PRINT 지나친 쾌락에 
									IF RAND:2 == 0
										PRINT 상대
											IF RAND:2 == 0
												PRINT 를 질타
											ELSE
												PRINT 를 응원
											ENDIF
										PRINT 하면서,
									ELSE
										PRINT 신음을 내며,
									ENDIF
							ELSEIF RAND:6 == 0
								PRINT 허리를 즐겁다는 듯이 흔들며, 
							ELSEIF RAND:5 == 0
								PRINT 허리를 음탕하게 흔들며, 
							ELSEIF RAND:4 == 0	
								PRINT 허리를 요염하게 흔들며, 
							ELSEIF RAND:3 == 0
								PRINT 허리를 달콤한 신음을 내며, 
							ELSEIF RAND:2 == 0
								PRINT 구멍을 강하게 조이며, 
							ELSE
								PRINT 구명을 천천히 조이며,  
							ENDIF
						ELSE
							IF RAND:5 == 0
								PRINT ＞점점 더 단단해지는 페니스
							ELSEIF RAND:4 == 0
								PRINT ＞조금씩 점액으로 얼룩져지는 페니스
							ELSEIF RAND:3 == 0
								PRINT ＞터질것 같이 부풀어오른 페니스
							ELSEIF RAND:2 == 0
								PRINT ＞조금씩 정액이 흘러나오는 페니스
							ELSE	
								PRINT ＞조금은 흐물흐물해진 페니스
							ENDIF
								IF RAND:9 == 0
									PRINT 의 단단한 귀두를 핥으며, 
								ELSEIF RAND:8 == 0
									PRINT 에서 조금씩 흘러나오는 정액을 핥으며, 
								ELSEIF RAND:7 == 0
									PRINT 의 요도를 혀로 괴롭히며, 
								ELSEIF RAND:6 == 0
									PRINT 를 천천히 삼키며, 
								ELSEIF RAND:5 == 0
									PRINT 를 강하게 빨면서, 
								ELSEIF RAND:4 == 0
									PRINT 를 애정을 담아 깨물면서, 
								ELSEIF RAND:3 == 0
									PRINT 를 목 깊숙히 집어 넣고, 
								ELSEIF RAND:2 == 0
									PRINT 를 혀로 깨끗하게 하면서, 
								ELSE	
									PRINT 를 천천히 관찰하며, 
								ENDIF
						ENDIF
							IF RAND:2 == 0
								PRINT 악마
							ELSE
								PRINT 남자
							ENDIF
									IF RAND:4 == 0	
										PRINT 의 욕정을 부추기며 
									ELSEIF RAND:3 == 0
										PRINT 를 쾌락에 빠트리며 
									ELSEIF RAND:2 == 0
										PRINT 를 기쁘게 만들며 
									ELSE
										PRINT 를 흥분시켜 
									ENDIF
										IF RAND:5 == 0
											PRINTFORML 자신이 주는 쾌락의 포로로 바꾸어 갔다…
										ELSEIF RAND:4 == 0
											PRINTFORML 페니스로부터 대량의 정액을 짜냈다…
										ELSEIF RAND:3 == 0
											PRINTFORML 천천히 페니스를 문질렀다…
										ELSEIF RAND:2 == 0
											PRINTFORML 그들의 정신을 침식해 갔다…
										ELSE
											PRINTFORML 그들을 유혹했다…
										ENDIF
				ELSEIF RAND:6 == 0
					PRINTFORM ＞악마들에게 둘러쌓인 %조사처리(CALLNAME:ARG,"는")% 
						IF RAND:6 == 0
							PRINT 자신을 둘러싼 페니스들에 
						ELSEIF RAND:5 == 0
							PRINT 자신을 바라보는 커다란 페니스를 보고 
						ELSEIF RAND:4 == 0
							PRINT 남자들의 개성적인 페니스를 보며 
						ELSEIF RAND:3 == 0
							PRINT 앞으로의 일을 상상하고는 
						ELSEIF RAND:2 == 0
							PRINT 숨이 막힐듯한 악취에 
						ELSE
							PRINT 다양한 크기의 페니스를 보고 
						ENDIF
							IF RAND:6 == 0
								PRINT 굉장히 흥분했다.
							ELSEIF RAND:5 == 0
								PRINT 요염하게 미소지었다.
							ELSEIF RAND:4 == 0
								PRINT 숨을 삼켰다.
							ELSEIF RAND:3 == 0
								PRINT 한층 더 젖어들었다.
							ELSEIF RAND:2 == 0
								PRINT 황홀한 표정을 지었다.
							ELSE
								PRINT 자기도 모르게 가랑이를 문질렀다.
							ENDIF
								IF RAND:5 == 0
									PRINT ＞그리고는 입으로 가까이 있던 페니스를 삼키며 
								ELSEIF RAND:4 == 0
									PRINT ＞그리고는 하나하나 손으로 만져가며 
								ELSEIF RAND:3 == 0
									PRINT ＞그리고는 커다란 순서대로 
								ELSEIF RAND:2 == 0
									PRINT ＞그리고는 앞뒤의 구멍을 손가락으로 벌려 
								ELSE
									PRINT ＞그리고는 엉덩이를 높게 들고 
								ENDIF
									IF RAND:4 == 0
										PRINTFORMW 페니스의 단단함을 느끼기 시작했다.
									ELSEIF RAND:3 == 0
										PRINTFORMW 스스로 자위를 하기 시작했다.
									ELSEIF RAND:2 == 0
										PRINTFORMW 야릇한 목소리로 삽입을 졸랐다.
									ELSE
										PRINTFORMW 가까이 있던 사람부터 받아 들였다.
									ENDIF
				ELSEIF RAND:5 == 0
					IF RAND:4 == 0
						PRINTFORM ＞악마들은 %CALLNAME:ARG%의 엉덩이를 들어 올리면서, 
					ELSEIF RAND:3 == 0
						PRINTFORM ＞악마들은 %CALLNAME:ARG%의 허리를 거칠게 붙잡으면서, 
					ELSEIF RAND:2 == 0
						PRINTFORM ＞악마들은 %조사처리(CALLNAME:ARG,"를")% 사랑하는듯이 안아올리면서, 
					ELSE
						PRINTFORM ＞악마들은 %조사처리(CALLNAME:ARG,"를")% 짐승처럼 격렬하게 범하면서, 
					ENDIF
						IF RAND:2 == 0
							IF TALENT :(ARG): 109
								PRINT 조그마한 
							ELSEIF TALENT :(ARG): 110
								PRINT 귀여운 
							ELSEIF TALENT :(ARG): 111
								PRINT 풍만한 
							ELSEIF TALENT :(ARG): 112
								PRINT 거대한 
							ELSE
								PRINT 
							ENDIF
								IF RAND:2 == 0
									PRINT 가슴을 
								ELSE
									PRINT 가슴의 유두를 
								ENDIF
									IF RAND:2 == 0
										PRINT 꼬집으며 
									ELSE
									ENDIF
										IF RAND:3 == 0
											PRINTL 괴롭혔다.
										ELSEIF RAND:2 == 0
											PRINTL 부드럽게 쓰다듬었다.
										ELSE
											PRINTL 강하게 빨았다.
										ENDIF
						ELSE
							IF RAND:4 == 0
								PRINT 자극을 요구하는 
							ELSEIF RAND:3 == 0
								PRINT 충분히 젖어든 
							ELSEIF RAND:2 == 0
								PRINT 사랑스러운 
							ELSE
								PRINT 떨리는 
							ENDIF
								IF RAND:3 == 0
									PRINT 애널에 
								ELSEIF RAND:2 == 0
									PRINT 구멍에 
								ELSE
									PRINT 음부에 
								ENDIF	
									IF RAND:3 == 0
										PRINTL 거칠게 침입했다.
									ELSEIF RAND:2 == 0
										PRINTL 농락하며 천천히 삽입했다.
									ELSE
										PRINTL 한번에 끝까지 삽입했다.
									ENDIF	
						ENDIF
						PRINTFORM ＞그러자 %조사처리(CALLNAME:ARG,"는")% 오히려 
							IF RAND:5 == 0
								PRINT 스스로 허리를 흔들며, 
							ELSEIF RAND:4 == 0
								PRINT 기쁨에 찬 교성을 지르며, 
							ELSEIF RAND:3 == 0
								PRINT 한층 더 쾌락을 요구하며, 
							ELSEIF RAND:2 == 0
								PRINT 짐승처럼 울부짖으며, 
							ELSE
								PRINT 격렬한 쾌락에 빠져들며, 
							ENDIF
								IF RAND:3 == 0
									PRINTFORML 절정에 다다른 페니스를 강하게 조였다…
								ELSEIF RAND:2 == 0
									PRINTFORML 단숨에 절정으로 치닫았다…
								ELSE
									PRINTFORML 사정하는 악마의 정액을 자궁으로 받아들였다…
								ENDIF
				ELSEIF RAND:4 == 0
					PRINTFORM ＞악취가 심한 페니스에 둘러쌓인 %조사처리(CALLNAME:ARG,"는")% 
						IF RAND:4 == 0
							PRINT 기쁜 표정으로 페니스를 핥으며, 
						ELSEIF RAND:3 == 0
							PRINT 거부감없이 입으로 페니스를 핥으며, 
						ELSEIF RAND:2 == 0
							PRINT 입으로 더러움을 청소하듯이 핥으며, 
						ELSE
							PRINT 입안에 퍼지는 악취에 황홀한 표정을 지으며, 
						ENDIF
							IF RAND:4 == 0
								PRINTFORML 악마가 사정한 정액을 얼굴에 뒤집어 썼다…
							ELSEIF RAND:3 == 0
								PRINTFORML 근처에 있던 다른 악마를 받아들였다…
							ELSEIF RAND:2 == 0
								PRINTFORML 참지못하고 스스로 자위를 하기 시작했다…
							ELSE
								PRINTFORML 요염하게 허리를 흔들어 다른 악마를 유혹했다…
							ENDIF
				ELSEIF RAND:3 && TALENT :(ARG):139
					IF RAND:3 == 0
						PRINTFORM ＞%조사처리(CALLNAME:ARG,"를")% 둘러싼 악마들은 그녀의 페니스를 
							IF RAND:3 == 0
								PRINTL 장난감을 다루듯이 거칠게 어루만졌다. 
							ELSEIF RAND:2 == 0
								PRINTL 귀엽다는 듯이 상냥하게 어루만졌다. 
							ELSE
								PRINTL 마치 자신의 것처럼 익숙하게 어루만졌다. 
							ENDIF
								IF RAND:4 == 0
									PRINTFORM ＞그러면서 약점이라 생각되는 귀두를 
								ELSEIF RAND:3 == 0
									PRINTFORM ＞그러면서 약점이라 생각되는 요도를 
								ELSEIF RAND:2 == 0
									PRINTFORM ＞그러면서 약점이라 생각되는 육봉을 
								ELSE
									PRINTFORM ＞그러면서 약점이라 생각되는 부분을 
								ENDIF
									IF RAND:3 == 0
										PRINT 어루만지며 
									ELSEIF RAND:2 == 0
										PRINT 괴롭히면서 
									ELSE
										PRINT 강하게 감싸며 
									ENDIF
										IF RAND:2 == 0
											PRINTL %CALLNAME:ARG%의 정액을 짜넀다…
										ELSE
											PRINTL %조사처리(CALLNAME:ARG,"를")% 절정시켰다…
										ENDIF
					ELSEIF RAND:2 == 0
						PRINTFORML ＞악마들의 애무를 기쁘게 받아들이던 %CALLNAME:ARG%의 페니스는 갑자기 크게 부풀어 오르더니, 주변에 정액을 흩뿌렸다…
					ELSE
						PRINTFORML ＞악마들이 %CALLNAME:ARG%의 페니스를 거칠게 만져대자, 금새 자신의 페니스가 주는 쾌락에 빠져들었다…
					ENDIF
				ELSEIF RAND:2 && TALENT :(ARG):57
					IF RAND:4 == 0
						SIF ABL :(ARG):노출증 > 5
						PRINTFORMW ＞M 모양으로 다리를 벌린 %조사처리(CALLNAME:ARG,"는")% 클리토리스를 만지며 절정과 동시에 오줌을 싸는 재주를 주변에 선보였다…
					ELSEIF RAND:3 == 0
						PRINTFORML ＞네발로 기어다니게 된 %조사처리(CALLNAME:ARG,"는")% 뒤쪽에 선 악마에게 범해지면서 한쪽 다리를 들어 마치 개처럼 오줌을 눴다…
					ELSEIF RAND:2 == 0
						PRINTFORML ＞뒤에서부터 다리를 잡혀 들어올려진 %CALLNAME:ARG%의 음부가 양쪽으로 크게 벌려졌다.
							IF ABL :(ARG):노출증 > 5
								PRINT ＞그리고는 담담한 표정으로, 
							ELSE
								PRINT > 그리고는 치욕에 얼굴을 붉히며, 
							ENDIF
								IF RAND:2 == 0
									PRINTL 오줌을 싸는 모습을 보여버렸다…
								ELSE
									PRINTL 갑자기 시작된 방뇨에 주변 악마들의 비웃음을 샀다…
								ENDIF
					ELSE
						PRINTFORML ＞%조사처리(CALLNAME:ARG,"는")% 가혹한 행위에 몇번이나 절정을 맞이해, 마침내 오줌을 싸버렸다…
					ENDIF
				ELSE
					PRINTFORM ＞악마들은 
						IF RAND:3 == 0
							PRINT 음탕한 목소리로 졸라대며, 
						ELSEIF RAND:2 == 0
							PRINT 계속해서 허리를 흔들며, 
						ELSE
							PRINT 쾌감에 절여든 신음소리와 함께, 
						ENDIF
						PRINTFORM %CALLNAME:ARG%
							IF RAND:5 == 0
								PRINTL 에게 앞뒤로 번갈아가며 삽입했다.
							ELSEIF RAND:4 == 0
								PRINTL 의 질내에 사정하며 절정을 맞이했다.
							ELSEIF RAND:3 == 0
								PRINTL 의 애무에 절정을 맞이했다.
							ELSEIF RAND:2 == 0
								PRINTL 를 크고작은 페니스들로 유린했다.
							ELSE
								PRINTL 에게 몇번이나 사정했다.
							ENDIF
								PRINT ＞그렇게 그들은 
									IF RAND:2 == 0
										PRINTFORML 점차 격렬한 쾌락에 몸을 맡기기 시작했다… 
									ELSE
										PRINTFORML 공허한 눈동자로 더욱 쾌락을 탐하기 시작했다… 
									ENDIF
				ENDIF
RESETCOLOR
			ELSE
		PRINTFORMW ＞%조사처리(CALLNAME:ARG,"는")% 태어난 그대로의 모습을 드러내고, 비밀스런 장소에 손가락을 넣어 좌우로 펼치며, 안타까운 목소리로 적을 유혹했다.
	ENDIF
ELSE
PRINTFORMW ＞%조사처리(CALLNAME:ARG,"는")% 태어난 그대로의 모습을 드러내고, 비밀스런 장소에 손가락을 넣어 좌우로 펼치며, 안타까운 목소리로 적을 유혹했다.
ENDIF
CALL FIELD_BADSTATE,ARG,ARG:1,1214







;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_1214
PRINTL [후열] 적 전체를 정신속성 공격으로 유혹한다. (매우 높은 확률로 CHARM)

;継承に필요な部位
@継承部位_1214,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_1214,ARG
RETURN 1
;とくにない

