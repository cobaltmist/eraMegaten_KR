;만능相性で랜덤상태이상부여
;アバチュボス勢に大인기스킬……まぁ아군ではまじつかわないんですがこれ
;스킬명
@SKILL_NAME_2066,ARG
RESULTS = 색즉시공

;　명중
@SKILL_HITRATE_2066, ARG
RETURN 100

;　위력
@SKILL_POWER_2066, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2066,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2066,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2066, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2066,ARG
RETURN 35

;　상성
@SKILL_TYPE_2066,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2066, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2066, ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2066
RETURN 1

;　사거리
@SKILL_RANGE_2066,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2066,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2066
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2066,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2066
RETURN 20
;질풍

;속도보정
@SKILL_SPEED_2066, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2066,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2066
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2066
RETURN 15

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2066,ARG
A = RAND:11
IF A == 0
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF A == 1
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 2
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 3
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 4
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 5
	RETURN GET_STATE_NUM("CLOSE")
ELSEIF A == 6
	RETURN GET_STATE_NUM("POISON")
ELSEIF A == 7
	RETURN GET_STATE_NUM("BOMB")
ELSEIF A == 8
	RETURN GET_STATE_NUM("PALYZE")
ELSEIF A == 9
	RETURN GET_STATE_NUM("SHOCK")
ELSE
	RETURN GET_STATE_NUM("FREEZE")
ENDIF

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2066,ARG
SIF ARG >= 0 && CFLAG:ARG:보스플래그 > 0 && 0 == RAND:2
	RETURN 17
IF A < 4
	RETURN GET_TYPE_NUM("정신")
ELSEIF A < 9
	RETURN GET_TYPE_NUM("신경")
ELSEIF A == 9
	RETURN GET_TYPE_NUM("전격")
ELSE
	RETURN GET_TYPE_NUM("빙결")
ENDIF

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2066,ARG
RETURN 50

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2066,ARG
RETURN 95

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2066,ARG,ARG:1
#LOCALSIZE 1
FOR LOCAL, CFLAG:ARG:PT플래그 ? 7 # 1, CFLAG:ARG:PT플래그 ? 17 # 7
	SIF POS(LOCAL) < 0
		CONTINUE
	SIF GET_STATE(CFLAG:POS(LOCAL):상태이상) == "DYING"
		CONTINUE
	CALL SINGLE_BADSTATE, ARG, POS(LOCAL), 2066
NEXT





;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2066
PRINTL [후열] 적 전체에 무작위 상태이상을 건다. (보통 확률로 무작위 상태이상)

;계승에 필요한 부위
@계승부위_2066,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2066,ARG
RETURN 1
;とくにない

