;サマエル専用
;真3では만능속성で상태이상は対応した내성があれば防げる。よくわからない。
;とりあえず추가효과상성を신경정신랜덤にしといた。
;데미지ないし추가효과부여率高い。파이널누드と同じ。

;스킬名
@SKILL_NAME_2086,ARG
RESULTS = 신의 악의

;　명중
@SKILL_HITRATE_2086, ARG
RETURN 100

;　위력
@SKILL_POWER_2086, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2086,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2086,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2086, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2086,ARG
RETURN 30

;　相性
@SKILL_TYPE_2086,ARG
RETURN 17
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2086, ARG
RETURN 1
;마법

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2086, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2086
RETURN 1

;　사거리
@SKILL_RANGE_2086,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2086,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2086
RETURN 3

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2086,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2086
RETURN 20

@継承不能_2086
RETURN 1
;

;속도보정
@SKILL_SPEED_2086, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2086,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2086
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2086
RETURN 23
;변화不能

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2086,ARG
A = RAND:6
IF A == 0
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 1
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 2
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 3
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 4
	RETURN GET_STATE_NUM("CLOSE")
ELSE
	RETURN GET_STATE_NUM("PALYZE")
ENDIF

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2086,ARG
IF A < 3
	RETURN 9
ELSE
	RETURN 8
ENDIF
RETURN 8

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2086,ARG
RETURN 80

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2086,ARG
RETURN 95

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2086,ARG,ARG:1
CALL FIELD_BADSTATE,ARG,ARG:1,2086






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2086
PRINTL [후열] 적 전체에 무작위 상태이상을 건다. (매우 높은 확률로 무작위 상태이상)



;継承に필요한部位
@継承部位_2086,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2086,ARG
RETURN 1
;とくにない

