;サマエル専用
;真3では万能属性で状態異常は対応した耐性があれば防げる。よくわからない。
;とりあえず追加効果相性を神経精神랜덤하게しといた。
;ダメージないし追加効果付与率高い。ファイナルヌードと同じ。

;스킬명
@SKILL_NAME_2086,ARG
RESULTS = 신의 악의

;　명중
@SKILL_HITRATE_2086, ARG
RETURN 100

;　위력
@SKILL_POWER_2086, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2086,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2086,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2086, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2086,ARG
RETURN 30

;　상성
@SKILL_TYPE_2086,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2086, ARG
RETURN 1
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2086, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2086
RETURN 1

;　사거리
@SKILL_RANGE_2086,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2086,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2086
RETURN 3

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2086,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2086
RETURN 20

@계승불능_2086
RETURN 1
;

;속도보정
@SKILL_SPEED_2086, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2086,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2086
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2086
RETURN 23
;변화不能

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2086,ARG
A = RAND:6
IF A == 0
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 1
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 2
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 3
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 4
	RETURN GET_STATE_NUM("CLOSE")
ELSE
	RETURN GET_STATE_NUM("PALYZE")
ENDIF

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2086,ARG
IF A < 3
	RETURN 9
ELSE
	RETURN 8
ENDIF
RETURN 8

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2086,ARG
RETURN 80

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2086,ARG
RETURN 95

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2086,ARG,ARG:1
CALL FIELD_BADSTATE,ARG,ARG:1,2086






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2086
PRINTL 적 전체에 랜덤한 상태이상을 부여한다



;계승에 필요한 부위
@계승부위_2086,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2086,ARG
RETURN 1
;とくにない

