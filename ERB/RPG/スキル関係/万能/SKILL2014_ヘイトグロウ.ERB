;メフィスト用

;스킬명
@SKILL_NAME_2014,ARG
RESULTS = 헤이트그로우

;　명중
@SKILL_HITRATE_2014, ARG
RETURN 9999

;　威힘
@SKILL_POWER_2014, ARG
#LOCALSIZE 3
SIF ARG < 0 || 스킬속성표시중
	RETURN 500
SIF CFLAG:ARG:타겟 == 19
	RETURN 500
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL == -1
	RETURN 500
LOCAL:1 = MAXBASE:LOCAL:파마 == 999 ? -200 # MAXBASE:LOCAL:파마
;念のため
SIF LOCAL:1 == 0
	RETURN 500
	
LOCAL:2 = 500 - (LOCAL:1 * 5 / 2)
LOCAL:2 = LOCAL:2 < 10 ? 10 # LOCAL:2
RETURN LOCAL:2

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2014,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2014,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2014, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2014,ARG
RETURN 50

@SKILL_TYPE_2014, ARG
RETURN GET_TYPE_NUM("만능")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2014, ARG
RETURN 2

;魔法
;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2014, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2014
RETURN 1

;　사거리
@SKILL_RANGE_2014,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2014,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2014
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2014,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입,継承不能
@SKILL_SUCCESSION_TYPE_2014
RETURN 20

@継承不能_2014
RETURN 1

;속도보정
@SKILL_SPEED_2014, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2014,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2014
RETURN 0

;스킬변화？용 랭크,もちろん不可
@SKILL_RANK_2014
RETURN 15

@SKILL_RANKUP_2014
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2014,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2014,ARG
RETURN 0

;基礎値高いけど上限は低め
@SKILL_ADDTIONAL_BASE_RATE_2014,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2014,ARG
RETURN 0
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2014, ARG, ARG:1
IF CFLAG:ARG:포지션 < 7
	LOCAL:1 = 7
	LOCAL:2 = 17 
ELSE
	LOCAL:1 = 1
	LOCAL:2 = 7
ENDIF
FOR LOCAL, LOCAL:1, LOCAL:2
	LOCALS = 포지션{LOCAL}
	;#;PRINTFORML FLAG:LOCALS = {FLAG:LOCALS}
	SIF FLAG:LOCALS < 0 || GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
	CFLAG:ARG:타겟 = LOCAL
	CALL ATTACK_SINGLE, ARG, FLAG:LOCALS, 2014
NEXT
CFLAG:ARG:타겟 = CFLAG:ARG:포지션 < 7 ? 22 # 19

;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2014
PRINTL [후열] 적 전체에 만능속성 초특대 마법데미지. 
PRINTL 적의 파마속성 내성에 따라 위력이 변한다. 명중률이 이상하게 높다.

;계승에 필요한 부위
@계승부위_2014,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2014,ARG
RETURN 1
;とくにない

