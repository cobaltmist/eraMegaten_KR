
;스킬명
@SKILL_NAME_2082,ARG
RESULTS = 태초에 숨은 악

;　명중
@SKILL_HITRATE_2082, ARG
RETURN 115

;　위력
@SKILL_POWER_2082, ARG
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2082,ARG
RETURN 1
;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2082,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2082, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2082,ARG
RETURN 100

;　상성
@SKILL_TYPE_2082,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2082, ARG
RETURN 1
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2082, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2082
RETURN 1

;　사거리
@SKILL_RANGE_2082,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2082,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2082
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2082,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2082
RETURN GET_SUCCESSION_NUM("만능")

@계승불능_2082
RETURN 1
;特殊


;속도보정
@SKILL_SPEED_2082, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2082,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2082
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2082
RETURN 10

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2082,ARG
SIF RAND:3
	RETURN GET_STATE_NUM("CLOSE")
SIF RAND:2
	RETURN GET_STATE_NUM("PALYZE")
RETURN GET_STATE_NUM("POISON")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2082,ARG
RETURN 9

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2082,ARG
RETURN 10

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2082,ARG
RETURN 40
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2082,ARG,ARG:1
IF CFLAG:(ARG):PT플래그 == 0
	LOCAL:7 = 1
	LOCAL:8 = 7
ELSE
	LOCAL:7 = 7
	LOCAL:8 = 17
ENDIF

FOR LOCAL, LOCAL:7, LOCAL:8
	LOCALS = 포지션{LOCAL}
	SIF FLAG:LOCALS < 0 || FLAG:LOCALS == ARG || CFLAG:(FLAG:LOCALS):보스플래그
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
	CALLFORM JUDG_HIT_2,ARG,FLAG:LOCALS,100
	IF RESULT == 1
		;命中した
		IF RAND:10 < 1
			;８３％（６分の５）ダメ、流石に90%ダメは酷いよね
			PRINTFORM 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
			PRINTFORML {(BASE:(FLAG:LOCALS):ＨＰ)*5 / 6}데미지
			CALL VAR_HP, FLAG:LOCALS, -((BASE:(FLAG:LOCALS):ＨＰ) / 10),0
		ELSEIF RAND:9 < 4
			;７５％ダメ
			PRINTFORM 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
			PRINTFORML {(BASE:(FLAG:LOCALS):ＨＰ)*3 / 4}데미지
			CALL VAR_HP, FLAG:LOCALS, -((BASE:(FLAG:LOCALS):ＨＰ)*3 / 4),0
		ELSEIF RAND:5 < 2
			;５０％ダメ
			PRINTFORM 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
			PRINTFORML {(BASE:(FLAG:LOCALS):ＨＰ) / 2}데미지
			CALL VAR_HP, FLAG:LOCALS, -((BASE:(FLAG:LOCALS):ＨＰ) / 2),0
		ELSE
			CALL SINGLE_BADSTATE,ARG,FLAG:LOCALS,2082
		ENDIF
	ELSE
		;外したらデカジャ効果
		PRINTFORML 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　Miss
		FOR LOCAL:1,0,6
			CFLAG:(FLAG:LOCALS):(GET_BATTLESTATUS(LOCAL:1) + "강화") = MIN(0, CFLAG:(FLAG:LOCALS):(GET_BATTLESTATUS(LOCAL:1) + "강화"))
		NEXT
		PRINTFORML 강화된 능력이 되돌아왔다！
	ENDIF
NEXT



;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2082
PRINTL 적 전체에 랜덤효과 발동

;계승에 필요한 부위
@계승부위_2082,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2082,ARG
RETURN 1
;とくにない
