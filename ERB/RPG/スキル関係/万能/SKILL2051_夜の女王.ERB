;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2051,ARG
RESULTS = 밤의 여왕

;　명중
@SKILL_HITRATE_2051, ARG
RETURN 125

;　위력
@SKILL_POWER_2051, ARG = -1
	RETURN 300

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2051,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2051,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2051, ARG
RETURN 1
;소비なし＝1

;　コストの量
@SKILL_COST_2051,ARG
RETURN 0

;　相性
@SKILL_TYPE_2051,ARG
RETURN 17
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2051, ARG
RETURN 1
;마법

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2051, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2051
RETURN 1

;　사거리
@SKILL_RANGE_2051,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2051,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2051
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2051,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2051
RETURN 20

;

;속도보정
@SKILL_SPEED_2051, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2051,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2051
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2051
RETURN 0

@적専用_2051
RETURN 1

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2051,ARG
IF RAND:4 == 0
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF RAND:3 == 0
	RETURN GET_STATE_NUM("PANIC")
ELSEIF RAND:2 == 0
	RETURN GET_STATE_NUM("SLEEP")
ELSE
	RETURN GET_STATE_NUM("CHARM")
ENDIF
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2051,ARG
;만능
RETURN 17

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2051,ARG
RETURN 25

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2051,ARG
RETURN 50
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2051,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,2051






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2051
PRINTL [후열] 적 전체에 만능속성 대 마법데미지. 명중률이 높다. (낮은 확률로 정신계 상태이상)

;継承に필요한部位
@継承部位_2051,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2051,ARG
RETURN 1
;とくにない
