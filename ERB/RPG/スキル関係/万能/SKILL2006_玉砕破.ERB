;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2006,ARG
IF SKILL_CHANGE(ARG, [[キャラ:E졸다트]])
	RESULTS = Blitz bombe
ELSE
	RESULTS = 옥쇄파
ENDIF

;　명중
@SKILL_HITRATE_2006, ARG
RETURN 140

;　위력
@SKILL_POWER_2006, ARG
RETURN 500

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2006,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2006,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2006, ARG
RETURN 1
;소비なし＝1

;　コストの量
@SKILL_COST_2006,ARG
RETURN 5

;　相性
@SKILL_TYPE_2006,ARG
IF SKILL_CHANGE(ARG, [[キャラ:E졸다트]])
	RETURN 6
ELSE
	RETURN 17
ENDIF
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2006, ARG
RETURN 1
;마법

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2006, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2006
RETURN 1

;　사거리
@SKILL_RANGE_2006,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2006,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2006
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2006,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2006
RETURN 20

@継承条件無視_2006
RETURN 1
;全て

;속도보정
@SKILL_SPEED_2006, ARG = -1
RETURN -5

;　구상の표시とその타입
@SKILL_MESSAGE_2006,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2006
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2006
RETURN 8


;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2006,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2006,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2006,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2006,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2006,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,2006

SIF CFLAG:ARG:상태이상 < GET_STATE_NUM("DYING")
	CALL VAR_HP,ARG,-BASE:ARG:ＨＰ,0



;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2006
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:E졸다트]]
	PRINTL 電光機関により生命力を変換し命と引き換えに敵全体に전격데미지
	PRINTL 옥쇄파のEゾルダート専用化スキル
ELSE
	PRINTL [후열] 목숨과 맞바꿔, 적 전체에 만능속성 초특대 물리데미지. 명중률이 아주 높고, 발동이 느리다.
ENDIF

;継承に필요한部位
@継承部位_2006,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2006,ARG
RETURN 1
;とくにない

