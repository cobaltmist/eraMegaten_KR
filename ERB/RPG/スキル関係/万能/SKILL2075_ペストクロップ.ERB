
;스킬名
@SKILL_NAME_2075,ARG
RESULTS = 전염병 생산

;　명중
@SKILL_HITRATE_2075, ARG
RETURN 100
;　위력
@SKILL_POWER_2075, ARG = -1
RETURN 160

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2075,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2075,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2075, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2075,ARG
RETURN 26

;　相性
@SKILL_TYPE_2075,ARG
RETURN 17
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2075, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2075, ARG
RETURN 2

;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2075
RETURN 1

;　사거리
@SKILL_RANGE_2075,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2075,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2075
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2075,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2075
RETURN 20
;만능

@継承不能_2075
RETURN 1
;

;속도보정
@SKILL_SPEED_2075, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2075,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2075
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2075
RETURN 5

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2075,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2075,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2075,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2075,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2075,ARG,ARG:1
IF CFLAG:(ARG):PTフラグ == 0
	LOCAL:7 = 1
	LOCAL:8 = 7
ELSE
	LOCAL:7 = 7
	LOCAL:8 = 17
ENDIF

FOR LOCAL,LOCAL:7,LOCAL:8
	LOCALS = 포지션{LOCAL}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF CFLAG:(FLAG:LOCALS):ステート == GET_STATE_NUM("DYING")
		CONTINUE
	CALL ATTACK_SINGLE,ARG,FLAG:LOCALS,2075
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF CFLAG:(FLAG:LOCALS):ステート != GET_STATE_NUM("POISON")
		CONTINUE
	PRINTFORM 　　TARGET:[{LOCAL,2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
	CALL ATTACK_BADSTATE,ARG,FLAG:LOCALS,GET_STATE_NUM("DYING"),17,200,95
NEXT

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2075
PRINTL [후열] 적 전체에 만능속성 중 마법데미지. POISON상태라면 적을 즉사시킨다.

;継承に필요한部位
@継承部位_2075,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2075,ARG
RETURN 1
;とくにない

