;

;스킬명
@SKILL_NAME_2024,ARG
RESULTS = 천벌

;　명중
@SKILL_HITRATE_2024, ARG
RETURN 9999

;　위력
@SKILL_POWER_2024, ARG = -1
RETURN 500

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2024,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2024,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2024, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2024,ARG
RETURN 45

;　상성
@SKILL_TYPE_2024,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2024, ARG
RETURN 1
;魔法

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2024, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2024
RETURN 4

;　사거리
@SKILL_RANGE_2024,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2024,ARG
RETURN 4

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2024
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2024,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2024
RETURN 20

@계승불능_2024
RETURN 1
;不可

;속도보정
@SKILL_SPEED_2024, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2024,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2024
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2024
RETURN 23
;変化不能

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2024,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2024,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2024,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2024,ARG
RETURN 0
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2024,ARG,ARG:1
FOR LOCAL, 1, 17
	SIF POS(LOCAL) < 0 || GET_STATE(CFLAG:POS(LOCAL):상태이상) == "DYING"
		CONTINUE
	SIF ABL:ARG:속성LC == ABL:POS(LOCAL):속성LC
		CONTINUE
	CALL ATTACK_SINGLE,ARG,POS(LOCAL),2024
NEXT
;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2024
PRINTL 데미지。사용자와 LAW/CHAOS 속성이 다른 캐릭터만 영향을 받는다

;계승에 필요한 부위
@계승부위_2024,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2024,ARG
RETURN 1
;とくにない

