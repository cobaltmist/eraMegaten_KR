;

;스킬명
@SKILL_NAME_2043,ARG
RESULTS = 돌핀킥

;　명중
@SKILL_HITRATE_2043, ARG
RETURN 100

;　위력
@SKILL_POWER_2043, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2043,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2043,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2043, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_2043,ARG
RETURN 0

;　상성
@SKILL_TYPE_2043,ARG
RETURN 17
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2043, ARG
RETURN 1
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2043, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2043
RETURN 1

;　사거리
@SKILL_RANGE_2043,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2043,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2043
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2043,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2043
RETURN 20
;만능

;속도보정
@SKILL_SPEED_2043, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2043,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2043
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2043
RETURN 23
;변화不能

@적전용_2043
RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2043,ARG
RETURN 12

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2043,ARG
RETURN 10

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2043,ARG
RETURN 35

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2043,ARG
RETURN 95
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2043,ARG,ARG:1
IF ARG:1 == 22
	LOCAL:1 = 7
	LOCAL:2 = 17
ELSE
	LOCAL:1 = 1
	LOCAL:2 = 7
ENDIF

FOR LOCAL,LOCAL:1,LOCAL:2
	LOCALS = 포지션{LOCAL}
	SIF FLAG:LOCALS < 0 || FLAG:LOCALS == ARG
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
	PRINTFORM 　　TARGET:[{CPOS(FLAG:LOCALS),2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
	CALLFORM JUDG_HIT_1,ARG,FLAG:LOCALS,50
	IF RESULT == 1 && CFLAG:(FLAG:LOCALS):보스플래그 == 0
		;명중した
		SELECTCASE LOCAL
			CASE 1 TO 3,7 TO 11
				PRINTFORMW {BASE:(FLAG:LOCALS):ＨＰ/2}데미지
				CALL VAR_HP,FLAG:LOCALS,-(BASE:(FLAG:LOCALS):ＨＰ/2),0
			CASEELSE
				PRINTFORMW {BASE:(FLAG:LOCALS):ＨＰ/4}데미지
				CALL VAR_HP,FLAG:LOCALS,-(BASE:(FLAG:LOCALS):ＨＰ/4),0
		ENDSELECT
	ELSE
		;外した
		PRINTW MISS
	ENDIF
NEXT





;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2043
PRINTL [후열] 민능속성 공격으로 적 전열에 현 HP의 1/2、후열에 HP의 1/4의 데미지. (보통 확률로 CURSE)

;계승에 필요한 부위
@계승부위_2043,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2043,ARG
RETURN 1
;とくにない

