;스킬名
@SKILL_NAME_2555,ARG
IF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RESULTS = 앙갈타 키갈슈
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RESULTS = 앙갈타 키갈슈
ELSEIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RESULTS = 트리슐라 샤크티
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RESULTS = 트리슐라 샤크티
ELSEIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RESULTS = 퀸텟 포이어
ELSE
	RESULTS = 레일건
ENDIF

;　명중
@SKILL_HITRATE_2555, ARG
RETURN 100

;　위력
@SKILL_POWER_2555, ARG
IF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 600
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 600
ELSEIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 230
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 230
ELSEIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN 640
ELSE
	RETURN 500
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2555,ARG
IF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 3
ELSE
	RETURN 1
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2555,ARG
IF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 3
ELSE
	RETURN 1
ENDIF

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2555, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2555,ARG
IF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 60
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 60
ELSEIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 65
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 65
ELSEIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN 60
ELSE
	RETURN 40
ENDIF

;　相性
@SKILL_TYPE_2555,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
IF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN GET_TYPE_NUM("충격")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN GET_TYPE_NUM("충격")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN GET_TYPE_NUM("빙결")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN GET_TYPE_NUM("빙결")
ELSEIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN GET_TYPE_NUM("만능")
ELSE
	SIF ARG < 0 || 스킬속성표시中
		RETURN GET_TYPE_NUM("전격")
	SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
		RETURN GET_TYPE_NUM("전격")
	LOCAL = POS(CFLAG:ARG:타겟)
	SIF LOCAL < 0
		RETURN GET_TYPE_NUM("전격")
	LOCAL:2 = -300
	LOCALS = 전격
	LOCALS:1 = 비구
	LOCALS:2 = 전격
	FOR LOCAL:1, 1, 3
		;판정用に数値を仕舞う。반사は-200扱い
		LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
		LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
		IF LOCAL:3 > LOCAL:2
			LOCAL:2 = LOCAL:3
			LOCALS = %LOCALS:(LOCAL:1)%
		ENDIF
	NEXT
	RETURN GET_TYPE_NUM(LOCALS)
ENDIF

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2555, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2555, ARG
RETURN 2

;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2555
RETURN 1

;　사거리	1:S 2:M 3:L
@SKILL_RANGE_2555,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2555,ARG
IF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 3
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 3
ELSE
	RETURN 1
ENDIF

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2555
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2555,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2555
RETURN 9

@継承不能_2555
RETURN 1

;속도보정
@SKILL_SPEED_2555, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2555,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2555
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2555
RETURN 0

@SKILL_RANKUP_2555
RETURN 0

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2555,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN GET_STATE_NUM("CHARM")
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN GET_STATE_NUM("CHARM")
SIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN 0
RETURN GET_STATE_NUM("SHOCK")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2555,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN GET_TYPE_NUM("빙결")
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN GET_TYPE_NUM("빙결")
SIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN 0
RETURN 6

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2555,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 60
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 60
SIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN 0
RETURN 80

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2555,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	RETURN 0
SIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	RETURN 80
SIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	RETURN 80
SIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	RETURN 0
RETURN 95

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2555,ARG,ARG:1
#LOCALSIZE 5
IF SKILL_CHANGE(ARG, [[キャラ:이슈타르]])
	CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 8)
	PRINTFORMW 마법위력이 크게 올라갔다！
	CALL ATTACK_FIELD,ARG,ARG:1,2555
ELSEIF SKILL_CHANGE(ARG, [[キャラ:린Fate]])
	CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 8)
	PRINTFORMW 마법위력이 크게 올라갔다！
	CALL ATTACK_FIELD,ARG,ARG:1,2555
ELSEIF SKILL_CHANGE(ARG, [[キャラ:파르바티]])
	CALL ATTACK_FIELD,ARG,ARG:1,2555
ELSEIF SKILL_CHANGE(ARG, [[キャラ:사쿠라]])
	CALL ATTACK_FIELD,ARG,ARG:1,2555
ELSEIF SKILL_CHANGE(ARG, [[キャラ:이리야]])
	CFLAG:ARG:마법위력강화 = MIN(32, CFLAG:ARG:마법위력강화 + 16)
	CFLAG:ARG:공격강화 = MIN(32, CFLAG:ARG:공격강화 - 4)
	CFLAG:ARG:방어강화 = MIN(32, CFLAG:ARG:방어강화 - 4)
	PRINTFORMW 공격력・방어력이 떨어지는 대신、마법위력이 매우 크게 올라갔다！
	LOCAL = POS(ARG:1)
	CALL ATTACK_SINGLE,ARG,LOCAL,2555
ELSE
	LOCAL = POS(ARG:1)
	CALL ATTACK_SINGLE,ARG,LOCAL,2555
ENDIF

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2555
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:이슈타르]]
	PRINTL 적 전체에 충격 상성 마법 궁극 데미지
	PRINTL 추가효과：공격 전에 자신의 마법위력을 크게 상승
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:린Fate]]
	PRINTL 적 전체에 충격 상성 마법 궁극 데미지
	PRINTL 추가효과：공격 전에 자신의 마법위력을 크게 상승
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:파르바티]]
	PRINTL 적 전체에 빙결 상성 3회 마법 중 데미지
	PRINTL 추가효과：고확률 매료
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:사쿠라]]
	PRINTL 적 전체에 빙결 상성 3회 마법 중 데미지
	PRINTL 추가효과：고확률 매료
ELSEIF U > 0 && U < CHARANUM && NO:U == [[キャラ:이리야]]
	PRINTL 적 1체에 만능 상성 마법 궁극 데미지
	PRINTL 추가효과：공격 전에 자신의 공격력・방어력이 하락、마법위력이 매우 크게 상승
ELSE
	PRINTL 적 1명에게 전격＋비구 복합 相性의 초특대 데미지. 매우 높은 확률로 감전시킨다
	PRINTL 학원도시의 초능력자 3위 미사카 미코토의 필살기
ENDIF

;継承に필요한部位
@継承部位_2555,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2555,ARG
RETURN 1
;とくにない

