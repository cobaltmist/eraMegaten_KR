;시부야 린 오의

;스킬명
@SKILL_NAME_2599,ARG
RESULTS = 아이올라이트·블루

;　명중
@SKILL_HITRATE_2599, ARG
SIF ARG == -1
	RETURN 100
IF BASE:ARG:LV >= 85
	RETURN 120
ELSEIF BASE:ARG:LV >= 70
	RETURN 115
ELSEIF BASE:ARG:LV >= 60
	RETURN 110
ELSEIF BASE:ARG:LV >= 40
	RETURN 105
ELSE
	RETURN 100
ENDIF

;　위력
@SKILL_POWER_2599, ARG
SIF ARG == -1
	RETURN 100
IF BASE:ARG:LV >= 85
	RETURN 200
ELSEIF BASE:ARG:LV >= 70
	RETURN 160
ELSEIF BASE:ARG:LV >= 60
	RETURN 140
ELSEIF BASE:ARG:LV >= 40
	RETURN 120
ELSE
	RETURN 100
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2599,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2599,ARG
RETURN 3

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_2599, ARG
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_2599,ARG
SIF ARG == -1
	RETURN 15
IF BASE:ARG:LV >= 85
	RETURN 50
ELSEIF BASE:ARG:LV >= 70
	RETURN 39
ELSEIF BASE:ARG:LV >= 60
	RETURN 31
ELSEIF BASE:ARG:LV >= 40
	RETURN 23
ELSE
	RETURN 15
ENDIF

;　상성
@SKILL_TYPE_2599,ARG
#LOCALSSIZE 4
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("화염")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("화염")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0 || CFLAG:LOCAL:물리반사플래그 || CFLAG:LOCAL:무효플래그
	RETURN GET_TYPE_NUM("화염")
LOCAL:2 = -1000
LOCALS = 화염
LOCALS:1 = 빙결
LOCALS:2 = 파마
LOCALS:3 = 화염
FOR LOCAL:1, 1, 4
	;판정用に数値を仕舞う。반사は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;화염+빙결+파마

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_2599, ARG
RETURN 1
;EXTRA. 화염계

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_2599, ARG
RETURN 1


;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_2599
RETURN 1

;　사거리
@SKILL_RANGE_2599,ARG
RETURN 1

;　범위　1 1명　2 １열　3 전체　4 적아군전체
@SKILL_SPHERE_2599,ARG
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_2599
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2599,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2599
RETURN 7
;화염

;속도수정
@SKILL_SPEED_2599, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2599,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2599
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2599
RETURN 15

@継承不能_2599
RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2599,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2599,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2599,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2599,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2599,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,2599

;====================================================
;실행내용 여기까지
;====================================================


;　 설명문 표시
@SKILL_EXPLAIN_2599
PRINTL 적 1명에 화염+빙결+파마 복합 상성 데미지를 3회 준다.
PRINTL 시부야 린의 오의.

;계승에 필요한 부위
@계승부위_2599,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2599,ARG
RETURN 1
;とくにない

