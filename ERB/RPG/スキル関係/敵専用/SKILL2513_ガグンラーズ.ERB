;

;스킬명
@SKILL_NAME_2513,ARG
IF SKILL_CHANGE(ARG, 1513) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 13
	RESULTS = 이터널제로
ELSEIF SKILL_CHANGE(ARG, 4219) && CFLAG:ARG:보스플래그
	RESULTS = 라이트 핸드
ELSEIF SKILL_CHANGE(ARG, 4220) && CFLAG:ARG:보스플래그
	RESULTS = 레프트 핸드
ELSEIF SKILL_CHANGE(ARG, [[キャラ:미카엘]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 96
	RESULTS = 신의 보호
ELSEIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 96
	RESULTS = 만물도륙
ELSEIF SKILL_CHANGE(ARG, [[キャラ:카마조츠]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 13
	RESULTS = 마계의바람
ELSE
	RESULTS = 가군라즈
ENDIF

;　명중
@SKILL_HITRATE_2513, ARG
RETURN 1000

;　威힘
@SKILL_POWER_2513, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2513,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2513,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2513, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_2513,ARG
RETURN 0

;　상성
@SKILL_TYPE_2513,ARG
RETURN GET_TYPE_NUM("만능")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2513, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2513, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2513
RETURN 1

;　사거리
@SKILL_RANGE_2513,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2513,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2513
RETURN 3

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2513,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2513
RETURN 24

;속도보정
@SKILL_SPEED_2513, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2513,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2513
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2513
RETURN 23


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2513,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2513,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2513,ARG
RETURN 100

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2513,ARG
RETURN 100
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2513,ARG,ARG:1
#LOCALSIZE 3

IF SKILL_CHANGE(ARG, 1513) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 13
	FOR LOCAL,1,7
		SIF POS(LOCAL) < 0
			CONTINUE
		FOR LOCAL:1,0,6
			CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") = -16
		NEXT
	NEXT
	FOR LOCAL,0,6
		CFLAG:ARG:(GET_BATTLESTATUS(LOCAL) + "강화") = 32
	NEXT
	PRINTFORML 아군 전체의 모든 능력이 최대까지 내려갔다！
	PRINTFORMW %CALLNAME:ARG%의 모든 능력이 최대까지 올라갔다！
ELSEIF SKILL_CHANGE(ARG, 4220) && CFLAG:ARG:보스플래그
	FOR LOCAL,1,7
		SIF POS(LOCAL) < 0
			CONTINUE
		PRINTFORM 　　TARGET:[{LOCAL,2}] %CALLNAME:POS(LOCAL),20,LEFT%　>>>>>>　
		LOCAL:1 = BASE:POS(LOCAL):ＨＰ * 20/100
		PRINTFORML {LOCAL:1}데미지
		CALL VAR_HP(POS(LOCAL), -LOCAL:1, 0)
		FOR LOCAL:1,0,6
			CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") = MAX(-16, CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") - 4)
		NEXT
	NEXT
	PRINTFORMW 아군 전체의 모든 능력이 내려갔다！
ELSEIF SKILL_CHANGE(ARG, 4219) && CFLAG:ARG:보스플래그
	FOR LOCAL,0,6
		CFLAG:ARG:(GET_BATTLESTATUS(LOCAL) + "강화") = MIN(32, CFLAG:ARG:(GET_BATTLESTATUS(LOCAL) + "강화") + 4)
	NEXT
	CFLAG:ARG:무효플래그 = 1
	PRINTFORML %CALLNAME:ARG%의 모든 능력이 오르고、모든공격이 무효화됐다！
ELSEIF SKILL_CHANGE(ARG, [[キャラ:미카엘]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 96
	FOR LOCAL,0,6
		CFLAG:ARG:(GET_BATTLESTATUS(LOCAL) + "강화") = MIN(32, CFLAG:ARG:(GET_BATTLESTATUS(LOCAL) + "강화") + 4)
	NEXT
	FOR LOCAL , 0 , FLAG:상성수
		CALL SKILL_WALL , ARG , GET_TYPE(LOCAL) , CPOS(ARG) , 1 , "非표시"
	NEXT
	PRINTFORML %CALLNAME:ARG%의 모든 능력이 오르고、모든 속성이 무효화됐다！
ELSEIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 96
	FOR LOCAL,1,7
		SIF POS(LOCAL) < 0
			CONTINUE
		PRINTFORM 　　TARGET:[{LOCAL,2}] %CALLNAME:POS(LOCAL),20,LEFT%　>>>>>>　
		LOCAL:1 = MAXBASE:POS(LOCAL):ＨＰ * 25/100
		PRINTFORML {LOCAL:1}데미지
		FOR LOCAL:1,0,6
			CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") = MAX(-16, CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:1) + "강화") - 4)
		NEXT
		CALL VAR_HP(POS(LOCAL), -LOCAL:1, 0)
	NEXT
	PRINTFORMW 아군 전체의 모든 능력이 내려갔다！
	
ELSEIF SKILL_CHANGE(ARG, [[キャラ:카마조츠]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 13
	CALL ACTION_1032,ARG,19
	CALL ACTION_1732,ARG,19
ELSE
	CFLAG:ARG:크리티컬강화 = 8
	FOR LOCAL, 1, 7
		SIF POS(LOCAL) < 0
			CONTINUE
		CFLAG:POS(LOCAL):검격무효화횟수 = 0
		CFLAG:POS(LOCAL):전격무효화횟수 = 0
		CFLAG:POS(LOCAL):물리반사플래그 = 0
		CFLAG:POS(LOCAL):마법반사플래그 = 0
		CFLAG:POS(LOCAL):무효플래그 = 0
		CFLAG:POS(LOCAL):방어강화 = MAX(-16, CFLAG:POS(LOCAL):방어강화 - 4)
	NEXT
	PRINTFORMW 아군 전체의 모든 능력이 내려갔다！
ENDIF



;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2513
PRINTL 궁니르가 적중하기 쉬워진다
PRINTL 오딘전용

;계승에 필요한 부위
@계승부위_2513,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2513,ARG
RETURN 1
;とくにない

