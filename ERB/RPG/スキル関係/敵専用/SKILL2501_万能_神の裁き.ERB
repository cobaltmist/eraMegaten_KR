;

;스킬명
@SKILL_NAME_2501,ARG
RESULTS = 신의 심판

;　명중
@SKILL_HITRATE_2501, ARG
RETURN 100

;　위력
@SKILL_POWER_2501, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2501,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2501,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2501, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2501,ARG
RETURN 30

;　상성
@SKILL_TYPE_2501,ARG
RETURN GET_TYPE_NUM("만능")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2501, ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2501, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2501
RETURN 1

;　사거리
@SKILL_RANGE_2501,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2501,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2501
RETURN 3

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2501,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2501
RETURN 14

;속도보정
@SKILL_SPEED_2501, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2501,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2501
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2501
RETURN 23


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2501,ARG
RETURN GET_STATE_NUM("DYING")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2501,ARG
RETURN GET_TYPE_NUM("만능")

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2501,ARG
RETURN 100

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2501,ARG
RETURN 100

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2501,ARG,ARG:1
#DIM CHARA
#DIM L_TARGET,6
#DIM L_COUNT
L_TARGET = -1
L_COUNT = 0
FOR LOCAL,1,7
	CHARA = POS(LOCAL)
	SIF CHARA < 0
		CONTINUE
	SIF GROUPMATCH(GET_STATE(CFLAG:CHARA:상태이상),"STONE","DYING")
		CONTINUE
	IF L_TARGET == -1 || MAXBASE:CHARA:마력 < MAXBASE:L_TARGET:마력
		L_COUNT = 0
		L_TARGET:(L_COUNT++) = CHARA
	ELSEIF MAXBASE:CHARA:마력 == BASE:CHARA:마력
		L_TARGET:(L_COUNT++) = CHARA
	ENDIF
NEXT
CHARA = L_TARGET:(RAND:L_COUNT)
IF EQUIP:CHARA:악세서리 == 5499
	PRINTFORMW [{LOCAL,2}] %CALLNAME:CHARA,20,LEFT%  >>>>>>  DEBUG-BLOCK
	RETURN
ENDIF
PRINTFORMW     TARGET:[{CPOS(CHARA),2}] %CALLNAME:CHARA,20,LEFT%  >>>>>>  DYING
CALL DEAD_CHARA,CHARA






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2501
PRINTL 가장 마력이 낮은 적 한 명에게 DYING 회피・저항 불능
PRINTL 마력이 최저인 캐릭터가 여럿 있는 경우 대상 중에서 랜덤으로 선택

;계승에 필요한 부위
@계승부위_2501,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2501,ARG
RETURN 1
;とくにない

