;

;스킬名
@SKILL_NAME_2535,ARG
RESULTS = 적전용전체공격
SIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	RESULTS = 아수라난격

;　명중
@SKILL_HITRATE_2535, ARG
RETURN 100

;　위력
@SKILL_POWER_2535, ARG
SIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	RETURN 140
RETURN 1

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2535,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	RETURN 3
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2535,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	RETURN 3
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2535, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2535,ARG
RETURN 10

;　相性
@SKILL_TYPE_2535,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	RETURN 2
RETURN GET_TYPE_NUM("만능")

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2535, ARG
RETURN 1

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2535, ARG
SIF SKILL_CHANGE((FLAG:行動順1), [[キャラ:아수라왕]]) && CFLAG:(FLAG:行動順1):ボスフラグ && FLAG:現던전 == 96
	RETURN 1
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2535
RETURN 1

;　사거리
@SKILL_RANGE_2535,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_2535,ARG
SIF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	RETURN 3
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2535
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2535,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2535
RETURN 24

;속도보정
@SKILL_SPEED_2535, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2535,ARG


;전투時이외に사용できるか
@SKILL_ACTIONABLE_FIELD_2535
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2535
RETURN 23


;テン플레이트用、추가효과시리즈
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2535,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2535,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2535,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2535,ARG
RETURN 0

@적専用_2535
RETURN 1
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2535,ARG,ARG:1
#LOCALSIZE 3
SIF CFLAG:ARG:PTフラグ > 0
	RETURN 0
IF SKILL_CHANGE(ARG, [[キャラ:아수라왕]]) && CFLAG:ARG:ボスフラグ && FLAG:現던전 == 96
	FOR LOCAL, 1, 7
		SIF POS(LOCAL) < 0
			CONTINUE
		SIF GET_STATE(CFLAG:POS(LOCAL):ステート) == "DYING"
			CONTINUE
		CFLAG:ARG:ターゲット = LOCAL
		LOCAL:1 = BASE:POS(LOCAL):ＨＰ
		CALL ATTACK_SINGLE, ARG, POS(LOCAL), 2067, 1
		IF POS(LOCAL) > 0 && LOCAL:1 > BASE:POS(LOCAL):ＨＰ && GET_STATE(CFLAG:POS(LOCAL):ステート) != "DYING"
			PRINTFORML 　　TARGET:[{LOCAL,2}] %CALLNAME:POS(LOCAL),20,LEFT%　>>>>>>　올라갔던 능력을 낮췄다！
			FOR LOCAL:2,0,6
				;CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:2) + "강화") > 0 
				CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:2) + "강화") -= CFLAG:POS(LOCAL):(GET_BATTLESTATUS(LOCAL:2) + "강화") > 0 ? 4 # 0
			NEXT
		ENDIF
		WAIT
	NEXT
ENDIF




;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2535
PRINTL 적 전용의 전체 공격


;継承に필요한部位
@継承部位_2535,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2535,ARG
RETURN 1
;とくにない

