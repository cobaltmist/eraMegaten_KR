;通常攻撃
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_2531,ARG
RESULTS = 대천세계의 바람

;　명중
@SKILL_HITRATE_2531, ARG
RETURN 140

;　위력
@SKILL_POWER_2531, ARG
RETURN 600

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2531,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2531,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2531, ARG
RETURN 1
;소비없음＝1

;　코스트량
@SKILL_COST_2531,ARG
RETURN 20

;　상성
@SKILL_TYPE_2531,ARG
#LOCALSSIZE 3
#LOCALSIZE 4
SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("충격")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("충격")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("충격")
LOCAL:2 = -1000
LOCALS = 충격
LOCALS:1 = 질풍
LOCALS:2 = 충격
FOR LOCAL:1, 1, 3
	;判定用に数値を仕舞う。反射は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	SIF CFLAG:LOCAL:독각플래그 == 2 && LOCAL:3 > 100
			LOCAL:3 = -LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1) +"웨이트") == 1 ? 0 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1) +"웨이트") == 2 ? -50 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:마법반사플래그 ? -200 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1) +"웨이트") == 3 ? -200 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;만능

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2531, ARG
RETURN 1
;魔法

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2531, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2531
RETURN 1

;　사거리
@SKILL_RANGE_2531,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2531,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2531
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2531,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2531
RETURN 20

;全て

;속도보정
@SKILL_SPEED_2531, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2531,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2531
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2531
RETURN 23

@계승불능_2531
RETURN 1
;계승불가

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2531,ARG
RETURN 19

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2531,ARG
RETURN 7

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2531,ARG
RETURN 50

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2531,ARG
RETURN 100
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2531,ARG,ARG:1
IF ARG:1 == 17
	LOCAL:1 = 1
	LOCAL:2 = 4
ELSEIF ARG:1 == 18
	LOCAL:1 = 4
	LOCAL:2 = 7
ELSEIF ARG:1 == 19
	LOCAL:1 = 1
	LOCAL:2 = 7
ELSEIF ARG:1 == 20
	LOCAL:1 = 7
	LOCAL:2 = 12
ELSEIF ARG:1 == 21
	LOCAL:1 = 12
	LOCAL:2 = 17
ELSEIF ARG:1 == 22
	LOCAL:1 = 7
	LOCAL:2 = 17
ELSEIF ARG:1 == 23
	LOCAL:1 = 1
	LOCAL:2 = 17
ENDIF
FOR LOCAL, LOCAL:1, LOCAL:2
	LOCALS = 포지션{LOCAL}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
	CALL ATTACK_SINGLE, ARG, FLAG:LOCALS, 2531
	SIF FLAG:LOCALS < 0
		CONTINUE
	CFLAG:(FLAG:LOCALS):SLIP중중력피탄플래그 = 1
NEXT




;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2531
PRINTL 적 전체에 충격+질풍 데미지+SLIP　이 공격의 SLIP은 회복하기 어렵다

;계승에 필요한 부위
@계승부위_2531,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2531,ARG
RETURN 1
;とくにない

