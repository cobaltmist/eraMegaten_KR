;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_2528,ARG
IF SKILL_CHANGE(ARG, [[キャラ:라후]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 13
	IF CFLAG:ARG:802 > 0
		RESULTS = 흡수
	ELSE
		RESULTS = 드래곤헤드
	ENDIF
ELSE
	RESULTS = 헤드
ENDIF

;　명중
@SKILL_HITRATE_2528, ARG
RETURN 100

;　威힘
@SKILL_POWER_2528, ARG
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2528,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2528,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2528, ARG
RETURN 1
;소비なし＝1

;　コストの量
@SKILL_COST_2528,ARG
RETURN 12

;　相性
@SKILL_TYPE_2528,ARG
RETURN 17
;검격

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2528, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_2528, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2528
RETURN 1

;　사거리
@SKILL_RANGE_2528,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_2528,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_2528
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2528,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2528
RETURN 17
;검격

;속도보정
@SKILL_SPEED_2528, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2528,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2528
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_2528
RETURN 3

@SKILL_RANKUP_2528
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2528,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2528,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2528,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2528,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================

@ACTION_2528,ARG,ARG:1
LOCAL = POS(ARG:1)

IF SKILL_CHANGE(ARG, [[キャラ:라후]]) && CFLAG:ARG:보스플래그 && FLAG:현던전 == 13
	IF CFLAG:ARG:802 < 1
				CFLAG:LOCAL:ステート = GET_STATE_NUM("HEAT")
				CFLAG:LOCAL:ステート経過ターン = 0
				CFLAG:LOCAL:회복불능플래그 = 99
				CFLAG:LOCAL:물리피데미지보정 = -100
				CFLAG:LOCAL:마법피데미지보정 = -100
				
				CFLAG:ARG:802 = ARG:1
				CFLAG:ARG:803 = BASE:ARG:ＨＰ
				CFLAG:ARG:不死身フラグ = 1
				PRINTFORMW ＞%조사처리(CALLNAME:LOCAL, "는")% 붙잡히고 말았다
	ELSE
		LOCAL = POS(CFLAG:ARG:802)
		LOCAL:1 = MAXBASE:LOCAL:ＨＰ * 8/100
		LOCAL:2 = MAXBASE:LOCAL:ＭＰ * 4/100
		CFLAG:LOCAL:회복불능플래그 = 99
				CFLAG:LOCAL:ステート = GET_STATE_NUM("HEAT")
				CFLAG:LOCAL:ステート経過ターン = 0
		
		PRINTFORML 　　TARGET:[{CPOS(LOCAL),2}] %CALLNAME:LOCAL ,20,LEFT%　>>>>>>　{LOCAL:1}
		PRINTFORML 　　TARGET:[{CPOS(LOCAL),2}] %CALLNAME:LOCAL ,20,LEFT%　>>>>>>　MP{LOCAL:2}
		CALL VAR_HP,LOCAL,-LOCAL:1,0
		CALL VAR_MP,LOCAL,-LOCAL:2,0
		PRINTFORML 　ATTACKER:[{CPOS(ARG),2}] %CALLNAME:ARG ,20,LEFT%　>>>>>>　{LOCAL:1}회복
		PRINTFORMW 　ATTACKER:[{CPOS(ARG),2}] %CALLNAME:ARG ,20,LEFT%　>>>>>>　MP{LOCAL:2}회복
		CALL VAR_HP,ARG,LOCAL:1,0
		CALL VAR_MP,ARG,LOCAL:2,0
		SIF BASE:LOCAL:ＨＰ < 1
			CALL ACTION_2527,ARG,ARG:1
	ENDIF
ENDIF


;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_2528
PRINTL [후열] 잡는다

;継承に필요な部位
@継承部位_2528,ARG
RETURN 1
;とくにない

@SKILL_ACTIONABLE_BATTLE_2528
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2528,ARG
RETURN 1
;とくにない

