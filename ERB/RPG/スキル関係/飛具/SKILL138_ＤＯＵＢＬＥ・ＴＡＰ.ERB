;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_138,ARG
RESULTS = ＤＯＵＢＬＥ・ＴＡＰ

;　명중
@SKILL_HITRATE_138, ARG
RETURN 100

;　위력
@SKILL_POWER_138, ARG
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_138,ARG
SIF ARG < 0
	RETURN 2
CALLFORM 최저공격횟수_{EQUIP:ARG:총}
LOCAL = RESULT * 2
RETURN LOCAL

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_138,ARG
SIF ARG < 0
	RETURN 2
CALLFORM 최대공격횟수_{EQUIP:ARG:총}
LOCAL = RESULT * 2
RETURN LOCAL

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_138, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_138,ARG
RETURN 5

;　相性
@SKILL_TYPE_138,ARG
SIF ARG < 0
	RETURN 1
CALLFORM 공격상성_{EQUIP:ARG:총}
RETURN RESULT
;총に依存
;RETURN 1
;비구

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_138, ARG
RETURN 0
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_138, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_138
RETURN 1

;　사거리
@SKILL_RANGE_138,ARG
SIF ARG < 0
	RETURN 3
CALLFORM 사거리_{EQUIP:ARG:총}
RETURN RESULT

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_138,ARG
SIF ARG < 0
	RETURN 1
CALLFORM 공격범위_{EQUIP:ARG:총}
RETURN RESULT

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_138
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_138,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_138
RETURN 1

;속도보정
@SKILL_SPEED_138, ARG = -1
SIF ARG == -1
	RETURN 0
TRYCCALLFORM 속도보정_{EQUIP:ARG:총}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;　구상の표시とその타입
@SKILL_MESSAGE_138,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_138
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_138
RETURN 0
;변화

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_138,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_138,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_138,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_138,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_138,ARG,ARG:1
CALL SKILL_SPHERE_138,ARG
IF RESULT == 1
	LOCAL = POS(ARG:1)
	CALL GUN_SINGLE,ARG,LOCAL,138
ELSE
	RESULT = 0
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:총}
	IF RESULT == 1
		CALL GUN_RAND,ARG,ARG:1,138
	ELSE
		CALL GUN_FIELD,ARG,ARG:1,138
	ENDIF
	
ENDIF





;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_138
PRINTL [특수] 캐릭터가 장비하고 있는 총으로 2회 공격한다.

;사용조건
@SKILL_SPECIAL_ACTIONABLE_138,ARG
SIF EQUIP:ARG:총 == 0 || CFLAG:ARG:악마변신 || TALENT:ARG:건슬링거 == 0 || TALENT:ARG:달인 == 0 
	RETURN 0
RETURN 1
;とくにない

