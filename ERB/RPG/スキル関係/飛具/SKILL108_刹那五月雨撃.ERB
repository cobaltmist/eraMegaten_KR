
;스킬명
@SKILL_NAME_108,ARG
;ナハトが使用する場合 R・クレイモアに
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RESULTS =  R・클레이모어
ELSE
	RESULTS = 찰나장대비쏘기
ENDIF

;　명중
@SKILL_HITRATE_108, ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 135
ELSE
	RETURN 120
ENDIF

;　위력
@SKILL_POWER_108, ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 125
ELSE
	RETURN 110
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 4
ELSE
	RETURN 3
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 4
ELSE
	RETURN 8
ENDIF

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_108,ARG
RETURN 3

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_108, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 8
ELSE
	RETURN 18
ENDIF

;　상성
@SKILL_TYPE_108,ARG
RETURN 1
;비구

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_108, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_108, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_108
RETURN 1

;　사거리
@SKILL_RANGE_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 2
ELSE
	RETURN 3
ENDIF

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 2
ELSE
	RETURN 3
ENDIF

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_108
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_108,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_108
RETURN 1
;타격

;속도보정
@SKILL_SPEED_108, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_108,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_108
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_108
RETURN 12

@SKILL_RANKUP_108
;ごぜつせっぱ
D:1 = 127
RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_108,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_108,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_108,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_108,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_108,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,108






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_108
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:나하트]]
	PRINTL 적 전열에 대랑의 티타늄 베어링탄을 흩뿌린다。
	PRINTL 발사되는 베어링탄의 수가 많아、다단 히트한다。
	PRINTL 정식명칭「레이어드・클레이모어」。어딘가의 지하 도시가 아니다。
ELSE
	PRINTL 적 전체 랜덤하게 ３～８회 공격
ENDIF

;계승에 필요한 부위
@계승부위_108,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_108,ARG
RETURN 1
;とくにない

