
;스킬名
@SKILL_NAME_108,ARG
;나하트が사용する場合 R・ク레이モアに
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RESULTS =  R・클레이모어
ELSE
	RESULTS = 찰나장대비쏘기
ENDIF

;　명중
@SKILL_HITRATE_108, ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 135
ELSE
	RETURN 120
ENDIF

;　威힘
@SKILL_POWER_108, ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 125
ELSE
	RETURN 110
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 4
ELSE
	RETURN 3
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 4
ELSE
	RETURN 8
ENDIF

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_108,ARG
RETURN 3

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_108, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 8
ELSE
	RETURN 18
ENDIF

;　相性
@SKILL_TYPE_108,ARG
RETURN 1
;비구

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_108, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_108, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_108
RETURN 1

;　사거리
@SKILL_RANGE_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 2
ELSE
	RETURN 3
ENDIF

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_108,ARG
IF SKILL_CHANGE(ARG, [[キャラ:나하트]])
	RETURN 2
ELSE
	RETURN 3
ENDIF

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_108
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_108,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_108
RETURN 1
;타격

;속도보정
@SKILL_SPEED_108, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_108,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_108
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_108
RETURN 12

@SKILL_RANKUP_108
;ごぜつせっぱ
D:1 = 127
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_108,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_108,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_108,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_108,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_108,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,108






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_108
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:나하트]]
	PRINTL 적전열에 많은 양의 티타늄 베어링탄을 흩뿌린다。
	PRINTL 발사되는 베어링탄의 수가 많고、다단 히트한다。
	PRINTL 정식명칭 「레이어드・클레이모어」。어딘가의 지하 도시가 아니다。
ELSE
	PRINTL [후열] 적 전체 3～8회(1인 최대 3회) 비구속성 소 물리데미지. 명중률이 조금 높다.
ENDIF

;継承に필요な部位
@継承部位_108,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_108,ARG
RETURN 1
;とくにない

