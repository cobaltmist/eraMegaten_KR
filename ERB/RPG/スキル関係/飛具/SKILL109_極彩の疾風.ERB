;20210813	NEO		八千穂明日香＠九龍妖魔學園紀（※飛具専門達人）が使用すると、속도보정+6で飛具を最大３回防ぐ壁に変化するように

;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_109,ARG
IF SKILL_CHANGE(ARG, [[キャラ:야치호]])
	RESULTS = 리턴
ELSE
	RESULTS = 극채의질풍
ENDIF

;　명중
@SKILL_HITRATE_109, ARG
RETURN 100

;　위력
@SKILL_POWER_109, ARG
RETURN 18

;　최저공격횟수
@SKILL_MINATTACKNUMBER_109,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_109,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_109, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_109,ARG
RETURN 24

;　相性
@SKILL_TYPE_109,ARG
RETURN 1
;비구

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_109, ARG
RETURN 1
;どちらでもない

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_109, ARG
RETURN 0


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_109
RETURN 2

;　사거리
@SKILL_RANGE_109,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_109,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_109
RETURN 4

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_109,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_109
RETURN 1
;화염

;속도보정
@SKILL_SPEED_109, ARG = -1
IF SKILL_CHANGE(ARG, [[キャラ:야치호]])
	RETURN 6
ELSE
	RETURN 0
ENDIF
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_109,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_109
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_109
RETURN 6




;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_109,ARG,ARG:1
;コスト支払い
;CALL PAY_COST,ARG,109

;実行
IF SKILL_CHANGE(ARG, [[キャラ:야치호]])
	IF ARG < 0
		CALL SKILL_WALL , ARG , "비구" , CFLAG:ARG:타겟 , 1
	ELSEIF 함락(ARG) == 0
		CALL SKILL_WALL , ARG , "비구" , CFLAG:ARG:타겟 , 1
	ELSEIF 함락(ARG) == 1
		CALL SKILL_WALL , ARG , "비구" , CFLAG:ARG:타겟 , 2
	ELSE
		CALL SKILL_WALL , ARG , "비구" , CFLAG:ARG:타겟 , 3
	ENDIF
ELSE
	CALL SKILL_WALL , ARG , "비구" , CFLAG:ARG:타겟 , 1
ENDIF





;====================================================
;実行内容ここまで
;====================================================


;　 説明文の표시
@SKILL_EXPLAIN_109
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:야치호]]
	PRINTL その手のラケットで的確にリターンを返す
	PRINTL その的確さはなんと飛具相性の攻撃を防ぐ壁を味方全体に（※最大で）３回分張ることに相当する
ELSE
	PRINTL [특수] 아군 전체에 비구속성 공격을 1회씩 막아주는 벽을 세운다.
ENDIF

;継承に필요な部位
@継承部位_109,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_109,ARG
RETURN 1
;とくにない

