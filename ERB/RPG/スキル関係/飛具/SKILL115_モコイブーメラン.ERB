
;스킬명
@SKILL_NAME_115,ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RESULTS = 화룡
ELSE
	RESULTS = 모코이부메랑
ENDIF

;　명중
@SKILL_HITRATE_115, ARG
RETURN 120

;　위력
@SKILL_POWER_115, ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RETURN 280
ELSE
	RETURN 120
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_115,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_115,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_115, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_115,ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RETURN 25
ELSE
	RETURN 6
ENDIF

;　상성
@SKILL_TYPE_115,ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RETURN 4
ELSE
	RETURN 1
	;飛具
ENDIF

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_115, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_115, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_115
RETURN 1

;　사거리
@SKILL_RANGE_115,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_115,ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RETURN 2
ELSE
	RETURN 3
ENDIF

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_115
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_115,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_115
RETURN 1

@계승불능_115
RETURN 1
;

;속도보정
@SKILL_SPEED_115, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_115,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_115
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_115
RETURN 0


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_115,ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RETURN GET_STATE_NUM("FLAME")
ELSE
	RETURN GET_STATE_NUM("PANIC")
ENDIF

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_115,ARG
IF SKILL_CHANGE(ARG, [[キャラ:코마키]])
	RETURN 4
ELSE
	RETURN 9
ENDIF

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_115,ARG
RETURN 30

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_115,ARG
RETURN 75
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_115,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,115






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_115
IF U > 0 && U < CHARANUM && NO:U == ([[キャラ:코마키]])
	PRINTL 불꽃의 기를 두른 화살을 날린다。
	PRINTL 적 1열에 화염 물리 중 데미지를 주고、확률로 FLAME 상태로 만든다。
ELSE
	PRINTL 적 전체에 비구 상성의 대미지＋때때로 혼란을 부여하는 모코이의 필살기。
ENDIF

;계승에 필요한 부위
@계승부위_115,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_115,ARG
RETURN 1
;とくにない

