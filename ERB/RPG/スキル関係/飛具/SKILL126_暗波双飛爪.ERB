
;스킬명
@SKILL_NAME_126,ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RESULTS = RIP샷
ELSE
	RESULTS = 암흑쌍비조
ENDIF

;　명중
@SKILL_HITRATE_126, ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN 100
ELSE
	RETURN 120
ENDIF

;　위력
@SKILL_POWER_126, ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN 440
ELSE
	RETURN 290
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_126,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_126,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_126, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_126,ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN 45
ELSE
	RETURN 24
ENDIF

;　상성
@SKILL_TYPE_126,ARG
RETURN 1
;비구

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_126, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_126, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_126
RETURN 1

;　사거리
@SKILL_RANGE_126,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_126,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_126
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_126,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_126
RETURN 1
;비구

;속도보정
@SKILL_SPEED_126, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_126,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_126
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_126
RETURN 12

@SKILL_RANKUP_126
D:1 = 107
;랭크アップ先は1つ
RETURN 1

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_126,ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN GET_STATE_NUM("DYING")
ELSE
	RETURN GET_STATE_NUM("PALYZE")
ENDIF

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_126,ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN 1
ELSE
	RETURN 8
ENDIF

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_126,ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN 10
ELSE
	RETURN 40
ENDIF

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_126,ARG
IF SKILL_CHANGE(ARG, [[キャラ:아란]])
	RETURN 50
ELSE
	RETURN 70
ENDIF
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_126,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,126






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_126
IF U > 0 && U < CHARANUM && NO:U == ([[キャラ:아란]])
	PRINTL RIPShot。RIP＝Rest_in_peace＝편안히 잠들어라。
	PRINTL 적 전체에 비구 물리 특대 데미지를 주고、저확률로 DYING。
ELSE
	PRINTL 적 전체에 비구 상성의 대 데미지＋PALYZE。
ENDIF


;계승에 필요한 부위
@계승부위_126,ARG
SIF TALENT:ARG:날개
	RETURN 1
RETURN 0



;사용조건
@SKILL_SPECIAL_ACTIONABLE_126,ARG
RETURN 1
;とくにない

