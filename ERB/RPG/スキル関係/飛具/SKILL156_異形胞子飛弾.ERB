;ファントム反逆EDが作られた際に最終ボスになるだろうから割とズルい性能にしておく。でも、비구무효이상의 相性と테트라칸だけは勘弁な！

;스킬名
@SKILL_NAME_156,ARG
RESULTS = 이형포자비탄
;　명중
@SKILL_HITRATE_156, ARG
RETURN 120

;　위력
@SKILL_POWER_156, ARG
RETURN 140

;　최저공격횟수
@SKILL_MINATTACKNUMBER_156,ARG
RETURN 5

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_156,ARG
RETURN 10

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_156,ARG
RETURN 4

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_156, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_156,ARG
RETURN 40

;　相性
@SKILL_TYPE_156,ARG
RETURN 1
;비구

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_156, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_156, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_156
RETURN 1

;　사거리
@SKILL_RANGE_156,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_156,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_156
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_156,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_156
RETURN 1
;타격

;속도보정
@SKILL_SPEED_156, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_156,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_156
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_156
RETURN 18

@SKILL_RANKUP_156
RETURN 0

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_156,ARG
A = RAND:8
IF A == 0
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 1
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 2
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 3
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF A == 4
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 5
	RETURN GET_STATE_NUM("CLOSE")
ELSEIF A == 6
	RETURN GET_STATE_NUM("POISON")
ELSE
	RETURN GET_STATE_NUM("PALYZE")
ENDIF
;추가효과상성
@SKILL_ADDTIONAL_TYPE_156,ARG
IF A < 4
	RETURN 9
ELSE
	RETURN 8
ENDIF
RETURN 8

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_156,ARG
;데미지もシャレにならないし、바늘程度にしておく
RETURN 40

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_156,ARG
RETURN 60

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_156,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,156






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_156
PRINTL [후열] 적 전체에 5～10회(1인 최대 4회) 비구속성 소 물리데미지. 명중률이 조금 높다. (보통 확률로 무작위 상태이상)
PRINTL 大霊 カドクラ 大霊 マニトゥの得意技

;継承に필요한部位
@継承部位_156,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_156,ARG
RETURN 1
;とくにない

