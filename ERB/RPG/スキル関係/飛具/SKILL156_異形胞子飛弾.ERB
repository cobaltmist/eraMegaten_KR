;ファントム反逆EDが作られた際に最終ボスになるだろうから割とズルい性能にしておく。でも、飛具無効以上の相性とテトラカーンだけは勘弁な！

;스킬명
@SKILL_NAME_156,ARG
RESULTS = 이형포자비탄
;　명중
@SKILL_HITRATE_156, ARG
RETURN 120

;　위력
@SKILL_POWER_156, ARG
RETURN 140

;　최저공격횟수
@SKILL_MINATTACKNUMBER_156,ARG
RETURN 5

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_156,ARG
RETURN 10

;一体辺りの最大ＨＩＴ数
@SKILL_MAXATK_PER_156,ARG
RETURN 4

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_156, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_156,ARG
RETURN 40

;　상성
@SKILL_TYPE_156,ARG
RETURN 1
;비구

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_156, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_156, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_156
RETURN 1

;　사거리
@SKILL_RANGE_156,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_156,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_156
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_156,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_156
RETURN 1
;타격

;속도보정
@SKILL_SPEED_156, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_156,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_156
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_156
RETURN 18

@SKILL_RANKUP_156
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_156,ARG
A = RAND:8
IF A == 0
	RETURN GET_STATE_NUM("CHARM")
ELSEIF A == 1
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 2
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 3
	RETURN GET_STATE_NUM("HAPPY")
ELSEIF A == 4
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 5
	RETURN GET_STATE_NUM("CLOSE")
ELSEIF A == 6
	RETURN GET_STATE_NUM("POISON")
ELSE
	RETURN GET_STATE_NUM("PALYZE")
ENDIF
;추가효과 상성
@SKILL_ADDTIONAL_TYPE_156,ARG
IF A < 4
	RETURN 9
ELSE
	RETURN 8
ENDIF
RETURN 8

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_156,ARG
;데미지もシャレにならないし、바늘程度にしておく
RETURN 40

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_156,ARG
RETURN 60

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_156,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,156






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_156
PRINTL 적 전체에 비구 상성으로 랜덤하게 ５～１０회 공격해、여러 가지 상태이상을 부여한다
PRINTL 대령 카도쿠라 대령 マニトゥ의 주특기

;계승에 필요한 부위
@계승부위_156,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_156,ARG
RETURN 1
;とくにない

