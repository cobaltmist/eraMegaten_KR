;달인당신、とくに디바전용に作られた스킬。取扱주의

;스킬名
@SKILL_NAME_430,ARG
RESULTS = Never More

;　명중
@SKILL_HITRATE_430, ARG
RETURN 100

;　위력
@SKILL_POWER_430, ARG
RETURN 200

;　최저공격횟수
@SKILL_MINATTACKNUMBER_430,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_430,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_430, ARG, ARG:1
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_430, ARG
SIF ARG == -1
    RETURN 100
IF ABL:ARG:가창기능 == 0
    RETURN 100
ELSEIF ABL:ARG:가창기능 == 1
    RETURN 100
ELSEIF ABL:ARG:가창기능 == 2
    RETURN 100
ELSEIF ABL:ARG:가창기능 == 3
    RETURN 100
ELSEIF ABL:ARG:가창기능 == 4
    RETURN 95
ELSEIF ABL:ARG:가창기능 == 5
    RETURN 90
ELSEIF ABL:ARG:가창기능 == 6
    RETURN 90
ELSEIF ABL:ARG:가창기능 == 7
    RETURN 85
ELSEIF ABL:ARG:가창기능 == 8
    RETURN 85
ELSEIF ABL:ARG:가창기능 == 9
    RETURN 85
ELSE
    RETURN 80
ENDIF
    RETURN 100

;　相性
@SKILL_TYPE_430,ARG
RETURN 0

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_430, ARG
RETURN 2

;　1物理데미지で計算するか2魔法데미지で計算するか
@SKILL_DAMAGETYPE_430, ARG
RETURN 0


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_430
RETURN 2

;　사거리
@SKILL_RANGE_430,ARG
RETURN 1

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_430,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_430
RETURN 2

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_430,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_430
RETURN 4

@継承不能_430
RETURN 1

;속도보정
@SKILL_SPEED_430, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_430,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_430
RETURN 1

;스킬変化？用랭크
@SKILL_RANK_430
RETURN 22

@SKILL_RANKUP_430
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_430,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_430,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_430,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_430,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_430,ARG,ARG:1
;コスト支払い
;CALL PAY_COST, ARG, 430

IF ARG:1 == 19
	LOCAL:7 = 1
	LOCAL:8 = 7
ELSE
	LOCAL:7 = 7
	LOCAL:8 = 17
ENDIF

FOR LOCAL:9,LOCAL:7,LOCAL:8
	LOCALS = 포지션{LOCAL:9}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
;	PRINTFORM 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　
;	;회복量
;	LOCAL = MAXBASE:(FLAG:LOCALS):ＨＰ
;	PRINTFORML {LOCAL - BASE:(FLAG:LOCALS):ＨＰ}회복
;	CALL VAR_HP,FLAG:LOCALS,LOCAL,3
;	CALL SKILL_CURE_STATE_430, CFLAG:(FLAG:LOCALS):스테이터스
;	IF RESULT == 1
;		PRINTFORML 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(FLAG:LOCALS),20,LEFT%　>>>>>>　상태가 회복되었다
;		CFLAG:(FLAG:LOCALS):상태이상 = 0
;		CFLAG:(FLAG:LOCALS):상태이상경과턴수 = 0
;	ENDIF
	CALL ALL_CURE_STATE, ARG, POS(LOCAL:9), 0
	CALL ALL_HEAL, ARG, POS(LOCAL:9), 0
NEXT
WAIT

;何のバッドステータスを治せるか
@SKILL_CURE_STATE_430, ARG
SELECTCASE GET_STATE(ARG)
	CASE "FLY","GOOD","DYING","ORGY","HEAT"
		RETURN 0
	CASEELSE
		RETURN 1
ENDSELECT


;====================================================
;전투중以外での実行
;====================================================
@ACTION_FIELD_430,ARG,ARG:1
;コスト支払い
;CALL VAR_MP,ARG,-35,0

FOR LOCAL:9,1,7
	LOCALS = 포지션{LOCAL:9}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF GET_STATE(CFLAG:(FLAG:LOCALS):상태이상) == "DYING"
		CONTINUE
		
	CALL ALL_CURE_STATE, ARG, POS(LOCAL:9), 0
	CALL ALL_HEAL, ARG, POS(LOCAL:9), 0,
NEXT
WAIT
;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_430
PRINTL [회복] 아군 전체의 HP를 모두 회복하고, DYING을 제외한 상태이상을 치료한다. 
PRINTL 소중한 동료마를 생각하는 노래. 가창능력에 따라 비용이 감소한다.

;継承に필요な部位
@継承部位_430,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_430,ARG
CALL CHECK_COST,ARG,430
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない
