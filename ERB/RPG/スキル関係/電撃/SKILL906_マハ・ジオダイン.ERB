;통상공격
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_906,ARG
LOCALS = 마를로네
IF SKILL_CHANGE(ARG, [[キャラ:무명의대학생]]) && NAME:ARG == LOCALS
	RESULTS = 라이트닝
ELSEIF SKILL_CHANGE(ARG, [[キャラ:텐도]])
	RESULTS = 외법・사뢰초래
ELSE
	RESULTS = 마하・지오다인
ENDIF

;　명중
@SKILL_HITRATE_906, ARG
RETURN 90

;　위력
@SKILL_POWER_906, ARG
RETURN 295

;　최저공격횟수
@SKILL_MINATTACKNUMBER_906,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_906,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_906, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_906,ARG
RETURN 26

;　상성
@SKILL_TYPE_906,ARG
RETURN 6
;전격

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_906, ARG
RETURN 2
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_906, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_906
RETURN 1

;　사거리
@SKILL_RANGE_906,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_906,ARG
RETURN 2

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_906
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_906,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_906
RETURN 9
;전격

;속도보정
@SKILL_SPEED_906, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_906,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_906
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_906
RETURN 10


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_906,ARG
RETURN GET_STATE_NUM("SHOCK")


;추가효과 상성
@SKILL_ADDTIONAL_TYPE_906,ARG
RETURN 6

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_906,ARG
RETURN 15

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_906,ARG
RETURN 95
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_906,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,905






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_906
LOCALS = 마를로네
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:무명의대학생]] && NAME:U == LOCALS
	PRINTL 「별과 달의 지팡이」로부터 발해지는 마리의 필살기。
	PRINTL 전격으로 적 １열을 공격한다。
ELSE
	PRINTL 전격으로 적 １열을 공격한다。
ENDIF

;계승에 필요한 부위
@계승부위_906,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_906,ARG
RETURN 1
;とくにない

