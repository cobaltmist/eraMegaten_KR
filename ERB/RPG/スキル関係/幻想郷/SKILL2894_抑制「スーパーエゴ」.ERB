;통상공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_2894,ARG
RESULTS = 억제「슈퍼에코」
SIF  GETBIT (FLAG:커스텀게임화면,1)
	RESULTS = 抑制「スーパーエゴ」

;　명중
@SKILL_HITRATE_2894, ARG
RETURN 100

;　위력
@SKILL_POWER_2894, ARG
RETURN 150

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2894,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2894,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2894, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2894,ARG
RETURN 20

;　상성
@SKILL_TYPE_2894,ARG
RETURN GET_TYPE_NUM("신경")

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2894, ARG
RETURN 1
;EXTRA

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2894, ARG
RETURN 1

;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2894
RETURN 3

;　사거리
@SKILL_RANGE_2894,ARG
RETURN 3

;　범위　1１체　2１열　3전체　4　적아군 전체
@SKILL_SPHERE_2894,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2894
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2894,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2894
RETURN 12
;정신

;속도보정
@SKILL_SPEED_2894, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2894,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2894
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2894
RETURN 12

@SKILL_RANKUP_2894
RETURN 0

@継承不能_2894
RETURN 1

;テンプレート用、추가효과시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2894,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2894,ARG
RETURN 17
;만능

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2894,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2894,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2894,ARG,ARG:1
IF CFLAG:(ARG):PT플래그 >= 1
	CALL ATTACK_FIELD,ARG, (22),2894
	FOR (LOCAL:9),1,7
		LOCALS = 포지션{LOCAL:9}
		SIF FLAG:LOCALS < 0
			CONTINUE
		LOCAL = FLAG:LOCALS
		CALL SKILL_CURE_STATE_2894,CFLAG:LOCAL:상태이상
		IF RESULT == 1
		SIF GET_STATE(CFLAG:LOCAL:상태이상) == "PANIC" || GET_STATE(CFLAG:LOCAL:상태이상) == "CHARM" || GET_STATE(CFLAG:LOCAL:상태이상) == "HAPPY"
			CFLAG:LOCAL:혼란매료리커버 = 1
		PRINTFORML 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(LOCAL),20,LEFT%　>>>>>>　正気に戻った
		CFLAG:LOCAL:상태이상 = 0
		CFLAG:LOCAL:상태이상경과턴수 = 0
		ENDIF
	NEXT
ELSE
	CALL ATTACK_FIELD,ARG, (19),2894
	FOR (LOCAL:9),7,17
		LOCALS = 포지션{LOCAL:9}
		SIF FLAG:LOCALS < 0
			CONTINUE
		LOCAL = FLAG:LOCALS
		CALL SKILL_CURE_STATE_2894,CFLAG:LOCAL:상태이상
		IF RESULT == 1
		SIF GET_STATE(CFLAG:LOCAL:상태이상) == "PANIC" || GET_STATE(CFLAG:LOCAL:상태이상) == "CHARM" || GET_STATE(CFLAG:LOCAL:상태이상) == "HAPPY"
			CFLAG:LOCAL:혼란매료리커버 = 1
		PRINTFORML 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(LOCAL),20,LEFT%　>>>>>>　正気に戻った
		CFLAG:LOCAL:상태이상 = 0
		CFLAG:LOCAL:상태이상경과턴수 = 0
		ENDIF
	NEXT
ENDIF



;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2894
PRINTL 超自我が적아군의本能を抑制する。
PRINTL 적 전체に신경相性の물리공격を行いつつ、아군의HAPPY、PANIC、CHARMを解消する。
PRINTL 마왕　古明地 코이시のスペル카드（継承）

;계승에 필요한 부위
@계승부위_2894,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2894,ARG
RETURN 1
;とくにない

;控えに使えるか
@SKILL_T_RESERVE_2894
RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
@SKILL_CURE_STATE_2894,ARG
SELECTCASE GET_STATE(ARG)
	CASE "HAPPY","CLOSE","PANIC","CHARM"
		RETURN 1
	CASEELSE
		RETURN 0
ENDSELECT

