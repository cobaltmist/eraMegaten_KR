;20210810	NEO		ドリルの性能だったのを名前にあわせてウニっぽく変更

;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_2885,ARG
RESULTS = 극부「뇌운극어」

;　명중
@SKILL_HITRATE_2885, ARG
;RETURN 90
RETURN 110

;　위력
@SKILL_POWER_2885, ARG
RETURN 95

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2885,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2885,ARG
RETURN 3

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2885, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2885,ARG
RETURN 15

;　상성
@SKILL_TYPE_2885,ARG
RETURN 6
;전격

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2885, ARG
RETURN 1
;EXTRA

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2885, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2885
RETURN 1

;　사거리
@SKILL_RANGE_2885,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2885,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2885
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2885,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2885
RETURN 9
;전격

;속도보정
@SKILL_SPEED_2885, ARG = -1
;「衣玖の周囲に電撃を纏う」ので、近づかなければならないが、衣玖は動きが遅い方であるため速度補正マイナス
RETURN -5
;RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2885,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2885
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2885
RETURN 10

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2885,ARG
RETURN GET_STATE_NUM("SHOCK")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2885,ARG
RETURN 6

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2885,ARG
;RETURN 5
RETURN 50

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2885,ARG
RETURN 20

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2885,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,2885






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2885

;ドリルではなくウニなのでテキストを変更
;PRINTL 空気と전격으로 形作られた、あらゆる敵を削り突き通すドリル(物理依存・電撃相性攻撃)。
PRINTL 전격을 몸에 두르고、둥실둥실 다가가 감전시킨다（물리의존・전격상성공격）。
PRINTL 천녀　나가에 이쿠의 스펠카드

;계승에 필요한 부위
@계승부위_2885,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2885,ARG
RETURN 1
;とくにない

