;20210810	NEO		ドリルの性能だったのを名前にあわせてウニっぽく変更

;通常攻撃
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_2885,ARG
RESULTS = 극부「뇌운극어」

;　명중
@SKILL_HITRATE_2885, ARG
RETURN 110

;　위력
@SKILL_POWER_2885, ARG
RETURN 95

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2885,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2885,ARG
RETURN 3

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2885, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2885,ARG
RETURN 15

;　相性
@SKILL_TYPE_2885,ARG
RETURN 6
;전격

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2885, ARG
RETURN 1
;EXTRA

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2885, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2885
RETURN 1

;　사거리
@SKILL_RANGE_2885,ARG
RETURN 1

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2885,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2885
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2885,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2885
RETURN 9
;전격

;속도보정
@SKILL_SPEED_2885, ARG = -1
;「衣玖の周囲に電撃を纏う」ので、近づかなければならないが、衣玖は動きが遅い方であるため속도보정マイナス
RETURN -5

;　구상の표시とその타입
@SKILL_MESSAGE_2885,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2885
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2885
RETURN 10

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2885,ARG
RETURN GET_STATE_NUM("SHOCK")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2885,ARG
RETURN 6

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2885,ARG
RETURN 50

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2885,ARG
RETURN 20

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2885,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,2885






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2885

; PRINTL 공기와 전격으로 형성되어진、모든적을 깍아 꿰뜷는 드릴(물리의존・전격대칭공격)。
; PRINTL 천녀　나가에 이쿠의 스펠카드
PRINTL 電撃を身にまとい、フワフワと近寄り感電させる（物理依存・電撃相性攻撃）。
PRINTL 天女　永江衣玖のスペルカード

;継承に필요한部位
@継承部位_2885,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2885,ARG
RETURN 1
;とくにない

