;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2931,ARG
RESULTS = 몽부「황색의 착종미몽」

;　명중
@SKILL_HITRATE_2931, ARG
RETURN 100

;　위력
@SKILL_POWER_2931, ARG
RETURN 300

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2931,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2931,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2931, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2931,ARG
RETURN 36


;　相性
@SKILL_TYPE_2931,ARG
RETURN GET_TYPE_NUM("만능")


;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2931, ARG
RETURN 1
;EXTRA

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2931, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2931
RETURN 1

;　사거리
@SKILL_RANGE_2931,ARG
RETURN 3

;　범위　11명　26列　3전체　4　적아군전체
@SKILL_SPHERE_2931,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2931
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2931,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します


;속도보정
@SKILL_SPEED_2931, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2931,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2931
RETURN 0

;계승타입
@SKILL_SUCCESSION_TYPE_2931
RETURN 12
;睡眠

;스킬변화？用랭크
@SKILL_RANK_2931
RETURN 11

@継承不能_2931
RETURN 1

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2931,ARG
RETURN GET_STATE_NUM("SLEEP")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2931,ARG
RETURN GET_TYPE_NUM("만능")

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2931,ARG
RETURN 50

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2931,ARG
RETURN 95

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2931,ARG,ARG:1
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,2931

;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2931
PRINTL 적 1명에게 만능속성으로 대위력 마법 공격을 1회한다. 추가효과：만능속성 수면
PRINTL 회전을 동반한 탄막으로 공격을 하는 동시에 상대를 잠으로 떨어뜨린다.
PRINTL 몽마 도레미・스위트의 스펠카드（전용）

;継承に필요한部位
@継承部位_2931,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2931,ARG
RETURN 1
;とくにない

