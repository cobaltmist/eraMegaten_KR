;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2929,ARG
RESULTS = 총부「루나틱 건」

;　명중
@SKILL_HITRATE_2929, ARG
RETURN 140

;　위력
@SKILL_POWER_2929, ARG
RETURN 50

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2929,ARG
SIF ARG == -1
	RETURN 3
IF BASE:ARG:LV >= 49
	RETURN 10
ELSEIF BASE:ARG:LV >= 44
	RETURN 9
ELSEIF BASE:ARG:LV >= 39
	RETURN 8
ELSEIF BASE:ARG:LV >= 34
	RETURN 7
ELSEIF BASE:ARG:LV >= 29
	RETURN 6
ELSEIF BASE:ARG:LV >= 24
	RETURN 5
ELSEIF BASE:ARG:LV >= 19
	RETURN 4
ELSE
	RETURN 3
ENDIF


;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2929,ARG
SIF ARG == -1
	RETURN 3
IF BASE:ARG:LV >= 49
	RETURN 10
ELSEIF BASE:ARG:LV >= 44
	RETURN 9
ELSEIF BASE:ARG:LV >= 39
	RETURN 8
ELSEIF BASE:ARG:LV >= 34
	RETURN 7
ELSEIF BASE:ARG:LV >= 29
	RETURN 6
ELSEIF BASE:ARG:LV >= 24
	RETURN 5
ELSEIF BASE:ARG:LV >= 19
	RETURN 4
ELSE
	RETURN 3
ENDIF

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2929, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2929,ARG
RETURN 15

;　相性
@SKILL_TYPE_2929,ARG
RETURN GET_TYPE_NUM("비구")
;비구

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2929, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2929, ARG
RETURN 1

;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2929
RETURN 1

;　사거리
@SKILL_RANGE_2929,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2929,ARG
RETURN 2

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2929
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2929,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2929
RETURN 23
;無し

;속도보정
@SKILL_SPEED_2929, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2929,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2929
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2929
RETURN 15

@SKILL_RANKUP_2929
RETURN 0

@継承不能_2929
RETURN 1
;

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2929,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2929,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2929,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2929,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2929,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,2929






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2929
PRINTL 적 １열에게 랜덤으로 비구 속성 낮은 위력의 물리 공격을 3회 날린다
PRINTL 조건：사용자의 레벨에 따라 최저, 최대공격횟수 증가
PRINTL 다른 차원에서 호출한 탄환과 적을 겨냥한 탄환을 뿌려서 자리를 제압한다.
PRINTL 사용자가 강해지지 않으면 제압력도 떨어진다。요수　세이란의 스펠카드（전용）

;継承に필요한部位
@継承部位_2929,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2929,ARG
RETURN 1
;とくにない

