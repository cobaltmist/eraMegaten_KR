;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2916,ARG
RESULTS = 귀의금강력


;　명중
@SKILL_HITRATE_2916, ARG
RETURN 100

;　위력
@SKILL_POWER_2916, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2916,ARG
RETURN 0

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2916,ARG
RETURN 0

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2916, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2916,ARG
RETURN 0

;　相性
@SKILL_TYPE_2916,ARG
RETURN 0

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2916, ARG
RETURN 1
;EXTRA

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2916, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2916
RETURN 4

;　사거리
@SKILL_RANGE_2916,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2916,ARG
RETURN 4

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2916
RETURN 4

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2916,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2916
RETURN 23
;継承ナシ

;속도보정
@SKILL_SPEED_2916, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2916,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2916
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2916
RETURN 3

@적専用_2916
RETURN 1

@継承不能_2916
RETURN 1
;

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2916,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2916,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2916,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2916,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2916,ARG,ARG:1
#LOCALSIZE 3
;コストを支払う
;CALL VAR_MP,ARG,-20

;LOCALを초기化
LOCAL = 0

FOR LOCAL,0,6
	CFLAG:(ARG):(GET_BATTLESTATUS(LOCAL) + "강화") = MAX(0, CFLAG:(ARG):(GET_BATTLESTATUS(LOCAL) + "강화"))
NEXT

CFLAG:(ARG):공격강화 = MIN(32, CFLAG:POS(LOCAL):공격강화 + 4)

PRINTFORML \@ CFLAG:ARG:PT플래그 > 0 ? 아군 # 적 \@의 저하한 능력이 돌아왔다
PRINTFORMW 또한\@ CFLAG:ARG:PT플래그 == 0 ? 적 # 아군 \@의 공격력이 올랐다！






;====================================================
;実行内容ここまで
;====================================================

;====================================================
;전투중以外での実行
;====================================================


;====================================================
;実行内容ここまで
;====================================================


;　 설명文の표시
@SKILL_EXPLAIN_2916
PRINTL 자신에게 걸린 약체화 마법을 떼고、거기다 공격력을 올린다。
PRINTL 적 귀신 전용 기술。

;継承に필요한部位
@継承部位_2916,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2916,ARG
RETURN 1
;とくにない

