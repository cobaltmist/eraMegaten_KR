;통상공격
;スキルのテンプレートになっています

;스킬명
@SKILL_NAME_2828,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RESULTS = 의부「오러리스 선」
ELSE
	RESULTS = 열핵의난무
ENDIF

;　명중
@SKILL_HITRATE_2828, ARG
RETURN 90

;　위력
;仕様上,単体相手なら1~3回になるのでダイン相当の320
;上限HIT数に引っかからなければ単体あたりの期待値は240
;スペカの方は回数増やした分ちょっと弱体化。
@SKILL_POWER_2828, ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 140
ELSE
	RETURN 160
ENDIF

;最低がここで渋いのは同ランクにあたるマハ系が最大5HITしかできないから
;敵総数の判定を全後列合計にできるという強みもあるので、ここで少し絞っといても総HIT数は割合稼げる…はず
;最低攻撃回数
@SKILL_MINATTACKNUMBER_2828,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 3
ELSE
	RETURN 2
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2828,ARG
RETURN 8

;一体辺りの最大ＨＩＴ数
@SKILL_MAXATK_PER_2828,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 4
ELSE
	RETURN 3
ENDIF

;一体辺りの最低ＨＩＴ数
@SKILL_MINATK_PER_2828,ARG
RETURN 0

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2828, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2828,ARG
RETURN 15

;　상성
@SKILL_TYPE_2828,ARG
RETURN GET_TYPE_NUM("핵열")
;핵열

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2828, ARG
RETURN 1
;EXTRA

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2828, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2828
RETURN 1

;　사거리
@SKILL_RANGE_2828,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2828,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2828
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2828,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2828
RETURN GET_SUCCESSION_NUM("핵열")
;핵열

;속도보정
@SKILL_SPEED_2828, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2828,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2828
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2828
RETURN 7

@SKILL_RANKUP_2828
;랭크アップなし
RETURN 0


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2828,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2828,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2828,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2828,ARG
RETURN 0
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2828,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,2828






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2828
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:마리사]]
	PRINTL 천체를 본뜬 탄막을 자신의 주위에 선회시켜 핵열 상성의 데미지를 여러 차례 준다。
	PRINTL 키리사메 마리사의 스펠카드。
ELSE
	PRINTL 원자마저 무너뜨리는 핵반응으로 적 전체에 랜덤으로 여러 차례 공격한다。
	PRINTL 환상향에 아직도 전해지고 있던、오래된 이능 중 하나。
ENDIF



;継承に必要な部位
@계승부위_2828,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2828,ARG
RETURN 1
;とくにない

