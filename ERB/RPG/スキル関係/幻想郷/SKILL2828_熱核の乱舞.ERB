;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2828,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RESULTS = 의부「오릴리즈산」
ELSE
	RESULTS = 열핵의난무
ENDIF

;　명중
@SKILL_HITRATE_2828, ARG
RETURN 90

;　위력
;仕様上,1명相손なら1~3回になるのでダイン相当の320
;上限HIT数に引っかからなければ1명あたりの期待値は240
@SKILL_POWER_2828, ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 140
ELSE
	RETURN 160
ENDIF

;최저がここで渋いのは同랭크にあたるマハ系が최대5HITしかできないから
;적総数の판정を全후열합계にできるという強みもあるので、ここで少し絞っといても総HIT数は割合稼げる…はず
;최저공격횟수
@SKILL_MINATTACKNUMBER_2828,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 3
ELSE
	RETURN 2
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2828,ARG
RETURN 8

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_2828,ARG
IF SKILL_CHANGE(ARG, [[キャラ:마리사]])
	RETURN 4
ELSE
	RETURN 3
ENDIF

;一体辺りの최저ＨＩＴ数
@SKILL_MINATK_PER_2828,ARG
RETURN 0

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2828, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2828,ARG
RETURN 15

;　相性
@SKILL_TYPE_2828,ARG
RETURN GET_TYPE_NUM("핵열")
;핵열

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2828, ARG
RETURN 1
;EXTRA

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2828, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2828
RETURN 1

;　사거리
@SKILL_RANGE_2828,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2828,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2828
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2828,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2828
RETURN GET_SUCCESSION_NUM("핵열")
;핵열

;속도보정
@SKILL_SPEED_2828, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2828,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2828
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2828
RETURN 7

@SKILL_RANKUP_2828
;랭크アップなし
RETURN 0


;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2828,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2828,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2828,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2828,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2828,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,2828






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2828
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:마리사]]
	PRINTL 天体を模した弾幕を自身の周囲に旋廻させ핵열相性の데미지を複数回与える。
	PRINTL 霧雨마리사のスペル카드。
ELSE
	PRINTL 원자를 무너뜨리는 핵반응으로 적전체에 랜덤으로 여러차례 공격한다。
	PRINTL 환상향에 아직 전해지고 있던、오래된 마법 중 하나。
ENDIF



;継承に필요한部位
@継承部位_2828,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2828,ARG
RETURN 1
;とくにない

