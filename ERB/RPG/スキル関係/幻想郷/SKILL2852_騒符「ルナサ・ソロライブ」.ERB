;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2852,ARG
RESULTS = 소부「루나사･솔로라이브」
SIF  GETBIT (FLAG:커스텀게임화면,1)
	RESULTS = 소부「루나사･솔로라이브」

;　명중
@SKILL_HITRATE_2852, ARG
RETURN 100

;　위력
@SKILL_POWER_2852, ARG
SIF ARG == -1
	RETURN 110
IF ABL:ARG:가창기능 == 0
	RETURN 110
ELSEIF ABL:ARG:가창기능 == 1
	RETURN 130
ELSEIF ABL:ARG:가창기능 == 2
	RETURN 150
ELSEIF ABL:ARG:가창기능 == 3
	RETURN 170
ELSEIF ABL:ARG:가창기능 == 4
	RETURN 190
ELSEIF ABL:ARG:가창기능 == 5
	RETURN 210
ELSEIF ABL:ARG:가창기능 == 6
	RETURN 240
ELSEIF ABL:ARG:가창기능 == 7
	RETURN 300
ELSEIF ABL:ARG:가창기능 == 8
	RETURN 350
ELSEIF ABL:ARG:가창기능 == 9
	RETURN 400
ELSE
	RETURN 430
ENDIF
RETURN 90

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2852,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2852,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2852, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2852,ARG
SIF ARG == -1
	RETURN 9
IF ABL:ARG:가창기능 == 0
	RETURN 9
ELSEIF ABL:ARG:가창기능 == 1
	RETURN 12
ELSEIF ABL:ARG:가창기능 == 2
	RETURN 15
ELSEIF ABL:ARG:가창기능 == 3
	RETURN 18
ELSEIF ABL:ARG:가창기능 == 4
	RETURN 23
ELSEIF ABL:ARG:가창기능 == 5
	RETURN 26
ELSEIF ABL:ARG:가창기능 == 6
	RETURN 29
ELSEIF ABL:ARG:가창기능 == 7
	RETURN 34
ELSEIF ABL:ARG:가창기능 == 8
	RETURN 38
ELSEIF ABL:ARG:가창기능 == 9
	RETURN 41
ELSE
	RETURN 45
ENDIF
RETURN 9

;　相性
@SKILL_TYPE_2852,ARG
RETURN 7
;충격

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2852, ARG
RETURN 1
;EXTRA

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2852, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2852
RETURN 1

;　사거리
@SKILL_RANGE_2852,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2852,ARG
RETURN 2

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2852
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2852,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2852
RETURN 10
;충격

;속도보정
@SKILL_SPEED_2852, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2852,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2852
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2852
RETURN 12


;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2852,ARG
RETURN GET_STATE_NUM("PANIC")

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2852,ARG
RETURN 8

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2852,ARG
RETURN 30

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2852,ARG
RETURN 90
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2852,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,2852





;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2852
PRINTL 적의 정신을 파괴하는우울한 소리。적 １열에게  데미지를 주면서 동시에 PANIC으로 한다。
PRINTL 사용자의 가창 기능에 따라 위력이 증가한다。
PRINTL 악령　루나사・프리즘리버스펠카드

;継承に필요한部位
@継承部位_2852,ARG
SIF TALENT:ARG:입
	RETURN 1
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2852,ARG
RETURN 1
;とくにない

