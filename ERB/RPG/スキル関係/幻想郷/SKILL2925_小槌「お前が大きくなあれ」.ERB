;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2925,ARG
RESULTS = 소추「네가 커져라 얍」


;　명중
@SKILL_HITRATE_2925, ARG
RETURN 100

;　위력
@SKILL_POWER_2925, ARG
RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2925,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2925,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2925, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2925,ARG
RETURN 45

;　相性
@SKILL_TYPE_2925,ARG
RETURN 0

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2925, ARG
RETURN 1
;마법

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2925, ARG
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2925
RETURN 1

;　사거리
@SKILL_RANGE_2925,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2925,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2925
RETURN 4

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2925,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2925
RETURN 5
;지원

;속도보정
@SKILL_SPEED_2925, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2925,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2925
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2925
RETURN 7

@継承不能_2925
RETURN 1

;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2925,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2925,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2925,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2925,ARG
RETURN 0

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2925,ARG,ARG:1
#LOCALSIZE 3
IF CFLAG:(ARG):PT플래그 == 0
	LOCAL:1 = 1
	LOCAL:2 = 7
ELSE
	LOCAL:1 = 7
	LOCAL:2 = 17
ENDIF

FOR LOCAL,LOCAL:1,LOCAL:2
	SIF POS(LOCAL) < 0
		CONTINUE
	CFLAG:POS(LOCAL):공격강화 = MIN(32, CFLAG:POS(LOCAL):공격강화 + 8)
	CFLAG:POS(LOCAL):방어강화 = MAX(-16, CFLAG:POS(LOCAL):방어강화 - 8)
	CFLAG:POS(LOCAL):마법효과강화 = MAX(-16, CFLAG:POS(LOCAL):마법효과강화 - 8)
	CFLAG:POS(LOCAL):명중강화 = MAX(-16, CFLAG:POS(LOCAL):명중강화 - 8)
	CFLAG:POS(LOCAL):회피강화 = MAX(-16, CFLAG:POS(LOCAL):회피강화 - 8)
NEXT
PRINTFORMW 커지게 된 적의 공격력이 큰폭으로 오르고 방어・명중・회피가 저하한다.






;====================================================
;実行内容ここまで
;====================================================

;====================================================
;전투중以外での実行
;====================================================


;====================================================
;実行内容ここまで
;====================================================


;　 설명文の표시
@SKILL_EXPLAIN_2925
PRINTL 적의 물리공격력을 2단계 상승시키고 방어력・명중・회피을 2단계 저하시킨다.
PRINTL 적을 거대화시키 힘을 올리는 대신 피하기 어렵고, 맞추기 어렵고, 방어하기 어렵게 한다
PRINTL 영걸 스쿠나 신묘마루 스펠카드（전용）

;継承に필요한部位
@継承部位_2925,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2925,ARG
RETURN 1
;とくにない

