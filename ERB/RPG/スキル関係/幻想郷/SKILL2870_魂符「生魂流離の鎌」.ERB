;통상공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_2870,ARG
RESULTS = 혼부「생혼을빼는낫」

;　명중
@SKILL_HITRATE_2870, ARG
RETURN 95

;　위력
@SKILL_POWER_2870, ARG
RETURN 260

;　흡수率
@SKILL_HP_ABSORBER_2870, ARG
RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2870,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2870,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2870, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2870,ARG
RETURN 15

;　상성
@SKILL_TYPE_2870,ARG
RETURN 0
;검격

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2870, ARG
RETURN 1
;EXTRA

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2870, ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2870
RETURN 1

;　사거리
@SKILL_RANGE_2870,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2870,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2870
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2870,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2870
RETURN 0
;검격

;속도보정
@SKILL_SPEED_2870, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2870,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2870
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2870
RETURN 10


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2870,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2870,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2870,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2870,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2870,ARG,ARG:1
CALL ATTACK_SINGLE, ARG, POS(ARG:1),2870
;#LOCALSIZE 2
;LOCAL = POS(ARG:1)
;
;LOCAL:1 = BASE:LOCAL:ＨＰ
;CALL ATTACK_SINGLE,ARG,LOCAL,2870
;LOCAL:1 = MAX(0, LOCAL:1 - BASE:LOCAL:ＨＰ)
;SIF LOCAL:1 + BASE:ARG:ＨＰ >= MAXBASE:ARG:ＨＰ
;	LOCAL:1 = MAXBASE:ARG:ＨＰ - BASE:ARG:ＨＰ
;
;IF LOCAL:1 > 0
;	PRINTFORMW 　　TARGET:[{CFLAG:ARG:포지션}] %CALLNAME:ARG,20,LEFT%　>>>>>>　{LOCAL:1}회복
;	CALL VAR_HP, ARG , LOCAL:1, 3
;ENDIF

;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2870
PRINTL 낫으로 적을 도려내고、흘러나온 적의 생명력을 흡수한다。
PRINTL 사신 오노즈카 코마치의 스펠카드

;계승에 필요한 부위
@계승부위_2870,ARG
SIF TALENT:ARG:무기
	RETURN 1
RETURN 0
;무기


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2870,ARG
RETURN 1
;とくにない

