;스킬명
@SKILL_NAME_2959,ARG
RESULTS = 보석마술


;　명중
@SKILL_HITRATE_2959, ARG
RETURN 100

;　위력
@SKILL_POWER_2959, ARG
IF ARG == -1
	RETURN 300
ENDIF
IF BASE:ARG:LV < 60
	RETURN BASE:ARG:LV * 10
ELSE
	RETURN 600
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2959,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2959,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_2959, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_2959,ARG
RETURN 5

;　상성
@SKILL_TYPE_2959,ARG
;　相性 火炎・電撃(レベル40まで)・氷結(レベル60まで)・疾風(それ以上)のうち、もっともダメージが高くなるものになる
#LOCALSSIZE 5
#LOCALSIZE 5
SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("화염")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("화염")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("화염")
	
IF BASE:ARG:LV < 40
	LOCAL:4 = 3
ELSEIF BASE:ARG:LV < 60
	LOCAL:4 = 4
ELSE
	LOCAL:4 = 5
ENDIF
	
LOCAL:2 = -300
;デフォは火炎をいれとく
LOCALS = 화염
LOCALS:1 = 전격
LOCALS:2 = 화염
LOCALS:3 = 빙결
LOCALS:4 = 질풍
FOR LOCAL:1, 1, LOCAL:4
	;判定用に数値を仕舞う。反射は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = !(HAVE_SKILL(ARG,[[스킬:영세라이도우]]) || HAVE_SKILL(ARG,2947)) && CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? -5 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"가드킬") && LOCAL:3 < 100 ? 100 # LOCAL:3
	LOCAL:3 = CFLAG:LOCAL:마법반사플래그 ? -200 # LOCAL:3

	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_2959, ARG
RETURN 2
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_2959, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_2959
RETURN 1

;　사거리
@SKILL_RANGE_2959,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_2959,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_2959
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_2959,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_2959
RETURN 7
;火炎

;속도보정
@SKILL_SPEED_2959, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_2959,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_2959
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_2959
RETURN 7

@SKILL_RANKUP_2959
RETURN 0



;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_2959,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_2959,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2959,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_2959,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_2959,ARG,ARG:1
IF CFLAG:ARG:PT플래그 > 0
	IF BASE:ARG:LV < 60
		MONEY -=  BASE:ARG:LV * 200
	ELSE
		MONEY -=  12000
	ENDIF
	SIF MONEY < 0
		MONEY = 0
ENDIF
PRINTFORMW %CALLNAME:ARG%は宝石を砕いて魔力を開放した！
LOCAL = POS(ARG:1)
CALL ATTACK_SINGLE,ARG,LOCAL,2959






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_2959
PRINTL 宝石を砕いて複合属性の魔力弾として放つ。

;계승에 필요한 부위
@계승부위_2959,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2959,ARG
IF CFLAG:ARG:PT플래그 > 0
	IF BASE:ARG:LV < 60
		SIF MONEY < BASE:ARG:LV * 200
			RETURN 0
	ELSE
		SIF MONEY < 12000
			RETURN 0
	ENDIF
ENDIF
RETURN 1
;金の有無、

