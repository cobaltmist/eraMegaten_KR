;통상공격
;스킬のテン플레이트になっています

;스킬名
@SKILL_NAME_2970,ARG
RESULTS = 배럴레플리카

;　명중
@SKILL_HITRATE_2970, ARG
RETURN 100

;　위력
@SKILL_POWER_2970, ARG

IF ARG == -1
 RETURN 400
ENDIF

IF 現HP割合(ARG) < 33
	SIF FLAG:확산플래그
		RETURN 400
	RETURN 600
ELSE
	SIF FLAG:확산플래그
		RETURN 250
	RETURN 400
ENDIF

@SKILL_CRITICAL_RATE_2970, ARG
RETURN 20

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2970,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2970,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2970, ARG
SIF FLAG:확산플래그
	RETURN 1
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2970,ARG
RETURN 50

;　相性
@SKILL_TYPE_2970,ARG
RETURN GET_TYPE_NUM("만능")

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2970, ARG
RETURN 1
;どちらでもない

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_2970, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2970
RETURN 1

;　사거리
@SKILL_RANGE_2970,ARG
RETURN 3

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_2970,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_2970
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2970,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2970
RETURN 23
;

;속도보정
@SKILL_SPEED_2970, ARG = -1
RETURN -2

;　구상の표시とその타입
@SKILL_MESSAGE_2970,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2970
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_2970
RETURN 23


;テン플레이트用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2970,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2970,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2970,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2970,ARG
RETURN 0

;拡散공격用추가処理
@SKILL_SPREAD_2970,ARG
RETURN 1

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2970,ARG,ARG:1
CALL ATTACK_SPREAD,ARG,ARG:1,2970






;====================================================
;実行内容ここまで
;====================================================

;　 설명文の표시
@SKILL_EXPLAIN_2970
PRINTL 적 1명에게 빛의 총탄을 발사해、그 주위 함께 후려쳐 넘긴다。
PRINTL 크리티컬율 높음・만능속성
PRINTL 시온・엘트남・아틀라시아의 필살기

;継承に필요한部位
@継承部位_2970,ARG
SIF !TALENT:ARG:사격무기
	RETURN 0
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_2970,ARG
RETURN 1

