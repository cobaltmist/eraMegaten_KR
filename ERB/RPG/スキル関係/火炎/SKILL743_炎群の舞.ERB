;弱点相손に撃つと공격(爆弾化)→공격(爆発)→D-COOPとえごいことになりそう

;스킬名
@SKILL_NAME_743,ARG
IF SKILL_CHANGE(ARG, [[キャラ:리글]])
	RESULTS = 등부「파이어플라이 피너미넌」
ELSE
	RESULTS = 염군의 춤
ENDIF

;　명중
@SKILL_HITRATE_743, ARG
RETURN 90

;　威힘
;舞3HIT＞폭렬의손톱2HIT＝(아기다인)＞舞2HIT＞大舌
@SKILL_POWER_743, ARG
RETURN 130

;최저공격횟수
@SKILL_MINATTACKNUMBER_743,ARG
RETURN 3

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_743,ARG
RETURN 5

;一体辺りの최대ＨＩＴ数
@SKILL_MAXATK_PER_743,ARG
IF SKILL_CHANGE(ARG, [[キャラ:리글]])
	RETURN 4
ELSE
	RETURN 3
ENDIF

;一体辺りの최저ＨＩＴ数
@SKILL_MINATK_PER_743,ARG
RETURN 0

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_743, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_743,ARG
RETURN 32

;　相性
@SKILL_TYPE_743,ARG
RETURN GET_TYPE_NUM("화염")
;화염

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_743, ARG
RETURN 1

;　1물리데미지で計算するか2마법데미지で計算するか
@SKILL_DAMAGETYPE_743, ARG
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_743
RETURN 1

;　사거리
@SKILL_RANGE_743,ARG
RETURN 2

;　범위　11명　2１열　3전체　4　적아군전체
@SKILL_SPHERE_743,ARG
RETURN 2

;　 効果の타입　1공격、2회복、3상태변화、4特殊、5상태회복
@SKILL_EFECT_743
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_743,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_743
RETURN 7
;화염

;속도보정
@SKILL_SPEED_743, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_743,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_743
RETURN 0

;스킬변화？用랭크
@SKILL_RANK_743
RETURN 5

@SKILL_RANKUP_743
;랭크アップなし
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_743,ARG
IF SKILL_CHANGE(ARG, [[キャラ:리글]])
	RETURN 18
ELSE
	RETURN 9
ENDIF

;추가효과상성
@SKILL_ADDTIONAL_TYPE_743,ARG
IF SKILL_CHANGE(ARG, [[キャラ:리글]])
	RETURN GET_TYPE_NUM("화염")
ELSE
	RETURN GET_TYPE_NUM("신경")
ENDIF

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_743,ARG
IF SKILL_CHANGE(ARG, [[キャラ:리글]])
	RETURN 25
ELSE
	RETURN 2
ENDIF

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_743,ARG
IF SKILL_CHANGE(ARG, [[キャラ:리글]])
	RETURN 40
ELSE
	RETURN 10
ENDIF

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_743,ARG,ARG:1
CALL ATTACK_RAND,ARG,ARG:1,743






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_743
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:리글]]
	PRINTL 흩날리는 형화로 적 전1열랜덤으로 3〜5회 물리공격＋화상부여。후열사용가능。
	PRINTL 요충　리글・나이트버그의 스펠카드。
ELSE
PRINTL [후열] 적 전열에 3～5회(1인 최대 3회) 화염속성 소 물리데미지. (낮은 확률로 BOMB)
ENDIF

;継承に필요な部位
@継承部位_743,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_743,ARG
RETURN 1
;とくにない

