;통상공격
;스킬のテンプレートになっています

;스킬명
@SKILL_NAME_755,ARG
RESULTS = 마라라기다인

;　명중
@SKILL_HITRATE_755, ARG
RETURN 90

;　위력
@SKILL_POWER_755, ARG
RETURN 420

;　최저공격횟수
@SKILL_MINATTACKNUMBER_755,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_755,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_755, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_755,ARG
RETURN 90

;　상성
@SKILL_TYPE_755,ARG
RETURN 4
;화염

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_755, ARG
RETURN 2
;마법

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_755, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_755
RETURN 1

;　사거리
@SKILL_RANGE_755,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_755,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_755
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_755,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_755
RETURN 7

@継承不能_755
RETURN 1
;

;속도보정
@SKILL_SPEED_755, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_755,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_755
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_755
RETURN 10


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_755,ARG
RETURN GET_STATE_NUM("CHARM")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_755,ARG
RETURN 9

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_755,ARG
RETURN 60

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_755,ARG
RETURN 80
;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_755,ARG,ARG:1
;IF NO:ARG == 292
;SETCOLOR 0xFF33FF
;PRINTFORML ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓　
	;PRINTFORM ┃＞%조사처리(CALLNAME:ARG,"는")%
		;IF RAND:2
			;PRINT 그분의 훌륭한 물건을 더욱 팽창시키면서
		;ELSE
			;PRINT 스스로 박동하는 거체를 하늘로 우뚝 세우며
		;ENDIF
		;PRINTFORML %" ",18,LEFT%┃
			;IF RAND:3 == 0
				;PRINTL ┃＞거북이의 머리와 비슷한 끝에서 녹색의 액체를 발사했다!　　　　　　　　　　　┃
			;ELSEIF RAND:2 == 0
				;PRINTL ┃＞용암과 같은 정렬을 주변에 흩뿌렸다!　　　　　　　　　　　┃
			;ELSE
				;PRINTL ┃＞숨이 막힐듯한 냄새와 함께 걸쭉한 무언가를 퍼부었다!　　　　　　　　　　┃
			;ENDIF
			;PRINTW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;RESETCOLOR
;ENDIF


CALL ATTACK_FIELD,ARG,ARG:1,755





;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_755
PRINTL [후열] 적 전체에 화염속성 특대 마법데미지. (높은 확률로 CHARM)


;계승에 필요한 부위
@계승부위_755,ARG
;오토코노코とか후타나리とかその時点で優位なんだから一つくらい純粋な男に利点あってもいいよね？
SIF TALENT:ARG:140
	RETURN 1
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_755,ARG
RETURN 1
;とくにない

