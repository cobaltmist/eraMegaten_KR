

;스킬명
@SKILL_NAME_725,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미즈치(SD)]])
	RESULTS = 파이어볼
ELSE
	RESULTS = 파이어볼
ENDIF

;　명중
@SKILL_HITRATE_725, ARG
RETURN 100

;　위력
@SKILL_POWER_725, ARG
IF SKILL_CHANGE(ARG, [[キャラ:미즈치(SD)]])
	RETURN 370
ELSE
	RETURN 225
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_725,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_725,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_725, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_725,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미즈치(SD)]])
	RETURN 25
ELSE
	RETURN 8
ENDIF

;　상성
@SKILL_TYPE_725,ARG
RETURN 4
;화염

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_725, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_725, ARG
RETURN 2


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_725
RETURN 1

;　사거리
@SKILL_RANGE_725,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_725,ARG
IF SKILL_CHANGE(ARG, [[キャラ:미즈치(SD)]])
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_725
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_725,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_725
RETURN 7
;화염

;속도보정
@SKILL_SPEED_725, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_725,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_725
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_725
RETURN 4



@SKILL_RANKUP_725
;버닝링
D:1 = 726
;사자염진
D:2 = 727
;랭크アップ先は二つ
RETURN 2


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_725,ARG
RETURN 18

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_725,ARG
RETURN 4

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_725,ARG
RETURN 30

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_725,ARG
RETURN 95

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_725,ARG,ARG:1
IF SKILL_CHANGE(ARG, [[キャラ:미즈치(SD)]])
	CALL ATTACK_FIELD,ARG,ARG:1,725
ELSE
	LOCAL = POS(ARG:1)
	CALL ATTACK_SINGLE,ARG,LOCAL,725
ENDIF






;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_725
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:미즈치(SD)]]
	PRINTL [후열] 적 1열에 화염속성 특대 마법데미지. (낮은 확률로 FLAME)
ELSE
	PRINTL [후열] 적 1체에 화염속성 중 마법데미지. (낮은 확률로 FLAME)
ENDIF

;계승에 필요한 부위
@계승부위_725,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_725,ARG
RETURN 1
;とくにない

