;追加キャラ作成用テンプレ、コピペ・コメントアウトしXXをフラグで置き換える
;サンプルキャラ(種族)
;@SORP_PLAY_10XX, ARGS
;#DIM CHARA, 1
;#LOCALSSIZE 1
;SELECTCASE ARGS
;	CASE "娼婦番号"
;		RESULTS = [10XX]
;	CASE "キャラ名"
;		RESULTS = サンプルキャラ
;	CASE "指名名"
;		RESULTS = サンプルキャラ(種族)
;	CASE "所属"
;		;表なら1、裏なら2、援助なら3
;		RETURN 1
;	CASE "判定"
;		;指名可能なら0、不可なら1
;		IF 롯폰기창관플래그:XX >= 3
;			RETURN 1
;		ELSEIF (出現条件)
;			RETURN 0
;		ELSE
;			RETURN 1
;		ENDIF
;	CASE "価格"
;		RETURN 100
;	CASE "紹介文"
;		IF 롯폰기창관플래그:XX == 0
;			CALL MESSAGE_WINDOW_D, "受付の男", @"成程、〇〇嬢をご希望ですか/(初回説明)"
;			CALL MESSAGE_WINDOW_D, "露出したウェイトレス", @"(初回説明)";レストラン用
;		ELSE
;			CALL MESSAGE_WINDOW_D, "受付の男", @"成程、〇〇嬢ですか/(リピータ説明)"
;			CALL MESSAGE_WINDOW_D, "露出したウェイトレス", @"(リピータ説明)";レストラン用
;		ENDIF
;	CASE "確認"
;		CALL MESSAGE_WINDOW_D, "受付の男", @"さて、〇〇嬢嬢ですが…/一晩で？００マッカになります"
;		CALL MESSAGE_WINDOW_D, "露出したウェイトレス", @"？００マッカでの援助を希望しておりますがー/どうしますかー？";レストラン用
;	CASE "プレイ口上"
;		;CALL DUNGEON_WORP,1,13,3;表娼館用
;		;CALL DUNGEON_WORP,2,5,10;裏娼館用
;		;条件によって分岐の追加も可
;		IF 롯폰기창관플래그:XX == 0
;			CALL MESSAGE_WINDOW_D, "〇〇", @"(初回口上)"
;			CALL MESSAGE_WINDOW_D, "", @"………………/…………/……"
;			CALL SHOW_PICTURE, "EMPTY"
;			CALL MESSAGE_WINDOW_D, "", @"＞楽しく遊びました…"
;			;経験フラグを設定
;			롯폰기창관플래그:XX = 1
;		ELSE
;			CALL MESSAGE_WINDOW_D, "〇〇", @"(リピーター口上)"
;			CALL MESSAGE_WINDOW_D, "", @"………………/…………/……"
;			CALL SHOW_PICTURE, "EMPTY"
;			CALL MESSAGE_WINDOW_D, "", @"＞楽しく遊びました…"
;			;見受け可能フラグを設定
;			롯폰기창관플래그:XX = 2
;		ENDIF
;		;プレイ経験(任意の経験を追加可)
;		EXP:MASTER:성교경험 += ABL:MASTER:기교 * 2 + 1
;		EXP:MASTER:절정경험 += ABL:MASTER:기교
;		CALL INCREASE_LC,5
;	CASE "身受け価格"
;		RETURN 10000
;	CASE "身受け確認"
;		CALL MESSAGE_WINDOW_D, "受付の男", @"〇〇嬢を身請けされるのですかな？"
;		CALL MESSAGE_WINDOW_D, "受付の男", @"でしたら、？？００マッカ頂く事になります/よろしいですかな？"
;		CALL MESSAGE_WINDOW_D, "露出したウェイトレス", @"(身受け説明)";レストラン用
;		CALL MESSAGE_WINDOW_D, "露出したウェイトレス", @"？？００マッカでお持ち帰りされても良いとのことですがー/いかがいたしますかー？";レストラン用
;	CASE "身受け口上"
;		;ここで加入処理を作成
;		CALL ADD_NEW_COMPANION, (元キャラのキャラ番号), 3000
;		CHARA = RESULT:1
;		NAME:CHARA = サンプルキャラ
;		CALLNAME:CHARA = サンプルキャラ
;		CSTR:CHARA:일인칭 = 私
;		CSTR:CHARA:이인칭 = ご主人様
;		;即戦力として使えるように色々底上げ
;		CFLAG:CHARA:매춘인기 = 100
;		CFLAG:CHARA:매각가능 = 2
;		CFLAG:CHARA:의존도 = 1000
;		;ベース
;		MAXBASE:CHARA:체력 += 2000
;		BASE:CHARA:체력 = MAXBASE:CHARA:체력
;		MAXBASE:CHARA:기력 += 2000
;		BASE:CHARA:기력 = MAXBASE:CHARA:기력
;		BASE:CHARA:ＭＡＧ = MAXBASE:CHARA:ＭＡＧ
;		;素質
;		;能力
;		;経験
;		;口上
;		CALL MESSAGE_WINDOW_D, "〇〇", @"コンゴトモ　ヨロシク"
;		MONEY:1 -= 10000
;		롯폰기창관플래그:XX = 3
;	CASE "宣伝"
;		;
;ENDSELECT

