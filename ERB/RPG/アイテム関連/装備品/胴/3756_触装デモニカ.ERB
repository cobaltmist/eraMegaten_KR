
;======================================================
;장비품의 데이터
;촉수옷데모니카
;======================================================

@기본능력수정_3756,ARG,ARG:1 = -1

SELECTCASE ARG
ENDSELECT
RETURN 0

@전투능력수정_3756,ARG,ARG:1 = -1
SIF ARG == -1
			RETURN 0
SELECTCASE ARG
	CASE 2
		SIF ARG:1 == -1
			RETURN 0
		IF FLAG:무후후전개
			IF EQUIP:(ARG:1):머리 == 0 && EQUIP:(ARG:1):팔 == 0 && EQUIP:(ARG:1):발 == 0
				LOCAL = MIN(BASE:(ARG:1):LV * 2 + 30 , 200)
				RETURN LOCAL
			ELSE
			RETURN 0
			ENDIF
		ELSE
		RETURN 0
		ENDIF
	CASE 3
		SIF ARG:1 == -1
			RETURN 0
		IF FLAG:무후후전개
			IF EQUIP:(ARG:1):머리 == 0 && EQUIP:(ARG:1):팔 == 0 && EQUIP:(ARG:1):발 == 0
				LOCAL = MIN(BASE:(ARG:1):LV * 1 + 10 , 130)
				RETURN LOCAL
			ELSE
			RETURN 0
			ENDIF
		ELSE
		RETURN 0
		ENDIF
	CASE 5
		SIF ARG:1 == -1
			RETURN 0
		IF FLAG:무후후전개
			IF EQUIP:(ARG:1):머리 == 0 && EQUIP:(ARG:1):팔 == 0 && EQUIP:(ARG:1):발 == 0
				LOCAL = MIN(BASE:(ARG:1):LV / 2 + 10 , 60)
				RETURN LOCAL
			ELSE
			RETURN 0
			ENDIF
		ELSE
		RETURN 0
		ENDIF
ENDSELECT

RETURN 0

@방어상성_3756,ARG,ARG:1 = -1
SIF ARG == -1
			RETURN 100
SIF ARG:1 == -1
			RETURN 100
SELECTCASE ARG
	CASE 8,9,10,11
		IF FLAG:무후후전개
			RETURN 100
		ELSE
		ENDIF
	CASEELSE
		IF FLAG:무후후전개
			IF EQUIP:(ARG:1):머리 == 0 && EQUIP:(ARG:1):팔 == 0 && EQUIP:(ARG:1):발 == 0
				RETURN 50
			ELSE
			RETURN 100
			ENDIF
		ELSE
		ENDIF
ENDSELECT
RETURN 100

@BS내성_3756,ARG,ARG:1 = -1
SELECTCASE ARG
	CASE GET_STATE_NUM("FLY"),GET_STATE_NUM("ORGY"),GET_STATE_NUM("HEAT")
		RETURN 0
	CASE GET_STATE_NUM("DYING")
		IF FLAG:무후후전개
			RETURN -100
			ELSE
			ENDIF
	CASEELSE
		IF FLAG:무후후전개
		RETURN -100
		ELSE
		ENDIF
ENDSELECT
RETURN 0

@공격상성_3756, ARG = -1
RETURN 2

@최저공격횟수_3756, ARG = -1

RETURN 1

@최대공격횟수_3756, ARG = -1

RETURN 1

@사거리_3756, ARG = -1

RETURN 1

@공격범위_3756, ARG = -1

RETURN 1

@장비부위_3756

RETURN GET_EQUIPNUM("몸통")

@필요레벨_3756
RETURN 1

@장비조건_3756,ARG
RETURN 1

@구입조건_3756
RETURN 0

;마카교환 가능한가
@EQUIP_EXCHANGE_3756
RETURN 0

@장비해설_3756
PRINTFORML アメリカ軍が開発した戦闘服デモニカスーツの設計図を盗用し魔界技術が用いられた、色が禍々しいコピー品。
PRINTFORML 外装は如何にもそれっぽいが内部には触手が蠢いており、余程触手に慣れていない限り着用者はマトモに戦えない。
PRINTFORML 全身装備でありアクセサリを除く他の防具を装備出来ないが、慣れればホンモノよりも着心地がイイらしい。
PRINTFORML ただし、触手が戦闘中に機能するような展開にならなければ防御性能は全く発揮されない。

@장비조건설명_3756
PRINTL 장비조건：없음