
;======================================================
;장비品の데이터
;롱기누스아머
;======================================================

@기본능력수정_3612,ARG,ARG:1 = -1

SELECTCASE ARG
ENDSELECT
RETURN 0

@전투능력수정_3612,ARG,ARG:1 = -1

SELECTCASE ARG
	CASE 0
		SIF ARG:1 == -1
			RETURN 30
		IF EQUIP:(ARG:1):팔 == 4051
			LOCAL = BASE:(ARG:1):LV * 1 / 4 +50
			RETURN LOCAL
		ELSE
			RETURN 0
		ENDIF
	CASE 1
		SIF ARG:1 == -1
			RETURN 30
		IF EQUIP:(ARG:1):팔 == 4051
			LOCAL = BASE:(ARG:1):LV * 1 / 4 +50
			RETURN LOCAL
		ELSE
			RETURN 0
		ENDIF
	CASE 2
		SIF ARG:1 == -1
			RETURN 30
		IF EQUIP:(ARG:1):머리 == 3207
			LOCAL = BASE:(ARG:1):LV * 1 / 4 +50
			RETURN LOCAL
		ELSE
			RETURN 0
		ENDIF
	CASE 3
		SIF ARG:1 == -1
			RETURN 30
		IF EQUIP:(ARG:1):발 == 4604
			LOCAL = BASE:(ARG:1):LV * 1 / 4 +50
			RETURN LOCAL
		ELSE
			RETURN 0
		ENDIF
	CASE 5
		SIF ARG:1 == -1
			RETURN 30
		IF EQUIP:(ARG:1):머리 == 3207
			LOCAL = BASE:(ARG:1):LV * 1 / 4 +50
			RETURN LOCAL
		ELSE
			RETURN 0
		ENDIF
ENDSELECT
RETURN 0

@방어상성_3612,ARG,ARG:1 = -1
IF ARG:1 > -1 && CSTR:(ARG:1):종족명 == "성창騎士" && EQUIP:(ARG:1):머리 == 3207 && EQUIP:(ARG:1):팔 == 4051 && EQUIP:(ARG:1):발 == 4604
	SELECTCASE ARG
	;0,검격
	;1,비구
	;2,타격
	;3,전술
	;4,화염
	;5,빙결
	;6,전격
	;7,충격
	;8,신경
	;9,정신
	;10,파마
	;11,주살
	;一応
	;12,지변
	;13,수격
	;14,질풍
	;15,중력
	;16,핵열
	;17,만능
	;18,사이코
	CASE 0
		RETURN 25
	CASE 1
		RETURN 25
	CASE 2
		RETURN 25
	CASE 3
		RETURN 25
	CASE 4
		SIF NO:(ARG:1) == [[キャラ:롱기누스８]] || NO:(ARG:1) == [[キャラ:롱기누스１３]]
			RETURN 200		
	CASE 12
	SIF NO:(ARG:1) == [[キャラ:롱기누스９]] || NO:(ARG:1) == [[キャラ:롱기누스１２]]
			RETURN 200
	SIF NO:(ARG:1) == [[キャラ:롱기누스１０]]
			RETURN -100
	CASE 13
	SIF NO:(ARG:1) == [[キャラ:롱기누스１]] || NO:(ARG:1) == [[キャラ:롱기누스２]] || NO:(ARG:1) == [[キャラ:롱기누스３]] || NO:(ARG:1) == [[キャラ:롱기누스４]] || NO:(ARG:1) == [[キャラ:롱기누스５]] || NO:(ARG:1) == [[キャラ:롱기누스６]] || NO:(ARG:1) == [[キャラ:롱기누스７]] || NO:(ARG:1) == [[キャラ:롱기누스１１]]
			RETURN 200
	CASE 14
	SIF NO:(ARG:1) == [[キャラ:롱기누스１０]]
			RETURN 200
	SIF NO:(ARG:1) == [[キャラ:롱기누스９]]
			RETURN -100
	ENDSELECT
ELSE
	SELECTCASE ARG
	;0,검격
	;1,비구
	;2,타격
	;3,전술
	;4,화염
	;5,빙결
	;6,전격
	;7,충격
	;8,신경
	;9,정신
	;10,파마
	;11,주살
	;一応
	;12,지변
	;13,수격
	;14,질풍
	;15,중력
	;16,핵열
	;17,만능
	;18,사이코
	CASE 3
		RETURN 80
	CASE 4
		RETURN 125	
	CASE 12
		RETURN 125	
	CASE 13
		RETURN 125
	ENDSELECT
ENDIF
RETURN 100

@バステ내성_3612,ARG,ARG:1 = -1
IF ARG:1 > -1 && CSTR:(ARG:1):종족명 == "성창騎士" && EQUIP:(ARG:1):머리 == 3207 && EQUIP:(ARG:1):팔 == 4051 && EQUIP:(ARG:1):발 == 4604
	SELECTCASE ARG
		CASE GET_STATE_NUM("HAPPY")
			RETURN -100
		CASE GET_STATE_NUM("SHOCK")
			RETURN 150
		CASE GET_STATE_NUM("PANIC")
			RETURN -100
		CASE GET_STATE_NUM("SLEEP")
			RETURN -100
		CASE GET_STATE_NUM("FREEZE")
			RETURN -100
		CASE GET_STATE_NUM("BIND")
			RETURN -100
		CASE GET_STATE_NUM("CLOSE")
			RETURN -100
		CASE GET_STATE_NUM("POISON")
			RETURN -100
		CASE GET_STATE_NUM("CHARM")
			RETURN -100
		CASE GET_STATE_NUM("PALYZE")
			RETURN -100
		CASE GET_STATE_NUM("CURSE")
			RETURN -100
		CASE GET_STATE_NUM("FLY")
			RETURN -100
		CASE GET_STATE_NUM("STONE")
			RETURN -100
		CASE GET_STATE_NUM("DYING")
			RETURN -100
	ENDSELECT
ELSE
	SELECTCASE ARG
	ENDSELECT
ENDIF

@공격상성_3612, ARG = -1

RETURN 2

@최저공격횟수_3612, ARG = -1

RETURN 1

@최대공격횟수_3612, ARG = -1

RETURN 1

@사거리_3612, ARG = -1

RETURN 1

@공격범위_3612, ARG = -1

RETURN 1

@장비箇所_3612

RETURN GET_EQUIPNUM("몸통")

@필요레벨_3612
RETURN 1

@장비해설_3612
PRINTFORML 성당기사단의 파워 수트「힌멜 포이어」를 복원한 것
PRINTFORML 복원한 다른 부품을 사용함으로써 진가를 발휘한다
SETCOLOR 0x87ceeb
PRINTL 《롱기누스헤드》《롱기누스암》《롱기누스레그》
RESETCOLOR

@장비조건_3612,ARG
SIF CSTR:ARG:종족명 == "성창騎士"
	RETURN 1
SIF BASE:ARG:LV < 40
	RETURN 0
RETURN 1

@장비조건설명_3612
PRINTL 장비조건：성창기사or레벨40이상

@구입조건_3612
RETURN 0

@引継ぎ可不可_3612
RETURN 1
