;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;재래식캐넌
;======================================================

@기본능력수정_2804,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 4
	CASE "속도"
		RETURN 4
ENDSELECT
RETURN 0

@전투능력수정_2804,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2804,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

;척탄병 세트. 수류탄 발사 능력
@장비추가스킬_2804,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
IF ARG:1 != -1
	;1-1번 세트. 케블러 방어구 세트시 파편수류탄 능력
	IF EQUIP:(ARG:1):몸통 == 3805 && EQUIP:(ARG:1):팔 == 4072 && EQUIP:(ARG:1):발 == 4700
		SELECTCASE ARG
			CASE 0
				;수류탄발사
				RETURN 3704
			CASE 1
				;잠복(XCOM)
				RETURN 589
		ENDSELECT
	ELSE
		SELECTCASE ARG
		ENDSELECT
	ENDIF
ENDIF
RETURN 0

@공격상성_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비箇所_2804
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 15
	RETURN 60
ELSEIF BASE:ARG:LV >= 14
	RETURN 58
ELSEIF BASE:ARG:LV >= 13
	RETURN 56
ELSEIF BASE:ARG:LV >= 12
	RETURN 54
ELSEIF BASE:ARG:LV >= 11
	RETURN 52
ELSEIF BASE:ARG:LV >= 10
	RETURN 50
ELSEIF BASE:ARG:LV >= 9
	RETURN 48
ELSEIF BASE:ARG:LV >= 8
	RETURN 46
ELSEIF BASE:ARG:LV >= 7
	RETURN 44
ELSEIF BASE:ARG:LV >= 6
	RETURN 42
ELSEIF BASE:ARG:LV >= 5
	RETURN 40
ELSE
	RETURN 40
ENDIF

@총명중_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 37
IF BASE:ARG:LV >= 15
	RETURN 37
ELSEIF BASE:ARG:LV >= 14
	RETURN 36
ELSEIF BASE:ARG:LV >= 13
	RETURN 35
ELSEIF BASE:ARG:LV >= 12
	RETURN 34
ELSEIF BASE:ARG:LV >= 11
	RETURN 33
ELSEIF BASE:ARG:LV >= 10
	RETURN 32
ELSEIF BASE:ARG:LV >= 9
	RETURN 31
ELSEIF BASE:ARG:LV >= 8
	RETURN 30
ELSEIF BASE:ARG:LV >= 7
	RETURN 20
ELSEIF BASE:ARG:LV >= 6
	RETURN 28
ELSEIF BASE:ARG:LV >= 5
	RETURN 27
ELSE
	RETURN 27
ENDIF

@특수탄창_2804, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

@총타입_2804
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어썰트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 3

@速度보정_2804
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2
;핸드건 8 샷건 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2804
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

@장비조건_2804,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 5
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2804
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV5～

@장비해설_2804
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM에서 사용하는 통상적인 척탄병의 캐넌 무기.
PRINTL 　　　　　　　　레벨 15까지 무기 위력과 명중이 소폭 증가한다。
PRINTL 케블러 방어구 세트와 함께 장비시 수류탄발사 능력을 얻는다. 레벨 10부터는 척탄병 능력 보너스 획득.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2804
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2804
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0