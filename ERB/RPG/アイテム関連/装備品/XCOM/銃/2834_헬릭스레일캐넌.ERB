;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;헬릭스레일캐넌
;======================================================

@기본능력수정_2834,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 8
	CASE "속도"
		RETURN 8
ENDSELECT
RETURN 0

@전투능력수정_2834,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
;버그픽스용
SIF (ARG:1) == -1
	ARG:1 = 0
SELECTCASE GET_BATTLESTATUS(ARG)
	CASE "공격"
		SIF ARG == -1
			RETURN 100
		IF BASE:ARG:LV >= 30
			RETURN 100
		ELSEIF BASE:ARG:LV >= 29
			RETURN 99
		ELSEIF BASE:ARG:LV >= 28
			RETURN 98
		ELSEIF BASE:ARG:LV >= 27
			RETURN 97
		ELSEIF BASE:ARG:LV >= 26
			RETURN 96
		ELSEIF BASE:ARG:LV >= 25
			RETURN 95
		ELSEIF BASE:ARG:LV >= 24
			RETURN 93
		ELSEIF BASE:ARG:LV >= 23
			RETURN 91
		ELSEIF BASE:ARG:LV >= 22
			RETURN 89
		ELSEIF BASE:ARG:LV >= 21
			RETURN 87
		ELSEIF BASE:ARG:LV >= 20
			RETURN 85
		ELSEIF BASE:ARG:LV >= 19
			RETURN 84
		ELSEIF BASE:ARG:LV >= 18
			RETURN 83
		ELSEIF BASE:ARG:LV >= 17
			RETURN 82
		ELSEIF BASE:ARG:LV >= 16
			RETURN 81
		ELSEIF BASE:ARG:LV >= 15
			RETURN 80
		ELSE
			RETURN 80
		ENDIF
	CASE "명중"
		RETURN 90
ENDSELECT
RETURN 0


@방어상성_2834,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@공격상성_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2834
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 100
IF BASE:ARG:LV >= 30
	RETURN 100
ELSEIF BASE:ARG:LV >= 29
	RETURN 99
ELSEIF BASE:ARG:LV >= 28
	RETURN 98
ELSEIF BASE:ARG:LV >= 27
	RETURN 97
ELSEIF BASE:ARG:LV >= 26
	RETURN 96
ELSEIF BASE:ARG:LV >= 25
	RETURN 95
ELSEIF BASE:ARG:LV >= 24
	RETURN 93
ELSEIF BASE:ARG:LV >= 23
	RETURN 91
ELSEIF BASE:ARG:LV >= 22
	RETURN 89
ELSEIF BASE:ARG:LV >= 21
	RETURN 87
ELSEIF BASE:ARG:LV >= 20
	RETURN 85
ELSEIF BASE:ARG:LV >= 19
	RETURN 84
ELSEIF BASE:ARG:LV >= 18
	RETURN 83
ELSEIF BASE:ARG:LV >= 17
	RETURN 82
ELSEIF BASE:ARG:LV >= 16
	RETURN 81
ELSEIF BASE:ARG:LV >= 15
	RETURN 80
ELSE
	RETURN 80
ENDIF

@총명중_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 90

@특수탄창_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

@총타입_2834
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 3

@速度보정_2834
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 7
;핸드건 8 샷건 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 추가효과処理
;-----------------------------
@추가효과_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_STATE_NUM("PANIC")

@추가효과상성_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9

@추가효과명중률_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 15
IF BASE:ARG:LV >= 25
	RETURN 15
ELSE
	RETURN 0
ENDIF

@추가효과최대명중률_2834, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 35
IF BASE:ARG:LV >= 25
	RETURN 35
ELSE
	RETURN 0
ENDIF

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2834
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

@장비조건_2834,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 15
	RETURN 0
SIF CSTR:ARG:종족명 != "SPARK"
	RETURN 0
RETURN 1

@장비조건설명_2834
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV15～, SPARK

@장비해설_2834
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL SPARK 유닛이 사용하는 주무기의 강화형.
PRINTL 　　　　　　　　합금이 추가되어 내구성과 위력이 향상되었다. SPARK유닛 전용.
PRINTL 레벨 25부터 특성 '위협'에 따라 [GUN] 사용마다 PANIC 상태이상을 일으킬 수 있게 된다.
PRINTL 총기 능력치 만큼의 통상 공격력과 명중률을 추가로 제공한다.
;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2834
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2834
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0