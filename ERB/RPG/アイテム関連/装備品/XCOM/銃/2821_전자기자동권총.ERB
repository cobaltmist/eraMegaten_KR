;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;전자기자동권총
;======================================================

@기본능력수정_2821,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
;버그픽스용
SIF (ARG:1) == -1
	ARG:1 = 0
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
        	RETURN 6
	CASE "속도"
		;섀도스텝 (속도 + 5)
		IF EQUIP:(ARG:1):머리 == 4076 && EQUIP:(ARG:1):팔 == 4079 && EQUIP:(ARG:1):발 == 4707 && EQUIP:(ARG:1):몸통 == 3812 && EQUIP:(ARG:1):검 == 2246
			RETURN 11
		ELSE
			RETURN 6
		ENDIF
ENDSELECT
RETURN 0

@전투능력수정_2821,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2821,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@장비추가스킬_2821,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
IF ARG:1 != -1
	;판금 템플러 방어구 세트와 장비시
	IF EQUIP:(ARG:1):머리 == 4076 && EQUIP:(ARG:1):팔 == 4079 && EQUIP:(ARG:1):발 == 4707 && EQUIP:(ARG:1):몸통 == 3812 && EQUIP:(ARG:1):검 == 2246
		SELECTCASE ARG
			CASE 0
				;날렵한·손
				RETURN 166
			CASE 1
				;대결
				RETURN 167
			ENDSELECT
	ELSE
		SELECTCASE ARG
		ENDSELECT
	ENDIF
ENDIF
RETURN 0

@공격상성_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@사거리_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2821
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 40
IF BASE:ARG:LV >= 30
	RETURN 40
ELSEIF BASE:ARG:LV >= 28
	RETURN 39
ELSEIF BASE:ARG:LV >= 26
	RETURN 38
ELSEIF BASE:ARG:LV >= 24
	RETURN 37
ELSEIF BASE:ARG:LV >= 22
	RETURN 36
ELSEIF BASE:ARG:LV >= 20
	RETURN 35
ELSEIF BASE:ARG:LV >= 19
	RETURN 34
ELSEIF BASE:ARG:LV >= 18
	RETURN 33
ELSEIF BASE:ARG:LV >= 17
	RETURN 32
ELSEIF BASE:ARG:LV >= 16
	RETURN 31
ELSEIF BASE:ARG:LV >= 15
	RETURN 30
ELSE
	RETURN 30
ENDIF

@총명중_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 50
IF BASE:ARG:LV >= 30
	RETURN 50
ELSEIF BASE:ARG:LV >= 28
	RETURN 49
ELSEIF BASE:ARG:LV >= 26
	RETURN 48
ELSEIF BASE:ARG:LV >= 24
	RETURN 47
ELSEIF BASE:ARG:LV >= 22
	RETURN 46
ELSEIF BASE:ARG:LV >= 20
	RETURN 45
ELSEIF BASE:ARG:LV >= 19
	RETURN 44
ELSEIF BASE:ARG:LV >= 18
	RETURN 43
ELSEIF BASE:ARG:LV >= 17
	RETURN 42
ELSEIF BASE:ARG:LV >= 16
	RETURN 41
ELSEIF BASE:ARG:LV >= 15
	RETURN 40
ELSE
	RETURN 40
ENDIF

@특수탄창_2821, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

@총타입_2821
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 1

@速度보정_2821
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 8
;핸드건 8 샷 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2821
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

@장비조건_2821,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 15
	RETURN 0
SIF CSTR:ARG:종족명 != "템플러"
	RETURN 0
RETURN 1

@장비조건설명_2821
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV15～, 템플러

@장비해설_2821
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 템플러용 자동 권총에 전자기 발사체 가속기를 도입한 무기。
PRINTL 　　　　　　　　레벨 30까지 위력과 명중률이 소폭 증가한다。
PRINTL 전자기 방어구 세트 장비시 즉시 섀도스텝(속도 + 5), 날렵한·손, 대결 스킬 획득.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2821
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2821
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0