;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;Kal·90불펍소총
;======================================================

@기본능력수정_2832,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 12
	CASE "속도"
		RETURN 12
ENDSELECT
RETURN 0

@전투능력수정_2832,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2832,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@공격상성_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("핵열")

@최저공격횟수_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@최대공격횟수_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@사거리_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비箇所_2832
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 70
IF BASE:ARG:LV >= 40
	RETURN 70
ELSEIF BASE:ARG:LV >= 39
	RETURN 69
ELSEIF BASE:ARG:LV >= 38
	RETURN 68
ELSEIF BASE:ARG:LV >= 37
	RETURN 67
ELSEIF BASE:ARG:LV >= 36
	RETURN 66
ELSEIF BASE:ARG:LV >= 35
	RETURN 65
ELSEIF BASE:ARG:LV >= 34
	RETURN 64
ELSEIF BASE:ARG:LV >= 33
	RETURN 63
ELSEIF BASE:ARG:LV >= 32
	RETURN 62
ELSEIF BASE:ARG:LV >= 31
	RETURN 61
ELSEIF BASE:ARG:LV >= 30
	RETURN 60
ELSE
	RETURN 60
ENDIF

@총명중_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 90
IF BASE:ARG:LV >= 40
	RETURN 90
ELSEIF BASE:ARG:LV >= 39
	RETURN 89
ELSEIF BASE:ARG:LV >= 38
	RETURN 88
ELSEIF BASE:ARG:LV >= 37
	RETURN 87
ELSEIF BASE:ARG:LV >= 36
	RETURN 86
ELSEIF BASE:ARG:LV >= 35
	RETURN 85
ELSEIF BASE:ARG:LV >= 34
	RETURN 84
ELSEIF BASE:ARG:LV >= 33
	RETURN 83
ELSEIF BASE:ARG:LV >= 32
	RETURN 82
ELSEIF BASE:ARG:LV >= 31
	RETURN 81
ELSEIF BASE:ARG:LV >= 30
	RETURN 80
ELSE
	RETURN 80
ENDIF

@특수탄창_2832, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 6

@총타입_2832
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 3

@速度보정_2832
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 8
;핸드건 8 샷건 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2832
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 30

@장비조건_2832,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 30
	RETURN 0
SIF CSTR:ARG:종족명 != "스커미셔"
	RETURN 0
RETURN 1

@장비조건설명_2832
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV30～, 스커미셔

@장비해설_2832
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 스커미셔가 사용하는 불펍 설정의 에너지 기반 소총. 스커미셔 전용.
PRINTL 　　　　　　　　확산 빔을 발사하며 발사체 버전에 비해 좀 더 우아하고 덜 직관적이지만 강력한 무기.
PRINTL 특성 '제로 인'에 따라 총기 명중에 + 20 보정을 받는다.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2832
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2832
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0