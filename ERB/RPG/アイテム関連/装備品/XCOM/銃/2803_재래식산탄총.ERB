;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;재래식산탄총
;======================================================

@기본능력수정_2803,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 4
	CASE "속도"
		RETURN 4
ENDSELECT
RETURN 0

@전투능력수정_2803,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
;버그픽스용
SIF (ARG:1) == -1
	ARG:1 = 0
SELECTCASE GET_BATTLESTATUS(ARG)
	CASE "명중"
		;블레이드 마스터 효과
		IF EQUIP:(ARG:1):검 == 2273 && EQUIP:(ARG:1):몸통 == 3805 && EQUIP:(ARG:1):팔 == 4072 && EQUIP:(ARG:1):발 == 4700 && BASE:(ARG:1):LV > 9
			RETURN 10
		ELSE
			RETURN 0
		ENDIF
ENDSELECT
RETURN 0

@방어상성_2803,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@공격상성_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2803
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 15
	RETURN 60
ELSEIF BASE:ARG:LV >= 14
	RETURN 58
ELSEIF BASE:ARG:LV >= 13
	RETURN 56
ELSEIF BASE:ARG:LV >= 12
	RETURN 54
ELSEIF BASE:ARG:LV >= 11
	RETURN 52
ELSEIF BASE:ARG:LV >= 10
	RETURN 50
ELSEIF BASE:ARG:LV >= 9
	RETURN 48
ELSEIF BASE:ARG:LV >= 8
	RETURN 46
ELSEIF BASE:ARG:LV >= 7
	RETURN 44
ELSEIF BASE:ARG:LV >= 6
	RETURN 42
ELSEIF BASE:ARG:LV >= 5
	RETURN 40
ELSE
	RETURN 40
ENDIF

@총명중_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 40
IF BASE:ARG:LV >= 15
	RETURN 40
ELSEIF BASE:ARG:LV >= 14
	RETURN 39
ELSEIF BASE:ARG:LV >= 13
	RETURN 38
ELSEIF BASE:ARG:LV >= 12
	RETURN 37
ELSEIF BASE:ARG:LV >= 11
	RETURN 36
ELSEIF BASE:ARG:LV >= 10
	RETURN 35
ELSEIF BASE:ARG:LV >= 9
	RETURN 34
ELSEIF BASE:ARG:LV >= 8
	RETURN 33
ELSEIF BASE:ARG:LV >= 7
	RETURN 32
ELSEIF BASE:ARG:LV >= 6
	RETURN 31
ELSEIF BASE:ARG:LV >= 5
	RETURN 30
ELSE
	RETURN 30
ENDIF

@특수탄창_2803, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

@총타입_2803
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 2

@速度보정_2803
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN -1
;핸드건 8 샷건 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2803
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

@장비조건_2803,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 5
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2803
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV5～

@장비해설_2803
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM에서 사용하는 통상적인 돌격병의 산탄총. 적 1체만 공격 가능하다。
PRINTL 　　　　　　　　레벨 15까지 무기 위력과 명중이 소폭 증가한다。
PRINTL 구식검과 케블러 방어구 세트를 함께 장비시 추가 효과를 얻는다.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2803
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2803
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0