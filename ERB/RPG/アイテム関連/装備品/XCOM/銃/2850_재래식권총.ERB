;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;재래식 권총. TACTICAL LEGACY용
;======================================================

@기본능력수정_2850,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 6 ;장비 특전
	CASE "속도"
		RETURN 6 ;장비 특전
ENDSELECT
RETURN 0

@전투능력수정_2850,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2850,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@장비추가스킬_2850,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
IF ARG:1 != -1
	;1-1번 세트. 레벨 9 이상시 잠복 + 응사
	IF BASE:(ARG:1):LV > 9
		SELECTCASE ARG
			CASE 0
				;잠복(XCOM)
				RETURN 589
			CASE 1
				;응사(XCOM)
				RETURN 190
		ENDSELECT
	ELSE
		SELECTCASE ARG
			CASE 0
				;잠복(XCOM)
				RETURN 589
		ENDSELECT
	ENDIF
ENDIF
RETURN 0

@공격상성_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비箇所_2850
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 45
IF BASE:ARG:LV >= 15
	RETURN 45
ELSEIF BASE:ARG:LV >= 14
	RETURN 44
ELSEIF BASE:ARG:LV >= 13
	RETURN 43
ELSEIF BASE:ARG:LV >= 12
	RETURN 42
ELSEIF BASE:ARG:LV >= 11
	RETURN 41
ELSEIF BASE:ARG:LV >= 10
	RETURN 40
ELSEIF BASE:ARG:LV >= 9
	RETURN 38
ELSEIF BASE:ARG:LV >= 8
	RETURN 36
ELSEIF BASE:ARG:LV >= 7
	RETURN 34
ELSEIF BASE:ARG:LV >= 6
	RETURN 32
ELSEIF BASE:ARG:LV >= 5
	RETURN 30
ELSE
	RETURN 30
ENDIF

@총명중_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 15
	RETURN 60
ELSEIF BASE:ARG:LV >= 14
	RETURN 59
ELSEIF BASE:ARG:LV >= 13
	RETURN 58
ELSEIF BASE:ARG:LV >= 12
	RETURN 57
ELSEIF BASE:ARG:LV >= 11
	RETURN 56
ELSEIF BASE:ARG:LV >= 10
	RETURN 55
ELSEIF BASE:ARG:LV >= 9
	RETURN 53
ELSEIF BASE:ARG:LV >= 8
	RETURN 51
ELSEIF BASE:ARG:LV >= 7
	RETURN 49
ELSEIF BASE:ARG:LV >= 6
	RETURN 47
ELSEIF BASE:ARG:LV >= 5
	RETURN 45
ELSE
	RETURN 45
ENDIF

@특수탄창_2850, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@총타입_2850
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어썰트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 1

@速度보정_2850
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9
;핸드건 8 샷 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2850
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 8

@장비조건_2850,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 8
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2850
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV8～

@장비해설_2850
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 : WAR OF THE CHOSEN - TACTICAL LEGACY PACK 특전 장비。
PRINTL 　　　　　　　　대구경권총과 큰 차이는 없으나 레벨 제한이 높고 기본 능력치가 더 좋다。
PRINTL 레벨 10부터 적 1체에게 반격하는 '응사'를 획득.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2850
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2850
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0