;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;레일 권총. TACTICAL LEGACY용
;======================================================

@기본능력수정_2852,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 8 ;장비 특전
	CASE "속도"
		RETURN 8 ;장비 특전
ENDSELECT
RETURN 0

@전투능력수정_2852,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2852,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

;강화된 저격병용 야전방호복 세트
@장비추가스킬_2852,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
IF ARG:1 != -1
	;1-1번 세트. 강화된 저격병용 야전방호복 + 강화 아머
	;레일 권총 장비시 + 레벨 20
	IF (EQUIP:(ARG:1):몸통 == 3836 && EQUIP:(ARG:1):팔 == 4102 && EQUIP:(ARG:1):발 == 4730)	|| (EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708)	|| (EQUIP:(ARG:1):몸통 == 3814 && EQUIP:(ARG:1):팔 == 4081 && EQUIP:(ARG:1):발 == 4709)	|| (EQUIP:(ARG:1):몸통 == 3809 && EQUIP:(ARG:1):팔 == 4076 && EQUIP:(ARG:1):발 == 4704)
	IF BASE:(ARG:1):LV < 20
		SELECTCASE ARG
			CASE 0
				;날렵한손
				RETURN 166
			CASE 1
				;응사(XCOM)
				RETURN 190
			CASE 2
				;잠복(XCOM)
				RETURN 589
			ENDSELECT
		ENDIF
	IF BASE:(ARG:1):LV > 19 && BASE:(ARG:1):LV < 30
		SELECTCASE ARG
			CASE 0
				;날렵한손
				RETURN 166
			CASE 1
				;응사(XCOM)
				RETURN 190
			CASE 2
				;잠복(XCOM)
				RETURN 589
			ENDSELECT
		ENDIF
	;1-1번 세트. 강화된 저격병용 야전방호복 세트
	;전자기 권총 장비시 + 레벨 30
	IF BASE:(ARG:1):LV > 29
		SELECTCASE ARG
			CASE 0
				;날렵한손
				RETURN 166
			CASE 1
				;응사(XCOM)
				RETURN 190
			CASE 2
				;대결
				RETURN 167
			CASE 3
				;데드샷
				RETURN 2497
			CASE 4
				;잠복(XCOM)
				RETURN 589
			ENDSELECT
		ENDIF
	ENDIF
ENDIF
RETURN 0

@공격상성_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 2
IF BASE:ARG:LV >= 25
	RETURN 2
ELSE
	RETURN 1
ENDIF

@최대공격횟수_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 2
IF BASE:ARG:LV >= 25
	RETURN 2
ELSE
	RETURN 1
ENDIF

@사거리_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2852
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 30
	RETURN 60
ELSEIF BASE:ARG:LV >= 28
	RETURN 59
ELSEIF BASE:ARG:LV >= 26
	RETURN 58
ELSEIF BASE:ARG:LV >= 24
	RETURN 57
ELSEIF BASE:ARG:LV >= 22
	RETURN 56
ELSEIF BASE:ARG:LV >= 30
	RETURN 55
ELSEIF BASE:ARG:LV >= 19
	RETURN 53
ELSEIF BASE:ARG:LV >= 18
	RETURN 51
ELSEIF BASE:ARG:LV >= 17
	RETURN 49
ELSEIF BASE:ARG:LV >= 16
	RETURN 47
ELSEIF BASE:ARG:LV >= 15
	RETURN 45
ELSE
	RETURN 45
ENDIF

@총명중_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 75
IF BASE:ARG:LV >= 30
	RETURN 75
ELSEIF BASE:ARG:LV >= 28
	RETURN 74
ELSEIF BASE:ARG:LV >= 26
	RETURN 73
ELSEIF BASE:ARG:LV >= 24
	RETURN 72
ELSEIF BASE:ARG:LV >= 22
	RETURN 71
ELSEIF BASE:ARG:LV >= 20
	RETURN 70
ELSEIF BASE:ARG:LV >= 19
	RETURN 66
ELSEIF BASE:ARG:LV >= 17
	RETURN 64
ELSEIF BASE:ARG:LV >= 16
	RETURN 62
ELSEIF BASE:ARG:LV >= 15
	RETURN 60
ELSE
	RETURN 60
ENDIF

@특수탄창_2852, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

@총타입_2852
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 1

@速度보정_2852
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 9
;핸드건 8 샷 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2852
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 18

@장비조건_2852,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 18
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2852
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV18～

@장비해설_2852
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 : WAR OF THE CHOSEN - TACTICAL LEGACY PACK 특전 장비。
PRINTL 　　　　　　　　전자기 권총과 큰 차이는 없으나 레벨 제한이 높고 기본 능력치가 더 좋다。
PRINTL EXO 아머슈트, 포식자 방어구, 강화된 저격병용 야전 방호복 세트, 스파이더 슈트 세트를 장비시 레벨에 따라 추가 특성 획득.
PRINTL 레벨 19:응사(자신을 공격한 적에게 즉시 반격)
PRINTL 레벨 20:날렵한손(적 1체 공격. 빠른 발동+높은 크리티컬률)
PRINTL 레벨 25:퀵드로(최저, 최대 공격횟수 +1)
PRINTL 레벨 30:대결, 데드샷(적 전체에 1회씩 사격, 치명타율 상승)

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2852
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2852
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0