;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;재래식저격총
;======================================================

@기본능력수정_2805,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 4
	CASE "속도"
		RETURN 3
ENDSELECT
RETURN 0

@전투능력수정_2805,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2805,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@장비추가스킬_2805,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
	CASE 0
		;잠복(XCOM)
		RETURN 589
ENDSELECT
RETURN 0

@공격상성_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 3
IF BASE:ARG:LV >= 10
	RETURN 3
ELSE
	RETURN 2
ENDIF

@공격범위_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2805
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 15
	RETURN 60
ELSEIF BASE:ARG:LV >= 14
	RETURN 58
ELSEIF BASE:ARG:LV >= 13
	RETURN 56
ELSEIF BASE:ARG:LV >= 12
	RETURN 54
ELSEIF BASE:ARG:LV >= 11
	RETURN 52
ELSEIF BASE:ARG:LV >= 10
	RETURN 50
ELSEIF BASE:ARG:LV >= 9
	RETURN 48
ELSEIF BASE:ARG:LV >= 8
	RETURN 46
ELSEIF BASE:ARG:LV >= 7
	RETURN 44
ELSEIF BASE:ARG:LV >= 6
	RETURN 42
ELSEIF BASE:ARG:LV >= 5
	RETURN 40
ELSE
	RETURN 40
ENDIF

@총명중_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 15
	RETURN 60
ELSEIF BASE:ARG:LV >= 14
	RETURN 59
ELSEIF BASE:ARG:LV >= 13
	RETURN 58
ELSEIF BASE:ARG:LV >= 12
	RETURN 57
ELSEIF BASE:ARG:LV >= 11
	RETURN 56
ELSEIF BASE:ARG:LV >= 10
	RETURN 55
ELSEIF BASE:ARG:LV >= 9
	RETURN 54
ELSEIF BASE:ARG:LV >= 8
	RETURN 53
ELSEIF BASE:ARG:LV >= 7
	RETURN 52
ELSEIF BASE:ARG:LV >= 6
	RETURN 51
ELSEIF BASE:ARG:LV >= 5
	RETURN 50
ELSE
	RETURN 50
ENDIF

@특수탄창_2805, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4

@총타입_2805
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 4

@速度보정_2805
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN -4
;핸드건 8 샷건 -1 라이플 0 저격라이플 -6 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2805
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

@장비조건_2805,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 5
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2805
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV5～

@장비해설_2805
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM에서 사용하는 통상적인 저격병의 저격 무기. 특별한 능력은 없다.
PRINTL 　　　　　　　　레벨 15까지 무기 위력과 명중이 소폭 증가한다.
PRINTL 레벨 10에 저격수 저격병 특성 '원거리경계'에 의해 공격 사거리가 L로 증가.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2805
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2805
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0