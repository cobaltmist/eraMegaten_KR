;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;대구경권총
;======================================================

@기본능력수정_2802,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 4
	CASE "속도"
		RETURN 4
ENDSELECT
RETURN 0

@전투능력수정_2802,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 0

@방어상성_2802,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@장비추가스킬_2802,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
IF ARG:1 != -1
	;1-1번 세트. 레벨 9 이상시 잠복 + 응사
	IF BASE:(ARG:1):LV > 9
		SELECTCASE ARG
			CASE 0
				;잠복(XCOM)
				RETURN 589
			CASE 1
				;응사(XCOM)
				RETURN 190
		ENDSELECT
	ELSE
		SELECTCASE ARG
			CASE 0
				;잠복(XCOM)
				RETURN 589
		ENDSELECT
	ENDIF
ENDIF
RETURN 0

@공격상성_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비箇所_2802
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 30
IF BASE:ARG:LV >= 15
	RETURN 30
ELSEIF BASE:ARG:LV >= 14
	RETURN 29
ELSEIF BASE:ARG:LV >= 13
	RETURN 28
ELSEIF BASE:ARG:LV >= 12
	RETURN 27
ELSEIF BASE:ARG:LV >= 11
	RETURN 26
ELSEIF BASE:ARG:LV >= 10
	RETURN 25
ELSEIF BASE:ARG:LV >= 9
	RETURN 24
ELSEIF BASE:ARG:LV >= 8
	RETURN 23
ELSEIF BASE:ARG:LV >= 7
	RETURN 22
ELSEIF BASE:ARG:LV >= 6
	RETURN 21
ELSEIF BASE:ARG:LV >= 5
	RETURN 20
ELSE
	RETURN 20
ENDIF

@총명중_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 40
IF BASE:ARG:LV >= 15
	RETURN 40
ELSEIF BASE:ARG:LV >= 14
	RETURN 39
ELSEIF BASE:ARG:LV >= 13
	RETURN 38
ELSEIF BASE:ARG:LV >= 12
	RETURN 37
ELSEIF BASE:ARG:LV >= 11
	RETURN 36
ELSEIF BASE:ARG:LV >= 10
	RETURN 35
ELSEIF BASE:ARG:LV >= 9
	RETURN 34
ELSEIF BASE:ARG:LV >= 8
	RETURN 33
ELSEIF BASE:ARG:LV >= 7
	RETURN 32
ELSEIF BASE:ARG:LV >= 6
	RETURN 31
ELSEIF BASE:ARG:LV >= 5
	RETURN 30
ELSE
	RETURN 30
ENDIF

@특수탄창_2802, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@총타입_2802
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어썰트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 1

@速度보정_2802
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 8
;핸드건 8 샷 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2802
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

@장비조건_2802,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 5
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2802
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV5～

@장비해설_2802
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM에서 사용하는 통상적인 재래식 권총. 특별한 능력은 없다。
PRINTL 　　　　　　　　레벨 15까지 무기 위력과 명중이 소폭 증가한다。
PRINTL 레벨 10부터 적 1체에게 반격하는 '응사'를 획득.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2802
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2802
RETURN 0