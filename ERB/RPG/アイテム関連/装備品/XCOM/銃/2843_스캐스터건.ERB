;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;스캐스터 건. TACTICAL LEGACY용
;======================================================

@기본능력수정_2843,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜" ;능력치 1.5배
		RETURN 6
	CASE "속도" ;능력치 1.5배
		RETURN 6
	CASE "운" ;레이저 조준기
		RETURN 2
ENDSELECT
RETURN 0

@전투능력수정_2843,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
;버그픽스용
SIF (ARG:1) == -1
	ARG:1 = 0
SELECTCASE GET_BATTLESTATUS(ARG)
	CASE "명중"
		;블레이드 마스터 효과
		IF EQUIP:(ARG:1):검 == 2254 && EQUIP:(ARG:1):몸통 == 3829 && EQUIP:(ARG:1):팔 == 4095 && EQUIP:(ARG:1):발 == 4723 && BASE:(ARG:1):LV > 9
			RETURN 10
		ELSE
			RETURN 0
		ENDIF
ENDSELECT
RETURN 0

@방어상성_2843,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@공격상성_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

@공격범위_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2843
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 90
IF BASE:ARG:LV >= 15
	RETURN 90
ELSEIF BASE:ARG:LV >= 14
	RETURN 86
ELSEIF BASE:ARG:LV >= 13
	RETURN 82
ELSEIF BASE:ARG:LV >= 12
	RETURN 78
ELSEIF BASE:ARG:LV >= 11
	RETURN 74
ELSEIF BASE:ARG:LV >= 10
	RETURN 70
ELSEIF BASE:ARG:LV >= 9
	RETURN 68
ELSEIF BASE:ARG:LV >= 8
	RETURN 66
ELSEIF BASE:ARG:LV >= 7
	RETURN 62
ELSEIF BASE:ARG:LV >= 6
	RETURN 62
ELSEIF BASE:ARG:LV >= 5
	RETURN 60
ELSE
	RETURN 60
ENDIF

@총명중_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 60
IF BASE:ARG:LV >= 15
	RETURN 60
ELSEIF BASE:ARG:LV >= 14
	RETURN 59
ELSEIF BASE:ARG:LV >= 13
	RETURN 58
ELSEIF BASE:ARG:LV >= 12
	RETURN 57
ELSEIF BASE:ARG:LV >= 11
	RETURN 56
ELSEIF BASE:ARG:LV >= 10
	RETURN 55
ELSEIF BASE:ARG:LV >= 9
	RETURN 53
ELSEIF BASE:ARG:LV >= 8
	RETURN 51
ELSEIF BASE:ARG:LV >= 7
	RETURN 49
ELSEIF BASE:ARG:LV >= 6
	RETURN 47
ELSEIF BASE:ARG:LV >= 5
	RETURN 45
ELSE
	RETURN 45
ENDIF

@특수탄창_2843, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

@총타입_2843
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 2

@速度보정_2843
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;핸드건 8 샷건 -1 라이플 0 저격라이플 -8 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2843
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 8

@장비조건_2843,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 8
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2843
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV8～

@장비해설_2843
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 : WAR OF THE CHOSEN - TACTICAL LEGACY PACK 특전 장비。
PRINTL 　　　　　　　　재래식산탄총과 특별한 차이는 없으나 레벨 제한이 높고 기본 능력치가 더 좋다。
PRINTL 재래식검과 함께 장비시 추가 효과를 얻는다.

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2843
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2843
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0