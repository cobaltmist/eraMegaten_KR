;======================================================
;장비품 데이터. XCOM2 WOTC TLP용
;반물질소총. TACTICAL LEGACY용
;======================================================

@기본능력수정_2851,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
	CASE "지혜"
		RETURN 8 ;장비 특전
	CASE "속도"
		RETURN 10 ;기본 7 + 고급 자동 장전기 (3)
ENDSELECT
RETURN 0

@전투능력수정_2851,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
;버그픽스용
SIF (ARG:1) == -1
	ARG:1 = 0
SELECTCASE GET_BATTLESTATUS(ARG)
	CASE "회피"
		;천상의 레퀴엠 효과. 레벨 25
		IF (EQUIP:(ARG:1):몸통 == 3836 && EQUIP:(ARG:1):팔 == 4102 && EQUIP:(ARG:1):발 == 4730 && BASE:(ARG:1):LV > 24) || (EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708 && BASE:(ARG:1):LV > 24) || (EQUIP:(ARG:1):몸통 == 3814 && EQUIP:(ARG:1):팔 == 4081 && EQUIP:(ARG:1):발 == 4709 && BASE:(ARG:1):LV > 24)
			RETURN 15
		ELSE
			RETURN 0
		ENDIF
	CASE "명중"
		RETURN 20 ;고급 스코프
ENDSELECT
RETURN 0

;레벨 20 저격 포식자방어구 스킬
@장비추가스킬_2851,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
IF ARG:1 != -1
	;1-1번 세트. 레벨 20에 데드아이 획득
	IF (EQUIP:(ARG:1):몸통 == 3836 && EQUIP:(ARG:1):팔 == 4102 && EQUIP:(ARG:1):발 == 4730) || (EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708) || (EQUIP:(ARG:1):몸통 == 3814 && EQUIP:(ARG:1):팔 == 4081 && EQUIP:(ARG:1):발 == 4709)
	IF BASE:(ARG:1):LV < 20 ;20 미만까지
	SELECTCASE ARG
			CASE 0
				;잠복(XCOM)
				RETURN 589
			ENDSELECT
		ENDIF
	;레벨 20에 데드아이 획득
	IF BASE:(ARG:1):LV > 19 && BASE:(ARG:1):LV < 30 ;20~29까지
		SELECTCASE ARG
			CASE 0
				;데드아이
				RETURN 171
			CASE 1
				;잠복(XCOM)
				RETURN 589
			ENDSELECT
		ENDIF
	;레벨 30일시 살상지대도 획득
	IF BASE:(ARG:1):LV > 29
		SELECTCASE ARG
			CASE 0
				;데드아이
				RETURN 171
			CASE 1
				;잠복(XCOM)
				RETURN 589
			CASE 2
				;살상지대
				RETURN 172
			CASE 3
				;데드샷
				RETURN 2497
			ENDSELECT
		ENDIF
	ENDIF
ENDIF
RETURN 0

@방어상성_2851,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@공격상성_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("비구")

@최저공격횟수_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

@공격범위_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비부위_2851
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("총")

@총공격_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 120
IF BASE:ARG:LV >= 30
	RETURN 120
ELSEIF BASE:ARG:LV >= 28
	RETURN 116
ELSEIF BASE:ARG:LV >= 26
	RETURN 112
ELSEIF BASE:ARG:LV >= 24
	RETURN 108
ELSEIF BASE:ARG:LV >= 22
	RETURN 104
ELSEIF BASE:ARG:LV >= 20
	RETURN 100
ELSEIF BASE:ARG:LV >= 19
	RETURN 98
ELSEIF BASE:ARG:LV >= 18
	RETURN 96
ELSEIF BASE:ARG:LV >= 17
	RETURN 94
ELSEIF BASE:ARG:LV >= 16
	RETURN 92
ELSEIF BASE:ARG:LV >= 15
	RETURN 90
ELSE
	RETURN 90
ENDIF

@총명중_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 105
IF BASE:ARG:LV >= 30
	RETURN 105
ELSEIF BASE:ARG:LV >= 28
	RETURN 104
ELSEIF BASE:ARG:LV >= 26
	RETURN 103
ELSEIF BASE:ARG:LV >= 24
	RETURN 102
ELSEIF BASE:ARG:LV >= 22
	RETURN 101
ELSEIF BASE:ARG:LV >= 20
	RETURN 100
ELSEIF BASE:ARG:LV >= 19
	RETURN 98
ELSEIF BASE:ARG:LV >= 18
	RETURN 96
ELSEIF BASE:ARG:LV >= 17
	RETURN 94
ELSEIF BASE:ARG:LV >= 16
	RETURN 92
ELSEIF BASE:ARG:LV >= 15
	RETURN 90
ELSE
	RETURN 90
ENDIF

@특수탄창_2851, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

@총타입_2851
#LOCALSIZE 2
#LOCALSSIZE 2
;1 = 핸드건 2 = 샷건 3 = 어설트라이플、머신건 4 = 저격라이플 5 = 그외
RETURN 4

@速度보정_2851
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0
;핸드건 8 샷건 -1 라이플 0 저격라이플 -6 그외 ? 

;-----------------------------
;- 조건・설명処理
;-----------------------------
@필요레벨_2851
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 18

@장비조건_2851,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF BASE:ARG:LV < 18
	RETURN 0
SIF CSTR:ARG:종족명 == "SPARK"
	RETURN 0
SIF CSTR:ARG:종족명 == "리퍼"
	RETURN 0
SIF CSTR:ARG:종족명 == "스커미셔"
	RETURN 0
SIF CSTR:ARG:종족명 == "템플러"
	RETURN 0
SIF CSTR:ARG:종족명 == "사이오니스트"
	RETURN 0
RETURN 1

@장비조건설명_2851
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 장비조건：LV18～

@장비해설_2851
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 : WAR OF THE CHOSEN - TACTICAL LEGACY PACK 특전 장비。
PRINTL 　　　　　　　　반물질 소총이라는 이름이지만 실상은 조금 강력한 대물 저격총의 한 종류다.
PRINTL 가우스 저격총과 큰 차이는 없으나 레벨 제한이 높고 기본 능력치가 더 좋다。
PRINTL '원거리경계'가 적용되어 있으며 EXO 아머슈트, 강화된 저격병용 야전방호복 세트, 스파이더 슈트를 세트로 장비시 추가 스킬을 획득한다.
PRINTL 레벨 20:데드아이(통상의 약 1.5배 위력으로 사격, 명중 패널티)
PRINTL 레벨 25:천상의레퀴엠(회피 +15)
PRINTL 레벨 30:살상지대, 데드샷(4~7회 랜덤하게 연속사격, 치명타율 상승)

;-----------------------------
;- 구입・引き継ぎ
;-----------------------------
@구입조건_2851
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2851
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0