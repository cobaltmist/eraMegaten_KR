;======================================================
;장비품 데이터
;스턴랜스
;======================================================

@기본능력수정_2236,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE GET_BASESTATUS(ARG)
ENDSELECT
RETURN 0

@전투능력수정_2236,ARG,ARG:1 = 0
#LOCALSIZE 2
#LOCALSSIZE 2
;버그픽스용
SIF (ARG:1) == -1
	ARG:1 = 0
SELECTCASE GET_BATTLESTATUS(ARG)
	CASE "공격"
		SIF ARG == -1
			RETURN 100
		IF BASE:(ARG:1):LV >= 21
			RETURN 100
		ELSEIF BASE:(ARG:1):LV >= 11
			RETURN 70
		ELSEIF BASE:(ARG:1):LV >= 10
			RETURN 40
		ELSE
			RETURN 40
		ENDIF
	CASE "명중"
		RETURN 75
	CASE "방어"
		SIF ARG == -1
			RETURN -30
		IF BASE:(ARG:1):LV >= 21
			RETURN -30
		ELSEIF BASE:(ARG:1):LV >= 11
			RETURN -20
		ELSEIF BASE:(ARG:1):LV >= 10
			RETURN -10
		ELSE
			RETURN -10
		ENDIF
	CASE "회피"
		RETURN -30
ENDSELECT
RETURN 0

@방어상성_2236,ARG,ARG:1 = -1
#LOCALSIZE 2
#LOCALSSIZE 2
SELECTCASE ARG
ENDSELECT
RETURN 100

@공격상성_2236, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("검격")

@최저공격횟수_2236, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@최대공격횟수_2236, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@사거리_2236, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@공격범위_2236, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비箇所_2236
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_EQUIPNUM("검")

;-----------------------------
;- 条件・説明処理
;-----------------------------
@필요레벨_2236
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비조건_2236,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

@장비해설_2236
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 스턴랜서의 무기. 벨런스를 위해 특별히 제작됐다. 스턴랜서 전용

;-----------------------------
;- 購入・引き継ぎ
;-----------------------------
@구입조건_2236
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

@引継ぎ可不可_2236
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0