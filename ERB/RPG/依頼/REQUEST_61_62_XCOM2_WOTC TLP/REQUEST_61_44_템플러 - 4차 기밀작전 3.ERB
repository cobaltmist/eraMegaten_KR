;====================
;설명
;====================
@EXPLAIN_REQUEST_61_44
#LOCALSIZE 2
#LOCALSSIZE 2
	PRINTL
	PRINTFORMW 4차 기밀 작전에서 템플러 조직과 함께한 결과가 도착했다.
	PRINTFORMW 즉시 확인해보자.
	PRINTL
;====================
;내용
;====================
@REQUEST_61_44
#LOCALSIZE 2
#LOCALSSIZE 2
	LOCAL = FINDCHARA_ID(STRFLAG_NUM_REQ("조사奴隷"))
	PRINTFORMW %조사처리(CALLNAME:LOCAL,"가")% 돌아왔다.
	PRINTL
	SETCOLOR 255, 50, 255
	PRINTFORMW 가이스트:우린 함께 우리의 임무에 성공했소.
	RESETCOLOR
	PRINTL
	CFLAG:LOCAL:이자리에없는플래그 = 0
	PRINTFORML 기밀 작전은 성공적으로 완료했다.
	IF TALENT:LOCAL:비전투원 == 1
		TALENT:LOCAL:비전투원 = 0
		PRINTFORML %조사처리(CALLNAME:LOCAL,"는")% 훈련을 아주 빡쎄게 받은 덕분에 전투에 어느정도 소양이 생겼다.
		PRINTFORML 이제부터는 아마 한 사람몫을 할 수 있을 것이다.
		PRINTFORMW 그리고……
	ENDIF

	CALL LEVEL_UP_XCOM, MASTER, 5 ;마스터
	PRINTFORMW %CALLNAME:MASTER%의 레벨이 5 올랐다.

	CALL LEVEL_UP_XCOM, LOCAL, 5 ;파견자
	PRINTFORMW %CALLNAME:LOCAL%의 레벨이 5 올랐다.

	PRINTFORML -보수로 %CALLNAME:MASTER%, %CALLNAME:LOCAL%의 서머너 레벨과 도구지식 레벨을 3 올릴 수 있다.
	PRINTFORMW %CALLNAME:MASTER%의 [서머너] 레벨을 3 올립니까? (상한 5)
	PRINTL [0] 올린다
	PRINTL [1] 올리지 않는다
		$INPUT_LOOP1
		INPUT
		IF RESULT == 0
			IF TALENT:MASTER:서머너 == 0
				TALENT:MASTER:서머너 = 3
			ELSEIF TALENT:MASTER:서머너 == 1
				TALENT:MASTER:서머너 = 4	
			ELSEIF TALENT:MASTER:서머너 == 2
				TALENT:MASTER:서머너 = 5
			ELSEIF TALENT:MASTER:서머너 == 3
				TALENT:MASTER:서머너 = 5
			ELSEIF TALENT:MASTER:서머너 == 4
				TALENT:MASTER:서머너 = 5
			ELSEIF TALENT:MASTER:서머너 == 5
				TALENT:MASTER:서머너 = 5
			ENDIF
			PRINTFORMW %CALLNAME:MASTER%의 [서머너] 레벨이 3 올랐다. (상한 5)
		ELSEIF RESULT == 1
			PRINTFORMW %CALLNAME:MASTER%의 [서머너] 레벨은 오르지 않았다.
		ENDIF
	PRINTL
	PRINTFORMW %CALLNAME:MASTER%의 [도구지식] 레벨을 3 올립니까? (상한 5)
	PRINTL [0] 올린다
	PRINTL [1] 올리지 않는다
		$INPUT_LOOP2
		INPUT
		IF RESULT == 0
			IF TALENT:MASTER:도구지식 == 0
				TALENT:MASTER:도구지식 = 3
			ELSEIF TALENT:MASTER:도구지식 == 1
				TALENT:MASTER:도구지식 = 4	
			ELSEIF TALENT:MASTER:도구지식 == 2
				TALENT:MASTER:도구지식 = 5
			ELSEIF TALENT:MASTER:도구지식 == 3
				TALENT:MASTER:도구지식 = 5
			ELSEIF TALENT:MASTER:도구지식 == 4
				TALENT:MASTER:도구지식 = 5
			ELSEIF TALENT:MASTER:도구지식 == 5
				TALENT:MASTER:도구지식 = 5
			ENDIF
			PRINTFORMW %CALLNAME:MASTER%의 [도구지식] 레벨이 3 올랐다. (상한 5)
		ELSEIF RESULT == 1
			PRINTFORMW %CALLNAME:MASTER%의 [도구지식] 레벨은 오르지 않았다.
		ENDIF
	PRINTL
	PRINTFORMW %CALLNAME:LOCAL%의 [서머너] 레벨을 3 올립니까? (상한 5)
	PRINTL [0] 올린다
	PRINTL [1] 올리지 않는다
		$INPUT_LOOP3
		INPUT
		IF RESULT == 0
			IF TALENT:LOCAL:서머너 == 0
				TALENT:LOCAL:서머너 = 3
			ELSEIF TALENT:LOCAL:서머너 == 1
				TALENT:LOCAL:서머너 = 4	
			ELSEIF TALENT:LOCAL:서머너 == 2
				TALENT:LOCAL:서머너 = 5
			ELSEIF TALENT:LOCAL:서머너 == 3
				TALENT:LOCAL:서머너 = 5
			ELSEIF TALENT:LOCAL:서머너 == 4
				TALENT:LOCAL:서머너 = 5
			ELSEIF TALENT:LOCAL:서머너 == 5
				TALENT:LOCAL:서머너 = 5
			ENDIF
			PRINTFORMW %CALLNAME:LOCAL%의 [서머너] 레벨이 3 올랐다. (상한 5)
		ELSEIF RESULT == 1
			PRINTFORMW %CALLNAME:LOCAL%의 [서머너] 레벨은 오르지 않았다.
		ENDIF
	PRINTL
	PRINTFORMW %CALLNAME:LOCAL%의 [도구지식] 레벨을 3 올립니까? (상한 5)
	PRINTL [0] 올린다
	PRINTL [1] 올리지 않는다
		$INPUT_LOOP4
		INPUT
		IF RESULT == 0
			IF TALENT:LOCAL:도구지식 == 0
				TALENT:LOCAL:도구지식 = 3
			ELSEIF TALENT:LOCAL:도구지식 == 1
				TALENT:LOCAL:도구지식 = 4	
			ELSEIF TALENT:LOCAL:도구지식 == 2
				TALENT:LOCAL:도구지식 = 5
			ELSEIF TALENT:LOCAL:도구지식 == 3
				TALENT:LOCAL:도구지식 = 5
			ELSEIF TALENT:LOCAL:도구지식 == 4
				TALENT:LOCAL:도구지식 = 5
			ELSEIF TALENT:LOCAL:도구지식 == 5
				TALENT:LOCAL:도구지식 = 5
			ENDIF
			PRINTFORMW %CALLNAME:LOCAL%의 [도구지식] 레벨이 3 올랐다. (상한 5)
		ELSEIF RESULT == 1
			PRINTFORMW %CALLNAME:LOCAL%의 [도구지식] 레벨은 오르지 않았다.
		ENDIF
	PRINTL
	PRINTFORMW 다음 기밀 작전을 검토해보자.
	의뢰플래그:61:0 = 35
	CALL FLAG_RESET,61,3