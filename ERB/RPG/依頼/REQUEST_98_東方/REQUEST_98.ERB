

@PLAYABLE_REQUEST_98
SIF GETBIT(FLAG:EXTRA이벤트설정, 0) == 0 || GETBIT(FLAG:EXTRA이벤트필터, 6) == 1
	RETURN 0
SIF 의뢰플래그:98:0 != 0 
	RETURN 0
IF FLAG:동방의뢰진행도 == 0
	RESULTS = (동방MOD) 신종 악마의 토벌
	RETURN 1
ELSEIF FLAG:동방의뢰진행도 == 1
	RESULTS = (동방MOD) 수수께끼의 악령
	RETURN 1
ELSEIF FLAG:동방의뢰진행도 == 2
	RESULTS = (동방MOD) 비신의 구인？
	RETURN 1
ENDIF

@EXPLAIN_REQUEST_98
IF FLAG:동방의뢰진행도 == 0
	CALL EXPLAIN_REQUEST_98_01
ELSEIF FLAG:동방의뢰진행도 == 1
	CALL EXPLAIN_REQUEST_98_02
ELSEIF FLAG:동방의뢰진행도 == 2
	CALL EXPLAIN_REQUEST_98_03
ENDIF

@REQUEST_98
IF FLAG:동방의뢰진행도 == 0
	CALL REQUEST_98_01
ELSEIF FLAG:동방의뢰진행도 == 1
	CALL REQUEST_98_02
ELSEIF FLAG:동방의뢰진행도 == 2
	CALL REQUEST_98_03
ENDIF

