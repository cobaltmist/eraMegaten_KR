;====================================
;등록済みの악마をリスト化するための関数です
;레벨順リストは기본的に特殊종족と인간を표시しません。
;자코악마等を選択したりする用です。
;====================================



@SHOPCOMABLE_189
[IF_DEBUG]
	RESULTS = 스킬일람2
	RETURN 1
[ENDIF]
IF FLAG:DEBUG
	RESULTS = 스킬일람2
	RETURN 1
ENDIF
RETURN -1

@SHOP_COM_189
PRINTL
CLEARLINE LINECOUNT
PRINTFORML ;-------------------------------
PRINTFORML ;%GAMEBASE_TITLE% {GAMEBASE_VERSION/1000},{GAMEBASE_VERSION % 1000}
GETTIME
PRINTFORML ;%RESULTS%
PRINTFORML ;-------------------------------
PRINTFORML ;-------------------------------
PRINTFORML ;この資料は資料フォルダ内にある
PRINTFORML ;SORT_SKILL_LIST.ERBを利用して作成されています
PRINTFORML ;何か追加する際はコピペするのではなく、
PRINTFORML ;SORT_SKILL_LIST.ERBを利用して出力してください
PRINTFORML ;-------------------------------
FOR LOCAL, 0 ,3000
	SIF LOCAL == 2303
		GOTO SUMMON
	TRYCCALLFORM SKILL_NAME_{LOCAL}
		$SUMMON
		IF LOCAL == 2303
			PRINTFORM 동료마소환,
		ELSE
			PRINTFORM %RESULTS%,
		ENDIF
		SELECTCASE LOCAL
			;罪아군の専用마법シリーズは타겟いねーと落ちるので
			CASE 1520, 1620, 1720 ,1923, 2025
				PRINTFORM 특수,
			CASEELSE
				RESULT = -1
				TRYCALLFORM SKILL_TYPE_{LOCAL}
				IF RESULT == -1
					PRINTFORM 특수,
				ELSE
					PRINTFORM %GET_TYPE(RESULT)%,
				ENDIF
		ENDSELECT
		TRYCALLFORM SKILL_RANK_{LOCAL}		
		PRINTFORM {RESULT},
		RESULT = 0
		TRYCALLFORM SKILL_HITRATE_{LOCAL}
		PRINTFORM {RESULT},
		RESULT = 0
		TRYCALLFORM SKILL_POWER_{LOCAL}
		PRINTFORM {RESULT},
		RESULT = 0
		TRYCALLFORM SKILL_COSTTYPE_{LOCAL}
		LOCAL:1 = RESULT
		RESULT = 0
		TRYCALLFORM SKILL_COST_{LOCAL}
		PRINTFORM \@ LOCAL:1 == 2 ? {RESULT,3} # {RESULT,3} \@,
		TRYCALLFORM SKILL_RANGE_{LOCAL}
		PRINTFORM {RESULT},
		RESULT = 0
		TRYCALLFORM SKILL_SPHERE_{LOCAL}
		SELECTCASE RESULT
			CASE 1
				PRINTFORM １체,
			CASE 2
				PRINTFORM 열,
			CASE 3
				PRINTFORM 전체,
			CASE 4
				PRINTFORM 적아군,
			CASEELSE
				PRINT 없음,
		ENDSELECT
		PRINTL
	CATCH
	ENDCATCH
NEXT
WAIT
