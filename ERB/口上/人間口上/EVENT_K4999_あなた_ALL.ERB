;-------------------------------------------------
;당신구상（캐릭터の이름を入れる、下にコンセプトなど一言あれば入れる）
;
;
;-------------------------------------------------

;-------------------------------------------------
;eraMegaten専用구상テンプレート
;X1とX2をキャラ번호に置換し、Y1を조수のキャラ번호に置換してください
;ただし、キャラ번호が一桁のキャラの場合、X1,Y1は1、X2は01のように変えてください
;そしてコマンド内の適当な조건の下のPRINTFORMWの後ろに半角スペースを置いてセリフを추가してください
;구상を추가しないコマンドは無記入でも構いませんが、CFLAG:XXXの머리に『;』をつけてコメントアウトすると尚良いでしょう
;エ키스トラ構아야など、コメントアウトされている構아야を使う際には、『;』を外してコメントアウトを해제する필요があります
;最後にread meにも눈を通しておくことをお勧めします
;-------------------------------------------------

;-------------------------------------------------
;汎用구상の作成に사용する場合は、4999→キャラ번호ではなく、
;K4999→PUB회화타입번호　へと変換してご사용ください
;-------------------------------------------------
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:EVENT_K4999_ASSI.ERB
;	Facility	:회화타입：조수時구상
;
;	Modification Data:
;	Edit	Date			Author					Reason
;	2.00    20180220        名無し                  구상テンプレ2.2をベースに、당신구상２種をマージし、加筆
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;ライセンスについて
;	・기본항목(허가する항목を"○"に、허가しない항목を"×"に변경してください。)
;	+----+----+-------------------------------+
;	|번호|허가|ライセンス内容                 |
;	+----+----+-------------------------------+
;	|   1| ○ | 処理上のバグ수정              |
;	+----+----+-------------------------------+
;	|   2| ○ | 구상の誤字・脱字の수정        |
;	+----+----+-------------------------------+
;	|   3| ○ | 口上まとめへの収録             |
;	+----+----+-------------------------------+
;	|   4| ○ | 口上の新規追加                 |
;	+----+----+-------------------------------+
;	|   5| ○ | 既存口上の改変                |
;	+----+----+-------------------------------+
;	|   6| ○ | 改変した口上の自由な再配布   |
;	+----+----+-------------------------------+
;	|   7| ○ | 口上内容の転載                |
;	+----+----+-------------------------------+

;--------------------------------------------------
;구상셀렉트기능：구상설명
;--------------------------------------------------
;この関数のコメントアウトを外すと、구상の選択時に選択肢として候補にあがります
;既に구상のあるキャラや회화타입の別版の구상を作成する時に이용してください
;その際、この関数を含めたこのファイル内の関数名すべてに_任意の1-49(汎用구상なら1-9)の数字を加えてください
;例： EVENTTRAIN_K4999 → EVENTTRAIN_K4999_12
;--------------------------------------------------
;@셀렉트구상설명_K4999,ARG = 0
;SIF ARG == 1
;	RETURN 0
;;구상名
;PRINTFORML ここに구상の명칭を記述してください
;;구상설명문
;PRINTFORML    ここに구상の簡가운な설명や注意点などを記述してください

;--------------------------------------------------
;EVENTTRAIN関係（4999をキャラ번호に置換）
;조교開始時のセリフ CFLAG 201～219を사용
;--------------------------------------------------
@EVENTTRAIN_K4999
;--------------------------------------------------
;엑스트라모드構아야　CFLAG:220を사용
;はいを選択した時に初조교時に구상설정用の소질に변경できます
;下記に처녀化する場合の構아야を샘플として載せておきます
;--------------------------------------------------
;IF CFLAG:220 == 0
;	PRINTL 처녀モードに移行しますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	$INPUT_LOOP
;	INPUT
;	IF RESULT == 0
;		PRINTFORMW ―처녀モードに소질、능력を변경します―
;		TALENT:TARGET:0 = 1
;		ABL:TARGET:4 = 0
;		EXP:TARGET:0 = 0
;	ELSEIF RESULT == 1
;		PRINTFORMW ―통상モードで조교を開始します―
;	ELSE
;		GOTO INPUT_LOOP
;	ENDIF
;	CFLAG:220 = 1
;ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;簡易조수구상の発動時に初조교時구상をスキップ（Y1を조수のキャラ번호にする）
;複数人いる際には、SIF NO:ASSI == Y1 || NO:ASSI == Z1と추가していく
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교開始時（一回のみ） CFLAG201
;-------------------------------------------------
DRAWLINE
;初조교時
IF CFLAG:201 == 0 && TALENT:연모 == 0
	CFLAG:201 = 1
	RETURN 0
;굴복각인Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	PRINTFORMW 
	CFLAG:201 = 2
	RETURN 0
;굴복각인Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	PRINTFORMW 
	CFLAG:201 = 3
	RETURN 0
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 
	CFLAG:201 = 4
	RETURN 0
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모
	IF TALENT:3
		IF CFLAG:미조교함락 == 1
			;조교せずに연모をつけた場合
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	;음란
	ELSEIF TALENT:4
		PRINTFORMW 
	;복종
	ELSEIF TALENT:5
		PRINTFORMW 
	ENDIF
	CFLAG:201 = 5
	RETURN 0
ENDIF

;-------------------------------------------------
;簡易조수구상（Y1を조수のキャラ번호に置換、4999をキャラ번호に변경する) CFLAG203
;掛け合いをするキャラを増やす場合は、構아야をコピー＆ペーストをして、CFLAGの数を204、205と増やす
;-------------------------------------------------
;조수の有無を체크（いない場合は二回目以降へ飛ぶ）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K4999
;조수
;ELSEIF NO:ASSI == Y1
	;初めて
;	IF CFLAG:203 == 0
		;함락済み＋함락時初구상が発生済み
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			PRINTFORMW 
		;それ以外
;		ELSE
;			PRINTFORMW 
;		ENDIF
;		CFLAG:203 = 1
	;二回目以降
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락済み
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			PRINTFORMW 
		;それ以外
;		ELSE
;			PRINTFORMW 
;		ENDIF
;		CFLAG:203 = 2
;	ENDIF
;구상のある조수が居ない場合は、통상の二回目以降の구상へ飛ぶ
ELSE
	CALL EVENTTRAIN_SECOND_K4999
ENDIF

;-------------------------------------------------
;조교開始時（2回目以降、4999をキャラ번호に변경する）
;-------------------------------------------------
@EVENTTRAIN_SECOND_K4999
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질無し
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORML 
	CFLAG:202 = 4
;함락済み
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모、친애
	IF TALENT:3 || TALENT:6
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		IF RAND:3 == 0
			LOCALS:0 = 
		ELSEIF RAND:2 == 0
			LOCALS:0 = 
		ELSE
			LOCALS:0 = 
		ENDIF
	;음란、창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		IF RAND:3 == 0
			LOCALS:0 = 
		ELSEIF RAND:2 == 0
			LOCALS:0 = 
		ELSE
			LOCALS:0 = 
		ENDIF
	;복종、예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		IF RAND:3 == 0
			LOCALS:0 = 
		ELSEIF RAND:2 == 0
			LOCALS:0 = 
		ELSE
			LOCALS:0 = 
		ENDIF
	ENDIF
	;그외、共通
	IF LOCALS:0 == ""
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		IF RAND:3 == 0
			PRINTFORML 
		ELSEIF RAND:2 == 0
			PRINTFORML 
		ELSE
			PRINTFORML 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF
 
FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM 
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:COUNT == 0
	PRINTW
RETURN 0

;-------------------------------------------------
;EVENTEND関係（4999をキャラ번호に置換）
;조교종료시のセリフ
;-------------------------------------------------
@EVENTEND_K4999
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K4999
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교종료시のセリフ
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:217 = 1
;굴복각인Lv1以下
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	PRINTFORMW 
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질無し
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 
	CFLAG:219 = 3
;함락済み＋체력500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모、친애
	IF TALENT:3 || TALENT:6
		PRINTFORMW 
	;음란、창부
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORMW 
	;복종、예속
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:218 = 1
;함락済み
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모、친애
	IF TALENT:3 || TALENT:6
		PRINTFORMW 
	;음란、창부
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORMW 
	;복종、예속
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:219 = 4
ENDIF
RETURN 0

;--------------------------------------------------
;@KOJO_MESSAGE_COM関係（4999をキャラ번호に置換）
;コマンド実行時に출력されます
;--------------------------------------------------
@KOJO_MESSAGE_COM_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;LOCALS
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;-------------------------------------------------
;コマンド実行時のセリフ CFLAG 300～500
;-------------------------------------------------
SELECTCASE SELECTCOM
;=================================================
;애무계コマンド
;=================================================
;-------------------------------------------------
;0,애무
;-------------------------------------------------
	CASE 0
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:300 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:300 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:300 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:300 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:300 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:300 = 3
				ENDIF
				;それ以外
				IF (CFLAG:300 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:300 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
	CASE 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:301 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:301 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:301 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:301 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:301 = 3
			ENDIF
			;それ以外
			IF (CFLAG:301 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;2,펠라한다
;-------------------------------------------------
	CASE 2
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:302 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:302 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:302 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF TALENT:MASTER:4 || TALENT:MASTER:7
					IF TALENT:짐승
						IF RAND:2 == 0
							PRINTFORMW 인간とは違う形状の페니스を嬉しそうに頬張った…
						ELSE
							PRINTFORMW 눈の前に差し出された、雄臭さと짐승臭さに溢れる페니스を舌先から舐り始めた…
						ENDIF
					ELSE
						IF RAND:2 == 0
							PRINTFORMW 上눈遣いで%CALLNAME:TARGET%を見つめ、視깨달음的にも性欲を刺激していく…
						ELSE
							PRINTFORMW 눈の前に差し出された、雄臭さと짐승臭さに溢れる페니스を舌先から舐り始めた…
						ENDIF
					ENDIF
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:302 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:302 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:302 = 3
			ENDIF
			;それ以外
			IF (CFLAG:302 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:302 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;3,손가락삽입
;-------------------------------------------------
	CASE 3
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:303 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:303 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:303 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:303 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:303 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:303 = 3
			ENDIF
			;それ以外
			IF (CFLAG:303 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:303 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
	CASE 4
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:304 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:304 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:304 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:304 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:304 = 3
			ENDIF
			;それ以外
			IF (CFLAG:304 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,애널애무
;-------------------------------------------------
	CASE 5
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:305 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:305 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:305 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:305 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:305 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:305 = 3
				ENDIF
				;それ以外
				IF (CFLAG:305 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:305 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;6,가슴주무르기
;-------------------------------------------------
	CASE 6
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:306 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:306 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:306 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:306 = 3
				ENDIF
				;それ以外
				IF (CFLAG:306 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:306 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:306 == 1
				CFLAG:306 = 0
			K = 306
			GOTO 가슴系共通
		ENDIF
;-------------------------------------------------
;7,유두자극
;-------------------------------------------------
	CASE 7
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:307 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:307 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:307 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:307 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:307 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:307 = 3
				ENDIF
				;それ以外
				IF (CFLAG:307 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:307 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:307 == 1
				CFLAG:307 = 0
			K = 307
			GOTO 가슴系共通
		ENDIF
;-------------------------------------------------
;8,가슴애무
;-------------------------------------------------
	CASE 8
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:308 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:308 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:308 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:308 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:308 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:308 = 3
				ENDIF
				;それ以外
				IF (CFLAG:308 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:308 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:308 == 1
				CFLAG:308 = 0
			K = 308
			GOTO 가슴系共通
		ENDIF
;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
	CASE 9
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:309 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:309 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:309 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:309 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:309 = 3
			ENDIF
			;それ以外
			IF (CFLAG:309 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:309 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;10,가슴자위
;-------------------------------------------------
	CASE 10
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:310 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:310 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:310 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:310 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:310 = 3
			ENDIF
			;それ以外
			IF (CFLAG:310 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:310 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;11,자위
;-------------------------------------------------
	CASE 11
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:311 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:311 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:311 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:311 = 4
			ENDIF
			;순종Lv3이상
			IF ABL:0 >= 3 && (CFLAG:311 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:311 = 3
			ENDIF
			;それ以外
			IF (CFLAG:311 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:311 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,날개애무
;-------------------------------------------------
	CASE 12
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:435 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:435 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:435 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:435 = 4
				ENDIF
				;순종Lv3이상
				IF ABL:0 >= 3 && (CFLAG:435 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:435 = 3
				ENDIF
				;それ以外
				IF (CFLAG:435 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:435 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;13,꼬리애무
;-------------------------------------------------
	CASE 13
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:436 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:436 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:436 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:436 = 4
				ENDIF
				;순종Lv3이상
				IF ABL:0 >= 3 && (CFLAG:436 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:436 = 3
				ENDIF
				;それ以外
				IF (CFLAG:436 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:436 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;14,클리애무
;-------------------------------------------------
	CASE 14
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:442 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:442 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:442 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:442 = 4
			ENDIF
			;순종Lv3이상
			IF ABL:0 >= 3 && (CFLAG:442 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:442 = 3
			ENDIF
			;それ以外
			IF (CFLAG:442 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:442 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,엉덩이애무
;-------------------------------------------------
	CASE 15
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:444 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:444 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:444 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:444 = 4
				ENDIF
				;순종Lv3이상
				IF ABL:0 >= 3 && (CFLAG:444 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:444 = 3
				ENDIF
				;それ以外
				IF (CFLAG:444 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:444 = 2
				ENDIF
			ENDIF
		;촉수あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
;-------------------------------------------------
;가슴系共通
;6가슴주무르기　7유두자극　8가슴애무
;-------------------------------------------------
	CASE 11111
		$가슴系共通
		;初めて
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;それ以外
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;=================================================
;コミュニケーション系コマンド
;=================================================
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
	CASE 20
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:312 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:312 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:312 = 4
			ENDIF
			;순종Lv3이상
			IF ABL:0 >= 3 && (CFLAG:312 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:312 = 3
			ENDIF
			;それ以外
			IF (CFLAG:312 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:312 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;21,아무것도안한다
;-------------------------------------------------
	CASE 21
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:313 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:313 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:313 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:313 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:313 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:313 = 3
			ENDIF
			;それ以外
			IF (CFLAG:313 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:313 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;22,회화　연모、친애のみ
;-------------------------------------------------
	CASE 22
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:314 == 0
			PRINTFORML 
			CFLAG:314 = 1
		;二回目以降
		ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:314 = 2
		ENDIF
;-------------------------------------------------
;23,프레젠트　연모、친애のみ
;-------------------------------------------------
	CASE 23
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:315 == 0
			PRINTFORML 
			CFLAG:315 = 1
		;二回目以降
		ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:315 = 2
		ENDIF
;-------------------------------------------------
;24,스킨쉽　연모、친애のみ
;-------------------------------------------------
	CASE 24
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:316 == 0
			PRINTFORML 
			CFLAG:316 = 1
		;二回目以降
		ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:316 = 2
		ENDIF
;-------------------------------------------------
;25,느긋하게있는다　연모、친애のみ
;-------------------------------------------------
	CASE 25
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:317 == 0
			PRINTFORML 
			CFLAG:317 = 1
		;二回目以降
		ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:317 = 2
		ENDIF
;-------------------------------------------------
;26,가사　연모、친애のみ
;-------------------------------------------------
	CASE 26
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:318 == 0
			PRINTFORML 
			CFLAG:318 = 1
		;二回目以降
		ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:318 = 2
		ENDIF
;-------------------------------------------------
;27,노래연습　연모、친애のみ
;-------------------------------------------------
	CASE 27
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:319 == 0
			PRINTFORML 
			CFLAG:319 = 1
		;二回目以降
		ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:319 = 2
		ENDIF
;-------------------------------------------------
;28,매도
;-------------------------------------------------
	CASE 28
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:320 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:320 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:320 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:320 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:320 = 3
			ENDIF
			;それ以外
			IF (CFLAG:320 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:320 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;29,비디오감상　함락済みのみ
;-------------------------------------------------
	CASE 29
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:321 == 0
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:321 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:321 = 2
		ENDIF
;-------------------------------------------------
;30,파후파후　함락済みのみ
;-------------------------------------------------
	CASE 30
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:422 == 0
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:422 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:422 = 2
		ENDIF
;=================================================
;道具사용コマンド
;=================================================
;-------------------------------------------------
;40,로터
;-------------------------------------------------
	CASE 40
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:322 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:322 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:322 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:322 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:322 = 3
			ENDIF
			;それ以外
			IF (CFLAG:322 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
	CASE 41
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:323 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:323 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:323 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:323 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:323 = 3
			ENDIF
			;それ以外
			IF (CFLAG:323 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:323 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;42,클리캡
;-------------------------------------------------
	CASE 42
		CALL TRAIN_MESSAGE
		IF TEQUIP:11
			;初めて
			IF CFLAG:324 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:324 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:324 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:324 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:324 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:324 = 3
				ENDIF
				;それ以外
				IF (CFLAG:324 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:324 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;43,오나홀
;-------------------------------------------------
	CASE 43
		CALL TRAIN_MESSAGE
		IF TEQUIP:12
			;初めて
			IF CFLAG:325 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:325 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:325 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:325 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:325 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:325 = 3
				ENDIF
				;それ以外
				IF (CFLAG:325 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:325 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;44,바이브
;-------------------------------------------------
	CASE 44
		CALL TRAIN_MESSAGE
		IF TEQUIP:13
			;初めて
			IF CFLAG:326 == 0
				;처녀
				IF TALENT:0
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;それ以外
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				;非처녀
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;それ以外
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				ENDIF
				CFLAG:326 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:326 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:326 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:326 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:326 = 3
				ENDIF
				;それ以外
				IF (CFLAG:326 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:326 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;45,애널바이브
;-------------------------------------------------
	CASE 45
		CALL TRAIN_MESSAGE
		IF TEQUIP:14
			;初めて
			IF CFLAG:327 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:327 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:327 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:327 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:327 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:327 = 3
				ENDIF
				;それ以外
				IF (CFLAG:327 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:327 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;46,애널비즈
;-------------------------------------------------
	CASE 46
		CALL TRAIN_MESSAGE
		IF TEQUIP:15
			;初めて
			IF CFLAG:328 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:328 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:328 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:328 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:328 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:328 = 3
				ENDIF
				;それ以外
				IF (CFLAG:328 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:328 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;47,유두캡
;-------------------------------------------------
	CASE 47
		CALL TRAIN_MESSAGE
		IF TEQUIP:16
			;初めて
			IF CFLAG:329 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:329 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:329 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:329 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:329 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:329 = 3
				ENDIF
				;それ以外
				IF (CFLAG:329 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:329 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
	CASE 48
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:330 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:330 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:330 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:330 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:330 = 3
			ENDIF
			;それ以外
			IF (CFLAG:330 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:330 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;49,착유기
;-------------------------------------------------
	CASE 49
		CALL TRAIN_MESSAGE
		IF TEQUIP:17
			;初めて
			IF CFLAG:331 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:331 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:331 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:331 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:331 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:331 = 3
				ENDIF
				;それ以外
				IF (CFLAG:331 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:331 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;=================================================
;조교자도구계コマンド
;=================================================
;-------------------------------------------------
;50,클리캡쓴다
;-------------------------------------------------
	CASE 50
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;初めて
			IF CFLAG:453 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:453 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:453 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:453 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:453 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:453 = 3
				ENDIF
				;それ以外
				IF (CFLAG:453 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:453 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:453 == 1
				CFLAG:453 = 0
			K = 453
			L = 11
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;51,오나홀쓴다
;-------------------------------------------------
	CASE 51
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;初めて
			IF CFLAG:454 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:454 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:454 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:454 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:454 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:454 = 3
				ENDIF
				;それ以外
				IF (CFLAG:454 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:454 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:454 == 1
				CFLAG:454 = 0
			K = 454
			L = 12
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;52,바이브쓴다
;-------------------------------------------------
	CASE 52
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;初めて
			IF CFLAG:455 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:455 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:455 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:455 = 3
				ENDIF
				;それ以外
				IF (CFLAG:455 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:455 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:455 == 1
				CFLAG:455 = 0
			K = 455
			L = 13
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;53,애널바이브쓴다
;-------------------------------------------------
	CASE 53
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;初めて
			IF CFLAG:456 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:456 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:456 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:456 = 3
				ENDIF
				;それ以外
				IF (CFLAG:456 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:456 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:456 == 1
				CFLAG:456 = 0
			K = 456
			L = 14
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;54,애널비즈쓴다
;-------------------------------------------------
	CASE 54
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:15
			;初めて
			IF CFLAG:457 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:457 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:457 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:457 = 3
				ENDIF
				;それ以外
				IF (CFLAG:457 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:457 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:457 == 1
				CFLAG:457 = 0
			K = 457
			L = 15
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;55,유두캡쓴다
;-------------------------------------------------
	CASE 55
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;初めて
			IF CFLAG:458 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:458 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:458 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:458 = 3
				ENDIF
				;それ以外
				IF (CFLAG:458 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:458 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:458 == 1
				CFLAG:458 = 0
			K = 458
			L = 16
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;56,착유기쓴다
;-------------------------------------------------
	CASE 56
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;初めて
			IF CFLAG:459 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:459 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:459 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:459 = 3
				ENDIF
				;それ以外
				IF (CFLAG:459 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:459 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:459 == 1
				CFLAG:459 = 0
			K = 459
			L = 17
			GOTO 조교자도구계コマンド共通
		ENDIF
;-------------------------------------------------
;조교자도구계コマンド共通
;50클리캡쓴다　51오나홀쓴다　　52바이브쓴다
;53애널바이브쓴다　54애널비즈쓴다　55유두캡쓴다
;56착유기쓴다
;-------------------------------------------------
	CASE 11111
		$조교자도구계コマンド共通
		IF TEQUIP:PLAYER:L
			;初めて
			IF CFLAG:K == 0
				PRINTFORML 
				CFLAG:K = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:K = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:K = 3
				ENDIF
				;それ以外
				IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					CFLAG:K = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
		ENDIF
;=================================================
;섹스系コマンド
;=================================================
;-------------------------------------------------
;60,정상위
;-------------------------------------------------
	CASE 60
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:332 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:332 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:332 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:332 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:332 = 3
			ENDIF
			;それ以外
			IF (CFLAG:332 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:332 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:332 == 1
				CFLAG:332 = 0
			K = 332
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;61,후배위
;-------------------------------------------------
	CASE 61
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:333 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:333 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:333 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:333 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:333 = 3
			ENDIF
			;それ以外
			IF (CFLAG:333 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:333 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:333 == 1
				CFLAG:333 = 0
			K = 333
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;62,정상위애널
;-------------------------------------------------
	CASE 62
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:334 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:334 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:334 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:334 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:334 = 3
			ENDIF
			;それ以外
			IF (CFLAG:334 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:334 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:334 == 1
				CFLAG:334 = 0
			K = 334
			GOTO 애널섹스系共通
		ENDIF
;-------------------------------------------------
;63,후배위애널
;-------------------------------------------------
	CASE 63
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:335 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:335 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:335 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:335 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:335 = 3
			ENDIF
			;それ以外
			IF (CFLAG:335 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:335 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:335 == 1
				CFLAG:335 = 0
			K = 335
			GOTO 애널섹스系共通
		ENDIF
;-------------------------------------------------
;64,역강간
;-------------------------------------------------
	CASE 64
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:336 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:336 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:336 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:336 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:336 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:336 = 3
			ENDIF
			;それ以外
			IF (CFLAG:336 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:336 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;65,젖성교
;-------------------------------------------------
	CASE 65
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:337 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:337 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:337 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:337 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:337 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:337 = 3
			ENDIF
			;それ以外
			IF (CFLAG:337 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:337 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;66,기승위
;-------------------------------------------------
	CASE 66
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:338 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:338 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:338 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:338 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:338 = 3
			ENDIF
			;それ以外
			IF (CFLAG:338 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:338 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:338 == 1
				CFLAG:338 = 0
			K = 338
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;67,기승위애널
;-------------------------------------------------
	CASE 67
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:339 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:339 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:339 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:339 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:339 = 3
			ENDIF
			;それ以外
			IF (CFLAG:339 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:339 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:339 == 1
				CFLAG:339 = 0
			K = 339
			GOTO 애널섹스系共通
		ENDIF
;-------------------------------------------------
;68,대면좌위
;-------------------------------------------------
	CASE 68
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:340 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:340 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:340 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:340 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:340 = 3
			ENDIF
			;それ以外
			IF (CFLAG:340 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:340 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:340 == 1
				CFLAG:340 = 0
			K = 340
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;69,배면좌위
;-------------------------------------------------
	CASE 69
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:341 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:341 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:341 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:341 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:341 = 3
			ENDIF
			;それ以外
			IF (CFLAG:341 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:341 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:341 == 1
				CFLAG:341 = 0
			K = 341
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;70,대면좌위애널
;-------------------------------------------------
	CASE 70
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:342 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:342 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:342 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:342 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:342 = 3
			ENDIF
			;それ以外
			IF (CFLAG:342 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:342 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:342 == 1
				CFLAG:342 = 0
			K = 342
			GOTO 애널섹스系共通
		ENDIF
;-------------------------------------------------
;71,배면좌위애널
;-------------------------------------------------
	CASE 71
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:343 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:343 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:343 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:343 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:343 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:343 = 3
			ENDIF
			;それ以外
			IF (CFLAG:343 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:343 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:343 == 1
				CFLAG:343 = 0
			K = 343
			GOTO 애널섹스系共通
		ENDIF
;-------------------------------------------------
;72,속옷비켜삽입
;-------------------------------------------------
	CASE 72
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:420 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:420 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:420 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:420 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:420 = 3
			ENDIF
			;それ以外
			IF (CFLAG:420 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:420 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:420 == 1
				CFLAG:420 = 0
			K = 420
			GOTO 섹스系共通
		ENDIF
;-------------------------------------------------
;73,속옷비켜삽입애널
;-------------------------------------------------
	CASE 73
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:421 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:421 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:421 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:421 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:421 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:421 = 3
			ENDIF
			;それ以外
			IF (CFLAG:421 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:421 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:421 == 1
				CFLAG:421 = 0
			K = 421
			GOTO 애널섹스系共通
		ENDIF
;-------------------------------------------------
;74,역강간당한다
;-------------------------------------------------
	CASE 74
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:460 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:460 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:460 = 3
			ENDIF
			;それ以外
			IF (CFLAG:460 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:460 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;섹스系共通
;60,정상위　　61,후배위　　66,기승위
;68,대면좌위　69,배면좌위　72,속옷비켜삽입
;-------------------------------------------------
	CASE 11111
		$섹스系共通
		;初めて
		IF CFLAG:K == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;それ以外
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;애널섹스系共通
;62,정상위애널　　63,후배위애널　　67,기승위애널
;70,대면좌위애널　71,배면좌위애널　72,속옷비켜삽입애널
;-------------------------------------------------
	CASE 11111
		$애널섹스系共通
		;初めて
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;それ以外
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;=================================================
;봉사계コマンド
;=================================================
;-------------------------------------------------
;80,수음
;-------------------------------------------------
	CASE 80
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:344 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:344 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:344 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:344 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:344 = 3
			ENDIF
			;それ以外
			IF (CFLAG:344 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:344 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;81,펠라치오
;-------------------------------------------------
	CASE 81
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:345 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:345 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:345 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:345 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:345 = 3
			ENDIF
			;それ以外
			IF (CFLAG:345 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:345 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:345 == 1
				CFLAG:345 = 0
			K = 345
			GOTO フェラ共通
		ENDIF
;-------------------------------------------------
;82,파이즈리
;-------------------------------------------------
	CASE 82
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:346 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:346 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:346 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:346 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:346 = 3
			ENDIF
			;それ以外
			IF (CFLAG:346 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:346 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:346 == 1
				CFLAG:346 = 0
			K = 346
			GOTO 파이즈리共通
		ENDIF
;-------------------------------------------------
;83,스마타
;-------------------------------------------------
	CASE 83
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:347 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:347 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:347 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:347 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:347 = 3
			ENDIF
			;それ以外
			IF (CFLAG:347 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:347 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
	CASE 84
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:348 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:348 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:348 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:348 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:348 = 3
			ENDIF
			;それ以外
			IF (CFLAG:348 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:348 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;85,풋잡
;-------------------------------------------------
	CASE 85
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:349 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:349 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:349 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:349 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:349 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:349 = 3
			ENDIF
			;それ以外
			IF (CFLAG:349 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:349 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;86,다리핥기강제
;-------------------------------------------------
	CASE 86
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:350 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:350 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:350 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:350 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:350 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:350 = 3
			ENDIF
			;それ以外
			IF (CFLAG:350 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:350 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;87,세로파이즈리
;-------------------------------------------------
	CASE 87
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:351 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:351 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:351 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:351 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:351 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:351 = 3
			ENDIF
			;それ以外
			IF (CFLAG:351 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:351 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:351 == 1
				CFLAG:351 = 0
			K = 351
			GOTO 파이즈리共通
		ENDIF
;-------------------------------------------------
;88,파이즈리펠라
;-------------------------------------------------
	CASE 88
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:352 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:352 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:352 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:352 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:352 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:352 = 3
			ENDIF
			;それ以外
			IF (CFLAG:352 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:352 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			SIF CFLAG:352 == 1
				CFLAG:352 = 0
			K = 352
			GOTO フェラ共通
		ENDIF
;-------------------------------------------------
;89,모유마시기　함락済みのみ
;-------------------------------------------------
	CASE 89
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:353 == 0
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:353 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:353 = 2
		ENDIF
;-------------------------------------------------
;211,귀두공격(受)
;-------------------------------------------------
	CASE 211
		;初めて
		IF CFLAG:479 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:479 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:479 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:479 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:479 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:479 = 3
			ENDIF
			;それ以外
			IF (CFLAG:479 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:479 = 2
			ENDIF
		ENDIF
		IF LOCALS:0 == ""
			K = 353
			GOTO フェラ共通
		ENDIF
;-------------------------------------------------
;フェラ共通
;81,펠라치오　88,파이즈리펠라　211,귀두공격(受)
;-------------------------------------------------
	CASE 11111
		$フェラ共通
		;初めて
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;それ以外
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;파이즈리共通
;82,파이즈리　87,세로파이즈리
;-------------------------------------------------
	CASE 11111
		$파이즈리共通
		;初めて
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;それ以外
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF
;=================================================
;조교자봉사계コマンド
;=================================================
;-------------------------------------------------
;90,수음한다
;-------------------------------------------------
	CASE 90
		CALL TRAIN_MESSAGE
		IF TEQUIP:12 == 0
			;初めて
			IF CFLAG:423 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:423 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:423 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:423 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:423 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:423 = 3
				ENDIF
				;それ以外
				IF (CFLAG:423 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:423 = 2
				ENDIF
			ENDIF
		ELSE
;-------------------------------------------------
;オナホコキする
;-------------------------------------------------
			;初めて
			IF CFLAG:461 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:461 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:461 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:461 = 3
				ENDIF
				;それ以外
				IF (CFLAG:461 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:461 = 2
				ENDIF
			ENDIF
		ENDIF
;-------------------------------------------------
;91,파이즈리한다
;-------------------------------------------------
	CASE 91
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:424 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:424 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:424 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:424 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:424 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:424 = 3
			ENDIF
			;それ以外
			IF (CFLAG:424 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:424 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;92,정상위시킨다
;-------------------------------------------------
	CASE 92
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:425 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:425 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:425 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:425 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:425 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:425 = 3
			ENDIF
			;それ以外
			IF (CFLAG:425 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:425 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;93,후배위시킨다
;-------------------------------------------------
	CASE 93
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:426 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:426 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:426 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:426 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:426 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:426 = 3
			ENDIF
			;それ以外
			IF (CFLAG:426 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:426 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;94,기승위한다
;-------------------------------------------------
	CASE 94
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:427 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:427 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:427 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:427 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:427 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:427 = 3
			ENDIF
			;それ以外
			IF (CFLAG:427 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:427 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;95,애널정상위시킨다
;-------------------------------------------------
	CASE 95
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:428 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:428 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:428 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:428 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:428 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:428 = 3
			ENDIF
			;それ以外
			IF (CFLAG:428 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:428 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;96,애널후배위시킨다
;-------------------------------------------------
	CASE 96
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:429 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:429 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:429 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:429 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:429 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:429 = 3
			ENDIF
			;それ以外
			IF (CFLAG:429 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:429 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;97,애널기승위한다
;-------------------------------------------------
	CASE 97
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:430 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:430 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:430 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:430 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:430 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:430 = 3
			ENDIF
			;それ以外
			IF (CFLAG:430 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:430 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;98,모유먹인다　함락済みのみ
;-------------------------------------------------
	CASE 98
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:443 == 0
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:443 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;연모、친애
			IF (TALENT:3 || TALENT:6)
				LOCALS:0 =
			;음란、창부
			ELSEIF (TALENT:4 || TALENT:7)
				LOCALS:0 =
			;복종、예속
			ELSEIF (TALENT:5 || TALENT:8)
				LOCALS:0 =
			ENDIF
			;함락済み共通
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF
			CFLAG:443 = 2
		ENDIF
;-------------------------------------------------
;99,애무시킨다
;-------------------------------------------------
	CASE 99
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:462 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:462 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:462 = 3
			ENDIF
			;それ以外
			IF (CFLAG:462 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:462 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;210,귀두공격
;-------------------------------------------------
	CASE 210
		;初めて
		IF CFLAG:480 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:480 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:480 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:480 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:480 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:480 = 3
			ENDIF
			;それ以外
			IF (CFLAG:480 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:480 = 2
			ENDIF
		ENDIF
;=================================================
;SM系コマンド
;=================================================
;-------------------------------------------------
;100,스팽킹
;-------------------------------------------------
	CASE 100
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:354 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:354 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:354 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:354 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:354 = 3
			ENDIF
			;それ以外
			IF (CFLAG:354 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:354 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;101,젖스팽킹
;-------------------------------------------------
	CASE 101
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:355 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:355 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:355 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:355 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:355 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:355 = 3
			ENDIF
			;それ以外
			IF (CFLAG:355 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:355 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;102,채찍
;-------------------------------------------------
	CASE 102
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:356 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:356 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:356 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:356 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:356 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:356 = 3
			ENDIF
			;それ以外
			IF (CFLAG:356 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:356 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;103,양초
;-------------------------------------------------
	CASE 103
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:357 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:357 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:357 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:357 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:357 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:357 = 3
			ENDIF
			;それ以外
			IF (CFLAG:357 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:357 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;104,바늘
;-------------------------------------------------
	CASE 104
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:358 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:358 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:358 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:358 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:358 = 3
			ENDIF
			;それ以外
			IF (CFLAG:358 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:358 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;105,아이마스크
;-------------------------------------------------
	CASE 105
		CALL TRAIN_MESSAGE
		IF TEQUIP:18
			;初めて
			IF CFLAG:359 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:359 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:359 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:359 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:359 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:359 = 3
				ENDIF
				;それ以外
				IF (CFLAG:359 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:359 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;106,밧줄
;-------------------------------------------------
	CASE 106
		CALL TRAIN_MESSAGE
		IF TEQUIP:19
			;初めて
			IF CFLAG:360 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:360 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:360 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:360 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:360 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:360 = 3
				ENDIF
				;それ以外
				IF (CFLAG:360 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:360 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;107,볼재갈
;-------------------------------------------------
	CASE 107
		CALL TRAIN_MESSAGE
		IF TEQUIP:20
			;初めて
			IF CFLAG:361 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:361 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:361 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:361 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:361 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:361 = 3
				ENDIF
				;それ以外
				IF (CFLAG:361 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:361 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;108,삼각목마
;-------------------------------------------------
	CASE 108
		CALL TRAIN_MESSAGE
		IF TEQUIP:41
			;初めて
			IF CFLAG:463 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:463 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:463 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:463 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:463 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:463 = 3
				ENDIF
				;それ以外
				IF (CFLAG:463 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:463 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;109,사정멈추기
;-------------------------------------------------
	CASE 109
		CALL TRAIN_MESSAGE
		IF TEQUIP:42
			;初めて
			IF CFLAG:464 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:464 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:464 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:464 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:464 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:464 = 3
				ENDIF
				;それ以外
				IF (CFLAG:464 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:464 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;=================================================
;조수によるコマンド、及び레즈プレイ
;=================================================
;-------------------------------------------------
;120,커닐링구스강제
;-------------------------------------------------
	CASE 120
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:362 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:362 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:362 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:362 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:362 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:362 = 3
			ENDIF
			;それ以外
			IF (CFLAG:362 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:362 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;121,조개맞대기
;-------------------------------------------------
	CASE 121
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:363 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:363 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:363 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:363 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:363 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:363 = 3
			ENDIF
			;それ以外
			IF (CFLAG:363 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:363 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;122,조수를범하게한다
;-------------------------------------------------
	CASE 122
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:364 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:364 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:364 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:364 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:364 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:364 = 3
			ENDIF
			;それ以外
			IF (CFLAG:364 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:364 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;조수が気持ちよくて感じているセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;조수がこっちに来てと言うセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;조수に大丈남편と言っているセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;犯されて強がりを言うセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;うまくなってきたねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;123,풋잡한다
;-------------------------------------------------
	CASE 123
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:365 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:365 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:365 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:365 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:365 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:365 = 3
			ENDIF
			;それ以外
			IF (CFLAG:365 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:365 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
	CASE 124
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:366 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:366 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:366 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:366 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:366 = 3
			ENDIF
			;それ以外
			IF (CFLAG:366 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:366 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;一緒に気持ちよくしようというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私のほうが気持ちよくしてるというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;煽りに対してのセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;いつもより感じてるねというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;레즈っぽいセリフに対するセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		;二人のフェラは気持ちいいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(6)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
	CASE 125
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:367 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:367 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:367 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:367 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:367 = 3
			ENDIF
			;それ以外
			IF (CFLAG:367 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:367 = 2
			ENDIF
		ENDIF
		
		;조수구상호출関数
		;一緒に気持ちよくしようというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私のほうが気持ちよくしてるというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;煽りに対してのセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;いつもより感じてるねというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;お互いに유두がこすれて気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		;二人の파이즈리で気持ちよくなってセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(6)
		;파이즈리で感じているの？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(7)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;126,가슴맞대기
;-------------------------------------------------
	CASE 126
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:368 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:368 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:368 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:368 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:368 = 3
			ENDIF
			;それ以外
			IF (CFLAG:368 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:368 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;127,자위과시
;-------------------------------------------------
	CASE 127
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:432 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:432 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:432 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:432 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:432 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:432 = 3
			ENDIF
			;それ以外
			IF (CFLAG:432 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:432 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;128,조개젖히기과시
;-------------------------------------------------
	CASE 128
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:465 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:465 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:465 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:465 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:465 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:465 = 3
			ENDIF
			;それ以外
			IF (CFLAG:465 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:465 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;129,자위구음과시
;-------------------------------------------------
	CASE 129
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:466 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:466 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:466 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:466 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:466 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:466 = 3
			ENDIF
			;それ以外
			IF (CFLAG:466 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:466 = 2
			ENDIF
		ENDIF
;=================================================
;ハードな조교コマンド
;=================================================
;-------------------------------------------------
;140,이라마치오
;-------------------------------------------------
	CASE 140
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:369 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:369 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:369 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:369 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:369 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:369 = 3
			ENDIF
			;それ以外
			IF (CFLAG:369 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:369 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;141,피스트퍽　복종、예속のみ
;-------------------------------------------------
	CASE 141
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:370 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:370 = 1
		;二回目以降
		ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:370 = 2
		ENDIF
;-------------------------------------------------
;142,애널피스트　복종、예속のみ
;-------------------------------------------------
	CASE 142
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:371 == 0
			PRINTFORML 
			CFLAG:371 = 1
		;二回目以降
		ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:371 = 2
		ENDIF
;-------------------------------------------------
;143,양구멍피스트　복종、예속のみ
;-------------------------------------------------
	CASE 143
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:372 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:372 = 1
		;二回目以降
		ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:372 = 2
		ENDIF
;-------------------------------------------------
;144,방뇨
;-------------------------------------------------
	CASE 144
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:373 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:373 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:373 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:373 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:373 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:373 = 3
			ENDIF
			;それ以外
			IF (CFLAG:373 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:373 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;145,애널봉사강제
;-------------------------------------------------
	CASE 145
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:374 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:374 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:374 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:374 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:374 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:374 = 3
			ENDIF
			;それ以外
			IF (CFLAG:374 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:374 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;146,관장기＋플러그　해제すると方便
;-------------------------------------------------
	CASE 146
		CALL TRAIN_MESSAGE
		IF TEQUIP:21
			;初めて
			IF CFLAG:375 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:375 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:375 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:375 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:375 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:375 = 3
				ENDIF
				;それ以外
				IF (CFLAG:375 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:375 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;147,공기관장　おならさせる
;-------------------------------------------------
	CASE 147
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:376 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:376 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:376 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:376 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:376 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:376 = 3
			ENDIF
			;それ以外
			IF (CFLAG:376 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:376 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;148,확장벌룬
;-------------------------------------------------
	CASE 148
		CALL TRAIN_MESSAGE
		IF TEQUIP:22
			;初めて
			IF CFLAG:377 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:377 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:377 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:377 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:377 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:377 = 3
				ENDIF
				;それ以外
				IF (CFLAG:377 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:377 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;149,애널전극　복종、예속のみ
;-------------------------------------------------
	CASE 149
		CALL TRAIN_MESSAGE
		IF TEQUIP:23
			;初めて
			IF CFLAG:378 == 0
				PRINTFORML 
				CFLAG:378 = 1
			;二回目以降
			ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:378 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:378 == 1 || FLAG:7 == 2
				PRINTFORML 
			ENDIF
		ENDIF
;-------------------------------------------------
;150,유방전극　복종、예속のみ
;-------------------------------------------------
	CASE 150
		CALL TRAIN_MESSAGE
		IF TEQUIP:24
			;初めて
			IF CFLAG:379 == 0
				PRINTFORML 
				CFLAG:379 = 1
			;二回目以降
			ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:379 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:379 == 1 || FLAG:7 == 2
				PRINTFORML 
			ENDIF
		ENDIF
;-------------------------------------------------
;151,뱀장어욕탕　복종、예속のみ
;-------------------------------------------------
	CASE 151
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:380 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:380 = 1
		;二回目以降
		ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:380 = 2
		ENDIF
;-------------------------------------------------
;152,벌레욕탕　복종、예속のみ
;-------------------------------------------------
	CASE 152
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:381 == 0
			PRINTFORML 
			CFLAG:381 = 1
		;二回目以降
		ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
			PRINTFORML 
			CFLAG:381 = 2
		ENDIF
;-------------------------------------------------
;154,요정오나홀　小人体系のみ
;-------------------------------------------------
	CASE 154
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:467 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:467 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:467 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:467 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:467 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:467 = 3
			ENDIF
			;それ以外
			IF (CFLAG:467 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:467 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;155,구슬킥
;-------------------------------------------------
	CASE 155
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:468 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:468 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:468 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:468 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:468 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:468 = 3
			ENDIF
			;それ以外
			IF (CFLAG:468 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:468 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;156,음뇨
;-------------------------------------------------
	CASE 156
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:469 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:469 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:469 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:469 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:469 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:469 = 3
			ENDIF
			;それ以外
			IF (CFLAG:469 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:469 = 2
			ENDIF
		ENDIF
;=================================================
;촉수계コマンド
;=================================================
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
	CASE 160
		CALL TRAIN_MESSAGE
		IF TEQUIP:25
			;初めて
			IF CFLAG:382 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:382 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:382 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:382 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:382 = 3
				ENDIF
				;それ以外
				IF (CFLAG:382 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:382 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	CASE 44949
		$SHOKUSHU_PLAY
		SELECTCASE SELECTCOM
;-------------------------------------------------
;0,촉수애무
;-------------------------------------------------
			CASE 0
				;初めて
				IF CFLAG:455 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:455 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:455 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:455 = 3
					ENDIF
					;それ以外
					IF (CFLAG:455 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:455 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;5,촉수애널애무
;-------------------------------------------------
			CASE 5
				;初めて
				IF CFLAG:456 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:456 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:456 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:456 = 3
					ENDIF
					;それ以外
					IF (CFLAG:456 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:456 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;6,촉수가슴주무르기
;-------------------------------------------------
			CASE 6
				;初めて
				IF CFLAG:457 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:457 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:457 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:457 = 3
					ENDIF
					;それ以外
					IF (CFLAG:457 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:457 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;7,촉수유두자극（短）
;-------------------------------------------------
			CASE 7
				;初めて
				IF CFLAG:458 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:458 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:458 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:458 = 3
					ENDIF
					;それ以外
					IF (CFLAG:458 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:458 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;8,촉수가슴애무
;-------------------------------------------------
			CASE 8
				;初めて
				IF CFLAG:459 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:459 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:459 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:459 = 3
					ENDIF
					;それ以外
					IF (CFLAG:459 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:459 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;12,촉수날개애무
;-------------------------------------------------
			CASE 12
				;初めて
				IF CFLAG:460 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:460 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:460 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:460 = 3
					ENDIF
					;それ以外
					IF (CFLAG:460 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:460 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;13,촉수꼬리애무
;-------------------------------------------------
			CASE 13
				;初めて
				IF CFLAG:461 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:461 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:461 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:461 = 3
					ENDIF
					;それ以外
					IF (CFLAG:461 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:461 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;15,촉수엉덩이애무
;-------------------------------------------------
			CASE 15
				;初めて
				IF CFLAG:462 == 0
					PRINTFORML 
					LOCALS:0 =
					CFLAG:462 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 3 || FLAG:7 == 2)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부	
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속	
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 = 
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 = 
						ENDIF
						CFLAG:462 = 4
					ENDIF
					;굴복각인Lv3
					IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
						LOCALS:0 = 
						CFLAG:462 = 3
					ENDIF
					;それ以外
					IF (CFLAG:462 || FLAG:7 == 2) && LOCALS:0 == ""
						PRINTFORML
						LOCALS:0 =
						CFLAG:462 = 2
					ENDIF
				ENDIF
		ENDSELECT
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
;-------------------------------------------------
;161,촉수삽입
;-------------------------------------------------
	CASE 161
		CALL TRAIN_MESSAGE
		IF TEQUIP:13
			;初めて
			IF CFLAG:383 == 0
				;처녀
				IF TALENT:0
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;それ以外
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				;非처녀
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;연모、친애
						IF (TALENT:3 || TALENT:6)
							LOCALS:0 = 
						;음란、창부
						ELSEIF (TALENT:4 || TALENT:7)
							LOCALS:0 = 
						;복종、예속
						ELSEIF (TALENT:5 || TALENT:8)
							LOCALS:0 =
						ENDIF
						;함락済み共通
						IF LOCALS:0 == ""
							LOCALS:0 =
						ENDIF
					ENDIF
					;それ以外
					IF LOCALS:0 == ""
						PRINTFORML 
						LOCALS:0 =
					ENDIF
				ENDIF
				CFLAG:383 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:383 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:383 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:383 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:383 = 3
				ENDIF
				;それ以外
				IF (CFLAG:383 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:383 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;162,애널촉수
;-------------------------------------------------
	CASE 162
		CALL TRAIN_MESSAGE
		IF TEQUIP:14
			;初めて
			IF CFLAG:384 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:384 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:384 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:384 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:384 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:384 = 3
				ENDIF
				;それ以外
				IF (CFLAG:384 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:384 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;163,촉수클리자극
;-------------------------------------------------
	CASE 163
		CALL TRAIN_MESSAGE
		IF TEQUIP:11
			;初めて
			IF CFLAG:385 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:385 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:385 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:385 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:385 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:385 = 3
				ENDIF
				;それ以外
				IF (CFLAG:385 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:385 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;164,촉수유두자극
;-------------------------------------------------
	CASE 164
		CALL TRAIN_MESSAGE
		IF TEQUIP:16
			;初めて
			IF CFLAG:386 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:386 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:386 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:386 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:386 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:386 = 3
				ENDIF
				;それ以外
				IF (CFLAG:386 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:386 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;165,촉수착유
;-------------------------------------------------
	CASE 165
		CALL TRAIN_MESSAGE
		IF TEQUIP:17
			;初めて
			IF CFLAG:387 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:387 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:387 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:387 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:387 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:387 = 3
				ENDIF
				;それ以外
				IF (CFLAG:387 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:387 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;166,촉수긴박
;-------------------------------------------------
	CASE 166
		CALL TRAIN_MESSAGE
		IF TEQUIP:19
			;初めて
			IF CFLAG:388 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:388 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:388 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:388 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:388 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:388 = 3
				ENDIF
				;それ以外
				IF (CFLAG:388 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:388 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;167,촉수관장　極太の촉수が菊穴をふさぎ、体液を流し込んだ
;-------------------------------------------------
	CASE 167
		CALL TRAIN_MESSAGE
		IF TEQUIP:21
			;初めて
			IF CFLAG:389 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:389 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:389 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:389 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:389 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:389 = 3
				ENDIF
				;それ以外
				IF (CFLAG:389 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:389 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;168,촉수구욕
;-------------------------------------------------
	CASE 168
		CALL TRAIN_MESSAGE
		IF TEQUIP:36
			;初めて
			IF CFLAG:390 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:390 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:390 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:390 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:390 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:390 = 3
				ENDIF
				;それ以外
				IF (CFLAG:390 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:390 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;169,촉수페니스자극
;-------------------------------------------------
	CASE 169
		CALL TRAIN_MESSAGE
		IF TEQUIP:12
			;初めて
			IF CFLAG:391 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:391 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:391 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:391 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:391 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:391 = 3
				ENDIF
				;それ以外
				IF (CFLAG:391 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:391 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;170,촉수삽입시킨다
;-------------------------------------------------
	CASE 170
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;初めて
			IF CFLAG:470 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:470 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:470 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:470 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:470 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:470 = 3
				ENDIF
				;それ以外
				IF (CFLAG:470 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:470 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;171,애널촉수시킨다
;-------------------------------------------------
	CASE 171
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;初めて
			IF CFLAG:471 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:471 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:471 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:471 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:471 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:471 = 3
				ENDIF
				;それ以外
				IF (CFLAG:471 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:471 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;172,촉수클리자극시킨다
;-------------------------------------------------
	CASE 172
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;初めて
			IF CFLAG:472 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:472 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:472 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:472 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:472 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:472 = 3
				ENDIF
				;それ以外
				IF (CFLAG:472 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:472 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;173,촉수유두자극시킨다
;-------------------------------------------------
	CASE 173
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;初めて
			IF CFLAG:473 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:473 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:473 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:473 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:473 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:473 = 3
				ENDIF
				;それ以外
				IF (CFLAG:473 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:473 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;174,촉수착유시킨다
;-------------------------------------------------
	CASE 174
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;初めて
			IF CFLAG:474 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:474 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:474 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:474 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:474 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:474 = 3
				ENDIF
				;それ以外
				IF (CFLAG:474 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:474 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;175,촉수페니스자극시킨다
;-------------------------------------------------
	CASE 175
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;初めて
			IF CFLAG:475 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:475 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:475 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:475 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:475 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:475 = 3
				ENDIF
				;それ以外
				IF (CFLAG:475 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:475 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;=================================================
;특수커맨드
;=================================================
;-------------------------------------------------
;180,로션
;-------------------------------------------------
	CASE 180
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:392 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:392 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:392 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:392 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:392 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:392 = 3
			ENDIF
			;それ以外
			IF (CFLAG:392 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:392 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;181,미약
;-------------------------------------------------
	CASE 181
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:393 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:393 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:393 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:393 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:393 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:393 = 3
			ENDIF
			;それ以外
			IF (CFLAG:393 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:393 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;182,이뇨제
;-------------------------------------------------
	CASE 182
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:394 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:394 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:394 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:394 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:394 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:394 = 3
			ENDIF
			;それ以外
			IF (CFLAG:394 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:394 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;183,비디오카메라
;-------------------------------------------------
	CASE 183
		CALL TRAIN_MESSAGE
		IF TEQUIP:28
			;初めて
			IF CFLAG:395 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:395 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:395 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:395 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:395 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:395 = 3
				ENDIF
				;それ以外
				IF (CFLAG:395 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:395 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;184,야외플레이
;-------------------------------------------------
	CASE 184
		CALL TRAIN_MESSAGE
		;TEQUIP:29でどこの場所選んでも同じ구상が出るように
		;변경･場所ごとで変えたければ==にして数字をいじる
		;廃ビル=1　森林=2　公園=4　川辺=8
		IF TEQUIP:29
			;初めて
			IF CFLAG:396 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:396 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:396 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:396 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:396 = 3
				ENDIF
				;それ以外
				IF (CFLAG:396 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:396 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;185,조수를범한다
;-------------------------------------------------
	CASE 185
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:397 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:397 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:397 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:397 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:397 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:397 = 3
			ENDIF
			;それ以外
			IF (CFLAG:397 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:397 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;犯されてとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私たち、とっても仲がいいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;うらやましいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;こっちが気になってるみたいだねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;186,수치플레이
;-------------------------------------------------
	CASE 186
		CALL TRAIN_MESSAGE
		IF TEQUIP:30
			;初めて
			IF CFLAG:398 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:398 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:398 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:398 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:398 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:398 = 3
				ENDIF
				;それ以外
				IF (CFLAG:398 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:398 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;187,목욕탕플레이
;-------------------------------------------------
	CASE 187
		CALL TRAIN_MESSAGE
		IF TEQUIP:31
			;初めて
			IF CFLAG:399 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:399 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:399 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:399 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:399 = 3
				ENDIF
				;それ以外
				IF (CFLAG:399 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:399 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;188,샤워
;-------------------------------------------------
	CASE 188
		CALL TRAIN_MESSAGE
		IF TEQUIP:32
			;初めて
			IF CFLAG:400 == 0
				PRINTFORML 
				LOCALS:0 =
				CFLAG:400 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:400 < 3 || FLAG:7 == 2)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:400 = 4
				ENDIF
				;굴복각인Lv3
				IF MARK:2 == 3 && (CFLAG:400 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:400 = 3
				ENDIF
				;それ以外
				IF (CFLAG:400 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =
					CFLAG:400 = 2
				ENDIF
			ENDIF
		;해제
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;연모、친애
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 =
				;음란、창부
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 =
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 =
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 =
				ENDIF
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && LOCALS:0 == ""
				LOCALS:0 =
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
				LOCALS:0 =
			ENDIF
		ENDIF
;-------------------------------------------------
;189,새댁플레이　연모、친애のみ
;-------------------------------------------------
	CASE 189
		CALL TRAIN_MESSAGE
		IF TEQUIP:33
			;初めて
			IF CFLAG:401 == 0
				PRINTFORML 
				CFLAG:401 = 1
			;二回目以降
			ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:401 = 2
			ENDIF
		ELSE
		;해제
			IF CFLAG:401 == 1 || FLAG:7 == 2
				PRINTFORML 
			ENDIF
		ENDIF
;-------------------------------------------------
;190,사진촬영
;-------------------------------------------------
	CASE 190
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:402 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:402 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:402 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:402 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:402 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:402 = 3
			ENDIF
			;それ以外
			IF (CFLAG:402 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:402 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;191,강정신약
;-------------------------------------------------
	CASE 191
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:403 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:403 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:403 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:403 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:403 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:403 = 3
			ENDIF
			;それ以外
			IF (CFLAG:403 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:403 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;192,콘돔정음（노예）
;-------------------------------------------------
	CASE 192
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:404 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:404 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:404 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:404 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:404 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:404 = 3
			ENDIF
			;それ以外
			IF (CFLAG:404 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:404 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;193,콘돔정음（조수）
;-------------------------------------------------
	CASE 193
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:405 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:405 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:405 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:405 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:405 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:405 = 3
			ENDIF
			;それ以外
			IF (CFLAG:405 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:405 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;정액とてもおいしいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;これはあたしのものよ！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;うらやましいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;先にいただくわセリフ（仲がいい）
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
 
;-------------------------------------------------
;194,정액키스
;-------------------------------------------------
	CASE 194
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:406 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:406 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:406 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:406 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:406 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:406 = 3
			ENDIF
			;それ以外
			IF (CFLAG:406 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:406 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;195,유선개발
;-------------------------------------------------
	CASE 195
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:431 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:431 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:431 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:431 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:431 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:431 = 3
			ENDIF
			;それ以外
			IF (CFLAG:431 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:431 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;196,미약쓴다
;-------------------------------------------------
	CASE 196
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:437 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:437 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:437 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:437 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:437 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:437 = 3
			ENDIF
			;それ以外
			IF (CFLAG:437 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:437 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;197,팬티훑기
;-------------------------------------------------
	CASE 197
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:438 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:438 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:438 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:438 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:438 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:438 = 3
			ENDIF
			;それ以外
			IF (CFLAG:438 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:438 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;200,사람을부른다
;-------------------------------------------------
	CASE 200
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:433 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:433 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:433 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:433 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:433 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:433 = 3
			ENDIF
			;それ以外
			IF (CFLAG:433 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:433 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;201,윤간
;-------------------------------------------------
	CASE 201
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:434 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:434 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:434 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:434 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:434 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:434 = 3
			ENDIF
			;それ以外
			IF (CFLAG:434 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:434 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;202,사람을모으게한다
;-------------------------------------------------
	CASE 202
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:476 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:476 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:476 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:476 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:476 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:476 = 3
			ENDIF
			;それ以外
			IF (CFLAG:476 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:476 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;203,원격조교
;-------------------------------------------------
	CASE 203
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:477 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:477 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:477 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:477 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:477 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:477 = 3
			ENDIF
			;それ以外
			IF (CFLAG:477 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:477 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;204,정액먹기
;-------------------------------------------------
	CASE 204
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:478 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:478 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:478 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:478 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:478 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:478 = 3
			ENDIF
			;それ以外
			IF (CFLAG:478 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:478 = 2
			ENDIF
		ENDIF
;=================================================
;500番台,派生コマンド
;=================================================
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
	CASE 500
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:407 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:407 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:407 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:407 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:407 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:407 = 3
			ENDIF
			;それ以外
			IF (CFLAG:407 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:407 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;ナカがとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;どんどんイキなさい！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;とても気持ちよさそうセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;ここに入れてセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;501,식스나인
;-------------------------------------------------
	CASE 501
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:408 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:408 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:408 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:408 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:408 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:408 = 3
			ENDIF
			;それ以外
			IF (CFLAG:408 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:408 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;502,이와시미즈
;-------------------------------------------------
	CASE 502
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:409 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:409 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:409 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:409 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:409 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:409 = 3
			ENDIF
			;それ以外
			IF (CFLAG:409 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:409 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;503,G스팟자극
;-------------------------------------------------
	CASE 503
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:410 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:410 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:410 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:410 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:410 = 3
			ENDIF
			;それ以外
			IF (CFLAG:410 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:410 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;504,흐트러진모란
;-------------------------------------------------
	CASE 504
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:411 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:411 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:411 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:411 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:411 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:411 = 3
			ENDIF
			;それ以外
			IF (CFLAG:411 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:411 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;505,기승유방자위
;-------------------------------------------------
	CASE 505
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:412 == 0
			;처녀
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			;非처녀
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;연모、친애
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;음란、창부
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;복종、예속
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 =
					ENDIF
					;함락済み共通
					IF LOCALS:0 == ""
						LOCALS:0 =
					ENDIF
				ENDIF
				;それ以外
				IF LOCALS:0 == ""
					PRINTFORML 
					LOCALS:0 =
				ENDIF
			ENDIF
			CFLAG:412 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:412 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:412 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:412 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:412 = 3
			ENDIF
			;それ以外
			IF (CFLAG:412 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:412 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;506,삽입음부자극
;-------------------------------------------------
	CASE 506
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:413 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:413 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:413 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:413 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:413 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:413 = 3
			ENDIF
			;それ以外
			IF (CFLAG:413 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:413 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;507,삽입자궁구자극
;-------------------------------------------------
	CASE 507
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:414 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:414 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:414 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:414 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:414 = 3
			ENDIF
			;それ以外
			IF (CFLAG:414 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:414 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;508,６９파이즈리
;-------------------------------------------------
	CASE 508
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:415 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:415 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:415 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:415 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:415 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:415 = 3
			ENDIF
			;それ以外
			IF (CFLAG:415 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:415 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;509,착유
;-------------------------------------------------
	CASE 509
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:416 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:416 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:416 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:416 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:416 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:416 = 3
			ENDIF
			;それ以外
			IF (CFLAG:416 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:416 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;510,더블스마타
;-------------------------------------------------
	CASE 510
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:417 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:417 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:417 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:417 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:417 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:417 = 3
			ENDIF
			;それ以外
			IF (CFLAG:417 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:417 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;一緒に気持ちよくしようというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私のほうが気持ちよくしてるというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;煽りに対してのセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;いつもより感じてるねというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;お互いに気持ちいいねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;511,두개펠라
;-------------------------------------------------
	CASE 511
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:418 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:418 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:418 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:418 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:418 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:418 = 3
			ENDIF
			;それ以外
			IF (CFLAG:418 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:418 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;とても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;自分もしっかり気持ちよくしてセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;マ스타の페니스が当たって気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;二人分の정액飲ませてあげるセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;うまくなってきたねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;512,애널기승유방자위
;-------------------------------------------------
	CASE 512
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:419 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:419 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:419 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:419 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:419 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:419 = 3
			ENDIF
			;それ以外
			IF (CFLAG:419 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:419 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;513,입과바기나를범한다
;-------------------------------------------------
	CASE 513
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:439 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:439 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:439 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:439 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:439 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:439 = 3
			ENDIF
			;それ以外
			IF (CFLAG:439 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:439 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;ナカがとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;입がとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;どんどんイキなさい！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;とても気持ちよさそうセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;514,입과애널을범한다
;-------------------------------------------------
	CASE 514
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:440 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:440 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:440 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:440 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:440 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:440 = 3
			ENDIF
			;それ以外
			IF (CFLAG:440 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:440 = 2
			ENDIF
		ENDIF
		;조수구상호출関数
		;ナカがとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;입がとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;どんどんイキなさい！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;とても気持ちよさそうセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	PRINTFORML 
 
;-------------------------------------------------
;515,애널흐트러진모란
;-------------------------------------------------
	CASE 515
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:441 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:441 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:441 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:441 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:441 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:441 = 3
			ENDIF
			;それ以外
			IF (CFLAG:441 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:441 = 2
			ENDIF
		ENDIF
;=================================================
;600番台,비전투원コマンド
;=================================================
;-------------------------------------------------
;600,기초훈련
;-------------------------------------------------
	CASE 600
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:481 == 0
			PRINTFORML 
			LOCALS:0 =
			CFLAG:481 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:481 < 3 || FLAG:7 == 2)
				;연모、친애
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;함락済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:481 = 4
			ENDIF
			;굴복각인Lv3
			IF MARK:2 == 3 && (CFLAG:481 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:481 = 3
			ENDIF
			;それ以外
			IF (CFLAG:481 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				LOCALS:0 =
				CFLAG:481 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;601,시험소환
;-------------------------------------------------
	CASE 601
		CALL TRAIN_MESSAGE
		IF TALENT:서머너 && !TALENT:비전투원
			PRINTFORML 
		ENDIF
;-------------------------------------------------
;602,그림자추출
;-------------------------------------------------
	CASE 602
		CALL TRAIN_MESSAGE
		IF TALENT:페르소나구사자 && !TALENT:비전투원
			PRINTFORML 
;			;초기페르소나추가
			;ABL:초기페르소나 =
			
;			;초기페르소나능력변경
			;ABL:페르소나1데이터／스킬1 =
			
			;ABL:페르소나1데이터／습득스킬1 =
			;ABL:페르소나1데이터／습득LV1 =
 
		ENDIF
;-------------------------------------------------
;603,각성원조
;-------------------------------------------------
	CASE 603
		CALL TRAIN_MESSAGE
		IF TALENT:이능자 && !TALENT:비전투원
			PRINTFORML 
;			;능력변경
			;ABL:스킬1 = 
			
			;ABL:습득스킬1
			;ABL:습득LV1
 
		ENDIF
;-------------------------------------------------
;604,바이러스조사
;-------------------------------------------------
	CASE 604
		CALL TRAIN_MESSAGE
		IF TALENT:식노 && !TALENT:비전투원
			PRINTFORML 
		ENDIF
;-------------------------------------------------
;605,영매실험
;-------------------------------------------------
	CASE 605
		CALL TRAIN_MESSAGE
		IF TALENT:데빌시프터 && !TALENT:비전투원
			PRINTFORML 
;			;초기링크악마
			;CFLAG:초기링크악마
			
;			;초기링크악마능력변경
			;ABL:초기변신악마스킬1
			
			;ABL:초기변신악마습득스킬1
			;ABL:초기변신악마습득LV1
			
			;ABL:초기링크악마공격상성
			;ABL:초기링크악마최저공격횟수
			;ABL:초기링크악마최대공격횟수
			;ABL:초기링크악마사거리
			;ABL:초기링크악마공격범위
			;ABL:초기링크악마추가효과
			;ABL:초기링크악마추가효과상성
			;ABL:초기링크악마추가효과명중률
			;ABL:초기링크악마추가효과최대명중률
		ENDIF
 
;-------------------------------------------------
;606,개조수술
;-------------------------------------------------
	CASE 606
		CALL TRAIN_MESSAGE
		IF TALENT:기계의처녀 && !TALENT:비전투원
			PRINTFORML 
		ENDIF
 
;------------------------------------------------
;コマンド구상ここまで
;------------------------------------------------
	CASEELSE
		SIF FLAG:6 > 0
			CALL TRAIN_MESSAGE
ENDSELECT
 
FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:COUNT == 0
	PRINTW

;-------------------------------------------------
;윤간時の구상
;CASE 0 内は必ず通るようになっており
;RETURNで0を返すと、この後통상の윤간行動を出します
;CFLAGは使っていません
;COMF201_윤간内でLOCALを使っているので使いません
;-------------------------------------------------
@KOJO_MESSAGE_RINKAN_K4999, ARG:1 , ARG:2

FOR COUNT,0,51
	LOCALS:COUNT = 
NEXT

SELECTCASE ARG:1
	CASE 0
	;CASE 0はオリジナル윤간구상
	;C:一回あたりの帰宅人数、D：興奮度증가(及び減少)
	;CとDはオリジナル윤간구상内のみ使う
	;C += 、D += を使う
		IF ARG:2 == 1
		;ハードON
		
		ELSE
		;ハードOFF
		
		ENDIF
	;하드계
	;가슴を踏まれる（거유・폭유のみ）
	CASE 1				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
		ENDIF		
		;それ以外		
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
		ENDIF		
	;머리を踏まれる
	CASE 2
	
	;暴力を振るわれる
	CASE 3
	
	;タバコの根性焼き
	;興奮度が低く、반항적、꿋꿋함、프라이드높음、건방짐、저항を持っている
	CASE 4
	
	;地面に垂れた정액舐めとらせる
	;마조끼３이상
	CASE 5
	
	;体に落書きをされて犯される
	CASE 6
	
	;尻突き上げてバックから犯されつつ、머리を踏んづけられて屈辱で涙눈상태
	CASE 7
	
	;통상系
	;음란系で눈の前に棒並べられて、どれが欲しいか選ばせるとか（음란・창부のみ）
	CASE 8
	
	;거유이상의 キャラだと「その乳は飾りか？遊ばせてるんじゃねえ」とか罵られ파이즈리한다（거유・폭유のみ）
	CASE 9
	
	;挿れられるのだけは회피しようと必死に입と손で群がる짐ポにご봉사する（恋幕・친애のみ）
	CASE 10
	
	;;両穴突かれながら無理やり笑顔とピースサインとらされて記念촬영
	CASE 11
	
	;;輸姦後の後始末描写で、本人정액でどろどろにされてるのに周囲の男達の肉棒を舌で掃除させられてる
	CASE 12
	
	;中に出されても即座にほかの人に交代
	CASE 13
	
	;一斉사정で정액まみれ
	CASE 14
	
	;;両穴突かれて同時사정
	CASE 15
	
	;기승위+両脇コキ
	CASE 16
	
	;;입+マンコ
	CASE 17
	
ENDSELECT

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORML %LOCALS:COUNT%
NEXT
SIF LOCALS:0 != ""
	PRINTW
SIF LOCALS:0 != ""
	RETURN 1
RETURN 0
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係（4999をキャラ번호に置換）
;パラメータ변화などをトリガーにした구상　CFLAG 221～260を사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（4999をキャラ번호に置換）
;조교자の사정をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자の사정時の텍스트
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;-------------------------------------------------
;사정개소변경時の텍스트　有効時はここで関数종료
;-------------------------------------------------


IF CFLAG:106
	CALL PALAM_MESSAGE_A
	;머리카락
	IF CFLAG:106 == 1				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:221= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:221= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:221= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:221= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:221= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:221 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:221= 1	
		ENDIF		
	;顔
	ELSEIF CFLAG:106 == 2				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:222 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:222= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:222= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:222= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:222= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:222= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:222 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:222= 1	
		ENDIF		
	;가슴
	ELSEIF CFLAG:106 == 3				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:223 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:223= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:223= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:223= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:223= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:223= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:223 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:223= 1	
		ENDIF		
	;腹
	ELSEIF CFLAG:106 == 4				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:224 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:224= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:224= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:224= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:224= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:224= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:224 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:224= 1	
		ENDIF		
	;腋
	ELSEIF CFLAG:106 == 5				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:225 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:225= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:225= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:225= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:225= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:225= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:225 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:225= 1	
		ENDIF		
	;太腿
	ELSEIF CFLAG:106 == 6				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:226 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:226= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:226= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:226= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:226= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:226= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:226 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:226= 1	
		ENDIF		
	;割れ눈
	ELSEIF CFLAG:106 == 7				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:227 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:227= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:227= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:227= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:227= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:227= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:227 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:227= 1	
		ENDIF		
	;尻
	ELSEIF CFLAG:106 == 8				
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:228 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:228= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:228= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:228= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:228= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:228= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:228 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:228= 1	
		ENDIF		
	;ゴム
	ELSEIF CFLAG:106 == 9
		;섹스로사정かつ、애널섹스以外を実行時
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72 , 504 , 505 , 506 , 507)					
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:229 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:229= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:229= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:229= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:229= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:229= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:229 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:229= 1	
			ENDIF		
		ELSE					
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:230= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:230= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:230= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:230= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:230= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:230 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:230= 1	
			ENDIF		
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;손、입以外で사정
;対応コマンドは키스、스마타、발コキ、애널強制봉사、더블스마타、조수를범하게한다
;-------------------------------------------------
IF TFLAG:9 > 0
	CALL PALAM_MESSAGE_A
	;大量사정				
	IF TFLAG:9 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:230= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:230= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:230= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:230= 1	
		ENDIF		
	ENDIF
	;통상				
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:230= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:230= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:230= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:230= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:230 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:230= 1	
		ENDIF		
	ENDIF
ENDIF

;-------------------------------------------------
;조수를범한다로사정
;-------------------------------------------------
IF TFLAG:7 > 0
	CALL PALAM_MESSAGE_A			
	;大量사정					
	IF TFLAG:7 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:232= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:232= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:232= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:232= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:232= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:232= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:232= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:232 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:232= 1	
		ENDIF		
	ENDIF	
ENDIF

;-------------------------------------------------
;입으로사정
;-------------------------------------------------
IF TFLAG:0 > 0
	CALL PALAM_MESSAGE_A
	;이라마치오で사정
	IF SELECTCOM == 140			
		;大量사정						
		IF TFLAG:0 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	;それ以外の입사정
	ELSE
		;大量사정						
		IF TFLAG:0 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:233= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:233= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:233= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:233= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:233 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:233= 1	
			ENDIF		
		ENDIF	
	ENDIF
ENDIF

;-------------------------------------------------
;손으로사정
;-------------------------------------------------
IF TFLAG:1 > 0
	CALL PALAM_MESSAGE_A	
	;大量사정					
	IF TFLAG:1 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:234= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:234= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:234= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:234= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:234= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:234= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:234= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:234 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:234= 1	
		ENDIF		
	ENDIF	
ENDIF

;-------------------------------------------------
;가슴으로사정
;-------------------------------------------------
IF TFLAG:34 > 0
	CALL PALAM_MESSAGE_A		
	;大量사정					
	IF TFLAG:34 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:235= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:235= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:235= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:235= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:235= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:235= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:235= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:235 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:235= 1	
		ENDIF		
	ENDIF	
ENDIF
;-------------------------------------------------
;오나홀에사정
;-------------------------------------------------
IF TFLAG:16 == 1
	CALL PALAM_MESSAGE_A		
	;大量사정					
	IF TFLAG:16 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:236= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:236= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:236= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:236 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:236= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:236= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:236= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:236= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:236 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:236= 1	
		ENDIF		
	ENDIF	
ENDIF

;-------------------------------------------------
;섹스로사정
;-------------------------------------------------
IF TFLAG:2 > 0
	CALL PALAM_MESSAGE_A
	;애널섹스系
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 512
		;大量사정						
		IF TFLAG:2 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:237= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:237= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:237= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:237= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:237= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:237= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:237= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:237= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:237 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:237= 1	
			ENDIF		
		ENDIF
	;奥攻め섹스系
	ELSEIF SELECTCOM == 506 || SELECTCOM == 507
		;大量사정						
		IF TFLAG:2 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:238= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:238= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:238= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:238 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:238= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:238= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:238= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:238= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:238= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:238 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:238= 1	
			ENDIF		
		ENDIF
	;그외섹스系
	ELSE
		;大量사정						
		IF TFLAG:2 > 1
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:239= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:239= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:239= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:239 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:239= 1	
			ENDIF		
		ENDIF	
		;통상						
		IF LOCALS:0 == ""
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)		
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
					CFLAG:239= 4
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
					CFLAG:239= 3
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
					CFLAG:239= 3
				ENDIF	
			ENDIF		
			;굴복각인Lv3		
			IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
				LOCALS:0 = 	
				CFLAG:239= 2	
			ENDIF		
			;それ以外		
			IF (CFLAG:239 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
				PRINTFORML 
				CFLAG:239= 1	
			ENDIF		
		ENDIF	
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로사정させた
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A		
	;大量사정					
	IF TFLAG:10 > 1
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:240= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:240= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:240= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
	;통상					
	IF LOCALS:0 == ""
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)		
			;연모、친애	
			IF (TALENT:3 || TALENT:6)	
				LOCALS:0 = 
				CFLAG:240= 4
			;음란、창부	
			ELSEIF (TALENT:4 || TALENT:7)	
				LOCALS:0 = 
				CFLAG:240= 3
			;복종、예속	
			ELSEIF (TALENT:5 || TALENT:8)	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				LOCALS:0 = 
				CFLAG:240= 3
			ENDIF	
		ENDIF		
		;굴복각인Lv3		
		IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
			LOCALS:0 = 	
			CFLAG:240= 2	
		ENDIF		
		;それ以外		
		IF (CFLAG:240 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
			PRINTFORML 
			CFLAG:240= 1	
		ENDIF		
	ENDIF	
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:COUNT == 0
	PRINTW

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（4999をキャラ번호に置換）
;촉수の사정をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수の사정時の텍스트
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;-------------------------------------------------
;촉수가사정
;-------------------------------------------------
IF TFLAG:15 >0
	CALL PALAM_MESSAGE_E			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:241 < 3 || FLAG:7 == 2)		
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:241= 4
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:241= 3
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:241= 3
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:241= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:241 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:241= 2	
	ENDIF		
	;それ以外		
	IF (CFLAG:241 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:241= 1	
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:COUNT == 0
	PRINTW
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B関係（4999をキャラ번호に置換）
;奴隷の절정をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;奴隷の절정時の텍스트
;-------------------------------------------------
;-------------------------------------------------
;初めてC절정
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:242 == 0
	PRINTFORMW 
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;初めてV절정
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:243 == 0
	PRINTFORMW 
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;初めてA절정
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:244 == 0
	PRINTFORMW 
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;初めてB절정
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:245 == 0
	PRINTFORMW 
	CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;절정
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT

;強절정
IF TFLAG:29 > 1
	CALL PALAM_MESSAGE_B			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)		
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:246= 4
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:246= 3
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:246= 2	
	ENDIF		
	;それ以外		
	IF (CFLAG:246 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:246= 1	
	ENDIF		
;절정
ELSEIF TFLAG:29 > 0
	CALL PALAM_MESSAGE_B			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)		
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:246= 4
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:246= 3
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:246= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:246= 2	
	ENDIF		
	;それ以外		
	IF (CFLAG:246 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:246= 1	
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;사정
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;사정している
IF TFLAG:10 > 0				
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:247 < 3 || FLAG:7 == 2)		
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:247= 4
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:247= 3
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:247= 3
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:247= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:247= 2	
	ENDIF		
	;それ以外		
	IF (CFLAG:247 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:247= 1	
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;분유
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;분유している
IF TFLAG:11 > 0			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:248 < 3 || FLAG:7 == 2)		
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:248= 4
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:248= 3
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:248= 3
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:248= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:248= 2	
	ENDIF		
	;それ以外		
	IF (CFLAG:248 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:248= 1	
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;방뇨
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
;방뇨している
IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)			
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:249 < 3 || FLAG:7 == 2)		
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
			CFLAG:249= 4
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
			CFLAG:249= 3
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
			CFLAG:249= 3
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""	
			LOCALS:0 = 
			CFLAG:249= 3
		ENDIF	
	ENDIF		
	;굴복각인Lv3		
	IF MARK:2 == 3 && (CFLAG:249 < 2 || FLAG:7 == 2) && LOCALS:0 == ""		
		LOCALS:0 = 	
		CFLAG:249= 2	
	ENDIF		
	;それ以外		
	IF (CFLAG:249 < 1 || FLAG:7 == 2) && LOCALS:0 == ""		
		PRINTFORML 
		CFLAG:249= 1	
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C関係（4999をキャラ번호に置換）
;조교後の변화をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교後の텍스트
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀のみ)
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT
IF TFLAG:3 > 0
	CALL PALAM_MESSAGE_C
	;연모か친애かつ主人の섹스による처녀상실
	IF TFLAG:20
		PRINTFORMW 
	;それ以外の時に처녀상실
	ELSE			
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
		ENDIF	
		;굴복각인Lv3
		IF MARK:2 == 3 && LOCALS:0 == ""
			LOCALS:0 = 
		ENDIF
		;共通	
		IF LOCALS:0 == ""	
			PRINTFORML 
		ENDIF	
	ENDIF
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:COUNT == 0
	PRINTW

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D関係（4999をキャラ번호に置換）
;パラメータ변화をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;パラメータ변화時の텍스트
;-------------------------------------------------
;-------------------------------------------------
;初めて윤활がLV3超えた
;-------------------------------------------------
P = PALAM:4 + CUP:4
IF P > PALAMLV:4 && CFLAG:250 == 0
	CALL PALAM_MESSAGE_D
	;로션を使った場合
	IF SELECTCOM == 180
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;初めて욕정がLV2超えた
;-------------------------------------------------
P = PALAM:6 + CUP:6
IF P > PALAMLV:3 && CFLAG:251 == 0
	CALL PALAM_MESSAGE_D
	;미약を使った場合
	IF SELECTCOM == 181
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;初めて굴복がLV3超えた
;-------------------------------------------------
P = PALAM:7 + CUP:7
IF P > PALAMLV:3 && CFLAG:252 == 0
	CALL PALAM_MESSAGE_D
	PRINTFORMW 
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;初めて복종がLV3超えた
;-------------------------------------------------
P = PALAM:5 + CUP:5
IF P > PALAMLV:3 && CFLAG:253 == 0
	CALL PALAM_MESSAGE_D
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:253 = 1
ENDIF

;-------------------------------------------------
;初めて공포がLV3超えた
;-------------------------------------------------
P = PALAM:11 + CUP:11
IF P > PALAMLV:3 && CFLAG:254 == 0
	CALL PALAM_MESSAGE_D
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:254 = 1
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG関係（4999をキャラ번호に置換）
;각인変動をトリガーにした구상
;각인変動後に구상を発動します
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K4999
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の아야を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K4999
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K4999
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT

IF TFLAG:22 == 3
	CALL MARK_MESSAGE_A	
	;연모、친애	
	IF (TALENT:3 || TALENT:6)	
		LOCALS:0 = 
	;음란、창부	
	ELSEIF (TALENT:4 || TALENT:7)	
		LOCALS:0 = 
	;복종、예속	
	ELSEIF (TALENT:5 || TALENT:8)	
		LOCALS:0 = 
	ENDIF	
	;共通
	IF LOCALS:0 == ""		
		PRINTFORML 
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT

IF TFLAG:23 == 3
	CALL MARK_MESSAGE_P
	;연모、친애	
	IF (TALENT:3 || TALENT:6)	
		LOCALS:0 = 
	;복종、예속	
	ELSEIF (TALENT:5 || TALENT:8)	
		LOCALS:0 = 
	ENDIF	
	;共通
	IF LOCALS:0 == ""		
		PRINTFORML 
	ENDIF		
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	CALL MARK_MESSAGE_S
	;쾌락による굴복
	IF TFLAG:24 == 13
		PRINTFORMW 
	;공포と고통による굴복
	ELSEIF TFLAG:24 == 23
		PRINTFORMW 
	;굴복と복종による굴복
	ELSEIF TFLAG:24 == 33
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT

IF TFLAG:21 == 3
	CALL MARK_MESSAGE_R
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모、친애	
		IF (TALENT:3 || TALENT:6)	
			LOCALS:0 = 
		;음란、창부	
		ELSEIF (TALENT:4 || TALENT:7)	
			LOCALS:0 = 
		;복종、예속	
		ELSEIF (TALENT:5 || TALENT:8)	
			LOCALS:0 = 
		ENDIF	
		;함락済み共通	
		IF LOCALS:0 == ""
			LOCALS:0 = 
		ENDIF	
	ENDIF
	;それ以外
	IF LOCALS:0 == ""
		PRINTFORML 
	ENDIF
ENDIF

FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCALS:0 != ""
	PRINTW
;-------------------------------------------------
;이벤트구상集（4999をキャラ번호に置換する） CFLAG 261～299を사용
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_K4999

;--------------------------------------------------
;特定主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、4999を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K4999
;	RETURN 0
;ENDIF

FOR COUNT,0,51
	LOCALS:COUNT = 
	LOCAL:COUNT = 0
NEXT

SELECTCASE TFLAG:13
		;-------------------------------------------------
		;朝フェラ
		;-------------------------------------------------
	CASE 1
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:261 = 1
			ENDIF

		;-------------------------------------------------
		;朝파이즈리
		;-------------------------------------------------
	CASE 2
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
				;모유체질
				IF TALENT:149						
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;모유체질じゃない
				IF LOCALS:0 == ""
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						PRINTFORML 
					ENDIF	
				ENDIF
				CFLAG:262 = 1
			ENDIF

		;-------------------------------------------------
		;朝기승위
		;-------------------------------------------------
	CASE 3
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;朝기승위A
		;-------------------------------------------------
	CASE 24
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;朝키스 연모、친애前提
		;-------------------------------------------------
	CASE 25
			CALL EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;朝食
		;-------------------------------------------------
	CASE 26
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
				;歌いながら
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2						
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;歌ってない
				IF LOCALS:0 == ""
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						PRINTFORML 
					ENDIF	
				ENDIF
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;起きたら隣で寝てる 연모、친애前提
		;-------------------------------------------------
	CASE 27
			CALL EVENT_MESSAGE
			IF CFLAG:263 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;朝の발コキ
		;-------------------------------------------------
	CASE 28
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;朝の搾りたてミルク
		;-------------------------------------------------
	CASE 29
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;;お寝小
		;-------------------------------------------------
	CASE 30
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:263 = 1
			ENDIF

		;-------------------------------------------------
		;조교後레즈プレイ
		;-------------------------------------------------
	CASE 4
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:264 == 0
				PRINTFORML 
				CFLAG:264 = 1
			;それ以外
			ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:264 = 1
			ENDIF

		;-------------------------------------------------
		;조교後섹스
		;-------------------------------------------------
	CASE 5
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:265 = 1
			ENDIF

		;-------------------------------------------------
		;강정신약の反動
		;-------------------------------------------------
	CASE 6
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:266 = 2
			ENDIF
			IF (CFLAG:266 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:266 = 1
			ENDIF

		;-------------------------------------------------
		;조교後착유
		;-------------------------------------------------
	CASE 7
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:267 = 2
			ENDIF
			IF (CFLAG:267 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:267 = 1
			ENDIF

		;-------------------------------------------------
		;조교後자위
		;-------------------------------------------------
	CASE 8
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:268 = 2
			ENDIF
			IF (CFLAG:268 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:268 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（主人C 相手V）
		;-------------------------------------------------
	CASE 9
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（主人C 相手A）
		;-------------------------------------------------
	CASE 31
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（主人V 相手C）
		;-------------------------------------------------
	CASE 32
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（主人A 相手C）
		;-------------------------------------------------
	CASE 33
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;요바이（両者C（스마타）・両者C（貝合わせ））
		;-------------------------------------------------
	CASE 34
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;夜자위
		;-------------------------------------------------
	CASE 35
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					PRINTFORML 
				ENDIF	
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;一緒に寝る 연모、친애
		;-------------------------------------------------
	CASE 36
			CALL EVENT_MESSAGE
			IF CFLAG:269 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:269 = 1
			ENDIF

		;-------------------------------------------------
		;황색사탕사용時
		;-------------------------------------------------
	CASE 10
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					CALL EVENT_MESSAGE
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 자신의 부푼 아랫배를 부드럽게 어루만지고 뜻을 굳힌 후, 입 속에 레몬 색으로 빛나는 사탕을 던져 넣었다…
					PRINTFORML …………
					PRINTFORMW …………
					PRINTFORML …………
					PRINTFORMW …………
					PRINTFORMW 아랫배에서 내장·자궁을 뒤트는 듯한 통증이 올라온다…
					PRINTFORMW 사탕에 포함된 대량의 진통성분이 단속적으로 덮치는 격통을 억누르고、
					PRINTFORMW 얕은 호흡을 반복하면서 몇번이나 아랫배에 힘을 주어 격통의 근원을 밀어낸다…
					PRINTFORML …………
					PRINTFORMW …………
					PRINTFORMW 
					PRINTFORML …………
					PRINTFORMW …………
					PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%、\@ TALENT:남자 == 0 ? 질구 # 국문\@에서 두꺼운 껍질로 싸인 알을 낳고는, 의식을 잃었다…
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:270 = 2
						
			ENDIF
			IF (CFLAG:270 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORMW 당신은 자신의 부푼 아랫배를 부드럽게 어루만지고 뜻을 굳힌 후, 입 속에 레몬 색으로 빛나는 사탕을 던져 넣었다……
				PRINTFORML …………
				PRINTFORMW …………
				PRINTFORML …………
				PRINTFORMW …………
				PRINTFORMW 하복부에서 날카로운 아픔이 밀려온다…！
				PRINTFORML …………
				PRINTFORMW …………
				PRINTFORML …………
				PRINTFORMW …………
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 심한 진통에 시달리면서도 두터운 껍질에 싸인 알을 낳았다…
				CFLAG:270 = 1
			ENDIF

		;-------------------------------------------------
		;임신発깨달음時
		;-------------------------------------------------
	CASE 11
			SIF CFLAG:271 >= 1 && FLAG:7 != 2
				RETURN 0
			CALL EVENT_MESSAGE
			;임신대상をTARGETに格納し、父親は奴隷はV、조수はWとする
				;당신が女
				IF EXP:MASTER:출산경험 == 0
					IF TALENT:MASTER:남자 == 1
					;페니스の有無체크
						;페니스がある
						;당신は후타나리
						IF HAVE_PENIS(MASTER) || TALENT:MASTER:후타나리
							PRINTFORMW 
						;당신は女
						ELSE
							PRINTFORMW 
						ENDIF
					;당신が오토코노코
					ELSEIF TALENT:MASTER:오토코노코
						PRINTFORMW 
					ENDIF
				ELSEIF EXP:MASTER:출산경험 == 1
					IF TALENT:MASTER:남자 == 1
					;페니스の有無체크
						;페니스がある
						;당신は후타나리
						IF HAVE_PENIS(MASTER) || TALENT:MASTER:후타나리
							PRINTFORMW 
						;당신は女
						ELSE
							PRINTFORMW 
						ENDIF
					;당신が오토코노코
					ELSEIF TALENT:MASTER:오토코노코
						PRINTFORMW 
					ENDIF
				ELSEIF EXP:MASTER:출산경험 > 1
					IF TALENT:MASTER:남자 == 1
					;페니스の有無체크
						;페니스がある
						;당신は후타나리
						IF HAVE_PENIS(MASTER) || TALENT:MASTER:후타나리
							PRINTFORMW 
						;당신は女
						ELSE
							PRINTFORMW 
						ENDIF
					;당신が오토코노코
					ELSEIF TALENT:MASTER:오토코노코
						PRINTFORMW 
					ENDIF
				ELSEIF EXP:MASTER:출산경험 >= 3
					IF TALENT:MASTER:남자 == 1
					;페니스の有無체크
						;페니스がある
						;당신は후타나리
						IF HAVE_PENIS(MASTER) || TALENT:MASTER:후타나리
							PRINTFORMW 
						;당신は女
						ELSE
							PRINTFORMW 
						ENDIF
					;당신が오토코노코
					ELSEIF TALENT:MASTER:오토코노코
						PRINTFORMW 
					ENDIF
				ENDIF
			;붕괴してしまった場合
			IF TALENT:9 == 1
				PRINTFORMW 
			;父親が主人で母親が연모か친애持ち
			ELSEIF (TALENT:3 || TALENT:6) && CFLAG:102 == 1
				PRINTFORMW 
			;父親が奴隷か조수で母親が음란か창부持ち
			ELSEIF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3)
				;父親(조수）との相性が125より大きい場合
				IF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
					PRINTFORML 
			 	;父親(奴隷）との相性が125より大きい場合
				ELSEIF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125
					PRINTFORML 
				ELSE
					PRINTFORML 
				ENDIF
			;복종か예속持ち
			ELSEIF (TALENT:5 || TALENT:8)
				PRINTFORML 
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:271 = 1

		;-------------------------------------------------
		;출산時
		;-------------------------------------------------
	CASE 12
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)					
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:272 = 2
			ENDIF
			IF (CFLAG:272 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:272 = 1
			ENDIF

		;-------------------------------------------------
		;육아실
		;-------------------------------------------------
	CASE 13
			CALL EVENT_MESSAGE
			;출산前
			IF TALENT:153
				;함락済み
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
						PRINTFORML 임신한 몸이라고는 해도 왕성한 성욕을 가진 %조사처리(CALLNAME:MASTER,"는")% 
						PRINTFORMW 빨리 아이를 낳고 편안한 몸으로 섹스를 하고 싶다는 음정이 타오르고 있었다…
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
					CFLAG:273 = 2
				ENDIF
				IF (CFLAG:273 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORMW 아직 보지 못한 아이를 상상하면서 %조사처리(CALLNAME:MASTER,"는")% 누워 있다…
					CFLAG:273 = 1
				ENDIF
			;출산後
			ELSE
				;함락済み
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
					CFLAG:273 = 4
				ENDIF
				IF (CFLAG:273 < 3 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML 
					CFLAG:273 = 3
				ENDIF
			ENDIF

		;-------------------------------------------------
		;親離れ時
		;-------------------------------------------------
	CASE 14
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:274 = 2
			ENDIF
			IF (CFLAG:274 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORMW 아이를 낳은 %조사처리(CALLNAME:MASTER,"는")% 살짝 외로움에 휩싸였다…
				CFLAG:274 = 1
			ENDIF
		;-------------------------------------------------
		;定食포장마차時
		;-------------------------------------------------
	CASE 15
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:275 == 0
				PRINTFORML 
				CFLAG:275 = 1
			;それ以外
			ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
				;메이드
				IF CFLAG:MASTER:72 == 9
					PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 메이드로서 
					IF ABL:요리기능 == 0
						IF RAND:3 == 0
							PRINTFORMW 어쨌든 열심히 일하기로 했다…
						ELSEIF RAND:2 == 0
							PRINTFORMW 나름대로 열심히 일하기로 했다…
						ELSE
							PRINTFORMW 적극적으로 일하기로 했다…
						ENDIF
					ELSEIF ABL:요리기능 >= 5
						IF RAND:3 == 0
							PRINTFORMW 화려한 몸놀림으로 일하기로 했다…
						ELSEIF RAND:2 == 0
							PRINTFORMW 우아한 행동거지로 일하기로 했다…
						ELSE
							PRINTFORMW 주위에 지시를 하며 냉정하게 일하기로 했다…
						ENDIF
					ELSEIF ABL:요리기능 >= 1
						IF RAND:4 == 0
							PRINTFORMW 어쨌든 열심히 일하기로 했다…
						ELSEIF RAND:3 == 0
							PRINTFORMW 적확한 상황판단을 하면서 일하기로 했다…
						ELSEIF RAND:2 == 0
							PRINTFORMW 주위와 협조하며 일하기로 했다…
						ELSE
							PRINTFORMW 적극적으로 일을 찾아내 일하기로 했다…
						ENDIF
					ENDIF
				;執事
				ELSEIF CFLAG:MASTER:72 == 10
					PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 원래 직업인 집사로서 
					IF ABL:요리기능 == 0
						IF RAND:3 == 0
							PRINTFORMW 주위와 공동으로 일하기로 했다…
						ELSEIF RAND:2 == 0
							PRINTFORMW 나름대로 상황을 보고 일하기로 했다…
						ELSE
							PRINTFORMW 어쨌든 열심히 일하기로 했다…
						ENDIF
					ELSEIF ABL:요리기능 >= 5
						IF RAND:3 == 0
							PRINTFORMW 화려한 몸놀림과 지시로 일하기로 했다…
						ELSEIF RAND:2 == 0
							PRINTFORMW 우아한 행동 거지에서 일하기로 했다…
						ELSE
							PRINTFORMW 엘레강스하고 지능적으로 일하기로 했다…
						ENDIF
					ELSEIF ABL:요리기능 >= 1
						IF RAND:4 == 0
							PRINTFORMW 어쨌든 열심히 일하기로 했다…
						ELSEIF RAND:3 == 0
							PRINTFORMW 적확한 상황판단을 하면서 일하기로 했다…
						ELSEIF RAND:2 == 0
							PRINTFORMW 주위와 협조하며 일하기로 했다…
						ELSE
							PRINTFORMW 적극적으로 일을 찾아내 일하기로 했다…
						ENDIF
					ENDIF
				ENDIF
				CFLAG:275 = 1
			ENDIF

		;-------------------------------------------------
		;창관でバイト・설정時
		;-------------------------------------------------
	CASE 16
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:276 == 0
				CALL EVENT_MESSAGE
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 마음을 다잡고 매춘하기로 결정했다…
				SIF TALENT:MASTER:처녀
					PRINTFORMW 아직 남자를 받아들인 적 없는 %CALLNAME:MASTER%에게 있어 그것은 중대한 결심이었다…
				CFLAG:276 = 1
			;それ以外
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				IF TALENT:음란 || TALENT:창부
					PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 매춘부의 부업을 하기로 했다…
					PRINT 넘치는 육욕이 충족되는 것을 기대하며 
					IF TALENT:MASTER:후타나리
						PRINTFORMW 음경은 달콤하게 발기해 여자의 부분은 애액을 분비하기 시작했다…
					ELSEIF HAVE_PENIS(MASTER)
						IF ABL:5 >= 3
							PRINTFORMW 항문과 음경이 강하게 욱신거리기 시작했다…
						ELSE
							PRINTFORMW 음경이 강하게 욱신거리기 시작했다…
						ENDIF
					ELSEIF HAVE_VAGINA(MASTER)
						IF ABL:5 >= 3
							PRINTFORMW 항문과 질육이 강하게 욱신거리기 시작했다…
						ELSE
							PRINTFORMW 질육이 강하게 욱신거리기 시작했다…
						ENDIF
					ENDIF
				ELSE
					PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 매춘부로서 일하기로 했다…
				ENDIF
				IF TALENT:MASTER:임신
					PRINTFORMW 
				ELSE
					PRINTFORMW 
				ENDIF
				CFLAG:276 = 1
			ENDIF
	CASE 62
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 마음을 다잡고 매춘하기로 결정했다…
		;-------------------------------------------------
		;창관でバイト・実行時
		;-------------------------------------------------
	CASE 63
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:606 == 0
				PRINTFORMW 긴장하면서도 첫 창관 데뷔를 마쳤다…
				CFLAG:606 = 1
			;それ以外
			ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
				PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 목욕과 화장을 마치고 머리카락 옷매무새를 고친 후 
				;복장を整える処理を추가
				CALL CLOTHES_TRAIN,MASTER
					;■出勤前
					;당신が女
					IF !TALENT:MASTER:남자
					;페니스の有無체크
						;페니스がある
						;당신は후타나리
						IF HAVE_PENIS(MASTER) || TALENT:MASTER:후타나리
							;음란、창부
							IF TALENT:MASTER:4 || TALENT:MASTER:7
								IF TALENT:MASTER:임신
									PRINTFORMW 완만하게 부풀어오른 배를 누르면서、기쁜듯이 창관으로 출근했다…
								ELSE
									PRINTFORMW 기쁜듯이 창관으로 출근했다…
								ENDIF
							ELSE
								IF TALENT:MASTER:임신
									PRINTFORMW 완만하게 부풀어오른 배를 누르면서、창관으로 출근했다…
								ELSE
									PRINTFORMW 창관으로 출근했다…
								ENDIF
							ENDIF
						;당신は女
						ELSE
							;음란、창부
							IF TALENT:4 || TALENT:7
								IF TALENT:MASTER:임신
									PRINTFORMW 완만하게 부풀어오른 배를 누르면서、기쁜듯이 창관으로 출근했다…
								ELSE
									PRINTFORM 기쁜듯이 창관으로 출근했다…
								ENDIF
							ELSE
								IF TALENT:MASTER:임신
									PRINTFORMW 완만하게 부풀어오른 배를 누르면서、창관으로 출근했다…
								ELSE
									PRINTFORMW 창관으로 출근했다…
								ENDIF
							ENDIF
						ENDIF
					;당신が오토코노코
					ELSEIF TALENT:MASTER:오토코노코
						IF TALENT:4 || TALENT:7
							PRINTFORMW 기쁜듯이 창관으로 출근했다…
						ELSE
							PRINTFORMW 흐뭇하게 창관에 출근했다…
						ENDIF
					;당신は男
					ELSE
						IF TALENT:4 || TALENT:7
							PRINTFORMW 기쁜듯이 창관으로 출근했다…
						ELSE
							PRINTFORMW 창관에 출근했다…
						ENDIF
					ENDIF
					;■出勤中に視姦される
					IF (TALENT:MASTER:음란 || TALENT:MASTER:창부) && !IS_LOOKSLIKE_MALE(MASTER)
						PRINT 출근길
						IF RAND:6 == 0
							PRINT 에 탄 전차 내에서 
						ELSEIF RAND:5 == 0
							PRINT 가는 도중 계단의 뒤에서
						ELSEIF RAND:4 == 0
							PRINT 의 중간――인적 드문 뒷골목――에서 
						ELSEIF RAND:3 == 0
							PRINT 의 대학가에서 
						ELSEIF RAND:2 == 0
							PRINT 의 비즈니스 거리에서 
						ELSE
							PRINT 의 택시 승강장에서 
						ENDIF
						IF RAND:6 == 0
							PRINT 동아리의 학생
						ELSEIF RAND:5 == 0
							PRINT 학원에서 돌아오는 소년
						ELSEIF RAND:4 == 0
							PRINT 호스트풍의 잘생긴 남자
						ELSEIF RAND:3 == 0
							PRINT 억센 풍모의 야쿠자
						ELSEIF RAND:2 == 0
							PRINT 영업으로 돌아오는 지친 샐러리 맨
						ELSE
							PRINT 기름기 도는 중년 남자
						ENDIF
						PRINTFORMW 에게 성적인 시선을 받았다…
						;■衣装구상
						;세일러복
						IF TEQUIP:MASTER:옷 == 102 && EQUIP:MASTER:옷
							IF RAND:2 == 0
								PRINTL 다니는 학교의 교복은 그 안의 젊은 육체를 상상하게 된다…
							ELSE
								PRINTL 위장용 교복이 그 안의 싱싱한 육체를 상상하게 된다…
							ENDIF
						;블레이저교복
						ELSEIF TEQUIP:MASTER:겉옷 == 1003
							IF RAND:2 == 0
								PRINTL 다니는 학교의 교복은 그 안의 젊은 육체를 상상하게 된다…
							ELSE
								PRINTL 위장용 교복이 그 안의 싱싱한 육체를 상상하게 된다…
							ENDIF
						;메이드
						ELSEIF TEQUIP:MASTER:전신복 == 308
						;フレンチ메이드
						ELSEIF TEQUIP:MASTER:전신복 == 309
						;체육복＋블루머
						ELSEIF TEQUIP:MASTER:옷 == 103 && TEQUIP:MASTER:하의 == 211
							IF RAND:2 == 0
								PRINTL 거리를 다니기에는 어울리지 않는 모습이지만、튀어나올 듯한 둔부가 입은 자의 성을 어필하고 있다…
							ELSE
								PRINTL 거리를 다니기에는 어울리지 않는 모습이지만、노출된 다리 부분이 요염한 육체를 어필하고 있다…
							ENDIF
						;드레스、이브닝드레스
						ELSEIF TEQUIP:MASTER:전신복 == 304 && TEQUIP:MASTER:전신복 == 305
						;ギャル系私옷（핫팬츠に캐미솔or탱크탑）
						ELSEIF TEQUIP:MASTER:하의 == 206 && (TEQUIP:MASTER:옷 == 108 || TEQUIP:MASTER:옷 == 117)
							IF RAND:2 == 0
								PRINTL 화려하고 노출이 많은 옷은 남자의 욕망을 부추긴다…
							ELSE
								PRINTL 가슴이나 엉덩이 등 여성적인 부분을 노출, 강조하는 패션은 욕망을 끓어오르게 한다…
							ENDIF
						;お嬢様系私옷（서스펜더스커트）
						ELSEIF TEQUIP:MASTER:하의 == 213
						;エロマダム（強調下着）
						ELSEIF TEQUIP:MASTER:전신속옷 == 718
							PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 
							IF TALENT:MASTER:오토코노코
								PRINT 여성에 가까운 섬세한 몸을 
							ELSEIF TALENT:MASTER:빈유
								PRINT 살짝 도톰한 가슴을 
							ELSEIF TALENT:MASTER:절벽
								PRINT 유아처럼 납작한 가슴을 
							ELSEIF TALENT:MASTER:거유
								PRINT 휘어지며 흔들리는 두개의 가슴살을 감싸며 골짜기가 깊어지는 음란한 가슴을 
							ELSEIF TALENT:MASTER:폭유
								PRINT 허리둘레를 넘어서 브래지어를 벗어날 듯한 유량의 과실을 
								IF RAND:2 == 0
									PRINT 아래에서 끌어올려 
								ELSE
									PRINT 잡아올려 
								ENDIF
								PRINT 음탕한 분위기로 
							ELSE
								;普通のおっぱい
								PRINT 유방을 
							ENDIF
							PRINTL 강조하고, 
							IF RAND:4 == 0
								PRINT 복잡한 무늬로 바느질한 자수, 섬세한 시스루 레이스가 들어간 
								CALL COLOR_PATTERN
								PRINTW 의 욕정을 불러일으키는 란제리를 입고 있다…
							ELSEIF RAND:3 == 0
								PRINT 브래지어와 팬티의 각처를 리본·장미·프릴 등으로 장식한 
								CALL COLOR_PATTERN
								PRINTW 의 란제리를 입고 있다…
							ELSEIF RAND:2 == 0
								PRINT 성기 부분이 잘 비치는 
								CALL COLOR_PATTERN
								PRINTW 의 란제리를 입고 있다…
							ELSE
								PRINT 
								CALL COLOR_PATTERN
								PRINTW 의 란제리를 입고 있다…
							ENDIF
						;기모노
						ELSEIF TEQUIP:MASTER:전신복 == 301
						;유카타
						ELSEIF TEQUIP:MASTER:옷 == 118
						;바니수트
						ELSEIF TEQUIP:MASTER:전신복 == 311
						;차이나드레스
						ELSEIF TEQUIP:MASTER:전신복 == 310
						;원피스수영복
						ELSEIF TEQUIP:MASTER:전신속옷 == 716
						;비키니수영복
						ELSEIF TEQUIP:MASTER:전신속옷 == 715
						;경주용수영복
						ELSEIF TEQUIP:MASTER:전신속옷 == 713
						;ゴスロリ（黒）
						ELSEIF TEQUIP:MASTER:옷 == 122
							IF RAND:2 == 0
								PRINT 흑색 기조의 프릴과 리본은 귀여우면서도 어딘가 선정적이다…
							ELSE
								PRINTFORM 검은 고딕 로리타가 %마스터를% 더 귀엽게 보이게 한다…
							ENDIF
						;ゴスロリ（白）
						ELSEIF TEQUIP:MASTER:옷 == 123
							IF RAND:2 == 0
								PRINT 백색 기조의 프릴과 리본은 귀여우면서도 어딘가 선정적이다…
							ELSE
								PRINTFORM 하얀 고딕 로리타가 %마스터를% 더 귀엽게 보이게 한다…
							ENDIF
						;ここで一度切る
						ENDIF
						;그외（要するに普通の옷）
						ELSE
							;■上半身(옷、전신복、겉옷に類するものを着ているかどうか)
							IF EQUIP:21 > 0 || EQUIP:28 > 0
							ENDIF
							;■下半身
							;스커트系統
							IF TEQUIP:111 >= 10
								IF RAND:2 == 0
									PRINT 시선을 눈치채지 못하고 
								ELSE
									PRINT 시선을 알아차리지 못한 채 
								ENDIF
								IF RAND:2 == 0
									PRINTFORM 옷을 만지작거리는 %CALLNAME:MASTER%
								ELSE
									PRINTFORM 생각을 하고 있는 %CALLNAME:MASTER%
								ENDIF
								IF RAND:2 == 0
									PRINT 의 
								ELSE
									PRINTFORM %조사만처리(CALLNAME:MASTER,"가")% 
								ENDIF
								;스커트、미니스커트、타이트스커트、タイトミニを選んでいる
								IF TEQUIP:MASTER:하의 == 201 && EQUIP:MASTER:하의 > 0
									PRINTL 입은 
									IF RAND:3 == 0
										PRINT 무릎 위 프리티
									ELSEIF RAND:2 == 0
										PRINT 무릎 위 
									ELSE
										PRINT 체크 
									ENDIF
									PRINT 스커트로부터 
								ELSEIF TEQUIP:MASTER:하의 == 207
									PRINTFORML 입은 
									IF RAND:2 == 0
										PRINT 허벅지까지 올라오는 
									ELSE
										PRINT 체크 
									ENDIF
									PRINT 미니스커트에서 
								ELSEIF TEQUIP:MASTER:하의 == 208
									PRINTFORML 입은 
									IF RAND:2 == 0
										PRINT 엉덩이 모양이 잘 보이는 
									ELSE
										PRINT 체크무늬의 
									ENDIF
									PRINT 타이트스커트로부터 
								ELSEIF TEQUIP:MASTER:하의 == 209
									PRINTFORML 입은 
									IF RAND:2 == 0
										PRINT 볼기짝이 튀어나올 것 같은 
									ELSE
										PRINT 아슬아슬한 
									ENDIF
									PRINT 타이트미니에서 
								ELSEIF TEQUIP:MASTER:전신복 == 303
									PRINTFORML 입고 있는 
									IF RAND:5 == 0
										PRINT 자수가 놓인 
									ELSEIF RAND:4 == 0
										PRINT 유행하는 코디네이트로서 인기인 
									ELSEIF RAND:3 == 0
										PRINT 어른스럽고 세련된 디자인의 
									ELSEIF RAND:2 == 0
										PRINT 폭신폭신한 라인의 
									ELSE
										PRINT 위태로운 
									ENDIF
									PRINT 원피스로부터 
								ELSE
									;하의
									IF CFLAG:MASTER:GET_CLOTHESNAME(2) > 0
										CALL CLOTHES_NAME,2,MASTER
										IF TEQUIP:111 >= 10
											PRINTL 로부터 
										ELSE
											PRINTL 의 옷자락으로부터 
										ENDIF
									;전신복
									ELSEIF CFLAG:MASTER:GET_CLOTHESNAME(3) > 0
										CALL CLOTHES_NAME,3,MASTER
										IF TEQUIP:111 >= 10
											PRINTL 로부터 
										ELSE
											PRINTL 의 옷자락으로부터 
										ENDIF
									ENDIF
								ENDIF
								;속옷（하）（팬티、レース付き）
								IF (TEQUIP:MASTER:86 == 602 || TEQUIP:MASTER:86 == 611) && EQUIP:MASTER:27 == 0
									IF RAND:2 == 0
										CALL COLOR_PATTERN
										PRINT 의 
									ELSE
										CALL DESIGN_PANTY_K4999
										PRINT 의 
									ENDIF
									;팬티
									IF TEQUIP:MASTER:속옷（하） == 602 || TEQUIP:MASTER:속옷（하） == 609 || TEQUIP:MASTER:속옷（하） == 610
										PRINT 팬티
									;ドロワ
									ELSEIF TEQUIP:MASTER:속옷（하） == 601
										PRINT 드로워즈
									;レース付き
									ELSEIF TEQUIP:MASTER:속옷（하） == 611
										IF RAND:2 == 0
											PRINT 레이스 달린 팬티
										ELSE
											PRINT 프릴이 달린 팬티
										ENDIF
									;オープン
									ELSEIF TEQUIP:MASTER:속옷（하） == 603
										IF RAND:2 == 0
											PRINT 추잡한 슬릿이 달린 팬티
										ELSE
											PRINT 음부가 보일 것 같은 팬티
										ENDIF
									;마이クロ
									ELSEIF TEQUIP:MASTER:속옷（하） == 605
										IF RAND:2 == 0
											PRINT 천이 너무 작은 팬티
										ELSE
											PRINT 팬티라기보단 그냥 가리개
										ENDIF
									;바이브付き
									ELSEIF TEQUIP:MASTER:속옷（하） == 612 || TEQUIP:MASTER:속옷（하） == 613 || TEQUIP:MASTER:속옷（하） == 614
										IF RAND:2 == 0
											PRINT 기계음이 나는 팬티
										ELSE
											PRINT 조금씩 흔들리고 있는 팬티
										ENDIF
									;縞々
									ELSEIF TEQUIP:MASTER:속옷（하） == 617
										IF RAND:2 == 0
											PRINT 아기자기한 줄무늬의 팬티
										ELSE
											PRINT 줄무늬 팬티
										ENDIF
									;로우라이즈
									ELSEIF TEQUIP:MASTER:속옷（하） == 603
										PRINT 밑이 앝은 팬티
									ENDIF
									IF CFLAG:MASTER:GET_CLOTHESNAME(2) > 0 || CFLAG:MASTER:GET_CLOTHESNAME(3) > 0
										IF RAND:3 == 0
											PRINTW 가 완전히 노출되어 있다…
										ELSEIF RAND:2 == 0
											PRINTW 가 비어져 나와 시선을 모으고 있다…
										ELSE
											PRINTW 가 힐끗힐끗 보이고 있다…
										ENDIF
									ELSE
										IF RAND:3 == 0
											PRINTW 가 완전히 노출되어 있다…
										ELSEIF RAND:2 == 0
											PRINTW 가 비어져 나와 시선을 모으고 있다…
										ELSE
											PRINTW 가 치라 치라 보이고 있다…
										ENDIF
									ENDIF
								;穿いてない＋노출
								ELSEIF TEQUIP:MASTER:음순보임 > 0 && ABL:MASTER:노출증 > 3 && (TEQUIP:MASTER:속옷（하） <= 0 || EQUIP:MASTER:전신속옷 <= 0)
									IF !TALENT:MASTER:남자
										IF RAND:3 == 0
											PRINTW 의 균열이 보이고 있다…
										ELSEIF RAND:2 == 0
											PRINTW 의 젖은 음부가 노출되어 있다…
										ELSE
											PRINTW 의 은밀한 곳이 시선에 노출되어 있다…
										ENDIF
									ELSE
										PRINTW 의 은밀한 곳이 시선에 노출되어 있다…
									ENDIF
								ENDIF
							;퀼로트、바지、핫팬츠、ジーンズ、カットジーンズ
							ELSEIF TEQUIP:MASTER:하의 == 202 || TEQUIP:MASTER:하의 == 204 || TEQUIP:MASTER:하의 == 206 || TEQUIP:MASTER:하의 == 218 || TEQUIP:MASTER:하의 == 219
								IF TEQUIP:MASTER:하의 == 202 
								ELSEIF TEQUIP:MASTER:하의 == 204
								ELSEIF TEQUIP:MASTER:하의 == 206
								ELSEIF TEQUIP:MASTER:하의 == 218
								ELSEIF TEQUIP:MASTER:하의 == 219
								ENDIF
							ENDIF
						;ENDIF
					ENDIF
					;노출量で判断
					;여자만
					IF !IS_LOOKSLIKE_MALE(MASTER)
						SIF TEQUIP:MASTER:팔노출 || TEQUIP:MASTER:다리노출
							PRINTL 남자는 더욱 더 시선을 다른 곳에 향한다…
						;팔노출
						IF TEQUIP:MASTER:92
							IF TALENT:MASTER:절벽 || TALENT:MASTER:빈유
								PRINTFORML 부드러울 듯한 상박은 쭉쭉빵빵한 엉덩이살을 연상하게 만들어 남자의 욕망을 자극한다…
							ELSE
								PRINTFORML 부드러운 듯한 팔뚝은 통통한 엉덩이와 가슴을 연상시켜、남자의 욕망을 자극한다…
							ENDIF
						ENDIF
						;다리노출
						SIF TEQUIP:MASTER:다리노출
								PRINTFORML 적절하게 물이 오른 맨다리에 욕정의 시선이 핥듯이 엉겨든다…
						;유방노출
						IF TEQUIP:MASTER:유방노출
							IF RAND:2 == 0
								PRINTFORM 남자는 핵심으로 고개를 돌려 
							ELSE
								PRINTFORM 남자의 시선은 가슴으로 돌아가 
							ENDIF
							IF TALENT:MASTER:절벽
								PRINTFORML %CALLNAME:MASTER%의 빨래판 같은 가슴을 바라보고 있다…
							ELSEIF TALENT:MASTER:빈유
								PRINTFORML %CALLNAME:MASTER%의 작은 가슴을 바라보고 있다…
							ELSEIF TALENT:MASTER:거유
								PRINTFORML %CALLNAME:MASTER%의 큰 젖가슴을 보고 있다…
							ELSEIF TALENT:MASTER:폭유
								IF RAND:2 == 0 
									PRINTFORM %CALLNAME:MASTER%의 
									;옷
									IF CFLAG:MASTER:GET_CLOTHESNAME(1) > 0
										CALL CLOTHES_NAME,1,MASTER
									;전신복
									ELSEIF CFLAG:MASTER:GET_CLOTHESNAME(3) > 0
										CALL CLOTHES_NAME,3,MASTER
									;겉옷
									ELSEIF CFLAG:MASTER:GET_CLOTHESNAME(10) > 0
										CALL CLOTHES_NAME,10,MASTER
									ENDIF
									SIF RAND:2 == 0
										PRINT  가슴팍 
									PRINTFORMW 위로도 육감이 넘쳐흐르는 두 과실을 보고 있다…
								ELSE
									PRINT 풍만한 가슴의 
									;옷
									IF CFLAG:MASTER:GET_CLOTHESNAME(1) > 0
										CALL CLOTHES_NAME,1,MASTER
									;전신복
									ELSEIF CFLAG:MASTER:GET_CLOTHESNAME(3) > 0
										CALL CLOTHES_NAME,3,MASTER
									;겉옷
									ELSEIF CFLAG:MASTER:GET_CLOTHESNAME(10) > 0
										CALL CLOTHES_NAME,10,MASTER
									ENDIF
									PRINTFORML  너머로도 빤히 들여다보이는 골짜기를 바라보고 있다…
								ENDIF
							ELSE
								;普通のおっぱい
								PRINTFORML %CALLNAME:MASTER%의 생기 있는 가슴이 완전히 노출되어 있다…
							ENDIF
						ENDIF
						;배꼽노출
						IF TEQUIP:MASTER:배꼽노출 == 1 && !TALENT:MASTER:남자
							IF TALENT:MASTER:임신
								PRINTFORML 불룩한 배는 대담한 옷차림으로 임산부 특유의 음란함을 강조하고 
							ELSE
								PRINTFORML 빤히 들여다보이는 허리와 배꼽은 부드러운 고기, 그 안쪽에 있는 여자의 비오를 상기시킨다…
							ENDIF
						ENDIF
						PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 남자의 격렬한 욕정의 시선
						IF TALENT:음란 || TALENT:창부
							IF RAND:4 == 0
								PRINT 을 쿨하게 받아넘겨 웃으며 

							ELSEIF RAND:3 == 0
								PRINT 을 전혀 모르는 채로 
							ELSEIF RAND:2 == 0
								PRINT 을 알아채자 도발적으로 포즈를 취한 뒤 
								IF RAND:2 == 0
									PRINT 음란하게 웃으며 
								ELSE
									PRINT 킥 웃으면서 
								ENDIF
							ELSEIF RAND:1 == 0
								PRINTL 을 받아들이며
								PRINT 입을 열어 펠라 혀놀림을 보이며 
								IF RAND:2 == 0
									PRINT 음란하게 웃으며 
								ELSE
									PRINT 킥 웃으면서 
								ENDIF
							ELSE
								PRINTL 를 알아채고、
								PRINT 미소지으면서 윙크한뒤 
							ENDIF
							PRINTFORML 그 자리를 떠났다…
						ELSEIF TALENT:연모 || TALENT:친애
							IF RAND:2 == 0 
								PRINTFORM FAVORITE_1(MASTER)
								PRINT  외의 성적인 시선에 노출된 것을 불쾌해하면서 
							ELSE
								PRINT 을 깨닫고 마음대로 상대된 것을 불쾌해하면서 
							ENDIF
							PRINTFORML 그 자리를 떠났다…
						ELSEIF TALENT:예속 || TALENT:복종
							IF RAND:2 == 0 
								PRINT 을 무표정하게 바라보는 갚으며,
							ELSE
								PRINT 를 무시하고 아무 일도 없었다는 듯이
							ENDIF
							PRINTFORML 그 자리를 떠났다…
						ELSE
							PRINTFORML 를 전혀 눈치채지 못한 채、그 자리를 떠났다…
						ENDIF
						PRINTFORMW 　
					ENDIF
					
					CALL CHIKAN_MASTER
					
				;■창관についた
				IF RAND:2 == 0
					PRINTFORMW 창관에 도착해 준비를 하고 오늘의 상대를 기다리고 있으니, 지명이 들어왔다.…
				ELSE
					PRINTFORMW 창관에 도착하면 이미 지명 예약이 들어와 있었다…
				ENDIF
				;위험일分岐
				IF CFLAG:MASTER:위험일 && !TALENT:MASTER:임신
					PRINTL 오늘은 위험일인 것이 마음에 걸리지만 
				ELSEIF CFLAG:MASTER:배란유발제 && !TALENT:MASTER:임신
					PRINTL 오늘은 배란유발제를 마시고 있는 것이 걸리지만 
				ENDIF
				PRINTFORM %조사처리(CALLNAME:MASTER,"는")% 
				IF !IS_LOOKSLIKE_MALE(MASTER)
					;■ 衣装分岐
					;● 衣装上半身分岐
					;세일러복
					IF TEQUIP:MASTER:옷 == 102 && EQUIP:MASTER:옷
						SELECTCASE RAND:7
							CASE 0
								CALL COLOR_PATTERN
								PRINT 의 세일러복
							CASE 1
								PRINT 야소가미 고교의 세일러복
							CASE 2
								PRINT 칠자매학원의 세일러복
							CASE 3
								PRINT 성 엘민 학원의 세일러복
							CASE 4
								CALL COLOR_PATTERN
								PRINT 의 세일러복과 어울리는 타이
							CASE 5
								CALL COLOR_PATTERN
								PRINT 로 바뀐 디자인의 세일러복
							CASE 6
								PRINT 사쿠란 여학원과 비슷한 세일러복
						ENDSELECT
					;블레이저교복
					ELSEIF TEQUIP:MASTER:겉옷 == 107 && EQUIP:MASTER:옷
						SELECTCASE RAND:7
							CASE 0
								PRINT 카루코자카고교의 블레이저교복
							CASE 1
								PRINT 모 유명 여고의 블레이저교복
							CASE 2
								CALL COLOR_PATTERN
								PRINT 의 블레이저교복
							CASE 3
								CALL COLOR_PATTERN
								PRINT 의 리본과 블레이저교복
							CASE 4
								CALL COLOR_PATTERN
								PRINT 의 귀여운 디자인의 블레이저교복
							CASE 5
								PRINT 월광관 학원의 교복
							CASE 6
								PRINT 십성고교의 블레이저교복
						ENDSELECT
					;메이드（화이트브림＋메이드복）
					ELSEIF TEQUIP:MASTER:모자 == 1 && (TEQUIP:MASTER:전신복 == 308 || TEQUIP:MASTER:전신복 == 309)
						CALL COLOR_PATTERN
						IF RAND:3 == 0
							PRINT 의 메이드복
						ELSEIF RAND:2 == 0
							PRINT 의 고급 천으로 짜여진 메이드복
						ELSE
							PRINT 의 정교한 프릴이 달린 메이드복
						ENDIF
					;레오타드
					ELSEIF TEQUIP:MASTER:전신속옷 == 704
						CALL COLOR_PATTERN
						IF RAND:3 == 0
							PRINT 의 
						ELSEIF RAND:2 == 0
							PRINT 로 전신을 감싼 
						ELSE
							PRINT 로 몸을 감은 
						ENDIF
						PRINT 레오타드
					;체육복
					ELSEIF TEQUIP:MASTER:옷 == 103
						;블루머
						IF TEQUIP:MASTER:하의 == 211
							IF RAND:3 == 0
								PRINT 속옷이 조금 삐져나온 
							ELSEIF RAND:2 == 0
								PRINT 뚱글뚱글한 
							ELSE
								PRINT 엉덩이를 강조한 
							ENDIF
							PRINT 블루머와 체육복 
						;스패츠
						ELSEIF TEQUIP:MASTER:속옷（하） == 616
							IF RAND:3 == 0
								PRINT 엉덩이를 강조한 
							ELSEIF RAND:2 == 0
								PRINT 심이 들어있는 
							ELSE
								PRINT 스타일을 강조한 
							ENDIF
							PRINT 스패츠와 체육복
						ENDIF
					;드레스、이브닝드레스
					ELSEIF TEQUIP:MASTER:전신복 == 304 || TEQUIP:MASTER:전신복 == 305
						IF RAND:3 == 0
							PRINT 고급스러운 자수가 놓인 드레스
						ELSEIF RAND:2 == 0
							PRINT 가랑이의 부분이 강조된 드레스
						ELSE
							PRINT 가슴의 골짜기가 강조된 드레스
						ENDIF
						PRINT 
					;ギャル系私옷（비키니탑+핫팬츠）
					ELSEIF TEQUIP:MASTER:옷 == 104 && TEQUIP:MASTER:하의 == 206
						IF RAND:3 == 0
							PRINT 의 노출이 많은 
						ELSEIF RAND:2 == 0
							PRINT 의 악세서리가 많은 
						ELSE
							PRINT 이 메인칼라인 
						ENDIF
						PRINT 갸루패션
					;お嬢様系私옷（캐미솔＋모자）
					ELSEIF TEQUIP:MASTER:옷 == 117
						IF RAND:3 == 0
							PRINT 의 소박한 
						ELSEIF RAND:2 == 0
							PRINT 의 귀여운 
						ELSE
							PRINT 의 노출이 많은 
						ENDIF
						PRINT 캐미솔
					;기모노
					ELSEIF TEQUIP:MASTER:옷 == 115 && EQUIP:MASTER:옷
						IF RAND:3 == 0
							PRINT 의 차분한 
						ELSEIF RAND:2 == 0
							PRINT 의 우아한 
						ELSE
							PRINT 의 섹시한 
						ENDIF
						PRINT 기모노
					;유카타
					ELSEIF TEQUIP:MASTER:옷 == 118 && EQUIP:MASTER:옷
						IF RAND:3 == 0
							PRINT 의 귀여운 
						ELSEIF RAND:2 == 0
							PRINT 의 섹시한 
						ELSE
							PRINT 의 호화로운 
						ENDIF
						PRINT 유카타
					;女侍（기모노＋하카마）
					ELSEIF TEQUIP:MASTER:하의 == 215 && TEQUIP:MASTER:옷 == 115 && EQUIP:MASTER:옷 && EQUIP:MASTER:하의
						IF RAND:3 == 0
							PRINT 
						ELSEIF RAND:2 == 0
							PRINT 
						ELSE
							PRINT 
						ENDIF
						PRINT 하카마姿
					;여군（군복）
					ELSEIF TEQUIP:MASTER:전신복 == 316 && EQUIP:MASTER:전신복
						IF RAND:3 == 0
							PRINT 망국의 훈장으로 장식된
						ELSEIF RAND:2 == 0
							PRINT 완장을 붙인
						ELSE
							PRINT 
						ENDIF
						PRINT 군복
					;女騎士（전신갑주）
					ELSEIF TEQUIP:MASTER:전신복 == 306 && EQUIP:MASTER:전신복
						IF RAND:3 == 0
							PRINT 
						ELSEIF RAND:2 == 0
							PRINT 
						ELSE
							PRINT 
						ENDIF
						PRINT 전신갑주
					;여교사、OL（블라우스orYシャツ+스커트系衣装）
					ELSEIF (TEQUIP:MASTER:옷 == 107 || TEQUIP:MASTER:옷 == 107) && (TEQUIP:MASTER:하의 == 201 || TEQUIP:MASTER:하의 == 207 || TEQUIP:MASTER:하의 == 208 || TEQUIP:MASTER:하의 == 209 || TEQUIP:MASTER:전신복 == 303)
						IF RAND:3 == 0
							PRINT 교사풍
						ELSEIF RAND:2 == 0
							PRINT OL풍
						ELSE
							PRINT 영업풍
						ENDIF
						PRINT 의 패션
					;プリンセス・女王（관）＋드레스
					ELSEIF TEQUIP:MASTER:모자 == 16 && (TEQUIP:MASTER:전신복 == 304 || TEQUIP:MASTER:전신복 == 305)
						IF RAND:3 == 0
							PRINT 기품이 넘치는 
						ELSEIF RAND:2 == 0
							PRINT 고귀한 장식으로 치장된 
						ELSE
							PRINT 고급 천으로 짠 의상을 입은 
						ENDIF
						IF RAND:3 == 0
							PRINT 왕녀
						ELSEIF RAND:2 == 0 
							PRINT 여왕
						ELSE
							PRINT 귀부인
						ENDIF
					;婦警（警官の옷）
					ELSEIF TEQUIP:MASTER:옷 == 106
						IF RAND:3 == 0
							PRINT 미니스커트 여경
						ELSEIF RAND:2 == 0
							PRINT 여경
						ELSE
							PRINT 의 모습으로
						ENDIF
						PRINT 여경의 모습
					;간호사（간호사복＋너스캡）
					ELSEIF TEQUIP:MASTER:옷 == 105 && TEQUIP:MASTER:모자 == 2
						IF RAND:3 == 0
							PRINT 가슴과 엉덩이를 강조한 
						ELSEIF RAND:2 == 0
							PRINT 치마가 유난히 짧은 
						ELSE
							PRINT 청초한 인상의 
						ENDIF
						PRINT 간호사복
					;바니수트（うさ耳バンド＋바니수트）
					ELSEIF TEQUIP:MASTER:모자 == 4 && TEQUIP:MASTER:전신복 == 311
						PRINT 바니수트
					;차이나드레스
					ELSEIF TEQUIP:MASTER:전신복 == 310
						PRINT 차이나드레스
					;학교수영복
					ELSEIF TEQUIP:MASTER:전신속옷 == 710
						PRINT 학교수영복
					;원피스수영복
					ELSEIF TEQUIP:MASTER:전신속옷 == 716
						IF RAND:3 == 0
							PRINT 의 멋진 
						ELSEIF RAND:2 == 0
							PRINT 의 차분한 
						ELSE
							PRINT 의 큐트한 
						ENDIF
						PRINT 원피스
					;비키니수영복
					ELSEIF TEQUIP:MASTER:전신속옷 == 715
						IF RAND:3 == 0
							PRINT 유두만 간신히 가리는 
						ELSEIF RAND:2 == 0
							PRINT 의 매혹적으로 노출한 
						ELSE
							PRINT 의 섹시함을 부각시키는 
						ENDIF
						PRINT 비키니
					;경주용수영복
					ELSEIF TEQUIP:MASTER:전신속옷 == 713
						IF RAND:3 == 0
							PRINT 신소재의 
						ELSEIF RAND:2 == 0
							PRINT 스타일을 강조한 
						ELSE
							PRINT 단단한 
						ENDIF
						PRINT 경주용수영복
					ELSE
						PRINT 가게가 준비한 의상
					ENDIF
					;● 行動
					IF RAND:3 == 0
						PRINT 을 착용하고,
					ELSEIF RAND:2 == 0
						PRINT 을 입고, 
					ELSE
						PRINT 으로 갈아입고, 
					ENDIF
					;● 衣装バストサイズ가슴分岐
					PRINT 그 
					IF TALENT:빈유
						IF RAND:3 == 0
							PRINT 완만한 가슴
						ELSEIF RAND:2 == 0
							PRINT 얇은 곡선의 가슴
						ELSE
							PRINT 완만한 경사의 가슴
						ENDIF
					ELSEIF TALENT:절벽
						IF RAND:3 == 0
							PRINT 조금의 경사도 없는 가슴
						ELSEIF RAND:2 == 0
							PRINT 빨래판의 가슴
						ELSE
							PRINT 미안할 정도의 가슴
						ENDIF
					ELSEIF TALENT:거유 || TALENT:폭유
						PRINT 의상
						IF RAND:3 == 0
							PRINT 에 들어가지 못할 정도의 가슴
						ELSEIF RAND:2 == 0
							PRINT 을 타고 흐르는 듯한 가슴
						ELSE
							PRINT 으로는 감추지 못하는 가슴
						ENDIF
					;普通のおっぱい
					ELSE
						PRINT 의상에서 보이는 
						IF RAND:3 == 0
							PRINT 부드러운 가슴
						ELSEIF RAND:2 == 0
							PRINT 건강한 미유
						ELSE
							PRINT 만지고 싶어지는 가슴
						ENDIF
					ENDIF
					PRINTL 이 고객 욕망과 아랫배를 자극한다…
				ENDIF
				;■ 휴식
					;PRINTFORMW 
				;■ 휴식
				;● 체격分岐
				IF TALENT:MASTER:오토코노코 || TALENT:MASTER:남자
					PRINTFORM 남창으로 
					IF ABL:MASTER:섹스중독 >= 1 || TALENT:MASTER:섹스광 || TALENT:MASTER:음란 || TALENT:MASTER:창부
						PRINTFORM 오늘은 어떤 손님과 성관계를 하는지를 기대하면서 
					ELSEIF TALENT:MASTER:정조관념 || TALENT:MASTER:쾌감을부정
						PRINTFORM 부도덕하며 남자로서 부끄러운 행위라고 자기 혐오에 빠져, 
					ELSE
						PRINTFORM 동성, 이성, 심지어는 악마에게까지 안기는 것을 주저하며, 
					ENDIF
				ELSEIF TALENT:MASTER:유아체형
					;● 가슴サイズ分岐
					IF TALENT:MASTER:빈유
						PRINTFORM 아이 같은 몸을 숨기며, 
					ELSEIF TALENT:MASTER:거유 || TALENT:MASTER:폭유
						PRINTFORM 작은 신체에는 너무 큰 가슴을 헐떡거리면서, 
					ELSE
						;普通のおっぱい
						PRINTFORM  자그마한 몸을 헐떡거리면서, 
					ENDIF
				ELSEIF TALENT:MASTER:큰체형 || TALENT:MASTER:거체 && TALENT:MASTER:남자 == 1
					;● 가슴サイズ分岐
					IF TALENT:MASTER:빈유
						PRINTFORM 날씬한 체구에서 유유히 걷고 
					ELSEIF TALENT:MASTER:거유 || TALENT:MASTER:폭유
						PRINTFORM 신축성이 있는 체구에서 유유히 걷고 
					ELSE
						;普通のおっぱい
						PRINTFORM 날씬한 체구에서 유유히 걷고 
					ENDIF
				ELSE
					;普通の체격
					;● 가슴サイズ分岐
					IF TALENT:MASTER:빈유
						PRINTFORM 날씬한 몸이 자아내는 우아한 분위기를 풍기며
					ELSEIF TALENT:MASTER:거유
						PRINTFORM 신체를 강조하는 풍요로운 가슴을 보여 주며, 
					ELSEIF TALENT:MASTER:폭유
						PRINTFORM 옷 위로도 탄력이 넘치는 만큼의 가슴을 대어,
					ELSE
						;普通のおっぱい
						PRINTFORMW 터치감이 좋은 탄력성을 강조하며, 
					ENDIF
				ENDIF
				IF TALENT:MASTER:임신
					IF RAND:3 == 0
						PRINTFORM 임신한 배를 껴안으면서 
					ELSEIF RAND:2 == 0
						PRINTFORM 수정된 증거인 아랫배를 누르며 
					ELSE
						PRINTFORM 임신한 배를 보여주며 
					ENDIF
				ENDIF
				PRINTFORMW 지명한 손님을 미소로 맞이했다…
				PRINTFORMW 　
			ENDIF
			CFLAG:606 = 1
		;■위험일＆배란유발제分岐
		IF CFLAG:MASTER:113 == 1 || CFLAG:MASTER:배란유발제 == 1
			PRINTFORM 행위를 마치고 오늘 마지막 손님을 전송한 %조사처리(CALLNAME:MASTER,"는")% 
			IF RAND:2 == 0
				PRINTFORML 성교의 잔재를 씻기 위해 샤워를 하면서 
			ELSE
				PRINTFORML 옷을 갈아입고 집에 돌아가면서 
			ENDIF
			IF CFLAG:MASTER:113 == 1
				PRINTFORM 위험일에도 불구하고
				PRINTFORML 차례로 몸을 맡긴 손님에게 질내사정을 받고, 
				IF RAND:2 == 0
					PRINTFORM 아버지가 누군지 모르는 아이를 가질지도 모른다
				ELSE
					PRINTFORM 임신할지도 모른다
				ENDIF
				PRINTFORMW 라는 사실을 떠올리고, 
				IF TALENT:4 || TALENT:7
					PRINTFORMW 방긋 웃었다…
				ELSE
					PRINTFORMW 불안과 공포에 몸을 떨었다…
				ENDIF
			ELSEIF CFLAG:MASTER:배란유발제 == 1
				PRINTFORM 배란유발제를 먹은 뒤에 
				PRINTFORML 차례로 몸을 맡긴 손님에게 질내사정을 받고, 
				IF RAND:2 == 0
					PRINTFORM 아버지가 누군지 모르는 아이를 가질지도 모른다
				ELSE
					PRINTFORM 임신할지도 모른다
				ENDIF
				PRINTFORM 는 사실을 돌이켜 생각해보며、
				IF TALENT:4 || TALENT:7
					PRINTFORMW 방긋 웃었다…
				ELSE
					PRINTFORMW 불안과 공포에 몸을 떨었다…
				ENDIF
			ENDIF
		ENDIF


		;-------------------------------------------------
		;렌탈時
		;-------------------------------------------------
	CASE 17
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:277 == 0
				PRINTFORML 
				CFLAG:277 = 1
			;それ以外
			ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:277 = 1
			ENDIF

		;-------------------------------------------------
		;매각時
		;-------------------------------------------------
	CASE 18
			CALL EVENT_MESSAGE
			;반발각인Lv3
			IF MARK:3 == 3
				LOCALS:0 = 
			ENDIF
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && MARK:3 < 3 && LOCALS:0 == ""
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
			ENDIF
			;それ以外
			IF LOCALS:0 == ""
				PRINTFORML 
			ENDIF

		;-------------------------------------------------
		;ビデオ매각時
		;-------------------------------------------------
	CASE 19
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:278 = 2
			ENDIF
			IF (CFLAG:278 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:278 = 1
			ENDIF

		;-------------------------------------------------
		;コンサート時
		;-------------------------------------------------
	CASE 20
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:279 == 0
				PRINTFORML 
				CFLAG:279 = 1
			;それ以外
			ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:279 = 1
			ENDIF

		;-------------------------------------------------
		;乱交라이브時
		;-------------------------------------------------
	CASE 21
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:280 == 0
				PRINTFORML 
				CFLAG:280 = 1
			;それ以外
			ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:280 = 1
			ENDIF

		;-------------------------------------------------
		;공중변소プレイ時
		;-------------------------------------------------
	CASE 22
			CALL EVENT_MESSAGE
			;初めて
			IF CFLAG:281 == 0
				PRINTFORML 
				CFLAG:281 = 1
			;それ以外
			ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:281 = 1
			ENDIF

		;-------------------------------------------------
		;ビデオ保有時
		;-------------------------------------------------
	CASE 23
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:282 = 2
			ENDIF
			IF (CFLAG:282 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:282 = 1
			ENDIF

		;-------------------------------------------------
		;기억소거약사용時
		;-------------------------------------------------
	CASE 30
			CALL EVENT_MESSAGE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
				;연모、친애	
				IF (TALENT:3 || TALENT:6)	
					LOCALS:0 = 
				;음란、창부	
				ELSEIF (TALENT:4 || TALENT:7)	
					LOCALS:0 = 
				;복종、예속	
				ELSEIF (TALENT:5 || TALENT:8)	
					LOCALS:0 = 
				ENDIF	
				;함락済み共通	
				IF LOCALS:0 == ""	
					LOCALS:0 = 
				ENDIF	
				CFLAG:283 = 2
			ENDIF
			IF (CFLAG:283 < 1 || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML 
				CFLAG:283 = 1
			ENDIF

		;-------------------------------------------------
		;;[아내][남편]취득時 친애前提
		;-------------------------------------------------
	CASE 40
			CALL EVENT_MESSAGE
			IF CFLAG:283 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:283 = 1
			ENDIF
		;-------------------------------------------------
		;;[음마]취득時 창부前提
		;-------------------------------------------------
	CASE 41
			CALL EVENT_MESSAGE
			IF CFLAG:284 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:284 = 1
			ENDIF
		;-------------------------------------------------
		;;[専属奴隷]취득時 예속前提
		;-------------------------------------------------
	CASE 42
			CALL EVENT_MESSAGE
			IF CFLAG:285 < 1 || FLAG:7 == 2
				PRINTFORML 
				CFLAG:285 = 1
			ENDIF
		;-------------------------------------------------
		;;オープン/クローズ・オブ・オッパイ
		;-------------------------------------------------
	CASE 44
			CALL EVENT_MESSAGE
			IF TEQUIP:가슴앞열림
				;가슴を出す
				;함락済み
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;그외
					IF TALENT:빈유
						PRINTFORMW 자그맣게 부풀어 오른게 튀어나왔다
					ELSEIF TALENT:절벽
						PRINTFORMW %플레이어가% 노출된 유방을 가리려고 하지만、평평한 거기에는 젖꼭지 이외에 그럴듯한 것은 보이지 않았다…
					ELSEIF TALENT:거유
						PRINTFORMW 뾰족한 두 개의 가슴이 튀어나와 있다
					ELSEIF TALENT:폭유
						IF RAND:2
							PRINTFORMW %조사처리(CALLNAME:PLAYER,"가")% 팔에서 노출한 유방을 수줍게 만지자 그 골짜기가 더욱 강조된다…
						ELSE
							PRINTFORMW 중량감 있는 두 젖은 『푸르르』 『출렁』 같은 효과음이 들릴 정도로 멋들어진다…
						ENDIF
					ELSE
						;普通のおっぱい
						PRINTFORMW 
					ENDIF
			ENDIF
			CFLAG:285 = 1
				;가슴をしまう
				;함락済み
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
					;연모、친애	
					IF (TALENT:3 || TALENT:6)	
						LOCALS:0 = 
					;음란、창부	
					ELSEIF (TALENT:4 || TALENT:7)	
						LOCALS:0 = 
					;복종、예속	
					ELSEIF (TALENT:5 || TALENT:8)	
						LOCALS:0 = 
					ENDIF	
					;함락済み共通	
					IF LOCALS:0 == ""	
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;그외
					IF TALENT:빈유
						PRINTFORMW 
					ELSEIF TALENT:절벽
						PRINTFORMW 
					ELSEIF TALENT:거유
						PRINTFORMW 
					ELSEIF TALENT:폭유
						IF RAND:2
							PRINTFORMW 무리한 손놀림으로 눌려진 유방은 그 무게로 옷을 강하게 압박한다…
						ELSE
							PRINTFORMW たゆん
						ENDIF
					ELSE
						;普通のおっぱい
						PRINTFORMW 
					ENDIF
				CFLAG:287 = 1
		;-------------------------------------------------
		;;[愛ＮＴＲ]이벤트時
		;-------------------------------------------------
	CASE 45
		CALL EVENT_MESSAGE
		;함락済み
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;主人に함락しているor誰にも함락してない상태でＮＴＲ
				;연모、친애
				IF TALENT:3 || TALENT:6
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF	
				ENDIF
				;함락済み共通	
				IF LOCALS:0 == ""	
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						PRINTFORML 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						PRINTFORML 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						PRINTFORML 
					ENDIF
				ENDIF
			ELSE
				;主人以外の誰かに함락している상태で更に別のキャラにＮＴＲ
				;進行度分岐
				;初回
				IF CFLAG:애ＮＴＲ진행도 == 0
					PRINTFORML 
				;二回目（リーチ）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 1
					PRINTFORML 
				;三回目（함락）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 2
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[淫ＮＴＲ]이벤트時
		;-------------------------------------------------
	CASE 46
		CALL EVENT_MESSAGE
		;함락済み
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;主人に함락しているor誰にも함락してない상태でＮＴＲ
				;연모、친애
				IF TALENT:3 || TALENT:6
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				ENDIF
				;함락済み共通	
				IF LOCALS:0 == ""	
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						PRINTFORML 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						PRINTFORML 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						PRINTFORML 
					ENDIF
				ENDIF
			ELSE
				;主人以外の誰かに함락している상태で更に別のキャラにＮＴＲ
				;進行度分岐
				;初回
				IF CFLAG:음ＮＴＲ진행도 == 0
					PRINTFORML 
				;二回目（リーチ）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 1
					PRINTFORML 
				;三回目（함락）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 2
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[隷ＮＴＲ]이벤트時
		;-------------------------------------------------
	CASE 47
		CALL EVENT_MESSAGE
		;함락済み
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;主人に함락しているor誰にも함락してない상태でＮＴＲ
				;연모、친애
				IF TALENT:3 || TALENT:6
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						LOCALS:0 = 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						LOCALS:0 = 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						LOCALS:0 = 
					ENDIF
				ENDIF
				;함락済み共通	
				IF LOCALS:0 == ""	
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						PRINTFORML 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						PRINTFORML 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						PRINTFORML 
					ENDIF
				ENDIF
			ELSE
				;主人以外の誰かに함락している상태で更に別のキャラにＮＴＲ
				;進行度分岐
				;初回
				IF CFLAG:예ＮＴＲ진행도 == 0
					PRINTFORML 
				;二回目（リーチ）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 1
					PRINTFORML 
				;三回目（함락）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 2
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;愛系再함락時
		;;主人に対して연모系함락している、또는一度愛系ＮＴＲ상태にされたキャラに再び함락させられる時
		;-------------------------------------------------
	CASE 48
		CALL EVENT_MESSAGE
		;再함락前に함락しているキャラ→CFLAG:함락캐릭터 で취득가능、이름は%NTRNAME(CFLAG:함락캐릭터)%
		;再함락先のキャラ→ FAVORITE_1_ID(TARGET)　で취득가능、이름は%NTRNAME(FAVORITE_1_ID(TARGET))%
		;やや煩雑になるのでLOCAL、あるいは#DIM等を사용して分かりやすくする事を권장します
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;主人に対して함락し直し
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORML 
			;그외のキャラ・ＮＰＣにＮＴＲし直し
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;淫系再함락時
		;;主人に対して음란系함락している、또는一度淫系ＮＴＲ상태にされたキャラに再び함락させられる時
		;-------------------------------------------------
	CASE 49
		CALL EVENT_MESSAGE
		;再함락前に함락しているキャラ→CFLAG:함락캐릭터 で취득가능、이름は%NTRNAME(CFLAG:함락캐릭터)%
		;再함락先のキャラ→ FAVORITE_1_ID(TARGET)　で취득가능、이름は%NTRNAME(FAVORITE_1_ID(TARGET))%
		;やや煩雑になるのでLOCAL、あるいは#DIM等を사용して分かりやすくする事を권장します
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;主人に対して함락し直し
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORML 
			;그외のキャラ・ＮＰＣにＮＴＲし直し
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;隷系再함락時
		;;主人に対して복종系함락している、또는一度隷系ＮＴＲ상태にされたキャラに再び함락させられる時
		;-------------------------------------------------
	CASE 50
		CALL EVENT_MESSAGE
		;再함락前に함락しているキャラ→CFLAG:함락캐릭터 で취득가능、이름は%NTRNAME(CFLAG:함락캐릭터)%
		;再함락先のキャラ→ FAVORITE_1_ID(TARGET)　で취득가능、이름は%NTRNAME(FAVORITE_1_ID(TARGET))%
		;やや煩雑になるのでLOCAL、あるいは#DIM等を사용して分かりやすくする事を권장します
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;主人に対して함락し直し
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORML 
			;그외のキャラ・ＮＰＣにＮＴＲし直し
			ELSE
				PRINTFORML 
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・乱交パーティ
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 51
		CALL EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;以前연모ルートだった場合
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;以前복종ルートだった場合
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・公園
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 52
		CALL EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;以前연모ルートだった場合
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;以前복종ルートだった場合
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・변기
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 53
		CALL EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;以前연모ルートだった場合
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;以前복종ルートだった場合
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・夜の街
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 54
		CALL EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)				
			IF (TALENT:3 || TALENT:6)	
				;以前연모ルートだった場合
				LOCALS:0 =
			ELSEIF (TALENT:5 || TALENT:8)	
				;以前복종ルートだった場合
				LOCALS:0 = 
			ENDIF	
			;음란、함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ前逢引
		;;ＮＴＲ상태になる前に逢引する時
		;-------------------------------------------------
	CASE 55
		CALL EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)				
			IF CFLAG:함락캐릭터 != 0
			;	;主人以外に함락している場合
				LOCALS:0 = 
			ELSEIF (TALENT:3 || TALENT:6)	
			;	;연모ルート
				LOCALS:0 = 
			ELSEIF (TALENT:4 || TALENT:7)	
			;	;음란ルート
				LOCALS:0 = 
			ELSEIF (TALENT:5 || TALENT:8)	
			;	;복종ルート
				LOCALS:0 = 
			ENDIF	
			;함락済み共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ後初逢引
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)になった直後の一日の종료後、一度だけ行う
		;別のキャラに함락する度に起こる。同じキャラに再度함락した場合は起こらない
		;-------------------------------------------------
	CASE 56
		CALL EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;愛ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;隷ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・デート
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;-------------------------------------------------
	CASE 57
		CALL EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;愛ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;隷ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・本番
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;女性であれば入れられる側、男性であれば入れる側で固定
		;-------------------------------------------------
	CASE 58
		CALL EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;愛ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;隷ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・자위見せ合い
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;愛ＮＴＲ상태の場合、노출증3か자위중독1のどちらかを満たしている事が조건になります。
		;-------------------------------------------------
	CASE 59
		CALL EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;愛ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;隷ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・애널
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;CFLAG:애널횟수が1이상なら入れられている、CFLAG:삽입횟수が1이상なら入れています。
		;両方立っている場合は、地の아야では入れられている側が優先されます。
		;-------------------------------------------------
	CASE 60
		CALL EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;愛ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;隷ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외플레이
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;愛ＮＴＲの場合、노출증が4이상でないと発生しません
		;-------------------------------------------------
	CASE 61
		CALL EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF (TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4) ;&& (RAND:2 == 0)
				;愛ＮＴＲ
				LOCALS:0 = 
			ELSEIF (TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6) ;&& (RAND:2 == 0)
				;隷ＮＴＲ
				LOCALS:0 = 
			ENDIF	
			;共通	
			IF LOCALS:0 == ""	
				PRINTFORML 
			ENDIF	
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외조교
		;;隷ＮＴＲ상태(TALENT:ＮＴＲが 3 か 6)でCFLAG:현ＮＴＲ진행도が6이상의 場合１日の종료시に発生。
		;-------------------------------------------------
	CASE 62
		CALL EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			;隷ＮＴＲ
			PRINTFORML 
			CFLAG:605 = 1
		ENDIF
		;-------------------------------------------------
		;던전内성욕처리：仲魔と섹스する
		;やられちゃう側
		;PLAYERに実行者の번호が入っています
		;-------------------------------------------------
	CASE 79
		CALL EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF NO:PLAYER == 3501
				;당신が母親
				IF MASTER == FINDCHARA_ID(CFLAG:딸의친부모고유번호딸)
					IF TALENT:4 || TALENT:7
						PRINTFORMW 자기 아이에게 범해지는 기쁨에 %조사처리(CALLNAME:MASTER,"는")% 흥분하고 있다…
					ELSE
						PRINTFORMW %CALLNAME:PLAYER%의 성욕처리를 하는 것도 어머니의 일이라고 스스로를 떠올리며 몸을 맡겼다…
					ENDIF
				;당신が父親
				ELSEIF MASTER == FINDCHARA_ID(CFLAG:딸의부친고유번호딸)
					IF TALENT:4 || TALENT:7
						PRINTFORMW 자기 아이에게 범해지는 기쁨에 %조사처리(CALLNAME:MASTER,"는")% 흥분하고 있다…
					ELSE
						PRINTFORMW %CALLNAME:PLAYER%의 성욕처리를 하는 것도 아버지의 일이라고 스스로를 떠올리며 몸을 맡겼다…
					ENDIF
				ENDIF
			ELSE
			ENDIF
			CFLAG:602 = 1
		ENDIF
	;-------------------------------------------------
	;;던전内성욕처리：仲魔に봉사してもらう(봉사側)
	;PLAYERに実行者の번호が入っています
	;-------------------------------------------------
	CASE 80
		CALL EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:603 = 1
		ENDIF
	;-------------------------------------------------
	;;던전内성욕처리：その場で자위
	;-------------------------------------------------
	CASE 81
		CALL EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:604 = 1
		ENDIF
	;-------------------------------------------------
	;던전内성욕처리：物陰で자위
	;봉사する側
	;-------------------------------------------------
	CASE 82
		CALL EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:605 = 1
		ENDIF
	;-------------------------------------------------
	;던전内성욕처리：仲魔と섹스する
	;やる側
	;PLAYERに相手の번호が入っています
	;-------------------------------------------------
	CASE 83
		CALL EVENT_MESSAGE
		IF (CFLAG:606 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:606 = 1
		ENDIF
	;-------------------------------------------------
	;던전内성욕처리：仲魔に봉사させる
	;봉사される側
	;PLAYERに봉사する側の번호が入っています
	;-------------------------------------------------
	CASE 84
		CALL EVENT_MESSAGE
		IF (CFLAG:607 < 1 || FLAG:7 == 2)
			PRINTFORMW %CALLNAME:MASTER%は%CALLNAME:PLAYER%に봉사させた
			CFLAG:607 = 1
		ENDIF
	;-------------------------------------------------
	;던전内성욕처리：윤간이벤트
	;봉사する側
	;PLAYERに実行者=윤간する악마の번호が入っています
	;-------------------------------------------------
	CASE 85
		CALL EVENT_MESSAGE
		IF (CFLAG:608 < 1 || FLAG:7 == 2)
			PRINTFORML 
			CFLAG:608 = 1
		ENDIF
		

		;-------------------------------------------------
		;教会で祈る
		;-------------------------------------------------
	CASE 65
			CALL EVENT_MESSAGE
			;教会の종류によって分ける
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;メシア教会
				CASE 13
					;初めて
					IF CFLAG:609 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:609 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:609 = 1
					ENDIF
				;가이아教会
				CASE 14
					;初めて
					IF CFLAG:610 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:610 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:610 = 1
					ENDIF
				;教会
				CASE 15
					;初めて
					IF CFLAG:611 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:611 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:611 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;教会で입봉사
		;-------------------------------------------------
	CASE 66
			CALL EVENT_MESSAGE
			;教会の종류によって分ける
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;メシア教会
				CASE 13
					;初めて
					IF CFLAG:612 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:612 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:612 = 1
					ENDIF
				;가이아教会
				CASE 14
					;初めて
					IF CFLAG:613 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:613 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:613 = 1
					ENDIF
				;教会
				CASE 15
					;初めて
					IF CFLAG:614 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:614 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:614 = 1
					ENDIF
			ENDSELECT
		;-------------------------------------------------
		;教会で性봉사
		;-------------------------------------------------
	CASE 67
			CALL EVENT_MESSAGE
			;教会の종류によって分ける
			SELECTCASE 점포("종류",(CFLAG:노역플래그 - 10))
				;メシア教会
				CASE 13
					;初めて
					IF CFLAG:615 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:615 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:615 = 1
					ENDIF
				;가이아教会
				CASE 14
					;初めて
					IF CFLAG:616 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:616 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:616 = 1
					ENDIF
				;教会
				CASE 15
					;初めて
					IF CFLAG:617 == 0
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:617 = 1
					;それ以外
					ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
						;ロウ
						IF ABL:속성LC == 1
							PRINTFORML 
						;카오스
						ELSEIF ABL:속성LC == 3
							PRINTFORML 
						;ニュートラル
						ELSE
							PRINTFORML 
						ENDIF
						CFLAG:617 = 1
					ENDIF
			ENDSELECT
	CASEELSE
		SIF FLAG:6 > 0
			CALL EVENT_MESSAGE
ENDSELECT


FOR COUNT,0,51
	SIF LOCALS:COUNT == ""
		BREAK
	PRINTFORM  %LOCALS:COUNT%
	IF LOCAL:COUNT
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF LOCAL:COUNT == 0
	PRINTW
;-------------------------------------------------
;투기장패배時구상集（4999をキャラ번호に置換する） 기본的に見るとゲームオーバーなのでフラグは使わない事に
;-------------------------------------------------
@KOJO_MESSAGE_LOSE_COLOSSEUM_K4999
SELECTCASE TFLAG:투기장패배
		;-------------------------------------------------
		;오니＆蔦
		;-------------------------------------------------
	CASE 1
			CALL MESSAGE_LOSE_COLOSSEUM
			IF 1 == 2
			;연모、친애
			;IF TALENT:3 || TALENT:6
			;	PRINTFORMW 
			;음란、창부
			;ELSEIF TALENT:4 || TALENT:7
			;	PRINTFORMW 
			;복종、예속
			;ELSEIF TALENT:5 || TALENT:8
			;	PRINTFORMW 
			ELSE
				PRINTFORMW 
			ENDIF
ENDSELECT


;-------------------------------------------------
;@KOJO_MESSAGE_ENDING関係(4999、4999をキャラ번호に置換する)
;-------------------------------------------------
;-------------------------------------------------
;エンディング구상の有無を判別する関数
;記述したエンディングの종류に応じてコメントアウトを해제
;-------------------------------------------------
@ENDING_CHECK_K4999
SELECTCASE TSTR:END종별
	;CASE "개별END"
	;	SIF TALENT:아내 || TALENT:남편
	;		RETURN 1
	;	SIF TALENT:음마
	;		RETURN 1
	;	SIF TALENT:노리개
	;		RETURN 1
	;CASE "조교師END"
	;	RETURN 1
	;CASE "희대의조교사END"
	;	RETURN 1
	;CASE "식당경영END"
	;	RETURN 1
	;CASE "창관경영END"
	;	RETURN 1
	;CASE "아이돌END"
	;	RETURN 1
ENDSELECT
RETURN 0

;-------------------------------------------------
;エンド本아야
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K4999
;--------------------------------------------------
SELECTCASE TSTR:END종별
	;CASE "개별END"
		;연모1명エンド
		;IF TALENT:아내 || TALENT:남편 && CFLAG:21 == 1
			;DRAWLINE
			;PRINTFORMW …………
			;PRINTFORMW ………
			;PRINTFORMW ……
			;PRINTFORMW 

			;PRINTFORMW 
			;PRINTFORMW 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;음란1명エンド
		;ELSEIF TALENT:음마 && CFLAG:21 == 1
			;DRAWLINE
			;PRINTFORMW …………
			;PRINTFORMW ………
			;PRINTFORMW ……
			;PRINTFORMW 

			;PRINTFORMW 
			;PRINTFORMW 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;복종1명エンド
		;ELSEIF TALENT:노리개 && CFLAG:21 == 1
			;DRAWLINE
			;PRINTFORMW …………
			;PRINTFORMW ………
			;PRINTFORMW ……
			;PRINTFORMW 

			;PRINTFORMW 
			;PRINTFORMW 　　　　　　　　―― Extra Ending　（◯◯エンド）
		;ENDIF
	;CASE "조교師END"
	;CASE "희대의조교사END"
	;CASE "식당경영END"
	;CASE "창관경영END"
	;CASE "아이돌END"
ENDSELECT
;個別ENDの場合ここでRETURN 1すると、その後ゲームは続かず次周に行くかゲーム종료かの選択になります
RETURN 1


@ENEMY_BATTLE_EVENT_K4999, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;적でのデビルＣＯ－ＯＰ発動時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;적でのデビルＣＯ－ＯＰ참가時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

@BATTLE_EVENT_K4999, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;아군パーティでのデビルＣＯ－ＯＰ発動時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;아군パーティでのデビルＣＯ－ＯＰ참가時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 3
;아군パーティでの被소환時の台詞	
;			PRINTFORML ┏＠%CALLNAME:ARG,20,LEFT%━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF


@BATTLE_MESSAGE_K4999,ARG,ARG:1 = 0

;選択したコマンドにより分岐させたい場合、IF아야の조건にコマンドNoを記入すること
;IF CFLAG:ARG:입력행동 == 
;	PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;	PRINTFORML ┃%"",72,LEFT%┃
;	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

;ELSEIF ARG:1 > 0
	;충성도が上昇する行動をとった場合に표시する구상

;	CALLFORM SKILL_NAME_{CFLAG:ARG:입력행동},ARG
;	;사용した스킬の이름をLOCALSに代入
;	LOCALS = %RESULTS%

;	SELECTCASE ARG:1
;		;물리데미지を与える行動で、영맹持ちの충성도が上昇
;		CASE GETNUM(TALENT,"영맹")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;마법데미지を与える行動で、교활持ちの충성도が上昇
;		CASE GETNUM(TALENT,"교활")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;高위력の行動で、잔학持ちの충성도が上昇
;		CASE GETNUM(TALENT,"잔학")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;相手の약점を付いた공격or행동불능な상태이상の적に対する공격で、현명持ちの충성도が上昇
;		CASE GETNUM(TALENT,"현명")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;파마相性의 行動で、천사持ちの충성도が上昇
;		CASE GETNUM(TALENT,"천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;주살相性의 行動で、타천사持ちの충성도が上昇
;		CASE GETNUM(TALENT,"타천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;회복・상태회복行動では우애持ちの충성도が上昇
;		CASE GETNUM(TALENT,"우애")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;상태변화では교만持ちの충성도が上昇
;		CASE GETNUM(TALENT,"교만")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;特殊では냉정持ちの충성도が上昇
;		CASE GETNUM(TALENT,"냉정")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;우둔持ちは主人とのLv差だけ충성도が上昇
;		CASE GETNUM(TALENT,"우둔")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;허심持ちは최저２だけ충성도が上昇
;		CASE GETNUM(TALENT,"허심")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;	ENDSELECT
;ELSE
;	;システムによらず충성도を上げたいとか他があったらここに記載
;ENDIF

;당신専用
@COLOR_PATTERN
SELECTCASE RAND:18
	CASE 0
		PRINT 흑색
	CASE 1
		PRINT 보라색
	CASE 2
		PRINT 백색
	CASE 3
		PRINT 청색
	CASE 4
		PRINT 황록색
	CASE 5
		PRINT 핑크색
	CASE 6
		PRINT 흑백의 투톤컬러
	CASE 7
		PRINT 검정과 핑크의 투톤
	CASE 8
		PRINT 물색
	CASE 9
		PRINT 회색
	CASE 10
		PRINT 황색
	CASE 11
		PRINT 호피무늬
	CASE 12
		PRINT 감색
	CASE 13
		PRINT 카키색
	CASE 14
		PRINT 빨간 체크무늬
	CASE 15
		PRINT 갈색
	CASE 16
		PRINT 금색
	CASE 17
		PRINT 꽃무늬
ENDSELECT
RETURN

@DESIGN_PANTY_K4999
SELECTCASE RAND:13
	CASE 0
		PRINT 로우레그
	CASE 1
		PRINT 하이레그
	CASE 2
		PRINT 비키니타입
	CASE 3
		PRINT 끈
	CASE 4
		PRINT 보이레그
	CASE 5
		PRINT 스캔티
	CASE 6
		PRINT 브라질리안 컬
	CASE 7
		PRINT 나풀나풀
	CASE 8
		PRINT T백
	CASE 9
		PRINT 스캔티
	CASE 10
		PRINT C스트링
	CASE 11
		PRINT 시스루
	CASE 12
		PRINT 보통
ENDSELECT
RETURN

;痴漢이벤트
@CHIKAN_MASTER
IF TALENT:MASTER:남자
RETURN
ELSE


;확률は低めに
IF RAND:100 < 1
	PRINTFORMW 전철은 붐비고 있다…
	PRINTFORMW %CALLNAME:MASTER% 뒤에서 밀착해 오는 남자가 있다…
	PRINTFORMW 남자는 %CALLNAME:MASTER%의 엉덩이를 더듬기 시작했다
	PRINTFORMW
	PRINTFORMW 치한이다!
	PRINTFORMW 
	PRINTFORMW [1]뿌리친다　　[2]도망간다　　[3]참는다
	CALL INPUTINT, 1, 2, 3
	SELECTCASE RESULT
		CASE 1

		PRINTFORMW 치한의 손을 난폭하게 뿌리쳐 되돌아 봐 모습에 흘겨 보고 붙였다
		PRINTFORMW 소란을 무서워한 치한은 얌전하게 되었다……

		IF RAND:100 < 45
			PRINTFORMW 치한은 다른 장소에 이동한 것 같다
		;별로(너무) 난폭하게 하면(자) 화나게 해버리는 일도 있지요 ,  반드시!
		ELSE
			PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 팔에 따끔하고 한 아픔을 느꼈다
			PRINTFORMW 무슨 일인가 하고 되돌아 보려 했지만 시야가 희미해지며 급격한 현기증으로 서있지 못하고
			PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 치한에게 기대듯이 의식을 잃었다…
			PRINTFORMW 
			PRINTFORMW ···
			PRINTFORMW ··
			PRINTFORMW ·
			PRINTFORMW 
			IF RAND:2 == 0
				PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 낯선 방에서 의식을 되찾았다
				PRINTFORMW 양다리가 M자로 열리는 형태로 침대에 얽매여 검은 그림자에 덮쳐지고 있다…
				PRINTFORMW 
				PRINTFORMW 방금 전의 치한이 천박한 얼굴로 %조사처리(CALLNAME:PLAYER,"를")% 내려다보고 있었다
				PRINTFORMW %CALLNAME:PLAYER%의 비밀의 입술에는 깊숙이 페니스를 꽂을 수 있어 신체에는 백탁이 달라붙어 있다
				PRINTFORMW 치한이 허리를 찔러 붐빌 때에 보통이 아닌 쾌락이 %조사처리(CALLNAME:PLAYER,"를")% 덮친다……
				PRINTFORMW 전신에 번개와 같은 쾌감이 내달리고 %조사처리(CALLNAME:PLAYER,"는")% 간신히 미약이 투입된 것을 알아차렸다
				PRINTFORMW 
				PRINTFORMW 치한이 리드미컬하게 허리를 움직이는 것과 동시에 야무지지 못한 교성이 영향을 준다…
				PRINTFORMW 끝 없는 쾌락에 견딜 수 없어 %조사처리(CALLNAME:PLAYER,"는")% 다시 의식을 놓았다……
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 뒷골목에 걸레짝처럼 방치되어있다가 눈을 떴다
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 
			ELSE
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 역의 화장실 안에서 눈을 떴다
				PRINTFORMW 그러나 의식은 모호하고 초점이 맞지 않았으며、목소리도 잘 나오지 않는다
				PRINTFORMW 몸은 변기 위에 있고 속옷은 벗겨진 상태에 가슴은 다 벗은 상태이며、다리는 벌려진 상태로 남근을 드러낸 남자가 눈앞에 있다
				PRINTFORMW 그런 %CALLNAME:MASTER%의 모습을 남자는 히죽히죽 웃으면서 카메라로 찰칵 촬영하고 있다…
				PRINTFORMW 
				PRINTFORMW 남자는 %CALLNAME:MASTER%의 눈이 반쯤 떠져있는 것을 알고는、비렬에 남근을 들이밀었다
				PRINTFORMW 무어라 항의하려 했으나、남자가 벗긴 속옷이 입에 쑤셔넣어졌다
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 속옷을 문채 의식을 유지하는데 급급하여 남자에게 저항조차 할 수 없다…
				PRINTFORMW 
				PRINTFORMW 남자는 그대로 가슴을 주무르며 덮쳐온다…
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 움직이지 못한 채 몇 시간 동안 계속 범해졌다……
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 어느새 의식을 잃고있던 %조사처리(CALLNAME:MASTER,"는")% 남자가 떠난것을 알고、비틀거리며 돌아가기로 했다
				PRINTFORMW 
				PRINTFORMW 
				PRINTFORMW 
			ENDIF
		ENDIF
		RETURN

		CASE 2
		PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 인파를 밀어 헤쳐 그 자리를 떨어졌다…
		PRINTFORMW 
		PRINTFORMW ・
		PRINTFORMW ・
		PRINTFORMW ・
		PRINTFORMW ・
		PRINTFORMW 
		PRINTFORMW 전철을 내리는 무렵, 방금 전 있던 근처로부터 난폭한 숨결이 들린 것 같았다
		PRINTFORMW 
		PRINTFORMW 
		PRINTFORMW 
		
		RETURN

		CASE 3
		IF RAND:2 == 0
			PRINTFORMW 치한은 %CALLNAME:MASTER%의 가슴을 어루만지듯 만지고 온다
			PRINTFORMW 목덜미에서는 거친 숨이 들리며、옷감 스치는 소리가 들린다…
			PRINTFORMW 
			PRINTFORMW 
			PRINTFORMW 
			PRINTFORMW %조사처리(CALLNAME:MASTER,"가")% 도망가지 않는다고 이해한 치한은 행위를 가속하며、
			IF TALENT:MASTER:빈유
				PRINTFORMW 그 작은 가슴을 어루만지듯 주무르고 있다
			ELSEIF TALENT:절벽
				PRINTFORMW 그 얇은 가슴을 주무르듯이 만지고 있다
			ELSEIF TALENT:거유 || TALENT:폭유
				PRINTFORMW 그 가슴을 쥐어짜듯이 주물러대고 있다
			ELSE
				PRINTFORMW 그 가슴을 손바닥으로 만지작거리고 있다
			ENDIF
		ENDIF
		PRINTFORMW 치한은 %CALLNAME:PLAYER% 엉덩이를 이나 주물럭 거리며 더듬고 있다
		PRINTFORMW 귓불에 혀를 핥아지게 되어 소름끼치는 감촉이 등골을 기어 돈다…
		PRINTFORMW 
		PRINTFORMW 
		PRINTFORMW 
		SELECTCASE RAND:3
		CASE 0
		PRINTFORMW 치한은 한 바탕 %CALLNAME:PLAYER%의 몸을 즐겼다가 만족해서 떨어져 갔다
		PRINTFORMW 
		PRINTFORMW 


		CASEELSE
		PRINTFORMW 기분이 좋아진 치한은 엉덩이를 쓰다듬으며 손을 앞쪽으로 미끄러뜨려
		PRINTFORMW 그대로 속옷 안으로 손을 넣으려하고 있다
		PRINTFORMW [1]도망간다　　[2]참는다
		CALL INPUTINT, 1, 2
			SELECTCASE RESULT
			CASE 1
			PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 인파를 밀어 헤쳐 그 자리를 떨어졌다…
			PRINTFORMW 
			PRINTFORMW ・
			PRINTFORMW ・
			PRINTFORMW ・
			PRINTFORMW ・
			PRINTFORMW 
			PRINTFORMW 전철에서 내릴 무렵, 방금 전 있던 근처로부터 난폭한 숨결이 들린 것 같았다
			PRINTFORMW 
			PRINTFORMW 

			CASE 2
			PRINTFORMW 치한은 %CALLNAME:PLAYER%의 습기를 띤 균열을 느긋하게 어루만지고 있다…
			PRINTFORMW 굵은 손가락이 질 입구를 얕게 출입해, %CALLNAME:PLAYER%에게도 강한 쾌감을 준다
			PRINTFORMW 
			PRINTFORMW 치한은 다리 사이의 뜨거운 기둥을 %CALLNAME:PLAYER%에게 꽉 누르면서 애무를 계속하고 있다…
			PRINTFORMW 
			SELECTCASE RAND:3
				CASE 0
				PRINTFORMW 치한은 한 바탕 %CALLNAME:PLAYER%의 몸을 즐겼다가 만족해서 떨어져 갔다
				PRINTFORMW 
				PRINTFORMW 

				CASE 1
				PRINTFORMW 딱 애무가 멈추는가 싶더니、갑자기 강하게 엉덩이를 양손으로 잡았다
				PRINTFORMW 
				PRINTFORMW 본격적으로 핏줄선 페니스를 맹렬한 기세로 후후 숨을 몰아쉬며 %CALLNAME:MASTER%의 엉덩이에 문지르기 시작했다……
				PRINTFORMW 치한은 이 전차 안에서、엉덩이를 움직이기를 할 작정인 것 같다
				PRINTFORMW 
				PRINTFORMW [1]도망간다　　[2]참는다
					CALL INPUTINT, 1, 2
					SELECTCASE RESULT
					CASE 1
					PRINTFORMW 더 이상은 버틸 수 없었던 %조사처리(CALLNAME:MASTER,"는")% 흐트러진 착의 그대로 도망쳤다！
					PRINTFORMW 그 자리에는 숨을 난폭하게 쉬며 국부를 노출한 남자만이 남겨졌다……
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 

					CASE 2
					PRINTFORMW 치한은 허리를 흔들고、%CALLNAME:MASTER%의 속옷 너머의 엉덩이에 노출된 핏줄선 페니스를 열심히 비벼대고 있다……
					PRINTFORMW 주위에서 보면 분명히 이상한 행동을 하고 있을 텐데 주변에서는 못 본 체하고 있다…
					PRINTFORMW 투박한 양손이 %CALLNAME:MASTER%의 허리를 놓치지 않도록 단단히 누르고、
					PRINTFORMW 힘찬 움직임과 분비된 쿠퍼액 때문에 하체의 옷은 이미 흐트러져 있다
					PRINTFORMW 그 격렬한 난폭함에 %조사처리(CALLNAME:MASTER,"는")% 마치 자신이 범하고 있는 듯한 느낌을 받기 시작했다
					PRINTFORMW 
					PRINTFORMW 어느새 %조사처리(CALLNAME:MASTER,"는")% 호흡이 흐트러지고、젖꼭지가 서고、애액이 분비되고 있었다…
					PRINTFORMW 그 치한은 이미 ２발 정도 스커트 안에 발사가 끝났지만、변함없이 허리를 흔들고 있다…
					PRINTFORMW 덕분에 스커트는 군데군데 정액이 배어 꾸깃꾸깃해지고、속옷은 액을 너무 많이 흡수해서 끈적끈적하다
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 치한은 읏 하고 %CALLNAME:MASTER%의 귓가에 대고 신음하면서、３발째를 풀어헤쳤다…
					PRINTFORMW 아무래도 이제 끝내줬으면 한다…
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 결국 5발의 정액을 %CALLNAME:MASTER%의 치마 속에 다 쏟아 넣을 때까지 치한의 행위는 계속되었다……
					PRINTFORMW 그러나 당사자인 치한의 모습은 이미 사라지고 있었다
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 
					ENDSELECT
				CASE 2
				PRINTFORMW 딱 애무가 멈추었는지라고 생각 했지만,  뒤로 무엇인가 바스락바스락움직이고 있다
				PRINTFORMW 
				PRINTFORMW 순간, %CALLNAME:PLAYER%의 속옷이 비켜 놓아져 뜨거운 덩어리가 질 입구에 꽉 눌렸다……
				PRINTFORMW 치한은 전철 안임에도 관계없이 페니스를 %CALLNAME:PLAYER%에게 삽입하려고 하고 있다
				PRINTFORMW 
				PRINTFORMW [1]도망간다　　[2]참는다
					CALL INPUTINT, 1, 2
					SELECTCASE RESULT
					CASE 1
					PRINTFORMW 일선을 넘게 되면 견딜 수 없다고 느낀 %조사처리(CALLNAME:PLAYER,"는")% 흐트러진 착의인 채 도망갔다!
					PRINTFORMW 그 자리에는 숨을 난폭하게 쉬며 국부를 노출한 남자만이 남겨졌다……
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 

					CASE 2
					PRINTFORMW 치한의 강직이 %CALLNAME:PLAYER%의 질을 밀어 헤치면서 비틀어 박아진다……
					PRINTFORMW 주위는 전철의 흔들림과는 분명하게 다른 리듬으로 움직이고 있는 남녀가 있는 일을 알아차리지 않는 것 같다…
					PRINTFORMW 
					PRINTFORMW 치한은 공공연하게 피스톤 할 수 없는 것인가, 섬세한 움직임으로 %조사처리(CALLNAME:PLAYER,"를")% 고문 하듯이 괴롭힌다.
					PRINTFORMW 질 안쪽을 집요하게 자극되어 %조사처리(CALLNAME:PLAYER,"는")% 필사적으로 소리를 억제할 수 밖에 없다
					PRINTFORMW 
					PRINTFORMW 소폭의 피스톤과 동시에 억제하지 못한 목소리가 한숨이 되어 빠져 나온다…
					PRINTFORMW %CALLNAME:PLAYER%의 비밀장소로부터는 애액이 넓적다리까지 늘어져 점차 물소리가 크게 되었다
					PRINTFORMW 
					PRINTFORMW 치한은 그 일을 눈치채고는 허리의 움직임을 빨리 해 한층 더 %CALLNAME:PLAYER%의 안쪽의 안쪽까지 침입해 왔다…
					PRINTFORMW 자궁구를 얻어맞는 충격에 당신은 필사적으로 입을 가려 참고 있다…
					PRINTFORMW 
					PRINTFORMW 치한이 허리를 조금 당겨서 틈을 만들더니, 힘차게 허리를 두드려 붙였다!
					PRINTFORMW 질 안쪽에 큰 충격을 받은 %조사처리(CALLNAME:PLAYER,"는")% 더 이상 참지 못하고 소리를 높여 벌벌거리며 절정을 맞이했다
					PRINTFORMW 동시에 치한도 %CALLNAME:PLAYER%의 안쪽 욕망을 토해냈다………
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW %CALLNAME:PLAYER%의 소리에 주위의 인간은 두리번두리번하고 근처를 둘러보고 있다…
					PRINTFORMW 그러나 당사자인 치한의 모습은 이미 사라지고 있었다
					PRINTFORMW 
					PRINTFORMW 
					PRINTFORMW
				ENDSELECT
			ENDSELECT
		ENDSELECT
	ENDSELECT
	ENDSELECT
	ELSE
	RETURN
	ENDIF
ENDIF
RETURN
