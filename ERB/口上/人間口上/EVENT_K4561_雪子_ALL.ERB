;-------------------------------------------------
;%CALLNAME%구상（캐릭터の이름を入れる、下にコンセプトなど一言あれば入れる）
;[처녀] [자제심] [정조관념] [억압] [부끄럼쟁이] 
;[습득빠름] [매력] [Ｖ민감] [인기]  
;%CALLNAME%はキャラ名
;%조사처리(CALLNAME:MASTER,"는")% 당신名
;♡は？
;%CSTR:일인칭%は일인칭
;%CSTR:이인칭%は이인칭
;
;-------------------------------------------------
;SHOP화면から→襲わないで帰った場合、無理やり犯すでレ○プしちゃった場合
;던전조교→던전内
;-------------------------------------------------
;eratohoA及びeratohoA系列専用구상テンプレート
;X1とX2をキャラ번호に置換し、Y1を조수のキャラ번호に置換してください
;ただし、キャラ번호が一桁のキャラの場合、X1,Y1は1、X2は01のように変えてください
;そしてコマンド内の適当な조건の下のPRINTFORMWの後ろに半角スペースを置いてセリフを추가してください
;구상を추가しないコマンドは無記入でも構いませんが、CFLAG:XXXの머리に『;』をつけてコメントアウトすると尚良いでしょう
;エ키스トラ構文など、コメントアウトされている構文を使う際には、『;』を外してコメントアウトを해제する필요があります
;最後にread meにも눈を通しておくことをお勧めします
;-------------------------------------------------
;--------------------------------------------------
;EVENTTRAIN関係（X1をキャラ번호に置換）
;조교開始時のセリフ CFLAG 201～219を사용
;--------------------------------------------------
@EVENTTRAIN_K4561
;--------------------------------------------------
;엑스트라모드構文　CFLAG:220を사용
;はいを選択した時に初조교時に구상설정用の소질に변경できます
;下記に처녀化する場合の構文を샘플として載せておきます
;--------------------------------------------------
;IF CFLAG:220 == 0
;	PRINTL 처녀モードに移行しますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	$INPUT_LOOP
;	INPUT
;	IF RESULT == 0
;		PRINTFORMW ―처녀モードに소질、능력を변경します―
;		TALENT:TARGET:0 = 1
;		ABL:TARGET:4 = 0
;		EXP:TARGET:0 = 0
;	ELSEIF RESULT == 1
;		PRINTFORMW ―통상モードで조교を開始します―
;	ELSE
;		GOTO INPUT_LOOP
;	ENDIF
;	CFLAG:220 = 1
;ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTTRAIN_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL EVENTTRAIN_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;--------------------------------------------------
;簡易조수구상の発動時に初조교時구상をスキップ（Y1を조수のキャラ번호にする）
;複数人いる際には、SIF NO:ASSI == Y1 || NO:ASSI == Z1と추가していく
;--------------------------------------------------
;IF CFLAG:201 == 0 && ASSI > 0
;	SIF NO:ASSI == Y1
;		CFLAG:201 =1
;ENDIF
;-------------------------------------------------
;조교開始時（一回のみ） CFLAG201
;-------------------------------------------------
DRAWLINE
;初조교時
IF CFLAG:201 == 0 && TALENT:연모 == 0
	;던전내조교の場合　しかしこのまま조교って原作的に考えて死んじまうじゃなかろうか
	IF FLAG:던전내조교 == 1
		PRINTFORMW 「치에！놔 줘！…설마 당신！？」
		PRINTFORMW 『너무하네에 %CALLNAME%、나는 나야』
		PRINTFORMW 『단지 조금 %CALLNAME:MASTER%에게 농락되어 버려…이제 돌아갈 수 없어』
		PRINTFORMW 『그러니까 %NAME:TARGET%도 함께라고、괜찮아！타락해도 함께니까…』
		PRINTFORMW 그리 말하면서 치에는 %조사처리(CALLNAME,"를")% 뒤에서부터 부드럽게 끌어안았다
		PRINTFORMW 체력을 다 써버려、몸에 힘이 들어가지 않는 %조사처리(CALLNAME,"를")% 붙잡는데 힘은 필요없었다
		PRINTFORMW 「%CSTR:이인칭%！치에에게 뭘한 거야！？」
		PRINTFORMW 「싫어！이쪽으로 오지 마！손대지 마！」
	;치에ちゃん使ってムフフしてその場で頂いちゃったよ　こっちの方がマズいかｗ
	ELSEIF DB:72:8 == 3 && CFLAG:341 == 0
		PRINTFORMW 「…응……어라？」
		PRINTFORMW 「여기…어디였지？」
		PRINTFORMW 「그러니까、어제는 분명 여관일을 돕다、챠임이 울려서…」
		PRINTFORMW 「정신을 차렸더니 성 안에서…또 한사람의 %조사처리(CSTR:일인칭,"가")% 있어서…」
		PRINTFORMW 「그렇다！치에！치에가 구해주러 와서…어？」
		PRINTFORMW 「도망치지 못해서…그대로………아팟…」
		PRINTFORMW 사타구니에 위화감을 느껴 집게손가락으로 만져보면 피가 묻어있다
		PRINTFORMW 창백해져 가는 %NAME:TARGET%에게 이미 방에 들어와 있던 %조사처리(CALLNAME:MASTER,"는")% 말을 걸었다
		PRINTFORMW 「%CSTR:이인칭%！치에에게 뭘한 거야！？」
		PRINTFORMW 「뭐가 목적인 거야！？돈？때문엣……」
		PRINTFORMW 하던 말을 끊어 입을 다물고 %조사처리(CALLNAME:MASTER,"를")% 노려보았다
		PRINTFORMW (치에도 %CSTR:일인칭%처럼……용서못해！)
	;いい人だと思ってたのに
	ELSEIF DB:72:8 < 3
		PRINTFORMW 「여기로…좋은 거야？」
		PRINTFORMW 「이런 곳에서 뭘하는 거야？앗、숨바꼭질이라면 자신있어！」
		PRINTFORMW 평소보다 말수가 많아、불온한 분위기를 짐작하고 있는 것같다
		PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 그런 %조사처리(NAME:TARGET,"를")% 난폭하게 밀어 넘어뜨렸다
		PRINTFORMW 「아팟、저기、이건 무슨 장난？」
		PRINTFORMW 「……조교라고！무슨 짓！저기、대답해 줘！」
	ENDIF
	IF CFLAG:전투참가불가능 == 0
		DRAWLINE
		PRINTFORMW ※%조사처리(NAME:TARGET,"는")% %CALLNAME:MASTER%에 대한 신뢰를 잃어、전투에 참가하지 않게 되었습니다※
		SIF CFLAG:포지션 > 0
			CALL REMOVE_POSITION,CFLAG:포지션
		CFLAG:전투참가불가능 = 1
	ENDIF
	CFLAG:201 = 1
;굴복각인Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	PRINTFORMW 「이런 곳에 가둬서…」
	PRINTFORMW 「설마…%조사처리(CSTR:이인칭,"가")% 연속살인사건의 범인！？」
	CFLAG:201 = 2
	RETURN 0
;굴복각인Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	PRINTFORMW 「저기！ 제발 빨리 해방해 줘！」
	PRINTFORMW 「지금、엄마가 쓰러져서 우리 여관 큰일인 거야！그러니까…」
	PRINTFORMW 「안된…다고…요、그래…」
	CFLAG:201 = 3
	RETURN 0
;굴복각인Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 방에 들어가려고 한 %CALLNAME:MASTER%의 귀에 %CALLNAME%의 혼잣말이 들려왔다
	PRINTFORMW 「성 안에서 또 한 사람의 %조사처리(CSTR:일인칭,"는")%、어딘가 멀리 가고 싶어、라고 말했어…」
	PRINTFORMW 「%CSTR:일인칭%、그 마을을 어떻게 생각한 거지」
	PRINTFORMW 「이렇게 되어서…사실은、다행인 걸까…」
	CFLAG:201 = 4
	RETURN 0
;함락
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;연모　ラブラブさせてー
	IF TALENT:3
		PRINTFORMW 「아、잠깐 기다려！」
		PRINTFORMW 「저기、제대로 된 답례를 아직 하지 못했으니까…」
		PRINTFORMW 「성에서、구해줘서…고마워…」
		PRINTFORMW 「이렇게 돼서 쭉…무서웠어」
		PRINTFORM 「여기는 좁고、어둡고、
		IF CFLAG:미조교함락 == 1
		;조교せずに연모をつけた場合の分岐
			PRINTW 계속 시간이 흘러도 집에 돌려보내 주지않고…」
		ELSE
			PRINTW 창문도 없고…」
			PRINTFORMW 「%조사처리(CSTR:이인칭,"가")% 와서 야한 짓을 하고 싶은 것만 하고 나가서…」
		ENDIF
		PRINTFORMW 「좋은 것은 하나도 없어、어두컴컴한 공간에서…굉장히…싫었어…」
		PRINTFORMW 「싫었던…건데…」
		PRINTFORMW 
		PRINTFORMW 「지금은 %조사처리(CSTR:이인칭,"를")% 좋아해」
		PRINTFORMW 「%조사처리(CSTR:이인칭,"가")% 와주는 것만으로 기뻐서」
		IF CFLAG:미조교함락 == 1
			PRINTFORMW 「%조사처리(CSTR:이인칭,"과")% 어깨를 마주하고 싸우는 것이 행복해서」
		ELSE
			PRINTFORMW 「%조사처리(CSTR:이인칭,"를")% 위해서 몸을 써 봉사하는 게 즐거워서」
		ENDIF
		PRINTFORMW 「이상해요…이런 거…」
		PRINTFORMW 「……」
		PRINTFORMW 「이런 건 잘못되어 있다고 알고 있어」
		PRINTFORMW 「겉모습만으로 좋아、소꿉장난으로 좋으니까」
		PRINTFORMW 「그러니까……%조사처리(CSTR:이인칭,"를")%、왕자님이라고、불러도 돼？」
		CALL INPUT_YN,"상관없다","지금 이대로가 좋다"
		IF RESULT == 0
			CSTR:12 = 왕자님
			PRINTFORMW 「정말！？　고마워、%CSTR:일인칭%의 %CSTR:이인칭%！♡」
		ELSE
			PRINTFORMW 「알았어。%조사처리(CSTR:이인칭,"가")% 그렇게 하길 바란다면、그렇게 해♡」
		ENDIF
		PRINTFORMW %조사처리(CALLNAME,"는")% 웃음을 흘리면서、%CALLNAME:MASTER%에게 안겨들어 왔다。
	;음란　ドＳ心に火をつけて…
	ELSEIF TALENT:4
		PRINTFORMW %조사처리(CALLNAME:MASTER,"가")% 방에 들어가자 %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%의 몸을 만지작거리기 시작한다
		PRINTFORMW 「%CSTR:일인칭%에게、이런 걸 가르친 것이 %CSTR:이인칭%의 운의 끝」
		PRINTFORMW 「반드시、손들게 하겠어」
		PRINTFORMW %CALLNAME%의 눈에는 수상한 불꽃이 머물기 시작했다…
	;복종
	ELSEIF TALENT:5
		PRINTFORMW 
	ENDIF
	CFLAG:201 = 5
	RETURN 0
ENDIF

;-------------------------------------------------
;簡易조수구상（Y1を조수のキャラ번호に置換、X1をキャラ번호に변경する) CFLAG203
;掛け合いをするキャラを増やす場合は、構文をコピー＆ペーストをして、CFLAGの数を204、205と増やす
;-------------------------------------------------
;조수の有無を체크（いない場合は二回目以降へ飛ぶ）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_K4561
;조수
ELSEIF NO:ASSI == 4560
	;初めて
	IF CFLAG:203 == 0
		;함락済み＋함락時初구상が発生済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
			PRINTFORMW 「역시、%CALLNAME:ASSI%도、인 거네……」
			PRINTFORMW 『결국 우리、이런 곳에서도 함께인 거네……』
			PRINTFORMW 두 사람은 서로 입을 열면서、누가 먼저랄 것도 없이 손을 맞잡는다。
			PRINTFORMW 「두 사람이 함께、같은 사람을……」
		;それ以外
		ELSE
			PRINTFORMW 「%CALLNAME:ASSI%……%CSTR:이인칭%、%CALLNAME:ASSI%에게 뭘！」
			PRINTFORMW %조사처리(CALLNAME,"가")% 너무 화난 건지、%CALLNAME:MASTER%에게 손을 쳐들었지만、%조사처리(CALLNAME:ASSI,"가")% 그 사이에 섰다。
			PRINTFORMW 『그만둬、%CALLNAME%。나는 말야、스스로의 의사로 여기에 있는 거야』
			PRINTFORMW 「그런、%CALLNAME:ASSI%、어째서……」
			PRINTFORMW 『%조사처리(CALLNAME,"는")%、내가 지켜 줄게。앞으로도…』
			PRINTFORMW 그리 말하자 %조사처리(CALLNAME:ASSI,"는")%、천천히 %CALLNAME%에게 다가갔다。
		ENDIF
		CFLAG:203 = 1
	;二回目以降
	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;함락済み
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
			PRINTFORMW 「오늘은 %CALLNAME:ASSI%도 함께인 거네」
			PRINTFORMW 『나、오늘은 %CALLNAME%에게 지지 않아』
			PRINTFORMW 두 사람은 미소지은 채、손을 맞잡아、짧은 환담을 하고 있다。
		;それ以外
		ELSE
			PRINTFORMW 「%CALLNAME:ASSI%……싫어…」
			PRINTFORMW %조사처리(CALLNAME,"는")% 두려워하는 듯이 떨고 있지만、%조사처리(CALLNAME:ASSI,"는")% 천천히 다가간다。
			PRINTFORMW 『내가 가르쳐 줄게、%CALLNAME%…』
		ENDIF
		CFLAG:203 = 2
	ENDIF
;구상のある조수が居ない場合は、통상の二回目以降の구상へ飛ぶ
ELSE
	CALL EVENTTRAIN_SECOND_K4561
ENDIF

;-------------------------------------------------
;조교開始時（2回目以降、X1をキャラ번호に변경する）
;-------------------------------------------------
@EVENTTRAIN_SECOND_K4561
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORMW 「다가오지 마！！！」
	PRINTFORMW %조사처리(CALLNAME,"는")% %조사처리(CALLNAME:MASTER,"가")% 방에 들어가자마자 큰소리로 딱 잘라 말했다。
	CFLAG:202 = 4
;굴복각인Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	PRINTFORMW 「이런 범죄같은 짓을 해서、용서받을 거라고 생각하는 거야！」
	PRINTFORMW 「%조사처리(CSTR:일인칭,"는")%、절대로 %CSTR:이인칭%의 생각대로 되진 않아！」
	CFLAG:202 = 1
;굴복각인Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	PRINTFORML 「매번、매번、잘도 질리지 않는 거군요？%CSTR:일인칭%、%CSTR:이인칭%만은 절대 용서하지 않을테니까」
	PRINTFORMW 방에 들어온 %CALLNAME:MASTER%에게 예리한 눈빛을 날리는 %CALLNAME%。아직도 조교의 효과는 적은 것 같다‥‥
	CFLAG:202 = 2
;굴복각인Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	PRINTFORML 방에 들어온 %조사처리(CALLNAME:MASTER,"를")% 매섭게 쏘아보는 %CALLNAME%。하지만、그 눈에는 명백하게 박력이 빠져 있다
	PRINTFORMW （또、또‥‥？ 이제 그만둬‥‥이 이상 당하면‥‥%CSTR:일인칭%‥‥%CSTR:일인칭%‥‥）
	CFLAG:202 = 3
;굴복각인Lv3＋함락소질無し
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORML 방에 들어온 %조사처리(CALLNAME:MASTER,"를")% 보자、%조사처리(CALLNAME,"는")% 스스로 옷을 벗기 시작한다
	PRINTFORML 「아、아냣！이、이건 단지 %CSTR:이인칭%에게 벗겨지는 것이 싫으니까‥‥」
	PRINTFORMW 무의식 중에 옷을 벗고 있는 것을 알아차려、당황하며 부정하는 %CALLNAME%。하지만、그 눈에는 명백하게 기대의 기색이 보인다
	CFLAG:202 = 4
;함락済み
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;연모、친애
	IF TALENT:3 || TALENT:6
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		K:1 = RAND:3
		IF K:1 == 0
			PRINTFORML 방에 들어가자、%조사처리(CALLNAME,"가")% 공손히 머리를 숙인다
			PRINTFORML 「어서 오십시오、%CSTR:이인칭%。아마기 여관에、오신 것을 환영합니다。」
			PRINTFORML 「오늘은 느긋하게、피로가 풀릴 때까지 있어주세요？」
			PRINTFORML 「어땠어？여관의 접객이란、나쁘지 않죠？」
			PRINTFORML 대답 대신 %CALLNAME%의 머리를 쓰다듬어 주자、
			PRINTFORMW 더 해달라고 졸라온다
		ELSEIF K:1 == 1
			PRINTFORML 「아‥‥어서 오세요」
			PRINTFORML 「그러니까‥‥다녀 오셨어요、%CSTR:이인칭%」
			PRINTFORML 「목욕을 해？식사를 해？아니면‥‥%CSTR:일인칭%？」
			PRINTFORML 약간 얼굴을 붉히며 팔짱을 껴온다
			PRINTFORMW 「후훗、추천 메뉴는 %CSTR:일인칭%、일까」
		ELSE
			PRINTFORML 방에 들어가자、%조사처리(CALLNAME,"는")% 침대에서 자고 있는 것 같았다。
			PRINTFORML 「응‥‥으응‥‥%CSTR:이인칭%……」
			PRINTFORML 무리하게 일으키는 일도 어려우므로、머리맡에 걸터앉아、
			PRINTFORML 잠시 기다리기로 했다。
			PRINTFORML 「으음‥‥어라？‥‥으응？」
			PRINTFORMW 막 잠에서 깨어나서 상황을 파악하지 못한 %CALLNAME%에게、가볍게 입맞춤을 했다。
		ENDIF
	;음란、창부
	ELSEIF TALENT:4 || TALENT:7
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		K:1 = RAND:3
		IF K:1 == 0
			PRINTFORML 방에 들어가자、이미 %CALLNAME%의 옷은 흐트러져 있어、거친 숨을 내쉬고 있다
			PRINTFORML 「우후후、겨우 왔다。더 기다릴 수 없어서 스스로 해버린 거야‥‥자、봐 줘？」
			PRINTFORMW 스커트와 검은 색의 팬티 스타킹을 내려、스스로의 성기를 벌리는 %CALLNAME%。거기에선 투명한 액체가 하염없이 넘쳐흐르고 있다
		ELSEIF K:1 == 1
			PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 방에 들어가자 방은 벌써 %CALLNAME%의 냄새로 채워져 있다。
			PRINTFORML 「늦어 정말。저기、빨리、오늘도 기분 좋은 걸、잔뜩 해주겠죠？」
			PRINTFORMW 침대에서 다리를 벌리고 허리를 비틀어 야릇하게 유혹하는 %CALLNAME%에게、이끌리듯이 %조사처리(CALLNAME:MASTER,"가")% 덮친다
		ELSE
			PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 방에 들어가자、%조사처리(CALLNAME,"가")% 머뭇머뭇거리면서 다가왔다。
			PRINTFORML 「저기、너무 참아서 이상하게 될 것 같아。빨리、기분 좋은 걸 해 줘‥‥？」
			PRINTFORML 눈을 치켜뜨고 이쪽을 살피는 %CALLNAME%。침대로 향하지 않고 그대로 서 있자、
			PRINTFORMW 몸이 달은 건지、%조사처리(CALLNAME,"가")% %CALLNAME:MASTER%의 손을 잡아、스스로의 성기를 문질러 대기 시작한다‥‥
		ENDIF
	;복종、예속
	ELSEIF TALENT:5 || TALENT:8
		;랜덤で구상が변화する（使わない場合はすべて同じにすればよい）
		K:1 = RAND:3
		IF K:1 == 0
			PRINTFORMW 
		ELSEIF K:1 == 1
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF
RETURN 0

;-------------------------------------------------
;EVENTEND関係（X1をキャラ번호に置換）
;조교종료시のセリフ
;-------------------------------------------------
@EVENTEND_K4561
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL EVENTEND_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL EVENTEND_PY1_K4561
;	RETURN 0
;ENDIF
;--------------------------------------------------
;조교종료시のセリフ
;--------------------------------------------------
DRAWLINE
;반발각인Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	PRINTFORMW 「%조사처리(CSTR:일인칭,"는")% %조사처리(CSTR:이인칭,"를")% 절대로 용서 못해！」
	PRINTFORMW %조사처리(CALLNAME,"는")% %조사처리(CALLNAME:MASTER,"를")% 날카롭게 노려보았다。
	;もう許してやれよ…
	CFLAG:217 = 1
;굴복각인Lv1以下
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	PRINTFORMW 「빨리 나가라니까！」
	PRINTFORMW 「%CSTR:이인칭%의 얼굴따위…보고 싶지 않아！」
	CFLAG:219 = 1
;굴복각인Lv2
ELSEIF MARK:2 == 2
	PRINTFORML 「끝났‥‥어‥‥？」
	PRINTFORML 「그래。그럼、빨리 나가라고。」
	PRINTFORMW 고개 숙인 %CALLNAME%의 표정은、격한 갈등에 시달리고 있는 것 같았다。
	CFLAG:219 = 2
;굴복각인Lv3＋함락소질無し
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORML 「저기、끝났다고 후딱 나가는 건 아니잖아？」
	PRINTFORML 「무료하게 보내는 것도 큰일이니까」
	PRINTFORML 「말상대 쯤은 어울려 줘요」
	PRINTFORMW 최근 %CALLNAME%의 태도가 부드러워져 온 것 같이 느껴진다‥‥
	CFLAG:219 = 3
;함락済み＋체력500미만
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;연모、친애
	IF TALENT:3 || TALENT:6
		PRINTFORML 「오늘도 격렬했죠？」
		PRINTFORML 「다음은 언제 올 수 있는 거야？」
		PRINTFORML 「가급적、빨리 와줘요？반드시요？」
		PRINTFORMW 울 것 같은 얼굴로 들어버렸다。가급적 빨리 오자‥‥
	;음란、창부
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORML 「응하아‥‥오늘도 잔뜩 기분 좋았다」
		PRINTFORML 「저기、다음은 언제 오는 거야？가급적 빨리‥‥？」
		PRINTFORML %조사처리(CALLNAME,"가")% 체력의 한계가 아니었다면、다시 엉망진창으로 만들었지도 모른다
		PRINTFORMW 그만큼、%조사처리(CALLNAME,"는")% 선정적이었다‥‥
	;복종、예속
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:218 = 1
;함락済み
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;연모、친애
	IF TALENT:3 || TALENT:6
		PRINTFORML 「저기、지쳤겠죠？」
		PRINTFORML 「%CSTR:일인칭%、엄마에게 마사지 배웠던 거야」
		PRINTFORML 「어때？마사지 해줄까？」
		PRINTFORMW 「우후후。손님、얼마든지 느긋하게 있어주세요？」
	;음란、창부
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORML 「어라？벌써 끝？」
		PRINTFORML 「%CSTR:일인칭%、아직 괜찮으니까 그만두지 마？」
		PRINTFORML 「마、마라、마라‥‥라고 하니까ー！」
		PRINTFORML 「풉、크크크크큭、아하하하하하하핫」
		PRINTFORMW %조사처리(CALLNAME,"는")% 아직 멀쩡해 보이는 것 같다。
	;복종、예속
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:219 = 4
ENDIF
RETURN 0

;--------------------------------------------------
;@KOJO_MESSAGE_COM関係（X1をキャラ번호に置換）
;コマンド実行時に출력されます
;--------------------------------------------------
@KOJO_MESSAGE_COM_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL TRAIN_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_COM_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_COM_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;コマンド実行時のセリフ CFLAG 300～500
;-------------------------------------------------
;=================================================
;애무계コマンド
;=================================================
;-------------------------------------------------
;0,애무
;-------------------------------------------------
SELECTCASE SELECTCOM
CASE 0
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:301 == 0
		PRINTFORML 「잠깐、당신、그런 곳‥‥」
		PRINTFORML 유키코는 비부에 얼굴을 가까이하는 %CALLNAME:MASTER%의 의도를 눈치챘는지、필사적으로 저항한다
		PRINTFORML 하지만、허벅지 윗부분을 붙잡힌 뒤에는、수치에 떨면서、
		PRINTFORMW 비부를 핥아질 뿐이었다
		CFLAG:301 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응읏、응하아‥‥아앙」
				PRINTFORMW 아직도 부끄러워서、비부를 핥는 %조사처리(CALLNAME:MASTER,"를")% 본、유키코는 얼굴을 빨갛게 한다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「응아아！좋아、거기이、더 해줘어」
				PRINTFORMW 유키코는、%CALLNAME:MASTER%의 비부로의 애무에 격하게 느끼고 있는 것 같다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:301 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:301 < 3 || FLAG:7 == 2)
			PRINTFORML 「응응‥‥응아‥‥싫어어‥‥」
			PRINTFORMW 시종일관 이쪽을 보려고도 하지 않았지만、그 몸은 솔직하게 반응했다
			CFLAG:301 = 3
		;それ以外
		ELSEIF CFLAG:301 < 2 || FLAG:7 == 2
			PRINTFORML 「이런 짓 해서、재미있는 거야？」
			PRINTFORMW 「흐으응、이상한 사람」
			CFLAG:301 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;1,커닐링구스
;-------------------------------------------------
CASE 1
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:301 == 0
		PRINTFORML 「잠깐、%CSTR:이인칭%、그런 곳‥‥」
		PRINTFORML %조사처리(CALLNAME,"는")% 비부에 얼굴을 가까이하는 %CALLNAME:MASTER%의 의도를 눈치챘는지、필사적으로 저항한다
		PRINTFORML 하지만、허벅지 윗부분을 붙잡힌 뒤에는、수치에 떨면서、
		PRINTFORMW 비부를 핥아질 뿐이었다
		CFLAG:301 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응읏、응하아‥‥아앙」
				PRINTFORMW 아직도 부끄러워서、비부를 핥는 %조사처리(CALLNAME:MASTER,"를")% 본、%조사처리(CALLNAME,"는")% 얼굴을 빨갛게 한다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「응아아！좋아、거기이、더 해줘어」
				PRINTFORMW %조사처리(CALLNAME,"는")%、%CALLNAME:MASTER%의 비부로의 애무에 격하게 느끼고 있는 것 같다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:301 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:301 < 3 || FLAG:7 == 2)
			PRINTFORML 「응응‥‥응아‥‥싫어어‥‥」
			PRINTFORMW 시종일관 이쪽을 보려고도 하지 않았지만、그 몸은 솔직하게 반응했다
			CFLAG:301 = 3
		;それ以外
		ELSEIF CFLAG:301 < 2 || FLAG:7 == 2
			PRINTFORML 「이런 짓 해서、재미있는 거야？」
			PRINTFORMW 「흐으응、이상한 사람」
			CFLAG:301 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;2,펠라한다
;-------------------------------------------------
CASE 2
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:302 == 0
		PRINTFORMW 
		CFLAG:302 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:302 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:302 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:302 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:302 = 3
		;それ以外
		ELSEIF CFLAG:302 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:302 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;3,손가락삽입
;-------------------------------------------------
CASE 3
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:303 == 0
		PRINTFORMW 「엣、잠깐、안돼！」
		CFLAG:303 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:303 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응아앗、%CSTR:이인칭%의 손가락、들어왔어‥‥」
				PRINTFORMW 젖고 젖은 거기는、%CALLNAME:MASTER%의 손가락을 단단히 졸라 놓지 않는다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「응응‥‥후우、응아‥‥」
				PRINTFORMW 손가락으로는 불만스러운 것 같다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:303 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:303 < 3 || FLAG:7 == 2)
			PRINTFORML 「응………하아……」
			PRINTFORMW 스스로의 안에서 꿈틀거리는 손가락에、쾌락을 느껴 오고 있는 것같다
			CFLAG:303 = 3
		;それ以外
		ELSEIF CFLAG:303 < 2 || FLAG:7 == 2
			PRINTFORMW 스스로의 안에서 꿈틀거리는 손가락에 대해、%조사처리(CALLNAME,"는")% 뚜렷이 얼굴에 혐오를 띄우고 있다
			CFLAG:303 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;4,애널핥기
;-------------------------------------------------
CASE 4
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:304 == 0
		PRINTFORMW 「우우‥‥싫어어‥‥」
		CFLAG:304 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「앗‥‥응응‥‥너무、움직이면、싫어어‥‥」
				PRINTFORMW 애무를 받아 기쁜 것과、엉덩이를 핥아졌다는 것에의 부끄러움으로、복잡한 표정을 하고 있다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORM 「응하아、좋은 거얏‥‥%CSTR:일인칭%、
				;엉덩이구멍狂い＋Ａ감각10
				IF TALENT:76 && Abl:5 == 10
					IF RAND:3 == 0
						PRINTFORM \@ RAND:2 == 0 ? 궁둥이보지 # 엉덩이보지 \@
					ELSEIF RAND:2 == 0
						PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
					ELSE
						PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
					ENDIF
				;엉덩이구멍狂い＋Ａ감각9
				ELSEIF TALENT:76 && Abl:5 == 9
					IF RAND:2 == 0
						PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
					ELSE
						PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
					ENDIF
				;엉덩이구멍狂い＋Ａ감각8
				ELSEIF TALENT:76 && Abl:5 == 8
					PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
				;それ以外
				ELSE
					PRINTFORM 엉덩이
				ENDIF
				PRINTFORML 도 느껴버리는 거야아」
				PRINTFORMW 「더、안까지 혀로 후벼줘어」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:304 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:304 < 3 || FLAG:7 == 2)
			PRINTFORMW 「우우‥‥그만둬、그만둬어‥‥」
			CFLAG:304 = 3
		;それ以外
		ELSEIF CFLAG:304 < 2 || FLAG:7 == 2
			PRINTFORMW 「싫어어‥‥그런 곳‥‥」
			CFLAG:304 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;5,애널애무
;-------------------------------------------------
CASE 5
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:305 == 0
		PRINTFORMW 「싫엇、그만둬！」
		CFLAG:305 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:305 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「이제、그만둬어‥‥부끄러워어‥‥」
				PRINTFORML 절레절레 고개를 저으면서、애널으로의 애무를 받고 있다
				PRINTFORMW 더 심술을 부리고 싶어졌다‥‥
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORM 「응하아、좋은 거야‥‥%CSTR:일인칭%、
				;엉덩이구멍狂い＋Ａ감각10
				IF TALENT:76 && Abl:5 == 10
					IF RAND:3 == 0
						PRINTFORM \@ RAND:2 == 0 ? 궁둥이보지 # 엉덩이보지 \@
					ELSEIF RAND:2 == 0
						PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
					ELSE
						PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
					ENDIF
				;엉덩이구멍狂い＋Ａ감각9
				ELSEIF TALENT:76 && Abl:5 == 9
					IF RAND:2 == 0
						PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
					ELSE
						PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
					ENDIF
				;엉덩이구멍狂い＋Ａ감각8
				ELSEIF TALENT:76 && Abl:5 == 8
					PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
				;それ以外
				ELSE
					PRINTFORM 엉덩이
				ENDIF
				PRINTFORML 도 느껴버리는 거야아」
				PRINTFORMW 「더、안까지 후벼줘어」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:305 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:305 < 3 || FLAG:7 == 2)
			PRINTFORMW 「기분나빠‥‥」
			CFLAG:305 = 3
		;それ以外
		ELSEIF CFLAG:305 < 2 || FLAG:7 == 2
			PRINTFORMW 「그만둬、그만두라니까！」
			CFLAG:305 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;6,가슴주무르기
;-------------------------------------------------
CASE 6
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:306 == 0
		PRINTFORMW 「기분나빠‥‥」
		CFLAG:306 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「가슴、만지는 거 좋아하는 거야？‥‥좋아、더 만져」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「응아、아응읏、후훗、더 세게 만져도 돼？」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:306 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:306 < 3 || FLAG:7 == 2)
			PRINTFORMW 「잠깐、그렇게 하면‥‥응앗‥‥」
			CFLAG:306 = 3
		;それ以外
		ELSEIF CFLAG:306 < 2 || FLAG:7 == 2
			PRINTFORMW 「큿‥‥응읏‥‥」
			CFLAG:306 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;7,유두자극
;-------------------------------------------------
CASE 7
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:307 == 0
		PRINTFORMW 「아팟‥‥정말、뭐！」
		CFLAG:307 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:307 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「아팟‥‥정말、%CSTR:일인칭%에겐 어울리지 않으니까、좀 더 부드럽게‥‥말야？」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「더、세게 주물러어、응아아앗」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:307 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:307 < 3 || FLAG:7 == 2)
			PRINTFORMW 「으극‥‥흐으‥‥아흑」
			CFLAG:307 = 3
		;それ以外
		ELSEIF CFLAG:307 < 2 || FLAG:7 == 2
			PRINTFORMW 「아파、아프다니까！」
			CFLAG:307 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;8,가슴애무
;-------------------------------------------------
CASE 8
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:308 == 0
		PRINTFORMW 「흐응‥‥응아‥‥아후‥‥」
		CFLAG:308 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:308 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「하아、아응읏‥‥응아아‥‥좋아、기분좋아‥‥」
				PRINTFORMW %조사처리(CALLNAME,"는")% 물기띈 눈동자로 %조사처리(CALLNAME:MASTER,"를")% 보고 있다‥‥
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「하아、아응읏‥‥응아아‥‥좋아、기분좋아‥‥」
				PRINTFORMW %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%의 손을 겹치도록 해서、스스로도 가슴을 애무하고 있다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:308 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:308 < 3 || FLAG:7 == 2)
			PRINTFORMW 「큭‥‥응아‥‥응으‥‥」
			CFLAG:308 = 3
		;それ以外
		ELSEIF CFLAG:308 < 2 || FLAG:7 == 2
			PRINTFORMW 「흑‥‥큭‥‥손대지 마！」
			CFLAG:308 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;9,조개젖히기
;-------------------------------------------------
CASE 9
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:309 == 0
		PRINTFORML 「！！‥‥그런 걸、할 수 있을 리‥‥！！」
		PRINTFORML 반항적인 %CALLNAME%에게、때리려는 것 같은 움직임을 보였다
		PRINTFORMW 「읏‥‥알겠어요‥‥큭‥‥왜 %조사처리(CSTR:일인칭,"가")%‥‥이런 걸！！」
		CFLAG:309 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「에？‥‥응。좋아」
				PRINTFORML 얼굴을 빨갛게 하고 다리를 Ｍ자로 벌려、스스로의 성기를 손가락으로 벌려 보인다
				PRINTFORMW 「부끄러워‥‥그렇게 뚫어지게 보지 말아 줘？」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「응。알았어。‥‥자알 봐요？」
				PRINTFORML 다리를 Ｍ자로 벌려、스스로의 성기를 손가락으로 크게 벌려 보인다。
				PRINTFORML 「우후후‥‥어때？흥분돼？감상료는、여기에、응？」
				PRINTFORMW 그리 말한、%조사처리(CALLNAME,"는")% 스스로의 성기를 가리킨다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:309 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:309 < 3 || FLAG:7 == 2)
			PRINTFORMW %조사처리(CALLNAME,"는")% 얼굴을 빨갛게 하면서도、솔직히 따랐다
			CFLAG:309 = 3
		;それ以外
		ELSEIF CFLAG:309 < 2 || FLAG:7 == 2
			PRINTFORMW %조사처리(CALLNAME,"는")% 명령에、마지못해서 따랐다。
			CFLAG:309 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;10,가슴자위
;-------------------------------------------------
CASE 10
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:310 == 0
		PRINTFORMW 「스스로 하라니‥‥%CSTR:이인칭%、정말로 쓰레기네요」
		CFLAG:310 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응읏、아아응읏‥‥하아‥‥우아、하앗！」
				PRINTFORML （아아‥‥보고 있어、그곳、그렇게 크게 해서、%CSTR:일인칭%의 걸‥‥）
				PRINTFORMW （안돼、그렇게 보면、느껴버려‥‥）
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「우후후、그렇게 뚫어지게 보다니‥‥」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:310 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:310 < 3 || FLAG:7 == 2)
			PRINTFORMW %조사처리(CALLNAME,"는")% 눈을 감아、되도록 사람이 있는 걸 의식하지 않도록 하고 있다‥‥
			CFLAG:310 = 3
		;それ以外
		ELSEIF CFLAG:310 < 2 || FLAG:7 == 2
			PRINTFORMW 「크응읏‥‥응아‥‥하앙‥‥」
			CFLAG:310 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;11,자위
;-------------------------------------------------
CASE 11
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:311 == 0
		PRINTFORMW 「스스로 하라니‥‥%CSTR:이인칭%、정말로 쓰레기네요」
		CFLAG:311 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:311 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응읏、아아응읏‥‥하아‥‥우아、하앗！」
				PRINTFORML （아아‥‥보고 있어、그곳、그렇게 크게 해서、%CSTR:일인칭%의 걸‥‥）
				PRINTFORMW （안돼、그렇게 보면、느껴버려‥‥）
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「우후후、그렇게 뚫어지게 보다니‥‥」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:311 = 4
		;순종Lv3이상
		ELSEIF ABL:0 >= 3 && (CFLAG:311 < 3 || FLAG:7 == 2)
			PRINTFORMW %조사처리(CALLNAME,"는")% 눈을 감아、되도록 사람이 있는 걸 의식하지 않도록 하고 있다‥‥
			CFLAG:311 = 3
		;それ以外
		ELSEIF CFLAG:311 < 2 || FLAG:7 == 2
			PRINTFORMW 「크응읏‥‥응아‥‥하앙‥‥」
			CFLAG:311 = 2
		ENDIF
	ENDIF


;=================================================
;コミュニケーション系コマンド
;=================================================
;-------------------------------------------------
;20,키스한다
;-------------------------------------------------
CASE 20
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:312 == 0
		PRINTFORML 「응응ー！우웁、푸핫！」
		PRINTFORMW 「최저‥‥정말로‥‥최에에저！！」
		CFLAG:312 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응‥‥츄릅、츄르르‥‥낼름‥‥츕、푸핫」
				PRINTFORMW 「저기、다시 한번、괜찮겠죠？」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「으흡‥‥츄르르、츄르릅‥‥츄릅‥‥」
				PRINTFORMW 이쪽이 입술을 떼어내려고 해도、등을 붙잡혀서 떼어낼 수 없다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:312 = 4
		;순종Lv3이상
		ELSEIF ABL:0 >= 3 && (CFLAG:312 < 3 || FLAG:7 == 2)
			PRINTFORMW 「응읍‥‥츄릅、츄릅‥‥쪽‥‥」
			CFLAG:312 = 3
		;それ以外
		ELSEIF CFLAG:312 < 2 || FLAG:7 == 2
			PRINTFORMW 「응읍‥‥츄릅、츄릅‥‥」
			CFLAG:312 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;21,아무것도안한다
;-------------------------------------------------
CASE 21
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:313 == 0
		PRINTFORMW 「애태울 작정인 거야？」
		CFLAG:313 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:313 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW %조사처리(CALLNAME,"는")%、%조사처리(CALLNAME:MASTER,"과")% 눈이 마주치자、생긋 미소지었다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「무슨 일이야？혹시、지쳐 버렸어？」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:313 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:313 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:313 = 3
		;それ以外
		ELSEIF CFLAG:313 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:313 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;22,회화　연모、친애のみ
;-------------------------------------------------
CASE 22
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:314 == 0
		PRINTFORML 「이야기？으으응‥‥그렇네‥‥」
		PRINTFORML %조사처리(CALLNAME,"과")% 얘기를 주고받으려고 생각했지만、자신만 얘기해 버렸다。
		PRINTFORML 상당히 듣는 게 능숙한 것 같다‥‥
		PRINTFORMW 슬슬 %조사처리(CALLNAME,"과")% 더 사이가 깊어진 느낌이 든다‥‥
		CFLAG:314 = 1
;二回目以降
	ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
		PRINTFORML 「흐으응、그래‥‥풋‥‥크큭 아하하하하하핫」
		PRINTFORML 「우후후、크크크큭、미안해‥‥그치만、크크크크」
		PRINTFORML 방금 전의 이야기로 뭔가 스위치가 들어간 것 같다。
		PRINTFORML 이렇게 되면 %조사처리(CALLNAME,"는")% 오래다‥‥
		PRINTFORMW %CALLNAME:MASTER%의 끈기가 올랐다
		CFLAG:314 = 2
	ENDIF

;-------------------------------------------------
;23,프레젠트　연모、친애のみ
;-------------------------------------------------
CASE 23
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:315 == 0
		PRINTFORMW 「와아、고마워！」
		CFLAG:315 = 1
;二回目以降
	ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
		PRINTFORMW 「우후후、기뻐。」
		CFLAG:315 = 2
	ENDIF

;-------------------------------------------------
;24,스킨쉽　연모、친애のみ
;-------------------------------------------------
CASE 24
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:316 == 0
		PRINTFORML %CALLNAME%에게 스킨쉽을 제안해 보았다
		PRINTFORML 「스킨쉽？ 그렇게 선언해 주는 걸로도 아니라고 생각하는데‥‥」
		PRINTFORML 「스킨쉽‥‥좋아하는 사람과、좋아、스킨‥‥십？」
		PRINTFORML 「풉‥‥크큭‥‥스킨십이 뭐야‥‥？크큭」
		PRINTFORML 웃게하지 않으려고 껴안아 붙들었지만、%조사처리(CALLNAME,"는")% 얼굴을 %CALLNAME:MASTER%의 가슴에 밀어붙이면서 쿡쿡 하고 웃기 시작한다
		PRINTFORMW 슬슬 %조사처리(CALLNAME,"과")% 더 사이가 깊어진 느낌이 든다‥‥
		CFLAG:316 = 1
;二回目以降
	ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
		PRINTFORML %CALLNAME%에게 기모노를 입혀 주었다。
		PRINTFORML 예쁘다고 칭찬하자、%조사처리(CALLNAME,"는")% 새빨갛게 되어 수줍어했다‥‥
		PRINTFORMW %CALLNAME:MASTER%의 전달력이 올랐다。
		CFLAG:316 = 2
	ENDIF

;-------------------------------------------------
;25,느긋하게있는다　연모、친애のみ
;-------------------------------------------------
CASE 25
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:317 == 0
		PRINTFORML %조사처리(CALLNAME,"과")% 어깨를 마주하고 느긋하게 지냈다。
		PRINTFORML 문득 깨달으면 %조사처리(CALLNAME,"는")% 꼬박꼬박 졸고 있다。
		PRINTFORMW 잠자는 얼굴을 충분히 만끽하고 나서 일으켰다
		CFLAG:317 = 1
;二回目以降
	ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
		PRINTFORML %CALLNAME%의 요망으로 무릎 베개를 하고 있다。
		PRINTFORML %CALLNAME%의 무게가 무릎에 전해져、느낌이 좋다
		PRINTFORML 아래 쪽에서 새근거리는 숨소리가 들려 온다。
		PRINTFORML 아무래도 잠이 들어버린 것 같다。
		PRINTFORMW 슬슬 무릎이 저려 왔다‥‥
		CFLAG:317 = 2
	ENDIF

;-------------------------------------------------
;26,가사　연모、친애のみ
;-------------------------------------------------
CASE 26
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:318 == 0
		PRINTFORML 「가사？‥‥하、할 수 있어요‥‥아마」
		PRINTFORML 카레일텐데、정체를 알 수 없는 고체 물질이 완성되었다‥‥
		PRINTFORML 「죄송합니다」
		PRINTFORML 낙담하는 %CALLNAME%의 머리를 쓰다듬어 위로한다。
		PRINTFORMW 슬슬 %조사처리(CALLNAME,"과")% 더 사이가 깊어진 느낌이 든다
		CFLAG:318 = 1
;二回目以降
	ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
			PRINTFORML 오늘은 도시락을 가지고 왔다。%조사처리(CALLNAME,"과")% 먹으려고‥‥
		IF RAND:2 == 0
			PRINTFORML 「맛있어！%CSTR:이인칭%、요리 능숙한 거네요？」
			PRINTFORMW %조사처리(CALLNAME,"는")% 만족한 것 같다‥‥
		ELSE
			PRINTFORML %조사처리(CALLNAME,"는")%、도시락의 샐러드를 지그시 바라보고 있다。
			PRINTFORML 잠시 후에 얼굴을 들어、%CALLNAME:MASTER%의 얼굴을 진지한 시선으로 바라보며、
			PRINTFORM 「이 샐러드에、%CSTR:이인칭%의
				IF Abl:16 >= 3
					PRINTFORM \@ RAND:2 == 0 ? 좆물 # 정액 \@
				ELSE
					PRINTFORM 정액
				ENDIF
			PRINTFORML 을 드레싱 대신 뿌리면、틀림없이 맛있어질 거라고 생각해！」
			PRINTFORMW 유키노는 진심이다…。
		ENDIF
	ENDIF

;-------------------------------------------------
;27,노래연습　연모、친애のみ
;-------------------------------------------------
CASE 27
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:319 == 0
		PRINTFORML %CALLNAME%에게 마시지의 연습상대를 자청했다‥‥
		PRINTFORML 경악할 만큼 어깨가 가볍다！%조사처리(CALLNAME,"는")% 득의양양해 한다‥‥
		PRINTFORML %조사처리(CALLNAME,"를")% 껴안아、머리를 쓰다듬어 칭찬했다。
		PRINTFORML 얼굴을 빨갛게 하면서도、%조사처리(CALLNAME,"는")% 기뻐 보인다‥‥
		PRINTFORMW 슬슬 %조사처리(CALLNAME,"과")% 더 사이가 깊어진 느낌이 든다
		CFLAG:319 = 1
;二回目以降
	ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
		PRINTFORML %CALLNAME%에게 안마의 비법을 배웠다。
		PRINTFORML %CALLNAME:MASTER%의 지식이 올랐다。
		PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 뭔가가 잘못되었다는 생각이 들었다‥‥
		PRINTFORMW %조사처리(CALLNAME,"과")% 즐겁게 잠깐의 시간을 보냈다。
		CFLAG:319 = 2
	ENDIF

;-------------------------------------------------
;28,매도
;-------------------------------------------------
CASE 28
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:320 == 0
		PRINTFORMW 
		CFLAG:320 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:320 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:320 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:320 = 3
		;それ以外
		ELSEIF CFLAG:320 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:320 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;29,비디오감상　함락済みのみ
;-------------------------------------------------
CASE 29
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:321 == 0
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:321 = 1
;二回目以降
	ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:321 = 2
	ENDIF


;=================================================
;道具사용コマンド
;=================================================
;40,로터
;-------------------------------------------------
CASE 40
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:322 == 0
		PRINTFORML 「뭐、뭐야、그거」
		PRINTFORML 대체적인 짐작은 하고 있는 것인지。
		PRINTFORML %조사처리(CALLNAME,"는")% 단단하게 다리를 닫고 있다。
		PRINTFORMW 억지로 다리를 벌려、로터를 클리토리스에 밀어붙인다‥‥
		CFLAG:322 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「장난감、쓰는 거야？」
				PRINTFORML 「으응‥‥별로 싫다는 건 아니야」
				PRINTFORML 「그치만、%조사처리(CSTR:일인칭,"는")%、%CSTR:이인칭%의 손으로 해주길 바라는데」
				PRINTFORMW 로터를 클리토리스에 밀어붙이자、조심스럽게 헐떡이는 소리를 지른다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「응아아아아아、기분 좋아앗、클리가 이상해져 버릴 것 같아！
				PRINTFORMW 「아아아아아아、응아아아아、히아아앗」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:322 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:322 < 3 || FLAG:7 == 2)
			PRINTFORMW 「큭、후응응읏‥‥응하앗」
			CFLAG:322 = 3
		;それ以外
		ELSEIF CFLAG:322 < 2 || FLAG:7 == 2
			PRINTFORMW 「큭、흐응읏、응아아아」
			CFLAG:322 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;41,전기마사지기
;-------------------------------------------------
CASE 41
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:323 == 0
		PRINTFORMW 「마사지 기계？‥‥%CSTR:이인칭%니까、정상적인 사용법이 아닐 것 같네요」
		CFLAG:323 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:323 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:323 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:323 = 3
		;それ以外
		ELSEIF CFLAG:323 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:323 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;42,클리캡
;-------------------------------------------------
CASE 42
	IF TEQUIP:11 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:324 == 0
			PRINTFORMW 
			CFLAG:324 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:324 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:324 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:324 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:324 = 3
			;それ以外
			ELSEIF CFLAG:324 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:324 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:324 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;43,오나홀
;-------------------------------------------------
CASE 43
	IF TEQUIP:12 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:325 == 0
			PRINTFORML 여성 안의 기분좋음을 알 수 있다고 꺼내온 이것을 본、
			PRINTFORMW %조사처리(CALLNAME,"는")% 이를 갈며 분한듯이 고개를 숙였다。
			CFLAG:325 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:325 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORML 「이거、%CSTR:이인칭%도 쓰는 일 있는 거야？」
					PRINTFORML 「흐ー응‥‥。있구나。」
					PRINTFORML %조사처리(CALLNAME,"는")% 자신의 애액을 %CALLNAME%의 거기에 바르고、오나홀을 씌웠다
					PRINTFORMW 좁아、지지 않을 거야、라고 중얼거리는 느낌이 들었다。
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:325 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:325 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:325 = 3
			;それ以外
			ELSEIF CFLAG:325 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:325 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:325 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;44,바이브
;-------------------------------------------------
CASE 44
	IF TEQUIP:13 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:326 == 0
			;처녀
			IF TALENT:0
				PRINTFORML 「힉、아파 아파 아파！싫어어、빼 줘어！」
				PRINTFORML 「그으우우、용서못해！죽여 버리겠어、그으우、히그으우우우」
				PRINTFORML %CALLNAME:MASTER%으로의 미움으로 얼버무릴 수 있던 것도、처음 얼마 동안뿐
				PRINTFORML %조사처리(CALLNAME,"는")% 스스로의 처음이 사람이 아닌 무기물에 꿰뚫어졌다고 하는 사실에 괴로워하며、
				PRINTFORMW 뚝뚝하고 눈물을 흘린다
			;それ以外
			ELSE
				PRINTFORML 「절대로 용서못해　응아앗、그러니까‥‥절대로‥‥」
				PRINTFORMW 바이브의 진동을 참으면서、한결같이 %CALLNAME:MASTER%에 대해、원망하는 말을 계속했다
			ENDIF
			CFLAG:326 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:326 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORML 「장난감、쓰는 거야？」
					PRINTFORML 「으응‥‥별로 싫다는 건 아니야」
					PRINTFORML 「그치만、%조사처리(CSTR:일인칭,"는")%、%CSTR:이인칭%의 것을 원하는데」
					PRINTFORMW 바이브를 %CALLNAME%의 질내에 넣자、조심스럽게 헐떡이는 소리를 지른다
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORML 「아하아、그거、좋아해‥‥큰 걸로、안을 엉망진창으로 휘저어 줘‥‥」
					PRINTFORML 「그치마안、%CSTR:이인칭%에게 질척질척되는 쪽을、더 좋아해」
					PRINTFORML 그 말을 들으면서、사양하지 않고 바이브를 넣어 간다
					PRINTFORML 「하아아아、들어왔다아、굵은 거어‥‥응아아、하아아아아」
					PRINTFORMW 칠칠치 못하게 입을 벌려、침을 흘리며、바이브를 즐기고 있다‥‥
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:326 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:326 < 3 || FLAG:7 == 2)
				PRINTFORML 「앗、응앗、하아앙、우아아아」
				PRINTFORMW 바이브가 꿈틀거릴 때마다 %조사처리(CALLNAME,"는")% 귀여운 울음소리를 지른다
				CFLAG:326 = 3
			;それ以外
			ELSEIF CFLAG:326 < 2 || FLAG:7 == 2
				PRINTFORMW 「빼 줫、이런 거‥‥빨리 빼 줘어！」
				CFLAG:326 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:326 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;45,애널바이브
;-------------------------------------------------
CASE 45
	IF TEQUIP:14 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:327 == 0
			PRINTFORML 「힉、아파 아파 아파！싫어어、빼 줘어！」
			PRINTFORML 「그으우우、용서못해！죽여버리겠어、그으우、히그으우우우」
			PRINTFORML %CALLNAME:MASTER%에 대한 미움으로 얼버무릴 수 있던 것도、처음 얼마 동안뿐
			PRINTFORML %조사처리(CALLNAME,"는")% 스스로의 배설기관이 심하게 휘둘려지고 있다는 사실에 괴로워하며、
			PRINTFORMW 뚝뚝하고 눈물을 흘린다
			CFLAG:327 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:327 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORML 「장난감、쓰는 거야？」
					PRINTFORML 「으응‥‥별로 싫다는 건 아니야」
					PRINTFORML 「그치만、%조사처리(CSTR:일인칭,"는")%、%CSTR:이인칭%의 것을 원하는데」
					PRINTFORMW 애널바이브를 %CALLNAME%의 국문에 넣자、조심스럽게 헐떡이는 소리를 지른다
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORML 「아하아、그거、좋아해‥‥큰 걸로、안을 엉망진창으로 휘저어 줘‥‥」
					PRINTFORML 「그치마안、%CSTR:이인칭%에게 질척질척되는 쪽을、더 좋아해」
					PRINTFORML 그 말을 들으면서、사양하지 않고 애널바이브를 넣어 간다
					PRINTFORML 「하아아아、들어왔다아、굵은 거어‥‥응아아、하아아아아」
					PRINTFORMW 칠칠치 못하게 입을 벌려、침을 흘리며、애널바이브를 즐기고 있다‥‥
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:327 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:327 < 3 || FLAG:7 == 2)
				PRINTFORML 「앗、응앗、하아앙、우아아아」
				PRINTFORMW 애널바이브가 꿈틀거릴 때마다 %조사처리(CALLNAME,"는")% 귀여운 울음소리를 지른다
				CFLAG:327 = 3
			;それ以外
			ELSEIF CFLAG:327 < 2 || FLAG:7 == 2
				PRINTFORMW 「빼 줫、이런 거‥‥빨리 빼 줘어！」
				CFLAG:327 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:327 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;46,애널비즈
;-------------------------------------------------

CASE 46
	IF TEQUIP:15 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:328 == 0
			PRINTFORML 「힉、아파 아파 아파！싫어어、빼 줘어！」
			PRINTFORML 「그으우우、용서못해！죽여버리겠어、그으우、히그으우우우」
			PRINTFORML %CALLNAME:MASTER%에 대한 미움으로 얼버무릴 수 있던 것도、처음 얼마 동안뿐
			PRINTFORMW %조사처리(CALLNAME,"는")% 구슬이 들어올 때마다 신음 소리를 지른다。
			CFLAG:328 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:328 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORML 「장난감、쓰는 거야？」
					PRINTFORML 「으응‥‥별로 싫다는 건 아니야」
					PRINTFORML 「그치만、%조사처리(CSTR:일인칭,"는")%、%CSTR:이인칭%의 것을 원하는데」
					PRINTFORMW 애널비즈의 구슬을 %CALLNAME%의 국문이 삼킬 때마다、조심스럽게 헐떡이는 소리를 지른다
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORML 「앗、응앗、하아앙、우아아아」
					PRINTFORMW 애널비즈의 구슬을 국문이 삼킬 때마다 %조사처리(CALLNAME,"는")% 귀여운 울음소리를 지른다
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:328 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:328 < 3 || FLAG:7 == 2)
				PRINTFORMW 「빼 줫、이런 거‥‥빨리 빼 줘어！」
				CFLAG:328 = 3
			;それ以外
			ELSEIF CFLAG:328 < 2 || FLAG:7 == 2
				PRINTFORMW 「싫어어、이런 거‥‥싫어어！」
				CFLAG:328 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:328 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;47,유두캡
;-------------------------------------------------
CASE 47
	IF TEQUIP:16 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:329 == 0
			PRINTFORMW 
			CFLAG:329 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:329 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:329 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:329 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:329 = 3
			;それ以外
			ELSEIF CFLAG:329 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:329 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:329 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;48,유두로터
;-------------------------------------------------
CASE 48
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:330 == 0
		PRINTFORMW 
		CFLAG:330 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:330 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:330 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:330 = 3
		;それ以外
		ELSEIF CFLAG:330 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:330 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;49,착유기
;-------------------------------------------------
CASE 49
	IF TEQUIP:17 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:331 == 0
			PRINTFORMW 
			CFLAG:331 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:331 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:331 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:331 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:331 = 3
			;それ以外
			ELSEIF CFLAG:331 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:331 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:331 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;=================================================
;섹스系コマンド
;=================================================
;-------------------------------------------------
;60,정상위
;-------------------------------------------------
CASE 60
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:332 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 4 || FLAG:7 == 2)
				PRINTFORML %CALLNAME%의 처음을 받고 싶다는 뜻을 말했다。
				PRINTFORML 「‥‥응。좋아、%CSTR:이인칭%에게라면、처음을 줘도。」
				PRINTFORML 「그치만、대단히 아플 것 같으니까、하다못해、상냥하게、응？」
				PRINTFORML %CALLNAME%의 입구에 몇번 귀두를 문질러、조금씩 허리를 앞으로 나아간다
				PRINTFORML 「아、그으우、하아아아아、큿우우우」
				PRINTFORML 겨우、모조리 %CALLNAME%의 질에 들어갔다。거기에서는 처음의 증표가 흘러나간다‥‥
				PRINTFORML 「아프‥‥지만、기、뻐、어。」
				PRINTFORML 「%조사처리(CSTR:일인칭,"는")%、힘내서、참으니까、움직여도、돼。괴、롭、지요？」
				PRINTFORML 그 말을 받아들여、
				PRINTFORML 매우 지나치게 흥분해서 아플 정도의 페니스를 피스톤하자、
				PRINTFORMW 현기증 날 것 같은 쾌감이 몰려온다‥‥
			ELSE
				PRINTFORML 페니스를 훑으면서 %CALLNAME%의 성기에 대었다
				PRINTFORML 그대로、난폭하게 찔러넣어 허리를 그라인드하자、격한 쾌락이 %조사처리(CALLNAME:MASTER,"를")% 덮쳤다
				PRINTFORMW 방에는 %CALLNAME%에게 허리를 쳐박는 소리와、%CALLNAME%의 흐느끼는 소리만이 울려퍼지고 있다‥‥
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 4 || FLAG:7 == 2)
				PRINTFORML 「정상위‥‥？이런‥‥걸까？」
				PRINTFORML %조사처리(CALLNAME,"는")% 침대에 누워、천천히 다리를 열어、%조사처리(CALLNAME:MASTER,"가")% 삽입하기 쉽도록、손가락으로 성기를 벌렸다。
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")% %CALLNAME%의 안을 맛보면서、천천히 페니스를 나아가게 했다。
				PRINTFORML 「아‥‥하아‥‥응응읏‥‥。」
				PRINTFORMW 「%CSTR:이인칭%의 얼굴、보면서 할 수 있으니까‥‥좋아할、지도‥‥」
			ELSE
				PRINTFORML 「예、부디。후딱 끝내고 나가라니까。」
				PRINTFORML %조사처리(CALLNAME,"는")% 이쪽을 노려보며、반쯤 자포자기해 다리를 벌린다。
				PRINTFORML 「크、응응읏！우앗웃！‥‥아앗！」
				PRINTFORMW %조사처리(CALLNAME:MASTER,"가")% 페니스를 삽입해、움직이기 시작해도、절대로 %CALLNAME:MASTER%의 얼굴을 보지 않으려고、얼굴을 돌리고 있다‥‥
			ENDIF
		ENDIF
		CFLAG:332 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「앗、앙읏、하아、으음、하아앗」
				PRINTFORML 「기분、좋앗？하아、그래、응아앗、다행이다아」
				PRINTFORML %CALLNAME:MASTER%의 목에 손을 감아、키스를 조른다。
				PRINTFORMW 거기에 응해 주자、행복해하여 빙그레 웃는다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「아하아、더、더、찔러 줘어！」
				PRINTFORML 「응아아아앗、응핫、앗、아아아아아아아！！」
				PRINTFORML 「안이 박박 문질려서、뺄 때 끝부분이 입구 세게 문질러서」
				PRINTFORML 「핫、격렬、해애、하앗、대단、아아아아！」
				PRINTFORMW 지나친 격렬함에、%조사처리(CALLNAME,"는")% 크게 입을 벌리고 필사적으로 %CALLNAME:MASTER%의 목에 매달려 있다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:332 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:332 < 3 || FLAG:7 == 2)
			PRINTFORML 「응아아아앗、응핫、앗、아아아아아아아！！」
			PRINTFORMW %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%에게 찔려져、명백한 쾌락을 느끼고 있다
			CFLAG:332 = 3
		;それ以外
		ELSEIF CFLAG:332 < 2 || FLAG:7 == 2
			PRINTFORML 「응읏、으극、하아、으읍、아앗」
			PRINTFORMW %조사처리(CALLNAME,"는")% 스스로의 손가락을 물어 헐떡이는 소리를 참고 있는 것 같다
			CFLAG:332 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;61,후배위
;-------------------------------------------------
CASE 61
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:333 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 4 || FLAG:7 == 2)
				PRINTFORML 「뒤、뒤에서라니‥‥이、이렇게？」
				PRINTFORML 엉덩이를 높게 올려、성기를 스스로 벌려 보인다。
				PRINTFORML 「우우‥‥부끄러워。처음인데‥‥。」
				PRINTFORML 「천천히、야。어쩔 수 없는 아픔은 참으니까。부드럽게 해요？」
				PRINTFORML %CALLNAME%의 허리를 잡아、천천히 페니스를 안으로 묻어 간다。
				PRINTFORML 「응‥‥으그우우우우、그으‥‥아아아아、들어、갔다？」
				PRINTFORML 「%CSTR:이인칭%의 얼굴、볼 수 없는、것은、아쉽、지만、그 만큼、깊게 %CSTR:이인칭%의 것이‥‥」
				PRINTFORML 「들어온 걸‥‥알겠어。그치만、움직이는 건 기다려‥‥아직、안이‥‥」
				PRINTFORML 약간이라도 아픔을 달랠 수 있도록、손을 앞으로 돌려서、클리토리스와 가슴을 천천히 애무한다
				PRINTFORML 「응‥‥손、상냥해‥‥。미안해요。좀 더、하면、움직여도‥‥괜찮을 것 같으니까‥‥」
				PRINTFORMW 잠시 몸을 애무하고、그리고 나서 천천히 허리를 움직이기 시작한다‥‥
			ELSE
				PRINTFORML 억지로、엎드리게 해、허리를 들어올리고、페니스를 훑으면서 %CALLNAME%의 성기에 대었다
				PRINTFORML 그대로、난폭하게 찔러넣어 허리를 그라인드하자、격한 쾌락이 %조사처리(CALLNAME:MASTER,"를")% 덮쳤다
				PRINTFORMW 방에는 %CALLNAME%에게 허리를 쳐박는 소리와、%CALLNAME%의 흐느끼는 소리만이 울려퍼지고 있다‥‥
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 4 || FLAG:7 == 2)
				PRINTFORML 「좋아、잠깐。‥‥어때？」
				PRINTFORML %조사처리(CALLNAME,"는")% 엎드리면서 엉덩이를 천천히 들어올려、좌우로 흔들어 보인다
				PRINTFORML 「뒤에서 라는 건、이런、거지요？」
				PRINTFORML 「좋아、와 줘‥‥？」
				PRINTFORMW 뒤에서 단번에 찔러 넣자、몸을 부들부들 떨며、쾌락을 느끼고 있다
			ELSE
				PRINTFORML 뒤에서、%조사처리(CALLNAME:MASTER,"가")% %조사처리(CALLNAME,"를")% 범하자、입을 한일자로 굳게 다물고
				PRINTFORMW 시트를 움켜쥐어、한결같이 굴욕에 견디고 있다‥‥
			ENDIF
		ENDIF
		CFLAG:333 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「예、부디」
				PRINTFORML 엉덩이를 이쪽으로 향해、성기를 스스로 벌려 보인다
				PRINTFORML 「정말‥‥빤히 보지 마。부끄러우니까‥‥」
				PRINTFORML 「응읏、들어‥‥온、다아、하아아아아」
				PRINTFORML 움직이지 않으면、%조사처리(CALLNAME,"는")% 스스로 허리를 움직이기 시작한다
				PRINTFORML 「사양、말고、움직여 주지 않으면、애타니까‥‥」
				PRINTFORMW 말한 대로 움직이기 시작하자、성기에서 투명한 액체를 떨어뜨리며、환희의 소리를 지른다。
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「빨릿‥‥빨리、%CSTR:이인칭%의 것、안까지 넣어 줘어」
				PRINTFORML 단번에 페니스를 찔러 넣자、%CALLNAME%의 질은 환희에 떨며、꾹꾹하고 단단히 조른다。
				PRINTFORMW 「역시、%CSTR:이인칭%의 걸로 하는 것이、제일、좋앗、응하앗！」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:333 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:333 < 3 || FLAG:7 == 2)
			PRINTFORML 「크、응읏、핫、하앗」
			PRINTFORMW %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%의 물건을 단단히 조이며、기뻐하고 있는 것 같다‥‥
			CFLAG:333 = 3
		;それ以外
		ELSEIF CFLAG:333 < 2 || FLAG:7 == 2
			PRINTFORMW 「응응읏、응아앗、으그으우우우」
			CFLAG:333 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;62,정상위애널
;-------------------------------------------------
CASE 62
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:334 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 4 || FLAG:7 == 2)
				PRINTFORML %CALLNAME%의 엉덩이 구멍을 쓰고 싶다는 뜻을 말했다。
				PRINTFORML 「‥‥응。좋아、%CSTR:이인칭%에게라면、이곳、써도。」
				PRINTFORML 「그치만、대단히 아플 것 같으니까、하다못해、상냥하게、응？」
				PRINTFORML %CALLNAME%의 균열에서 흘러내린 애액을 귀두에 바르고 나서、국화에 조금씩 허리를 앞으로 나아가게 한다
				PRINTFORML 「아、그으우、하아아아아、큭우우우」
				PRINTFORML 겨우、모조리 %CALLNAME%의 국화에 들어갔다。조금 찢어진 건지 피가 똑 떨어졌다‥‥
				PRINTFORML 「아프‥‥지만、기、뻐、어。」
				PRINTFORML 「%조사처리(CSTR:일인칭,"는")%、힘내서、참으니까、움직여도、돼。괴、롭、지요？」
				PRINTFORMW 그 말을 받아들여、매우 지나치게 흥분해서 아플 정도의 페니스를 피스톤하자、현기증 날 것 같은 쾌감이 몰려온다‥‥
			ELSE
				PRINTFORML 페니스를 훑으면서 %CALLNAME%의 국화에 대었다
				PRINTFORML 그대로、난폭하게 찔러넣어 허리를 그라인드하자、격한 쾌락이 %조사처리(CALLNAME:MASTER,"를")% 덮쳤다
				PRINTFORMW 방에는 %CALLNAME%에게 허리를 쳐박는 소리와、%CALLNAME%의 흐느끼는 소리만이 울려퍼지고 있다‥‥
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 4 || FLAG:7 == 2)
				PRINTFORML 「정상위‥‥？이런‥‥걸까？」
				PRINTFORML %조사처리(CALLNAME,"는")% 침대에 침대에 누워、천천히 다리를 열어、%조사처리(CALLNAME:MASTER,"가")% 삽입하기 쉽도록、양손으로 엉덩이살을 벌렸다。
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")% %CALLNAME%의 국화를 맛보면서、천천히 페니스를 나아가게 했다。
				PRINTFORML 「아‥‥하아‥‥응응읏‥‥。」
				PRINTFORMW 「%CSTR:이인칭%의 얼굴、보면서 할 수 있으니까‥‥좋아할、지도‥‥」
			ELSE
				PRINTFORML 「예、부디。후딱 끝내고 나가라니까。」
				PRINTFORML %조사처리(CALLNAME,"는")% 이쪽을 이쪽을 노려보며、반쯤 자포자기해 다리를 벌린다。
				PRINTFORML 「크、응응읏！우앗웃！‥‥아앗！」
				PRINTFORMW %조사처리(CALLNAME:MASTER,"가")% 페니스를 삽입해、움직이기 시작해도、절대로 %CALLNAME:MASTER%의 얼굴을 보지 않으려고、얼굴을 돌리고 있다‥‥
			ENDIF
		ENDIF
		CFLAG:334 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「앗、앙읏、하아、으음、하아앗」
				PRINTFORML 「기분、좋앗？하아、그래、응아앗、다행이다아」
				PRINTFORML %CALLNAME:MASTER%의 목에 손을 감아、키스를 조른다。
				PRINTFORMW 거기에 응해 주자、행복해하여 빙그레 웃는다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「아하아、더、더、찔러 줘어！」
				PRINTFORML 「응아아아앗、응핫、앗、아아아아아아아！！」
				PRINTFORML 「뱃속이 박박 문질려서、뺄 때 끝부분이 입구 세게 문질러서」
				PRINTFORML 「핫、격렬、해애、하앗、대단、아아아아！」
				PRINTFORMW 지나친 격렬함에、%조사처리(CALLNAME,"는")% 크게 입을 벌리고 필사적으로 %CALLNAME:MASTER%의 목에 매달려 있다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:334 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:334 < 3 || FLAG:7 == 2)
			PRINTFORML 「응아아아앗、응핫、앗、아아아아아아아！！」
			PRINTFORMW %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%에게 엉덩이 구멍을 찔려져、명백한 쾌락을 느끼고 있다
			CFLAG:334 = 3
		;それ以外
		ELSEIF CFLAG:334 < 2 || FLAG:7 == 2
			PRINTFORML 「응읏、으극、하아、으읍、아앗」
			PRINTFORMW %조사처리(CALLNAME,"는")% 스스로의 손가락을 물어 헐떡이는 소리를 참고 있는 것 같다
			CFLAG:334 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;63,후배위애널
;-------------------------------------------------
CASE 63
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:335 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 4 || FLAG:7 == 2)
				PRINTFORML 「뒤、뒤에서라니‥‥이、이렇게？」
				PRINTFORML 엉덩이를 높게 올려、엉덩이살을 스스로 벌려 보인다。
				PRINTFORML 「우우‥‥부끄러워。처음인데‥‥。」
				PRINTFORML 「천천히、야。어쩔 수 없는 아픔은 참으니까。부드럽게 해요？」
				PRINTFORML %CALLNAME%의 허리를 잡아、국화로 천천히 페니스를 안으로 묻어 간다。
				PRINTFORML 「응‥‥으그우우우우、그으‥‥아아아아、들어、갔다？」
				PRINTFORML 「%CSTR:이인칭%의 얼굴、볼 수 없는、것은、아쉽、지만、그 만큼、깊게 %CSTR:이인칭%의 것이‥‥」
				PRINTFORML 「들어온 걸‥‥알겠어。그치만、움직이는 건 기다려‥‥아직、뱃속이‥‥」
				PRINTFORML 약간이라도 아픔을 달랠 수 있도록、손을 앞으로 돌려서、클리토리스와 가슴을 천천히 애무한다
				PRINTFORML 「응‥‥손、상냥해‥‥。미안해요。좀 더、하면、움직여도‥‥괜찮을 것 같으니까‥‥」
				PRINTFORMW 잠시 몸을 애무하고、그리고 나서 천천히 허리를 움직이기 시작한다‥‥
			ELSE
				PRINTFORML 억지로、엎드리게 해、허리를 들어올리고、페니스를 훑으면서 %CALLNAME%의 국화에 대었다
				PRINTFORML 그대로、난폭하게 찔러넣어 허리를 그라인드하자、격한 쾌락이 %조사처리(CALLNAME:MASTER,"를")% 덮쳤다
				PRINTFORMW 방에는 %CALLNAME%에게 허리를 쳐박는 소리와、%CALLNAME%의 흐느끼는 소리만이 울려퍼지고 있다‥‥
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 4 || FLAG:7 == 2)
				PRINTFORML 「좋아、잠깐。‥‥어때？」
				PRINTFORML %조사처리(CALLNAME,"는")% 엎드리면서 엉덩이를 천천히 들어올려、좌우로 흔들어 보인다
				PRINTFORML 「뒤에서 라는 건、이런、거지요？」
				PRINTFORML 「좋아、와 줘‥‥？」
				PRINTFORMW 뒤에서 단번에 찔러 넣자、몸을 부들부들 떨며、쾌락을 느끼고 있다
			ELSE
				PRINTFORML 뒤에서、%조사처리(CALLNAME:MASTER,"가")% %조사처리(CALLNAME,"를")% 범하자、입을 한일자로 굳게 다물고
				PRINTFORMW 시트를 움켜쥐어、한결같이 굴욕에 견디고 있다‥‥
			ENDIF
		ENDIF
		CFLAG:335 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「예、부디」
				PRINTFORML 엉덩이를 이쪽으로 향해、엉덩이살을 스스로 벌려 보인다
				PRINTFORML 「정말‥‥빤히 보지 마。부끄러우니까‥‥」
				PRINTFORML 「응읏、들어‥‥온、다아、하아아아아」
				PRINTFORML 움직이지 않으면、%조사처리(CALLNAME,"는")% 스스로 허리를 움직이기 시작한다
				PRINTFORML 「사양、말고、움직여 주지 않으면、애타니까‥‥」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「빨릿‥‥빨리、%CSTR:이인칭%의 것、안까지 넣어 줘어」
				PRINTFORML 단번에 페니스를 찔러 넣자、%CALLNAME%의 엉덩이 구멍은 환희에 떨며、꾹꾹하고 단단히 조른다。
				PRINTFORMW 「역시、%CSTR:이인칭%의 걸로 하는 것이、제일、좋앗、응하앗！」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:335 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:335 < 3 || FLAG:7 == 2)
			PRINTFORML 「크、응읏、핫、하앗」
			PRINTFORMW %CALLNAME%의 엉덩이 구멍은 %CALLNAME:MASTER%의 물건을 단단히 조이며、기뻐하고 있는 것 같다‥‥
			CFLAG:335 = 3
		;それ以外
		ELSEIF CFLAG:335 < 2 || FLAG:7 == 2
			PRINTFORMW 「응응읏、응아앗、으그으우우우」
			CFLAG:335 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;64,역강간
;-------------------------------------------------
CASE 64
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:336 == 0
		PRINTFORMW 
		CFLAG:336 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:336 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:336 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:336 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:336 = 3
		;それ以外
		ELSEIF CFLAG:336 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:336 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;65,젖성교
;-------------------------------------------------
CASE 65
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:337 == 0
		PRINTFORMW 
		CFLAG:337 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:337 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「그렇게、%CSTR:일인칭%의 가슴、좋아해？우후후、그래。」
				PRINTFORML 어딘지 모르게 기쁜듯이 웃으면서、자신의 골짜기에 침을 떨어드려 간다
				PRINTFORML 「미끈미끈해서、넣어달라는 듯이 보이겠죠？」
				PRINTFORMW 「더 가슴에서 비벼도 되는 거야？잔뜩 정자 싸는 곳 보여 줘요」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:337 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:337 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:337 = 3
		;それ以外
		ELSEIF CFLAG:337 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:337 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;66,기승위
;-------------------------------------------------
CASE 66
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:338 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 4 || FLAG:7 == 2)
				PRINTFORML 「에엣！？스、스스로？%CSTR:일인칭%、처음인 건데‥‥」
				PRINTFORML 「빨리라고‥‥정말、알았으니까。」
				PRINTFORML 「정말、어째서 %조사처리(CALLNAME:MASTER,"는")% 이런、매니악한 짓만。하아‥‥。」
				PRINTFORML %조사처리(CALLNAME,"는")%、툴툴 대면서도、%CALLNAME:MASTER%의 위에 올라타고 천천히 허리를 내려 간다。
				PRINTFORML 「아파‥‥으그으、우으우우우‥‥。드、들어갔다‥‥？」
				PRINTFORML 내리깐 눈에 고인 눈물을 닦아주자、약간 기분을 푼 것 같다
				PRINTFORML 「자、움직여요？‥‥절대로、%조사처리(CSTR:이인칭,"는")% 움직이면 안돼？」
				PRINTFORMW 그리고、천천히 %CALLNAME:MASTER%에게 봉사를 시작한다‥‥
			ELSE
				PRINTFORML 「싫어、절대로 싫어！」
				PRINTFORML 바둥거리며 날뛰는 %CALLNAME%의 허리를、공중에서 고정하고、단번에 허리를 밀어올려 꿰뚫는다。
				PRINTFORML 「으그아아아！아파、빼 줘！빼 달라니까」
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%、눈에 눈물을 띄워 움직일 상황이 아닌 %조사처리(CALLNAME,"를")% 아래에서 사정없이 밀어올린다
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 4 || FLAG:7 == 2)
				PRINTFORML 「응읏、하아아아아、아아아아、깊어、엇」
				PRINTFORML 「하앗、하앗。잘 할 수 없어도、일어나지 마요？」
				PRINTFORMW %조사처리(CALLNAME,"는")% 그리 말하고、%CALLNAME:MASTER%에게 매달려、허리만을 천천히 위아래로 움직이기 시작한다‥‥
			ELSE
				PRINTFORML 허리를 떨어뜨리고 나서 전혀 움직일 기색이 없으므로、아래에서 밀어올린다
				PRINTFORMW %조사처리(CALLNAME,"는")% 단단히 다문 입에서 오열을 흘리면서、될대로 되고 있다
			ENDIF
		ENDIF
		CFLAG:338 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%의 위에 올라타고、애태우듯이 페니스를 성기에 비벼댄다
				PRINTFORML 「우후후、넣고 싶어？응、알았어。」
				PRINTFORML 「하아아아아아、안까지 들어와 버렸다아」
				PRINTFORML 「%CSTR:이인칭%의 것、씰룩씰룩거려‥‥。기다리고 있어、금방 기분 좋게 해줄게。」
				PRINTFORMW 그리 말하고、%CALLNAME:MASTER%의 가슴팍에 손을 대 원을 그리듯이 허리를 움직이기 시작한다‥‥
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML %조사처리(CALLNAME,"는")% 페니스에 손을 따라、단번에 허리를 떨어뜨렸다
				PRINTFORML 「하아아아아아、기분、좋아아、하아、응아아아、미、미안、하아、허리、빠져 버렸다」
				PRINTFORMW 움직이지 못하게 된 %CALLNAME%대신에 아래서 밀어올려 주자、침을 흘리면서 헐떡이는 소리를 지른다。
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:338 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:338 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			PRINTFORMW 
			CFLAG:338 = 3
		;それ以外
		ELSEIF CFLAG:338 < 2 || FLAG:7 == 2
			PRINTFORML 허리를 떨어뜨리고 나서 전혀 움직일 기색이 없으므로、아래에서 밀어올린다
			PRINTFORML %조사처리(CALLNAME,"는")% 단단히 다문 입에서 오열을 흘리면서、될대로 되고 있다
			CFLAG:338 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;67,기승위애널
;-------------------------------------------------
CASE 67
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:339 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 4 || FLAG:7 == 2)
				PRINTFORML 「에엣！？스、스스로？%CSTR:일인칭%、처음인 건데‥‥」
				PRINTFORML 「빨리라고‥‥정말、알았으니까。」
				PRINTFORML 「정말、어째서 %조사처리(CALLNAME:MASTER,"는")% 이런、매니악한 짓만。하아‥‥。」
				PRINTFORML %조사처리(CALLNAME,"는")%、툴툴 대면서도、%CALLNAME:MASTER%의 위에 올라타고 천천히 허리를 내려 간다。
				PRINTFORML 「아파‥‥으그으、우으우우우‥‥。드、들어갔다‥‥？」
				PRINTFORML 내리깐 눈에 고인 눈물을 닦아주자、약간 기분을 푼 것 같다
				PRINTFORML 「자、움직여요？‥‥절대로、%조사처리(CSTR:이인칭,"는")% 움직이면 안돼？」
				PRINTFORMW 그리고、천천히 %CALLNAME:MASTER%에게 봉사를 시작한다‥‥
			ELSE
				PRINTFORML 「싫어、절대로 싫어！」
				PRINTFORML 바둥거리며 날뛰는 %CALLNAME%의 허리를、공중에서 고정하고、단번에 허리를 밀어올려 꿰뚫는다。
				PRINTFORML 「으그아아아！아파、빼 줘！빼 달라니까」
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%、눈에 눈물을 띄워 움직일 상황이 아닌 %조사처리(CALLNAME,"를")% 아래에서 사정없이 밀어올린다
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 4 || FLAG:7 == 2)
				PRINTFORML 「응읏、하아아아아、아아아아、깊어、엇」
				PRINTFORML 「하앗、하앗。잘 할 수 없어도、일어나지 마요？」
				PRINTFORMW %조사처리(CALLNAME,"는")% 그리 말하고、%CALLNAME:MASTER%에게 매달려、허리만을 천천히 위아래로 움직이기 시작한다‥‥
			ELSE
				PRINTFORML 허리를 떨어뜨리고 나서 전혀 움직일 기색이 없으므로、아래에서 밀어올린다
				PRINTFORMW %조사처리(CALLNAME,"는")% 단단히 다문 입에서 오열을 흘리면서、될대로 되고 있다
			ENDIF
		ENDIF
		CFLAG:339 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML %조사처리(CALLNAME,"는")% %CALLNAME:MASTER%의 위에 올라타고、애태우듯이 페니스를 국화에 비벼댄다
				PRINTFORML 「우후후、넣고 싶어？응、알았어。」
				PRINTFORML 「하아아아아아、안까지 들어와 버렸다아」
				PRINTFORML 「%CSTR:이인칭%의 것、씰룩씰룩거려‥‥。기다리고 있어、금방 기분 좋게 해줄게。」
				PRINTFORMW 그리 말하고、%CALLNAME:MASTER%의 가슴팍에 손을 대 원을 그리듯이 허리를 움직이기 시작한다‥‥
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML %조사처리(CALLNAME,"는")% 페니스에 손을 따라、단번에 허리를 떨어뜨렸다
				PRINTFORML 「하아아아아아、기분、좋아아、하아、응아아아、미、미안、하아、허리、빠져 버렸다」
				PRINTFORMW 움직이지 못하게 된 %CALLNAME%대신에 아래서 밀어올려 주자、침을 흘리면서 헐떡이는 소리를 지른다。
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:339 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:339 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:339 = 3
		;それ以外
		ELSEIF CFLAG:339 < 2 || FLAG:7 == 2
			PRINTFORML 허리를 떨어뜨리고 나서 전혀 움직일 기색이 없으므로、아래에서 밀어올린다
			PRINTFORML %조사처리(CALLNAME,"는")% 굳게 다문 입에서 오열을 흘리면서、될대로 되고 있다
			CFLAG:339 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;68,대면좌위
;-------------------------------------------------
CASE 68
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:340 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 4 || FLAG:7 == 2)
				PRINTFORML %CALLNAME%에게 목에 손을 돌려 껴안게 했다。
				PRINTFORML 「이、이대로、넣는 거야‥‥？」
				PRINTFORML 「조금、무서운、걸까？」
				PRINTFORML 그 말대로、약간 떨고 있는 %조사처리(CALLNAME,"를")% 상냥하게 껴안아 붙들면、
				PRINTFORML 숨이 닿을 만큼의 거리에서、%조사처리(CALLNAME,"가")% 작은 소리로 중얼거린다
				PRINTFORML 「그러니까、키스하면서、하길 바라는데？안되는、걸까？」
				PRINTFORML 대답 대신 키스를 해、그대로 %CALLNAME%의 안으로 천천히 페니스를 삽입해 간다。
				PRINTFORMW 모조리 %CALLNAME%의 안에 들어간 뒤에도 당분간 키스를 해、아픔을 완화한다
			ELSE
				PRINTFORML 「싫어、절대로 싫어！」
				PRINTFORML 바둥거리며 날뛰는 %CALLNAME%의 허리를、단단히 붙잡고、단번에 허리를 밀어올려 꿰뚫었다。
				PRINTFORML 「으그아아아！아파、빼 줘！빼 달라니까」
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%、눈에 눈물을 띄워、오열을 흘리는 %조사처리(CALLNAME,"를")%、마치 자위도구인 것처럼 억지로 상하로 움직였다。
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 4 || FLAG:7 == 2)
				PRINTFORML %조사처리(CALLNAME:MASTER,"과")% %조사처리(CALLNAME,"는")% 마주 보며 깊은 곳까지 이어졌다。
				PRINTFORMW 두사람은 서로의 입술에 빨려들어가듯이 입맞춤을 하고、피차 어느 쪽이라 할 것 없이 허리를 움직이기 시작한다。
			ELSE
				PRINTFORML 껴안듯이 %CALLNAME%의 안으로 페니스를 삽입했다。
				PRINTFORMW 그대로、움직일 기색이 없는 %조사처리(CALLNAME,"를")% 자위도구처럼 다루자、%CALLNAME%의 눈에서 한줄기의 눈물이 흘러내렸다
			ENDIF
		ENDIF
		CFLAG:340 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「%CSTR:일인칭%、이걸 제일 좋아해」
				PRINTFORML 그리 말한 %조사처리(CALLNAME,"는")% 스스로 허리를 움직이기 시작한다。%조사처리(CALLNAME:MASTER,"가")% 움직이려 하자 살며시 멈춘다
				PRINTFORML 「응읏、앗、응하앗、우선 한번、%CSTR:일인칭%에게 싸 줘？그렇게 하면 맘대로 해도 좋으니까」
				PRINTFORMW 그리 말하며 음란하게 미소지은 %CALLNAME%에게、%조사처리(CSTR:이인칭,"는")% 한번 입을 맞추고、그대로 몸을 맡긴다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 마주보며 %CALLNAME%의 안으로 삽입했다。
				PRINTFORML 「응하아、깊어어、%CSTR:이인칭%의 것이 문질러서、안에 닿고 있어어」
				PRINTFORML 정말로 행복한 것처럼、눈을 게슴츠레해 만족한 듯한 한숨을 흘린다。
				PRINTFORML 「응아아아、엉망진창 해 줘어、완구처럼 깊은 곳까지 질척하게 해줘어」
				PRINTFORMW 부탁받은 대로 난폭하게 허리를 쳐박자、환희의 소리를 지른다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:340 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:340 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			PRINTFORMW 
			CFLAG:340 = 3
		;それ以外
		ELSEIF CFLAG:340 < 2 || FLAG:7 == 2
			PRINTFORML 허리를 잡아 깊게 페니스를 찔러넣어、밑에서 밀어 올린다
			PRINTFORMW %조사처리(CALLNAME,"는")% 굳게 다문 입에서 오열을 흘리면서、될대로 되고 있다
			CFLAG:340 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;69,배면좌위
;-------------------------------------------------
CASE 69
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:341 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 「거기에 앉으면 되는 거에요？알았…에！넣는 거야？！」
					PRINTFORMW 「아、알았어！힘낼게！」
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
					PRINTFORMW 
				ELSE
					PRINTFORMW 
					PRINTFORMW 
				ENDIF
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 「따뜻해…뭔가 마음이 놓여…」
					PRINTFORMW 「저기、잠깐만…이대로도 괜찮을까？」
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
					PRINTFORMW 
				ELSE
					PRINTFORMW 
					PRINTFORMW 
				ENDIF
			ENDIF
		ENDIF
		CFLAG:341 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응아아아、%CSTR:이인칭%의 손놀림、굉장히 상냥해」
				PRINTFORMW 「그렇게、여러、곳、응하아、만져버림、시러어」
				PRINTFORMW 하지만、%CALLNAME:MASTER%의 손이 %CALLNAME%의 유두나 클리토리스를 만질 때마다、애달픈 듯이 %CALLNAME%의 질은 %조사처리(CALLNAME:MASTER,"를")% 조여댄다
				PRINTFORMW 그 감촉을 즐기면서、서서히 허리의 움직임을 빠르게 했다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「후후、커어다란 거、안에서 실룩거리고 있다」
				PRINTFORML 「가슴과 클리 만져버리면、너무 느껴버려어」
				PRINTFORML 「우후후、자、%조사처리(CSTR:일인칭,"가")% 움직일 테니까、%CSTR:이인칭%、응아앗、손에 집중、해 줘요」
				PRINTFORMW 그리 말하곤、허리를 크게 상하로 움직여、%CALLNAME:MASTER%에게 봉사를 시작한다‥‥ 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:341 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:341 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			PRINTFORMW 
			CFLAG:341 = 3
		;それ以外
		ELSEIF CFLAG:341 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:341 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;70,대면좌위애널
;-------------------------------------------------
CASE 70
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:342 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 4 || FLAG:7 == 2)
				PRINTFORML %CALLNAME%에게 목에 손을 돌려 껴안게 했다。
				PRINTFORML 「이、이대로、넣는 거야‥‥？」
				PRINTFORML 「조금、무서운、걸까？」
				PRINTFORML 그 말대로、약간 떨고 있는 %조사처리(CALLNAME,"를")% 상냥하게 껴안아 붙들면、
				PRINTFORML 숨이 닿을 만큼의 거리에서、%조사처리(CALLNAME,"가")% 작은 소리로 중얼거린다
				PRINTFORML 「그러니까、키스하면서、하길 바라는데？안되는、걸까？」
				PRINTFORML 대답 대신 키스를 해、그대로 %CALLNAME%의 국화로 천천히 페니스를 삽입해 간다。
				PRINTFORMW 모조리 %CALLNAME%의 안에 들어간 뒤에도 당분간 키스를 해、아픔을 완화한다
			ELSE
				PRINTFORML 「싫어、절대로 싫어！」
				PRINTFORML 바둥거리며 날뛰는 %CALLNAME%의 허리를、단단히 붙잡고、단번에 허리를 밀어올려 꿰뚫었다。
				PRINTFORML 「으그아아아！아파、빼 줘！빼 달라니까」
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%、눈에 눈물을 띄워、오열을 흘리는 %조사처리(CALLNAME,"를")%、마치 자위도구인 것처럼 억지로 상하로 움직였다。
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 4 || FLAG:7 == 2)
				PRINTFORML %조사처리(CALLNAME:MASTER,"과")% %조사처리(CALLNAME,"는")% 마주 보며 깊은 곳까지 이어졌다。
				PRINTFORMW 두사람은 서로의 입술에 빨려들어가듯이 입맞춤을 하고、피차 어느 쪽이라 할 것 없이 허리를 움직이기 시작한다。
			ELSE
				PRINTFORML 껴안듯이 %CALLNAME%의 안으로 페니스를 삽입했다。
				PRINTFORMW 그대로、움직일 기색이 없는 %조사처리(CALLNAME,"를")% 자위도구처럼 다루자、%CALLNAME%의 눈에서 한줄기의 눈물이 흘러내렸다
			ENDIF
		ENDIF
		CFLAG:342 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「%CSTR:일인칭%、이걸 제일 좋아해」
				PRINTFORML 그리 말한 %조사처리(CALLNAME,"는")% 스스로 허리를 움직이기 시작한다。%조사처리(CALLNAME:MASTER,"가")% 움직이려 하자 살며시 멈춘다
				PRINTFORML 「응읏、앗、응하앗、우선 한번、%CSTR:일인칭%에게 싸 줘？그렇게 하면 맘대로 해도 좋으니까」
				PRINTFORMW 그리 말하며 음란하게 미소지은 %CALLNAME%에게、%조사처리(CSTR:이인칭,"는")% 한번 입을 맞추고、그대로 몸을 맡긴다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 마주보며 %CALLNAME%의 안으로 삽입했다。
				PRINTFORML 「응하아、깊어어、%CSTR:이인칭%의 것이 문질러서、뱃속에 닿고 있어어」
				PRINTFORML 정말로 행복한 것처럼、눈을 게슴츠레해 만족한 듯한 한숨을 흘린다。
				PRINTFORML 「응아아아、엉망진창 해 줘어、완구처럼 뱃속까지 질척하게 해줘어」
				PRINTFORMW 부탁받은 대로 난폭하게 허리를 쳐박자、환희의 소리를 지른다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:342 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:342 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:342 = 3
		;それ以外
		ELSEIF CFLAG:342 < 2 || FLAG:7 == 2
			PRINTFORML 허리를 잡아 깊게 페니스를 찔러넣어、밑에서 밀어 올린다
			PRINTFORMW %조사처리(CALLNAME,"는")% 굳게 다문 입에서 오열을 흘리면서、될대로 되고 있다
			CFLAG:342 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;71,배면좌위애널
;-------------------------------------------------
CASE 71
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:343 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 「거기에 앉으면 되는 거에요？알았…에！넣는 거야？！」
					PRINTFORMW 「아、알았어！힘낼게！」
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
					PRINTFORMW 
				ELSE
					PRINTFORMW 
					PRINTFORMW 
				ENDIF
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 「따뜻해…뭔가 마음이 놓여…」
					PRINTFORMW 「저기、잠깐만…이대로도 괜찮을까？」
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
					PRINTFORMW 
				ELSE
					PRINTFORMW 
					PRINTFORMW 
				ENDIF
			ENDIF
		ENDIF
		CFLAG:343 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:343 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응아아아、%CSTR:이인칭%의 손놀림、굉장히 상냥해」
				PRINTFORML 「그렇게、여러、곳、응하아、만져버림、시러어」
				PRINTFORML 하지만、%CALLNAME:MASTER%의 손이 %CALLNAME%의 유두나 클리토리스를 만질 때마다、애달픈 듯이 %CALLNAME%의 엉덩이구멍은 %조사처리(CALLNAME:MASTER,"를")% 조여댄다
				PRINTFORMW 그 감촉을 즐기면서、서서히 허리의 움직임을 빠르게 했다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「후후、커어다란 거、안에서 실룩거리고 있다」
				PRINTFORML 「가슴과 클리 만져버리면、너무 느껴버려어」
				PRINTFORML 「우후후、자、%조사처리(CSTR:일인칭,"가")% 움직일 테니까、%CSTR:이인칭%、응아앗、손에 집중、해 줘요」
				PRINTFORMW 그리 말하곤、허리를 크게 상하로 움직여、%CALLNAME:MASTER%에게 봉사를 시작한다‥‥ 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:343 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:343 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:343 = 3
		;それ以外
		ELSEIF CFLAG:343 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:343 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;72,속옷비켜삽입
;-------------------------------------------------
CASE 72
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:420 == 0
		;처녀
		IF TALENT:0
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
					PRINTFORMW 
				ELSE
					PRINTFORMW 
					PRINTFORMW 
				ENDIF
			ENDIF
		;それ以外
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
					PRINTFORMW 
				ELSE
					PRINTFORMW 
					PRINTFORMW 
				ENDIF
			ENDIF
		ENDIF
		CFLAG:420 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:420 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:420 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			PRINTFORMW 
			CFLAG:420 = 3
		;それ以外
		ELSEIF CFLAG:420 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:420 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;73,속옷비켜삽입애널
;-------------------------------------------------
CASE 73
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:421 == 0
		PRINTFORMW 
		CFLAG:421 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:421 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:421 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:421 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:421 = 3
		;それ以外
		ELSEIF CFLAG:421 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:421 = 2
		ENDIF
	ENDIF


;=================================================
;봉사계コマンド
;=================================================
;-------------------------------------------------
;80,수음
;-------------------------------------------------
CASE 80
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:344 == 0
		PRINTFORML 「그걸、어떻게 하라는 거야？」
		PRINTFORML 「손으로‥‥？싫어！그런 기분나쁜 것！」
		PRINTFORML 완강하게 자신이 하려고 하지 않기 때문에、억지로 손을 잡아 벌리게 하고
		PRINTFORML 페니스를 쥐어주고、%CALLNAME%의 손에 자신의 손을 겹쳐 상하로 움직이게 한다。
		PRINTFORMW 자신의 손에서 훑어지는 페니스를 %조사처리(CALLNAME,"는")% 불쾌한듯이 보고 있다
		CFLAG:344 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「뿌리를、훑으면서、귀두를 문지르는 거、기분 좋아？」
				PRINTFORML 「후훗、알고 있어。%CSTR:일인칭%의 안에서 %CSTR:이인칭%의 것이 움찔거리는 걸」
				PRINTFORMW 「언제든지 싸도 되니까 말야？」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「아핫、%CSTR:일인칭%의 손으로 하는 것 마음에 들었어？」
				PRINTFORML 「후훗、왜냐면 %CSTR:이인칭%의 것이 굉장히 딱딱한 걸」
				PRINTFORMW 「그치만、싸는 건、%CSTR:일인칭%의 질내에서、응？」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:344 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:344 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:344 = 3
		;それ以外
		ELSEIF CFLAG:344 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:344 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;81,펠라치오
;-------------------------------------------------
CASE 81
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:345 == 0
		PRINTFORML 「입으로‥‥？무슨 말하는 거야！」
		PRINTFORML 완강하게 입으로의 봉사를 꺼리는 걸、깨물지 않도록 다짐하게 하고、억지로 안에 밀어넣는다
		PRINTFORML 「그읍！응앗！」
		PRINTFORMW 눈물을 흘리면서 혀로 밀어내려고 하지만、그게 쾌감이 되는 것은 알아채지 못한 것 같다
		CFLAG:345 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「츄우‥‥할짝‥‥츄웁、츄릅」
				PRINTFORML 「후후‥‥실룩거리고 있어？」
				PRINTFORML 「잔뜩、잔뜩、기분 좋게 되세요？」
				PRINTFORMW 그리 말하고、%조사처리(CALLNAME,"는")% 입으로의 봉사를 보다 격한 것으로 해 갔다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「응응‥‥츄우、츄르릅‥‥츕‥‥츄릅‥‥」
				PRINTFORML 크게 입을 벌려 페니스를 삼키고、혀로 귀두 주변을 핥아주면서、얼굴을 앞뒤로 움직이며 입술로 훑는다。
				PRINTFORML 「으흡、흡、후앗、으긋」
				PRINTFORMW 아무래도 곧 갈 것처럼 된 %조사처리(CALLNAME:MASTER,"를")%、%조사처리(CALLNAME,"는")% 만족한 기분으로 바라보고 있다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:345 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:345 < 3 || FLAG:7 == 2)
			PRINTFORML 「츄우‥‥할짝‥‥츄웁、츄릅」
			PRINTFORMW %조사처리(CALLNAME,"는")% 어딘지 모르게 갖고 싶은 듯이 %CALLNAME:MASTER%의 것을 빨고 있다
			CFLAG:345 = 3
		;それ以外
		ELSEIF CFLAG:345 < 2 || FLAG:7 == 2
			PRINTFORMW 「츄우‥‥할짝‥‥츄웁、츄릅」
			CFLAG:345 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;82,파이즈리
;-------------------------------------------------
CASE 82
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:346 == 0
		PRINTFORML 「가슴으로 하라니 싫어！%CSTR:이인칭%의 것이 몸에 닿는 것만으로 한기가 들어요！」
		PRINTFORML 스스로 할 생각은 없는 것 같아서、%CALLNAME%의 상반신에 올라타고、페니스를 끼워、허리를 상하로 움직인다
		PRINTFORMW %조사처리(CALLNAME,"는")% 스스로의 유방에서 들어갔다 나왔다 하는 페니스를、불쾌한듯이 보고 있다
		CFLAG:346 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「%조사처리(CSTR:일인칭,"가")% 하는 것보다、%조사처리(CSTR:이인칭,"가")% 하는 쪽이 좋잖아？」
				PRINTFORML 그리 말하곤、침대에 누워、손으로 가슴을 가까이 대보인다。
				PRINTFORML 「예、부디。우후후、%CSTR:이인칭%의 것 굉장히 뜨거워‥‥」
				PRINTFORMW 「%CSTR:일인칭%의 가슴으로 잔뜩 기분좋게 되세요？」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML %조사처리(CALLNAME,"는")% 침대 가장자리에 %조사처리(CALLNAME:MASTER,"를")% 앉히고、그 가슴으로 %CALLNAME:MASTER%의 페니스를 감싸、몸을 상하로 움직인다
				PRINTFORML 「유두가 귀두에 닿는 게 좋은 거야？」
				PRINTFORML 「그래。%CSTR:일인칭%도 유두가 문질러져서 기분좋으니까 좋아해？」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:346 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:346 < 3 || FLAG:7 == 2)
			PRINTFORML 「…우、크…」
			PRINTFORMW %조사처리(CALLNAME,"는")% 유방에 끼워진 육봉을、빨개진 얼굴로 바라보고 있다。
			CFLAG:346 = 3
		;それ以外
		ELSEIF CFLAG:346 < 2 || FLAG:7 == 2
			PRINTFORML 「…최저…」
			PRINTFORMW %조사처리(CALLNAME,"는")% 얼굴을 돌리고 있다。
			CFLAG:346 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;83,스마타
;-------------------------------------------------
CASE 83
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:347 == 0
		PRINTFORML 「가랑이에 끼워？멋대로 하면 되잖아」
		PRINTFORML 안에 넣지 않는 걸 알자 안심한 것 같다。
		PRINTFORML 말한대로 페니스를 전후로 움직인다
		PRINTFORML 「엣、잠깐 기다려、이건、응앗！」
		PRINTFORMW 전후로 움직이는 페니스로 클리토리스가 문질러 지는 걸 깨닫지 못한 것 같다
		CFLAG:347 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML %CALLNAME%의 가랑이에 페니스를 넣자、건강한 허벅지가 편안하게 조인다。
				PRINTFORML 「%CSTR:일인칭%의 허벅지、좋아하는 거야？」
				PRINTFORML 「친구의 것은 탄탄하고 가느니까、조금 부러웠지만」
				PRINTFORMW 「%조사처리(CSTR:이인칭,"를")% 좋아한다고 말해주면、기쁜 걸？」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「에、비비는 것뿐이야？」
				PRINTFORML 「그래。%조사처리(CSTR:이인칭,"는")% 감질나는 것이 좋은 거야？」
				PRINTFORML 「우후후、참을 수 없게 되면 그대로 넣어도 되니까？」
				PRINTFORMW 그리 말하고、애액 투성이의 성기를 페니스에 문지른다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:347 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:347 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:347 = 3
		;それ以外
		ELSEIF CFLAG:347 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:347 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;84,거품춤
;-------------------------------------------------
CASE 84
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:348 == 0
		PRINTFORMW 
		CFLAG:348 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:348 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:348 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:348 = 3
		;それ以外
		ELSEIF CFLAG:348 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:348 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;85,풋잡
;-------------------------------------------------
CASE 85
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:349 == 0
		PRINTFORML 「이런 걸로、기분좋은 거야？」
		PRINTFORML 「그래。%조사처리(CSTR:일인칭,"는")% 정말로 기분나쁠 뿐이야」
		PRINTFORMW 「냉큼 싸서 끝내줘」
		CFLAG:349 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:349 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「아팠다면、미안해？힘조절이 어려워、이거」
				PRINTFORML 그리 말하곤、마주보고 앉은 %CALLNAME:MASTER%의 페니스에 향해 양다리를 뻗어、
				PRINTFORML 발바닥으로 끼워、천천히 위아래로 움직인다
				PRINTFORMW 「응읏、응읏、읏샤、싸고 싶다면、참지 말아줘？」
				
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 자신의 애액을、%CALLNAME:MASTER%의 페니스와 발바닥에 마구 처바르고、발바닥으로 짓밟듯이 자극한다
				PRINTFORML 「어때？이 정도？좀 더、센 편을 좋아해？」
				PRINTFORML 「후훗、잔뜩 커졌으니깐‥‥여기에、말야？」
				PRINTFORML 그리 말하곤 %CALLNAME:MASTER%에게 잘 보이도록、자신의 손으로 성기를 벌려 보인다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:349 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:349 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:349 = 3
		;それ以外
		ELSEIF CFLAG:349 < 2 || FLAG:7 == 2
			PRINTFORMW 「…모처럼이니 으깨버릴까…」
			CFLAG:349 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;86,다리핥기강제
;-------------------------------------------------
CASE 86
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:350 == 0
		PRINTFORMW 
		CFLAG:350 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:350 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:350 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:350 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:350 = 3
		;それ以外
		ELSEIF CFLAG:350 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:350 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;87,세로파이즈리
;-------------------------------------------------
CASE 87
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:351 == 0
		PRINTFORMW 
		CFLAG:351 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:351 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:351 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:351 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:351 = 3
		;それ以外
		ELSEIF CFLAG:351 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:351 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;88,파이즈리펠라
;-------------------------------------------------
CASE 88
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:352 == 0
		PRINTFORMW 
		CFLAG:352 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:352 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML %조사처리(CALLNAME,"는")% %조사처리(CALLNAME:MASTER,"를")% 침대에 누이고、스스로의 가슴으로 %CALLNAME:MASTER%의 페니스를 끼워 넣었다
				PRINTFORML 「가슴에서 삐져나온 끝부분、입속에 넣으면、어떨까？」
				PRINTFORML 페니스를 가슴으로 훑으면서、귀두를 아래서 슥슥 자극받아、
				PRINTFORML 지나친 쾌락에 허리가 떠오를 것처럼 되었다。
				PRINTFORMW 그 모습을 보고 기쁜듯이 봉사를 격하게 해갔다‥‥
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML %조사처리(CALLNAME,"는")% 가슴을 페니스로 끼워、남은 귀두를 핥고 있다
				PRINTFORML 「벌써、투명한 물、나오고 있어？」
				PRINTFORML 「우후후、그렇게 기분 좋아？」
				PRINTFORMW 그쯤 말하고、%조사처리(CALLNAME,"는")% 정신이 멍해질 정도의 쾌락을、%CALLNAME:MASTER%에게 계속 준다
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:352 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:352 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:352 = 3
		;それ以外
		ELSEIF CFLAG:352 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:352 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;89,모유마시기　함락済みのみ
;-------------------------------------------------
CASE 89
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:353 == 0
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORML 「우왓、정말로 모유가 나온다‥‥」
			PRINTFORML %CALLNAME%의 가슴에서 모유가 방울져 떨어지는 광경은、대단히 관능적이었다
			PRINTFORML 「무슨 일이야‥‥？아、마셔보고 싶어？좋아。자、와요？」
			PRINTFORML 팔을 벌려 %CALLNAME%에게 달라붙어、그 달콤한 액체를 만끽한다
			PRINTFORMW 「아응읏、정말。후훗、아기같아‥‥。」
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:353 = 1
;二回目以降
	ELSEIF CFLAG:353 < 2 || FLAG:7 == 2
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORML %조사처리(CALLNAME,"는")% 가슴에 달라붙은 %CALLNAME:MASTER%의 머리카락을、사랑스럽다는 듯이 손으로 만지고 있다
			PRINTFORML 「후후、응석꾸러기씨。만족했어？」
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:353 = 2
	ENDIF


;=================================================
;조교자봉사계コマンド
;=================================================
;-------------------------------------------------
;수음한다
;-------------------------------------------------
CASE 90
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:423 == 0
		PRINTFORMW 
		CFLAG:423 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:423 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:423 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:423 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;파이즈리한다
;-------------------------------------------------
CASE 91
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:424 == 0
		PRINTFORMW 
		CFLAG:424 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:424 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:424 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:424 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;정상위시킨다
;-------------------------------------------------
CASE 92
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:425 == 0
		PRINTFORMW 
		CFLAG:425 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:425 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:425 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:425 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;후배위시킨다
;-------------------------------------------------
CASE 93
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:426 == 0
		PRINTFORMW 
		CFLAG:426 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:426 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:426 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:426 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;기승위한다
;-------------------------------------------------
CASE 94
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:427 == 0
		PRINTFORMW 
		CFLAG:427 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:427 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:427 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:427 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;애널정상위시킨다
;-------------------------------------------------
CASE 95
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:428 == 0
		PRINTFORMW 
		CFLAG:428 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:428 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:428 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:428 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;애널후배위시킨다
;-------------------------------------------------
CASE 96
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:429 == 0
		PRINTFORMW 
		CFLAG:429 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:429 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:429 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:429 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;애널기승위한다
;-------------------------------------------------
CASE 97
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:430 == 0
		PRINTFORMW 
		CFLAG:430 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:430 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:430 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:430 = 2
		ENDIF
	ENDIF

;=================================================
;SM系コマンド
;=================================================
;-------------------------------------------------
;100,스팽킹
;-------------------------------------------------
CASE 100
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:354 == 0
		PRINTFORMW 
		CFLAG:354 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:354 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:354 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:354 = 3
		;それ以外
		ELSEIF CFLAG:354 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:354 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;101,젖스팽킹
;-------------------------------------------------
CASE 101
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:355 == 0
		PRINTFORMW 
		CFLAG:355 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:355 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:355 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:355 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:355 = 3
		;それ以外
		ELSEIF CFLAG:355 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:355 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;102,채찍
;-------------------------------------------------
CASE 102
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:356 == 0
		PRINTFORMW 
		CFLAG:356 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:356 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:356 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:356 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:356 = 3
		;それ以外
		ELSEIF CFLAG:356 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:356 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;103,양초
;-------------------------------------------------
CASE 103
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:357 == 0
		PRINTFORMW 
		CFLAG:357 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:357 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:357 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:357 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:357 = 3
		;それ以外
		ELSEIF CFLAG:357 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:357 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;104,바늘
;-------------------------------------------------
CASE 104
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:358 == 0
		PRINTFORMW 「사람도 아냐！쓰레기 쓰레기！」
		CFLAG:358 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:358 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:358 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:358 = 3
		;それ以外
		ELSEIF CFLAG:358 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:358 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;105,아이마스크
;-------------------------------------------------
CASE 105
	IF TEQUIP:18 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:359 == 0
			PRINTFORMW 
			CFLAG:359 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:359 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:359 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:359 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:359 = 3
			;それ以外
			ELSEIF CFLAG:359 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:359 = 2
			ENDIF
		ENDIF
	ELSEIF SELECTCOM == 105
		IF CFLAG:359 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;106,밧줄
;-------------------------------------------------
CASE 106
	IF TEQUIP:19 == 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:360 == 0
			PRINTFORMW 
			CFLAG:360 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:360 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:360 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:360 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:360 = 3
			;それ以外
			ELSEIF CFLAG:360 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:360 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:360 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;107,볼재갈
;-------------------------------------------------
CASE 107
	IF TEQUIP:20
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:361 == 0
			PRINTFORMW 
			CFLAG:361 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:361 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:361 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:361 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:361 = 3
			;それ以外
			ELSEIF CFLAG:361 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:361 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:361 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;=================================================
;조수によるコマンド、及び레즈プレイ
;=================================================
;-------------------------------------------------
;120,커닐링구스강제
;-------------------------------------------------
CASE 120
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:362 == 0
		PRINTFORMW 
		CFLAG:362 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:362 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:362 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:362 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:362 = 3
		;それ以外
		ELSEIF CFLAG:362 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:362 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;121,조개맞대기
;-------------------------------------------------
CASE 121
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:363 == 0
		PRINTFORMW 
		CFLAG:363 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:363 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:363 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:363 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:363 = 3
		;それ以外
		ELSEIF CFLAG:363 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:363 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;122,조수를범하게한다
;-------------------------------------------------
CASE 122
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:364 == 0
		PRINTFORMW 
		CFLAG:364 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:364 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:364 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:364 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:364 = 3
		;それ以外
		ELSEIF CFLAG:364 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:364 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;123,풋잡한다
;-------------------------------------------------
CASE 123
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:365 == 0
		PRINTFORMW 
		CFLAG:365 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:365 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:365 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:365 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:365 = 3
		;それ以外
		ELSEIF CFLAG:365 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:365 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;124,더블펠라
;-------------------------------------------------
CASE 124
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:366 == 0
		PRINTFORMW 
		CFLAG:366 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:366 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:366 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:366 = 3
		;それ以外
		ELSEIF CFLAG:366 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:366 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;125,더블파이즈리
;-------------------------------------------------
CASE 125
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:367 == 0
		PRINTFORMW 
		CFLAG:367 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:367 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:367 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:367 = 3
		;それ以外
		ELSEIF CFLAG:367 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:367 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;126,가슴맞대기
;-------------------------------------------------
CASE 126
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:368 == 0
		PRINTFORMW 
		CFLAG:368 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:368 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:368 = 3
		;それ以外
		ELSEIF CFLAG:368 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:368 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;127,자위과시
;-------------------------------------------------
CASE 127
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:430 == 0
		PRINTFORMW 
		CFLAG:430 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:430 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:430 = 3
		;それ以外
		ELSEIF CFLAG:430 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:430 = 2
		ENDIF
	ENDIF


;=================================================
;ハードな조교コマンド
;=================================================
;-------------------------------------------------
;140,이라마치오
;-------------------------------------------------
CASE 140
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:369 == 0
		PRINTFORML 「윽、우엑、콜록、콜록」
		PRINTFORMW 눈물을 띄우면서 %조사처리(CALLNAME:MASTER,"를")% 노려보았다‥‥
		CFLAG:369 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:369 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:369 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:369 < 3 || FLAG:7 == 2)
			PRINTFORMW 「적어도 미리 알려줘…제발…」
			CFLAG:369 = 3
		;それ以外
		ELSEIF CFLAG:369 < 2 || FLAG:7 == 2
			PRINTFORMW 「콜록…이렇게 난폭하게 하면、부끄럽지 않은거야！」
			CFLAG:369 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;141,피스트퍽　복종、예속のみ
;-------------------------------------------------
CASE 141
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:370 == 0
		PRINTFORMW 
		CFLAG:370 = 1
;二回目以降
	ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:370 = 2
	ENDIF

;-------------------------------------------------
;142,애널피스트　복종、예속のみ
;-------------------------------------------------
CASE 142
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:371 == 0
		PRINTFORMW 
		CFLAG:371 = 1
;二回目以降
	ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:371 = 2
	ENDIF

;-------------------------------------------------
;143,양구멍피스트　복종、예속のみ
;-------------------------------------------------
CASE 143
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:372 == 0
		PRINTFORMW 
		CFLAG:372 = 1
;二回目以降
	ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:372 = 2
	ENDIF

;-------------------------------------------------
;144,방뇨
;-------------------------------------------------
CASE 144
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:373 == 0
		PRINTFORMW 
		CFLAG:373 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:373 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:373 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:373 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:373 = 3
		;それ以外
		ELSEIF CFLAG:373 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:373 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;145,애널봉사강제
;-------------------------------------------------
CASE 145
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:374 == 0
		PRINTFORMW 
		CFLAG:374 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:374 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:374 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:374 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:374 = 3
		;それ以外
		ELSEIF CFLAG:374 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:374 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;146,관장기＋플러그
;-------------------------------------------------
CASE 146
	IF TEQUIP:21
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:375 == 0
			PRINTFORMW 
			CFLAG:375 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:375 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:375 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:375 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:375 = 3
			;それ以外
			ELSEIF CFLAG:375 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:375 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:375 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;147,공기관장
;-------------------------------------------------
CASE 147
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:376 == 0
		PRINTFORMW 
		CFLAG:376 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:376 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:376 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:376 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:376 = 3
		;それ以外
		ELSEIF CFLAG:376 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:376 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;148,확장벌룬
;-------------------------------------------------
CASE 148
	IF TEQUIP:22
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:377 == 0
			PRINTFORMW 
			CFLAG:377 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:377 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:377 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:377 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:377 = 3
			;それ以外
			ELSEIF CFLAG:377 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:377 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:377 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;149,애널전극　복종、예속のみ
;-------------------------------------------------
CASE 149
	IF TEQUIP:23
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:378 == 0
			PRINTFORMW 
			CFLAG:378 = 1
		;二回目以降
		ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:378 = 2
		ENDIF
	ELSE
		IF CFLAG:378 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;150,유방전극　복종、예속のみ
;-------------------------------------------------
CASE 150
	IF TEQUIP:24
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:379 == 0
			PRINTFORMW 
			CFLAG:379 = 1
		;二回目以降
		ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:379 = 2
		ENDIF
	ELSE
		IF CFLAG:379 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;151,뱀장어욕탕　복종、예속のみ
;-------------------------------------------------
CASE 151
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:380 == 0
		;처녀
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:380 = 1
;二回目以降
	ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:380 = 2
	ENDIF

;-------------------------------------------------
;152,벌레욕탕　복종、예속のみ
;-------------------------------------------------
CASE 152
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:381 == 0
		PRINTFORMW 
		CFLAG:381 = 1
;二回目以降
	ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:381 = 2
	ENDIF


;=================================================
;촉수계コマンド
;=================================================
;-------------------------------------------------
;160,촉수소환
;-------------------------------------------------
CASE 160
	IF TEQUIP:25
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:382 == 0
			PRINTFORMW 
			CFLAG:382 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:382 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:382 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:382 = 3
			;それ以外
			ELSEIF CFLAG:382 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:382 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:382 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;161,촉수삽입
;-------------------------------------------------
CASE 161
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:383 == 0
		;처녀
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:383 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:383 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:383 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:383 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:383 = 3
		;それ以外
		ELSEIF CFLAG:383 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:383 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;162,애널촉수
;-------------------------------------------------
CASE 162
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:384 == 0
		PRINTFORMW 
		CFLAG:384 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:384 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:384 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:384 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:384 = 3
		;それ以外
		ELSEIF CFLAG:384 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:384 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;163,촉수클리자극
;-------------------------------------------------
CASE 163
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:385 == 0
		PRINTFORMW 
		CFLAG:385 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:385 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:385 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:385 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:385 = 3
		;それ以外
		ELSEIF CFLAG:385 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:385 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;164,촉수유두자극
;-------------------------------------------------
CASE 164
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:386 == 0
		PRINTFORMW 
		CFLAG:386 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:386 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:386 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:386 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:386 = 3
		;それ以外
		ELSEIF CFLAG:386 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:386 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;165,촉수착유
;-------------------------------------------------
CASE 165
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:387 == 0
		PRINTFORMW 
		CFLAG:387 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:387 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:387 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:387 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:387 = 3
		;それ以外
		ELSEIF CFLAG:387 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:387 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;166,촉수긴박
;-------------------------------------------------
CASE 166
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:388 == 0
		PRINTFORMW 
		CFLAG:388 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:388 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:388 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:388 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:388 = 3
		;それ以外
		ELSEIF CFLAG:388 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:388 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;167,촉수관장
;-------------------------------------------------
CASE 167
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:389 == 0
		PRINTFORMW 
		CFLAG:389 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:389 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:389 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:389 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:389 = 3
		;それ以外
		ELSEIF CFLAG:389 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:389 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;168,촉수구욕
;-------------------------------------------------
CASE 168
	IF TEQUIP:36
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:390 == 0
			PRINTFORMW 
			CFLAG:390 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:390 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:390 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:390 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:390 = 3
			;それ以外
			ELSEIF CFLAG:390 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:390 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:390 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;169,촉수페니스자극
;-------------------------------------------------
CASE 169
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:391 == 0
		PRINTFORMW 
		CFLAG:391 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:391 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:391 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:391 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:391 = 3
		;それ以外
		ELSEIF CFLAG:391 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:391 = 2
		ENDIF
	ENDIF


;=================================================
;특수커맨드
;=================================================
;-------------------------------------------------
;180,로션,
;-------------------------------------------------
CASE 180
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:392 == 0
		PRINTFORMW 「뭐야、이거？‥‥차가워서 기분 나빠‥‥」
		CFLAG:392 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:392 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「히얏、정말、그런 걸 쓰지 않아도、%조사처리(CSTR:이인칭,"가")% 만져주면 그걸로 되는데」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORML 「우후후、이러엏게 미끈미끈해 버려서。」
				PRINTFORMW 「이제부터、뭘 당해버리는 걸까」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:392 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:392 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:392 = 3
		;それ以外
		ELSEIF CFLAG:392 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:392 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;181,미약,
;-------------------------------------------------
CASE 181
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:393 == 0
		PRINTFORMW 
		CFLAG:393 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:393 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:393 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:393 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:393 = 3
		;それ以外
		ELSEIF CFLAG:393 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:393 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;182,이뇨제,
;-------------------------------------------------
CASE 182
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:394 == 0
		PRINTFORMW 
		CFLAG:394 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:394 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:394 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:394 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:394 = 3
		;それ以外
		ELSEIF CFLAG:394 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:394 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;183,비디오카메라,
;-------------------------------------------------
CASE 183
	IF TEQUIP:28
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:395 == 0
			PRINTFORMW 
			CFLAG:395 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:395 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:395 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:395 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:395 = 3
			;それ以外
			ELSEIF CFLAG:395 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:395 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:395 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;184,야외플레이,
;-------------------------------------------------
CASE 184
	IF TEQUIP:29
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:396 == 0
			PRINTFORMW 
			CFLAG:396 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:396 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:396 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:396 = 3
			;それ以外
			ELSEIF CFLAG:396 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:396 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:396 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF

;-------------------------------------------------
;185,조수를범한다,
;-------------------------------------------------
CASE 185
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:397 == 0
		PRINTFORMW 
		CFLAG:397 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:397 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:397 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:397 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:397 = 3
		;それ以外
		ELSEIF CFLAG:397 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:397 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;186,수치플레이,
;-------------------------------------------------
CASE 186
	IF TEQUIP:30
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:398 == 0
			PRINTFORMW 
			CFLAG:398 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:398 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:398 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:398 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:398 = 3
			;それ以外
			ELSEIF CFLAG:398 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:398 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:398 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;187,목욕탕플레이,
;-------------------------------------------------
CASE 187
	IF TEQUIP:31
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:399 == 0
			PRINTFORMW 
			CFLAG:399 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:399 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:399 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:399 = 3
			;それ以外
			ELSEIF CFLAG:399 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:399 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:399 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;188,샤워,
;-------------------------------------------------
CASE 188
	IF TEQUIP:32
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:400 == 0
			PRINTFORMW 
			CFLAG:400 = 1
		;二回目以降
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:400 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:400 = 4
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:400 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:400 = 3
			;それ以外
			ELSEIF CFLAG:400 < 2 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:400 = 2
			ENDIF
		ENDIF
	ELSE
		IF CFLAG:400 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;189,새댁플레이,　연모、친애のみ
;-------------------------------------------------
CASE 189
	IF TEQUIP:33
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:401 == 0
			PRINTFORMW 「와아！나풀거리는 에이프런 입는 거、%CSTR:일인칭%의 꿈이었던 거야！」
			CFLAG:401 = 1
		;二回目以降
		ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
			PRINTFORMW 「후훗、어때？잘 어울려？」
			CFLAG:401 = 2
		ENDIF
	ELSE
		IF CFLAG:401 == 1 || FLAG:7 == 2
			CALL TRAIN_MESSAGE
			PRINTFORMW 
		ENDIF
	ENDIF
;-------------------------------------------------
;190,사진촬영,
;-------------------------------------------------
CASE 190
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:402 == 0
		PRINTFORMW 
		CFLAG:402 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:402 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:402 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:402 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:402 = 3
		;それ以外
		ELSEIF CFLAG:402 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:402 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;191,강정신약,
;-------------------------------------------------
CASE 191
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:403 == 0
		PRINTFORMW 
		CFLAG:403 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:403 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:403 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:403 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:403 = 3
		;それ以外
		ELSEIF CFLAG:403 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:403 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;192,콘돔정음（노예）
;-------------------------------------------------
CASE 192
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:404 == 0
		PRINTFORMW 
		CFLAG:404 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:404 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:404 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:404 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:404 = 3
		;それ以外
		ELSEIF CFLAG:404 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:404 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;193,콘돔정음（조수）
;-------------------------------------------------
CASE 193
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:405 == 0
		PRINTFORMW 
		CFLAG:405 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:405 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:405 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:405 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:405 = 3
		;それ以外
		ELSEIF CFLAG:405 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:405 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;194,정액키스
;-------------------------------------------------
CASE 194
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:406 == 0
		PRINTFORMW 
		CFLAG:406 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:406 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:406 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:406 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:406 = 3
		;それ以外
		ELSEIF CFLAG:406 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:406 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;195,유선개발
;-------------------------------------------------
CASE 195
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:431 == 0
		PRINTFORMW 
		CFLAG:431 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:431 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:431 = 3
		;それ以外
		ELSEIF CFLAG:431 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:431 = 2
		ENDIF
	ENDIF


;=================================================
;500番台,派生コマンド
;=================================================
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
CASE 500
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:407 == 0
		;처녀
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:407 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:407 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:407 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:407 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:407 = 3
		;それ以外
		ELSEIF CFLAG:407 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:407 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;501,식스나인
;-------------------------------------------------
CASE 501
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:408 == 0
		PRINTFORMW 
		CFLAG:408 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:408 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:408 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:408 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:408 = 3
		;それ以外
		ELSEIF CFLAG:408 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:408 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;502,이와시미즈
;-------------------------------------------------
CASE 502
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:409 == 0
		PRINTFORMW 
		CFLAG:409 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:409 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:409 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:409 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:409 = 3
		;それ以外
		ELSEIF CFLAG:409 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:409 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;503,G스팟자극
;-------------------------------------------------
CASE 503
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:410 == 0
		PRINTFORMW 
		CFLAG:410 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:410 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:410 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:410 = 3
		;それ以外
		ELSEIF CFLAG:410 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:410 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;504,흐트러진모란
;-------------------------------------------------
CASE 504
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:411 == 0
		;처녀
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:411 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:411 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:411 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:411 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:411 = 3
		;それ以外
		ELSEIF CFLAG:411 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:411 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;505,기승유방자위
;-------------------------------------------------
;바기나派生
CASE 505
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:412 == 0
		;처녀
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:412 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:412 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:412 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:412 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:412 = 3
		;それ以外
		ELSEIF CFLAG:412 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:412 = 2
		ENDIF
	ENDIF
;애널派生
CASE 512
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:419 == 0
		PRINTFORMW 
		CFLAG:419 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:419 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:419 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:419 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:419 = 3
		;それ以外
		ELSEIF CFLAG:419 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:419 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;506,삽입Ｇ스팟자극
;-------------------------------------------------
CASE 506
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:413 == 0
		PRINTFORMW 「하아아아、응아아아아、기분좋은 곳、꾹꾹 눌려져 버려어」
		CFLAG:413 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:413 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「앗、거기이、입구쪽、잇、응하아、좋은 거야아、더、세게 눌러 줘어！」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「응냐아아、좋앗、좋앗、거기、기분좋아아！」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:413 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:413 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:413 = 3
		;それ以外
		ELSEIF CFLAG:413 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:413 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;507,삽입자궁구자극
;-------------------------------------------------
CASE 507
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:414 == 0
		PRINTFORMW 「우아아아、안됏！응하아아아앗！거긴 아기방인데에！」
		CFLAG:414 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「툭툭하면서、안까지 오고 있어엇！아하아、이상하게 돼버려어！」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「툭툭하면서、안까지 오고 있어엇！아하아、응하아、더、더 붙여 줘어！」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:414 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:414 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:414 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;508,６９파이즈리
;-------------------------------------------------
CASE 508
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:415 == 0
		PRINTFORMW 
		CFLAG:415 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:415 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:415 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:415 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:415 = 3
		;それ以外
		ELSEIF CFLAG:415 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:415 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;509,착유
;-------------------------------------------------
CASE 509
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:416 == 0
		PRINTFORMW 
		CFLAG:416 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:416 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:416 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:416 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:416 = 3
		;それ以外
		ELSEIF CFLAG:416 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:416 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;510,더블스마타
;-------------------------------------------------
CASE 510
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:417 == 0
		PRINTFORMW 
		CFLAG:417 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:417 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:417 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:417 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:417 = 3
		;それ以外
		ELSEIF CFLAG:417 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:417 = 2
		ENDIF
	ENDIF

;-------------------------------------------------
;511,두개펠라
;-------------------------------------------------
CASE 511
	CALL TRAIN_MESSAGE
;初めて
	IF CFLAG:418 == 0
		PRINTFORMW 
		CFLAG:418 = 1
;二回目以降
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:418 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:418 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:418 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:418 = 3
		;それ以外
		ELSEIF CFLAG:418 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:418 = 2
		ENDIF
	ENDIF

	CASEELSE
		CALL TRAIN_MESSAGE
ENDSELECT

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係（X1をキャラ번호に置換）
;パラメータ변화などをトリガーにした구상　CFLAG 221～260を사용
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（X1をキャラ번호に置換）
;조교자の사정をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_A_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교자の사정時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;사정개소변경時のテキスト　有効時はここで関数종료
;-------------------------------------------------
IF CFLAG:106
	CALL PALAM_MESSAGE_A
	;머리카락
	IF CFLAG:106 == 1
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)
			PRINTFORMW 「응읏、잔뜩、나왔네요‥‥후후、기분、좋았던 거야？」
			CFLAG:221 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:221 = 2
		;それ以外
		ELSEIF CFLAG:221 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:221 = 1
		ENDIF
	;顔
	ELSEIF CFLAG:106 == 2
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:222 < 3 || FLAG:7 == 2)
			PRINTFORML 「하아아아、굉장한 냄새。」
			PRINTFORMW %조사처리(CALLNAME,"는")%、얼굴에 퍼진 정액을 손가락으로 떠서 입으로 옮겨、마치 도발하는 것처럼 이쪽으로 시선을 향했다
			CFLAG:222 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:222 = 2
		;それ以外
		ELSEIF CFLAG:222 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:222 = 1
		ENDIF
	;가슴
	ELSEIF CFLAG:106 == 3
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:223 < 3 || FLAG:7 == 2)
			PRINTFORMW 「아핫、미끈미끈해서、유두 기분 좋아」
			CFLAG:223 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:223 = 2
		;それ以外
		ELSEIF CFLAG:223 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:223 = 1
		ENDIF
	;腹
	ELSEIF CFLAG:106 == 4
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:224 < 3 || FLAG:7 == 2)
			PRINTFORML 「우후후‥‥굉장히 뜨거운 거、많이 나왔네요？」
			PRINTFORMW %조사처리(CALLNAME,"는")%、배에 퍼진 정액을 손가락으로 떠서 입으로 옮겨、마치 도발하는 것처럼 이쪽으로 시선을 향했다
			CFLAG:224 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:224 = 2
		;それ以外
		ELSEIF CFLAG:224 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:224 = 1
		ENDIF
	;腋
	ELSEIF CFLAG:106 == 5
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:225 < 3 || FLAG:7 == 2)
			PRINTFORML 내뿜은 정액과 %CALLNAME%의 땀이 섞여、옆구리로 흘러내려 간다‥‥
			PRINTFORMW %조사처리(CALLNAME,"는")%、그 정액을 손가락으로 떠서 입으로 옮겨、마치 도발하는 것처럼 이쪽으로 시선을 향했다
			CFLAG:225 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:225 = 2
		;それ以外
		ELSEIF CFLAG:225 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:225 = 1
		ENDIF
	;太腿
	ELSEIF CFLAG:106 == 6
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:226 < 3 || FLAG:7 == 2)
			PRINTFORML 「우후후‥‥굉장히 뜨거운 거、많이 나왔네요？」
			PRINTFORMW %조사처리(CALLNAME,"는")%、허벅지에 퍼진 정액을 손가락으로 떠서 입으로 옮겨、마치 도발하는 것처럼 이쪽으로 시선을 향했다
			CFLAG:226 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:226 = 2
		;それ以外
		ELSEIF CFLAG:226 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:226 = 1
		ENDIF
	;割れ눈
	ELSEIF CFLAG:106 == 7
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:227 < 3 || FLAG:7 == 2)
			PRINTFORML 「우후후‥‥굉장히 뜨거운 거、많이 나왔네요？조금、아깝다는 생각이 들지만」
			PRINTFORML %조사처리(CALLNAME,"는")%、정액이 손가락에 묻는 것도 개의치 않고、스스로 음순을 벌려 보인다
			PRINTFORMW 「다음은 여기、질내에、응？」
			CFLAG:227 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:227 = 2
		;それ以外
		ELSEIF CFLAG:227 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:227 = 1
		ENDIF
	;尻
	ELSEIF CFLAG:106 == 8
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:228 < 3 || FLAG:7 == 2)
			PRINTFORML 「우후후‥‥굉장히 뜨거운 거、많이 나왔네요？조금、아깝다는 생각이 들지만」
			PRINTFORML %조사처리(CALLNAME,"는")%、정액투성이의 엉덩이를 요염하게 흔들며、%조사처리(CALLNAME:MASTER,"를")% 도발한다
			CFLAG:228 = 3
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:228 = 2
		;それ以外
		ELSEIF CFLAG:228 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:228 = 1
		ENDIF
	;ゴム
	ELSEIF CFLAG:106 == 9
		;섹스로사정かつ、애널섹스以外を実行時
		IF GROUPMATCH(SELECTCOM , 60 , 61 , 66 , 68 , 69 , 72)
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:229 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:229 = 3
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:229 = 2
			;それ以外
			ELSEIF CFLAG:229 < 1 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:229 = 1
			ENDIF
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:230 = 3
			;굴복각인Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:230 = 2
			;それ以外
			ELSEIF CFLAG:230 < 1 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:230 = 1
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;손、입以外で사정
;対応コマンドは키스、스마타、발コキ、애널強制봉사、더블스마타
;-------------------------------------------------
IF TFLAG:9 == 1
	CALL PALAM_MESSAGE_A
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:231 < 4 || FLAG:7 == 2)
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:231 = 4
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:231 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:231 = 3
	;それ以外
	ELSEIF CFLAG:231 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:231 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;조수를범한다로사정
;-------------------------------------------------
IF TFLAG:7 > 0
	CALL PALAM_MESSAGE_A
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:232 = 3
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:232 = 2
	;それ以外
	ELSEIF CFLAG:232 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:232 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;입으로사정
;-------------------------------------------------
IF TFLAG:0 > 0
	CALL PALAM_MESSAGE_A
	;이라마치오で사정
	IF SELECTCOM == 140
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 4 || FLAG:7 == 2)
		PRINTFORMW 「콜록、콜록、하아、굉장히 진했다‥‥기분 좋았어？」
			CFLAG:233 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:233 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:233 = 3
		;それ以外
		ELSEIF CFLAG:233 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:233 = 2
		ENDIF
	;それ以外の입사정
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:233 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:233 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:233 = 3
		;それ以外
		ELSEIF CFLAG:233 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:233 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;손으로사정
;-------------------------------------------------
IF TFLAG:1 > 0
	CALL PALAM_MESSAGE_A
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 4 || FLAG:7 == 2)
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:234 = 4
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:234 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:234 = 3
	;それ以外
	ELSEIF CFLAG:234 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:234 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;가슴으로사정
;-------------------------------------------------
IF TFLAG:34 > 0
	CALL PALAM_MESSAGE_A
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 4 || FLAG:7 == 2)
		PRINTFORMW %조사처리(CALLNAME,"는")% 그 정액을 손가락으로 떠서 입으로 옮겨、요염하게 미소지어 보인다
		CFLAG:235 = 4
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:235 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:235 = 3
	;それ以外
	ELSEIF CFLAG:235 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:235 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;섹스로사정
;-------------------------------------------------
IF TFLAG:2 > 0
	CALL PALAM_MESSAGE_A
	;애널섹스系
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 73 || SELECTCOM == 78 || SELECTCOM == 79 || SELECTCOM == 512 || SELECTCOM == 515
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 4 || FLAG:7 == 2)
			PRINTFORML 「응아아아아、안에、%CSTR:이인칭%의 것、잔뜩 나와 버렸어‥‥」
			PRINTFORML 「뱃속이……뜨거、워‥‥」
			PRINTFORML %CALLNAME%의 항문은 꾹꾹 육봉을 조이며、%조사처리(CALLNAME:MASTER,"로")%부터 정액을 쥐어짜내 간다‥‥
			CFLAG:237 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:237 < 3 || FLAG:7 == 2)
			PRINTFORMW 「아아、우‥‥아、안에서‥‥힛응‥‥아아‥‥」
			PRINTFORMW 「‥‥이제 됐어、맘대로 해‥‥」
			CFLAG:237 = 3
		;それ以外
		ELSEIF CFLAG:237 < 2 || FLAG:7 == 2
			PRINTFORMW 「뜨、뜨거워…싫어어어어어‥‥!」
			CFLAG:237 = 2
		ENDIF
	;奥攻め섹스系
	ELSEIF SELECTCOM == 506 || SELECTCOM == 507
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 4 || FLAG:7 == 2)
			PRINTFORML 「응아아아아、안！안에、%CSTR:이인칭%의 것、잔뜩 나와 버렸어‥‥」
			PRINTFORML 「이렇게 안에 싸면、아기 생겨버려‥‥」
			PRINTFORML 「‥‥우후후。그러니까、책임져 줘？」
			PRINTFORML %CALLNAME%의 질은 꾹꾹 조이면서、%CALLNAME:MASTER%의 정액을 삼켜 간다‥‥
			CFLAG:238 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:238 < 3 || FLAG:7 == 2)
			PRINTFORMW 「아아、우‥‥아、안잇‥‥힛응‥‥아아‥‥」
			PRINTFORMW 「‥‥이제 됐어、맘대로 해‥‥」
			CFLAG:238 = 3
		;それ以外
		ELSEIF CFLAG:238 < 2 || FLAG:7 == 2
			PRINTFORMW 「밖에！　싸 줘！　아아‥‥」
			PRINTFORMW 「너무해‥‥이렇게、안에、싸다니‥‥」
			CFLAG:238 = 2
		ENDIF
	;그외섹스系
	ELSE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 4 || FLAG:7 == 2)
			PRINTFORML 「응아아아아、안！안에、%CSTR:이인칭%의 것、잔뜩 나와 버렸어‥‥」
			PRINTFORML 「이렇게 깊게 싸면、생겨버려‥‥」
			PRINTFORML 「‥‥우후후。그러니까、책임져 줘？」
			PRINTFORML %CALLNAME%의 질은 꾹꾹 조이면서、%CALLNAME:MASTER%의 정액을 쥐어짜내 간다‥‥
			CFLAG:238 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:238 < 3 || FLAG:7 == 2)
			PRINTFORMW 「아아、우‥‥아、안에서‥‥힛응‥‥아아‥‥」
			PRINTFORMW 「‥‥이제 됐어、맘대로 해‥‥」
			CFLAG:238 = 3
		;それ以外
		ELSEIF CFLAG:238 < 2 || FLAG:7 == 2
			PRINTFORMW 「밖에！　싸 줘！　아아‥‥」
			PRINTFORMW 「너무해‥‥안에、싸다니‥‥」
			CFLAG:238 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;역강간으로사정させた
;-------------------------------------------------
IF SELECTCOM == 64 && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:239 = 3
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:239 = 2
	;それ以外
	ELSEIF CFLAG:239 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:239 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（X1をキャラ번호に置換）
;촉수の사정をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_E_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;촉수の사정時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;촉수가사정
;-------------------------------------------------
IF TFLAG:15 >0
	CALL PALAM_MESSAGE_E
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:240 = 3
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:240 = 2
	;それ以外
	ELSEIF CFLAG:240 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:240 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B関係（X1をキャラ번호に置換）
;奴隷の절정をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_B_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;奴隷の절정時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;初めてC절정
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:241 == 0
	PRINTFORMW 「시 싫어！잠깐 멈춰！기다려어어어」
	PRINTFORML 「싫어어、뭔가 와버려、이상한 곳 만져져서、%CSTR:일인칭%、싫엇、싫어어！」
	PRINTFORMW 그리고 %조사처리(CALLNAME,"는")% 팽팽하게 손발을 뻗었다。아무래도 가버린 것 같다。
	CFLAG:241 = 1
ENDIF

;-------------------------------------------------
;初めてV절정
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:242 == 0
	PRINTFORML 「응하아、시러어、이런 거、시이、안됏！또、움직여버리、응아아아아아！！」
	PRINTFORMW %CALLNAME%의 절규와 함께、질내는 꾹꾹 조여 왔다。
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;初めてA절정
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:243 == 0
	PRINTFORML 「싫어！이제 안돼！만지지 마！？시 시러어어어어」
	PRINTFORMW 「엉덩이로라니…싫어……、이제 싫어…」
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;初めてB절정
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:244 == 0
	PRINTFORML 「싫어！？이제 만지지 마！와버려！와버리니까아아아？！」
	PRINTFORML 「싫어어、와버려、가슴 만져져서、%CSTR:일인칭%、싫엇、싫어어어어어어어！」
	PRINTFORMW 몸을 부들부들 떨며、%조사처리(CALLNAME,"는")% 절정에 이르렀다
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;절정
;-------------------------------------------------
;強절정
IF TFLAG:29 > 1
	CALL PALAM_MESSAGE_B
	;애널系
	IF SELECTCOM == 4 || SELECTCOM == 5 || SELECTCOM == 45 || SELECTCOM == 46 || SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 73 || SELECTCOM == 142 || SELECTCOM == 149 || SELECTCOM == 512 || SELECTCOM == 514 || (SELECTCOM == 505 && TFLAG:40 == 2)
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:245 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				IF RAND:2 == 0
					PRINTFORM 「아아아아……기분좋아아、
					;엉덩이구멍狂い＋Ａ감각10
					IF TALENT:76 && Abl:5 == 10
						IF RAND:3 == 0
							PRINTFORM \@ RAND:2 == 0 ? 궁둥이보지 # 엉덩이보지 \@
						ELSEIF RAND:2 == 0
							PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
						ELSE
							PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
						ENDIF
					;엉덩이구멍狂い＋Ａ감각9
					ELSEIF TALENT:76 && Abl:5 == 9
						IF RAND:2 == 0
							PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
						ELSE
							PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
						ENDIF
					;엉덩이구멍狂い＋Ａ감각8
					ELSEIF TALENT:76 && Abl:5 == 8
						PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
					;それ以外
					ELSE
						PRINTFORM 엉덩이
					ENDIF
					PRINTFORML 로 간다、가버려、아히이이이이！」
				ELSE
					PRINTFORM 「아우우우……간다、간다、
					;엉덩이구멍狂い＋Ａ감각10
					IF TALENT:76 && Abl:5 == 10
						IF RAND:3 == 0
							PRINTFORM \@ RAND:2 == 0 ? 궁둥이보지 # 엉덩이보지 \@
						ELSEIF RAND:2 == 0
							PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
						ELSE
							PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
						ENDIF
					;엉덩이구멍狂い＋Ａ감각9
					ELSEIF TALENT:76 && Abl:5 == 9
						IF RAND:2 == 0
							PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
						ELSE
							PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
						ENDIF
					;엉덩이구멍狂い＋Ａ감각8
					ELSEIF TALENT:76 && Abl:5 == 8
						PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
					;それ以外
					ELSE
						PRINTFORM 엉덩이
					ENDIF
					PRINTFORML 가버려、가버리는 거야아아！」
				ENDIF
					PRINTFORML %조사처리(CALLNAME,"는")% 장벽으로 육봉을 빡빡하게 조이고、
						IF TALENT:0
							PRINTFORM 미관통의 처녀질에서 희고 탁한 점액을 늘어뜨리며
						ELSE
							PRINTFORM 이쪽도 가고 싶다고 말하는 것처럼、희고 탁한 점액을 흘리는 질구멍에서
						ENDIF
					PRINTFORMW 푸슈우우우우웃 하고 기세좋게 애액을 내뿜었다。
						IF TALENT:0 && TALENT:76
							PRINTFORM 「아아、%조사처리(CSTR:일인칭,"는")%、처녀인데…%CSTR:이인칭%에게、
							;엉덩이구멍狂い＋Ａ감각10
							IF TALENT:76 && Abl:5 == 10
								IF RAND:3 == 0
									PRINTFORM \@ RAND:2 == 0 ? 궁둥이보지 # 엉덩이보지 \@
								ELSEIF RAND:2 == 0
									PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
								ELSE
									PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
								ENDIF
							;엉덩이구멍狂い＋Ａ감각9
							ELSEIF TALENT:76 && Abl:5 == 9
								IF RAND:2 == 0
									PRINTFORM \@ RAND:2 == 0 ? 똥구멍 # 엉덩이구멍 \@
								ELSE
									PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
								ENDIF
							;엉덩이구멍狂い＋Ａ감각8
							ELSEIF TALENT:76 && Abl:5 == 8
								PRINTFORM \@ RAND:2 == 0 ? 애널 # 엉덩이 \@
							;それ以外
							ELSE
								PRINTFORM 엉덩이
							ENDIF
							PRINTFORMW 로、가버렸다………하아아…♡」
						ENDIF
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:245 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:245 < 3 || FLAG:7 == 2)
			PRINTFORMW 「이런 거、엉덩이인데、참을、수 없어！싫어어어어어어！！！」
			PRINTFORMW %조사처리(CALLNAME,"는")% 혀를 쑥 내밀어、칠칠치 못하게 침을 늘어뜨리면서 항열의 클라이맥스로 저속해져 갔다。
			CFLAG:245 = 3
		;それ以外
		ELSEIF CFLAG:245 < 2 || FLAG:7 == 2
			PRINTFORMW 「이렇게、엉덩이로、라니이이！아아아！」
			PRINTFORMW %조사처리(CALLNAME,"는")% 침을 늘러뜨리면서 전신을 격하게 경련하며、퓻퓻하고 애액을 뿜었다。
			CFLAG:245 = 2
		ENDIF
	;함락済み＋애널以外
	ELSEIF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:245 < 4 || FLAG:7 == 2)
		PRINTFORML 「핫、핫、하아、응아아、꽉 해、꽉 해줘어어！」
		PRINTFORMW 말한대로 세게 껴안자、안심한 것처럼 절정에 몸을 맡긴다
		CFLAG:245 = 4
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:245 < 3 || FLAG:7 == 2)
		PRINTFORML 「싫어어어어！간다！가버려어어！」
		PRINTFORMW %조사처리(CALLNAME,"는")% 혀를 쑥 내밀어、칠칠치 못하게 침을 늘어뜨리면서 전신을 격하게 경련했다。
		CFLAG:245 = 3
	;それ以外
	ELSEIF CFLAG:245 < 2 || FLAG:7 == 2
		PRINTFORML 「싫어어어어！와버려！와버려어！」
		PRINTFORMW %조사처리(CALLNAME,"는")% 몸을 꿰뚫는 쾌감에 전신을 부들부들 떨었다。
		CFLAG:245 = 2
	ENDIF
;절정
ELSEIF TFLAG:29 > 0
	CALL PALAM_MESSAGE_B
	;애널系
	IF SELECTCOM == 4 || SELECTCOM == 5 || SELECTCOM == 45 || SELECTCOM == 46 || SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 73 || SELECTCOM == 142 || SELECTCOM == 149 || SELECTCOM == 512 || SELECTCOM == 514 || (SELECTCOM == 505 && TFLAG:40 == 2)
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:245 < 4 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
			PRINTFORMW 「아아、가버려、엉덩이로、가버려어！」
			PRINTFORMW %조사처리(CALLNAME,"는")% 항학의 쾌감에 전신을 경련했다。
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:245 = 4
		;굴복각인Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:245 < 3 || FLAG:7 == 2)
			PRINTFORMW 「싫어、싫어、엉덩이따위로 가고 싶지 않아！싫어어어어어어！」
			PRINTFORMW %조사처리(CALLNAME,"는")% 항학의 쾌감에 전신을 경련했다。
			CFLAG:245 = 3
		;それ以外
		ELSEIF CFLAG:245 < 2 || FLAG:7 == 2
			PRINTFORMW 「시、싫어！엉덩이、뜨거워져、싫어어！」
			PRINTFORMW %조사처리(CALLNAME,"는")% 항학의 쾌감에 전신을 경련했다。
			CFLAG:245 = 2
		ENDIF
	;함락済み＋애널以外
	ELSEIF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:245 < 4 || FLAG:7 == 2)
		;연모、친애
		IF TALENT:3 || TALENT:6
		PRINTFORML 「아아아、간닷、간다、간다아！」
		PRINTFORMW %조사처리(CALLNAME,"는")% 쾌감에 전신을 경련했다。
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:245 = 4
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:245 < 3 || FLAG:7 == 2)
		PRINTFORML 「싫어어、싫으은、싫은데에、와버려、와버리는 거야아！」
		PRINTFORMW %조사처리(CALLNAME,"는")% 쾌감에 전신을 경련했다。
		CFLAG:245 = 3
	;それ以外
	ELSEIF CFLAG:245 < 2 || FLAG:7 == 2
		PRINTFORML 「싫어어、이제、싫은데에、오지 마아、싫은데에！」
		PRINTFORMW %조사처리(CALLNAME,"는")% 쾌감에 전신을 경련했다。
		CFLAG:245 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;사정
;-------------------------------------------------
;사정している
IF TFLAG:10 > 0
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)
		PRINTFORMW 「하아、불끈불끈 해버려어、하아、사정하는 거、좋아해」
		CFLAG:246 = 3
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:246 = 2
	;それ以外
	ELSEIF CFLAG:246 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:246 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;분유
;-------------------------------------------------
;분유している
IF TFLAG:11 > 0
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:247 < 3 || FLAG:7 == 2)
		PRINTFORMW 「응아아아아！젖 나와버린다、부끄러워어」
		CFLAG:247 = 3
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:247 = 2
	;それ以外
	ELSEIF CFLAG:247 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:247 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;방뇨
;-------------------------------------------------
;방뇨している
IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)
	;함락済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:248 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:248 = 3
	;굴복각인Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:248 = 2
	;それ以外
	ELSEIF CFLAG:248 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:248 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C関係（X1をキャラ번호に置換）
;조교後の변화をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_C_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;조교後のテキスト
;-------------------------------------------------
;-------------------------------------------------
;처녀상실(처녀のみ)
;-------------------------------------------------
IF TFLAG:3 > 0
	CALL PALAM_MESSAGE_C
	;연모か친애かつ主人の섹스による처녀상실
	IF TFLAG:20
		PRINTFORML 순결의 증거가 흘러나오는 걸 보며、%조사처리(CALLNAME,"는")% 맥없이 미소지었다
		PRINTFORML 「이걸로、몸도 마음도、%CSTR:이인칭%의 것이네요‥‥」
		PRINTFORML 그리 중얼거리고、돌연 뚝뚝 눈물을 흘리기 시작한다
		PRINTFORML 「소중히、소중히、해 줘。%CSTR:일인칭%도、할 수 있는 건、뭐든지 해줄 테니까」
		PRINTFORML 「반드시、버리지 말아줘요‥‥%CSTR:일인칭%의、왕자님…」
		PRINTFORMW 대답 대신에 %조사처리(CALLNAME,"가")% 울음을 그칠 때까지、세게 껴안았다‥‥
	;それ以外の時に처녀상실
	ELSE
		;함락済み
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
			PRINTFORML %조사처리(CALLNAME,"는")% 자신의 입장을 이해하고는 있지만、
			PRINTFORML 이번은 역시 한마디 하고 싶은 것 같다。
			PRINTFORML 「적어도 처음만큼은‥‥좀 더、상냥하게 해주길 바랬는데‥‥」
			PRINTFORMW 단념한 듯이 중얼거린 %CALLNAME%의 눈꼬리에는 약간이지만 눈물이 맺혀 있다。
		;굴복각인Lv3
		ELSEIF MARK:2 == 3
			PRINTFORML %조사처리(CALLNAME,"는")% 자신의 입장을 이해하고는 있지만、
			PRINTFORML 이번은 역시 한마디 하고 싶은 것 같다。
			PRINTFORML 「적어도 처음만큼은‥‥좀 더、상냥하게 해주길 바랬는데‥‥」
			PRINTFORMW 단념한 듯이 중얼거린 %CALLNAME%의 눈꼬리에는 약간이지만 눈물이 맺혀 있다。
		;それ以外
		ELSE
			PRINTFORML 「‥‥이런、이런 게‥‥%CSTR:일인칭%의、처음、이、라니‥‥」
			PRINTFORMW 어이없이 빼앗긴 순결에、%조사처리(CALLNAME,"는")% 눈물을 흘리면서 망연자실하고 있다。
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D関係（X1をキャラ번호に置換）
;パラメータ변화をトリガーにした구상
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_PALAMCNG_D_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;パラメータ변화時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;初めて윤활がLV3超えた
;-------------------------------------------------
P = PALAM:4 + UP:4
IF P > PALAMLV:4 && CFLAG:245 == 0
	CALL PALAM_MESSAGE_D
	;로션を使った場合
	IF SELECTCOM == 180
		PRINTFORML %CALLNAME%의 몸에 로션을 바르면서、차분하게 애무한다。
		PRINTFORML 「‥‥이런 걸 바르면、젖는게 당연하잖아」
		PRINTFORML 「끈적거려…나중에 씻어내는 것도 큰일같아…」
		PRINTFORMW 그렇게 %조사처리(CALLNAME,"는")% 딴청을 부리지만、그녀의 몸이 로션이외의 것으로도 젖고 있는 것은、당연히 알아챘다。
	;それ以外
	ELSE
		PRINTFORMW 문득 깨달으면、%CALLNAME%의 사타구니는 요염하게 미끈거리고 있다。
		PRINTFORMW 그걸 지적해 주자、%조사처리(CALLNAME,"는")% 분한듯이 눈을 돌린다。
	ENDIF
	CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;初めて욕정がLV2超えた
;-------------------------------------------------
P = PALAM:6 + UP:6
IF P > PALAMLV:3 && CFLAG:246 == 0
	CALL PALAM_MESSAGE_D
	;미약を使った場合
	IF SELECTCOM == 181
		PRINTFORMW 「비、비겁햇！　이런 약을 쓰다닛」
		PRINTFORMW %조사처리(CALLNAME,"는")% 위세좋게 소리치지만、그녀의 몸은 순식간에 달아올라 간다‥‥
	;それ以外
	ELSE
		PRINTFORMW %CALLNAME%의 가랑이에서 점액을 건져내、그녀의 눈앞에 내민다。
		PRINTFORMW 변명할 수 없는 증거에、%조사처리(CALLNAME,"는")% 얼굴을 빨갛게하고 눈을 내리깐다。
	ENDIF
	CFLAG:246 = 1
ENDIF

;-------------------------------------------------
;初めて굴복がLV3超えた
;-------------------------------------------------
P = PALAM:7 + UP:7
IF P > PALAMLV:3 && CFLAG:247 == 0
	CALL PALAM_MESSAGE_D
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:247 = 1
ENDIF

;-------------------------------------------------
;初めて복종がLV3超えた
;-------------------------------------------------
P = PALAM:5 + UP:5
IF P > PALAMLV:3 && CFLAG:248 == 0
	CALL PALAM_MESSAGE_D
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:248 = 1
ENDIF

;-------------------------------------------------
;初めて공포がLV3超えた
;-------------------------------------------------
P = PALAM:11 + UP:11
IF P > PALAMLV:3 && CFLAG:249 == 0
	CALL PALAM_MESSAGE_D
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:249 = 1
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG関係（X1をキャラ번호に置換）
;각인変動をトリガーにした구상
;각인変動後に구상を発動します
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K4561
;조수が조교した時に구상をスキップする（好みに応じて使う、行머리の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;볼재갈着用時には地の文を표시して구상をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;特定キャラが조수、主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_MARKCNG_PY1_K4561
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL KOJO_MESSAGE_MARKCNG_PY1_K4561
;		RETURN 0
;	ENDIF
;ENDIF
;-------------------------------------------------
;고통각인Lv3
;-------------------------------------------------
IF TFLAG:22 == 3
	CALL MARK_MESSAGE_A
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;쾌락각인Lv3
;-------------------------------------------------
IF TFLAG:23 == 3
	CALL MARK_MESSAGE_P
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 「저기、음란하게 돼도、싫어하지 말아줘요？약속이야？」
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 「저기、더、기분좋게 해줘？」
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;굴복각인Lv3
;-------------------------------------------------
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	CALL MARK_MESSAGE_S
	;쾌락による굴복
	IF TFLAG:24 == 13
		PRINTFORML （하아‥‥하‥‥뭐야 이거‥‥기분 너무 좋아‥‥）
		PRINTFORMW 「제발、이제 그만둬‥‥뭐든 할테니까、더 이상‥‥%CSTR:일인칭%의 몸을 만지지 마！」
	;공포と고통による굴복
	ELSEIF TFLAG:24 == 23
		PRINTFORML （아‥‥아‥‥무서워‥‥부서져‥‥）
		PRINTFORMW 「제발、이제 그만둬‥‥뭐든 할테니까、이제‥‥」
	;굴복と복종による굴복
	ELSEIF TFLAG:24 == 33
		PRINTFORMW 「제발、이제 그만둬‥‥뭐든 할테니까、더 이상‥‥」
	ENDIF
	PRINTFORMW (‥‥미안해、치에。　나、이제‥‥)
ENDIF

;-------------------------------------------------
;반발각인Lv3
;-------------------------------------------------
IF TFLAG:21 == 3
	CALL MARK_MESSAGE_R
	;함락済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모、친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;음란、창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종、예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;이벤트구상集（X1、X2をキャラ번호に置換する） CFLAG 261～299を사용
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_K4561
;--------------------------------------------------
;特定主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_K4561
;	RETURN 0
;ENDIF
;-------------------------------------------------
;朝フェラ
;-------------------------------------------------
SELECTCASE TFLAG:13
	CASE 1
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
			PRINTFORML 「응‥‥안녕。우후후、참을 수 없게 되면、싸줘요？」
			PRINTFORMW 그리 말하고、다시 %조사처리(CALLNAME,"는")% 봉사에 몰두해 갔다。
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
			PRINTFORML 「응후우、안녕‥‥。오늘도 진한 거、많이 싸줘요？」
			PRINTFORMW 그리 말하며 페니스에 키스를 하고、다시 삼켜 갔다‥‥
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:261 = 1
		ENDIF

;-------------------------------------------------
;朝파이즈리
;-------------------------------------------------
	CASE 2
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				;모유체질かどうか
				IF TALENT:149
			PRINTFORML 「응‥‥안녕。우후후、참을 수 없게 되면、싸줘요？」
			PRINTFORML 그리 말하고、다시 %조사처리(CALLNAME,"는")% 모유를 뿜어내면서 봉사에 몰두해 갔다。
				ELSE
			PRINTFORML 「응‥‥안녕。우후후、참을 수 없게 되면、싸줘요？」
			PRINTFORML 그리 말하고、다시 %조사처리(CALLNAME,"는")% 봉사에 몰두해 갔다。
				ENDIF
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				;모유체질かどうか
				IF TALENT:149
			PRINTFORML 「응후아、안녕‥‥。오늘도 진한 거、많이 싸줘요？」
			PRINTFORML 그리 말하며 페니스에 키스를 하고、모유를 뿜어내면서 봉사에 열중해 갔다‥‥
				ELSE
			PRINTFORML 「응후아、안녕‥‥。오늘도 진한 거、많이 싸줘요？」
			PRINTFORML 그리 말하며 페니스에 키스를 하고、봉사에 열중해 갔다‥‥
				ENDIF
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				;모유체질かどうか
				IF TALENT:149
					PRINTFORMW 
				ELSE
					PRINTFORMW 
				ENDIF
			ENDIF
			CFLAG:262 = 1
		ENDIF

;-------------------------------------------------
;朝기승위
;-------------------------------------------------
	CASE 3
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
			PRINTFORML 「응아、안、녕‥‥。커져、응아아、있었으니까、넣어버렸다」
			PRINTFORML 「아침 첫번째의 진한 밀크、질내에 잔뜩 넣어 줘요？」
			PRINTFORMW 그리 말하고、천천히 허리를 상하로 움직인다‥‥
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
			PRINTFORML 「응 정말、겨우、일어난 거야？」
			PRINTFORML 「스스론、안까지、할 수 없으、응읏、니까、애가 탔던 거야‥‥」
			PRINTFORMW 「저기、빨리、더 안까지 휘저어 줘어！」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;朝기승위A
;-------------------------------------------------
	CASE 24
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;朝키스
;-------------------------------------------------
	CASE 25
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「응～～～、음、츄릅……」
				PRINTFORMW 숨이 막혀 눈을 뜨자、%CALLNAME%에게 입을 빨리고 있다
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;朝食
;-------------------------------------------------
	CASE 26
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				;歌いながらかどうか
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2
					PRINTFORMW 
				ELSE
					PRINTFORMW 
				ENDIF
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				;歌いながらかどうか
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2
					PRINTFORMW 
				ELSE
					PRINTFORMW 
				ENDIF
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				;歌いながらかどうか
				IF (TALENT:25 || TALENT:28) && ABL:14 >= 2
					PRINTFORMW 
				ELSE
					PRINTFORMW 
				ENDIF
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;起きたら隣で寝てる
;-------------------------------------------------
	CASE 27
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;朝の발コキ
;-------------------------------------------------
	CASE 28
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;;朝の搾りたてミルク
;-------------------------------------------------
	CASE 29
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;;お寝小
;-------------------------------------------------
	CASE 30
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:263 = 1
		ENDIF

;-------------------------------------------------
;조교後레즈プレイ
;-------------------------------------------------
	CASE 4
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:264 == 0
			PRINTFORMW 
			CFLAG:264 = 1
		;それ以外
		ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:264 = 1
		ENDIF

;-------------------------------------------------
;조교後섹스
;-------------------------------------------------
	CASE 5
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORML 「상스러운 것은、싫어？」
				PRINTFORML 「그치만、%CSTR:일인칭%、참을 수 없는 거야…」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「저기、아직이겠죠？좀 더、해줄 거죠？응？」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:265 = 1
		ENDIF

;-------------------------------------------------
;강정신약の反動
;-------------------------------------------------
	CASE 6
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:266 = 2
		ELSEIF CFLAG:266 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:266 = 1
		ENDIF

;-------------------------------------------------
;조교後착유
;-------------------------------------------------
	CASE 7
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「하앗、잔뜩 나왔다‥‥。우후후、나중에 먹여 줘야지‥‥」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:267 = 2
		ELSEIF CFLAG:267 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:267 = 1
		ENDIF

;-------------------------------------------------
;조교後자위
;-------------------------------------------------
	CASE 8
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「우으‥‥안타까워어‥‥더、안길 원해애‥‥」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「우으‥‥안타까워어‥‥더、안길 원해애‥‥」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:268 = 2
		ELSEIF CFLAG:268 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:268 = 1
		ENDIF

;-------------------------------------------------
;요바이（主人C 相手V）
;-------------------------------------------------
	CASE 9
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「아무리 해도、참을 수 없던 거야。미안해요‥‥」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「아무리 해도、참을 수 없던 거야。미안해요‥‥。」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;요바이（主人C 相手A）
;-------------------------------------------------
	CASE 31
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「아무리 해도、참을 수 없던 거야。미안해요‥‥。」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「아무리 해도、참을 수 없던 거야。미안해요‥‥。」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;요바이（主人V 相手C）
;-------------------------------------------------
	CASE 32
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「아무리 해도、참을 수 없던 거야。미안해요‥‥。」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 「아무리 해도、참을 수 없던 거야。미안해요‥‥。」
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;요바이（主人A 相手C）
;-------------------------------------------------
	CASE 33
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;요바이（両者C（스마타）・両者C（貝合わせ））
;-------------------------------------------------
	CASE 34
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 「하아、하아、%CSTR:이인칭%、%CSTR:이인칭%、%CSTR:이인칭%♡」
				PRINTFORMW %조사처리(CALLNAME,"는")% 열중해서 %CALLNAME:MASTER%에게 조개를 맞대 문지르는、음란한 유희에 빠져들었다。
				PRINTFORMW 이윽고 한계가 가깝다고 본、%조사처리(CALLNAME:MASTER,"는")% %CALLNAME%의 머리를 붙잡아 격한 키스를 나눈다。
				PRINTFORMW 「응……응우우우우으으으으으으으……♡」
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;夜자위
;-------------------------------------------------
	CASE 35
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;一緒に寝る
;-------------------------------------------------
	CASE 36
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:269 = 1
		ENDIF

;-------------------------------------------------
;황색사탕사용時
;-------------------------------------------------
	CASE 10
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:270 = 2
		ELSEIF CFLAG:270 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:270 = 1
		ENDIF

;-------------------------------------------------
;임신発깨달음時
;-------------------------------------------------
	CASE 11
		SIF CFLAG:271 >= 1 && FLAG:7 != 2
			RETURN 0
		CALL EVENT_MESSAGE
		;임신대상をTARGETに격납し、父親は奴隷はV、조수はWとする
		;붕괴してしまった場合
		IF TALENT:9 == 1
			PRINTFORMW 
		;父親が主人で母親が연모か친애持ち
		ELSEIF (TALENT:3 || TALENT:6)
			IF CFLAG:102 == 1
				IF FAVORITE_1(TARGET) == MASTER
					PRINTFORML 
					PRINTFORML 「아아、마침내 생겨버렸다‥‥。」
					PRINTFORML 대사와는 다르게、어조는 밝다
					PRINTFORML 「저기？책임、져 주겠지？」
					PRINTFORML 그리고 응석부리듯이 팔짱을 껴온다。
					PRINTFORMW 「소중히 대하지 않으면 안될 사람이、한 사람 늘어버렸죠？아빠？」
					PRINTFORMW 그리 말하곤 상냥하게 미소지었다
					PRINTFORML 
				ELSE
					PRINTFORMW 
				ENDIF
			ELSE
				IF FAVORITE_1(TARGET) != MASTER
					IF TALENT:ＮＴＲ == 1
						PRINTFORML 
					ELSEIF TALENT:ＮＴＲ == 3
						PRINTFORMW 
					ENDIF
				ELSE
					PRINTFORMW 
				ENDIF
			ENDIF
		;父親が奴隷か조수で母親が음란か창부持ち
		ELSEIF (TALENT:4 || TALENT:7)
			IF CFLAG:102 == 1
			;父親が당신
				PRINTFORMW 
			;父親(조수）との相性が125より大きい場合
			ELSEIF CFLAG:102 == 2 && GET_RELATION(TARGET, W) > 125
				PRINTFORMW 
	 		;父親(奴隷）との相性が125より大きい場合
			ELSEIF CFLAG:102 == 3 && GET_RELATION(TARGET, V) > 125
				PRINTFORMW 
			ELSE
				IF FAVORITE_1(TARGET) != MASTER
					IF TALENT:ＮＴＲ == 1
						PRINTFORML 
						PRINTFORMW 「우후후♡」
						PRINTFORMW 「앗、%CALLNAME:MASTER%、%CSTR:일인칭%…생겨버린 거야♪」
						PRINTFORMW 「엣？책임지는 건 좋아、그치만 당신의 아이가 아닌 거야」
						PRINTFORMW 「기뻐、이걸로 그 사람에게 더 소중히 대해지는 거야♡」
						PRINTFORML 
					ELSEIF TALENT:ＮＴＲ == 3
						PRINTFORMW 
					ENDIF
				ELSE
					PRINTFORMW 
				ENDIF
			ENDIF
		;복종か예속持ち
		ELSEIF (TALENT:5 || TALENT:8)
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:271 = 1

;-------------------------------------------------
;출산時
;-------------------------------------------------
	CASE 12
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:272 = 2
		ELSEIF CFLAG:272 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:272 = 1
		ENDIF

;-------------------------------------------------
;육아실
;-------------------------------------------------
	CASE 13
		CALL EVENT_MESSAGE
		;출산前
		IF TALENT:153
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:273 = 2
			ELSEIF CFLAG:273 < 1 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:273 = 1
			ENDIF
		;출산後
		ELSE
			;함락済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
				;연모、친애
				IF TALENT:3 || TALENT:6
					PRINTFORMW 
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					PRINTFORMW 
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					PRINTFORMW 
				ENDIF
				CFLAG:273 = 4
			ELSEIF CFLAG:273 < 3 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:273 = 3
			ENDIF
		ENDIF

;-------------------------------------------------
;親離れ時
;-------------------------------------------------
	CASE 14
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:274 = 2
		ELSEIF CFLAG:274 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:274 = 1
		ENDIF

;-------------------------------------------------
;定食포장마차時
;-------------------------------------------------
	CASE 15
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:275 == 0
			PRINTFORML 「좋ー아、힘내자ー！」
			PRINTFORMW %CALLNAME%의 활기넘치는 모습에 뭔가 말할 수 없는 불안을 느낀다…
			CFLAG:275 = 1
		;それ以外
		ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
			PRINTFORML 「좋ー아、힘내자ー！」
			PRINTFORMW %CALLNAME%의 활기넘치는 모습에 뭔가 말할 수 없는 불안을 느낀다…
			CFLAG:275 = 1
		ENDIF

;-------------------------------------------------
;창관でバイト時
;-------------------------------------------------
	CASE 16
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:276 == 0
		PRINTFORMW 「저、정말로‥‥여기서‥‥？」
		PRINTFORMW 역시 몸을 파는 건 다소 저항이 있는 건지、확인하듯이 이쪽을 돌아보는 %CALLNAME%에게、고개를 끄덕여 준다。
		PRINTFORMW 「우‥‥왜、이런 짓에‥‥」
		PRINTFORMW %조사처리(CALLNAME,"는")% 단념한 것처럼 머리를 떨구고、창관으로 향했다‥‥
			CFLAG:276 = 1
		;それ以外
		ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:276 = 1
		ENDIF

;-------------------------------------------------
;렌탈時
;-------------------------------------------------
	CASE 17
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:277 == 0
			PRINTFORMW 
			CFLAG:277 = 1
		;それ以外
		ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:277 = 1
		ENDIF

;-------------------------------------------------
;매각時
;-------------------------------------------------
	CASE 18
		CALL EVENT_MESSAGE
		;함락済み
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
			;연모、친애+반발각인Lv3미만
			IF (TALENT:3 || TALENT:6) && MARK:3 < 3
			PRINTFORML 「거짓말、이겠죠？저기、거짓말이죠！」
			PRINTFORML 「너무해、너무해‥‥소중히 한다고 말했잖아」
			PRINTFORML 「뭐가 나빴던 거야！？%조사처리(CSTR:이인칭,"가")% 바라는 건 뭐든지 응했는데！」
			PRINTFORML 「어째서！어째서야아‥‥」
			PRINTFORML 얼마간 고개를 숙이고나서、천천히 일어서서、단호하게 %조사처리(CALLNAME:MASTER,"를")% 응시한다
			PRINTFORMW 「절대로、잊지 않으니까‥‥미워、미워、미워！」
			PRINTFORMW 「그리고、제일、좋아한 사람이야‥‥」
			PRINTFORMW 눈물을 띄우면서 미소짓고、%조사처리(CALLNAME,"는")% 한번도 뒤돌아보지 않았다。
			;음란、창부+반발각인Lv3미만
			ELSEIF (TALENT:4 || TALENT:7) && MARK:3 < 3
				PRINTFORMW 
			;복종、예속+반발각인Lv3미만
			ELSEIF (TALENT:5 || TALENT:8) && MARK:3 < 3
				PRINTFORMW 
			ENDIF
		;반발각인Lv3
		ELSEIF MARK:3 == 3
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF

;-------------------------------------------------
;ビデオ매각時
;-------------------------------------------------
	CASE 19
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:278 = 2
		ELSEIF CFLAG:278 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:278 = 1
		ENDIF

;-------------------------------------------------
;コンサート時
;-------------------------------------------------
	CASE 20
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:279 == 0
			PRINTFORMW 
			CFLAG:279 = 1
		;それ以外
		ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:279 = 1
		ENDIF

;-------------------------------------------------
;乱交라이브時
;-------------------------------------------------
	CASE 21
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:280 == 0
			PRINTFORMW 
			CFLAG:280 = 1
		;それ以外
		ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:280 = 1
		ENDIF

;-------------------------------------------------
;공중변소プレイ時
;-------------------------------------------------
	CASE 22
		CALL EVENT_MESSAGE
		;初めて
		IF CFLAG:281 == 0
			PRINTFORMW 
			CFLAG:281 = 1
		;それ以外
		ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:281 = 1
		ENDIF

;-------------------------------------------------
;ビデオ保有時
;-------------------------------------------------
	CASE 23
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:282 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:282 = 2
		ELSEIF CFLAG:282 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:282 = 1
		ENDIF

;-------------------------------------------------
;기억소거약사용時
;-------------------------------------------------
	CASE 30
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 2 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:283 = 2
		ELSEIF CFLAG:283 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:283 = 1
		ENDIF

;-------------------------------------------------
;;[아내][남편]취득時
;-------------------------------------------------
	CASE 40
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:283 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:283 = 1
		ENDIF

;-------------------------------------------------
;;[음마]취득時
;-------------------------------------------------
	CASE 41
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:284 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:284 = 1
		ENDIF

;-------------------------------------------------
;;[専属奴隷]취득時
;-------------------------------------------------
	CASE 42
		CALL EVENT_MESSAGE
		;함락済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
			;연모、친애
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;음란、창부
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;복종、예속
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:285 = 1
		ENDIF
		;-------------------------------------------------
		;;[愛ＮＴＲ]이벤트時
		;-------------------------------------------------
	CASE 45
		CALL EVENT_MESSAGE
		;함락済み
		IF (CFLAG:288 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;主人に함락しているor誰にも함락してない상태でＮＴＲ
				;연모、친애
				IF TALENT:3 || TALENT:6
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;그외
				ELSE
					;進行度分岐
					;初回
					IF CFLAG:애ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:애ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				ENDIF
			ELSE
				;主人以外の誰かに함락している상태で更に別のキャラにＮＴＲ
				;進行度分岐
				;初回
				IF CFLAG:애ＮＴＲ진행도 == 0
					PRINTFORMW 
				;二回目（リーチ）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 1
					PRINTFORMW 
				;三回目（함락）
				ELSEIF CFLAG:애ＮＴＲ진행도 == 2
					PRINTFORMW 
				ENDIF
			ENDIF
			CFLAG:288 = 1
		ENDIF
		;-------------------------------------------------
		;;[淫ＮＴＲ]이벤트時
		;-------------------------------------------------
	CASE 46
		CALL EVENT_MESSAGE
		;함락済み
		IF (CFLAG:289 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;主人に함락しているor誰にも함락してない상태でＮＴＲ
				;연모、친애
				IF TALENT:3 || TALENT:6
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						PRINTFORMW 「우후후、더 희고 걸쭉한 걸 안에 싸줘어♪」
					ENDIF
				;그외
				ELSE
					;進行度分岐
					;初回
					IF CFLAG:음ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:음ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				ENDIF
			ELSE
				;主人以外の誰かに함락している상태で更に別のキャラにＮＴＲ
				;進行度分岐
				;初回
				IF CFLAG:음ＮＴＲ진행도 == 0
					PRINTFORMW 
				;二回目（リーチ）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 1
					PRINTFORMW 
				;三回目（함락）
				ELSEIF CFLAG:음ＮＴＲ진행도 == 2
					PRINTFORMW 
				ENDIF
			ENDIF
			CFLAG:289 = 1
		ENDIF
		;-------------------------------------------------
		;;[隷ＮＴＲ]이벤트時
		;-------------------------------------------------
	CASE 47
		CALL EVENT_MESSAGE
		;함락済み
		IF (CFLAG:290 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 == CFLAG:MASTER:캐릭터고유번호
				;主人に함락しているor誰にも함락してない상태でＮＴＲ
				;연모、친애
				IF TALENT:3 || TALENT:6
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;음란、창부
				ELSEIF TALENT:4 || TALENT:7
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;복종、예속
				ELSEIF TALENT:5 || TALENT:8
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				;그외
				ELSE
					;進行度分岐
					;初回
					IF CFLAG:예ＮＴＲ진행도 == 0
						PRINTFORMW 
					;二回目（リーチ）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 1
						PRINTFORMW 
					;三回目（함락）
					ELSEIF CFLAG:예ＮＴＲ진행도 == 2
						PRINTFORMW 
					ENDIF
				ENDIF
			ELSE
				;主人以外の誰かに함락している상태で更に別のキャラにＮＴＲ
				;進行度分岐
				;初回
				IF CFLAG:예ＮＴＲ진행도 == 0
					PRINTFORMW 
				;二回目（リーチ）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 1
					PRINTFORMW 
				;三回目（함락）
				ELSEIF CFLAG:예ＮＴＲ진행도 == 2
					PRINTFORMW 
				ENDIF
			ENDIF
			CFLAG:290 = 1
		ENDIF
		;-------------------------------------------------
		;;愛系再함락時
		;;主人に対して연모系함락している、또는一度愛系ＮＴＲ상태にされたキャラに再び함락させられる時
		;-------------------------------------------------
	CASE 48
		CALL EVENT_MESSAGE
		;再함락前に함락しているキャラ→CFLAG:함락캐릭터 で취득가능、이름は%NTRNAME(CFLAG:함락캐릭터)%
		;再함락先のキャラ→ FAVORITE_1_ID(TARGET)　で취득가능、이름は%NTRNAME(FAVORITE_1_ID(TARGET))%
		;やや煩雑になるのでLOCAL、あるいは#DIM等を사용して分かりやすくする事を권장します
		
		IF (CFLAG:291 < 1 || FLAG:7 == 2)
			;主人に対して함락し直し
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORMW 「%CSTR:12%・・・、역시、헤어질 수 없어」
			;그외のキャラ・ＮＰＣにＮＴＲし直し
			ELSE
				PRINTFORMW 
			ENDIF
			CFLAG:291 = 1
		ENDIF
		;-------------------------------------------------
		;;淫系再함락時
		;;主人に対して음란系함락している、또는一度淫系ＮＴＲ상태にされたキャラに再び함락させられる時
		;-------------------------------------------------
	CASE 49
		CALL EVENT_MESSAGE
		;再함락前に함락しているキャラ→CFLAG:함락캐릭터 で취득가능、이름は%NTRNAME(CFLAG:함락캐릭터)%
		;再함락先のキャラ→ FAVORITE_1_ID(TARGET)　で취득가능、이름は%NTRNAME(FAVORITE_1_ID(TARGET))%
		;やや煩雑になるのでLOCAL、あるいは#DIM等を사용して分かりやすくする事を권장します
					
		IF (CFLAG:292 < 1 || FLAG:7 == 2)
			;主人に対して함락し直し
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORMW 
			;그외のキャラ・ＮＰＣにＮＴＲし直し
			ELSE
				PRINTFORMW 
			ENDIF
			CFLAG:292 = 1
		ENDIF
		;-------------------------------------------------
		;;隷系再함락時
		;;主人に対して복종系함락している、또는一度隷系ＮＴＲ상태にされたキャラに再び함락させられる時
		;-------------------------------------------------
	CASE 50
		CALL EVENT_MESSAGE
		;再함락前に함락しているキャラ→CFLAG:함락캐릭터 で취득가능、이름は%NTRNAME(CFLAG:함락캐릭터)%
		;再함락先のキャラ→ FAVORITE_1_ID(TARGET)　で취득가능、이름は%NTRNAME(FAVORITE_1_ID(TARGET))%
		;やや煩雑になるのでLOCAL、あるいは#DIM等を사용して分かりやすくする事を권장します
					
		IF (CFLAG:293 < 1 || FLAG:7 == 2)
			;主人に対して함락し直し
			IF FAVORITE_1_ID(TARGET) == CFLAG:MASTER:캐릭터고유번호
				PRINTFORMW 
			;그외のキャラ・ＮＰＣにＮＴＲし直し
			ELSE
				PRINTFORMW 
			ENDIF
			CFLAG:293 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・乱交パーティ
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 51
		CALL EVENT_MESSAGE
		IF (CFLAG:294 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;以前연모ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:음란 || TALENT:창부
				;以前음란ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:복종 || TALENT:예속
				;以前복종ルートだった場合
				PRINTFORMW 
			ELSE
				PRINTFORMW
			ENDIF
			CFLAG:294 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・公園
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 52
		CALL EVENT_MESSAGE
		IF (CFLAG:295 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;以前연모ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:음란 || TALENT:창부
				;以前음란ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:복종 || TALENT:예속
				;以前복종ルートだった場合
				PRINTFORML 
				PRINTFORML 「아저씨♪아저씨의 자지、제 안에 넣어주세요。그리고、정자 잔뜩 싸줘、부・탁・해・요☆」
				PRINTFORMW 유키코는 부랑자 앞에서 가랑이를 벌려、졸라대고 있다
				PRINTFORMW 질내에서는 조금 전까지 유키코를 범했던 남자의 정자가 흘러나오고 있다
				PRINTFORML 
			ELSE
				PRINTFORMW
			ENDIF
			CFLAG:295 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・변기
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 53
		CALL EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;以前연모ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:음란 || TALENT:창부
				;以前음란ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:복종 || TALENT:예속
				;以前복종ルートだった場合
				PRINTFORML 
				PRINTFORML 「더、더 싸줘어・・이것만으론 임신 못하는 거야아・・」
				PRINTFORMW 유키코가 질내사정을 조르자、거기에 답하듯이 남자들이 일제히 모여들었다
				PRINTFORMW 남자들이 떠난 뒤에는、바란대로 몸속을 백탁으로 더럽혀、질에서 하염없이 정액을 흘려내고 있는 유키코만이 남아 있다
				PRINTFORML 
			ELSE
				PRINTFORMW
			ENDIF
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・夜の街
		;;淫ＮＴＲ상태（TALENT:ＮＴＲ == 2 か TALENT:ＮＴＲ == 5）である時のみ
		;-------------------------------------------------
	CASE 54
		CALL EVENT_MESSAGE
		IF (CFLAG:296 < 1 || FLAG:7 == 2)
			IF TALENT:연모 || TALENT:친애
				;以前연모ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:음란 || TALENT:창부
				;以前음란ルートだった場合
				PRINTFORMW 
			ELSEIF TALENT:복종 || TALENT:예속
				;以前복종ルートだった場合
				PRINTFORML 
				PRINTFORML 「응、좋아 생으로。엣？후훗、돈따위보다 이 안에 있는 것、저～언부 싸주면 기쁘겠는데♪」
				PRINTFORMW 유키코는 바지 너머로 남자의 페니스를 어루만지며 음란한 웃음을 띄웠다
				PRINTFORMW 그리고、남자가 권유하는대로 호텔로 들어갔다
				PRINTFORML 
			ELSE
				PRINTFORMW
			ENDIF
			CFLAG:296 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ前逢引
		;;ＮＴＲ상태になる前に逢引する時
		;-------------------------------------------------
	CASE 55
		CALL EVENT_MESSAGE
		IF (CFLAG:297 < 1 || FLAG:7 == 2)
			IF CFLAG:함락캐릭터 != 0
				;主人以外に함락している場合
				PRINTFORMW 
			ELSEIF TALENT:연모 || TALENT:친애
				;연모ルート
				PRINTFORMW 
			ELSEIF TALENT:음란 || TALENT:창부
				;음란ルート
				PRINTFORMW 
			ELSEIF TALENT:복종 || TALENT:예속
				;복종ルート
				PRINTFORMW 
			ELSE
				PRINTFORMW
			ENDIF
			CFLAG:297 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・ＮＴＲ後初逢引
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)になった直後の一日の종료後、一度だけ行う
		;別のキャラに함락する度に起こる。同じキャラに再度함락した場合は起こらない
		;-------------------------------------------------
	CASE 56
		CALL EVENT_MESSAGE
		IF (CFLAG:298 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORML 
				PRINTFORML 「저기、%CSTR:일인칭%…당신을 위해서라면 뭐든지 해、그러니까…말야♡」
				PRINTFORMW 유키코가 침대에 엎드려 누워 다리를 벌리자 %조사처리(NTRNAME(CFLAG:함락캐릭터),"가")% 덮쳤다
				PRINTFORML 「응읏응응응읏…앗하아아♡」
				PRINTFORML （하아♡역시 %CALLNAME:MASTER%보다…크다♡）
				PRINTFORMW （이렇게 기분 좋은 걸 알아버리면、이제…헤어질 수 없어어♡）
				PRINTFORML 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:298 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・デート
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;-------------------------------------------------
	CASE 57
		CALL EVENT_MESSAGE
		IF (CFLAG:299 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORMW 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:299 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・本番
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;女性であれば入れられる側、男性であれば入れる側で固定
		;-------------------------------------------------
	CASE 58
		CALL EVENT_MESSAGE
		IF (CFLAG:601 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORML 
				PRINTFORMW 「저기…%CSTR:일인칭%의 안…응읏…기분…좋아？」
				PRINTFORMW 「다행이다♪ %CSTR:일인칭%도 기분 좋아、%CALLNAME:MASTER%보다……읏！！응아아앗！？」
				PRINTFORML %조사처리(NTRNAME(CFLAG:함락캐릭터),"가")% 갑자기 허리를 세게 부딪힌다
				PRINTFORMW 「응읏하아♡미안…해애요…지금은、당신잇、제일입니다아♡읏아아아아아아아아♡」
				PRINTFORMW 큐키코의 말에 만족한 %조사처리(NTRNAME(CFLAG:함락캐릭터),"는")% 유키코의 질 안으로 정을 터뜨렸다
				PRINTFORML 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:601 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・자위見せ合い
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;愛ＮＴＲ상태の場合、노출증3か자위중독1のどちらかを満たしている事が조건になります。
		;-------------------------------------------------
	CASE 59
		CALL EVENT_MESSAGE
		IF (CFLAG:602 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORMW 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:602 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・애널
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;CFLAG:애널횟수が1이상なら入れられている、CFLAG:삽입횟수が1이상なら入れています。
		;両方立っている場合は、地の文では入れられている側が優先されます。
		;-------------------------------------------------
	CASE 60
		CALL EVENT_MESSAGE
		IF (CFLAG:603 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORMW 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:603 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외플레이
		;;愛ＮＴＲか隷ＮＴＲ상태(TALENT:ＮＴＲが 1 か 3 か 4 か 6)で１日の종료시に発生。
		;愛ＮＴＲの場合、노출증が4이상でないと発生しません
		;-------------------------------------------------
	CASE 61
		CALL EVENT_MESSAGE
		IF (CFLAG:604 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORMW 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:604 = 1
		ENDIF
		;-------------------------------------------------
		;;ＮＴＲ요바이・야외조교
		;;隷ＮＴＲ상태(TALENT:ＮＴＲが 3 か 6)でCFLAG:현ＮＴＲ진행도が6이상의 場合１日の종료시に発生。
		;-------------------------------------------------
	CASE 62
		CALL EVENT_MESSAGE
		IF (CFLAG:605 < 1 || FLAG:7 == 2)
			IF TALENT:ＮＴＲ == 1 || TALENT:ＮＴＲ == 4
				;愛ＮＴＲ
				PRINTFORMW 
			ELSEIF TALENT:ＮＴＲ == 3 || TALENT:ＮＴＲ == 6
				;隷ＮＴＲ
				PRINTFORMW 
			ENDIF
			CFLAG:605 = 1
		ENDIF
	CASEELSE
		CALL EVENT_MESSAGE
ENDSELECT
;-------------------------------------------------
;@KOJO_MESSAGE_ENDING関係(X1、X2をキャラ번호に置換する)
;-------------------------------------------------
;-------------------------------------------------
;조건설정
;-------------------------------------------------
@EVENTTURNEND_K4561
;特殊ハーレムエンド用조건판정構文샘플
;XX内に조건とするキャラのキャラ번호を入れると、好きな組み合わせのハーレムエンドを설정できる
;ハーレムエンドの조건を満たしたとき、ここの特殊ハーレムエンドの조건も満たしている場合、ハーレムエンドから派生する
;A = 0
;B = 0
;REPEAT CHARANUM
;	IF NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX
;		SIF TALENT:COUNT:6 || TALENT:COUNT:7 || TALENT:COUNT:8
;			A += 1
;		SIF TALENT:COUNT:3 || TALENT:COUNT:4 || TALENT:COUNT:5 || TALENT:COUNT:6 || TALENT:COUNT:7 || TALENT:COUNT:8
;			B += 1
;	ENDIF
;REND
;SIF A && A+B >= XXX
;	CFLAG:21 = 2
;１체エンド用分岐フラグ、１체エンドのある上位함락소질のみコメントアウトを해제する
;SIF TALENT:6
;	CFLAG:21 = 1
;SIF TALENT:7
;	CFLAG:21 = 1
;SIF TALENT:8
;	CFLAG:21 = 1
;-------------------------------------------------
;エンド本文
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_K4561
;--------------------------------------------------
;特定主人時に特殊キャラ구상を呼び出す(Y1を特定キャラ、X1を奴隷のキャラ번호に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_ENDING_PY1_K4561
;	RETURN 0
;ENDIF
;--------------------------------------------------
;연모１체エンド
;IF TALENT:6 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.1X2A (○○エンド)
;음란１체エンド
;ELSEIF TALENT:7 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.1X2B (○○エンド)
;복종１체エンド
;ELSEIF TALENT:8 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.1X2C (○○エンド)
;ENDIF

;特殊ハーレムエンド
;IF CFLAG:21 == 2
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.2X2 (○○エンド)
;ENDIF

;CALL GAME_CONTINUE
;RETURN 0


@ENEMY_BATTLE_EVENT_K4561, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;적でのデビルＣＯ－ＯＰ発動時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;적でのデビルＣＯ－ＯＰ참가時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF

@BATTLE_EVENT_K4561, ARG, ARG:1, ARG:2
IF TFLAG:전투이벤트 == 1
;아군パーティでのデビルＣＯ－ＯＰ発動時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃좋아！기가 꺾였어、공격해！
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF
IF TFLAG:전투이벤트 == 2
;아군パーティでのデビルＣＯ－ＯＰ참가時の台詞
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%"",72,LEFT%┃사라져 버리세요！
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
ENDIF


@BATTLE_MESSAGE_K4561,ARG,ARG:1 = 0

;選択したコマンドにより分岐させたい場合、IF文の조건にコマンドNoを記入すること
;IF CFLAG:ARG:입력행동 == 
;	PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;	PRINTFORML ┃%"",72,LEFT%┃
;	PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

;ELSEIF ARG:1 > 0
	;충성도が上昇する行動をとった場合に표시する구상

;	CALLFORM SKILL_NAME_{CFLAG:ARG:입력행동},ARG
;	;사용した스킬の이름をLOCALSに代入
;	LOCALS = %RESULTS%

;	SELECTCASE ARG:1
;		;물리데미지を与える行動で、영맹持ちの충성도が上昇
;		CASE GETNUM(TALENT,"영맹")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;마법데미지を与える行動で、교활持ちの충성도が上昇
;		CASE GETNUM(TALENT,"교활")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;高위력の行動で、잔학持ちの충성도が上昇
;		CASE GETNUM(TALENT,"잔학")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;相手の약점を付いた공격or行動不能な상태이상の적に対する공격で、현명持ちの충성도が上昇
;		CASE GETNUM(TALENT,"현명")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;파마相性의 行動で、천사持ちの충성도が上昇
;		CASE GETNUM(TALENT,"천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;주살相性의 行動で、타천사持ちの충성도が上昇
;		CASE GETNUM(TALENT,"타천사")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;회복・상태회복行動では우애持ちの충성도が上昇
;		CASE GETNUM(TALENT,"우애")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;상태변화では교만持ちの충성도が上昇
;		CASE GETNUM(TALENT,"교만")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;特殊では냉정持ちの충성도が上昇
;		CASE GETNUM(TALENT,"냉정")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;우둔持ちは主人とのLv差だけ충성도が上昇
;		CASE GETNUM(TALENT,"우둔")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;		;허심持ちは최저２だけ충성도が上昇
;		CASE GETNUM(TALENT,"허심")
;			PRINTFORML ┏>MESSAGE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
;			PRINTFORML ┃%LOCALS + "",72,LEFT%┃
;			PRINTFORMW ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
;	ENDSELECT
;ELSE
;	;システムによらず충성도を上げたいとか他があったらここに記載
;ENDIF

