;--------------------------------------------------
;세이브데이터互換処理
;--------------------------------------------------
@ISIMTERCHANGEABILITY_K4518
;의존도が0でない、かつ、バージョン0.1미만
;ちょっとでも조교してれば의존도が0であることはまずめったに無いだろうということで
;조교状況のみ白紙化가능
;引継ぎ処理考えたらいらないなぁという気分に
IF CFLAG:2 && CFLAG:501 < 10
;	PRINTL すでに作成された호시 아카리をRPG部分での능력そのままに
;	PRINTL 조교されていない状況に戻すことができます
;	PRINTL 初期化処理をしますか？
;	PRINTL [0] はい
;	PRINTL [1] いいえ
;	CALL INPUTINT(0,1)
;	IF RESULT == 0
;		CALL RESET_CHARA_TRAIN
;		NAME:TARGET = 소녀
;		CALLNAME:TARGET = 소녀
;		PRINTW 初期化処理を実行しました
;	ELSE
;		PRINTW 初期化処理をせずに進みます
;	ENDIF
ELSEIF CFLAG:501 < 20
	TALENT:일선을넘지않음 = 0
ELSEIF CFLAG:591 < 291
	CALL SET_EVENTFLAG(9, 1)
ENDIF
;--------------------------------------------------
;バージョン취득
;세이브데이터互換を切る場合に備える
;バージョンさえとっておけば、互換処理の作りやすさが段違いなので
;--------------------------------------------------
CFLAG:501 = 300
