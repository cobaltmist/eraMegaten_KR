;-------------------------------------------------
;@ENEMY_COUNT(ARG = 0)
;ARG 0.生存者のみ 0이외.死者も数える
;-------------------------------------------------
@ENEMY_COUNT(ARG = 0)
#FUNCTION
#LOCALSIZE 2
#LOCALSSIZE 1
LOCAL = 0
FOR LOCAL:1,0,10
	LOCALS = 포지션{LOCAL:1+7}
	SIF FLAG:LOCALS < 0
		CONTINUE
	SIF ARG == 0 && GET_STATE(CFLAG:(FLAG:LOCALS):ステート) == "DYING"
		CONTINUE
	LOCAL++
NEXT
RETURNF LOCAL

