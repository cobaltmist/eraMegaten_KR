;==========================================================
;そのキャラが최대でいくつ스킬を持てるのか調べる
;ARG 대상のキャラ
;ARG:1 0=人間時　1=今の状態時
;==========================================================
@CHARA_SKILLCOUNT(ARG,ARG:1 = 1)
#FUNCTION
SIF CFLAG:ARG:PTフラグ < 1 && CFLAG:ARG:ボスフラグ
	RETURNF 20
	SIF TALENT:ARG:페르소나구사자
	RETURNF FLAG:스킬数
SIF (TALENT:ARG:이능자 || TALENT:ARG:달인 || TALENT:ARG:인수라) && (!CFLAG:ARG:악마변신 || ARG:1 == 0)
	RETURNF FLAG:이능자스킬数
RETURNF FLAG:스킬数
