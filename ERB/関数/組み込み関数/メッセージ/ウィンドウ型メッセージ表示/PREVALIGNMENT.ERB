;-------------------
;前回のALIGNMENTを取得する
;ただし、設定は手動で行うのでSET_ALIGNMENTを経過してないと更新されないので注意
;-------------------
@PREVALIGNMENT, ARGS
#FUNCTIONS
#LOCALSSIZE 1
IF ARGS == ""
	SIF LOCALS == ""
		LOCALS = LEFT
	RETURNF LOCALS
ELSE
	LOCALS = %ARGS%
ENDIF
