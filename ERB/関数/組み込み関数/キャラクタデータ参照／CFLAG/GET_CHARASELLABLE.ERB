;-----------------------------
;매각가능かを取る関数
;ARG キャラ番号
;-----------------------------
@GET_CHARASELLABLE(ARG)
#FUNCTION
SIF ARG == MASTER
	RETURNF 0
SIF CFLAG:ARG:보스플래그
	RETURNF 0
SIF CFLAG:ARG:이자리에없는플래그 == 1
	RETURNF 0
;育児部屋に移動しているキャラは除外
SIF CHECK_CHILD_CARE(ARG) && FLAG:출산기능ONOFF == 1
	RETURNF 0
SIF CFLAG:ARG:노역플래그 == 3
	RETURNF 0
SIF CFLAG:ARG:매각가능 == 0
	RETURNF 0
SIF CFLAG:ARG:매각불가플래그
	RETURNF 0
RETURNF 1
