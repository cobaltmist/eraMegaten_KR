;=================================================================
;その他(戦闘関連以外)設定スイッチに関する関数郡
; FLAG:그외설정스위치 にBIT値で設定を記録する
;  0 = 外見・スリーサイズ機能設定
;  1 = 마카レート変動機能設定
;  2 = ベルベットルームでのステータスアップ画面設定

;=================================================================

;-----------------------------------------------------------------
;@SETTING_IS_3SIZE
;外見・スリーサイズ기능を이용するか설정を返す
;　引数　なし
; 戻り値 0=설정が무효 1=설정が有効
;-----------------------------------------------------------------
@SETTING_IS_3SIZE
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;설정が有効
	SIF GETBIT(FLAG:그외설정스위치, 0) == 1
		RETURNF 1
	;설정が무효
	RETURNF 0

;-----------------------------------------------------------------
;@SETTING_INVERT_3SIZE
;外見・スリーサイズ기능を이용するかの설정を行う、関数を呼び出すごとにON/OFF변경
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_INVERT_3SIZE
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:그외설정스위치, 0


;-----------------------------------------------------------------
;@SETTING_SET_3SIZE
;外見・スリーサイズ기능を이용するかの설정を行う
;　引数　ARG:0 0=설정が무효 1=설정が有効
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_SET_3SIZE, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:그외설정스위치, 0
	ELSE
		SETBIT FLAG:그외설정스위치, 0
	ENDIF

;-----------------------------------------------------------------
;@SETTING_IS_MAKKA_RATE
;마카환율変動기능を이용するか설정を返す
;　引数　なし
; 戻り値 0=설정が무효 1=설정が有効
;-----------------------------------------------------------------
@SETTING_IS_MAKKA_RATE
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;설정が有効
	SIF GETBIT(FLAG:그외설정스위치, 1) == 1
		RETURNF 1
	;설정が무효
	RETURNF 0

;-----------------------------------------------------------------
;@SETTING_INVERT_MAKKA_RATE
;마카환율変動기능を이용するかの설정を行う、関数を呼び出すごとにON/OFF변경
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_INVERT_MAKKA_RATE
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:그외설정스위치, 1


;-----------------------------------------------------------------
;@SETTING_SET_MAKKA_RATE
;마카환율変動기능を이용するかの설정を行う
;　引数　ARG:0 0=설정が무효 1=설정が有効
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_SET_MAKKA_RATE, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:그외설정스위치, 1
	ELSE
		SETBIT FLAG:그외설정스위치, 1
	ENDIF

;-----------------------------------------------------------------
;@SETTING_IS_VELVET_STATUS_UP
;벨벳룸での스테이터스アップ화면설정기능を利用するか설정を返す
;　引数　なし
; 戻り値 0=설정が無効 1=설정が有効
;-----------------------------------------------------------------
@SETTING_IS_VELVET_STATUS_UP
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;설정が有効
	SIF GETBIT(FLAG:그외설정스위치, 2) == 1
		RETURNF 1
	;설정が無効
	RETURNF 0

;-----------------------------------------------------------------
;@SETTING_INVERT_VELVET_STATUS_UP
;벨벳룸での스테이터스アップ화면설정기능を利用するかの설정を行う、関数を呼び出すごとにON/OFF변경
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_INVERT_VELVET_STATUS_UP
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:그외설정스위치, 2


;-----------------------------------------------------------------
;@SETTING_SET_VELVET_STATUS_UP
;벨벳룸での스테이터스アップ화면설정기능を利用するかの설정を行う
;　引数　ARG:0 0=설정が無効 1=설정が有効
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_SET_VELVET_STATUS_UP, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:그외설정스위치, 2
	ELSE
		SETBIT FLAG:그외설정스위치, 2
	ENDIF

[SKIPSTART]
;그외설정스위치のテンプレート
;  XXX　関数名
;  YYY　기능名
;  ZZZ　사용するBit
;-----------------------------------------------------------------
;@SETTING_IS_XXX
;YYY기능を利用するか설정を返す
;　引数　なし
; 戻り値 0=설정が無効 1=설정が有効
;-----------------------------------------------------------------
@SETTING_IS_XXX
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;설정が有効
	SIF GETBIT(FLAG:그외설정스위치, ZZZ) == 1
		RETURNF 1
	;설정が無効
	RETURNF 0

;-----------------------------------------------------------------
;@SETTING_INVERT_XXX
;YYY기능を利用するかの설정を行う、関数を呼び出すごとにON/OFF변경
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_INVERT_XXX
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:그외설정스위치, ZZZ


;-----------------------------------------------------------------
;@SETTING_SET_XXX
;YYY기능を利用するかの설정を行う
;　引数　ARG:0 0=설정が無効 1=설정が有効
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_SET_XXX, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:그외설정스위치, ZZZ
	ELSE
		SETBIT FLAG:그외설정스위치, ZZZ
	ENDIF
[SKIPEND]