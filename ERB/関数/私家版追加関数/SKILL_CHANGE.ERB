;==============================================================================================
;@SKILL_CHANGE
;キャラによって性能等が変化するスキルで、使用者が特定のキャラかどうかを判定する関数
;ARG    スキルを使用するキャラの登録番号
;ARG:1  判定したいキャラの固有番号。これと使用者の固有番号が同じなら1を返す
;==============================================================================================

@SKILL_CHANGE, ARG, ARG:1
#FUNCTION

IF ARG > -1
	IF GETCHARA(ARG:1) > -1
		IF NO:ARG == ARG:1
			RETURNF 1
		ELSE
			RETURNF 0
		ENDIF
	ELSE
		RETURNF 0
	ENDIF
ELSE
	RETURNF 0
ENDIF
