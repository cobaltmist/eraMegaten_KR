;-------------------------------------------------
;동값 체크 함수 @EQUALCHECK
;	인수0：체크시킬 대상
;	인수1～20：체크에 이용되는 수치
;식중에서 사용되는 함수입니다。인수0의 값이 주어진 값 중 하나와 같을 경우 1을 반환합니다
;인수에 0을 포함시킬 경우는、0을 마지막 인수로 하지 마십시오
;인수0은 체크할 대상、체크되는 수치는 인수1～20까지 감당 가능
;PANCTION로부터의 이식
;-------------------------------------------------
@EQUALCHECK(ARG, ARG:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6, ARG:7, ARG:8, ARG:9, ARG:10, ARG:11, ARG:12, ARG:13, ARG:14, ARG:15, ARG:16, ARG:17, ARG:18, ARG:19, ARG:20)
#FUNCTION
#LOCALSIZE 2
IF ARG
	FOR LOCAL:1, 1, 21
		SIF ARG == ARG:(LOCAL:1)
			RETURNF 1
	NEXT
ELSE
	;지정변수가 0인 경우에만 별도 거동
	LOCAL = 0
	FOR LOCAL:1, 0, 20
		SIF ARG:(20-(LOCAL:1)) && !LOCAL
			LOCAL = (LOCAL:1)
	NEXT
	FOR LOCAL:1, 1, 20-LOCAL
		SIF ARG == ARG:(LOCAL:1)
			RETURNF 1
	NEXT
ENDIF
RETURNF 0