;-------------------------------------------------
;문자 입력용 함수 @INPUT_CHAR
;인수 ARGS에 포함된 문자가 입력될 때까지 문자 입력을 기다립니다
;유효한 문자가 입력되면 그것을 RESULTS에 저장합니다
;전각문자는 지원하지 않습니다
;-------------------------------------------------
;ARG 값이 0이 아닐 경우, 리턴 키("")를 유효한 입력으로 간주합니다。
@INPUT_CHAR(ARGS,ARG = 0)
#LOCALSIZE 1
ONEINPUTS
SIF ARG != 0 && RESULTS == ""
	RETURN
FOR LOCAL,0,STRLENS(ARGS),1
	SIF RESULTS == SUBSTRING(ARGS,LOCAL,1)
		RETURN
NEXT
SIF RESULTS == ""
	RESTART
CLEARLINE 1
REUSELASTLINE 
RESTART