;-------------------------------------------------
;커닐링구스판정기본
;-------------------------------------------------
@PLAY_CUNNI,ARG,ARG:1
#FUNCTION
;性器が불결ているとMASTERの場合커닐링구스はできない（조교者が불결무시か애액、모유のよごれならＯＫ）
SIF DIRTY("바기나", ARG:1) && ARG == MASTER && TALENT:ARG:불결무시 == 0
	RETURNF 0
;性器が불결ており、악취민감で순종3以下のASSIの場合커닐링구스はできない（조교者が불결무시か애액、모유のよごれならＯＫ）
IF DIRTY("바기나", ARG:1) && ARG == ASSI
	SIF ABL:ARG:순종 <= 3 && TALENT:ARG:악취민감 && TALENT:ARG:불결무시 == 0
		RETURNF 0
ENDIF
SIF !USE_MOUTH(ARG)
	RETURNF 0

SIF !USE_CLI(ARG:1,1)
	RETURNF 0

RETURNF 1