;-------------------------------------------------
;フェラ판정기본
;ARGが行為者(ARGがARG:1の페니스を慰める)
;-------------------------------------------------
@PLAY_FELLA,ARG,ARG:1
#FUNCTION
;性器が불결ているとMASTERの場合フェラはできない（조교者が불결무시か애액、모유のよごれならＯＫ）
SIF DIRTY("페니스", ARG:1) && ARG == MASTER && TALENT:ARG:불결무시 == 0
	RETURNF 0
;性器が불결ており、악취민감で순종3以下のASSIの場合フェラはできない（조교者が불결무시か애액、모유のよごれならＯＫ）
IF DIRTY("페니스", ARG:1) && ARG == ASSI
	SIF ABL:ARG:순종 <= 3 && TALENT:ARG:악취민감 && TALENT:ARG:불결무시 == 0
		RETURNF 0
ENDIF
;고양이혀の조교者の場合は기교Lv2이상필요
SIF TALENT:ARG:고양이혀 && ABL:ARG:기교 < 3
	RETURNF 0
SIF !USE_MOUTH(ARG)
	RETURNF 0
SIF !USE_PENIS(ARG:1)
	RETURNF 0

RETURNF 1