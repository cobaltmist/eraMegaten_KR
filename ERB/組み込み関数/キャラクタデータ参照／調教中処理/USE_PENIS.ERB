;-------------------------------------------------
;페니스사용판정기본
;-------------------------------------------------
@USE_PENIS,ARG,ARG:1 = 0
#FUNCTION
;남자か후타나리じゃないとダメ & 비대음핵이 없으면 안됨
IF !HAVE_PENIS(ARG) && TALENT:ARG:비대음핵 == 0
	RETURNF 0
ENDIF

;Vずらし中ならOK
SIF TEQUIP:ARG:Vずらし中 == -1
	RETURNF 1

;下半身下着をつけている、스커트以外の下半身겉옷を履いている、下の空いてない全身겉옷を着ているとダメ
SIF TEQUIP:ARG:음순노출 != -1 && ((ARG:1 & 1 && (TEQUIP:ARG:Ｃ촉각 & 2)) || (ARG:1 & 2 && (TEQUIP:ARG:Ｃ촉각 & 4)) ) == 0
	RETURNF 0

;오나홀사용중はダメ
SIF ITEM_PENIS(ARG) > 0
	RETURNF 0
RETURNF 1