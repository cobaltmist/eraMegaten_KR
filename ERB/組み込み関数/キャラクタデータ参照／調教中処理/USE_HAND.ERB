;-------------------------------------------------
;손사용판정기본
;-------------------------------------------------
@USE_HAND,ARG
#FUNCTION
SIF !IS_HUMAN(ARG) && !TALENT:ARG:팔 && !TALENT:ARG:날개 
	RETURNF 0

;갈퀴、또는날개のみの조교자の場合は기교Lv2이상필요
SIF ( TALENT:ARG:갈퀴 || ( TALENT:ARG:날개 && !TALENT:ARG:팔 ) ) && ABL:ARG:2 < 2 && ARG == PLAYER
	RETURNF 0

SIF ITEM_HAND(ARG) > 0
	RETURNF 0

RETURNF 1