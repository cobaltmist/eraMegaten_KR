;===============================
;特定の스킬を、幾つ습득しているか
;습득するかどうかの판정に장비스킬まで見るのはどうかと思ったので…
;とりあえず保留
;==============================
@CHECK_SKILL_OVERLAP(ARG,ARG:1)
#FUNCTION
#LOCALSIZE 2
LOCAL = 0

LOCAL:1 = GETNUM(ABL , "스킬1")
LOCAL += MATCH(ABL:ARG:0, ARG:1, LOCAL:1, LOCAL:1 + CHARA_SKILLCOUNT(ARG) + 1)

;IF TALENT:ARG:이능자 || TALENT:ARG:달인 || TALENT:ARG:인수라
;	LOCAL += MATCH(ABL:ARG:0, ARG:1, LOCAL:1, LOCAL:1 + FLAG:이능자스킬수+1)
;ELSE
;	LOCAL += MATCH(ABL:ARG:0, ARG:1, LOCAL:1, LOCAL:1 + FLAG:스킬수+1)
;ENDIF
RETURNF LOCAL
