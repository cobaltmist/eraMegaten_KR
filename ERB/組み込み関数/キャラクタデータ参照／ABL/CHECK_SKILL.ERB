;==========================================================
;特定の스킬を습득しているかどうか
;ARG 대상のキャラ
;ARG:1 대상の스킬
;==========================================================
@CHECK_SKILL(ARG,ARG:1)
#FUNCTION
;ARGが스킬ARG:1を持っているかを参照する
;IF TALENT:ARG:이능자 || TALENT:ARG:달인 || TALENT:ARG:인수라
;	FOR LOCAL,1,FLAG:이능자스킬수+1
	LOCAL:1 = CHARA_SKILLCOUNT(ARG) + 1
	FOR LOCAL,1,LOCAL:1
		LOCALS = 스킬{LOCAL}
		SIF ABL:ARG:LOCALS == ARG:1
			RETURNF 1
	NEXT
;ELSE
;	FOR LOCAL,1,FLAG:스킬수+1
;		LOCALS = 스킬{LOCAL}
;		SIF ABL:ARG:LOCALS == ARG:1
;			RETURNF 1
;	NEXT
;ENDIF
RETURNF 0
