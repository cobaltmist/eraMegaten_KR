;-------------------
;今のALIGNMENTを취득する
;ただし、설정は손動で行うのでSET_ALIGNMENTを経過してないと更新されないので注意
;-------------------
@NOWALIGNMENT, ARGS
#FUNCTIONS
#LOCALSSIZE 1
IF ARGS == ""
	SIF LOCALS == ""
		LOCALS = LEFT
	RETURNF LOCALS
ELSE
	LOCALS = %ARGS%
ENDIF
