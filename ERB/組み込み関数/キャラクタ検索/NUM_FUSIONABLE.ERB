;===============================
;合体가능な仲魔（악마）の人数
;==============================
@NUM_FUSIONABLE()
#FUNCTION
LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	SIF ABL:LOCAL:종족 < 1 || ABL:LOCAL:종족 > 44
		CONTINUE
	SIF CFLAG:LOCAL:合体불가
		CONTINUE
	SIF CFLAG:LOCAL:この場に居ないフラグ
		CONTINUE
	SIF CFLAG:LOCAL:PTフラグ == 0
		CONTINUE
	LOCAL:1 += 1
NEXT
RETURNF LOCAL:1