;===============================
;ＣＯＭＰを사용가능な人数
;===============================
@NUM_SUMMONER(ARG = 3)
#FUNCTION
;서머너LvがARG이상의 人数を返す
SIF FLAG:COMP사용불능
	RETURNF 0
LOCAL:1 = 0
FOR LOCAL,1,7
	LOCALS = 포지션{LOCAL}
;	IF FLAG:LOCALS > -1 && TALENT:(FLAG:LOCALS):서머너 && (ARG == 1 || TALENT:(FLAG:LOCALS):이능자 == 0)
	IF FLAG:LOCALS > -1 && TALENT:(FLAG:LOCALS):서머너 >= ARG
		SELECTCASE GET_STATE(CFLAG:(FLAG:LOCALS):상태이상)
			CASE "DYING","STONE","FLY","PALYZE","BIND","CHARM","SLEEP","PANIC"
				CONTINUE
			CASEELSE
				LOCAL:1 += 1
		ENDSELECT
	ENDIF
NEXT
RETURNF LOCAL:1