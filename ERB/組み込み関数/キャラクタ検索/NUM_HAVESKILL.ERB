;===============================
;行動可能な特定スキル所有者人数カウント @NUM_HAVESKILL(ARG)
; @NUM_SUMMONER(ARG = 0)を改変流用
;===============================
@NUM_HAVESKILL(ARG)
#FUNCTION
LOCAL:1 = 0
FOR LOCAL, 1, 7
	LOCALS = 포지션{LOCAL}
	IF FLAG:LOCALS > -1 && HAVE_SKILL(FLAG:LOCALS, ARG)
		SELECTCASE GET_STATE(CFLAG:(FLAG:LOCALS):상태이상)
			CASE "DYING", "STONE", "FLY", "PALYZE", "BIND", "CHARM", "SLEEP", "PANIC"
				CONTINUE
			CASEELSE
				LOCAL:1 += 1
		ENDSELECT
	ENDIF
NEXT
RETURNF LOCAL:1