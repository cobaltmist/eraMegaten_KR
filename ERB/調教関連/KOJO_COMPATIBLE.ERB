;추가要素とかに対して구상で互換性をもたせられたら嬉しい関数
;主に EVENT_K.ERB @구상呼び出し での사용を想定

;====================================
;　구상呼び出し前処理
;　呼び出したいCOM번호に差し替える
;　引数1　: ARG   = 대상キャラ(東方系とか판정して互換したり
;
;　戻り値 : 差し替え前のCOM번호
;====================================
@KOJO_COMPATIBLE_BOFORE, ARG
	;역강간(애널)の場合
	IF SELECTCOM == 75
		SELECTCOM = 66
		LOCAL     = 75
	ENDIF

	RETURN LOCAL

;====================================
;구상呼び出し後処理
;구상以外の処理呼び出しため差し替えた번호を元に戻す
;　引数1　: ARG   = 구상キャラのNo
;　引数2　: ARG:1 = 差し替え대상のCOM번호
;
;　戻り値 : 特に意味なし
;====================================
@KOJO_COMPATIBLE_AFTER, ARG, ARG:1

	SELECTCOM = ARG:1

	RETURN 1
