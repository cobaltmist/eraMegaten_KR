;--------------------------------------------------------
@EVENT_COUNTER11
;見つめる
CALL EVENT_COUNTER_MESSAGE11
SOURCE:TARGET:정애 += 700
;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 4

;의존도보정値(연모で1、친애で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF
;--------------------------------------------------------
@EVENT_COUNTER12
;体を摺り寄せる
CALL EVENT_COUNTER_MESSAGE12
SOURCE:TARGET:복종 += 50
SOURCE:TARGET:3 += 100
;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 4

;의존도보정値(연모で1、친애で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF
;--------------------------------------------------------
@EVENT_COUNTER13
;耳元に息を吹きかける
CALL EVENT_COUNTER_MESSAGE13
SOURCE:TARGET:정애 += 500
SOURCE:TARGET:성행위 += 500
SOURCE:TARGET:일탈 += 100
;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 4

;의존도보정値(연모で1、친애で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF
;--------------------------------------------------------
@EVENT_COUNTER14		
;尻を撫でる
CALL EVENT_COUNTER_MESSAGE14
SOURCE:TARGET:정애 += 500
SOURCE:TARGET:성행위 += 600
SOURCE:TARGET:일탈 += 300
;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 4

;의존도보정値(연모で1、친애で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;--------------------------------------------------------
@EVENT_COUNTER30
;キス
CALL EVENT_COUNTER_MESSAGE30
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 10
SOURCE:TARGET:굴종 += 10
SOURCE:TARGET:일탈 += 10
IF ABL:키스중독
	SOURCE:TARGET:정애 += ABL:키스중독*10 + ABL:PLAYER:키스중독*10
	SOURCE:TARGET:성행위 += ABL:키스중독*20 + ABL:PLAYER:키스중독*10
ENDIF	
;奴隷の입⇔조교자の입의 불결が移動
CALL MOVE_STAIN("입", TARGET, "입", PLAYER)
A = 3
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF
;키스경험
CALL ADDS_EXP("키스경험", 1, TARGET)
CALL ADDS_EXP("키스경험", 1, PLAYER)

;애정경험
IF (TALENT:연모 || TALENT:친애) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

;의존도ベクトル
TFLAG:33 = 2

;의존도보정値(연모で1、친애で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;--------------------------------------------------------
@EVENT_COUNTER33
;가슴を揉ませる
CALL EVENT_COUNTER_MESSAGE33
;조교자→조교대상
TFLAG:28 = 2
SOURCE:TARGET:쾌Ｂ += 80
SOURCE:TARGET:정애 += 20
;奴隷のＢ⇔조교자の指의 불결が移動
CALL MOVE_STAIN("가슴", TARGET, "손", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:1+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:1+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 5
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 2
;--------------------------------------------------------
@EVENT_COUNTER36
;가슴もみもみ
CALL EVENT_COUNTER_MESSAGE36
;조교자←조교대상
TFLAG:28 = 1
SOURCE:PLAYER:쾌Ｂ += 80
SOURCE:PLAYER:정애 += 20
;奴隷のＢ⇔조교자の指의 불결が移動
CALL MOVE_STAIN("가슴", PLAYER, "손", target)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:1+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:1+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 5
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:30 = 2
;--------------------------------------------------------
@EVENT_COUNTER50
;수음
CALL EVENT_COUNTER_MESSAGE50
;조교대상→조교자
TFLAG:28 = 1
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
SOURCE:TARGET:일탈 += 100
;조교자に쾌Ｃ
SOURCE:PLAYER:0 += 80
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 3
;사정전선택の可・불가
TFLAG:704 = 1
;奴隷の指⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("손", TARGET, "페니스", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1
;強度
LOCAL = 500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER51
;입淫
CALL EVENT_COUNTER_MESSAGE51
;조교대상→조교자
TFLAG:28 = 1
TEQUIP:TARGET:입삽입자 = PLAYER
TFLAG:삽입속행 = 1
SOURCE:TARGET:정애 += 100
SOURCE:TARGET:성행위 += 300
SOURCE:TARGET:달성 += 150
SOURCE:TARGET:굴종 += 700
SOURCE:TARGET:일탈 += 200
;조교자に쾌Ｃ
SOURCE:PLAYER:0 += 120
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 4
;사정전선택の可・불가
TFLAG:704 = 1
;奴隷の입⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("입", TARGET, "페니스", PLAYER)

;봉사정신ＬＶ２이상、기교ＬＶ２이상なら汚れをなめ取る（볼재갈사용時は除く）
IF ABL:7 >= 2 && ABL:2 >= 2 && TEQUIP:20 == 0
	;STAIN:PLAYER:2 = 2
	;タイミングをずらす
	TFLAG:청소펠라발생 = 1
	TFLAG:8 = 1
ENDIF
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;펠라경험
;PRINTFORML %EXPNAME:22%＋１
CALL ADDS_EXP("펠라경험", 1, TARGET)

;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;強度
LOCAL = 800
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 20
;--------------------------------------------------------
@EVENT_COUNTER52
;스마타、貝合わせ
CALL EVENT_COUNTER_MESSAGE52
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:TARGET:쾌Ｃ += 200
SOURCE:TARGET:성행위 += 200
SOURCE:TARGET:달성 += 100
SOURCE:TARGET:노출 += 50
SOURCE:TARGET:굴종 += 400
SOURCE:TARGET:일탈 += 400
;조교자にも同様の쾌Ｃ
SOURCE:PLAYER:쾌Ｃ = 200
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 1
;奴隷のＶ⇔조교자のＰ의 불결が移動
IF HAVE_PENIS(PLAYER)
	CALL MOVE_STAIN("바기나", TARGET, "페니스", PLAYER)
ELSEIF HAVE_PENIS(TARGET) && HAVE_PENIS(PLAYER)
	CALL MOVE_STAIN("페니스", TARGET, "페니스", PLAYER)
ELSE
	CALL MOVE_STAIN("바기나", TARGET, "바기나", PLAYER)
ENDIF

IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;強度
LOCAL = 800
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 20
;--------------------------------------------------------
@EVENT_COUNTER53
;파이즈리
CALL EVENT_COUNTER_MESSAGE53
;조교대상→조교자
TFLAG:28 = 1
SOURCE:TARGET:쾌Ｂ += 150
SOURCE:TARGET:정애 += 150
SOURCE:TARGET:성행위 += 400
SOURCE:TARGET:달성 += 200
SOURCE:TARGET:굴종 += 1000
SOURCE:TARGET:일탈 += 400
;조교자に쾌Ｃ
SOURCE:PLAYER:0 += 150
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 5
;사정전선택の可・불가
TFLAG:704 = 1
;奴隷のＢ⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("가슴", TARGET, "페니스", PLAYER)

;봉사정신ＬＶ２이상、기교ＬＶ２이상なら汚れをなめ取る（볼재갈사용時は除く）
IF ABL:7 >= 2 && ABL:2 >= 2 && TEQUIP:20 == 0
	;STAIN:PLAYER:2 = 2
	;タイミングをずらす
	TFLAG:청소펠라발생 = 1
	TFLAG:8 = 1
ENDIF
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;強度
LOCAL = 1000
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;--------------------------------------------------------
@EVENT_COUNTER54
;발コキ
CALL EVENT_COUNTER_MESSAGE54
;조교대상→조교자
TFLAG:28 = 1
SOURCE:TARGET:성행위 += 700
SOURCE:TARGET:달성 += 500
SOURCE:TARGET:굴종 += 800
SOURCE:TARGET:일탈 += 400
;조교자に쾌Ｃ
SOURCE:PLAYER:0 += 80
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 1
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:6+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:6+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;強度
LOCAL = 500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER55
;애널애무
CALL EVENT_COUNTER_MESSAGE55
;조교자←조교대상
TFLAG:28 = 1
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
SOURCE:TARGET:일탈 += 100
SOURCE:PLAYER:2 += 100
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 3
;사정전선택の可・불가
TFLAG:704 = 1
;奴隷の指⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("손", TARGET, "애널", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
CALL ADDS_EXP("Ａ경험", 1, PLAYER)
A = 6
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 5

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;의존도보정値(복종で1、예속で2）
IF TALENT:5 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:8 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

LOCAL = 500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER56
;애널핥기
CALL EVENT_COUNTER_MESSAGE56
;조교자←조교대상
TFLAG:28 = 1
SOURCE:TARGET:성행위 += 300
SOURCE:TARGET:달성 += 300
SOURCE:TARGET:굴종 += 2500
SOURCE:TARGET:일탈 += 1200
;조교자に쾌Ａ
SOURCE:PLAYER:2 += 50
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 1
;奴隷の입⇔조교자のＡ의 불결が移動
CALL MOVE_STAIN("입", TARGET, "애널", PLAYER)
CALL ADDS_EXP("Ａ경험", 1, PLAYER)
A = 6
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 5

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;의존도보정値(복종で1、예속で2）
IF TALENT:5 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:8 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

LOCAL = 250
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER57
;指挿入
CALL EVENT_COUNTER_MESSAGE57
;조교대상→조교자
TFLAG:28 = 1
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
SOURCE:TARGET:일탈 += 100
SOURCE:PLAYER:1 += 120
;奴隷の指⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("손", TARGET, "질내", PLAYER)
;V경험
SIF TALENT:0 == 0 && TALENT:1 == 0
	CALL ADDS_EXP("Ｖ경험", 1, PLAYER)
;레즈경험
A = 1
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
IF TALENT:0
	TFLAG:33 = 3
ELSE
	TFLAG:33 = 1
ENDIF
SOURCE:PLAYER:액체 += 20
;--------------------------------------------------------
@EVENT_COUNTER58
;가슴애무
CALL EVENT_COUNTER_MESSAGE58
SOURCE:PLAYER:쾌Ｂ += 100
SOURCE:PLAYER:성행위 += 60
SOURCE:PLAYER:노출 += 25
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 60
SOURCE:TARGET:일탈 += 30
CALL MOVE_STAIN("손", TARGET, "가슴", PLAYER)
A = 5
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;조교자が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:PLAYER:54
	CALL ADDS_EXP("가학쾌락경험", 1, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 2
;--------------------------------------------------------
@EVENT_COUNTER59
;유두吸い
CALL EVENT_COUNTER_MESSAGE59
;조교대상→조교자
TFLAG:28 = 1
SOURCE:PLAYER:쾌Ｂ += 100
SOURCE:PLAYER:성행위 += 60
SOURCE:PLAYER:노출 += 25
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 60
SOURCE:TARGET:일탈 += 30

A = 6
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;의존도ベクトル
;P = PLAYER
IF GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:33 = 2
ELSEIF GET_RELATION(TARGET, PLAYER) < 100
	TFLAG:33 = 3
ELSE
	TFLAG:33 = 1
ENDIF

;의존도보정値(相性100↑かつ음란で2、창부で4）
;P = PLAYER
IF TALENT:4 && GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:30 += 2
ELSEIF TALENT:7 && GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:30 += 4
ENDIF


;봉사쾌락フラグ
TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER60
;ディープキス
CALL EVENT_COUNTER_MESSAGE60
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 10
SOURCE:TARGET:굴종 += 10
SOURCE:TARGET:일탈 += 10
IF ABL:키스중독
	SOURCE:TARGET:정애 += ABL:키스중독*10 + ABL:PLAYER:키스중독*10
	SOURCE:TARGET:성행위 += ABL:키스중독*20 + ABL:PLAYER:키스중독*10
ENDIF	
;奴隷の입⇔조교자の입의 불결が移動
CALL MOVE_STAIN("입", TARGET, "입", PLAYER)
A = 3
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF
;키스경험
CALL ADDS_EXP("키스경험", 1, TARGET)
CALL ADDS_EXP("키스경험", 1, PLAYER)

;애정경험
IF (TALENT:연모 || TALENT:친애) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

;의존도ベクトル
TFLAG:33 = 2

;의존도보정値(연모で1、친애で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF
;--------------------------------------------------------
@EVENT_COUNTER70
;커닐링구스강제
CALL EVENT_COUNTER_MESSAGE70
;조교대상→조교자
TFLAG:28 = 1
SOURCE:TARGET:성행위 += 300
SOURCE:TARGET:달성 += 150
SOURCE:TARGET:굴종 += 500
SOURCE:TARGET:일탈 += 200
;조교자の쾌Ｃ증가

SOURCE:PLAYER:쾌Ｃ += 100
;奴隷の입⇔조교자のＶ의 불결が移動
CALL MOVE_STAIN("입", TARGET, "바기나", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 6
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;의존도ベクトル
;P = PLAYER
IF GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:33 = 2
ELSEIF GET_RELATION(TARGET, PLAYER) < 100
	TFLAG:33 = 3
ELSE
	TFLAG:33 = 1
ENDIF

;의존도보정値(相性100↑かつ음란で2、창부で4）
;P = PLAYER
IF TALENT:4 && GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:30 += 2
ELSEIF TALENT:7 && GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:30 += 4
ENDIF


;봉사쾌락フラグ
TFLAG:100 = 1

LOCAL = 1000
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;--------------------------------------------------------
@EVENT_COUNTER71
;펠라치오強制
CALL EVENT_COUNTER_MESSAGE71
;조교자→조교대상
TFLAG:28 = 2
SOURCE:TARGET:쾌Ｃ += 120
SOURCE:TARGET:노출 += 20
SOURCE:TARGET:일탈 += 20
;奴隷のＰ⇔조교자の입의 불결が移動
CALL MOVE_STAIN("페니스", TARGET, "입", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:5+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:5+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 3
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;PRINTFORML 펠라경험(조교자) + 1
CALL ADDS_EXP("펠라경험", 1, PLAYER)

;조교자が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:PLAYER:54
	CALL ADDS_EXP("가학쾌락경험", 1, PLAYER)
ENDIF

;의존도ベクトル
IF TALENT:PLAYER:140 == 1
	TFLAG:33 = 3
ELSE
	TFLAG:33 = 2
ENDIF
;--------------------------------------------------------
@EVENT_COUNTER72
;애널봉사
CALL EVENT_COUNTER_MESSAGE72
;조교자→조교대상
TFLAG:28 = 2
SOURCE:TARGET:쾌Ａ += 50
SOURCE:TARGET:액체 += 50
SOURCE:TARGET:일탈 += 40
;奴隷のＡ⇔조교자の입의 불결が移動
CALL MOVE_STAIN("애널", TARGET, "입", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:5+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:5+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 3
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;조교자が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:PLAYER:54
	CALL ADDS_EXP("가학쾌락경험", 1, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 3

;--------------------------------------------------------
@EVENT_COUNTER74
;乳合わせ
CALL EVENT_COUNTER_MESSAGE74
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:TARGET:쾌Ｂ += 200
SOURCE:TARGET:성행위 += 150
SOURCE:TARGET:노출 += 50
SOURCE:TARGET:일탈 += 100
;조교자にも同様の쾌Ｂ
SOURCE:PLAYER:쾌Ｂ += 200
;奴隷の가슴⇔조교자の가슴의 불결が移動
CALL MOVE_STAIN("가슴", TARGET, "가슴", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:3+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:3+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;	PRINTFORML 봉사쾌락경험(조교자) + 1
	CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
;의존도ベクトル
;P = PLAYER
IF GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:33 = 2
ELSEIF GET_RELATION(TARGET, PLAYER) < 100
	TFLAG:33 = 3
ELSE
	TFLAG:33 = 1
ENDIF

;의존도보정値(相性100↑かつ음란で2、창부で4）
;P = PLAYER
IF TALENT:4 && GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:30 += 2
ELSEIF TALENT:7 && GET_RELATION(TARGET, PLAYER) > 100
	TFLAG:30 += 4
ENDIF
;--------------------------------------------------------
@EVENT_COUNTER75
;タワシ洗い
CALL EVENT_COUNTER_MESSAGE75
;조교대상→조교자
TFLAG:28 = 1
SOURCE:TARGET:쾌Ｃ += 200
SOURCE:TARGET:정애 += 150
SOURCE:TARGET:성행위 += 450
SOURCE:TARGET:달성 += 250
SOURCE:TARGET:굴종 += 1200
SOURCE:TARGET:일탈 += 600
;奴隷의 불결
RESET_STAIN TARGET
;조교자의 불결
RESET_STAIN PLAYER
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER80
;挿入される
IF TEQUIP:PLAYER:50 > 0 && TEQUIP:PLAYER:50 < 2
	TEQUIP:PLAYER:50 = 2
ELSE
	TEQUIP:PLAYER:50 = 1
ENDIF
CALL EVENT_COUNTER_MESSAGE80
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:0 += 400
SOURCE:5 += 500
SOURCE:14 += 1000
SOURCE:12 += 500
;조교자に쾌Ｖ
SOURCE:PLAYER:1 += 400
SIF (TALENT:PLAYER:0 == 1 || TALENT:PLAYER:1 == 1)
	TFLAG:31 = 1
	
;조교자が처녀か재생처녀（이상경험は一回のみ）
IF (TALENT:PLAYER:0 == 1 || TALENT:PLAYER:1 == 1)
	TIMES SOURCE:13 , 20.00
	TIMES SOURCE:14 , 3.00
	PRINTFORM ＜%CALLNAME:PLAYER%は
	SIF TALENT:PLAYER:1 == 1
		PRINT 再生
	PRINTL 처녀を失いました＞
	;TALENT:PLAYER:0 = 0
	;TALENT:PLAYER:1 = 0

	;이상경험추가
	SIF !IS_MALE(TARGET)
		CALL ADDS_ABNORMAL_EXP("처녀탈취（동성）", TARGET)
	SIF !IS_MALE(TARGET)
		CALL ADDS_ABNORMAL_EXP("처녀상실（동성）", PLAYER)
ENDIF
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 0
;질내に파과혈의 불결を付加
SIF TALENT:PLAYER:0 == 1 || TALENT:PLAYER:1 == 1
	CALL SET_STAIN("질내", "파과혈", PLAYER)
;奴隷の질내⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("질내", TARGET, "페니스", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;조교자のV경험
CALL ADDS_EXP("Ｖ경험", 1, PLAYER)

;PRINTFORML 봉사쾌락경험(조교자) + 1
;EXP:PLAYER:봉사쾌락경험 += 1
;PRINTFORML 성교경험(조교자) + 1
;EXP:PLAYER:성교경험 += 1
CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
CALL ADDS_EXP("성교경험", 1, PLAYER)
;수간경험
IF (TALENT:TARGET:짐승 || TALENT:TARGET:새) && !(TALENT:PLAYER:짐승 || TALENT:PLAYER:새)
	CALL ADDS_EXP("수간경험", 1, PLAYER)
	
ENDIF

;처녀で同性の場合이상경험
IF TALENT:PLAYER:처녀 && !IS_MALE(TARGET)
	CALL ADDS_ABNORMAL_EXP("처녀탈취（동성）", TARGET)
	CALL ADDS_ABNORMAL_EXP("처녀상실（동성）", PLAYER)
ENDIF
;근친상간の場合이상경험부여
IF 近親체크(PLAYER , TARGET)
	CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:0%）", PLAYER)
	CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:1%）", TARGET)
ENDIF
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:40%+{A}
;	PRINTFORML %EXPNAME:40%(조교자)+{A}
;	EXP:40 += A
;	EXP:PLAYER:40 += A
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:41%+{A}
;	PRINTFORML %EXPNAME:41%(조교자)+{A}
;	EXP:41 += A
;	EXP:PLAYER:41 += A
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	
	
ENDIF



;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER82
;후배위애널
IF TEQUIP:PLAYER:51 > 0 && TEQUIP:PLAYER:51 < 2
	TEQUIP:PLAYER:51 = 2
ELSE
	TEQUIP:PLAYER:51 = 1
ENDIF
CALL EVENT_COUNTER_MESSAGE82
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:0 += 650
SOURCE:5 += 500
SOURCE:6 += 1000
SOURCE:15 += 750
SOURCE:17 += 1500
;조교자に쾌Ａ
SOURCE:PLAYER:2 += 400
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 0
;조교자のＡ⇔奴隷のＰ의 불결が移動
CALL MOVE_STAIN("애널", PLAYER, "페니스", TARGET)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;조교자のＡ경험
CALL ADDS_EXP("Ａ경험", 1, PLAYER)

;PRINTFORML 봉사쾌락경험(조교자) + 1
;EXP:PLAYER:봉사쾌락경험 += 1
;PRINTFORML 성교경험(조교자) + 1
;EXP:PLAYER:성교경험 += 1
CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
CALL ADDS_EXP("성교경험", 1, PLAYER)
;수간경험
IF (TALENT:TARGET:짐승 || TALENT:TARGET:새) && !(TALENT:PLAYER:짐승 || TALENT:PLAYER:새)
	CALL ADDS_EXP("수간경험", 1, PLAYER)
	
ENDIF
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:40%+{A}
;	PRINTFORML %EXPNAME:40%(조교자)+{A}
;	EXP:40 += A
;	EXP:PLAYER:40 += A
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:41%+{A}
;	PRINTFORML %EXPNAME:41%(조교자)+{A}
;	EXP:41 += A
;	EXP:PLAYER:41 += A
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	
	
ENDIF

;의존도ベクトル
TFLAG:33 = 3

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

LOCAL = 1500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER83
;배면좌위애널
IF TEQUIP:PLAYER:51 > 0 && TEQUIP:PLAYER:51 < 2
	TEQUIP:PLAYER:51 = 2
ELSE
	TEQUIP:PLAYER:51 = 1
ENDIF
CALL EVENT_COUNTER_MESSAGE83
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:0 += 600
SOURCE:5 += 500
SOURCE:6 += 1000
SOURCE:15 += 750
SOURCE:17 += 1500
;조교자に쾌Ａ
SOURCE:PLAYER:0 += 350
SOURCE:PLAYER:2 += 350
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 0
;조교자のＡ⇔奴隷のＰ의 불결が移動
CALL MOVE_STAIN("애널", PLAYER, "페니스", TARGET)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;조교자のＡ경험
CALL ADDS_EXP("Ａ경험", 1, PLAYER)

;PRINTFORML 봉사쾌락경험(조교자) + 1
;EXP:PLAYER:봉사쾌락경험 += 1
;PRINTFORML 성교경험(조교자) + 1
;EXP:PLAYER:성교경험 += 1
CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
CALL ADDS_EXP("성교경험", 1, PLAYER)
;수간경험
IF (TALENT:TARGET:짐승 || TALENT:TARGET:새) && !(TALENT:PLAYER:짐승 || TALENT:PLAYER:새)
	CALL ADDS_EXP("수간경험", 1, PLAYER)
	
ENDIF
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:40%+{A}
;	PRINTFORML %EXPNAME:40%(조교자)+{A}
;	EXP:40 += A
;	EXP:PLAYER:40 += A
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:41%+{A}
;	PRINTFORML %EXPNAME:41%(조교자)+{A}
;	EXP:41 += A
;	EXP:PLAYER:41 += A
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	
	
ENDIF

;의존도ベクトル
TFLAG:33 = 3

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

LOCAL = 1500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER81
;기승위애널
IF TEQUIP:PLAYER:51 > 0 && TEQUIP:PLAYER:51 < 2
	TEQUIP:PLAYER:51 = 2
ELSE
	TEQUIP:PLAYER:51 = 1
ENDIF
CALL EVENT_COUNTER_MESSAGE81
;조교자←→조교대상
TFLAG:28 = 3
SOURCE:0 += 450
SOURCE:5 += 500
SOURCE:4 += 1000
SOURCE:17 += 500
SOURCE:15 += 750
;조교자に쾌Ａ
SOURCE:PLAYER:2 += 400
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 0
;조교자のＡ⇔奴隷のＰ의 불결が移動
CALL MOVE_STAIN("애널", PLAYER, "페니스", TARGET)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;조교자のＡ경험
CALL ADDS_EXP("Ａ경험", 1, PLAYER)

;PRINTFORML 봉사쾌락경험(조교자) + 1
;EXP:PLAYER:봉사쾌락경험 += 1
;PRINTFORML 성교경험(조교자) + 1
;EXP:PLAYER:성교경험 += 1
CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
CALL ADDS_EXP("성교경험", 1, PLAYER)
;수간경험
IF (TALENT:TARGET:짐승 || TALENT:TARGET:새) && !(TALENT:PLAYER:짐승 || TALENT:PLAYER:새)
	CALL ADDS_EXP("수간경험", 1, PLAYER)
	
ENDIF
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:40%+{A}
;	PRINTFORML %EXPNAME:40%(조교자)+{A}
;	EXP:40 += A
;	EXP:PLAYER:40 += A
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
;	PRINTFORML %EXPNAME:41%+{A}
;	PRINTFORML %EXPNAME:41%(조교자)+{A}
;	EXP:41 += A
;	EXP:PLAYER:41 += A
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	
	
ENDIF

;의존도ベクトル
TFLAG:33 = 3

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

LOCAL = 1500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:PLAYER:액체 += 10
;--------------------------------------------------------
@EVENT_COUNTER91
;기승위역강간
IF TEQUIP:TARGET:50 > 0 && TEQUIP:TARGET:50 < 2
	TEQUIP:TARGET:50 = 2
ELSE
	TEQUIP:TARGET:50 = 1
ENDIF
CALL EVENT_COUNTER_MESSAGE91
;V경험을수반하는커맨드のフラグ(처녀膜再生対応)
TFLAG:19 = 1
SIF (TALENT:3 || TALENT:6) && ASSIPLAY == 0 && EXP:0 == 0
	TFLAG:20 = 1
;처녀상실時の텍스트用処理
SIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 61 || PREVCOM == 66 || PREVCOM == 68 || PREVCOM == 69) && (TFLAG:전회조교자 == PLAYER) && TFLAG:31
	TFLAG:31 = 2

;조교자←→조교대상
TFLAG:28 = 3
SOURCE:1 += 500
SOURCE:TARGET:정애 += 250
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 250
SOURCE:TARGET:중독충족 += 350
SOURCE:TARGET:노출 += 120
SOURCE:TARGET:일탈 += 600
;처녀だった場合は、고통의 소스と反発의 소스を추가する
IF TALENT:0 == 1
	SOURCE:TARGET:아픔 += 2000
	SOURCE:TARGET:반감 += 3000
ENDIF

;재생처녀だった場合は、고통의 소스と反発의 소스を추가する
IF TALENT:1 == 1
	SOURCE:TARGET:아픔 += 500
	SOURCE:TARGET:반감 += 1000
ENDIF
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 1
;사정전선택の可・불가
TFLAG:704 = 1
;질내に파과혈의 불결を付加
SIF TALENT:0 == 1 || TALENT:1 == 1
	CALL SET_STAIN("질내", "파과혈", TARGET)
;奴隷の질내⇔조교자のＰ의 불결が移動
CALL MOVE_STAIN("질내", TARGET, "페니스", PLAYER)
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:10+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;V경험
CALL ADDS_EXP("Ｖ경험", 1, TARGET)

;성교경험
CALL ADDS_EXP("성교경험", 1, TARGET)
;PRINTS EXPNAME:4;
;PRINTL +1
;수간경험
IF (TALENT:PLAYER:짐승 || TALENT:PLAYER:새) && !(TALENT:TARGET:짐승 || TALENT:TARGET:새)
	CALL ADDS_EXP("수간경험", 1, TARGET)
ENDIF

;이상경험
IF TALENT:0 == 1
	SIF !IS_MALE(PLAYER)
		CALL ADDS_ABNORMAL_EXP("처녀상실（동성）", TARGET)
	SIF !IS_MALE(PLAYER)
		CALL ADDS_ABNORMAL_EXP("처녀탈취（동성）", PLAYER)
	CALL ADDS_ABNORMAL_EXP("처녀상실（기승위）", TARGET)
ENDIF
;근친상간の場合이상경험부여
IF 近親체크(TARGET , PLAYER)
	CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:0%）", TARGET)
	CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:1%）", PLAYER)
	
ENDIF
;	EXP:50 += 3
;	PRINTFORML %EXPNAME:50%＋３
;ELSEIF TALENT:0 == 1
;	EXP:50 += 2
;	PRINTFORML %EXPNAME:50%＋２
;ENDIF

;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;애정경험
IF TALENT:3 == 1 && ASSIPLAY == 0
	;PRINTS EXPNAME:23
	;PRINTL +2
	CALL ADDS_EXP("애정경험", 2, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ELSEIF TALENT:6 == 1 && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +4
	CALL ADDS_EXP("애정경험", 4, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(연모で2、친애で4）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 2
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 4
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1

LOCAL = 2000
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;--------------------------------------------------------
@EVENT_COUNTER500_1
;リアクション
CALL EVENT_COUNTER_MESSAGE500_1
;膣性交
SOURCE:TARGET:정애 += 300

LOCAL = 300
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;--------------------------------------------------------
@EVENT_COUNTER500_2
;だいしゅきホールド
CALL EVENT_COUNTER_MESSAGE500_2
LOCAL = 100
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 1000
;--------------------------------------------------------
@EVENT_COUNTER500_3
;性交見せつけ
CALL EVENT_COUNTER_MESSAGE500_3
LOCAL = 100
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 300
SOURCE:TARGET:성행위 += 300
SOURCE:TARGET:달성 += 300
SOURCE:TARGET:중독충족 += 400
SOURCE:TARGET:노출 += 200
SOURCE:TARGET:일탈 += 700
;--------------------------------------------------------
@EVENT_COUNTER502
;수음
CALL EVENT_COUNTER_MESSAGE502
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 3
;사정전선택の可・불가
TFLAG:704 = 1
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;強度
LOCAL = 200
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER503_1
;펠라치오
CALL EVENT_COUNTER_MESSAGE503_1
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 4
;사정전선택の可・불가
TFLAG:704 = 1
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;強度
LOCAL = 300
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;펠라경험
;PRINTFORML %EXPNAME:22%＋１
CALL ADDS_EXP("펠라경험", 1, TARGET)

;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;奴隷が고양이혀持ちの場合は、조교자に가학쾌락경험を추가
IF TALENT:TARGET:54
	CALL ADDS_EXP("가학쾌락경험", 1, TARGET)
;	PRINTFORML %EXPNAME:32%＋１
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER503_2
;ディープスロート
CALL EVENT_COUNTER_MESSAGE503_2
SOURCE:TARGET:정애 += 250
LOCAL = 500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;--------------------------------------------------------
@EVENT_COUNTER504_1
;파이즈리
CALL EVENT_COUNTER_MESSAGE504_1
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 5
;사정전선택の可・불가
TFLAG:704 = 1
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:7+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;強度
LOCAL = 300
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;레즈경험
A = 7
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;애정경험
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
;	PRINTS EXPNAME:23
;	PRINTL +1
	CALL ADDS_EXP("애정경험", 1, TARGET)
;	PRINTFORML 애정경험(主人)＋１
	CALL ADDS_EXP("애정경험", 1, MASTER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER504_2
;세로파이즈리
CALL EVENT_COUNTER_MESSAGE504_2
LOCAL = 300
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 200
SOURCE:TARGET:성행위 += 350
SOURCE:TARGET:달성 += 250
SOURCE:TARGET:굴종 += 1500
SOURCE:TARGET:일탈 += 800
;--------------------------------------------------------
@EVENT_COUNTER504_3
;빈유
CALL EVENT_COUNTER_MESSAGE504_3
LOCAL = 500
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 150
SOURCE:TARGET:성행위 += 400
SOURCE:TARGET:달성 += 200
SOURCE:TARGET:굴종 += 1000
SOURCE:TARGET:일탈 += 400
;--------------------------------------------------------
@EVENT_COUNTER505_1
;스마타
CALL EVENT_COUNTER_MESSAGE505_1
;사정先타입の설정
TCVAR:PLAYER:페니스사용대상 = TARGET
TCVAR:PLAYER:사정개소 = 14
;사정전선택の可・불가
TFLAG:704 = 1
IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
	LOCAL = (RAND:8+1) * BASE:LV
	CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
ENDIF
;強度
LOCAL = 300
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
;--------------------------------------------------------
@EVENT_COUNTER505_2
;수음추가
CALL EVENT_COUNTER_MESSAGE505_2
LOCAL = 300
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
;레즈경험
A = 4
IF IS_LESBIAN(TARGET,PLAYER)
	CALL ADDS_EXP("레즈경험", A, TARGET)
	CALL ADDS_EXP("레즈경험", A, PLAYER)
ELSEIF IS_GAY(TARGET,PLAYER)
	CALL ADDS_EXP("ＢＬ경험", A, TARGET)
	CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
ENDIF

;의존도ベクトル
TFLAG:33 = 1

;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF

;봉사쾌락フラグ
TFLAG:100 = 1
;--------------------------------------------------------
@EVENT_COUNTER600_1
;玉揉み
CALL EVENT_COUNTER_MESSAGE600_1
LOCAL = 200
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
SOURCE:TARGET:일탈 += 100
;--------------------------------------------------------
@EVENT_COUNTER600_2
;玉揉み
CALL EVENT_COUNTER_MESSAGE600_2
LOCAL = 200
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
SOURCE:TARGET:일탈 += 100
;--------------------------------------------------------
@EVENT_COUNTER601
;애널애무
CALL EVENT_COUNTER_MESSAGE601
LOCAL = 100
;再装填処理中は사정ゲージは20分の1に
SIF CFLAG:PLAYER:9 > 0
	LOCAL /= 20
SIF HAVE_PENIS(PLAYER)
	BASE:PLAYER:2 += LOCAL
SOURCE:TARGET:정애 += 50
SOURCE:TARGET:성행위 += 250
SOURCE:TARGET:달성 += 50
SOURCE:TARGET:굴종 += 350
SOURCE:TARGET:일탈 += 100
SOURCE:PLAYER:2 += 300



;기능未調整のためコメントアウト
;@DATUI_BOTTOM(ARG,ARG:1)
;SELECTCASE ARG:1
;	CASE 0
;		TCVAR:ARG:下半身탈의行動 = 1
;		EQUIP:PLAYER:下半身下着１ = 0
;		EQUIP:PLAYER:下半身下着２ = 0
;		EQUIP:PLAYER:下半身 겉옷１ = 0
;		EQUIP:PLAYER:下半身 겉옷２ = 0
;		EQUIP:PLAYER:스커트 = 0
;		EQUIP:PLAYER:ボ디스ーツ = 0
;		EQUIP:PLAYER:원피스 = 0
;		EQUIP:PLAYER:着物 = 0
;		EQUIP:PLAYER:레오타드 = 0
;	CASE 1
;		TCVAR:ARG:下半身탈의行動 = 2
;		EQUIP:PLAYER:下半身下着１ = 0
;		EQUIP:PLAYER:下半身下着２ = 0
;		EQUIP:PLAYER:下半身 겉옷１ = 0
;		EQUIP:PLAYER:下半身 겉옷２ = 0
;		EQUIP:PLAYER:ボ디스ーツ = 0
;		EQUIP:PLAYER:레오타드 = 0
;	CASE 2
;		TCVAR:ARG:下半身탈의行動 = 3
;		EQUIP:PLAYER:下半身下着１ = 0
;		EQUIP:PLAYER:下半身 겉옷１ = 0
;		EQUIP:PLAYER:ボ디스ーツ = 0
;ENDSELECT
;
;@DATUI_BOTTOM_T(ARG,ARG:1)
;SELECTCASE ARG:1
;	CASE 0
;		TCVAR:ARG:TARGET自身の下半身탈의行動 = 1
;		EQUIP:ARG:下半身下着１ = 0
;		EQUIP:ARG:下半身下着２ = 0
;		EQUIP:ARG:下半身 겉옷１ = 0
;		EQUIP:ARG:下半身 겉옷２ = 0
;		EQUIP:ARG:스커트 = 0
;		EQUIP:ARG:ボ디스ーツ = 0
;		EQUIP:ARG:원피스 = 0
;		EQUIP:ARG:着物 = 0
;		EQUIP:ARG:레오타드 = 0
;	CASE 1
;		TCVAR:ARG:TARGET自身の下半身탈의行動 = 2
;		EQUIP:ARG:下半身下着１ = 0
;		EQUIP:ARG:下半身下着２ = 0
;		EQUIP:ARG:下半身 겉옷１ = 0
;		EQUIP:ARG:下半身 겉옷２ = 0
;		EQUIP:ARG:ボ디스ーツ = 0
;		EQUIP:ARG:레오타드 = 0
;	CASE 2
;		TCVAR:ARG:TARGET自身の下半身탈의行動 = 3
;		EQUIP:ARG:下半身下着１ = 0
;		EQUIP:ARG:下半身 겉옷１ = 0
;		EQUIP:ARG:ボ디스ーツ = 0
;ENDSELECT
;
;
;@DATUI_TOP(ARG,ARG:1)
;SELECTCASE ARG:1
;	CASE 0
;		TCVAR:ARG:上半身탈의行動 = 1
;		EQUIP:PLAYER:上半身下着１ = 0
;		EQUIP:PLAYER:上半身下着２ = 0
;		EQUIP:PLAYER:上半身 겉옷１ = 0
;		EQUIP:PLAYER:上半身 겉옷２ = 0
;		EQUIP:PLAYER:ボ디스ーツ = 0
;		EQUIP:PLAYER:원피스 = 0
;		EQUIP:PLAYER:着物 = 0
;		EQUIP:PLAYER:레오타드 = 0
;	CASE 1
;		TCVAR:ARG:上半身탈의行動 = 2
;		EQUIP:PLAYER:上半身下着１ = 0
;		EQUIP:PLAYER:上半身 겉옷１ = 0
;		EQUIP:PLAYER:ボ디스ーツ = 0
;		EQUIP:PLAYER:원피스 = 0
;		EQUIP:PLAYER:레오타드 = 0
;ENDSELECT
;
;@DATUI_TOP_T(ARG,ARG:1)
;SELECTCASE ARG:1
;	CASE 0
;		TCVAR:ARG:TARGET自身の上半身탈의行動 = 1
;		EQUIP:ARG:上半身下着１ = 0
;		EQUIP:ARG:上半身下着２ = 0
;		EQUIP:ARG:上半身 겉옷１ = 0
;		EQUIP:ARG:上半身 겉옷２ = 0
;		EQUIP:ARG:ボ디스ーツ = 0
;		EQUIP:ARG:원피스 = 0
;		EQUIP:ARG:着物 = 0
;		EQUIP:ARG:레오타드 = 0
;	CASE 1
;		TCVAR:ARG:TARGET自身の上半身탈의行動 = 2
;		EQUIP:ARG:上半身下着１ = 0
;		EQUIP:ARG:上半身 겉옷１ = 0
;		EQUIP:ARG:ボ디스ーツ = 0
;		EQUIP:ARG:원피스 = 0
;		EQUIP:ARG:레오타드 = 0
;ENDSELECT


