;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Modification Data:
;
;	Edit	Date			Author					Reason
;	001		2021/01/28		eMさ者					EQUIP_P_INSERT_V_TARGETの処理を変更
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;挿入継続の処理
;TRACHECKとセットで機能しています
;----------------------------------------------------------------------
@EQUIP_P_INSERT_V_TARGET
;----------------------------------------------------------------------
;IF TEQUIP:TARGET:50 > 0 && TEQUIP:TARGET:50 < 2
;TEQUIP50が3と4でで経験処理、1(挿入なしの状態でV挿入）、2(V挿入中にもう一度V挿入コマンド実行時）は処理しない。1,2のときはELSEIFで3,4に変更し、次回以降のV挿入コマンド以外のときに経験処理する
IF TEQUIP:TARGET:50 > 2
	;조교者←→조교대상
	TFLAG:28 = 3
	B += 750

	;再装填処理中は사정ゲージは20分の1に
	SIF CFLAG:PLAYER:9 > 0
		B /= 20

	SIF HAVE_PENIS(PLAYER)
		BASE:PLAYER:2 += B

	DOWNBASE:TARGET:체력 += 25
	DOWNBASE:TARGET:기력 += 50

	SOURCE:1 += 200
	SOURCE:TARGET:정애 += 75
	SOURCE:TARGET:노출 += 25

	;조교者に쾌Ｃ
	SOURCE:PLAYER:0 += 100

	;억압、저항持ちには、억울소스추가
	SIF TALENT:32 || TALENT:34
		SOURCE:TARGET:울굴 += 150

	LOCAL = 250
	LOCAL:1 = 150
	LOCAL:2 = 100

	;정조관념持ちで연모、친애持ち이외には常に反発의 소스を추가する（섹스系のみ）
	SIF TALENT:30 && !IS_LOVER(TARGET)
		SOURCE:TARGET:반감 += 500

	;Vへの고통は先に処理しておく
	;EXP:Ｖ경험をみる（처녀の반감は別途処理済みなので생략）
	IF EXP:0 < EXPLV:1
		TIMES LOCAL , 2.50
	ELSEIF EXP:0 < EXPLV:2
		TIMES LOCAL , 0.80
	ELSEIF EXP:0 < EXPLV:3
		TIMES LOCAL , 0.40
	ELSEIF EXP:0 < EXPLV:4
		TIMES LOCAL , 0.10
	ELSEIF EXP:0 < EXPLV:5
		TIMES LOCAL , 0.00
	ELSEIF EXP:0 >= EXPLV:5
		TIMES LOCAL , 0.00
	ENDIF

	;PALAM:윤활をみる
	IF PALAM:4 < PALAMLV:1
		TIMES LOCAL , 1.20
		TIMES LOCAL:1 , 1.50
	ELSEIF PALAM:4 < PALAMLV:2
		TIMES LOCAL , 0.70
		TIMES LOCAL:1 , 1.20
	ELSEIF PALAM:4 < PALAMLV:3
		TIMES LOCAL , 0.40
	ELSEIF PALAM:4 < PALAMLV:4
		TIMES LOCAL , 0.10
	ELSEIF PALAM:4 >= PALAMLV:4
		TIMES LOCAL , 0.10
	ENDIF

	;중독による중독충족의 소스は先に処理しておく
	;섹스중독を見る
	IF ABL:18 == 0
		TIMES LOCAL:2 , 0.00
	ELSEIF ABL:18 == 1
		TIMES LOCAL:2 , 0.30
	ELSEIF ABL:18 == 2
		TIMES LOCAL:2 , 0.60
	ELSEIF ABL:18 == 3
		TIMES LOCAL:2 , 0.90
	ELSEIF ABL:18 == 4
		TIMES LOCAL:2 , 1.20
	ELSEIF ABL:18 == 5
		TIMES LOCAL:2 , 1.50
	ELSEIF ABL:18 == 6
		TIMES LOCAL:2 , 1.80
	ELSEIF ABL:18 == 7
		TIMES LOCAL:2 , 2.10
	ELSEIF ABL:18 == 8
		TIMES LOCAL:2 , 2.40
	ELSEIF ABL:18 == 9
		TIMES LOCAL:2 , 3.00
	ELSEIF ABL:18 >= 10
		TIMES LOCAL:2 , 5.00
	ENDIF

	SOURCE:TARGET:아픔 += LOCAL
	SOURCE:TARGET:반감 += LOCAL:1
	SOURCE:TARGET:중독충족 += LOCAL:2

	;-------------------------------------------------
	;사정체크
	;-------------------------------------------------

	;사정先타입の설정
	TCVAR:PLAYER:페니스사용대상 = TARGET
	TCVAR:PLAYER:사정개소 = 1
	;사정전선택の可・불가
	TFLAG:704 = 1
	;-------------------------------------------------
	;汚れの処理
	;-------------------------------------------------
	;사정に関係なく行われる処理はここから
	;질내に파과혈의 불결を付加
	SIF TALENT:0 == 1 || TALENT:1 == 1
		CALL SET_STAIN("질내", "파과혈", TARGET)
	;奴隷の질내⇔조교者のＰ의 불결が移動
	CALL MOVE_STAIN("질내", TARGET, "페니스", PLAYER)

	;-------------------------------------------------
	;吸精処理
	;-------------------------------------------------
	IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
	ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
	ENDIF


	;경험の処理
	;-------------------------------------------------
	;V경험
	CALL ADDS_EXP("Ｖ경험", 1, TARGET)

	;성교경험
	CALL ADDS_EXP("성교경험", 1, TARGET)
	;PRINTS EXPNAME:4;
	;PRINTL +1
	;수간경험
	IF (TALENT:PLAYER:짐승 || TALENT:PLAYER:새) && !(TALENT:TARGET:짐승 || TALENT:TARGET:새)
		CALL ADDS_EXP("수간경험", 1, TARGET)
	ENDIF

	;처녀で同性の場合이상경험
	IF TALENT:TARGET:처녀
		SIF !IS_MALE(PLAYER) 
			CALL ADDS_ABNORMAL_EXP("처녀탈취（동성）", PLAYER)
		SIF !IS_MALE(PLAYER)
			CALL ADDS_ABNORMAL_EXP("처녀상실（동성）", TARGET)
	ENDIF

	;근친상간の場合이상경험부여
	IF 近親체크(TARGET , PLAYER)
		CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:0%）", TARGET)
		CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:1%）", PLAYER)
	ENDIF

	;레즈경험
	A = 4
	IF IS_LESBIAN(TARGET,PLAYER)
		CALL ADDS_EXP("레즈경험", A, TARGET)
		CALL ADDS_EXP("레즈경험", A, PLAYER)
	ELSEIF IS_GAY(TARGET,PLAYER)
		CALL ADDS_EXP("ＢＬ경험", A, TARGET)
		CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	ENDIF

	;애정경험
	IF TALENT:3 == 1 && ASSIPLAY == 0
	;	PRINTS EXPNAME:23
	;	PRINTL +2
		CALL ADDS_EXP("애정경험", 2, TARGET)
	;	PRINTFORML 애정경험(主人)＋１
		CALL ADDS_EXP("애정경험", 1, MASTER)
	;	PRINTFORML 봉사쾌락경험(조교者) + 1
		CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
	ELSEIF TALENT:6 == 1 && ASSIPLAY == 0
	;	PRINTS EXPNAME:23
	;	PRINTL +4
	;	EXP:23 += 4
		CALL ADDS_EXP("애정경험", 4, TARGET)
	;	PRINTFORML 애정경험(主人)＋１
		CALL ADDS_EXP("애정경험", 1, MASTER)
	;	PRINTFORML 봉사쾌락경험(조교者) + 1
		CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
	ENDIF

	;의존도ベクトル
	TFLAG:33 = 1

	;의존도보정値(연모で1、친애で2）
	IF TALENT:3 && ASSIPLAY == 0
		TFLAG:30 += 1
	ELSEIF TALENT:6 && ASSIPLAY == 0
		TFLAG:30 += 2
	ENDIF
	

;(V挿入コマンドの地の文で挿入なし→挿入の分岐用）
ELSEIF TEQUIP:TARGET:50 == 1
	TEQUIP:TARGET:50 = 3
;(V挿入コマンドの地の文で挿入継続時に挿入を続ける文の分岐用)
ELSEIF TEQUIP:TARGET:50 == 2
	TEQUIP:TARGET:50 = 4
ENDIF	
PRINTFORML ＜%CALLNAME:TARGET%にＶ挿入中＞


;----------------------------------------------------------------------
@EQUIP_P_INSERT_A_TARGET
;----------------------------------------------------------------------
IF TEQUIP:TARGET:51 > 0 && TEQUIP:TARGET:51 < 2

	B += 750
	;再装填処理中は사정ゲージは20分の1に
	SIF CFLAG:PLAYER:9 > 0
		B /= 20

	SIF HAVE_PENIS(PLAYER)
		BASE:PLAYER:2 += B

	;조교者←→조교대상
	TFLAG:28 = 3

	;-------------------------------------------------
	;소스추가処理
	;-------------------------------------------------
	;소비체력、기력
	DOWNBASE:TARGET:체력 += 40
	DOWNBASE:TARGET:기력 += 60

	SOURCE:TARGET:쾌Ａ += 200
	SOURCE:TARGET:정애 += 50
	SOURCE:TARGET:노출 += 30
	SOURCE:TARGET:일탈 += 100

	;조교者に쾌Ｃ
	SOURCE:PLAYER:0 += 300
	;-------------------------------------------------
	;特殊소스処理
	;-------------------------------------------------
	;억압、저항持ちには、억울소스추가
	SIF TALENT:32 || TALENT:34
		SOURCE:TARGET:울굴 += 250

	LOCAL = 400
	LOCAL:1 = 250
	LOCAL:2 = 100

	;정조관념持ちで연모、친애持ち이외には常に反発의 소스を추가する（섹스系のみ）
	SIF TALENT:30 && !IS_LOVER(TARGET)
		SOURCE:TARGET:반감 += 500

	;Vへの고통は先に処理しておく
	;EXP:Ｖ경험をみる（처녀の반감は別途処理済みなので생략）
	IF EXP:0 < EXPLV:1
		TIMES LOCAL , 2.50
	ELSEIF EXP:0 < EXPLV:2
		TIMES LOCAL , 0.80
	ELSEIF EXP:0 < EXPLV:3
		TIMES LOCAL , 0.40
	ELSEIF EXP:0 < EXPLV:4
		TIMES LOCAL , 0.10
	ELSEIF EXP:0 < EXPLV:5
		TIMES LOCAL , 0.00
	ELSEIF EXP:0 >= EXPLV:5
		TIMES LOCAL , 0.00
	ENDIF

	;PALAM:윤활をみる
	IF PALAM:4 < PALAMLV:1
		TIMES LOCAL , 1.20
		TIMES LOCAL:1 , 1.50
	ELSEIF PALAM:4 < PALAMLV:2
		TIMES LOCAL , 0.70
		TIMES LOCAL:1 , 1.20
	ELSEIF PALAM:4 < PALAMLV:3
		TIMES LOCAL , 0.40
	ELSEIF PALAM:4 < PALAMLV:4
		TIMES LOCAL , 0.10
	ELSEIF PALAM:4 >= PALAMLV:4
		TIMES LOCAL , 0.10
	ENDIF

	;중독による중독충족의 소스は先に処理しておく
	;섹스중독を見る
	IF ABL:18 == 0
		TIMES LOCAL:2 , 0.00
	ELSEIF ABL:18 == 1
		TIMES LOCAL:2 , 0.30
	ELSEIF ABL:18 == 2
		TIMES LOCAL:2 , 0.60
	ELSEIF ABL:18 == 3
		TIMES LOCAL:2 , 0.90
	ELSEIF ABL:18 == 4
		TIMES LOCAL:2 , 1.20
	ELSEIF ABL:18 == 5
		TIMES LOCAL:2 , 1.50
	ELSEIF ABL:18 == 6
		TIMES LOCAL:2 , 1.80
	ELSEIF ABL:18 == 7
		TIMES LOCAL:2 , 2.10
	ELSEIF ABL:18 == 8
		TIMES LOCAL:2 , 2.40
	ELSEIF ABL:18 == 9
		TIMES LOCAL:2 , 3.00
	ELSEIF ABL:18 >= 10
		TIMES LOCAL:2 , 5.00
	ENDIF

	SOURCE:TARGET:아픔 += LOCAL
	SOURCE:TARGET:반감 += LOCAL:1
	SOURCE:TARGET:중독충족 += LOCAL:2

	;-------------------------------------------------
	;사정체크
	;-------------------------------------------------
	;사정先타입の설정
	TCVAR:PLAYER:페니스사용대상 = TARGET
	TCVAR:PLAYER:사정개소 = 2
	;사정전선택の可・불가
	TFLAG:704 = 1

	;-------------------------------------------------
	;汚れの処理
	;-------------------------------------------------
	;사정に関係なく行われる処理はここから
	;奴隷のＡ⇔조교者のＰ의 불결が移動
	CALL MOVE_STAIN("애널", TARGET, "페니스", PLAYER)

	;-------------------------------------------------
	;吸精処理
	;-------------------------------------------------
	IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
	ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
	ENDIF

	;-------------------------------------------------
	;경험の処理
	;-------------------------------------------------
	;A경험
	CALL ADDS_EXP("Ａ경험", 2, TARGET)
	;PRINTL Ａ경험＋２

	;성교경험
	CALL ADDS_EXP("성교경험", 1, TARGET)
	;PRINTS EXPNAME:4;
	;PRINTL +1
	;수간경험
	IF (TALENT:PLAYER:짐승 || TALENT:PLAYER:새) && !(TALENT:TARGET:짐승 || TALENT:TARGET:새)
		CALL ADDS_EXP("수간경험", 1, TARGET)
	ENDIF

	;레즈경험
	A = 4
	IF IS_LESBIAN(TARGET,PLAYER)
		CALL ADDS_EXP("레즈경험", A, TARGET)
		CALL ADDS_EXP("레즈경험", A, PLAYER)
	ELSEIF IS_GAY(TARGET,PLAYER)
		CALL ADDS_EXP("ＢＬ경험", A, TARGET)
		CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	ENDIF

	;애정경험
	IF TALENT:3 == 1 && ASSIPLAY == 0
	;	PRINTS EXPNAME:23
	;	PRINTL +1
		CALL ADDS_EXP("애정경험", 1, TARGET)
	;	PRINTFORML 애정경험(主人)＋１
		CALL ADDS_EXP("애정경험", 1, MASTER)
	;	PRINTFORML 봉사쾌락경험(조교者) + 1
		CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
	ELSEIF TALENT:6 == 1 && ASSIPLAY == 0
	;	PRINTS EXPNAME:23
	;	PRINTL +2
		CALL ADDS_EXP("애정경험", 2, TARGET)
	;	PRINTFORML 애정경험(主人)＋１
		CALL ADDS_EXP("애정경험", 1, MASTER)
	;	PRINTFORML 봉사쾌락경험(조교者) + 1
		CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
	ENDIF

	;의존도ベクトル
	TFLAG:33 = 1

	;의존도보정値(복종で1、예속で2）
	IF TALENT:5 && ASSIPLAY == 0
		TFLAG:30 += 1
	ELSEIF TALENT:8 && ASSIPLAY == 0
		TFLAG:30 += 2
	ENDIF
ENDIF
PRINTFORML ＜%CALLNAME:TARGET%にＡ挿入中＞


;----------------------------------------------------------------------
@EQUIP_F_INSERT_V_TARGET
;----------------------------------------------------------------------
IF TEQUIP:TARGET:52 > 0 && TEQUIP:TARGET:52 < 2
	;조교者→조교대상
	TFLAG:28 = 2

	;-------------------------------------------------
	;소스추가処理
	;-------------------------------------------------
	;소비체력、기력
	DOWNBASE:TARGET:체력 += 15
	DOWNBASE:TARGET:기력 += 40


	IF TALENT:0 == 0 && TALENT:1 == 0
		SOURCE:1 += 60
		SOURCE:TARGET:노출 += 5
		SOURCE:TARGET:일탈 += 10
		LOCAL = 7
		LOCAL:1 = 10
	;V경험が0の時はV경험を伴わない物に派生
	;V의 구슬は入らないが、快Vが高いと慣らしてあるため처녀상실時に影響
	ELSE
		SIF PALAM:1 < 100
			SOURCE:1 += 30
		SOURCE:TARGET:노출 += 15
		SOURCE:TARGET:일탈 += 15
		LOCAL = 5
		LOCAL:1 += 15
	ENDIF


	;-------------------------------------------------
	;特殊소스処理
	;-------------------------------------------------
	;억압、저항持ちには、억울소스추가
	IF TALENT:32 || TALENT:34
		SOURCE:TARGET:울굴 += 10
		;처녀
		SIF TALENT:0
			SOURCE:TARGET:울굴 += 5
	ENDIF

	;Vへの고통は先に処理しておく
	;EXP:Ｖ경험をみる（처녀の반감は別途処理済みなので생략）
	IF EXP:0 < EXPLV:1
		TIMES LOCAL , 2.50
	ELSEIF EXP:0 < EXPLV:2
		TIMES LOCAL , 0.80
	ELSEIF EXP:0 < EXPLV:3
		TIMES LOCAL , 0.40
	ELSEIF EXP:0 < EXPLV:4
		TIMES LOCAL , 0.10
	ELSEIF EXP:0 < EXPLV:5
		TIMES LOCAL , 0.00
	ELSEIF EXP:0 >= EXPLV:5
		TIMES LOCAL , 0.00
	ENDIF

	;PALAM:윤활をみる
	IF PALAM:4 < PALAMLV:1
		TIMES LOCAL , 1.20
		TIMES LOCAL:1 , 1.50
	ELSEIF PALAM:4 < PALAMLV:2
		TIMES LOCAL , 0.70
		TIMES LOCAL:1 , 1.20
	ELSEIF PALAM:4 < PALAMLV:3
		TIMES LOCAL , 0.40
	ELSEIF PALAM:4 < PALAMLV:4
		TIMES LOCAL , 0.10
	ELSEIF PALAM:4 >= PALAMLV:4
		TIMES LOCAL , 0.10
	ENDIF

	SOURCE:TARGET:아픔 += LOCAL
	SOURCE:TARGET:반감 += LOCAL:1

	;-------------------------------------------------
	;汚れの処理
	;-------------------------------------------------
	;奴隷の질내⇔조교者の指의 불결が移動
	CALL MOVE_STAIN("질내", TARGET, "손", PLAYER)

	;-------------------------------------------------
	;吸精処理
	;-------------------------------------------------
	IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:4+1) * BASE:LV
		CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
	ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:4+1) * BASE:LV
		CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
	ENDIF

	;-------------------------------------------------
	;경험の処理
	;-------------------------------------------------
	;V경험
	SIF TALENT:0 == 0 && TALENT:1 == 0
		CALL ADDS_EXP("Ｖ경험", 1, TARGET)

	;레즈경험
	A = 1
	IF IS_LESBIAN(TARGET,PLAYER)
		CALL ADDS_EXP("레즈경험", A, TARGET)
		CALL ADDS_EXP("레즈경험", A, PLAYER)
	ELSEIF IS_GAY(TARGET,PLAYER)
		CALL ADDS_EXP("ＢＬ경험", A, TARGET)
		CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	ENDIF

	;의존도ベクトル
	IF TALENT:0
		TFLAG:33 = 3
	ELSE
		TFLAG:33 = 1
	ENDIF
ENDIF
PRINTFORML ＜ %CALLNAME:TARGET%に指をＶ挿入中 ＞


;----------------------------------------------------------------------
@EQUIP_F_INSERT_A_TARGET
;----------------------------------------------------------------------
IF TEQUIP:TARGET:53 > 0 && TEQUIP:TARGET:53 < 2
	;조교者→조교대상
	TFLAG:28 = 2

	;-------------------------------------------------
	;소스추가処理
	;-------------------------------------------------
	;소비체력、기력
	DOWNBASE:TARGET:체력 += 20
	DOWNBASE:TARGET:기력 += 50

	SOURCE:TARGET:쾌Ａ += 50
	SOURCE:TARGET:굴종 += 50
	SOURCE:TARGET:일탈 += 40


	;-------------------------------------------------
	;特殊소스処理
	;-------------------------------------------------
	;억압、저항持ちには、억울소스추가
	SIF TALENT:32 || TALENT:34
		SOURCE:TARGET:울굴 += 50


	LOCAL = 50
	LOCAL:1 = 30
	;Aへの고통は先に処理しておく
	;EXP:Ａ경험をみる
	IF EXP:1 < EXPLV:1
		TIMES LOCAL , 3.00
		TIMES LOCAL:1 , 3.00
	ELSEIF EXP:1 < EXPLV:2
		TIMES LOCAL , 1.20
		TIMES LOCAL:1 , 2.00
	ELSEIF EXP:1 < EXPLV:3
		TIMES LOCAL , 0.80
		TIMES LOCAL:1 , 1.50
	ELSEIF EXP:1 < EXPLV:4
		TIMES LOCAL , 0.60
		TIMES LOCAL:1 , 1.20
	ELSEIF EXP:1 < EXPLV:5
		TIMES LOCAL , 0.40
	ELSEIF EXP:1 >= EXPLV:5
		TIMES LOCAL , 0.20
	ENDIF

	;PALAM:윤활をみる
	IF PALAM:4 < PALAMLV:1
		TIMES LOCAL , 1.20
		TIMES LOCAL:1 , 2.00
	ELSEIF PALAM:4 < PALAMLV:2
		TIMES LOCAL , 1.00
		TIMES LOCAL:1 , 1.50
	ELSEIF PALAM:4 < PALAMLV:3
		TIMES LOCAL , 0.60
	ELSEIF PALAM:4 < PALAMLV:4
		TIMES LOCAL , 0.30
	ELSEIF PALAM:4 >= PALAMLV:4
		TIMES LOCAL , 0.20
	ENDIF

	SOURCE:TARGET:아픔 += LOCAL
	SOURCE:TARGET:반감 += LOCAL:1

	;-------------------------------------------------
	;汚れの処理
	;-------------------------------------------------
	IF TEQUIP:25
		;奴隷のＡに촉수의 불결がつく
		CALL SET_STAIN("애널", "粘液", TARGET)
	ELSE
		;奴隷のＡ⇔조교者の指의 불결が移動
		CALL MOVE_STAIN("애널", TARGET, "손", PLAYER)
	ENDIF

	;-------------------------------------------------
	;吸精処理
	;-------------------------------------------------
	IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:3+1) * BASE:LV
		CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
	ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:3+1) * BASE:LV
		CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
	ENDIF

	;-------------------------------------------------
	;경험の処理
	;-------------------------------------------------
	CALL ADDS_EXP("Ａ경험", 1, TARGET)
	;PRINTL Ａ경험＋１

	A = 2
	IF IS_LESBIAN(TARGET,PLAYER)
		CALL ADDS_EXP("레즈경험", A, TARGET)
		CALL ADDS_EXP("레즈경험", A, PLAYER)
	ELSEIF IS_GAY(TARGET,PLAYER)
		CALL ADDS_EXP("ＢＬ경험", A, TARGET)
		CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
	ENDIF

	;의존도ベクトル
	TFLAG:33 = 3

ENDIF
PRINTFORML ＜%CALLNAME:TARGET%に指をＡ挿入中＞


;----------------------------------------------------------------------
@EQUIP_P_INSERT_V_PLAYER
;----------------------------------------------------------------------
IF TEQUIP:PLAYER:50 > 0 && TEQUIP:PLAYER:50 < 2
	;조교者←→조교대상
	TFLAG:28 = 3
	;-------------------------------------------------
	;소스の計算
	;-------------------------------------------------
	LOSEBASE:0 += 25
	LOSEBASE:1 += 50


	IF TALENT:PLAYER:유아체형
		SOURCE:TARGET:쾌Ｃ = 150
		SOURCE:아픔 += 25
	ELSEIF TALENT:PLAYER:소인체형
		SOURCE:TARGET:쾌Ｃ = 250
		SOURCE:아픔 += 50
	ENDIF
	IF TALENT:큰체형
		SOURCE:TARGET:쾌Ｃ = 100
		SOURCE:아픔 += 25
	ELSEIF TALENT:거체
		SOURCE:TARGET:쾌Ｃ = 100
		SOURCE:아픔 += 50
	ENDIF

	SOURCE:TARGET:성행위 = 250
	SOURCE:TARGET:굴종 = 500
	SOURCE:TARGET:일탈 = 250
	IF IS_MALE(TARGET) && (TALENT:쾌감을부정 == 0 || ABL:욕망 > 2)
		SOURCE:TARGET:반감 += 25
	ELSE
		SOURCE:TARGET:반감 += 250
	ENDIF

	;조교者に쾌Ｖ
	SOURCE:PLAYER:1 += 200

	;-------------------------------------------------
	;사정체크
	;-------------------------------------------------

	;사정先타입の설정
	TCVAR:PLAYER:페니스사용대상 = TARGET
	TCVAR:PLAYER:사정개소 = 14
	;사정전선택の可・불가
	TFLAG:704 = 0

	;-------------------------------------------------
	;汚れの処理
	;-------------------------------------------------
	;사정に関係なく行われる処理はここから
	;질내に파과혈의 불결を付加
	SIF TALENT:PLAYER:0 == 1 || TALENT:PLAYER:1 == 1
		CALL SET_STAIN("질내", "파과혈", PLAYER)
	;奴隷の질내⇔조교者のＰ의 불결が移動
	CALL MOVE_STAIN("질내", TARGET, "페니스", PLAYER)

	;-------------------------------------------------
	;吸精処理
	;-------------------------------------------------
	IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
	ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
	ENDIF

	;-------------------------------------------------
	;경험の処理
	;-------------------------------------------------
	;조교者のV경험
	CALL ADDS_EXP("Ｖ경험", 1, PLAYER)

	;PRINTFORML 봉사쾌락경험(조교者) + 1
	;EXP:PLAYER:봉사쾌락경험 += 1
	;PRINTFORML 성교경험(조교者) + 1
	;EXP:PLAYER:성교경험 += 1
	CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
	CALL ADDS_EXP("성교경험", 1, PLAYER)
	;수간경험
	IF (TALENT:TARGET:짐승 || TALENT:TARGET:새) && !(TALENT:PLAYER:짐승 || TALENT:PLAYER:새)
		CALL ADDS_EXP("수간경험", 1, PLAYER)
		
	ENDIF

	;처녀で同性の場合이상경험
	IF TALENT:PLAYER:처녀 && !IS_MALE(TARGET)
		CALL ADDS_ABNORMAL_EXP("처녀탈취（동성）", TARGET)
		CALL ADDS_ABNORMAL_EXP("처녀상실（동성）", PLAYER)
	ENDIF
	;근친상간の場合이상경험부여
	IF 近親체크(PLAYER , TARGET)
		CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:0%）", PLAYER)
		CALL ADDS_ABNORMAL_EXP( @"근친상간（%RESULTS:1%）", TARGET)
	ENDIF
	;레즈경험
	A = 4
	IF IS_LESBIAN(TARGET,PLAYER)
	;	PRINTFORML %EXPNAME:40%+{A}
	;	PRINTFORML %EXPNAME:40%(조교者)+{A}
	;	EXP:40 += A
	;	EXP:PLAYER:40 += A
		CALL ADDS_EXP("레즈경험", A, TARGET)
		CALL ADDS_EXP("레즈경험", A, PLAYER)
	ELSEIF IS_GAY(TARGET,PLAYER)
	;	PRINTFORML %EXPNAME:41%+{A}
	;	PRINTFORML %EXPNAME:41%(조교者)+{A}
	;	EXP:41 += A
	;	EXP:PLAYER:41 += A
		CALL ADDS_EXP("ＢＬ경험", A, TARGET)
		CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
		
		
	ENDIF



	;의존도ベクトル
	TFLAG:33 = 1

	;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
	IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
		TFLAG:30 += 1
	ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
		TFLAG:30 += 2
	ENDIF

	;봉사쾌락フラグ
	TFLAG:100 = 1
ENDIF
PRINTFORML ＜%CALLNAME:TARGET%からＶ挿入中＞


;----------------------------------------------------------------------
@EQUIP_P_INSERT_A_PLAYER
;----------------------------------------------------------------------
IF TEQUIP:PLAYER:51 > 0 && TEQUIP:PLAYER:51 < 2
	;조교者←→조교대상
	TFLAG:28 = 3

	B += 750
	;再装填処理中は사정ゲージは20分の1に
	SIF CFLAG:PLAYER:9 > 0
		B /= 20
	SIF HAVE_PENIS(PLAYER)
		BASE:PLAYER:2 += B

	;조교대상→조교者
	TFLAG:28 = 1

	LOSEBASE:0 += 20
	LOSEBASE:1 += 80

	SOURCE:0 += 300
	SOURCE:5 += 250
	SOURCE:12 = 250
	SOURCE:14 += 500
	IF IS_MALE(TARGET) && (TALENT:쾌감을부정 == 0 || ABL:욕망 > 2)
		SOURCE:18 = 25
	ELSE
		SOURCE:18 = 250
	ENDIF

	SOURCE:15 += 375

	;조교者に쾌Ａ
	SOURCE:PLAYER:2 = 350

	;억압、저항持ちには、억울소스추가
	SIF TALENT:32 || TALENT:34
		SOURCE:16 += 350

	;-------------------------------------------------
	;사정체크
	;-------------------------------------------------

	;사정先타입の설정
	TCVAR:PLAYER:페니스사용대상 = TARGET
	TCVAR:PLAYER:사정개소 = 14
	;사정전선택の可・불가
	TFLAG:704 = 0


	;-------------------------------------------------
	;汚れの処理
	;-------------------------------------------------
	;사정に関係なく行われる処理はここから
	;조교者のＡ⇔奴隷のＰ의 불결が移動
	CALL MOVE_STAIN("애널", PLAYER, "페니스", TARGET)

	;-------------------------------------------------
	;吸精処理
	;-------------------------------------------------
	IF CFLAG:PLAYER:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,PLAYER,LOCAL,TARGET
	ELSEIF CFLAG:TARGET:ＭＡＧ비피흡수 == 0
		LOCAL = (RAND:10+1) * BASE:LV
		CALL DRAIN_MAG,TARGET,LOCAL,PLAYER
	ENDIF

	;-------------------------------------------------
	;경험の処理
	;-------------------------------------------------
	;조교者のＡ경험
	CALL ADDS_EXP("Ａ경험", 1, PLAYER)

	;PRINTFORML 봉사쾌락경험(조교者) + 1
	;EXP:PLAYER:봉사쾌락경험 += 1
	;PRINTFORML 성교경험(조교者) + 1
	;EXP:PLAYER:성교경험 += 1
	CALL ADDS_EXP("봉사쾌락경험", 1, PLAYER)
	CALL ADDS_EXP("성교경험", 1, PLAYER)
	;수간경험
	IF (TALENT:TARGET:짐승 || TALENT:TARGET:새) && !(TALENT:PLAYER:짐승 || TALENT:PLAYER:새)
		CALL ADDS_EXP("수간경험", 1, PLAYER)
	ENDIF
	;레즈경험
	A = 4
	IF IS_LESBIAN(TARGET,PLAYER)
	;	PRINTFORML %EXPNAME:40%+{A}
	;	PRINTFORML %EXPNAME:40%(조교者)+{A}
	;	EXP:40 += A
	;	EXP:PLAYER:40 += A
		CALL ADDS_EXP("레즈경험", A, TARGET)
		CALL ADDS_EXP("레즈경험", A, PLAYER)
	ELSEIF IS_GAY(TARGET,PLAYER)
	;	PRINTFORML %EXPNAME:41%+{A}
	;	PRINTFORML %EXPNAME:41%(조교者)+{A}
	;	EXP:41 += A
	;	EXP:PLAYER:41 += A
		CALL ADDS_EXP("ＢＬ경험", A, TARGET)
		CALL ADDS_EXP("ＢＬ경험", A, PLAYER)
		
		
	ENDIF

	;의존도ベクトル
	TFLAG:33 = 3

	;의존도보정値(主人かつ연모、음란、복종で1。친애、창부、예속で2）
	IF (TALENT:3 || TALENT:4 || TALENT:5) && ASSIPLAY == 0
		TFLAG:30 += 1
	ELSEIF (TALENT:6 || TALENT:7 || TALENT:8) && ASSIPLAY == 0
		TFLAG:30 += 2
	ENDIF

	;봉사쾌락フラグ
	TFLAG:100 = 1

ENDIF
PRINTFORML ＜%CALLNAME:TARGET%からＡ挿入中＞




;-------------------------------------------------
@REMOVE_P_INSERT_V_TARGET
;-------------------------------------------------
IF TEQUIP:TARGET:50 > 0	
    TFLAG:18 = 1
	TEQUIP:TARGET:50 = -1
	DOWNBASE:TARGET:체력 += 10
	DOWNBASE:TARGET:기력 += 10	
;	SOURCE:1 += 200
	SOURCE:TARGET:정애 = 10
	SOURCE:TARGET:노출 = 10
	SOURCE:TARGET:일탈 = 10
ELSE
	TEQUIP:TARGET:50 = 0
ENDIF
SIF TFLAG:18 == 1 && TEQUIP:TARGET:50 == -1
	PRINTFORML ＜Ｖ挿入해제＞

;-------------------------------------------------
@REMOVE_P_INSERT_A_TARGET
;-------------------------------------------------
IF TEQUIP:TARGET:51 > 0	
    TFLAG:18 = 1
    TEQUIP:TARGET:51 = -1
	DOWNBASE:TARGET:체력 += 10
	DOWNBASE:TARGET:기력 += 10	
;	SOURCE:TARGET:쾌Ａ += 200
	SOURCE:TARGET:정애 = 10
	SOURCE:TARGET:노출 = 10
	SOURCE:TARGET:일탈 = 10
ELSE
	TEQUIP:TARGET:51 = 0
ENDIF
SIF TFLAG:18 == 1 && TEQUIP:TARGET:51 == -1
	PRINTFORML ＜Ａ挿入해제＞

;-------------------------------------------------
@REMOVE_F_INSERT_V_TARGET
;-------------------------------------------------
IF TEQUIP:TARGET:52 > 0
    TFLAG:18 = 1
	TEQUIP:TARGET:52 = -1
	DOWNBASE:TARGET:체력 += 10
	DOWNBASE:TARGET:기력 += 10	
;	SOURCE:1 += 60
	SOURCE:TARGET:정애 = 10
	SOURCE:TARGET:노출 = 10
	SOURCE:TARGET:일탈 = 10
ELSE
	TEQUIP:TARGET:52 = 0
ENDIF
SIF TFLAG:18 == 1 && TEQUIP:TARGET:52 == -1
	PRINTFORML ＜指Ｖ挿入해제＞

;-------------------------------------------------
@REMOVE_F_INSERT_A_TARGET
;-------------------------------------------------
IF TEQUIP:TARGET:53 > 0
    TFLAG:18 = 1
	TEQUIP:TARGET:53 = -1
	DOWNBASE:TARGET:체력 += 10
	DOWNBASE:TARGET:기력 += 10	
;	SOURCE:TARGET:쾌Ａ += 50
	SOURCE:TARGET:정애 = 10
	SOURCE:TARGET:노출 = 10
	SOURCE:TARGET:일탈 = 10
ELSE
	TEQUIP:TARGET:53 = 0
ENDIF
SIF TFLAG:18 == 1 && TEQUIP:TARGET:53 == -1
	PRINTFORML ＜指Ａ挿入해제＞

;-------------------------------------------------
@REMOVE_P_INSERT_V_PLAYER
;-------------------------------------------------
IF TEQUIP:PLAYER:50 > 0
    TFLAG:18 = 1
	TEQUIP:PLAYER:50 = -1
	DOWNBASE:TARGET:체력 += 10
	DOWNBASE:TARGET:기력 += 10	
;	SOURCE:TARGET:쾌Ａ += 50
	SOURCE:TARGET:정애 = 10
	SOURCE:TARGET:노출 = 10
	SOURCE:TARGET:일탈 = 10
ELSE
	TEQUIP:PLAYER:50 = 0
ENDIF
SIF TFLAG:18 == 1 && TEQUIP:PLAYER:50 == -1
	PRINTFORML ＜Ｖ挿入해제(조교者)＞
	
;-------------------------------------------------
@REMOVE_P_INSERT_A_PLAYER
;-------------------------------------------------
IF TEQUIP:PLAYER:51 > 0
    TFLAG:18 = 1
	TEQUIP:PLAYER:51 = -1
	DOWNBASE:TARGET:체력 += 10
	DOWNBASE:TARGET:기력 += 10	
;	SOURCE:TARGET:쾌Ａ += 50
	SOURCE:TARGET:정애 = 10
	SOURCE:TARGET:노출 = 10
	SOURCE:TARGET:일탈 = 10
ELSE
	TEQUIP:PLAYER:51 = 0
ENDIF
SIF TFLAG:18 == 1 && TEQUIP:PLAYER:51 == -1
	PRINTFORML ＜Ａ挿入해제(조교者)＞
