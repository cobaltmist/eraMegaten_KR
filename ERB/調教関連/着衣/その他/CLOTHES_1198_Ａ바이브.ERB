
@CLOTHES_NAME_1198
RESULTS = Ａ바이브

;노출부위
@CLOTHES_EXPOSE_1198,ARG
;ARG = 노출되는부위
;1 = 팔노출 2 = 발노출 3 = 다리노출 4 = 유방노출 5 = 유두노출 6 = 배꼽노출 7 = 음순노출 8 = 둔부노출


SELECTCASE ARG
	CASE 0
		RETURN 0
	CASEELSE
		RETURN 1
ENDSELECT

;장비부위
@CLOTHES_PART_1198
RETURN 11

;옷의 앞쪽이 열리는가
;1 열 수 있다、2 열린 채
@CLOTHES_OPENFRONT_1198
RETURN 0

;가랑이의구조
;1 = 바지、2 = 비키니타입、3 = 스커트、4 = 구멍뚫림、5 앞열림、6 개폐가능
@CLOTHES_CROTCH_1198
RETURN 0

;장비시의 조교효과
@CLOTHES_SOURCE_1198,ARG
IF FLAG:SHOP커맨드 == 0
	SOURCE:ARG:쾌Ａ += 125
	SOURCE:ARG:아픔 += 100
	SOURCE:ARG:노출 += 25
	SOURCE:ARG:일탈 += 25
	SOURCE:ARG:반감 += 40

	;억압、저항持ちには、억울ソース추가
	SIF TALENT:ARG:억압 || TALENT:ARG:저항
		SOURCE:ARG:울굴 += 20
		
	;Vへの고통は先に処理しておく
	;EXP:ARG:Ｖ경험をみる（처녀はありえない）
	A = 100
	B = 40
	IF EXP:ARG:1 < EXPLV:1
		TIMES A , 3.00
		TIMES B , 3.00
	ELSEIF EXP:ARG:1 < EXPLV:2
		TIMES A , 1.20
		TIMES B , 2.00
	ELSEIF EXP:ARG:1 < EXPLV:3
		TIMES A , 0.80
		TIMES B , 1.50
	ELSEIF EXP:ARG:1 < EXPLV:4
		TIMES A , 0.60
		TIMES B , 1.20
	ELSEIF EXP:ARG:1 < EXPLV:5
		TIMES A , 0.40
	ELSEIF EXP:ARG:1 >= EXPLV:5
		TIMES A , 0.20
	ENDIF


	;PALAM:ARG:윤활をみる
	IF PALAM:ARG:4 < PALAMLV:1
		TIMES A , 1.20
		TIMES B , 2.00
	ELSEIF PALAM:ARG:4 < PALAMLV:2
		TIMES A , 1.00
		TIMES B , 1.50
	ELSEIF PALAM:ARG:4 < PALAMLV:3
		TIMES A , 0.60
	ELSEIF PALAM:ARG:4 < PALAMLV:4
		TIMES A , 0.30
	ELSEIF PALAM:ARG:4 >= PALAMLV:4
		TIMES A , 0.20
	ENDIF
	SOURCE:ARG:7 += A
	SOURCE:ARG:18 += B
	IF ARG == TARGET
		DOWNBASE:ARG:0 += 30
		DOWNBASE:ARG:1 += 80
	ENDIF
	
ELSE
ENDIF

;장비시의 경험추가
@CLOTHES_EXP_1198,ARG
IF FLAG:SHOP커맨드 == 0
	SIF ARG == -1
		RETURN 0
	PRINTFORML <Ａ바이브 삽입중>\@ ARG == MASTER ? (주인) #\@\@ ARG == ASSI ? (조수) #\@
	IF SELECTCOM == 11
		CALL ADDS_EXP("Ａ경험", 2, ARG)
	ELSE
		CALL ADDS_EXP("Ａ경험", 1, ARG)
	ENDIF
ELSE

ENDIF

;장비するための조건
@CLOTHES_PREREQUISITE_1198,ARG
SIF (CFLAG:ARG:속옷（하） == 625 || CFLAG:ARG:속옷（하） == 626)
	RETURN 0
SIF (CFLAG:ARG:그외 == 1198 || CFLAG:ARG:그외2 == 1198 || CFLAG:ARG:그외3 == 1198 || CFLAG:ARG:그외4 == 1198 || CFLAG:ARG:그외5 == 1198 || CFLAG:ARG:그외6 == 1198)
	RETURN 0
SIF (CFLAG:ARG:그외 == 1202 || CFLAG:ARG:그외2 == 1202 || CFLAG:ARG:그외3 == 1202 || CFLAG:ARG:그외4 == 1202 || CFLAG:ARG:그외5 == 1202 || CFLAG:ARG:그외6 == 1202)
	RETURN 0
SIF ARG == MASTER
	RETURN 1
SIF EXP:ARG:Ａ경험 < 30 && ABL:ARG:Ａ감각 < 2
	RETURN 0
SIF TALENT:ARG:소인체형 && EXP:ARG:Ａ확장경험 < 3 && TALENT:MASTER:금단의지식 == 0
	RETURN 0
SIF ABL:ARG:순종 >= 5
	RETURN 1
SIF ABL:ARG:마조끼 >= 3
	RETURN 1
SIF ABL:ARG:Ａ감각 >= 3
	RETURN 1
SIF MARK:ARG:굴복각인 >= 3
	RETURN 1
RETURN 0

@CLOTHES_PRICE_1198
RETURN 2000
