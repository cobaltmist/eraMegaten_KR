;가창기능アップ가능なら1を、能なら0を返す
@ISABLUP14
;연모、친애、복종、예속はMAXLV10
IF ABL:가창기능 >= 10
	RETURN 0
ELSEIF ABL:가창기능 >= 5 && !함락(TARGET) && TARGET != MASTER
	RETURN 0
ENDIF

CALL DECIDE_ABLUP14

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;가창기능
@ABLUP14
$ABLUP_LOOP

DRAWLINE
;연모、친애、복종、예속はMAXLV10
IF ABL:가창기능 >= 10
	PRINTW 이미 MAX입니다
	RETURN 0
ELSEIF ABL:가창기능 >= 5 && !함락(TARGET) && TARGET != MASTER
	PRINTW 이미 MAX입니다
	RETURN 0
ENDIF

CALL DECIDE_ABLUP14

PRINT [0] - 
PRINTS PALAMNAME:8
PRINT 의 구슬×
PRINTV A
PRINT 、
PRINTS EXPNAME:64
PRINTV B
PRINT 이상
PRINT ……
IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 구슬부족 
	SIF I & 2
		PRINT 경험부족 
ENDIF
PRINTL 

IF Z == 0
	WAIT
	RETURN
ENDIF

PRINTLC [100] - 그만둔다
PRINTFORMLC   %" " * 42%
PRINTLC [200] - 자동 능력 상승


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100 && RESULT != 200
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 조건을 만족하지 못했습니다
	RETURN 0
ELSEIF RESULT == 100
	RETURN 0
ELSEIF RESULT == 200
	CALL AUTOMAX, 14
	RETURN 0
ENDIF

ABL:가창기능 += 1

IF RESULT == 0
	JUEL:8 -= A
ENDIF

PRINTS ABLNAME:14
PRINT 의 레벨이 
PRINTV ABL:14
PRINTFORMW %조사만처리(ABL:14,"가")% 되었습니다
GOTO ABLUP_LOOP


;가창기능LVUP판정
@DECIDE_ABLUP14
;조건別にＯＫかダメかを記録する
I = 0

;능력アップ가능なら1にする
Z = 0

IF ABL:가창기능 == 0
	A = 75
	B = 5
ELSEIF ABL:가창기능 == 1
	A = 500
	B = 10
ELSEIF ABL:가창기능 == 2
	A = 3000
	B = 30
ELSEIF ABL:가창기능 == 3
	A = 9000
	B = 50
ELSEIF ABL:가창기능 == 4
	A = 24000
	B = 100
ELSEIF ABL:가창기능 == 5
	A = 30000
	B = 200
ELSEIF ABL:가창기능 == 6
	A = 35000
	B = 300
ELSEIF ABL:가창기능 == 7
	A = 40000
	B = 400
ELSEIF ABL:가창기능 == 8
	A = 45000
	B = 500
ELSEIF ABL:가창기능 == 9
	A = 50000
	B = 600
ENDIF

;연모
IF TALENT:3
	TIMES A , 0.60
	TIMES B , 0.60
ENDIF
;친애
IF TALENT:친애
	TIMES A , 0.40
	TIMES B , 0.40
ENDIF
;일선을넘지않음
IF TALENT:27 && (TALENT:연모 == 0 && TALENT:음란 == 0 && TALENT:복종 == 0 && 함락() < 2)
	TIMES A , 2.00
	TIMES B , 2.00
ENDIF


;습득의 구슬で上げる
SIF JUEL:8 < A
	I |= 1
SIF EXP:64 < B
	I |= 2
IF I == 0
	Z = 1
ENDIF

RETURN 1


