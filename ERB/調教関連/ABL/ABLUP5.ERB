;Ａ감각の可否
@ISABLUP5
;연모、친애、음란、창부はMAXLV10
IF ABL:Ａ감각 >= 10
	RETURN 0
ELSEIF ABL:Ａ감각 >= 5 && !함락(TARGET) && TARGET != MASTER
	RETURN 0
ENDIF

CALL DECIDE_ABLUP5

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;Ａ감각
@ABLUP5
$ABLUP_LOOP

DRAWLINE
;연모、친애、음란、창부はMAXLV10
IF ABL:Ａ감각 >= 10
	PRINTW 이미 MAX입니다
	RETURN 0
ELSEIF ABL:Ａ감각 >= 5 && !함락(TARGET) && TARGET != MASTER
	PRINTW 이미 MAX입니다
	RETURN 0
ENDIF

CALL DECIDE_ABLUP5

PRINT [0] - 
PRINTS PALAMNAME:2
PRINT 의 구슬×
PRINTV A
PRINT 、
PRINTS EXPNAME:1
PRINTV B
PRINT 이상
PRINT ……
IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 구슬부족 
	SIF I & 2
		PRINT 경험부족 
ENDIF
PRINTL 

IF Z == 0
	WAIT
	RETURN
ENDIF

PRINTLC [100] - 그만둔다
PRINTFORMLC   %" " * 42%
PRINTLC [200] - 자동 능력 상승


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100 && RESULT != 200
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 조건을 만족하지 못했습니다
	RETURN 0
ELSEIF RESULT == 100
	RETURN 0
ELSEIF RESULT == 200
	CALL AUTOMAX, 5
	RETURN 0
ENDIF

ABL:Ａ감각 += 1

IF RESULT == 0
	JUEL:2 -= A
ENDIF

PRINTS ABLNAME:5
PRINT 의 레벨이 
PRINTV ABL:Ａ감각
PRINTFORMW %조사만처리(ABL:Ａ감각,"가")% 되었습니다
GOTO ABLUP_LOOP


;Ａ감각LvUP판정
@DECIDE_ABLUP5
;조건別にＯＫかダメかを記録する
I = 0
;능력アップ가능なら1にする
Z = 0

IF ABL:Ａ감각 == 0
	A = 1
	B = 2
ELSEIF ABL:Ａ감각 == 1
	A = 50
	B = 10
	SIF EXP:Ａ경험 >= EXPLV:3
		A = 20
ELSEIF ABL:Ａ감각 == 2
	A = 400
	B = 30
	SIF EXP:Ａ경험 >= EXPLV:4
		A = 100
ELSEIF ABL:Ａ감각 == 3
	A = 5000
	B = 75
	SIF EXP:Ａ경험 >= EXPLV:5
		A = 4000
ELSEIF ABL:Ａ감각 == 4
	A = 15000
	B = 150
	SIF EXP:Ａ경험 >= EXPLV:5
		A = 8000
ELSEIF ABL:Ａ감각 == 5
	A = 30000
	B = 230
	SIF EXP:Ａ경험 >= EXPLV:5
		A = 24000
ELSEIF ABL:Ａ감각 == 6
	A = 40000
	B = 270
ELSEIF ABL:Ａ감각 == 7
	A = 50000
	B = 360
ELSEIF ABL:Ａ감각 == 8
	A = 60000
	B = 420
ELSEIF ABL:Ａ감각 == 9
	A = 70000
	B = 500
ENDIF

;음란
IF TALENT:4
	TIMES A , 0.80
	TIMES B , 0.80
ENDIF
;창부
IF TALENT:7
	TIMES A , 0.50
	TIMES B , 0.50
ENDIF

;0になった場合は1にする
SIF A == 0
	A = 1

;쾌Ａ의 구슬で上げる
SIF JUEL:2 < A
	I |= 1

SIF EXP:Ａ경험 < B
	I |= 2

IF I == 0
	Z = 1
ENDIF

RETURN 1

