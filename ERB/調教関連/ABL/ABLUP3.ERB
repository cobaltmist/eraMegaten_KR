;Ｃ감각の可否
@ISABLUP3
;연모、친애、음란、창부はMAXLV10
IF ABL:3 >= 10
	RETURN 0
ELSEIF ABL:3 >= 5 && !함락(TARGET) && TARGET != MASTER
	RETURN 0
ENDIF

CALL DECIDE_ABLUP3

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF

;Ｃ감각
@ABLUP3
$ABLUP_LOOP

DRAWLINE
;연모、친애、음란、창부はMAXLV10
IF ABL:3 >= 10
	PRINTW 이미 MAX입니다
	RETURN 0
ELSEIF ABL:3 >= 5 && !함락(TARGET) && TARGET != MASTER
	PRINTW 이미 MAX입니다
	RETURN 0
ENDIF

CALL DECIDE_ABLUP3

PRINT [0] - 
PRINTS PALAMNAME:0
PRINT 의 구슬×
PRINTV A
PRINT ……
IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 구슬부족 
	SIF I & 2
		PRINT 경험부족 
ENDIF
PRINTL 

IF Z == 0
	WAIT
	RETURN
ENDIF

PRINTLC [100] - 그만둔다
PRINTFORMLC   %" " * 42%
PRINTLC [200] - 자동 능력 상승


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100 && RESULT != 200
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 조건을 만족하지 못했습니다
	RETURN 0
ELSEIF RESULT == 100
	RETURN 0
ELSEIF RESULT == 200
	CALL AUTOMAX, 3
	RETURN 0
ENDIF

ABL:3 += 1

IF RESULT == 0
	JUEL:0 -= A
ENDIF

PRINTS ABLNAME:3
PRINT 의 레벨이 
PRINTV ABL:3
PRINTFORMW %조사만처리(ABL:3,"가")% 되었습니다
GOTO ABLUP_LOOP


;C감각LvUP판정
@DECIDE_ABLUP3
;조건別にＯＫかダメかを記録する
I = 0
;능력アップ가능なら1にする
Z = 0

IF ABL:3 == 0
	A = 1
ELSEIF ABL:3 == 1
	A = 20
ELSEIF ABL:3 == 2
	A = 400
ELSEIF ABL:3 == 3
	A = 5000
ELSEIF ABL:3 == 4
	A = 15000
ELSEIF ABL:3 == 5
	A = 30000
ELSEIF ABL:3 == 6
	A = 40000
ELSEIF ABL:3 == 7
	A = 50000
ELSEIF ABL:3 == 8
	A = 60000
ELSEIF ABL:3 == 9
	A = 70000
ENDIF

;음란
SIF TALENT:4
	TIMES A , 0.80
;창부
SIF TALENT:7
	TIMES A , 0.50

;0になった場合は1にする
SIF A == 0
	A = 1

;쾌Ｃ의 구슬で上げる
SIF JUEL:0 < A
	I |= 1

IF I == 0
	Z = 1
ENDIF
RETURN 1

