 
;-------------------------------------------------
;関数初期化
;-------------------------------------------------
K = 0
VARSET STR
VARSET W
;-------------------------------------------------
;コマンド実行時のセリフ CFLAG 300～500
;-------------------------------------------------
SELECTCASE SELECTCOM
;=================================================
;愛撫系コマンド
;=================================================
;-------------------------------------------------
;0,愛撫
;-------------------------------------------------
	CASE 0
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:300 == 0
				STR:(K++) =
				CFLAG:300 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:300 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:300 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:300 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:300 = 3
				ENDIF
				;それ以外
				IF (CFLAG:300 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:300 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;1,クンニ
;-------------------------------------------------
	CASE 1
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:301 == 0
			STR:(K++) =
			CFLAG:301 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:301 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:301 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:301 = 3
			ENDIF
			;それ以外
			IF (CFLAG:301 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:301 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;3,指挿入れ
;-------------------------------------------------
	CASE 3
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:303 == 0
			STR:(K++) =
			CFLAG:303 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:303 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:303 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:303 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:303 = 3
			ENDIF
			;それ以外
			IF (CFLAG:303 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:303 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;4,アナル舐め
;-------------------------------------------------
	CASE 4
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:304 == 0
			STR:(K++) =
			CFLAG:304 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:304 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:304 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:304 = 3
			ENDIF
			;それ以外
			IF (CFLAG:304 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:304 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;5,アナル愛撫
;-------------------------------------------------
	CASE 5
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:305 == 0
				STR:(K++) =
				CFLAG:305 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:305 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:305 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:305 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:305 = 3
				ENDIF
				;それ以外
				IF (CFLAG:305 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:305 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;6,胸揉み
;-------------------------------------------------
	CASE 6
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:306 == 0
				STR:(K++) =
				CFLAG:306 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:306 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:306 = 3
				ENDIF
				;それ以外
				IF (CFLAG:306 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:306 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;7,乳首責め
;-------------------------------------------------
	CASE 7
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:307 == 0
				STR:(K++) =
				CFLAG:307 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:307 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:307 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:307 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:307 = 3
				ENDIF
				;それ以外
				IF (CFLAG:307 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:307 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;8,胸愛撫
;-------------------------------------------------
	CASE 8
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:308 == 0
				STR:(K++) =
				CFLAG:308 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:308 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:308 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:308 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:308 = 3
				ENDIF
				;それ以外
				IF (CFLAG:308 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:308 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;9,秘貝開帳
;-------------------------------------------------
	CASE 9
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:309 == 0
			STR:(K++) =
			CFLAG:309 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:309 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:309 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:309 = 3
			ENDIF
			;それ以外
			IF (CFLAG:309 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:309 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;10,胸自慰
;-------------------------------------------------
	CASE 10
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:310 == 0
			STR:(K++) =
			CFLAG:310 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:310 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:310 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:310 = 3
			ENDIF
			;それ以外
			IF (CFLAG:310 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:310 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;11,自慰
;-------------------------------------------------
	CASE 11
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:311 == 0
			STR:(K++) =
			CFLAG:311 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:311 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:311 = 4
			ENDIF
			;従順Lv3以上
			IF ABL:0 >= 3 && (CFLAG:311 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:311 = 3
			ENDIF
			;それ以外
			IF (CFLAG:311 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:311 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;12,羽愛撫
;-------------------------------------------------
	CASE 12
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:435 == 0
				STR:(K++) =
				CFLAG:435 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:435 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:435 = 4
				ENDIF
				;従順Lv3以上
				IF ABL:0 >= 3 && (CFLAG:435 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:435 = 3
				ENDIF
				;それ以外
				IF (CFLAG:435 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:435 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;13,尻尾撫で
;-------------------------------------------------
	CASE 13
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:436 == 0
				STR:(K++) =
				CFLAG:436 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:436 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:436 = 4
				ENDIF
				;従順Lv3以上
				IF ABL:0 >= 3 && (CFLAG:436 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:436 = 3
				ENDIF
				;それ以外
				IF (CFLAG:436 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:436 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;14,クリ愛撫
;-------------------------------------------------
	CASE 14
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:442 == 0
			STR:(K++) =
			CFLAG:442 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:442 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:442 = 4
			ENDIF
			;従順Lv3以上
			IF ABL:0 >= 3 && (CFLAG:442 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:442 = 3
			ENDIF
			;それ以外
			IF (CFLAG:442 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:442 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;15,尻愛撫
;-------------------------------------------------
	CASE 15
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:444 == 0
				STR:(K++) =
				CFLAG:444 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:444 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:444 = 4
				ENDIF
				;従順Lv3以上
				IF ABL:0 >= 3 && (CFLAG:444 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:444 = 3
				ENDIF
				;それ以外
				IF (CFLAG:444 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:444 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
		;	GOTO SHOKUSHU_PLAY←CASE 44949を使うならコメントアウトを外す
		ENDIF
;-------------------------------------------------
;99,愛撫させる
;-------------------------------------------------
	CASE 99
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:462 == 0
			STR:(K++) =
			CFLAG:462 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:462 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:462 = 3
			ENDIF
			;それ以外
			IF (CFLAG:462 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:462 = 2
			ENDIF
		ENDIF
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
	CASE 44949
		$SHOKUSHU_PLAY
		SELECTCASE SELECTCOM
;-------------------------------------------------
;0,触手愛撫
;-------------------------------------------------
			CASE 0
				;初めて
				IF CFLAG:455 == 0
					STR:(K++) =
					CFLAG:455 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:455 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:455 = 3
					ENDIF
					;それ以外
					IF (CFLAG:455 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:455 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;5,触手アナル愛撫
;-------------------------------------------------
			CASE 5
				;初めて
				IF CFLAG:456 == 0
					STR:(K++) =
					CFLAG:456 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:456 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:456 = 3
					ENDIF
					;それ以外
					IF (CFLAG:456 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:456 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;6,触手胸揉み
;-------------------------------------------------
			CASE 6
				;初めて
				IF CFLAG:457 == 0
					STR:(K++) =
					CFLAG:457 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:457 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:457 = 3
					ENDIF
					;それ以外
					IF (CFLAG:457 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:457 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;7,触手乳首責め（短）
;-------------------------------------------------
			CASE 7
				;初めて
				IF CFLAG:458 == 0
					STR:(K++) =
					CFLAG:458 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:458 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:458 = 3
					ENDIF
					;それ以外
					IF (CFLAG:458 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:458 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;8,触手胸愛撫
;-------------------------------------------------
			CASE 8
				;初めて
				IF CFLAG:459 == 0
					STR:(K++) =
					CFLAG:459 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:459 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:459 = 3
					ENDIF
					;それ以外
					IF (CFLAG:459 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:459 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;12,触手羽愛撫
;-------------------------------------------------
			CASE 12
				;初めて
				IF CFLAG:460 == 0
					STR:(K++) =
					CFLAG:460 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:460 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:460 = 3
					ENDIF
					;それ以外
					IF (CFLAG:460 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:460 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;13,触手尻尾撫で
;-------------------------------------------------
			CASE 13
				;初めて
				IF CFLAG:461 == 0
					STR:(K++) =
					CFLAG:461 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:461 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:461 = 3
					ENDIF
					;それ以外
					IF (CFLAG:461 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:461 = 2
					ENDIF
				ENDIF
;-------------------------------------------------
;15,触手尻愛撫
;-------------------------------------------------
			CASE 15
				;初めて
				IF CFLAG:462 == 0
					STR:(K++) =
					CFLAG:462 = 1
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 3 || FLAG:7 == 2)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦	
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属	
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) = 
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) = 
						ENDIF
						CFLAG:462 = 4
					ENDIF
					;屈服刻印Lv3
					IF MARK:2 == 3 && (CFLAG:462 < 2 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) = 
						CFLAG:462 = 3
					ENDIF
					;それ以外
					IF (CFLAG:462 || FLAG:7 == 2) && STR:0 == ""
						STR:(K++) =
						CFLAG:462 = 2
					ENDIF
				ENDIF
		ENDSELECT
;~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
;=================================================
;コミュニケーション系コマンド
;=================================================
;-------------------------------------------------
;20,キスする
;-------------------------------------------------
	CASE 20
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:312 == 0
			STR:(K++) =
			CFLAG:312 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:312 = 4
			ENDIF
			;従順Lv3以上
			IF ABL:0 >= 3 && (CFLAG:312 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:312 = 3
			ENDIF
			;それ以外
			IF (CFLAG:312 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:312 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;21,何もしない
;-------------------------------------------------
	CASE 21
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:313 == 0
			STR:(K++) =
			CFLAG:313 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:313 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:313 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:313 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:313 = 3
			ENDIF
			;それ以外
			IF (CFLAG:313 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:313 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;22,会話　恋慕、親愛のみ
;-------------------------------------------------
	CASE 22
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:314 == 0
			STR:(K++) =
			CFLAG:314 = 1
		;二回目以降
		ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:314 = 2
		ENDIF
;-------------------------------------------------
;23,プレゼント　恋慕、親愛のみ
;-------------------------------------------------
	CASE 23
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:315 == 0
			STR:(K++) =
			CFLAG:315 = 1
		;二回目以降
		ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:315 = 2
		ENDIF
;-------------------------------------------------
;24,スキンシップ　恋慕、親愛のみ
;-------------------------------------------------
	CASE 24
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:316 == 0
			STR:(K++) =
			CFLAG:316 = 1
		;二回目以降
		ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:316 = 2
		ENDIF
;-------------------------------------------------
;25,まったりする　恋慕、親愛のみ
;-------------------------------------------------
	CASE 25
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:317 == 0
			STR:(K++) =
			CFLAG:317 = 1
		;二回目以降
		ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:317 = 2
		ENDIF
;-------------------------------------------------
;26,家事　恋慕、親愛のみ
;-------------------------------------------------
	CASE 26
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:318 == 0
			STR:(K++) =
			CFLAG:318 = 1
		;二回目以降
		ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:318 = 2
		ENDIF
;-------------------------------------------------
;27,歌の練習　恋慕、親愛のみ
;-------------------------------------------------
	CASE 27
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:319 == 0
			STR:(K++) =
			CFLAG:319 = 1
		;二回目以降
		ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:319 = 2
		ENDIF
;-------------------------------------------------
;28,罵倒
;-------------------------------------------------
	CASE 28
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:320 == 0
			STR:(K++) =
			CFLAG:320 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:320 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:320 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:320 = 3
			ENDIF
			;それ以外
			IF (CFLAG:320 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:320 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;29,ビデオ鑑賞　陥落済みのみ
;-------------------------------------------------
	CASE 29
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:321 == 0
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:321 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:321 = 2
		ENDIF
;-------------------------------------------------
;30,ぱふぱふ　陥落済みのみ
;-------------------------------------------------
	CASE 30
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:422 == 0
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:422 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:422 = 2
		ENDIF
;=================================================
;道具使用コマンド
;=================================================
;-------------------------------------------------
;40,ローター
;-------------------------------------------------
	CASE 40
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:322 == 0
			STR:(K++) =
			CFLAG:322 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:322 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:322 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:322 = 3
			ENDIF
			;それ以外
			IF (CFLAG:322 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:322 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;41,Ｅマッサージャ
;-------------------------------------------------
	CASE 41
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:323 == 0
			STR:(K++) =
			CFLAG:323 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:323 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:323 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:323 = 3
			ENDIF
			;それ以外
			IF (CFLAG:323 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:323 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;42,クリキャップ
;-------------------------------------------------
	CASE 42
		CALL TRAIN_MESSAGE
		IF TEQUIP:11
			;初めて
			IF CFLAG:324 == 0
				STR:(K++) =
				CFLAG:324 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:324 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:324 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:324 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:324 = 3
				ENDIF
				;それ以外
				IF (CFLAG:324 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:324 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;43,オナホール
;-------------------------------------------------
	CASE 43
		CALL TRAIN_MESSAGE
		IF TEQUIP:12
			;初めて
			IF CFLAG:325 == 0
				STR:(K++) =
				CFLAG:325 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:325 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:325 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:325 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:325 = 3
				ENDIF
				;それ以外
				IF (CFLAG:325 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:325 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;44,バイブ
;-------------------------------------------------
	CASE 44
		CALL TRAIN_MESSAGE
		IF TEQUIP:13
			;初めて
			IF CFLAG:326 == 0
				;処女
				IF TALENT:0
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) =
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) =
						ENDIF
					ENDIF
					;それ以外
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				;非処女
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) =
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) =
						ENDIF
					ENDIF
					;それ以外
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				CFLAG:326 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:326 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:326 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:326 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:326 = 3
				ENDIF
				;それ以外
				IF (CFLAG:326 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:326 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;45,アナルバイブ
;-------------------------------------------------
	CASE 45
		CALL TRAIN_MESSAGE
		IF TEQUIP:14
			;初めて
			IF CFLAG:327 == 0
				STR:(K++) =
				CFLAG:327 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:327 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:327 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:327 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:327 = 3
				ENDIF
				;それ以外
				IF (CFLAG:327 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:327 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;46,アナルビーズ
;-------------------------------------------------
	CASE 46
		CALL TRAIN_MESSAGE
		IF TEQUIP:15
			;初めて
			IF CFLAG:328 == 0
				STR:(K++) =
				CFLAG:328 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:328 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:328 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:328 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:328 = 3
				ENDIF
				;それ以外
				IF (CFLAG:328 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:328 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;47,二プルキャップ
;-------------------------------------------------
	CASE 47
		CALL TRAIN_MESSAGE
		IF TEQUIP:16
			;初めて
			IF CFLAG:329 == 0
				STR:(K++) =
				CFLAG:329 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:329 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:329 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:329 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:329 = 3
				ENDIF
				;それ以外
				IF (CFLAG:329 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:329 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;48,乳首ローター
;-------------------------------------------------
	CASE 48
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:330 == 0
			STR:(K++) =
			CFLAG:330 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:330 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:330 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:330 = 3
			ENDIF
			;それ以外
			IF (CFLAG:330 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:330 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;49,搾乳機
;-------------------------------------------------
	CASE 49
		CALL TRAIN_MESSAGE
		IF TEQUIP:17
			;初めて
			IF CFLAG:331 == 0
				STR:(K++) =
				CFLAG:331 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:331 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:331 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:331 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:331 = 3
				ENDIF
				;それ以外
				IF (CFLAG:331 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:331 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;109,射精止め
;-------------------------------------------------
	CASE 109
		CALL TRAIN_MESSAGE
		IF TEQUIP:42
			;初めて
			IF CFLAG:464 == 0
				STR:(K++) =
				CFLAG:464 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:464 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:464 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:464 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:464 = 3
				ENDIF
				;それ以外
				IF (CFLAG:464 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:464 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;=================================================
;調教者道具系コマンド
;=================================================
;-------------------------------------------------
;50,クリキャップ使う
;-------------------------------------------------
	CASE 50
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;初めて
			IF CFLAG:453 == 0
				STR:(K++) =
				CFLAG:453 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:453 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:453 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:453 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:453 = 3
				ENDIF
				;それ以外
				IF (CFLAG:453 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:453 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;51,オナホール使う
;-------------------------------------------------
	CASE 51
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;初めて
			IF CFLAG:454 == 0
				STR:(K++) =
				CFLAG:454 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:454 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:454 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:454 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:454 = 3
				ENDIF
				;それ以外
				IF (CFLAG:454 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:454 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;52,バイブ使う
;-------------------------------------------------
	CASE 52
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;初めて
			IF CFLAG:455 == 0
				STR:(K++) =
				CFLAG:455 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:455 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:455 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:455 = 3
				ENDIF
				;それ以外
				IF (CFLAG:455 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:455 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;53,アナルバイブ使う
;-------------------------------------------------
	CASE 53
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;初めて
			IF CFLAG:456 == 0
				STR:(K++) =
				CFLAG:456 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:456 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:456 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:456 = 3
				ENDIF
				;それ以外
				IF (CFLAG:456 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:456 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;54,アナルビーズ使う
;-------------------------------------------------
	CASE 54
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:15
			;初めて
			IF CFLAG:457 == 0
				STR:(K++) =
				CFLAG:457 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:457 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:457 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:457 = 3
				ENDIF
				;それ以外
				IF (CFLAG:457 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:457 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;55,二プルキャップ使う
;-------------------------------------------------
	CASE 55
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;初めて
			IF CFLAG:458 == 0
				STR:(K++) =
				CFLAG:458 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:458 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:458 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:458 = 3
				ENDIF
				;それ以外
				IF (CFLAG:458 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:458 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;56,搾乳機使う
;-------------------------------------------------
	CASE 56
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;初めて
			IF CFLAG:459 == 0
				STR:(K++) =
				CFLAG:459 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:459 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:459 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:459 = 3
				ENDIF
				;それ以外
				IF (CFLAG:459 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:459 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;196,媚薬使う
;-------------------------------------------------
	CASE 196
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:437 == 0
			STR:(K++) =
			CFLAG:437 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:437 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:437 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:437 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:437 = 3
			ENDIF
			;それ以外
			IF (CFLAG:437 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:437 = 2
			ENDIF
		ENDIF
;=================================================
;セックス系コマンド
;=================================================
;-------------------------------------------------
;60,正常位
;-------------------------------------------------
	CASE 60
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:332 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:332 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:332 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:332 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:332 = 3
			ENDIF
			;それ以外
			IF (CFLAG:332 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:332 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;61,後背位
;-------------------------------------------------
	CASE 61
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:333 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:333 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:333 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:333 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:333 = 3
			ENDIF
			;それ以外
			IF (CFLAG:333 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:333 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;64,逆レイプ
;-------------------------------------------------
	CASE 64
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:336 == 0
			STR:(K++) =
			CFLAG:336 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:336 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:336 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:336 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:336 = 3
			ENDIF
			;それ以外
			IF (CFLAG:336 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:336 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;65,乳ファック
;-------------------------------------------------
	CASE 65
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:337 == 0
			STR:(K++) =
			CFLAG:337 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:337 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:337 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:337 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:337 = 3
			ENDIF
			;それ以外
			IF (CFLAG:337 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:337 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;66,騎乗位
;-------------------------------------------------
	CASE 66
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:338 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:338 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:338 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:338 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:338 = 3
			ENDIF
			;それ以外
			IF (CFLAG:338 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:338 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;68,対面座位
;-------------------------------------------------
	CASE 68
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:340 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:340 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:340 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:340 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:340 = 3
			ENDIF
			;それ以外
			IF (CFLAG:340 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:340 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;69,背面座位
;-------------------------------------------------
	CASE 69
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:341 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:341 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:341 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:341 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:341 = 3
			ENDIF
			;それ以外
			IF (CFLAG:341 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:341 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;72,下着ずらし挿入
;-------------------------------------------------
	CASE 72
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:420 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:420 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:420 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:420 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:420 = 3
			ENDIF
			;それ以外
			IF (CFLAG:420 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:420 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;74,逆レイプされる
;-------------------------------------------------
	CASE 74
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:460 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:460 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:460 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:460 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:460 = 3
			ENDIF
			;それ以外
			IF (CFLAG:460 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:460 = 2
			ENDIF
		ENDIF
;=================================================
;アナルセックス系コマンド
;=================================================
;-------------------------------------------------
;62,正常位アナル
;-------------------------------------------------
	CASE 62
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:334 == 0
			STR:(K++) =
			CFLAG:334 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:334 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:334 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:334 = 3
			ENDIF
			;それ以外
			IF (CFLAG:334 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:334 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;63,後背位アナル
;-------------------------------------------------
	CASE 63
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:335 == 0
			STR:(K++) =
			CFLAG:335 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:335 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:335 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:335 = 3
			ENDIF
			;それ以外
			IF (CFLAG:335 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:335 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;67,騎乗位アナル
;-------------------------------------------------
	CASE 67
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:339 == 0
			STR:(K++) =
			CFLAG:339 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:339 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:339 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:339 = 3
			ENDIF
			;それ以外
			IF (CFLAG:339 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:339 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;70,対面座位アナル
;-------------------------------------------------
	CASE 70
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:342 == 0
			STR:(K++) =
			CFLAG:342 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:342 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:342 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:342 = 3
			ENDIF
			;それ以外
			IF (CFLAG:342 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:342 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;71,背面座位アナル
;-------------------------------------------------
	CASE 71
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:343 == 0
			STR:(K++) =
			CFLAG:343 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:343 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:343 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:343 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:343 = 3
			ENDIF
			;それ以外
			IF (CFLAG:343 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:343 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;73,下着ずらし挿入アナル
;-------------------------------------------------
	CASE 73
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:421 == 0
			STR:(K++) =
			CFLAG:421 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:421 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:421 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:421 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:421 = 3
			ENDIF
			;それ以外
			IF (CFLAG:421 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:421 = 2
			ENDIF
		ENDIF
;=================================================
;奉仕系コマンド
;=================================================
;-------------------------------------------------
;80,手淫
;-------------------------------------------------
	CASE 80
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:344 == 0
			STR:(K++) =
			CFLAG:344 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:344 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:344 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:344 = 3
			ENDIF
			;それ以外
			IF (CFLAG:344 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:344 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;81,フェラチオ
;-------------------------------------------------
	CASE 81
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:345 == 0
			STR:(K++) =
			CFLAG:345 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:345 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:345 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:345 = 3
			ENDIF
			;それ以外
			IF (CFLAG:345 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:345 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;82,パイズリ
;-------------------------------------------------
	CASE 82
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:346 == 0
			STR:(K++) =
			CFLAG:346 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:346 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:346 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:346 = 3
			ENDIF
			;それ以外
			IF (CFLAG:346 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:346 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;83,素股
;-------------------------------------------------
	CASE 83
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:347 == 0
			STR:(K++) =
			CFLAG:347 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:347 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:347 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:347 = 3
			ENDIF
			;それ以外
			IF (CFLAG:347 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:347 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;84,泡踊り
;-------------------------------------------------
	CASE 84
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:348 == 0
			STR:(K++) =
			CFLAG:348 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:348 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:348 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:348 = 3
			ENDIF
			;それ以外
			IF (CFLAG:348 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:348 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;85,足扱き
;-------------------------------------------------
	CASE 85
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:349 == 0
			STR:(K++) =
			CFLAG:349 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:349 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:349 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:349 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:349 = 3
			ENDIF
			;それ以外
			IF (CFLAG:349 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:349 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;86,足舐め強制
;-------------------------------------------------
	CASE 86
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:350 == 0
			STR:(K++) =
			CFLAG:350 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:350 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:350 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:350 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:350 = 3
			ENDIF
			;それ以外
			IF (CFLAG:350 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:350 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;87,縦パイズリ
;-------------------------------------------------
	CASE 87
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:351 == 0
			STR:(K++) =
			CFLAG:351 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:351 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:351 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:351 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:351 = 3
			ENDIF
			;それ以外
			IF (CFLAG:351 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:351 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;88,パイズリフェラ
;-------------------------------------------------
	CASE 88
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:352 == 0
			STR:(K++) =
			CFLAG:352 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:352 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:352 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:352 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:352 = 3
			ENDIF
			;それ以外
			IF (CFLAG:352 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:352 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;89,母乳飲み　陥落済みのみ
;-------------------------------------------------
	CASE 89
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:353 == 0
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:353 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:353 = 2
		ENDIF
;-------------------------------------------------
;197,パンツ扱き
;-------------------------------------------------
	CASE 197
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:438 == 0
			STR:(K++) =
			CFLAG:438 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:438 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:438 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:438 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:438 = 3
			ENDIF
			;それ以外
			IF (CFLAG:438 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:438 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;211,亀頭攻め(受)
;-------------------------------------------------
	CASE 211
		;初めて
		IF CFLAG:479 == 0
			STR:(K++) =
			CFLAG:479 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:479 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:479 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:479 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:479 = 3
			ENDIF
			;それ以外
			IF (CFLAG:479 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:479 = 2
			ENDIF
		ENDIF
;=================================================
;調教者奉仕系コマンド
;=================================================
;-------------------------------------------------
;2,フェラする
;-------------------------------------------------
	CASE 2
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:302 == 0
			STR:(K++) =
			CFLAG:302 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:302 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:302 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:302 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:302 = 3
			ENDIF
			;それ以外
			IF (CFLAG:302 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:302 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;90,手淫する
;-------------------------------------------------
	CASE 90
		CALL TRAIN_MESSAGE
		IF TEQUIP:12 == 0;←注意
			;初めて
			IF CFLAG:423 == 0
				STR:(K++) =
				CFLAG:423 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:423 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:423 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:423 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:423 = 3
				ENDIF
				;それ以外
				IF (CFLAG:423 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:423 = 2
				ENDIF
			ENDIF
		ELSE
;-------------------------------------------------
;オナホコキする
;-------------------------------------------------
			;初めて
			IF CFLAG:461 == 0
				STR:(K++) =
				CFLAG:461 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:461 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:461 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:461 = 3
				ENDIF
				;それ以外
				IF (CFLAG:461 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:461 = 2
				ENDIF
			ENDIF
		ENDIF
;-------------------------------------------------
;91,パイズリする
;-------------------------------------------------
	CASE 91
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:424 == 0
			STR:(K++) =
			CFLAG:424 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:424 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:424 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:424 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:424 = 3
			ENDIF
			;それ以外
			IF (CFLAG:424 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:424 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;92,正常位させる
;-------------------------------------------------
	CASE 92
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:425 == 0
			STR:(K++) =
			CFLAG:425 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:425 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:425 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:425 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:425 = 3
			ENDIF
			;それ以外
			IF (CFLAG:425 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:425 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;93,後背位させる
;-------------------------------------------------
	CASE 93
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:426 == 0
			STR:(K++) =
			CFLAG:426 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:426 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:426 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:426 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:426 = 3
			ENDIF
			;それ以外
			IF (CFLAG:426 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:426 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;94,騎乗位する
;-------------------------------------------------
	CASE 94
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:427 == 0
			STR:(K++) =
			CFLAG:427 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:427 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:427 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:427 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:427 = 3
			ENDIF
			;それ以外
			IF (CFLAG:427 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:427 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;95,アナル正常位させる
;-------------------------------------------------
	CASE 95
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:428 == 0
			STR:(K++) =
			CFLAG:428 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:428 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:428 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:428 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:428 = 3
			ENDIF
			;それ以外
			IF (CFLAG:428 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:428 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;96,アナル後背位させる
;-------------------------------------------------
	CASE 96
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:429 == 0
			STR:(K++) =
			CFLAG:429 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:429 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:429 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:429 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:429 = 3
			ENDIF
			;それ以外
			IF (CFLAG:429 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:429 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;97,アナル騎乗位する
;-------------------------------------------------
	CASE 97
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:430 == 0
			STR:(K++) =
			CFLAG:430 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:430 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:430 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:430 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:430 = 3
			ENDIF
			;それ以外
			IF (CFLAG:430 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:430 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;98,母乳飲ませる　陥落済みのみ
;-------------------------------------------------
	CASE 98
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:443 == 0
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:443 = 1
		;二回目以降
		ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
			;恋慕、親愛
			IF (TALENT:3 || TALENT:6)
				STR:(K++) =
			;淫乱、娼婦
			ELSEIF (TALENT:4 || TALENT:7)
				STR:(K++) =
			;服従、隷属
			ELSEIF (TALENT:5 || TALENT:8)
				STR:(K++) =
			ENDIF
			;陥落済み共通
			IF STR:(K++) == ""
				STR:(K++) =
			ENDIF
			CFLAG:443 = 2
		ENDIF
;-------------------------------------------------
;210,亀頭攻め
;-------------------------------------------------
	CASE 210
		;初めて
		IF CFLAG:480 == 0
			STR:(K++) =
			CFLAG:480 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:480 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:480 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:480 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:480 = 3
			ENDIF
			;それ以外
			IF (CFLAG:480 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:480 = 2
			ENDIF
		ENDIF
;=================================================
;SM系コマンド
;=================================================
;-------------------------------------------------
;100,スパンキング
;-------------------------------------------------
	CASE 100
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:354 == 0
			STR:(K++) =
			CFLAG:354 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:354 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:354 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:354 = 3
			ENDIF
			;それ以外
			IF (CFLAG:354 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:354 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;101,乳スパンキング
;-------------------------------------------------
	CASE 101
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:355 == 0
			STR:(K++) =
			CFLAG:355 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:355 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:355 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:355 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:355 = 3
			ENDIF
			;それ以外
			IF (CFLAG:355 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:355 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;102,鞭
;-------------------------------------------------
	CASE 102
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:356 == 0
			STR:(K++) =
			CFLAG:356 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:356 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:356 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:356 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:356 = 3
			ENDIF
			;それ以外
			IF (CFLAG:356 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:356 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;103,蝋燭
;-------------------------------------------------
	CASE 103
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:357 == 0
			STR:(K++) =
			CFLAG:357 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:357 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:357 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:357 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:357 = 3
			ENDIF
			;それ以外
			IF (CFLAG:357 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:357 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;104,針
;-------------------------------------------------
	CASE 104
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:358 == 0
			STR:(K++) =
			CFLAG:358 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:358 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:358 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:358 = 3
			ENDIF
			;それ以外
			IF (CFLAG:358 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:358 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;105,アイマスク
;-------------------------------------------------
	CASE 105
		CALL TRAIN_MESSAGE
		IF TEQUIP:18
			;初めて
			IF CFLAG:359 == 0
				STR:(K++) =
				CFLAG:359 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:359 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:359 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:359 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:359 = 3
				ENDIF
				;それ以外
				IF (CFLAG:359 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:359 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;106,縄
;-------------------------------------------------
	CASE 106
		CALL TRAIN_MESSAGE
		IF TEQUIP:19
			;初めて
			IF CFLAG:360 == 0
				STR:(K++) =
				CFLAG:360 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:360 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:360 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:360 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:360 = 3
				ENDIF
				;それ以外
				IF (CFLAG:360 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:360 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;107,ボールギャグ
;-------------------------------------------------
	CASE 107
		CALL TRAIN_MESSAGE
		IF TEQUIP:20
			;初めて
			IF CFLAG:361 == 0
				STR:(K++) =
				CFLAG:361 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:361 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:361 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:361 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:361 = 3
				ENDIF
				;それ以外
				IF (CFLAG:361 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:361 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;=================================================
;助手によるコマンド、及びレズプレイ
;=================================================
;-------------------------------------------------
;120,クンニ強制
;-------------------------------------------------
	CASE 120
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:362 == 0
			STR:(K++) =
			CFLAG:362 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:362 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:362 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:362 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:362 = 3
			ENDIF
			;それ以外
			IF (CFLAG:362 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:362 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;121,貝あわせ
;-------------------------------------------------
	CASE 121
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:363 == 0
			STR:(K++) =
			CFLAG:363 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:363 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:363 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:363 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:363 = 3
			ENDIF
			;それ以外
			IF (CFLAG:363 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:363 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;122,助手を犯させる
;-------------------------------------------------
	CASE 122
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:364 == 0
			STR:(K++) =
			CFLAG:364 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:364 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:364 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:364 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:364 = 3
			ENDIF
			;それ以外
			IF (CFLAG:364 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:364 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;助手が気持ちよくて感じているセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;助手がこっちに来てと言うセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;助手に大丈夫と言っているセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;犯されて強がりを言うセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;うまくなってきたねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;123,足コキする
;-------------------------------------------------
	CASE 123
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:365 == 0
			STR:(K++) =
			CFLAG:365 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:365 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:365 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:365 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:365 = 3
			ENDIF
			;それ以外
			IF (CFLAG:365 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:365 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;124,ダブルフェラ
;-------------------------------------------------
	CASE 124
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:366 == 0
			STR:(K++) =
			CFLAG:366 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:366 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:366 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:366 = 3
			ENDIF
			;それ以外
			IF (CFLAG:366 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:366 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;一緒に気持ちよくしようというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私のほうが気持ちよくしてるというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;煽りに対してのセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;いつもより感じてるねというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;レズっぽいセリフに対するセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		;二人のフェラは気持ちいいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(6)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;125,ダブルパイズリ
;-------------------------------------------------
	CASE 125
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:367 == 0
			STR:(K++) =
			CFLAG:367 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:367 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:367 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:367 = 3
			ENDIF
			;それ以外
			IF (CFLAG:367 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:367 = 2
			ENDIF
		ENDIF
		
		;助手口上呼び出し関数
		;一緒に気持ちよくしようというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私のほうが気持ちよくしてるというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;煽りに対してのセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;いつもより感じてるねというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;お互いに乳首がこすれて気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		;二人のパイズリで気持ちよくなってセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(6)
		;パイズリで感じているの？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(7)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;126,乳の揉み合い
;-------------------------------------------------
	CASE 126
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:368 == 0
			STR:(K++) =
			CFLAG:368 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:368 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:368 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:368 = 3
			ENDIF
			;それ以外
			IF (CFLAG:368 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:368 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;127,自慰見せつけ
;-------------------------------------------------
	CASE 127
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:432 == 0
			STR:(K++) =
			CFLAG:432 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:432 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:432 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:432 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:432 = 3
			ENDIF
			;それ以外
			IF (CFLAG:432 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:432 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;128,秘貝開帳見せつけ
;-------------------------------------------------
	CASE 128
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:465 == 0
			STR:(K++) =
			CFLAG:465 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:465 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:465 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:465 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:465 = 3
			ENDIF
			;それ以外
			IF (CFLAG:465 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:465 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;129,自慰口淫見せつけ
;-------------------------------------------------
	CASE 129
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:466 == 0
			STR:(K++) =
			CFLAG:466 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:466 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:466 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:466 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:466 = 3
			ENDIF
			;それ以外
			IF (CFLAG:466 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:466 = 2
			ENDIF
		ENDIF
;=================================================
;ハードな調教コマンド
;=================================================
;-------------------------------------------------
;108,三角木馬
;-------------------------------------------------
	CASE 108
		CALL TRAIN_MESSAGE
		IF TEQUIP:41
			;初めて
			IF CFLAG:463 == 0
				STR:(K++) =
				CFLAG:463 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:463 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:463 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:463 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:463 = 3
				ENDIF
				;それ以外
				IF (CFLAG:463 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:463 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;140,イラマチオ
;-------------------------------------------------
	CASE 140
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:369 == 0
			STR:(K++) =
			CFLAG:369 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:369 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:369 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:369 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:369 = 3
			ENDIF
			;それ以外
			IF (CFLAG:369 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:369 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;141,フィストファック　服従、隷属のみ
;-------------------------------------------------
	CASE 141
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:370 == 0
			;処女
			IF TALENT:0
				STR:(K++) =
			ELSE
				STR:(K++) =
			ENDIF
			CFLAG:370 = 1
		;二回目以降
		ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:370 = 2
		ENDIF
;-------------------------------------------------
;142,アナルフィスト　服従、隷属のみ
;-------------------------------------------------
	CASE 142
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:371 == 0
			STR:(K++) =
			CFLAG:371 = 1
		;二回目以降
		ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:371 = 2
		ENDIF
;-------------------------------------------------
;143,両穴フィスト　服従、隷属のみ
;-------------------------------------------------
	CASE 143
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:372 == 0
			;処女
			IF TALENT:0
				STR:(K++) =
			ELSE
				STR:(K++) =
			ENDIF
			CFLAG:372 = 1
		;二回目以降
		ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:372 = 2
		ENDIF
;-------------------------------------------------
;144,放尿
;-------------------------------------------------
	CASE 144
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:373 == 0
			STR:(K++) =
			CFLAG:373 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:373 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:373 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:373 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:373 = 3
			ENDIF
			;それ以外
			IF (CFLAG:373 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:373 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;145,アナル奉仕強制
;-------------------------------------------------
	CASE 145
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:374 == 0
			STR:(K++) =
			CFLAG:374 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:374 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:374 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:374 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:374 = 3
			ENDIF
			;それ以外
			IF (CFLAG:374 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:374 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;146,浣腸器＋プラグ　解除すると方便
;-------------------------------------------------
	CASE 146
		CALL TRAIN_MESSAGE
		IF TEQUIP:21
			;初めて
			IF CFLAG:375 == 0
				STR:(K++) =
				CFLAG:375 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:375 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:375 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:375 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:375 = 3
				ENDIF
				;それ以外
				IF (CFLAG:375 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:375 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;147,空気浣腸　おならさせる
;-------------------------------------------------
	CASE 147
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:376 == 0
			STR:(K++) =
			CFLAG:376 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:376 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:376 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:376 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:376 = 3
			ENDIF
			;それ以外
			IF (CFLAG:376 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:376 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;148,拡張バルーン
;-------------------------------------------------
	CASE 148
		CALL TRAIN_MESSAGE
		IF TEQUIP:22
			;初めて
			IF CFLAG:377 == 0
				STR:(K++) =
				CFLAG:377 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:377 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:377 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:377 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:377 = 3
				ENDIF
				;それ以外
				IF (CFLAG:377 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:377 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;149,アナル電極　服従、隷属のみ
;-------------------------------------------------
	CASE 149
		CALL TRAIN_MESSAGE
		IF TEQUIP:23
			;初めて
			IF CFLAG:378 == 0
				STR:(K++) =
				CFLAG:378 = 1
			;二回目以降
			ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
				STR:(K++) =
				CFLAG:378 = 2
			ENDIF
		ELSE
		;解除
			IF CFLAG:378 == 1 || FLAG:7 == 2
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;150,乳房電極　服従、隷属のみ
;-------------------------------------------------
	CASE 150
		CALL TRAIN_MESSAGE
		IF TEQUIP:24
			;初めて
			IF CFLAG:379 == 0
				STR:(K++) =
				CFLAG:379 = 1
			;二回目以降
			ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
				STR:(K++) =
				CFLAG:379 = 2
			ENDIF
		ELSE
		;解除
			IF CFLAG:379 == 1 || FLAG:7 == 2
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;151,鰻風呂　服従、隷属のみ
;-------------------------------------------------
	CASE 151
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:380 == 0
			;処女
			IF TALENT:0
				STR:(K++) =
			ELSE
				STR:(K++) =
			ENDIF
			CFLAG:380 = 1
		;二回目以降
		ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:380 = 2
		ENDIF
;-------------------------------------------------
;152,蟲風呂　服従、隷属のみ
;-------------------------------------------------
	CASE 152
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:381 == 0
			STR:(K++) =
			CFLAG:381 = 1
		;二回目以降
		ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
			STR:(K++) =
			CFLAG:381 = 2
		ENDIF
;-------------------------------------------------
;154,妖精オナホ　小人体系のみ
;-------------------------------------------------
	CASE 154
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:467 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:467 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:467 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:467 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:467 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:467 = 3
			ENDIF
			;それ以外
			IF (CFLAG:467 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:467 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;155,玉キック
;-------------------------------------------------
	CASE 155
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:468 == 0
			STR:(K++) =
			CFLAG:468 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:468 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:468 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:468 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:468 = 3
			ENDIF
			;それ以外
			IF (CFLAG:468 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:468 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;156,飲尿
;-------------------------------------------------
	CASE 156
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:469 == 0
			STR:(K++) =
			CFLAG:469 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:469 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:469 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:469 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:469 = 3
			ENDIF
			;それ以外
			IF (CFLAG:469 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:469 = 2
			ENDIF
		ENDIF
;=================================================
;触手系コマンド
;=================================================
;-------------------------------------------------
;160,触手召喚
;-------------------------------------------------
	CASE 160
		CALL TRAIN_MESSAGE
		IF TEQUIP:25
			;初めて
			IF CFLAG:382 == 0
				STR:(K++) =
				CFLAG:382 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:382 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:382 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:382 = 3
				ENDIF
				;それ以外
				IF (CFLAG:382 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:382 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;161,触手挿入
;-------------------------------------------------
	CASE 161
		CALL TRAIN_MESSAGE
		IF TEQUIP:13
			;初めて
			IF CFLAG:383 == 0
				;処女
				IF TALENT:0
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) =
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) =
						ENDIF
					ENDIF
					;それ以外
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				;非処女
				ELSE
					IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
						;恋慕、親愛
						IF (TALENT:3 || TALENT:6)
							STR:(K++) = 
						;淫乱、娼婦
						ELSEIF (TALENT:4 || TALENT:7)
							STR:(K++) = 
						;服従、隷属
						ELSEIF (TALENT:5 || TALENT:8)
							STR:(K++) =
						ENDIF
						;陥落済み共通
						IF STR:0 == ""
							STR:(K++) =
						ENDIF
					ENDIF
					;それ以外
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				CFLAG:383 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:383 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:383 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:383 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:383 = 3
				ENDIF
				;それ以外
				IF (CFLAG:383 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:383 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;162,アナル触手
;-------------------------------------------------
	CASE 162
		CALL TRAIN_MESSAGE
		IF TEQUIP:14
			;初めて
			IF CFLAG:384 == 0
				STR:(K++) =
				CFLAG:384 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:384 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:384 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:384 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:384 = 3
				ENDIF
				;それ以外
				IF (CFLAG:384 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:384 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;163,触手クリ責め
;-------------------------------------------------
	CASE 163
		CALL TRAIN_MESSAGE
		IF TEQUIP:11
			;初めて
			IF CFLAG:385 == 0
				STR:(K++) =
				CFLAG:385 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:385 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:385 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:385 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:385 = 3
				ENDIF
				;それ以外
				IF (CFLAG:385 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:385 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;164,触手乳首責め
;-------------------------------------------------
	CASE 164
		CALL TRAIN_MESSAGE
		IF TEQUIP:16
			;初めて
			IF CFLAG:386 == 0
				STR:(K++) =
				CFLAG:386 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:386 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:386 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:386 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:386 = 3
				ENDIF
				;それ以外
				IF (CFLAG:386 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:386 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;165,触手搾乳
;-------------------------------------------------
	CASE 165
		CALL TRAIN_MESSAGE
		IF TEQUIP:17
			;初めて
			IF CFLAG:387 == 0
				STR:(K++) =
				CFLAG:387 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:387 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:387 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:387 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:387 = 3
				ENDIF
				;それ以外
				IF (CFLAG:387 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:387 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;166,触手緊縛
;-------------------------------------------------
	CASE 166
		CALL TRAIN_MESSAGE
		IF TEQUIP:19
			;初めて
			IF CFLAG:388 == 0
				STR:(K++) =
				CFLAG:388 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:388 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:388 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:388 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:388 = 3
				ENDIF
				;それ以外
				IF (CFLAG:388 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:388 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;167,触手浣腸　極太の触手が菊穴をふさぎ、体液を流し込んだ
;-------------------------------------------------
	CASE 167
		CALL TRAIN_MESSAGE
		IF TEQUIP:21
			;初めて
			IF CFLAG:389 == 0
				STR:(K++) =
				CFLAG:389 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:389 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:389 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:389 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:389 = 3
				ENDIF
				;それ以外
				IF (CFLAG:389 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:389 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;168,触手口辱
;-------------------------------------------------
	CASE 168
		CALL TRAIN_MESSAGE
		IF TEQUIP:36
			;初めて
			IF CFLAG:390 == 0
				STR:(K++) =
				CFLAG:390 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:390 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:390 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:390 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:390 = 3
				ENDIF
				;それ以外
				IF (CFLAG:390 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:390 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;169,触手ペニス責め
;-------------------------------------------------
	CASE 169
		CALL TRAIN_MESSAGE
		IF TEQUIP:12
			;初めて
			IF CFLAG:391 == 0
				STR:(K++) =
				CFLAG:391 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:391 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:391 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:391 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:391 = 3
				ENDIF
				;それ以外
				IF (CFLAG:391 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:391 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;170,触手挿入させる
;-------------------------------------------------
	CASE 170
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:13
			;初めて
			IF CFLAG:470 == 0
				STR:(K++) =
				CFLAG:470 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:470 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:470 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:470 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:470 = 3
				ENDIF
				;それ以外
				IF (CFLAG:470 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:470 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;171,アナル触手させる
;-------------------------------------------------
	CASE 171
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:14
			;初めて
			IF CFLAG:471 == 0
				STR:(K++) =
				CFLAG:471 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:471 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:471 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:471 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:471 = 3
				ENDIF
				;それ以外
				IF (CFLAG:471 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:471 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;172,触手クリ責めさせる
;-------------------------------------------------
	CASE 172
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:11
			;初めて
			IF CFLAG:472 == 0
				STR:(K++) =
				CFLAG:472 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:472 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:472 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:472 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:472 = 3
				ENDIF
				;それ以外
				IF (CFLAG:472 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:472 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;173,触手乳首責めさせる
;-------------------------------------------------
	CASE 173
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:16
			;初めて
			IF CFLAG:473 == 0
				STR:(K++) =
				CFLAG:473 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:473 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:473 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:473 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:473 = 3
				ENDIF
				;それ以外
				IF (CFLAG:473 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:473 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;174,触手搾乳させる
;-------------------------------------------------
	CASE 174
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:17
			;初めて
			IF CFLAG:474 == 0
				STR:(K++) =
				CFLAG:474 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:474 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:474 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:474 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:474 = 3
				ENDIF
				;それ以外
				IF (CFLAG:474 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:474 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;175,触手ペニス責めさせる
;-------------------------------------------------
	CASE 175
		CALL TRAIN_MESSAGE
		IF TEQUIP:PLAYER:12
			;初めて
			IF CFLAG:475 == 0
				STR:(K++) =
				CFLAG:475 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:475 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:475 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:475 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:475 = 3
				ENDIF
				;それ以外
				IF (CFLAG:475 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:475 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;=================================================
;特殊コマンド
;=================================================
;-------------------------------------------------
;180,ローション
;-------------------------------------------------
	CASE 180
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:392 == 0
			STR:(K++) =
			CFLAG:392 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:392 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:392 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:392 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:392 = 3
			ENDIF
			;それ以外
			IF (CFLAG:392 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:392 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;181,媚薬
;-------------------------------------------------
	CASE 181
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:393 == 0
			STR:(K++) =
			CFLAG:393 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:393 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:393 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:393 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:393 = 3
			ENDIF
			;それ以外
			IF (CFLAG:393 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:393 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;182,利尿剤
;-------------------------------------------------
	CASE 182
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:394 == 0
			STR:(K++) =
			CFLAG:394 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:394 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:394 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:394 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:394 = 3
			ENDIF
			;それ以外
			IF (CFLAG:394 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:394 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;183,ビデオカメラ
;-------------------------------------------------
	CASE 183
		CALL TRAIN_MESSAGE
		IF TEQUIP:28
			;初めて
			IF CFLAG:395 == 0
				STR:(K++) =
				CFLAG:395 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:395 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:395 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:395 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:395 = 3
				ENDIF
				;それ以外
				IF (CFLAG:395 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:395 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;184,野外プレイ
;-------------------------------------------------
	CASE 184
		CALL TRAIN_MESSAGE
		;TEQUIP:29でどこの場所選んでも同じ口上が出るように
		;変更･場所ごとで変えたければ==にして数字をいじる
		;廃ビル=1　森林=2　公園=4　川辺=8
		IF TEQUIP:29
			;初めて
			IF CFLAG:396 == 0
				STR:(K++) =
				CFLAG:396 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:396 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:396 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:396 = 3
				ENDIF
				;それ以外
				IF (CFLAG:396 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:396 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;185,助手を犯す
;-------------------------------------------------
	CASE 185
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:397 == 0
			STR:(K++) =
			CFLAG:397 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:397 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:397 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:397 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:397 = 3
			ENDIF
			;それ以外
			IF (CFLAG:397 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:397 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;犯されてとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私たち、とっても仲がいいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;うらやましいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;こっちが気になってるみたいだねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;186,羞恥プレイ
;-------------------------------------------------
	CASE 186
		CALL TRAIN_MESSAGE
		IF TEQUIP:30
			;初めて
			IF CFLAG:398 == 0
				STR:(K++) =
				CFLAG:398 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:398 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:398 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:398 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:398 = 3
				ENDIF
				;それ以外
				IF (CFLAG:398 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:398 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;187,お風呂場プレイ
;-------------------------------------------------
	CASE 187
		CALL TRAIN_MESSAGE
		IF TEQUIP:31
			;初めて
			IF CFLAG:399 == 0
				STR:(K++) =
				CFLAG:399 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:399 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:399 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:399 = 3
				ENDIF
				;それ以外
				IF (CFLAG:399 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:399 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;188,シャワー
;-------------------------------------------------
	CASE 188
		CALL TRAIN_MESSAGE
		IF TEQUIP:32
			;初めて
			IF CFLAG:400 == 0
				STR:(K++) =
				CFLAG:400 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:400 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) = 
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) = 
					ENDIF
					CFLAG:400 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:400 < 2 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) = 
					CFLAG:400 = 3
				ENDIF
				;それ以外
				IF (CFLAG:400 || FLAG:7 == 2) && STR:0 == ""
					STR:(K++) =
					CFLAG:400 = 2
				ENDIF
			ENDIF
		;解除
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)	
					STR:(K++) =
				;淫乱、娼婦
				ELSEIF (TALENT:4 || TALENT:7)	
					STR:(K++) =
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)	
					STR:(K++) =
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && STR:0 == ""
				STR:(K++) =
			ENDIF
			;それ以外
			IF STR:0 == ""
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;189,新妻プレイ　恋慕、親愛のみ
;-------------------------------------------------
	CASE 189
		CALL TRAIN_MESSAGE
		IF TEQUIP:33
			;初めて
			IF CFLAG:401 == 0
				STR:(K++) =
				CFLAG:401 = 1
			;二回目以降
			ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
				STR:(K++) =
				CFLAG:401 = 2
			ENDIF
		ELSE
		;解除
			IF CFLAG:401 == 1 || FLAG:7 == 2
				STR:(K++) =
			ENDIF
		ENDIF
;-------------------------------------------------
;190,写真撮影
;-------------------------------------------------
	CASE 190
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:402 == 0
			STR:(K++) =
			CFLAG:402 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:402 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:402 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:402 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:402 = 3
			ENDIF
			;それ以外
			IF (CFLAG:402 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:402 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;191,強精神薬
;-------------------------------------------------
	CASE 191
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:403 == 0
			STR:(K++) =
			CFLAG:403 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:403 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:403 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:403 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:403 = 3
			ENDIF
			;それ以外
			IF (CFLAG:403 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:403 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;192,コンドーム精飲（奴隷）
;-------------------------------------------------
	CASE 192
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:404 == 0
			STR:(K++) =
			CFLAG:404 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:404 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:404 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:404 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:404 = 3
			ENDIF
			;それ以外
			IF (CFLAG:404 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:404 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;193,コンドーム精飲（助手）
;-------------------------------------------------
	CASE 193
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:405 == 0
			STR:(K++) =
			CFLAG:405 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:405 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:405 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:405 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:405 = 3
			ENDIF
			;それ以外
			IF (CFLAG:405 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:405 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;精液とてもおいしいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;これはあたしのものよ！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;うらやましいでしょ？セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;先にいただくわセリフ（仲がいい）
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
 
;-------------------------------------------------
;194,ザーメンキス
;-------------------------------------------------
	CASE 194
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:406 == 0
			STR:(K++) =
			CFLAG:406 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:406 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:406 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:406 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:406 = 3
			ENDIF
			;それ以外
			IF (CFLAG:406 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:406 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;195,乳腺開発
;-------------------------------------------------
	CASE 195
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:431 == 0
			STR:(K++) =
			CFLAG:431 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:431 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:431 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:431 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:431 = 3
			ENDIF
			;それ以外
			IF (CFLAG:431 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:431 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;200,人を呼ぶ
;-------------------------------------------------
	CASE 200
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:433 == 0
			STR:(K++) =
			CFLAG:433 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:433 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:433 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:433 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:433 = 3
			ENDIF
			;それ以外
			IF (CFLAG:433 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:433 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;201,輪姦
;-------------------------------------------------
	CASE 201
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:434 == 0
			STR:(K++) =
			CFLAG:434 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:434 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:434 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:434 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:434 = 3
			ENDIF
			;それ以外
			IF (CFLAG:434 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:434 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;202,人を集めさせる
;-------------------------------------------------
	CASE 202
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:476 == 0
			STR:(K++) =
			CFLAG:476 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:476 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:476 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:476 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:476 = 3
			ENDIF
			;それ以外
			IF (CFLAG:476 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:476 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;203,遠隔調教
;-------------------------------------------------
	CASE 203
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:477 == 0
			STR:(K++) =
			CFLAG:477 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:477 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:477 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:477 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:477 = 3
			ENDIF
			;それ以外
			IF (CFLAG:477 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:477 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;204,飲精
;-------------------------------------------------
	CASE 204
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:478 == 0
			STR:(K++) =
			CFLAG:478 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:478 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:478 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:478 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:478 = 3
			ENDIF
			;それ以外
			IF (CFLAG:478 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:478 = 2
			ENDIF
		ENDIF
;=================================================
;500番台,派生コマンド
;=================================================
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
	CASE 500
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:407 == 0
			STR:(K++) =
			CFLAG:407 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:407 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:407 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:407 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:407 = 3
			ENDIF
			;それ以外
			IF (CFLAG:407 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:407 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;ナカがとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;どんどんイキなさい！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;とても気持ちよさそうセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;ここに入れてセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;501,シックスナイン
;-------------------------------------------------
	CASE 501
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:408 == 0
			STR:(K++) =
			CFLAG:408 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:408 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:408 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:408 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:408 = 3
			ENDIF
			;それ以外
			IF (CFLAG:408 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:408 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;502,岩清水
;-------------------------------------------------
	CASE 502
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:409 == 0
			STR:(K++) =
			CFLAG:409 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:409 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:409 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:409 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:409 = 3
			ENDIF
			;それ以外
			IF (CFLAG:409 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:409 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;503,Gスポット刺激
;-------------------------------------------------
	CASE 503
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:410 == 0
			STR:(K++) =
			CFLAG:410 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:410 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:410 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:410 = 3
			ENDIF
			;それ以外
			IF (CFLAG:410 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:410 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;504,乱れ牡丹
;-------------------------------------------------
	CASE 504
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:411 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:411 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:411 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:411 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:411 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:411 = 3
			ENDIF
			;それ以外
			IF (CFLAG:411 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:411 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;505,騎乗乳房自慰
;-------------------------------------------------
	CASE 505
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:412 == 0
			;処女
			IF TALENT:0
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			;非処女
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						STR:(K++) = 
					;淫乱、娼婦
					ELSEIF (TALENT:4 || TALENT:7)
						STR:(K++) = 
					;服従、隷属
					ELSEIF (TALENT:5 || TALENT:8)
						STR:(K++) =
					ENDIF
					;陥落済み共通
					IF STR:0 == ""
						STR:(K++) =
					ENDIF
				ENDIF
				;それ以外
				IF STR:0 == ""
					STR:(K++) =
				ENDIF
			ENDIF
			CFLAG:412 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:412 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:412 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:412 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:412 = 3
			ENDIF
			;それ以外
			IF (CFLAG:412 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:412 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;506,挿入局所責め
;-------------------------------------------------
	CASE 506
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:413 == 0
			STR:(K++) =
			CFLAG:413 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:413 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:413 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:413 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:413 = 3
			ENDIF
			;それ以外
			IF (CFLAG:413 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:413 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;507,挿入子宮口責め
;-------------------------------------------------
	CASE 507
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:414 == 0
			STR:(K++) =
			CFLAG:414 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:414 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:414 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:414 = 3
			ENDIF
			;それ以外
			IF (CFLAG:414 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:414 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;508,６９パイズリ
;-------------------------------------------------
	CASE 508
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:415 == 0
			STR:(K++) =
			CFLAG:415 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:415 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:415 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:415 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:415 = 3
			ENDIF
			;それ以外
			IF (CFLAG:415 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:415 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;509,搾乳
;-------------------------------------------------
	CASE 509
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:416 == 0
			STR:(K++) =
			CFLAG:416 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:416 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:416 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:416 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:416 = 3
			ENDIF
			;それ以外
			IF (CFLAG:416 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:416 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;510,ダブル素股
;-------------------------------------------------
	CASE 510
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:417 == 0
			STR:(K++) =
			CFLAG:417 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:417 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:417 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:417 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:417 = 3
			ENDIF
			;それ以外
			IF (CFLAG:417 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:417 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;一緒に気持ちよくしようというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;私のほうが気持ちよくしてるというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;煽りに対してのセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;いつもより感じてるねというセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;お互いに気持ちいいねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;511,二本フェラ
;-------------------------------------------------
	CASE 511
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:418 == 0
			STR:(K++) =
			CFLAG:418 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:418 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:418 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:418 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:418 = 3
			ENDIF
			;それ以外
			IF (CFLAG:418 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:418 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;とても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;自分もしっかり気持ちよくしてセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;マスターのペニスが当たって気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;二人分の精液飲ませてあげるセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		;うまくなってきたねセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(5)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;512,アナル騎乗乳房自慰
;-------------------------------------------------
	CASE 512
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:419 == 0
			STR:(K++) =
			CFLAG:419 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:419 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:419 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:419 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:419 = 3
			ENDIF
			;それ以外
			IF (CFLAG:419 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:419 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;513,口とヴァギナを犯す
;-------------------------------------------------
	CASE 513
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:439 == 0
			STR:(K++) =
			CFLAG:439 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:439 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:439 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:439 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:439 = 3
			ENDIF
			;それ以外
			IF (CFLAG:439 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:439 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;ナカがとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;口がとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;どんどんイキなさい！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;とても気持ちよさそうセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;514,口とアナルを犯す
;-------------------------------------------------
	CASE 514
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:440 == 0
			STR:(K++) =
			CFLAG:440 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:440 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:440 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:440 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:440 = 3
			ENDIF
			;それ以外
			IF (CFLAG:440 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:440 = 2
			ENDIF
		ENDIF
		;助手口上呼び出し関数
		;ナカがとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(1)
		;口がとても気持ちいいセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(2)
		;どんどんイキなさい！セリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(3)
		;とても気持ちよさそうセリフ
		;		CALL KOJO_MESSAGE_ASSIPAIR_COM(4)
		
		;セリフがなかった時の保険(一行)
		;SIF RESULT == 0
		;	STR:(K++) =
 
;-------------------------------------------------
;515,アナル乱れ牡丹
;-------------------------------------------------
	CASE 515
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:441 == 0
			STR:(K++) =
			CFLAG:441 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:441 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:441 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:441 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:441 = 3
			ENDIF
			;それ以外
			IF (CFLAG:441 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:441 = 2
			ENDIF
		ENDIF
;=================================================
;600番台,非戦闘員コマンド(特殊コマンド)
;=================================================
;-------------------------------------------------
;600,基礎訓練
;-------------------------------------------------
	CASE 600
		CALL TRAIN_MESSAGE
		;初めて
		IF CFLAG:481 == 0
			STR:(K++) =
			CFLAG:481 = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:481 < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					STR:(K++) = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					STR:(K++) = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					STR:(K++) = 
				ENDIF
				;陥落済み共通
				IF STR:0 == ""
					STR:(K++) = 
				ENDIF
				CFLAG:481 = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:481 < 2 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) = 
				CFLAG:481 = 3
			ENDIF
			;それ以外
			IF (CFLAG:481 || FLAG:7 == 2) && STR:0 == ""
				STR:(K++) =
				CFLAG:481 = 2
			ENDIF
		ENDIF
;-------------------------------------------------
;601,試召喚
;-------------------------------------------------
	CASE 601
		CALL TRAIN_MESSAGE
		IF TALENT:サマナー && !TALENT:非戦闘員
			STR:(K++) =
		ENDIF
;-------------------------------------------------
;602,影抜き
;-------------------------------------------------
	CASE 602
		CALL TRAIN_MESSAGE
		IF TALENT:ペルソナ使い && !TALENT:非戦闘員
			STR:(K++) =
;			;初期ペルソナ追加
			;ABL:初期ペルソナ =
			
;			;初期ペルソナ能力変更
			;ABL:ペルソナ1データ／スキル1 =
			
			;ABL:ペルソナ1データ／習得スキル1 =
			;ABL:ペルソナ1データ／習得LV1 =
 
		ENDIF
;-------------------------------------------------
;603,覚醒援助
;-------------------------------------------------
	CASE 603
		CALL TRAIN_MESSAGE
		IF TALENT:異能者 && !TALENT:非戦闘員
			STR:(K++) =
;			;能力変更
			;ABL:スキル1 = 
			
			;ABL:習得スキル1
			;ABL:習得LV1
 
		ENDIF
;-------------------------------------------------
;604,ウィルス照射
;-------------------------------------------------
	CASE 604
		CALL TRAIN_MESSAGE
		IF TALENT:喰奴 && !TALENT:非戦闘員
			STR:(K++) =
		ENDIF
;-------------------------------------------------
;605,霊媒実験
;-------------------------------------------------
	CASE 605
		CALL TRAIN_MESSAGE
		IF TALENT:デビルシフター && !TALENT:非戦闘員
			STR:(K++) =
;			;初期リンク悪魔
			;CFLAG:初期リンク悪魔
			
;			;初期リンク悪魔能力変更
			;ABL:初期変身悪魔スキル1
			
			;ABL:初期変身悪魔習得スキル1
			;ABL:初期変身悪魔習得LV1
			
			;ABL:初期リンク悪魔攻撃相性
			;ABL:初期リンク悪魔最低攻撃回数
			;ABL:初期リンク悪魔最大攻撃回数
			;ABL:初期リンク悪魔射程
			;ABL:初期リンク悪魔攻撃範囲
			;ABL:初期リンク悪魔追加効果
			;ABL:初期リンク悪魔追加効果相性
			;ABL:初期リンク悪魔追加効果命中率
			;ABL:初期リンク悪魔追加効果最大命中率
		ENDIF
 
;-------------------------------------------------
;606,改造手術
;-------------------------------------------------
	CASE 606
		CALL TRAIN_MESSAGE
		IF TALENT:機械の乙女 && !TALENT:非戦闘員
			STR:(K++) =
		ENDIF
 
;------------------------------------------------
;コマンド口上ここまで
;------------------------------------------------
	CASEELSE
		SIF FLAG:6 > 0
			CALL TRAIN_MESSAGE
ENDSELECT
 
FOR GYOU,0,501
	SIF STR:GYOU == ""
		BREAK
	PRINTFORM %STR:GYOU%
	IF W:(GYOU + 1)
		PRINTW
	ELSE
		PRINTL
	ENDIF
NEXT
SIF W:GYOU == 0
	PRINTW
 

