;-------------------------------------------------
;
;-------------------------------------------------
@INDIVIDUAL_EVENT_KX1, ARG, ARG:1 = 0, ARG:2 = -1
;ARG:0　キャラ番号
;ARG:1　関数種類　1,開放条件　2,タイトル　3,イベント
;ARG:2　イベント番号（0～63まで）

SELECTCASE ARG:2
CASE 0
	;解放条件
	IF ARG:1 == 1
		;RETURN 0
		;RETURN 1
	ELSEIF ARG:1 == 2
	;タイトル
		IF GETBIT(CFLAG:ARG:個別イベント開放,ARG:2)
			;タイトル
			RESULTS = 
		ELSE
			;未開放時のタイトル（解放条件を入れておく）
			RESULTS = 
		ENDIF
	;イベント
	ELSEIF ARG:1 == 3
		
		
		
		
		;好感度以外は後に上昇値が表示される
		;CFLAG:ARG:(MASTER + 2100) += (好感度)
		;TCVAR:ARG:獲得依存度 += 
		;TCVAR:ARG:獲得忠誠度 += 
		;CALL ADDS_EXP("○○経験", 1, ARG)
	ENDIF
ENDSELECT
RETURN 0