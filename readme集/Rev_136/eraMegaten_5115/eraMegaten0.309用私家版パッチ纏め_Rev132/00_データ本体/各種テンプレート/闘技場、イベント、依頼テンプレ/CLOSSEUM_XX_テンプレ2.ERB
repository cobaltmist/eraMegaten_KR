;闘技場フラグ:XX:1
;闘技場フラグ:XX:2
;闘技場フラグ:XX:3
;闘技場フラグ:XX:4

;====================
;条件
;====================
@PLAYABLE_COLOSSEUM_XX
IF 闘技場フラグ:XX:0 == 0
	RESULTS = (タイトル)
	RETURN 1
ELSEIF 闘技場フラグ:XX:0 == 1
	RESULTS = (タイトル)
	RETURN 1
ELSEIF 闘技場フラグ:XX:0 == 2
	RESULTS = (タイトル)
	RETURN 1
ENDIF

RETURN 0
;RETURN 0 でイベント未出現

RETURN 1
;RETURN 1 でイベント出現

;====================
;内容
;====================
@COLOSSEUM_XX
IF 闘技場フラグ:XX:0 == 0
	PRINTFORMW (イベント)
	PRINTFORMW (イベント)
	PRINTFORMW (イベント)
	
	FLAG:逃走不可フラグ = 2
	FLAG:会話不能フラグ = 2
	FLAG:先制不意打ちキャンセル = 2
	
	CALL SET_ENEMY,(ポジション),[[キャラ:○○]],(LV),(死体残留フラグ),(ボス強度　0だとザコ)
	
	;CFLAG:(CHARANUM - 1):ＨＰ補正 = 1000
	;CFLAG:(CHARANUM - 1):ＭＰ補正 = 1000
	;CFLAG:(CHARANUM - 1):行動回数 = 2
	
	;CSTR:(CHARANUM - 1):思考パターン = あれば
	
	;BASE:(CHARANUM - 1):剣撃 = 100
	
	;ABL:(CHARANUM - 1):スキル1 = 1002
	
	
	CALL SYNC_STATUS,(CHARANUM - 1)
	CALL HEALTH_CHARA,(CHARANUM - 1)
	
	
	CALL BATTLE_START
	FLAG:逃走不可フラグ = 0
	FLAG:会話不能フラグ = 0
	FLAG:先制不意打ちキャンセル = 0
	
	PRINTFORMW (イベント)
	PRINTFORMW (イベント)
	PRINTFORMW (イベント)
	
	闘技場フラグ:XX:0 = 1
ELSEIF 闘技場フラグ:XX:0 == 1
	闘技場フラグ:XX:0 = 2
ELSEIF 闘技場フラグ:XX:0 == 2
	闘技場フラグ:XX:0 = 3
ENDIF

FOR LOCAL,0,CHARANUM
	CALL HEALTH_CHARA,LOCAL
NEXT

CALL FLAG_RESET,XX,1
;↑これを入れると再び○○が出現！のメッセージを出すことができる

;====================
;思考パターン
;====================
;@SET_ACTION_あれば,ARG
;IF CFLAG:ARG:気合フラグ == 0
;	CALL CHECK_ACTIONABLE_AND_SET,ARG,511
;ELSEIF CFLAG:ARG:気合フラグ == 1
;	CFLAG:ARG:入力行動 = 3
;	CALL RANDOM_TARGET,ARG,3
;ELSE
;	CALL ランダム行動 , ARG
;ENDIF