;
;
;
;違いがあるのは初めてとそれ以外と一番下
;他は2.1と一緒です
;



;-------------------------------------------------
;6,胸揉み
;-------------------------------------------------
	CASE 6
		CALL TRAIN_MESSAGE
		IF TEQUIP:25 == 0
			;初めて
			IF CFLAG:306 == 0
				PRINTFORML 
				LOCALS:0 =;←追加しました
				CFLAG:306 = 1
			ELSE
				IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 3 || FLAG:7 == 2)
					;恋慕、親愛
					IF (TALENT:3 || TALENT:6)
						LOCALS:0 = 
					;淫乱、娼婦	
					ELSEIF (TALENT:4 || TALENT:7)
						LOCALS:0 = 
					;服従、隷属	
					ELSEIF (TALENT:5 || TALENT:8)
						LOCALS:0 = 
					ENDIF
					;陥落済み共通
					IF LOCALS:0 == ""
						LOCALS:0 = 
					ENDIF
					CFLAG:306 = 4
				ENDIF
				;屈服刻印Lv3
				IF MARK:2 == 3 && (CFLAG:306 < 2 || FLAG:7 == 2) && LOCALS:0 == ""
					LOCALS:0 = 
					CFLAG:306 = 3
				ENDIF
				;それ以外
				IF (CFLAG:306 || FLAG:7 == 2) && LOCALS:0 == ""
					PRINTFORML
					LOCALS:0 =;←追加しました
					CFLAG:306 = 2
				ENDIF
			ENDIF
		;触手あり
		ELSE
			GOTO SHOKUSHU_PLAY
		ENDIF
		
		;ここまでLOCALS:0内に文章がなければ共通口上に飛ぶ
		;初めて、それ以外でも入ってしまうのでLOCALS:0内に入れておく
		IF LOCALS:0 == ""
			K = 306
			GOTO 胸系共通
		ENDIF
		
;-------------------------------------------------
;胸系共通
;6胸揉み　7乳首責め　8胸愛撫
;-------------------------------------------------
	CASE 11111;←ありえない項目（どのコマンドでも入ることはできない）
		$胸系共通;←ワープのみ入ることができる
		;初めて
		IF CFLAG:K == 0
			PRINTFORML 
			CFLAG:K = 1
		ELSE
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:K < 3 || FLAG:7 == 2)
				;恋慕、親愛
				IF (TALENT:3 || TALENT:6)
					LOCALS:0 = 
				;淫乱、娼婦	
				ELSEIF (TALENT:4 || TALENT:7)
					LOCALS:0 = 
				;服従、隷属	
				ELSEIF (TALENT:5 || TALENT:8)
					LOCALS:0 = 
				ENDIF
				;陥落済み共通
				IF LOCALS:0 == ""
					LOCALS:0 = 
				ENDIF
				CFLAG:K = 4
			ENDIF
			;屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:K < 2 || FLAG:7 == 2) && LOCALS:0 == ""
				LOCALS:0 = 
				CFLAG:K = 3
			ENDIF
			;それ以外
			IF (CFLAG:K || FLAG:7 == 2) && LOCALS:0 == ""
				PRINTFORML
				CFLAG:K = 2
			ENDIF
		ENDIF