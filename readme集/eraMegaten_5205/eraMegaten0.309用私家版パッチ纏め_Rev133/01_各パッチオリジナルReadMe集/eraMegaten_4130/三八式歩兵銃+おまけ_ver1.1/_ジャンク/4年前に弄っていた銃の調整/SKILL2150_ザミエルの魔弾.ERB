
;スキル名
@SKILL_NAME_2150,ARG
RESULTS = ザミエルの魔弾

;　命中
@SKILL_HITRATE_2150, ARG
RETURN 100

;　威力
@SKILL_POWER_2150, ARG
RETURN 300

;　最低攻撃回数
@SKILL_MINATTACKNUMBER_2150,ARG
RETURN 1

;　最大攻撃回数
@SKILL_MAXATTACKNUMBER_2150,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2150, ARG
RETURN 1
;消費なし＝3

;　コストの量
@SKILL_COST_2150,ARG
RETURN 35

;相性 飛具・火炎・氷結・電撃・衝撃・神経・精神・呪殺のうち、もっともダメージが高くなるものになる
@SKILL_TYPE_2150,ARG
#LOCALSSIZE 9
#LOCALSIZE 4
SIF ARG == -1
	RETURN GET_TYPE_NUM("飛具")
SIF CFLAG:ARG:ターゲット > 16 || CFLAG:ARG:ターゲット < 1
	RETURN GET_TYPE_NUM("飛具")
LOCAL = POS(CFLAG:ARG:ターゲット)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("飛具")
LOCAL:2 = -1000
LOCALS = 飛具
LOCALS:1 = 飛具
LOCALS:2 = 火炎
LOCALS:3 = 氷結
LOCALS:4 = 電撃
LOCALS:5 = 衝撃
LOCALS:6 = 神経
LOCALS:7 = 精神
LOCALS:8 = 呪殺

FOR LOCAL:1, 1, 9
	;判定用に数値を仕舞う。反射は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"無効化回数") ? 0 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2150
RETURN 1
;どちらでもない

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2150
RETURN 1

;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_2150
RETURN 1

;　射程
@SKILL_RANGE_2150,ARG
CALLFORM 射程_{EQUIP:ARG:銃}
RETURN RESULT

;　範囲　1単体　21列　3全体　4　敵味方全体
@SKILL_SPHERE_2150,ARG
RETURN 1

;　 効果のタイプ　1攻撃、2回復、3状態変化、4特殊、5状態回復
@SKILL_EFECT_2150
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2150,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;継承タイプ
@SKILL_SUCCESSION_TYPE_2150
RETURN 0
;継承不可

@継承不能_2150
RETURN 1

;速度補正
@SKILL_SPEED_2150, ARG = -1
SIF ARG == -1
	RETURN 0
TRYCCALLFORM 速度補正_{EQUIP:ARG:銃}
	RETURN RESULT
CATCH
	RETURN 0
ENDCATCH

;　口上の表示とそのタイプ
@SKILL_MESSAGE_2150,ARG

;戦闘時以外に使用できるか
@SKILL_ACTIONABLE_FIELD_2150
RETURN 0

;スキル変化？用ランク
@SKILL_RANK_2150
RETURN 0
;変化不可

;テンプレート用、追加効果シリーズ
;追加効果ステータス
@SKILL_ADDTIONAL_STATE_2150,ARG
A = RAND:5
IF A == 0
	RETURN GET_STATE_NUM("PALYZE")
ELSEIF A == 1
	RETURN GET_STATE_NUM("PANIC")
ELSEIF A == 2
	RETURN GET_STATE_NUM("SLEEP")
ELSEIF A == 3
	RETURN GET_STATE_NUM("BIND")
ELSEIF A == 4
	RETURN GET_STATE_NUM("CLOSE")
ELSE
	RETURN GET_STATE_NUM("CHARM")
ENDIF

;追加効果相性
@SKILL_ADDTIONAL_TYPE_2150,ARG
RETURN RAND:2 + 8

;追加効果基本付与確率
@SKILL_ADDTIONAL_BASE_RATE_2150,ARG
CALLFORM 銃タイプ_{EQUIP:ARG:銃}
SELECTCASE RESULT
	CASE 1
		RETURN 35
	CASE 2
		RETURN 20
	CASE 3
		RETURN 7
	CASE 4
		RETURN 50
	CASEELSE
		RETURN 40
ENDSELECT
RETURN 0

;追加効果付与確率上限
@SKILL_ADDTIONAL_MAX_RATE_2150,ARG
CALLFORM 銃タイプ_{EQUIP:ARG:銃}
SELECTCASE RESULT
	CASE 1
		RETURN 90
	CASE 2
		RETURN 50
	CASE 3
		RETURN 20
	CASE 4
		RETURN 95
	CASEELSE
		RETURN 90
ENDSELECT
RETURN 0

;====================================================
;スキル及び行動の実行
;====================================================
@ACTION_2150,ARG,ARG:1
#LOCALSIZE 1

CALL SKILL_SPHERE_2150,ARG
CFLAG:POS(ARG:1):BS付着率強化 = MIN(4, CFLAG:POS(ARG:1):BS付着率強化 + 2)
IF RESULT == 1
	LOCAL = POS(ARG:1)
	CALL GUN_SINGLE,ARG,LOCAL,2150
ELSE
	RESULT = 0
	TRYCALLFORM GUN_RANDOMTARGET_{EQUIP:ARG:銃}
	IF RESULT == 1
		CALL GUN_RAND,ARG,ARG:1,2150
	ELSE
		CALL GUN_FIELD,ARG,ARG:1,2150
	ENDIF
	
ENDIF

FOR LOCAL,0,6
	CFLAG:ARG:BS付着率強化 = MIN(4, CFLAG:ARG:BS付着率強化 + 2)
NEXT
PRINTFORMW 　　TARGET:[{CPOS(ARG),2}] %CALLNAME:ARG%は呪われた！
;====================================================
;実行内容ここまで
;====================================================

;　 説明文の表示
@SKILL_EXPLAIN_2150
PRINTL 魔弾をもたらすモノの破壊と呪詛を込めて放つ技
PRINTL 敵一体を弱点相性で撃ち抜きランダムな状態異常を与える
;PRINTL 達人専用

;使用条件
@SKILL_SPECIAL_ACTIONABLE_2150,ARG
SIF EQUIP:ARG:銃 == 0
	RETURN 0
SIF !TALENT:ARG:達人
	RETURN 0
SIF CFLAG:ARG:PTフラグ == 0
	RETURN 1
RETURN 1
;とくにない

